# CLAUDE.md · Bugeon Journey

이 파일은 프로젝트 맥락 제공용이다. **반드시 차단해야 하는 행위는 `.claude/settings.json`의 hooks로 통제할 예정이다**(Phase 0에서 활성화; 현재 `settings.json` hooks는 비어 있음). 이 문서는 판단 기준과 작업 규율을 제공한다. 저장소 자체가 최종 정보원이다.

---

## 북극성 (North Star)

> 사진, 장소, 비용과 짧은 감정을 자동으로 연결하여, **여행 당시의 기억과 여행이 나에게 남긴 의미를 다시 찾아주는** 개인 여행기록 앱.

모든 기능·설계·코드는 한 가지 질문으로 판단한다: **"이게 그 목적을 더 잘 이루는가?"**

## 비타협 원칙 (목적의 일부이지, 목적과 맞바꿀 대상이 아니다)

1. **사용자의 기억을 잃지 않는다.** **내구성 로컬 커밋(Dexie entity+operation atomic commit) 이후 앱 원인 유실은 0**이다(브라우저 축출·사용자의 사이트데이터 삭제는 앱 통제 밖 → 백업·persist()·경고로 완화, `docs/SYNC_PROTOCOL.md`). 원본 사진은 사용자 기기에서 변경/삭제하지 않는다.
2. **사용자 기록과 AI 생성물을 섞지 않는다.** AI 출력은 사용자 필드가 아니라 **`ai_artifacts` 테이블(Phase 7)**에만 저장하고, AI 결과를 사용자 작성글처럼 표시하지 않는다.
3. **개인자료는 기본 비공개.** 여행·사진·GPS·동행인·비용·회고 모두 비공개가 기본. 승인 없는 공유/소셜 기능 없음.
4. **정직한 완료.** 자동 검증층이 통과한 것만 "통과"라 말한다. 시각·픽셀·실기기 상호작용은 "라이브 렌더 미실행 / 사용자 확인 권장"으로 분리 표기한다.
5. **복구 가능성 우선.** 위험한 작업은 사전검증·작업기록·실패복구·재시도·되돌리기·결과확인을 갖춘다.

## 절대 위반 금지 (§0)

- 사용자 원본 자료를 임의로 삭제/덮어쓰지 않는다.
- Supabase `service_role` 키/DB 비밀번호/관리자 JWT를 프론트엔드·번들·저장소·로그·리포트에 넣지 않는다. 클라이언트는 anon/publishable 키만.
- RLS 검증 없이 테이블을 배포하지 않는다.
- 원본 사진을 기본적으로 Supabase에 저장하지 않는다(절약 모드 기본).
- 사진 압축 **전에** 촬영시각·위치정보(EXIF)를 먼저 읽어 별도 저장한다.
- 전체 코드베이스를 이유 없이 재작성하지 않는다.
- 다른 에이전트가 작업 중인 파일을 동시에 수정하지 않는다.
- 자동검사를 통과하지 않은 변경을 완료로 표시하지 않는다.
- 사용자 승인 없이 소셜/공개 공유 기능을 추가하지 않는다.

---

## 기술 스택

TypeScript(strict) · Vite · Vanilla TS 컴포넌트 · Supabase(Auth/Postgres/Storage) · Dexie(IndexedDB) · MapLibre GL JS · GeoJSON · Web Worker+OffscreenCanvas(WebP) · Service Worker · Vitest · Playwright · GitHub Actions.

> 프레임워크는 MVP에서 추가하지 않는다. Vanilla 구조가 유지 불가능하다고 **실제 측정**된 경우에만 기술변경 제안서를 쓴다.

## 순간(Moment) 중심 도메인

`Trip → TripDay → Moment → Media / Place / Expense / Companion / Reflection`
여행을 긴 글 하나로 저장하지 않는다. 상세는 `docs/DATA_MODEL.md`.

---

## 작업 규율 (모델 이식 가능 — Claude Code와 Codex 공통)

품질은 모델이 아니라 규율에서 나온다.

### 0. 코딩 전·중 육하원칙 자문 (Pre-flight 5W1H)

**모든 코드 변경은 착수 전과 진행 중에 여섯 질문에 답할 수 있어야 한다.** 이건 의례가 아니라 결함 예방 장치다 — 답이 막히면 그 지점이 바로 위험이다. 이 프로젝트에선 각 질문이 구체적 관심사에 매핑된다:

- **왜(Why)** — 이게 북극성("기억과 의미를 다시 찾아준다")을 더 잘 이루는가? 아니면 범위 밖인가?
- **무엇을(What)** — 정확한 범위와 완료의 정의는? 무엇을 **안** 바꾸는가(원본 사진·타 에이전트 파일)?
- **어디서(Where)** — 진실원(SSOT)은 어느 파일·문서인가? 이 사실이 이미 어딘가 있어 손편집 중복을 만드는가?
- **언제(When)** — 생명주기·동기화 타이밍(생성/수정/tombstone/복원)·경합(이중 탭·재렌더 중)은? version/updatedAt/read-back 순서가 맞는가?
- **누가(Who)** — 이 데이터의 소유자·RLS 범위는? 어느 에이전트 역할이 검토해야 하는가? 소비 기기인가 생성 기기인가?
- **어떻게(How)** — 구현 경로와 **검증 경로**(어떤 게이트·라이브 렌더로 "현실 확인"할지)는? 실패 시 되돌리기는?

한 질문의 답이 다른 답과 모순되면 멈추고 재설계한다. (예: cascade 삭제에서 "무엇을 함께 지우나(자식 순간·사진)" ↔ "어떻게 되돌리나(정확히 그 자식만 복원)"가 맞물려야 한다.)

### 실행 규율

1. **행동 전 정독, 추측 금지.** SSOT 문서(`docs/`)를 먼저 로드한다.
2. **단일 진실원(SSOT).** 어떤 사실이 2곳 이상에 나오면 하나의 레지스트리를 두고, 파생물은 스크립트로 **재생성**한다. 손편집 중복 자체가 결함이다.
3. **의도가 아니라 현실로 검증.** 정적 게이트가 못 보는 것은 헤드리스 브라우저/실제 DOM 이벤트로 확인한다.
4. **게이트는 비공허하게.** 알려진 실패를 주입해 RED로 잡히는지 확인한 뒤에만 게이트를 신뢰한다. 셀렉터 불일치로 조용히 통과하지 않는지 검사한다.
5. **정직한 완료 보고.** 통과/스킵/실패를 구분해 보고한다. "UI 확인함"을 라이브 렌더 없이 말하지 않는다.
6. **결함 → 결함군 승격.** 버그를 단건으로 고치지 않고 근본형을 한 문장으로 추상화해 모든 형제 위치를 쓸고 게이트를 추가한다.

### 수평전개와 대칭성 (§7 — 헌법 조항)

> **한 곳에서 옳다고 판단한 것은 형제 전부에서 옳아야 한다. 그리고 그 대칭은 선언이 아니라 구조가 지킨다.**

이 조항은 관념이 아니라 **세 번의 같은 사고**에서 나왔다. 형태가 매번 같았다 — *형제들 중 하나만 조용히 빠졌고, 규율은 이미 문서에 적혀 있었다.*

- 비용(`expenses`)에만 `restore*`가 없어 **비용만 실행취소 불가**였다. 형제 도메인엔 다 있었다.
- cascade 삭제가 순간엔 op를 만들고 **사진·비용엔 안 만들어** 서버·R2에 고아가 남았다(M-0006). `docs/LESSONS.md` §3에 "도메인 대칭성"이 **이미 적혀 있었는데도** 그랬다.
- 진단 화면 여섯 개가 각자 렌더 코드를 가져, 정상을 이상과 같은 무게로 나열하는 **같은 결함이 세 곳에서 동형 반복**됐다(2026-07-26 사용자 실기기 지적).

그래서 규칙은 세 층이다. **한 층만으로는 지켜지지 않는다는 것이 관측된 사실이다.**

1. **조항** — 여기 이 문장. 판단 기준을 준다. 그러나 조항만 있으면 위 세 사고가 다시 난다(이미 났다).
2. **구조적 강제** — 규칙을 **한 곳에만 구현**하고 형제들이 그것을 *통과할 수밖에 없게* 만든다. 같은 규율을 두 곳에 손으로 구현한 순간 드리프트는 시간 문제다. 예: 진단 도구는 `Verdict`(데이터)만 만들고 그리는 일은 `renderTool()` 한 곳이 한다 — 자기 렌더 코드를 갖는 선택지가 없다. 예: `TripChildren` 번들처럼 **누락이 컴파일 오류가 되게** 타입을 설계한다.
3. **기계 검사** — 이탈을 게이트가 잡는다(`check-domain-symmetry`, `check-verdict-symmetry`). 게이트는 §4에 따라 **알려진 실패를 주입해 RED를 확인한 뒤에만** 신뢰한다.

**적용 절차.** 어떤 개선을 하나의 대상에 적용했다면, 착수 전 5W1H의 "어디서(Where)"에 다음을 반드시 답한다:

- **형제 목록을 적는다.** 이 규칙이 걸려야 할 대상 전부(도메인 4종? 화면 6개? 도구 N개?). 손으로 세지 말고 등록부/디렉터리에서 뽑는다.
- **전부에 적용하거나, 적용하지 않는 대상마다 이유를 코드에 남긴다.** 이유 없는 제외는 결함이다(`NO_OP_REQUIRED` 맵이 그 예 — 비면 규율이 죽는다).
- **다음 형제가 자동으로 따라오는가**를 묻는다. "다음 사람이 이 규칙을 몰라도 지켜지는가?" 아니라면 2층(구조)이 빠진 것이다.

**반대 방향도 같은 조항이다 — 새로 만든 것은 형제의 규율을 물려받지 않는다.** 위 세 질문은 *"내가 만든 규칙이 형제 전부에 걸리는가"*를 묻는다. 그런데 사고는 **반대 방향**으로도 난다: 새 대상(새 큐 op·새 테이블·새 화면·새 지표)을 만들면서 **형제들이 이미 지키고 있는 규율 목록을 훑지 않는 것**이다. 새것은 그것이 *푸는 문제*로 태어나지, 형제들의 계약을 알고 태어나지 않는다.

> M-0033: `unpurge` op을 만들며 *"엔티티와 op은 한 트랜잭션"*(비타협 원칙 #1의 **atomic** commit)을 따라가지 않았다. 그 규율은 헌법에도 스킬 문서에도 **버젓이 적혀 있었고**, 기존 op들은 전부 지키고 있었다. 새로 만든 것 하나만 바깥에서 태어났다. 그리고 그 결함은 **고친 그 커밋에서** 났다 — 수정 직후가 가장 위험하다.

그래서 형제 목록은 **두 번 쓴다**: ① 내 규칙이 걸릴 대상으로, ② **형제가 나에게 걸 규칙의 출처로.** `npm run brief`가 뽑아 주는 목록을 열어 계약을 한 줄씩 대조한다.

**의도적 비대칭에는 이유를 코드에 적는다 — 안 적으면 다음 사람이 '고친다'.** §7은 대칭을 요구하므로, 대칭이 아닌 자리는 **결함처럼 보인다.** 이유가 없으면 다음 사람(사람이든 AI든)이 선의로 통일하고 그 순간 무언가 조용히 깨진다.

> 2026-07-27: ZIP 백업 사진 이름은 사진 id **8자**, R2 객체 키는 **32자 전체**다. 통일하고 싶은 모양이지만 그러면 안 된다 — ZIP은 복원이 `trip.json` 경로로 되읽으므로 짧아도 되고, R2는 **영구삭제하면 대조할 행이 사라져 파일 이름이 유일한 근거**가 되기 때문이다(M-0029). 그래서 두 함수가 나란히 있는 파일에 *왜 다른지*를 적었다. 적지 않았다면 "중복 같은데?" 한 줄로 사라졌을 것이다.

**사용자 대면 대칭.** 코드 대칭만이 아니라 **화면 대칭**도 같은 조항의 대상이다. 같은 성격의 화면은 같은 자리에 같은 어휘로 말해야 한다 — 판정 문장은 늘 맨 위, 주행동 버튼은 늘 같은 좌표, 정상은 늘 침묵, 색은 늘 글리프와 함께. 사용자는 화면마다 규칙을 새로 배우지 않아야 한다.

### 진단 도구의 제1 규율 (§8)

> **진단 도구는 관측이 아니라 판정을 한다.** "정상은 어떤 상태이고 지금 무엇이 다른가"를 말하지 못하면 그건 도구가 아니라 원자료 덤프다(2026-07-26 사용자 지적).

- **침묵이 정상이다.** 기대값과 일치하는 항목은 화면에서 사라지고 한 줄로만 집계된다. 남아 있는 것이 곧 문제다.
- **지표엔 기대값이 필수다.** `지금 3건 / 정상 0건`. 기준을 쓸 수 없는 값은 지표가 아니라 맥락이므로 접힌 곳으로 내린다.
- **모르는 것은 '확인 불가'다.** 정상으로도 문제로도 반올림하지 않는다(비타협 원칙 #4). 미검사를 통과로 적는 것은 거짓말이다.
- **고쳤다고 말하지 말고 다시 읽어라.** 수리 액션 후에는 재판정한다(데이터 안전의 read-back과 같은 규율).

상세 작업 지침은 `.claude/skills/diagnostics-dev/SKILL.md`.

### 착수 절차 (§9 — 코딩 전에 반드시)

> **적어놓고 어기는 이유는 읽지 않기 때문이다.** (사용자 지적 2026-07-26)

맞다. 그러나 **읽기만으로는 부족하다는 것도 관측된 사실이다** — M-0012에서 나는 §7을 *직접 쓰고 같은 커밋에서 어겼다*. 읽음은 그때 최대치였다. 실패한 것은 읽기가 아니라 **적용**이었다: 산문에 동의하고 넘어갔지, 그 절차가 요구하는 *산출물*을 만들지 않았다. 그래서 절차는 셋이다.

```
npm run brief            # 또는: npm run brief <고칠 파일들>
```

1. **정독** — 브리핑이 알려준 스킬 문서를 **먼저 읽는다.** 어느 문서인지는 기억이 아니라 라우팅 표(`scripts/brief.mjs`의 `SKILL_ROUTES`)가 정한다. `check-skill-routing`이 "모든 코드 영역이 읽을 문서를 갖는가"를 강제한다.
2. **구멍 메우기** — 정독 중 **문서와 코드가 어긋나거나 규칙이 빠져 있으면 그것부터 고친다.** 이게 이 단계의 진짜 일이다. 실제 사례: 오버레이 잘림 사고 때 `ui-responsive-dev`에 `vh`/`dvh`·오버레이 스크롤 규칙이 **아예 없었다** — 읽었어도 못 막았을 구멍이었다. 낡은 전제가 화석으로 남은 주석도 같은 부류다(M-0006).
3. **산출물 만들기** — 브리핑이 뽑아준 **형제 목록**을 보고 §7의 세 질문에 답한다: 전부에 적용하는가 / 제외한다면 이유를 코드에 남겼는가 / 다음 형제가 자동으로 따라오는가.
4. **세계를 본다(상태 의존 작업이면 필수)** — 데이터·서버 상태에 따라 답이 달라지는 일이면 **실서버 스냅샷을 한 번 뜬다.** 규칙 문서는 *무엇이어야 하는가*를 말하지 *지금 무엇인가*를 말하지 않는다.
   - 2026-07-26 M-0019: 사진 파일 대조를 만들며 "저장소는 하나"라고 가정했다. 실제로는 **13건 중 9건이 다른 저장소**에 있었고, 그 사실은 `docs/HANDOFF.md`에 **이미 적혀 있었다.** 스냅샷 한 번이 유닛 21건보다 먼저 잡았다.
   - 2026-07-26 M-0023: 영구삭제 방식을 두 번 바꾸면서 **옛 방식으로 이미 지워진 것들**에게는 아무 일도 일어나지 않는다는 걸 아무도 묻지 않았다.
   - 물을 것: **지금 데이터에 옛 방식으로 만들어진 것이 있는가? 있으면 누가 데려오는가?**

브리핑은 그 영역에서 **과거에 낸 실수**(`docs/records/coding-mistakes.md`)도 함께 띄운다 — 같은 자리에서 두 번 넘어지지 않게.

### 게이트가 못 잡는 것 (§10 — 2026-07-26에 6건을 겪고 세움)

> **자동 게이트는 만능이 아니다. 무엇을 못 잡는지 알고, 그 자리에 다른 층을 놓는다.**

하루에 결함 6건을 잡았는데 **정적 게이트가 잡은 것은 0건**이었다. 전부 **사용자의 실기기 화면**에서 나왔다. 사후에 분류해 보니 셋으로 갈렸고, 각각 처방이 다르다.

| 부류 | 왜 정적 게이트가 못 잡나 | 놓아야 할 층 |
|---|---|---|
| **① 계약 결함** — 새 테이블/연산의 권한 누락(M-0020·M-0024) | (잡을 수 있다) | **정적 게이트.** 코드가 하는 일과 서버가 허락하는 일을 대조 |
| **② 상태 의존** — 저장소 혼재(M-0019), 옛 방식 잔재(M-0023) | **코드에 답이 없다.** 지금 데이터가 어떤 모양인지에 달렸다 | **진단 도구가 곧 게이트다**(런타임). + §9 4단계 실서버 스냅샷 |
| **③ 전달 결함** — 판정 문장이 엉뚱한 곳을 가리킴(M-0021), 확인해놓고 화면에 안 알림(M-0022) | **자료구조는 옳다.** 틀린 것은 사용자에게 가는 것뿐 | 사용자 문장을 **순수 함수로 분리**해 유닛으로 · `check-report-fields` · **실기기 확인** |

**②의 핵심**: 정적 게이트는 *빌드 시점*이고 진단 도구는 *런타임*이다. 과거 데이터가 어긋날 수 있는 변경을 하면 **그 어긋남을 진단 지표로 만든다** — 「지웠는데 서버에 남은 항목」·「파일이 없는 사진 기록」·「옛 저장소에 남은 사진」이 전부 그 층이다. 지표가 곧 그 결함군의 게이트다.

**③의 핵심**: 검사의 시선이 **자료구조가 아니라 사용자**에 있어야 한다. M-0022에서 유닛 15건이 전부 통과했다 — 숫자는 다 맞았고, 화면에 무엇이 나가는지 검사한 것이 하나도 없었다. 사용자에게 보이는 문장을 만드는 로직은 **순수 함수로 뽑아라.** 그래야 검사할 수 있다.

**그래도 남는 것**: "이 문장이 사용자에게 이해되는가", "이 화면이 무섭게 보이는가"는 기계가 판정하지 못한다. 그 자리는 **실기기 확인**이 유일한 층이고, 그래서 완료의 정의에 들어 있다. 오늘 6/6이 그 층에서 나왔다는 사실이 그 증거다 — **이 앱에서 가장 강력한 검출기는 사용자의 실기기 화면이다.** 그것을 대체하려 들지 말고, 거기까지 가는 길을 짧게 만든다(진단 도구가 하는 일이 그것이다).

### 검사하는 것도 결함을 갖는다 (§11 — 같은 날 게이트 세 개가 서로 다르게 틀렸다)

> **게이트를 만들었다고 그 자리가 지켜지는 것이 아니다. 게이트도 검사받아야 한다.**

§4는 "알려진 실패를 주입해 RED로 잡히는지 확인한 뒤에만 게이트를 신뢰한다"고 이미 말한다.
그런데 2026-07-26에 그 규칙을 지키고도 세 번 뚫렸다. **주입을 *언제* 하는가가 빠져 있었다.**

1. **넓힐 때 다시 공허해진다.** `check-report-fields`에 대상을 하나 추가했더니 그 대상에
   대해서는 공허했다(중첩 필드를 안 봤다). **등록부에 한 줄 추가하는 것은 새 게이트를 만드는
   것과 같다** — 그 대상으로 주입을 다시 하라.
2. **셀프테스트가 옛 전제를 못박는다.** 위 잘못된 가정이 셀프테스트에 *정상 케이스*로 박혀
   있었다. 전제가 바뀌면 **케이스를 먼저 뒤집는다.** 통과시키려고 로직을 되돌리지 마라.
   (고치자마자 밀려 있던 진짜 결함이 하나 더 나왔다 — 그게 공허했다는 증거다.)
3. **오탐도 결함이다.** 정규식이 `typeof body.op === 'string'`을 op으로 셌다. 오탐은
   "빡빡한 게이트"가 아니라 **틀린 게이트**다. 사람이 무시하기 시작하면 그 게이트는 죽는다.

**그리고 래칫에 걸리면 우회하지 말고 덜어내라.** `check-fn-size`가 함수 증가를 막았을 때
주석을 줄이는 대신 헤더 조립을 함수로 뽑았고, 결과가 더 나았다 — **게이트가 설계를 밀어준다.**

### 사람에게 무엇을 대신 시키고 있는가 (§12 — 사용자가 스크린샷 수백 장을 찍은 날)

> *"하나하나 스크린샷 찍다보니 수백장은 찍은 거 같아. 미쳐버릴 지경이거든."* (2026-07-26)

그 수백 장은 **앱이 자기 상태를 말하지 못해 사람이 대신 옮긴 것**이다. 원인은 셋이었고 전부
"앱이 아는데 말하지 않았다"였다: 요약에 지표 숫자가 없었고, 판정 문장이 한정을 생략했고,
앱이 자기 시야의 경계를 밝히지 않았다.

기능을 하나 더 만들기 전에 물어라 — **"지금 이 앱은 사람에게 무엇을 대신 시키고 있는가?"**
답이 *화면을 사진으로 찍어 나르는 일*이면 그걸 먼저 없앤다.

상세 전말과 근본형 6가지: `docs/records/2026-07-26-STORAGE-DELETE-POSTMORTEM.md`
(`npm run brief`가 해당 영역 착수 시 자동으로 띄운다).

### 데이터 안전 불변식 (최고 위험 표면 — 상세 `docs/SYNC_PROTOCOL.md`)

- 안정적 `id` + `created_at` + `updated_at`; 동일 id 충돌은 LWW(최신 `updated_at` 우선).
- **하드 삭제 없음** — `deleted_at` tombstone. 오래된 활성 행이 tombstone을 이기지 못한다.
  - **유일한 예외: 영구삭제(휴지통 비우기, ADR-0030).** 2단계 확인을 거친 명시적 의도이므로 서버 행을 **실제로 지운다**. 이 조항의 목적은 "좀비 차단"이었는데, 그건 서버 원장(`journey.purged_ids`) + BEFORE INSERT 트리거가 **더 강하게** 달성한다. 남는 것은 id 한 줄뿐이고 자료는 남지 않는다. 일반 삭제(휴지통행)는 **여전히 tombstone 전용**이다.
- **빈-클라우드 가드**: 서버가 빈 배열을 줘도 로컬을 덮어쓰지 않는다.
- **정확한 read-back으로 확인**: HTTP 200/성공 토스트가 아니라 같은 레코드를 되읽어 확인한 뒤에만 완료 처리.
- 파이프라인 생성 행(`source=pipeline`)은 소비 기기에서 읽기전용. 로컬에 없다고 tombstone 하지 않는다(오래된 기기가 신선한 클라우드 데이터를 지운다).
- 카메라/EXIF 등 기계 파생 값은 `needs_review`로 시작하고, 재생성이 검토·tombstone된 행을 덮어쓰지 않는다.

## 완료의 정의 (Definition of Done)

구현 완료 → 단위검사 통과 → 통합검사 통과 → 보안검사 통과 → 회귀검사 통과 → 문서 갱신 → 사용자 흐름 확인 → Acceptance 승인. **일부가 아니라 전부** 통과해야 한다.

## Git / 협업

- 지정 브랜치에서 개발한다. 기본 브랜치에 직접 커밋하지 않는다.
- commit: `feat|fix|security|refactor|test|docs|build|chore(scope): 요약`.
- 각 작업 종료 시 `docs/HANDOFF.md`에 인계 기록. 중요한 결정은 `docs/DECISIONS.md`.
- 동시 수정 금지 파일: 동일 DB migration, media pipeline 핵심, 동기화 상태머신, 데이터 형식 정의, Supabase 정책.

## 문서 지도 (SSOT)

`docs/PROJECT_SPEC.md`(요구사항·최상위) · `ARCHITECTURE.md` · `DATA_MODEL.md` · `SECURITY.md` · `PRIVACY.md` · `SYNC_PROTOCOL.md` · `MEDIA_PIPELINE.md` · `DEPLOYMENT.md`(배포 계약) · `AGENT_REGISTRY.md` · `LESSONS.md` · `ROADMAP.md` · `TEST_PLAN.md` · `DECISIONS.md` · `ASSUMPTIONS.md` · `HANDOFF.md` · `CHANGELOG.md` · `REPOSITORY_AUDIT.md` · `CONFLICT_REPORT.md` · `ACTIVE_TASKS.md`. v0.2 원본은 `docs/reference/v0.2/`.
충돌하면 공유 문서(SPEC)가 이긴다. 특정 AI 도구 대화가 아니라 이 문서들이 기준이다.

## 에이전트 운영

139개 논리 역할(`docs/AGENT_REGISTRY.md`)을 통합·디자인 세트로 `.claude/agents/`에 구현(개수는 손으로 세지 않고 `src/app/registry.gen.ts` 자동 집계·`check-registry-gen`이 드리프트 차단). 동시에 다 돌리지 않고 Orchestrator가 필요한 역할만 호출한다. 규칙은 `AGENTS.md`.
