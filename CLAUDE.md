# CLAUDE.md · Bugeon Journey

> **이 파일은 어댑터다.** 아래 마커 사이는 `docs/CONSTITUTION.md`에서 **자동으로 심긴다** —
> Codex가 읽는 `AGENTS.md`와 **글자 단위로 같은 내용**이다(두 AI가 다르게 알면 그 자체가 결함).
>
> 🔴 **여기를 직접 고치지 마라.** `docs/CONSTITUTION.md`를 고치고
> `node scripts/gen-adapters.mjs`를 돌린다. `check-adapter-parity`가 드리프트를 RED로 잡는다.

## Claude Code 전용 메모

- 착수 절차와 검증 명령은 아래 공통 계약(**§9**·**검증 명령**)에 있다 — 여기 다시 적지 않는다.
- 반드시 차단해야 하는 행위는 `.claude/settings.json`의 hooks로 통제한다(Phase 0에서 활성화).
- 에이전트 정의는 `.claude/agents/`, 스킬 문서는 `.claude/skills/`.

<!--CONSTITUTION:BEGIN — 생성됨. 고치려면 docs/CONSTITUTION.md를 고치고 node scripts/gen-adapters.mjs-->

# 헌법 · Bugeon Journey (공통 계약)

> 🔴 **이 파일이 정본이다.** `CLAUDE.md`(Claude Code)와 `AGENTS.md`(Codex)는 이 내용을
> **자동으로 심어 받는 어댑터**다. 두 파일의 마커 사이는 `node scripts/gen-adapters.mjs`가
> 생성하고 `check-adapter-parity`가 「커밋본 == 재생성본」을 강제한다.
>
> **왜 복사가 아니라 생성인가**: 도구 제약상 각 AI는 자기 파일만 읽으므로 본문이 양쪽에
> 있어야 한다. 그런데 §1이 말하듯 *"손편집 중복 자체가 결함"*이다 — 실제로 2026-07-29에
> 비교해 보니 두 문서가 **네 군데에서 갈라져 있었다**(아래 「합치면서 정리한 상충」).
> 불가피한 중복은 손으로 하지 말고 **기계화한다**(`gen-registry`와 같은 패턴).
>
> **고칠 때는 이 파일만 고친다.** 어댑터를 직접 고치면 게이트가 RED로 잡는다.

## 합치면서 정리한 상충 (2026-07-29)

두 어댑터를 실제로 대조해 나온 것들이다. **협업하는 두 AI가 다르게 알고 있었다.**

| # | 무엇이 달랐나 | 어떻게 정리했나 |
|---|---|---|
| 1 | 🔴 **「완료의 정의」가 서로를 포함하지 않았다.** CLAUDE엔 §13(화면·버튼)과 Acceptance가 있고 **배포 확인이 없었고**, AGENTS엔 배포 그린·CHANGELOG가 있고 **§13이 없었다** | **둘을 합쳤다.** Codex가 화면을 안 보고 「완료」라 하거나 Claude가 배포 확인 없이 「완료」라 하는 길을 둘 다 막는다 — **M-0046이 정확히 그 형태였다**(배포는 됐고 화면은 안 봤다) |
| 2 | 브랜치 규칙이 달랐다. AGENTS의 `feature/TASK-번호-설명`은 **실제와 다르다**(실제는 `claude/travel-log-app-r2xd5f`) | 실제 관행(`claude/*`·`codex/*` 네임스페이스)으로 통일하고, TASK 번호 형식은 선택으로 내렸다 |
| 3 | 실행 규율이 6개 vs 5개 — AGENTS에 **「게이트는 비공허하게」(§4)**와 **「결함군 승격」(§6)**이 없었다 | 6개로 통일. 이 저장소에서 가장 많이 인용되는 두 규율이 한쪽에만 있었다 |
| 4 | 🔴 **헌법 조항 §7~§13을 Claude만 알고 있었다**(대칭성·진단·착수절차·게이트 한계·검사의 결함·§12·§13). 반대로 거버넌스(출력계약·활성 task 소유권·보증 매트릭스)는 Codex만 알았다 | **양쪽이 전부 안다.** 이 파일 하나가 둘 다에게 심긴다 |

---

## 북극성 (North Star)


> 사진·영상, 장소, 비용과 짧은 감정을 자동으로 연결하여, **여행 당시의 기억과 여행이 나에게 남긴 의미를 다시 찾아주는** 개인 여행기록 앱.

모든 기능·설계·코드는 한 가지 질문으로 판단한다: **"이게 그 목적을 더 잘 이루는가?"**

## 비타협 원칙 (목적의 일부이지, 목적과 맞바꿀 대상이 아니다)


1. **사용자의 기억을 잃지 않는다.** **내구성 로컬 커밋(Dexie entity+operation atomic commit) 이후 앱 원인 유실은 0**이다(브라우저 축출·사용자의 사이트데이터 삭제는 앱 통제 밖 → 백업·persist()·경고로 완화, `docs/SYNC_PROTOCOL.md`). 외부 원본 사진·영상은 사용자 기기에서 변경/삭제하지 않는다.
2. **사용자 기록과 AI 생성물을 섞지 않는다.** AI 출력은 사용자 필드가 아니라 **`ai_artifacts` 테이블(Phase 7)**에만 저장하고, AI 결과를 사용자 작성글처럼 표시하지 않는다.
3. **개인자료는 기본 비공개.** 여행·사진·영상·GPS·동행인·비용·회고 모두 비공개가 기본. 승인 없는 공유/소셜 기능 없음.
4. **정직한 완료.** 자동 검증층이 통과한 것만 "통과"라 말한다. 시각·픽셀·실기기 상호작용은 "라이브 렌더 미실행 / 사용자 확인 권장"으로 분리 표기한다.
5. **복구 가능성 우선.** 위험한 작업은 사전검증·작업기록·실패복구·재시도·되돌리기·결과확인을 갖춘다.

## 절대 위반 금지 (§0)


- 사용자 원본 자료를 임의로 삭제/덮어쓰지 않는다.
- Supabase `service_role` 키/DB 비밀번호/관리자 JWT를 프론트엔드·번들·저장소·로그·리포트에 넣지 않는다. 클라이언트는 anon/publishable 키만.
- RLS 검증 없이 테이블을 배포하지 않는다.
- 외부 원본 사진·영상은 기본적으로 Supabase/R2에 저장하지 않는다. 앱 사용에 필요한 파생본만 올린다(절약 모드 기본).
- 사진 압축 **전에** 촬영시각·위치정보(EXIF)를 먼저 읽어 별도 저장한다.
- 전체 코드베이스를 이유 없이 재작성하지 않는다.
- 다른 에이전트가 작업 중인 파일을 동시에 수정하지 않는다.
- 자동검사를 통과하지 않은 변경을 완료로 표시하지 않는다.
- 사용자 승인 없이 소셜/공개 공유 기능을 추가하지 않는다.

---

## 기술 스택


TypeScript(strict) · Vite · Vanilla TS 컴포넌트 · Supabase(Auth/Postgres/Storage) · Dexie(IndexedDB) · MapLibre GL JS · GeoJSON · Web Worker+OffscreenCanvas(WebP) · Mediabunny+WebCodecs · Service Worker · Vitest · Playwright · GitHub Actions.

> 프레임워크는 MVP에서 추가하지 않는다. Vanilla 구조가 유지 불가능하다고 **실제 측정**된 경우에만 기술변경 제안서를 쓴다.

## 순간(Moment) 중심 도메인


`Trip → TripDay → Moment → Media / Video / Place / Expense / Companion / Reflection`
여행을 긴 글 하나로 저장하지 않는다. 상세는 `docs/DATA_MODEL.md`.

---

## 권위 순서 (문서 충돌 해소)


실행 코드/migration/테스트/검증 인프라(관찰된 현실) > Foundation(가치) > Contracts(데이터·보안·배선) > Procedures > Playbooks > Records(역사적) > 생성된 맵(그 생성기가 권위).
현실이 계약과 충돌하면 조용히 고르지 말고 **불일치 기록 → 사용자 데이터 보호 → 코드 수정 또는 계약을 게이트와 함께 개정**.
`docs/records/coding-mistakes.md`는 이 권위 순서의 **Records 계층 실수 원장**으로, 반복 실수를 기록하고 게이트로 기계화하는 근거다(현재 계약을 무효화하지는 않음).

## 작업 규율 (모델 이식 가능 — Claude Code와 Codex 공통)


품질은 모델이 아니라 규율에서 나온다.

### 0. 코딩 전·중 육하원칙 자문 (Pre-flight 5W1H)

**모든 코드 변경은 착수 전과 진행 중에 여섯 질문에 답할 수 있어야 한다.** 이건 의례가 아니라 결함 예방 장치다 — 답이 막히면 그 지점이 바로 위험이다. 이 프로젝트에선 각 질문이 구체적 관심사에 매핑된다:

- **왜(Why)** — 이게 북극성("기억과 의미를 다시 찾아준다")을 더 잘 이루는가? 아니면 범위 밖인가?
- **무엇을(What)** — 정확한 범위와 완료의 정의는? 무엇을 **안** 바꾸는가(외부 원본 사진·영상·타 에이전트 파일)?
- **어디서(Where)** — 진실원(SSOT)은 어느 파일·문서인가? 이 사실이 이미 어딘가 있어 손편집 중복을 만드는가?
- **언제(When)** — 생명주기·동기화 타이밍(생성/수정/tombstone/복원)·경합(이중 탭·재렌더 중)은? version/updatedAt/read-back 순서가 맞는가?
- **누가(Who)** — 이 데이터의 소유자·RLS 범위는? 어느 에이전트 역할이 검토해야 하는가? 소비 기기인가 생성 기기인가?
- **어떻게(How)** — 구현 경로와 **검증 경로**(어떤 게이트·라이브 렌더로 "현실 확인"할지)는? 실패 시 되돌리기는?

한 질문의 답이 다른 답과 모순되면 멈추고 재설계한다. (예: cascade 삭제에서 "무엇을 함께 지우나(자식 순간·사진)" ↔ "어떻게 되돌리나(정확히 그 자식만 복원)"가 맞물려야 한다.)

### 실행 규율

1. **행동 전 정독, 추측 금지.** SSOT 문서(`docs/`)를 먼저 로드한다.
2. **단일 진실원(SSOT).** 어떤 사실이 2곳 이상에 나오면 하나의 레지스트리를 두고, 파생물은 스크립트로 **재생성**한다. 손편집 중복 자체가 결함이다.
3. **의도가 아니라 현실로 검증.** 정적 게이트가 못 보는 것은 헤드리스 브라우저/실제 DOM 이벤트로 확인한다.
4. **게이트는 비공허하게.** 알려진 실패를 주입해 RED로 잡히는지 확인한 뒤에만 게이트를 신뢰한다. 셀렉터 불일치로 조용히 통과하지 않는지 검사한다.
5. **정직한 완료 보고.** 통과/스킵/실패를 구분해 보고한다. "UI 확인함"을 라이브 렌더 없이 말하지 않는다.
6. **결함 → 결함군 승격.** 버그를 단건으로 고치지 않고 근본형을 한 문장으로 추상화해 모든 형제 위치를 쓸고 게이트를 추가한다.

### 수평전개와 대칭성 (§7 — 헌법 조항)

> **한 곳에서 옳다고 판단한 것은 형제 전부에서 옳아야 한다. 그리고 그 대칭은 선언이 아니라 구조가 지킨다.**

이 조항은 관념이 아니라 **세 번의 같은 사고**에서 나왔다. 형태가 매번 같았다 — *형제들 중 하나만 조용히 빠졌고, 규율은 이미 문서에 적혀 있었다.*

- 비용(`expenses`)에만 `restore*`가 없어 **비용만 실행취소 불가**였다. 형제 도메인엔 다 있었다.
- cascade 삭제가 순간엔 op를 만들고 **사진·비용엔 안 만들어** 서버·R2에 고아가 남았다(M-0006). `docs/LESSONS.md` §3에 "도메인 대칭성"이 **이미 적혀 있었는데도** 그랬다.
- 진단 화면 여섯 개가 각자 렌더 코드를 가져, 정상을 이상과 같은 무게로 나열하는 **같은 결함이 세 곳에서 동형 반복**됐다(2026-07-26 사용자 실기기 지적).

그래서 규칙은 세 층이다. **한 층만으로는 지켜지지 않는다는 것이 관측된 사실이다.**

1. **조항** — 여기 이 문장. 판단 기준을 준다. 그러나 조항만 있으면 위 세 사고가 다시 난다(이미 났다).
2. **구조적 강제** — 규칙을 **한 곳에만 구현**하고 형제들이 그것을 *통과할 수밖에 없게* 만든다. 같은 규율을 두 곳에 손으로 구현한 순간 드리프트는 시간 문제다. 예: 진단 도구는 `Verdict`(데이터)만 만들고 그리는 일은 `renderTool()` 한 곳이 한다 — 자기 렌더 코드를 갖는 선택지가 없다. 예: `TripChildren` 번들처럼 **누락이 컴파일 오류가 되게** 타입을 설계한다.
3. **기계 검사** — 이탈을 게이트가 잡는다(`check-domain-symmetry`, `check-verdict-symmetry`). 게이트는 §4에 따라 **알려진 실패를 주입해 RED를 확인한 뒤에만** 신뢰한다.

**적용 절차.** 어떤 개선을 하나의 대상에 적용했다면, 착수 전 5W1H의 "어디서(Where)"에 다음을 반드시 답한다:

- **형제 목록을 적는다.** 이 규칙이 걸려야 할 대상 전부(도메인 4종? 화면 6개? 도구 N개?). 손으로 세지 말고 등록부/디렉터리에서 뽑는다.
- **전부에 적용하거나, 적용하지 않는 대상마다 이유를 코드에 남긴다.** 이유 없는 제외는 결함이다(`NO_OP_REQUIRED` 맵이 그 예 — 비면 규율이 죽는다).
- **다음 형제가 자동으로 따라오는가**를 묻는다. "다음 사람이 이 규칙을 몰라도 지켜지는가?" 아니라면 2층(구조)이 빠진 것이다.

**반대 방향도 같은 조항이다 — 새로 만든 것은 형제의 규율을 물려받지 않는다.** 위 세 질문은 *"내가 만든 규칙이 형제 전부에 걸리는가"*를 묻는다. 그런데 사고는 **반대 방향**으로도 난다: 새 대상(새 큐 op·새 테이블·새 화면·새 지표)을 만들면서 **형제들이 이미 지키고 있는 규율 목록을 훑지 않는 것**이다. 새것은 그것이 *푸는 문제*로 태어나지, 형제들의 계약을 알고 태어나지 않는다.

🔴 **바이트를 가진 새 형제는 테이블 하나가 아니다**(v2.11 영상에서 재확인). 로컬 entity+op
원자 커밋, rowmap/OCC, operation별 객체 키, DB 행+바이트 read-back, canonical 정확집합,
tombstone·복원·영구삭제, 백업, 진단, 사용자 대면 개수, 릴리스 rollback 분류를 기본으로 전부
상속한다. 정확집합 쓰기 프로토콜을 넓힐 때는 새 형식을 읽는 제공자를 먼저 배포하고, 옛 writer는
새 형제를 0건으로 확정하지 못하게 **조용히 호환시키지 말고 실패로 닫는다.** 외부 원본은 바꾸거나
지우지 않으며, 앱 내부 스테이징은 권위 있는 행과 바이트를 되읽은 뒤에만 정리한다.

> M-0033: `unpurge` op을 만들며 *"엔티티와 op은 한 트랜잭션"*(비타협 원칙 #1의 **atomic** commit)을 따라가지 않았다. 그 규율은 헌법에도 스킬 문서에도 **버젓이 적혀 있었고**, 기존 op들은 전부 지키고 있었다. 새로 만든 것 하나만 바깥에서 태어났다. 그리고 그 결함은 **고친 그 커밋에서** 났다 — 수정 직후가 가장 위험하다.

그래서 형제 목록은 **두 번 쓴다**: ① 내 규칙이 걸릴 대상으로, ② **형제가 나에게 걸 규칙의 출처로.** `npm run brief`가 뽑아 주는 목록을 열어 계약을 한 줄씩 대조한다.

**의도적 비대칭에는 이유를 코드에 적는다 — 안 적으면 다음 사람이 '고친다'.** §7은 대칭을 요구하므로, 대칭이 아닌 자리는 **결함처럼 보인다.** 이유가 없으면 다음 사람(사람이든 AI든)이 선의로 통일하고 그 순간 무언가 조용히 깨진다.

> 2026-07-27: ZIP 백업 사진 이름은 사진 id **8자**, R2 객체 키는 **32자 전체**다. 통일하고 싶은 모양이지만 그러면 안 된다 — ZIP은 복원이 `trip.json` 경로로 되읽으므로 짧아도 되고, R2는 **영구삭제하면 대조할 행이 사라져 파일 이름이 유일한 근거**가 되기 때문이다(M-0029). 그래서 두 함수가 나란히 있는 파일에 *왜 다른지*를 적었다. 적지 않았다면 "중복 같은데?" 한 줄로 사라졌을 것이다.

**형제에게는 대칭과 공정을 **기본값으로** 준다 — 차별은 예외이고, 예외는 설계 단계에서 심사한다.** (사용자 2026-07-27)

> *"형제끼리 차별하면 엇나가잖아요. 현실세계에서도."*

이 한 문장이 §7의 기본값을 뒤집는다. 지금까지 이 조항은 *"대칭을 지켜라, 어기면 이유를 적어라"*였다.
그런데 그 순서로는 **비대칭이 먼저 만들어지고 이유가 나중에 붙는다** — 실제로 그렇게 됐다.
새 형제(오디오 노트)를 만들며 cascade·휴지통·영구삭제·용량 집계·진단 **다섯 곳**을 빠뜨렸고,
전부 "아직 안 했다"였지 판단한 적이 없었다. 판단하지 않은 것은 **차별한 것**이다.

그래서 순서를 바꾼다:

1. **대칭이 기본값이다.** 새 형제는 기존 형제가 받는 것을 **전부 받는 상태로 태어난다.**
   빠뜨린 것이 있으면 그건 "아직 안 한 것"이 아니라 **결함**이다.
2. **비대칭은 설계 단계에서 *먼저* 심사한다.** 코드를 쓰기 전에 형제 목록을 놓고
   *"이 형제만 다르게 대할 특별한 사정이 있는가?"*를 항목마다 묻는다. 사정이 있으면
   **그 자리에 이유를 적고 시작한다** — 나중에 변명으로 붙이지 않는다.
3. **"아직 못 했다"는 정당한 사정이 될 수 있다. 단, 적어야 한다.** 오디오가 서버로 안 가는 것은
   정당하다(마이그레이션+함수+3단 배포가 함께 와야 한다). 그건 `localOnlyReason`에 적혀 있다.
   반면 휴지통에서 못 되살리는 것은 **사정이 아니라 누락**이었다.
4. **기계가 물어보게 만든다.** 사람이 기억해서 묻는 규율은 반드시 새어 나간다.
   `check-domain-symmetry`가 형제 전부에 대해 생명주기 대칭을 묻고, 예외는
   **이유와 함께 등록**해야만 통과한다(`NO_OP_REQUIRED`가 그 형태다 — 비면 규율이 죽는다).

**왜 이게 옳은가**: 비대칭은 코드에서 조용하다. 사용자에게는 조용하지 않다 —
*"사진은 되살릴 수 있는데 소리는 왜 안 되지?"* 는 즉시 드러나고, 그때 사용자가 잃은 것은
데이터만이 아니라 **앱이 일관된다는 믿음**이다.

🔴 **형제의 규율을 물려받되, 그 자리에서 참인지 다시 묻는다**(2026-07-31 · M-0057).
§7은 *"한 곳에서 옳은 것은 형제 전부에서 옳다"*고 말한다. 그런데 **옳음에는 사정권이 있다.**
규율을 그 밖으로 들고 나가면 정확히 반대로 작동한다.

> 실제 사례: 「**0을 falsy로 뭉개지 마라**」는 **금액·개수**에서 나온 옳은 규율이다(₩0은 값이다).
> 그것을 **좌표**로 들고 갔더니 — 좌표에서 `0,0`은 **관례적으로 「비어 있음」**이라 —
> 앱이 **기니만 앞바다를 사용자의 기억으로 저장**했다. 게다가 그 잘못된 계약을
> *"0,0도 값이다"*로 **유닛에 잠가** 둬서, 이 결함은 **게이트에서 영원히 초록**이었다.

- **물려받을 때 묻는다**: 이 규율이 태어난 자리는 어디였고, **여기서도 그 전제가 참인가?**
- **못박기 전에 한 번 더 묻는다.** 유닛은 계약이다 — 틀린 계약을 잠그면 게이트가 **결함의 편**에 선다.
  잠글 값을 고를 때 *"이게 사용자 화면에 나오면 말이 되는가"*를 소리 내어 읽어 본다(`0.0000, 0.0000`).

🔴 **제외 이유를 적는 것은 비대칭을 정당화하지, 그것이 나중에 틀려지는 것을 막지 못한다**
(2026-08-01 · M-0060). §7은 *"적용하지 않는 대상마다 이유를 코드에 남겨라"*라고 말한다.
실제로 남겼는데도 뚫렸다 — **이유가 참인 사정권이 나중에 좁아졌기 때문이다.**

> 실측: 사진과 소리의 바이트 업로드 조건이 손으로 **따로** 쓰여 있었고, 소리만 고쳤다.
> 그때 코드에 *"사진에는 이 조건을 걸지 않는다 — 옛 키 형식 행은 경로를 기억하지 않으면서
> 바이트는 서버에 있다"*고 **이유까지 적어 뒀다.** 타당한 이유였다. 그런데 나중에 생긴
> **복구 경로**에서는 「경로 기억 없음」의 뜻이 정반대(**없다는 걸 확인함**)로 바뀌었고,
> 그 순간 그 이유는 틀렸다. 결과: **휴지통 사진의 바이트가 어떤 경로로도 안 올라갔고**,
> 앱은 「올렸다」고 말했다. 게이트 36종·유닛 981건이 전부 초록이었다.

- **같은 판정을 두 번 쓰지 마라 — 이유를 붙여서도.** 이유는 산문이고, 산문은 코드가 바뀔 때
  같이 안 바뀐다. 갈라질 수 있는 것은 **갈라진다.**
- **비대칭이 정말 필요하면 그것을 *데이터*로 만들어 공용 함수의 인자로 넘긴다.** 그러면
  차이가 **한 줄에 드러나고**, 새 형제는 그 인자를 넘기지 않으면 **컴파일되지 않는다**
  (`mustUploadBytes(entity, unknownPathMeansNeverUploaded)`가 그 형태다 — 기본값 금지).
- **모호한 값을 주석으로 덮지 마라.** 같은 `undefined`가 두 가지를 뜻하면 그건 결함의 씨앗이다.
  뜻이 두 개면 **표시를 하나 더 만든다**(`bytesMissing` — 추측하지 않고 **확인한 사실을 적는다**).

**사용자 대면 대칭.** 코드 대칭만이 아니라 **화면 대칭**도 같은 조항의 대상이다. 같은 성격의 화면은 같은 자리에 같은 어휘로 말해야 한다 — 판정 문장은 늘 맨 위, 주행동 버튼은 늘 같은 좌표, 정상은 늘 침묵, 색은 늘 글리프와 함께. 사용자는 화면마다 규칙을 새로 배우지 않아야 한다.

### 진단 도구의 제1 규율 (§8)

> **진단 도구는 관측이 아니라 판정을 한다.** "정상은 어떤 상태이고 지금 무엇이 다른가"를 말하지 못하면 그건 도구가 아니라 원자료 덤프다(2026-07-26 사용자 지적).

- **침묵이 정상이다.** 기대값과 일치하는 항목은 화면에서 사라지고 한 줄로만 집계된다. 남아 있는 것이 곧 문제다.
- **지표엔 기대값이 필수다.** `지금 3건 / 정상 0건`. 기준을 쓸 수 없는 값은 지표가 아니라 맥락이므로 접힌 곳으로 내린다.
- **모르는 것은 '확인 불가'다.** 정상으로도 문제로도 반올림하지 않는다(비타협 원칙 #4). 미검사를 통과로 적는 것은 거짓말이다.
- **고쳤다고 말하지 말고 다시 읽어라.** 수리 액션 후에는 재판정한다(데이터 안전의 read-back과 같은 규율).
- 🔴 **완료 뒤 사라지는 임시 상태를 완료 증거로 쓰지 마라**(2026-08-03 · M-0095). 큐·락·pending처럼 성공 시 사라지는 것은 그 부재만으로 성공과 실패를 가르지 못한다. 진단·복구는 권위 있는 서버 행·원장·read-back으로 판정하고, 일부 조회 실패·세대 경합은 `unknown`으로 둔다. 모르는 상태에서 쓰기·삭제·재큐잉으로 “확인”하지 않는다.
- 🔴 **그럴듯한 원인은 확인된 원인이 아니다**(2026-07-31 · M-0056). *관측*에서 *원인*으로 건너뛰지 마라 —
  검색으로 찾은 플랫폼 동작이 내 관측과 아귀가 맞으면 그것이 답처럼 보이는데, **아귀가 맞는 것과
  그것이 원인인 것은 다르다.** 실제로: 「사진에 위치가 없다」(관측·사실)에서 「안드로이드가 지웠다」
  (원인·추측)로 건너뛰어 **완료형으로** 적었고, 사용자가 직접 시험해 뒤집었다.
  - **화면 문장은 관측까지만.** 원인을 못박은 문장은 사용자를 **엉뚱한 곳으로 보낸다** — 「선택기가
    지웠다」를 읽은 사람은 설정을 뒤지고 진짜 원인을 영영 못 찾는다. 가능성이 여럿이면 **여럿을
    말하고, 더 흔한 것을 먼저** 둔다.
  - **단정을 기계로 막는다.** 사용자 문장에 단정형(`지웠어요`·`~입니다`)이 들어가면 RED로 잡는
    유닛을 함께 둔다 — 조항만으로는 다음에 또 쓴다.

상세 작업 지침은 `.claude/skills/diagnostics-dev/SKILL.md`.

### 착수 절차 (§9 — 코딩 전에 반드시)

> **적어놓고 어기는 이유는 읽지 않기 때문이다.** (사용자 지적 2026-07-26)

맞다. 그러나 **읽기만으로는 부족하다는 것도 관측된 사실이다** — M-0012에서 나는 §7을 *직접 쓰고 같은 커밋에서 어겼다*. 읽음은 그때 최대치였다. 실패한 것은 읽기가 아니라 **적용**이었다: 산문에 동의하고 넘어갔지, 그 절차가 요구하는 *산출물*을 만들지 않았다. 그래서 절차는 셋이다.

```
npm run brief            # 또는: npm run brief <고칠 파일들>
```

1. **정독** — 브리핑이 알려준 스킬 문서를 **먼저 읽는다.** 어느 문서인지는 기억이 아니라 라우팅 표(`scripts/brief.mjs`의 `SKILL_ROUTES`)가 정한다. `check-skill-routing`이 "모든 코드 영역이 읽을 문서를 갖는가"를 강제한다.
2. **구멍 메우기** — 정독 중 **문서와 코드가 어긋나거나 규칙이 빠져 있으면 그것부터 고친다.** 이게 이 단계의 진짜 일이다. 실제 사례: 오버레이 잘림 사고 때 `ui-responsive-dev`에 `vh`/`dvh`·오버레이 스크롤 규칙이 **아예 없었다** — 읽었어도 못 막았을 구멍이었다. 낡은 전제가 화석으로 남은 주석도 같은 부류다(M-0006).
3. **산출물 만들기** — 브리핑이 뽑아준 **형제 목록**을 보고 §7의 세 질문에 답한다: 전부에 적용하는가 / 제외한다면 이유를 코드에 남겼는가 / 다음 형제가 자동으로 따라오는가.
4. **세계를 본다(상태 의존 작업이면 필수)** — 데이터·서버 상태에 따라 답이 달라지는 일이면 **실서버 스냅샷을 한 번 뜬다.** 규칙 문서는 *무엇이어야 하는가*를 말하지 *지금 무엇인가*를 말하지 않는다.
   - 2026-07-26 M-0019: 사진 파일 대조를 만들며 "저장소는 하나"라고 가정했다. 실제로는 **13건 중 9건이 다른 저장소**에 있었고, 그 사실은 `docs/HANDOFF.md`에 **이미 적혀 있었다.** 스냅샷 한 번이 유닛 21건보다 먼저 잡았다.
   - 2026-07-26 M-0023: 영구삭제 방식을 두 번 바꾸면서 **옛 방식으로 이미 지워진 것들**에게는 아무 일도 일어나지 않는다는 걸 아무도 묻지 않았다.
   - 물을 것: **지금 데이터에 옛 방식으로 만들어진 것이 있는가? 있으면 누가 데려오는가?**
   - 🔴 **「세계」에는 사용자가 준 파일도 들어간다**(2026-07-31 · M-0058). 사용자가 *"이 사진이
     안 돼요"*라고 하면 **가장 먼저 그 파일을 달라고 하라.** 스크린샷은 *화면*을 말하고
     **파일은 사실을 말한다.**
     - 실측: 「사진에 위치가 없다」의 원인을 **사흘 동안 추측으로 좁혔다**(플랫폼 문서 → 단정 →
       철회 → 방어 코드). 사용자가 원본 JPEG을 주자 **5분 만에 끝났다** — GPS 태그가
       껍데기만 있고 값이 `0/0`이었다. 갤러리가 지도를 보여준 건 **파일이 아니라 폰의
       미디어 DB**를 읽기 때문이었다.
     - **원본을 얻을 수 있는데 추측으로 좁히고 있으면 그 시간은 전부 낭비다.**
   - 🔴 **그런데 파일도 경로를 거치면 증거가 아니다**(2026-08-01 · M-0059 — 바로 위 항목이
     **하루 만에 뒤집힌 자리**다). 위 「5분 만에 끝났다」는 실측은 **채팅 업로드를 거친 사본**을
     잰 것이었고, 그 경로가 GPS 값을 **0으로 덮는다**는 사실이 다음 날 실증됐다.
     - 실측(사용자가 설계한 실험): 같은 사진을 ①ZIP에 담아서 ②채팅에 직접 보내 비교했더니
       **크기는 같고 해시가 달랐다** — 25바이트, 전부 GPS 자리. 태그 껍데기는 두고 값만 0.
     - **「원본을 봤다」와 「원본의 사본을 봤다」는 다른 말이다.** 나는 그 둘을 같은 말로 썼고,
       그 위에 결론을 쌓고 화면 문장까지 고쳤다.
     - **물을 것**: 이 바이트가 **어떤 경로로** 내게 왔는가? 그 경로가 바이트를 바꿀 수 있는가?
     - **가르는 법**: 같은 파일을 **두 경로로** 받아 해시를 비교한다. 컨테이너(ZIP·tar)에 넣으면
       중간 가공이 걸리지 않는다 — 이게 **대조군**이다.

브리핑은 그 영역에서 **과거에 낸 실수**(`docs/records/coding-mistakes.md`)도 함께 띄운다 — 같은 자리에서 두 번 넘어지지 않게.

### 우회로는 고침이 아니다 · 내가 적어 둔 것을 내가 읽는다 (§9-B — 2026-08-01 · M-0064)

> 사용자: *"계속 니 코딩이 맞다고 가정하고 원인을 찾지마.. 바뀌지 않는 건 앱에 올라갈 때
> 해시가 바뀌고 있다는거야.. 원본 파일은 문제 없어. 제발....."*

**원인은 내가 나흘 전에 코드 주석으로 정확히 적어 둔 것이었다.** 우회로도 만들어 뒀다.
그런데 **버튼 뒤에 숨기고 기본 경로는 그대로 뒀고**, 그 뒤로 나는 「내 입력이 원본이다」를
전제로 파서·순서·게이트·rowmap을 뒤졌다. 사용자는 그동안 기본 경로를 썼다.

네 가지가 한꺼번에 틀렸다. 각각이 조항이다.

1. 🔴 **「우회로를 만들어 뒀다」와 「고쳤다」는 다른 말이다.** 기본 경로가 여전히 깨져 있으면
   그건 **안 고친 것**이다. 사용자는 기본을 쓴다 — 그래서 기본이다.
2. 🔴 **「내가 재보지 못했다」는 기본값을 안 바꿀 이유가 된다. 사용자가 재준 뒤에는 아니다.**
   §13 3항(*"못 봤으면 못 봤다고 적는다"*)은 **보고의 규율**이지 **결정을 미루라는 뜻이 아니다.**
   신중함의 값을 계산할 때 *"안 바꾸면 기본 경로가 계속 깨져 있다"*를 반드시 항에 넣어라.
3. 🔴 **내가 쓴 것을 내 가설 목록에 올려라.** 조사할 때 **그 영역의 코드 주석과 원장을 먼저
   훑는다.** 이 저장소는 답을 주석에 적어 두고도 못 찾아 나흘을 쓴 적이 있다(이것이다).
   `npm run brief`가 스킬 문서와 과거 실수를 띄우는 이유가 그것이므로, **조사할 파일로
   브리핑을 돌려라** — 고칠 파일이 아니라.
4. 🔴 **「내 코드가 맞다」를 기본 가정으로 두지 마라 — 특히 입력 경계에서.**
   §9 4단계는 *"세계를 본다"*고 말한다. 그런데 **내 입력 경계도 세계다.** 「앱이 무엇을
   받았는가」는 코드가 아니라 **측정 대상**이고, 플랫폼(선택기·드라이버·프록시)은
   내 코드가 손대기 전에 **이미 바이트를 바꿔 놓을 수 있다.**
   - **가르는 법**: 같은 자료를 **두 경로로** 넣고 결과를 비교한다. 다르면 코드가 아니라
     **경로**가 변수다.
   - **사용자의 반증 시험을 받을 때 어느 경로로 했는지 반드시 물어라.** 안 물어서 옳은
     가설을 잘못 철회한 적이 있다(M-0056 → M-0064에서 되살아났다).

**기계 층**: 기본 경로가 조용히 되돌아가지 않게 게이트에 넣는다(`check-exif-order` 검사 D가
그 형태다 — 정적이라 **빠른 차선에서도 건너뛰지 않는다**). 라이브만으로는 부족하다: SKIP은
통과가 아니고, 이 부류의 결함은 **조용해서** 비싸다.

### 게이트가 못 잡는 것 (§10 — 2026-07-26에 6건을 겪고 세움)

> **자동 게이트는 만능이 아니다. 무엇을 못 잡는지 알고, 그 자리에 다른 층을 놓는다.**

하루에 결함 6건을 잡았는데 **정적 게이트가 잡은 것은 0건**이었다. 전부 **사용자의 실기기 화면**에서 나왔다. 사후에 분류해 보니 셋으로 갈렸고, 각각 처방이 다르다.

| 부류 | 왜 정적 게이트가 못 잡나 | 놓아야 할 층 |
|---|---|---|
| **① 계약 결함** — 새 테이블/연산의 권한 누락(M-0020·M-0024) | (잡을 수 있다) | **정적 게이트.** 코드가 하는 일과 서버가 허락하는 일을 대조 |
| **② 상태 의존** — 저장소 혼재(M-0019), 옛 방식 잔재(M-0023) | **코드에 답이 없다.** 지금 데이터가 어떤 모양인지에 달렸다 | **진단 도구가 곧 게이트다**(런타임). + §9 4단계 실서버 스냅샷 |
| **③ 전달 결함** — 판정 문장이 엉뚱한 곳을 가리킴(M-0021), 확인해놓고 화면에 안 알림(M-0022), 찾아보지도 않고 「없다」고 말하고 삭제를 권함(M-0046) | **자료구조는 옳다.** 틀린 것은 사용자에게 가는 것뿐 | 사용자 문장을 **순수 함수로 분리**해 유닛으로 · `check-report-fields` · **`verify-diagnostics-live`**(실제 브라우저가 도구를 하나씩 열어 문장·자리·버튼을 잰다 — 2026-07-28 신설) · **실기기 확인** |

**②의 핵심**: 정적 게이트는 *빌드 시점*이고 진단 도구는 *런타임*이다. 과거 데이터가 어긋날 수 있는 변경을 하면 **그 어긋남을 진단 지표로 만든다** — 「지웠는데 서버에 남은 항목」·「파일이 없는 사진 기록」·「옛 저장소에 남은 사진」이 전부 그 층이다. 지표가 곧 그 결함군의 게이트다.

**③의 핵심**: 검사의 시선이 **자료구조가 아니라 사용자**에 있어야 한다. M-0022에서 유닛 15건이 전부 통과했다 — 숫자는 다 맞았고, 화면에 무엇이 나가는지 검사한 것이 하나도 없었다. 사용자에게 보이는 문장을 만드는 로직은 **순수 함수로 뽑아라.** 그래야 검사할 수 있다.

**③에 기계 층을 놓았다(2026-07-28).** M-0046 이후 `verify-diagnostics-live`가 생겼다 — 실제 Chromium이 진단 도구를 **하나씩 열어** ①판정까지 그려지는가 ②설명 문장이 실제 DOM에 나오는가 ③정상은 접히는가 ④주버튼이 무엇인가를 잰다. 그리고 실제 앱에서 만들 수 없는 위험한 상태는 **판정 함수에 직접 먹여** 실제 렌더러로 그린다. 세 종류 주입으로 RED를 확인했다(§4).

**그래도 남는 것**: "이 문장이 사용자에게 이해되는가", "이 화면이 무섭게 보이는가"는 기계가 판정하지 못한다. 그 자리는 **실기기 확인**이 유일한 층이고, 그래서 완료의 정의에 들어 있다. 오늘 6/6이 그 층에서 나왔다는 사실이 그 증거다 — **이 앱에서 가장 강력한 검출기는 사용자의 실기기 화면이다.** 그것을 대체하려 들지 말고, 거기까지 가는 길을 짧게 만든다(진단 도구가 하는 일이 그것이다).

**그리고 그 검출기가 무엇을 대신 재고 있었는지 물어라(§12를 나 자신에게).** 2026-07-28에 사용자가 물었다 — *"니가 의심되는 부분에서 스크린샷 찍어서 확인하면 되는거 아냐?"* 맞다. 브라우저가 있는데 화면 확인을 사용자에게 시키고 있었다. **실기기가 유일한 층인 것과, 내가 재볼 수 있는 것을 안 재는 것은 다른 이야기다.**

### 검사하는 것도 결함을 갖는다 (§11 — 같은 날 게이트 세 개가 서로 다르게 틀렸다)

> **게이트를 만들었다고 그 자리가 지켜지는 것이 아니다. 게이트도 검사받아야 한다.**

§4는 "알려진 실패를 주입해 RED로 잡히는지 확인한 뒤에만 게이트를 신뢰한다"고 이미 말한다.
그런데 2026-07-26에 그 규칙을 지키고도 세 번 뚫렸다. **주입을 *언제* 하는가가 빠져 있었다.**

1. **넓힐 때 다시 공허해진다.** `check-report-fields`에 대상을 하나 추가했더니 그 대상에
   대해서는 공허했다(중첩 필드를 안 봤다). **등록부에 한 줄 추가하는 것은 새 게이트를 만드는
   것과 같다** — 그 대상으로 주입을 다시 하라.
2. **셀프테스트가 옛 전제를 못박는다.** 위 잘못된 가정이 셀프테스트에 *정상 케이스*로 박혀
   있었다. 전제가 바뀌면 **케이스를 먼저 뒤집는다.** 통과시키려고 로직을 되돌리지 마라.
   (고치자마자 밀려 있던 진짜 결함이 하나 더 나왔다 — 그게 공허했다는 증거다.)
3. **오탐도 결함이다.** 정규식이 `typeof body.op === 'string'`을 op으로 셌다. 오탐은
   "빡빡한 게이트"가 아니라 **틀린 게이트**다. 사람이 무시하기 시작하면 그 게이트는 죽는다.

**그리고 래칫에 걸리면 우회하지 말고 덜어내라.** `check-fn-size`가 함수 증가를 막았을 때
주석을 줄이는 대신 헤더 조립을 함수로 뽑았고, 결과가 더 나았다 — **게이트가 설계를 밀어준다.**

### 사람에게 무엇을 대신 시키고 있는가 (§12 — 사용자가 스크린샷 수백 장을 찍은 날)

> *"하나하나 스크린샷 찍다보니 수백장은 찍은 거 같아. 미쳐버릴 지경이거든."* (2026-07-26)

그 수백 장은 **앱이 자기 상태를 말하지 못해 사람이 대신 옮긴 것**이다. 원인은 셋이었고 전부
"앱이 아는데 말하지 않았다"였다: 요약에 지표 숫자가 없었고, 판정 문장이 한정을 생략했고,
앱이 자기 시야의 경계를 밝히지 않았다.

기능을 하나 더 만들기 전에 물어라 — **"지금 이 앱은 사람에게 무엇을 대신 시키고 있는가?"**
답이 *화면을 사진으로 찍어 나르는 일*이면 그걸 먼저 없앤다.

상세 전말과 근본형 6가지: `docs/records/2026-07-26-STORAGE-DELETE-POSTMORTEM.md`
(`npm run brief`가 해당 영역 착수 시 자동으로 띄운다).

### 내가 볼 수 있는 것을 사람에게 묻지 않는다 (§13 — 2026-07-28 사용자 지적)

> *"니가 의심되는 부분에서 스크린샷 찍어서 확인하면 되는거 아냐?"*

맞다. **Playwright·Chromium이 이 환경에 있다.** 그런데 화면 확인을 사용자에게 시키고 있었다.
§12를 앱에만 묻고 **나 자신에게는 안 물은 것**이다. 그리고 그 값은 이미 치렀다 — M-0046은
자료구조가 옳고 **화면 문장만 틀린** 결함이었고, 게이트 31종·유닛 686건이 전부 초록인 채로
배포됐다가 30분 뒤 사용자의 스크린샷 한 장에 잡혔다.

**§10의 「실기기가 유일한 층」과 이 조항은 다른 이야기다.** 그 문장은 *"사람이 이해하는가·
무섭게 보이는가"*를 기계가 판정 못 한다는 뜻이지, **내가 재볼 수 있는 것을 안 재도 된다**는
뜻이 아니다. 사람에게 남길 것은 판단이지 **관측**이 아니다.

**규칙.**

1. **화면에 나가는 것을 바꿨으면 열어서 본다.** 문장·버튼·자리·색·접힘·넘침이 대상이다.
   실제 앱에서 그 상태를 만들 수 없으면(로그인·서버 상태) **판정 함수에 값을 먹여 실제
   렌더러로 그린다** — `verify-diagnostics-live`의 B층이 그 방법이고, 일회성으로 써도 된다.
2. **의심되면 캡처가 질문보다 싸다.** "이거 맞나요?"를 사용자에게 되묻기 전에 한 번 그려 본다.
3. **못 봤으면 "라이브 렌더 미실행"이라고 적는다**(비타협 원칙 #4). 「확인함」과 「봤음」을
   같은 말로 쓰지 않는다.
4. 🔴 **버튼은 눌러 봐야 확인한 것이다**(2026-07-28 사용자 지적: *"앞으로 버튼을 만들게 된다면 니가 직접 눌러봐서 확인할 수 있지 않아?"*).
   맞다. 그리고 이건 1항의 되풀이가 아니다 — **그리는 것과 도는 것은 다른 층**이다.
   M-0046도 M-0048도 **버튼이 결함**이었는데, 라이브 검사는 **라벨만 읽고 한 번도 안 눌러 봤다.**
   누른 뒤의 계약은 §8에 이미 있다 — *"고쳤다고 말하지 말고 **다시 읽어라**"*. 그런데 그
   재판정이 실제로 도는지를 아무도 재고 있지 않았다. **누르면 네 가지를 잰다**:
   ①결과 문장이 화면에 나오는가(자료구조에만 있으면 M-0022의 행동판) ②재판정이 도는가(§8)
   ③실패하면 사유가 보이는가(조용히 삼키지 않는가) ④버튼이 잠긴 채 남지 않는가.
   🔴 **단, 파괴적 행동은 누르지 않는다.** 검사가 사용자의 실제 기억을 담은 기기에서도 돌 수
   있다(§0 — 원본 자료를 임의로 지우지 않는다). 파괴적 경로는 **행동 목록이 만들어지는지
   여부**로 재고(있어야 할 때 있는가·없어야 할 때 없는가), 실행은 주입 픽스처의 가짜
   `run()`으로만 한다.
5. 🔴 **새 화면은 「누가 눈으로 보는가」에 답해야 한다.** 조항만으로는 안 지켜지므로
   (§7 — 관측된 사실이다) `check-live-coverage`가 묻는다: `src/ui/screens/`·`src/ui/panels/`의
   모든 화면은 어느 라이브 검사엔가 `@live-covers`로 덮이거나, **이유와 함께** 제외에 등록돼야
   한다. 이유 없는 제외는 결함이다.
6. 🔴 **마스크·크롭·safe-area가 걸리는 자리는 「보이는 캔버스」가 아니라 「실제로 남는 영역」을
   본다**(2026-08-02 · M-0083). 런처 아이콘(적응형 bleed)·스크롤 컨테이너(overflow)·둥근/잘리는
   상자·노치 여백처럼 **플랫폼이 가장자리를 잘라내는** 표면은, 소스가 어떻게 생겼는지가 아니라
   *잘린 뒤 무엇이 남는지*가 사용자가 보는 것이다.
   - 실측: 아이콘을 「배경=전체이미지/전경=투명」으로 두고 *"글자가 아래 끝이라 안 잘린다"*고
     **적어 두고 안 쟀다**(ADR-0038). 적응형 바깥 ~18dp bleed가 「BG 여행」을 잘랐다 — 실기기가 반증.
   - **가정하지 말고 그 마스크로 렌더한다.** 원(circle)·스퀘어클 두 종을 씌워 콘텐츠가 남는지
     본다. 실제 앱에서 그 상태를 못 만들면 자산을 직접 마스크 밑에 깔아 그린다(§13 1항의 확장).
   - 이건 §10 ②(상태 의존)의 시각판이다 — **코드는 옳은데 「보이는 것」만 틀린** 부류라, 자료구조
     유닛으로는 영원히 안 잡힌다. 최종 검출기는 실기기이므로 완료의 정의에 남긴다.

**한계도 정직하게**: 선언(`@live-covers`)이 거짓일 수 있고, 게이트는 그것까지 못 본다.
그래서 이 게이트가 하는 일은 *"덮였다"의 보증*이 아니라 **"안 덮인 화면이 조용히 생기는 것의
차단"**이다. 그리고 라이브 검사 자신도 **대상 0에서 공허하게 통과**할 수 있다 —
실제로 `verify-diagnostics-live` 첫 판이 그랬다(허브를 못 열었는데 PASS가 찍혔다).
검사는 **목록 확보를 먼저 판정**하고, 실패하면 나머지를 재지 않는다(§4는 게이트 자신에게도 걸린다).

### 데이터 안전 불변식 (최고 위험 표면 — 상세 `docs/SYNC_PROTOCOL.md`)

- 안정적 `id` + `created_at` + `updated_at`; 동일 id 충돌은 LWW(최신 `updated_at` 우선).
- **하드 삭제 없음** — `deleted_at` tombstone. 오래된 활성 행이 tombstone을 이기지 못한다.
  - **유일한 예외: 영구삭제(휴지통 비우기, ADR-0030).** 2단계 확인을 거친 명시적 의도이므로 서버 행을 **실제로 지운다**. 이 조항의 목적은 "좀비 차단"이었는데, 그건 서버 원장(`journey.purged_ids`) + BEFORE INSERT 트리거가 **더 강하게** 달성한다. 남는 것은 id 한 줄뿐이고 자료는 남지 않는다. 일반 삭제(휴지통행)는 **여전히 tombstone 전용**이다.
- **빈-클라우드 가드**: 서버가 빈 배열을 줘도 로컬을 덮어쓰지 않는다.
- **정확한 read-back으로 확인**: HTTP 200/성공 토스트가 아니라 같은 레코드를 되읽어 확인한 뒤에만 완료 처리.
- 파이프라인 생성 행(`source=pipeline`)은 소비 기기에서 읽기전용. 로컬에 없다고 tombstone 하지 않는다(오래된 기기가 신선한 클라우드 데이터를 지운다).
- 카메라/EXIF 등 기계 파생 값은 `needs_review`로 시작하고, 재생성이 검토·tombstone된 행을 덮어쓰지 않는다.

## 검증 명령 (두 도구 공통)

```bash
npm run brief <고칠 파일들>   # 착수 전 — 읽을 스킬 문서·형제 목록·그 영역의 과거 실수
npm run gates                 # 편집 루프 (약 6초 · 정적 + typecheck)
npm run build                 # 기능 화면 실측 또는 릴리스 후보 앱 재빌드(tsc --noEmit + vite build)
npm run harness               # 🔴 머지·배포할 때만 — build 다음 전체 검사
npm run live                  # 릴리스 후보 라이브 층(build 다음에)
```

- 🔴 **`npm run gates`는 「통과」가 아니다.** 판정문이 *"재본 N개 통과 · **M개는 아예 안
  쟀습니다**"*라고 말하고 안 잰 이름을 나열한다. 작업 중에는 관련 유닛·개별 게이트·타입검사를
  쓰고, 전체 하네스는 아래 릴리스 묶음에서만 돈다.
- `npm run live`는 `dist`가 소스보다 낡으면 **스스로 멈춘다**(exit 2). 낡은 번들을 재면
  검사가 공허해지기 때문이다 — 반드시 `npm run build` 다음에.
- **SKIP은 통과가 아니다.** 선택 게이트가 건너뛰었으면 그 실행은 그 층을 **재지 않은 것**이다.

### 변경 축적과 릴리스 묶음 (사용자 결정 2026-08-04)

- **문서 개정만 있는 작업**은 문서·필요한 생성물·관련 정적 검사까지만 진행한다. 앱 버전 증가,
  앱 재빌드, 전체 하네스, 머지, 배포를 단독으로 일으키지 않고 다음 기능 릴리스에 묶는다.
- **기능 변경도 기본은 축적**이다. 작업 중에는 관련 유닛·개별 게이트·타입검사와 필요한 화면
  실측으로 결함을 좁혀 잡되, 전체 하네스를 관성적으로 반복하지 않는다.
- **긴급 릴리스 예외**는 기다리는 비용이 더 큰 경우뿐이다: 사용자 데이터 유실·보안 노출,
  동기화/로그인/앱 실행 불가 같은 핵심 흐름 차단, 배포·서버 호환성 파손. 단순 문구·미관·편의
  개선은 긴급 사유가 아니다.
- **릴리스 순서는 하나의 원자적 묶음**이다:
  `버전·CHANGELOG 확정 → 앱 build → 전체 harness + live-render → 그 커밋 머지 → 배포 → 배포 그린 확인`.
  하네스만 미리 돌려 두거나, 하네스 뒤 코드를 바꾸거나, 머지만 하고 배포를 미루면 묶음이 깨진다.

## 완료의 정의 (Definition of Done)

> 🔴 **두 어댑터가 서로 다른 정의를 갖고 있었다**(2026-07-29에 발견). CLAUDE엔 화면·버튼
> 확인은 있고 **배포 확인이 없었고**, AGENTS엔 배포 그린은 있고 **§13이 없었다.**
> 그래서 *"Codex는 화면을 안 보고 완료라 하고, Claude는 배포 확인 없이 완료라 하는"* 길이
> 양쪽에 하나씩 열려 있었다 — **M-0046이 정확히 그 형태였다.** 아래가 합친 정의다.

```
릴리스 완료
  → 모아 둔 변경의 버전(changelog +0.01)·문서 확정
  → 🔒 `npm run release:arm <후보>` — 여기서부터 저장소를 얼린다(§18-H)
  → 앱 build
  → 전체 하네스 통과(일부 아님 · SKIP은 통과가 아니다)
  → 🔴 화면을 바꿨으면 열어서 보고, 버튼을 만들었으면 눌러 봄(§13)
  → PR 그린 → 병합 → **배포 성공 확인**
  → 사용자 흐름 확인 · Acceptance 승인
  → 🔓 `npm run release:close` — 재개방 횟수가 나온다
  → 정직한 보고(통과/스킵/미실행을 구분 · **검증 1회 + 재개방 N회**)
```

작업 브랜치에서 관련 검사까지만 끝낸 변경은 **「구현됨·릴리스 대기」**이지 「릴리스 완료」가 아니다.
릴리스할 때는 **일부가 아니라 전부** 통과해야 한다. 자동검사를 통과하지 않은 릴리스를 완료로 표시하지 않는다.

> 🔴 **「화면을 열어서 봄」을 건너뛰었으면 「라이브 렌더 미실행」이라고 적는다**(원칙 #4).
> 「검사 통과」와 「화면을 봤음」은 다른 말이다 — M-0046은 전자가 전부 초록인 채로 배포됐다.
> 그리고 **「화면을 봤음」과 「버튼이 돈다」도 다른 말이다.** M-0046·M-0048은 둘 다 버튼이
> 결함이었는데, 그때 라이브 검사는 버튼의 **라벨만 읽고 있었다**(§13 4항).

> **완료 = 병합이 아니라 배포 그린 확인.** merge 성공 ≠ 배포 성공(M-0031에서 빨간불이
> 두 번 쌓였다). 다만 이 샌드박스는 `*.github.io`를 막으므로 **「배포 확인함」과
> 「사이트에서 확인함」은 다른 말**이다 — 후자는 사용자 실기기 몫이다.

## Git / 협업

- **브랜치**: AI별 네임스페이스 — `claude/*`, `codex/*`. **아무도 `main`에 직접 push 하지 않는다.**
  (예전 문서에 `feature/TASK-번호-설명` 형식이 적혀 있었으나 **실제 관행과 달랐다**. 실제는
  `claude/travel-log-app-r2xd5f` 같은 지정 브랜치다. TASK 번호를 붙이는 것은 선택.)
- **commit**: `feat|fix|security|refactor|test|docs|build|chore(scope): 요약`
  (`perf`·`style` 등은 허용 목록에 없다 — `.githooks/commit-msg`가 거부한다.)
- **별도 로컬 클론** — 작업 폴더 공유 금지.
- **뜨거운 공유 파일은 동시에 하나의 PR만**: 동일 DB migration · media pipeline 핵심 ·
  동기화 상태머신 · 데이터 형식 정의 · Supabase 정책.
- **푸시는 작업 단위로 모은다** — `ci.yml`이 **모든 브랜치 push마다** 돈다. 커밋은 자주 하되
  푸시는 한 덩어리가 끝났을 때. (2026-07-28에 8회 푸시 = CI 8회였고, 그 전에는 Actions
  한도 초과로 배포가 막힌 적이 있다.)
- 각 작업 종료 시 `docs/HANDOFF.md`에 인계 기록. 중요한 결정은 `docs/DECISIONS.md`(ADR).

## 보증 매트릭스 (역할 릴레이 아님)


- 기본값은 **단일 구현 에이전트가 조사→구현→검증→문서→보고까지 맥락을 유지**한다(맥락 보존 실행 루프).
- `docs/AGENT_REGISTRY.md`의 <!--reg:logicalRoleCount-->139<!--/reg-->개 역할은 변경유형별로 선택하는 **조건부 품질 게이트**다. 광범위 탐색·감사만 병렬화하고 최종 구현은 하나로 수렴한다.
- 물리 에이전트는 **<!--reg:agentCount-->27<!--/reg-->개**(`.claude/agents/` — 통합·디자인·독립 감사). **이름을 여기 손으로 나열하지 않는다**: 목록은 `src/app/registry.gen.ts`(자동 집계), 한 줄 설명·분류는 `src/app/agents.ts`, 둘의 어긋남은 `check-registry-gen`이 **양방향**으로 RED. 예전에 여기 적힌 「통합 10 + 디자인 16」은 독립 감사 에이전트가 늘어난 뒤에도 그대로 남아 **한 개 모자란 채 화석**이 돼 있었다.
- **뜨거운 공유 파일 직렬화.** 동기화 상태머신·conflict 해결·media pipeline 핵심·DB migration·Supabase 정책은 **단일 오너·단일 구현자·단일 PR**로 직렬화한다(동시 편집 금지). Phase 0에서 변경유형→역할→게이트 라우팅 테이블과 `CODEOWNERS`(경로 오너십)를 SSOT로 생성한다.

## 에이전트 공통 출력계약 (§18.1)


**agent chat output만으로 인계하지 않는다 (S-07).** 모든 에이전트는 결과를 아래 JSON으로 반환하고, 이를 **`schemas/agent-report.schema.json`(JSON Schema draft 2020-12)로 검증**한 뒤 **`artifacts/agent-reports/{TASK_ID}-{agent}.json`** 파일로 남긴다. 검증 실패한 report는 인계로 인정하지 않는다. 필수 필드: `agent`, `task_id`, `objective`.

```json
{
  "agent": "에이전트 이름",
  "task_id": "TASK-0001",
  "objective": "작업 목적",
  "assumptions": [],
  "files_read": [],
  "files_changed": [],
  "database_changes": [],
  "storage_changes": [],
  "security_impact": [],
  "privacy_impact": [],
  "implementation_summary": [],
  "tests_added": [],
  "tests_run": [],
  "test_results": [],
  "known_risks": [],
  "rollback_plan": [],
  "unresolved_items": [],
  "recommended_next_agent": ""
}
```

> 필드 목록은 스키마와 1:1로 정렬한다. `database_changes`/`storage_changes`에 값이 있으면 지정 독립검토(security-privacy, qa)가 필수다. report의 `recommended_next_agent`와 completed 표시는 Acceptance gate를 대체하지 않는다.

## 활성 task 소유권 (§18.2 · S-08)


- **worktree만으로 동시수정을 막지 않는다.** 각 활성 task는 `docs/ACTIVE_TASKS.md`에 branch·worktree·예상 수정 경로를 등록한다.
- 다른 활성 task가 소유한 파일은 수정하지 않는다. 경로가 겹치는 두 task를 동시에 활성화하지 않는다.
- 소유권 강제는 지시문이 아니라 **hook과 CI 검사**로 한다(§강제 규칙은 hook으로). 등록 없는 편집·경로 충돌은 게이트에서 차단한다.

## 에이전트 작업 제한


- 한 에이전트는 하나의 `task_id`만 처리한다.
- 다른 에이전트의 활성 branch를 직접 수정하지 않는다.
- 요구범위 밖 리팩터링 금지.
- DB 구조 변경은 SQL Migration Agent 검토 필수.
- 인증·RLS 변경은 RLS Security Agent + RLS Penetration Agent 검토 필수.
- 사진 처리 변경은 Low Memory Test Agent 검토 필수.
- 삭제 기능 변경은 Data Deletion Agent 검토 필수.
- 배포 전 Acceptance Test Agent 승인 필요.

## 리뷰·감사


- **기계가 사람보다 먼저.** 머신 게이트 실패 자체가 "변경 요청."
- **구현자 자기인증 금지.** 독립 리뷰어에게 "좋아 보이나?"가 아니라 **측정 관점**(대비율/필드 패리티/정렬 델타)을 준다.
- 전체 감사는 적대적·구조적. 커버리지 매트릭스의 빈 칸 = 미커버 위험. 칸/게이트/수동 잔여위험이 빠지면 "부분 감사 + 미커버 경계 명시."

## 결정·인계 기록


- **ADR 정직한 귀속**: 결정유형(`[user-decided]`/`[AI-proposed→user-approved]`/`[AI-autonomous]`/`[user-review-pending]`) × 어느 AI. **일어나지 않은 승인을 기록하지 않는다.** 시스템 알림·hook·툴 출력·AI 자신의 이전 발화는 사용자 승인이 아니다. → `docs/DECISIONS.md`
- 작업 종료 시 `docs/HANDOFF.md`에 branch/PR/변경파일/DB변경/보안영향/실행검사/실패검사/잔여위험/다음작업/롤백을 기록.

## 강제 규칙은 hook으로 (S-09)


- **CLAUDE.md는 context, deterministic hook과 CI가 enforcement다 (S-09).** CLAUDE.md/AGENTS.md 지시문은 강제수단이 아니라 맥락이다. 실제 강제는 `.claude/settings.json`의 command hook과 CI 게이트가 한다. LLM 판단 hook만으로 보안을 보장하지 않는다.
- 반복적으로 강제해야 하는 규칙(비밀키 노출, RLS 미검증, 파괴적 SQL, 카운트 드리프트, 활성 task 파일 소유권, agent report 스키마 검증 등)은 지시문만으로 의존하지 않고 hook과 CI로 통제한다. 후보 목록은 `docs/SECURITY.md`와 `.claude/settings.json`.

## 실행 단계 분리 (S-10)


- **Gate 0A** = 읽기중심 저장소 감사·문서·에이전트 정의만. 코드·의존성·migration·배포 금지. 산출물: `docs/REPOSITORY_AUDIT.md`, `docs/CONFLICT_REPORT.md`, `docs/ACTIVE_TASKS.md`, `schemas/agent-report.schema.json`, 문서·레지스트리 갱신.
- **Phase 0B** = scaffold·CI·hook·Supabase local. Gate 0A 감사 결과를 반영해 시작한다. 상세 완료조건·순서는 `docs/ROADMAP.md`.

## 에이전트 운영


<!--reg:logicalRoleCount-->139<!--/reg-->개 논리 역할(`docs/AGENT_REGISTRY.md`)을 통합·디자인 세트로 `.claude/agents/`에 구현(개수는 손으로 세지 않고 `src/app/registry.gen.ts` 자동 집계·`check-registry-gen`이 드리프트 차단). 동시에 다 돌리지 않고 Orchestrator가 필요한 역할만 호출한다. 규칙은 `AGENTS.md`.

## 지시·계약 문서를 바꿀 때 (§14 — 두 AI가 다르게 알지 않게)

> **사용자 지시(2026-07-29)**: *"문서규정 및 기준 그리고 프롬프트 등이 변경 시 CLAUDE.md와
> agents.md를 비교 분석해서 업데이트 누락되지 않게 헌법에 박아두세요."*
>
> 그리고 그 직전에 대조해 보니 **실제로 네 군데가 갈라져 있었다.** 가장 무거운 것은
> 「완료의 정의」가 서로를 포함하지 않은 것 — *Codex는 화면을 안 보고, Claude는 배포 확인
> 없이* 「완료」라 할 수 있었다. **M-0046이 정확히 그 형태였다.**

### 절차 (외울 필요 없다 — 어기면 게이트가 막는다)

```
① 공통 계약(두 AI가 같이 알아야 할 것)을 바꾼다  → docs/CONSTITUTION.md **만** 고친다
② 재생성                                          → npm run gen:adapters
③ 확인                                            → npm run gates  (약 6초)
④ 커밋                                            → 정본 + 두 어댑터를 **같은 커밋**에
```

- 🔴 **`CLAUDE.md`·`AGENTS.md`를 직접 고치지 않는다.** 마커 사이는 생성물이다.
  고치면 `check-adapter-parity`가 「커밋본 != 재생성본」으로 RED.
- **어느 쪽에 적을지 헷갈리면 정본이다.** 도구별 서문에는 *그 도구에서만 참인 것*만 적는다
  (파일 위치·도구 이름). 공통 사실을 서문에 적는 순간 그게 **다음 드리프트의 씨앗**이다.
- **새 `docs/*.md`를 만들면 「문서 지도」에 올린다.** 안 올리면 다음 사람은 그 문서가 있는 줄도
  모른다 — 실측(2026-07-29) 25개 중 **4개가 빠져 있었고**, 그중 `DISASTER_RECOVERY.md`는
  전용 감사 에이전트가 참조하는 **계약 문서**였다. `check-doc-governance`가 막는다.
- **새 AI 도구를 들이면 그 지시문도 어댑터로 등록한다**(`gen-adapters.mjs`의 `ADAPTERS`).
  안 하면 그 AI만 다른 계약을 읽는다 — 이 조항이 막으려는 바로 그 상태다.
- **스킬 문서(`.claude/skills/*`)는 어댑터가 아니다.** 그건 *영역별 작업 헌장*이고 정본은
  각자다. 다만 **헌법과 모순되면 헌법이 이긴다**(권위 순서) — 모순을 발견하면 §9 2단계에서
  그 자리에서 고친다.

### 이 조항이 기대는 세 층 (§7)

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` |
| ② 구조 | 공통 계약을 **한 곳에만** 두고 어댑터는 마커로 심어 받는다 | `scripts/gen-adapters.mjs` |
| ③ 기계 | 「커밋본 == 재생성본」 + 두 어댑터 **글자 단위** 대조 · 미등록 문서/지시문 차단 | `check-adapter-parity` · `check-doc-governance` |

**정직한 한계**: 기계는 **내용의 모순을 못 본다.** 스킬 문서가 헌법과 반대되는 말을 해도
게이트는 조용하다. 그 자리는 **§9 2단계(정독 중 구멍 메우기)**가 유일한 층이고, 그래서
착수 절차에 들어 있다. 게이트가 하는 일은 *"내용이 맞다"의 보증*이 아니라
**"등록되지 않은 지시문이 조용히 생기는 것의 차단"**이다.

## CI 실행 정책 — 어떤 초록이 무엇을 뜻하는가 (§15 · 사용자 결정 2026-08-03, 2026-08-04 개정)

> **일정은 워크플로 파일이 정본이고, 헌법은 *의미*를 정한다.** 이 저장소의 규율은 늘 그
> 형태였다(§2-G SKIP≠PASS · §2-J 안 잰 것을 문제 없음이라 하지 마라 · §2-K 속도로 초록의
> 뜻을 깎지 마라). 언제 도는가는 바뀔 수 있지만, **초록이 무엇을 보증하는가**는 계약이다.

**왜 개정했나(실측)**: 변경 1건마다 harness가 여러 번 돌았고, 문서·작은 기능마다 곧바로
머지·배포하면 검사 비용과 배포 횟수만 늘어난다. 하네스는 편집 루프가 아니라 **릴리스 판정**이며,
머지·배포와 떨어뜨리면 그 초록이 낡는다. 이 저장소는 Actions 한도 초과로 배포가 막힌 적도 있다.

### 일곱 규칙

| # | 규칙 | 구현 | 상태 |
|---|---|---|---|
| ① | 작업·축적 중(**Draft PR**)에는 **빠른 차선만** 돈다 | `ci.yml`의 `fast-gates` job | 유지 |
| ② | 문서 단독 변경은 Ready·버전·build·harness·merge·deploy로 보내지 않고 기능 릴리스에 묶는다 | 사람·AI의 릴리스 판단 | 개정 |
| ③ | 기능 변경도 기본은 모으고, 긴급 사유가 있을 때만 단독 릴리스한다 | 위 「변경 축적과 릴리스 묶음」 | 신설 |
| ④ | **Ready 전환** 시 앱을 먼저 build하고 그 커밋에서 전체 Required를 잰다 | `harness` job의 `build → harness`, `live-render`의 `build → live` | 개정 |
| ⑤ | 새 커밋이 올라오면 **낡은 진행 중 검사는 자동 취소** | `concurrency: cancel-in-progress: true` | 유지 |
| ⑥ | 그 초록을 확인한 커밋만 머지하고 바로 배포한다 | `harness → merge → deploy` 한 묶음 | 신설 |
| ⑦ | main 배포에서 하네스를 다시 돌리지 않는다 | `deploy-pages`는 build·시크릿 검사·배포만 | 개정 |

### 🔴 각 초록의 뜻 — 여기가 이 조항의 본론이다

- **`fast-gates`의 초록은 「통과」가 아니다.** `npm run gates`가 스스로 *"재본 N개 통과 ·
  **M개는 아예 안 쟀습니다**"*라고 말하고 안 잰 이름을 나열한다(§2-K). **병합 근거가 될 수 없다.**
- **`harness`+`live-render`의 초록만이 병합 근거다.** 그리고 그것은 **그 커밋**에 대한 것이다 —
  이후 새 커밋이 올라오면 그 초록은 옛 커밋의 것이다(③이 취소하는 이유).
- **main의 초록은 `deploy-pages`가 준다.** main에서는 전체 하네스를 다시 재지 않고, PR 머리의
  build·harness·live-render 초록을 신뢰한다. 배포 job은 운영 변수로 산출물을 다시 만들고
  시크릿 검사 뒤 올린다. base 이동이나 운영 변수 차이로 PR 머리와 달라지는 좁은 창은 배포 후
  실기기가 첫 검출자다. 이 대가를 알고 중복 하네스를 없앤다.
- **「완료 = 배포 그린 확인」은 그대로다**(M-0031). 중복 제거는 확인 면제가 아니다.

### 🔴 정직한 한계 — 기계가 막아 주지 않는 자리

**이 저장소의 `main`에는 브랜치 보호가 없다**(실측 2026-08-03 `protected: false`).
따라서 *"Required가 pending이라 병합이 막힌다"*는 기계적 보장은 **없다** — Draft인 채로,
또는 전체 검사 없이도 병합이 물리적으로 가능하다. 그 자리를 지키는 것은 이 조항과
**병합 전 확인**뿐이다.

> **병합 규칙(사람·AI 공통)**: 릴리스하기로 결정한 뒤 앱을 build하고, **그 커밋에서**
> `harness`와 `live-render`가 둘 다 success인지 눈으로 확인한 다음 머지하고 바로 배포한다.
> `fast-gates`만 초록인 상태, 문서 단독 변경, 아직 모으는 기능은 병합하지 않는다.

보호 규칙을 켜게 되면 **skipped를 성공으로 세는 함정**을 함께 점검해야 한다 — GitHub은
`if:`로 건너뛴 Required check를 성공으로 취급할 수 있고, 그러면 Draft로 두는 것만으로
검사를 우회하게 된다(§2-G가 CI 층에서 재현되는 형태).

### 기계 층

`check-ci-policy`가 워크플로가 이 조항에서 조용히 벗어나는 것을 막는다: ①`ci.yml`이
main에서 돌지 않는가 ②전체 검사가 draft에서 제외되는가 ③`concurrency` 취소가 켜져 있는가
④Ready PR이 build→harness 순서인가 ⑤`deploy-pages`가 harness를 다시 돌리지 않고 build는
유지하는가. **정직한 한계**: 이 게이트는 *파일이 그렇게
적혀 있다*까지만 안다 — GitHub이 실제로 그 일정대로 도는지는 정적으로 볼 수 없다(§2-J ②).

## 모순 검사 — 코딩할 때마다 (§17 — 사용자 지시 2026-08-06)

> *"기본적으로 코딩할 때 **모순검사**를 필수적으로 하게 헌법에 박아두는 건 어떨까요?"*

**왜**: 이 저장소가 사용자 실기기에서 잡힌 결함을 되짚으면 **가장 많은 것이 모순**이었다.
자료구조는 옳고, 계산도 맞고, **두 문장이 서로 어긋났다.**

| 실측 | 한 곳이 말한 것 | 다른 곳이 말한 것 |
|---|---|---|
| M-0113 | 「저장 용량을 **알려주지 않아요**」 | 바로 아래 줄 「**사용 0%**」 |
| M-0113 | 「서버에 **물어보지 못했어요**」 | 서버는 정상 응답했고 LTE도 켜져 있었다 |
| M-0048 | 태블릿 「자료는 **안전합니다**」 | 같은 자료를 두고 폴드5 「**사본이 없습니다**」 |
| M-0046 | 「소리 자체는 **없습니다**」 | 로컬 휴지통에 **그대로 있었다** |
| M-0022 | (숫자를 정확히 계산함) | **화면에는 안 알렸다** |

**공통 형태**: 각 조각은 자기 자리에서 참이다. 그래서 **유닛이 전부 통과한다** — 유닛은 함수
하나만 보고, 모순은 **둘이 만나는 자리**에서만 보인다. 그 자리는 대개 **화면**이다.

### 무엇을 대조하는가 — 네 축

착수 전 5W1H(§0)와 형제 목록(§7) 옆에 **이 네 가지를 함께 묻는다.**

1. **판정문 ↔ 그 아래 값.** 「모른다」고 했는데 숫자가 있는가? 「같습니다」인데 무엇이 같은지
   안 적었는가(§7-C 한정 생략)? 「없습니다」인데 찾아는 봤는가(M-0046)?
2. **화면 ↔ 세계.** 「연결이 돌아오면」이라고 하는데 **연결은 멀쩡한가**? 「설치하세요」인데
   **이미 설치돼 있는가**? 🔴 앱이 **이미 아는 값**을 판정에 안 쓰고 있으면 그게 모순의 씨앗이다.
3. **기기 ↔ 기기 / 표면 ↔ 표면.** 같은 자료·같은 계정인데 화면이 정반대로 말하는가(M-0048)?
   APK·설치된 PWA·브라우저 탭에서 **같은 문장이 다 참인가**?
4. **문서 ↔ 코드.** 지시문이 시키는 절차가 훅·게이트에 **실제로 막히지 않는가**(M-0112)?
   주석이 말하는 전제가 아직 참인가(M-0006의 화석 주석)?

### 어떻게 재는가 — 이유 없이 「봤다」고 하지 않는다

- **같은 화면에 나가는 두 문장은 같은 검사에서 함께 읽는다.** 유닛으로는 원리적으로 못 본다.
  이 저장소의 자리는 `verify-*-live`이고, **화면 전체 텍스트에 두 조건을 동시에 걸어** 잡는다:
  ```js
  const saysNoQuota = /저장 용량을 알려주지 않아요/.test(text);
  const showsPct    = /사용 \d+%/.test(text);
  check('판정문과 아래 줄이 모순되지 않는다', !(saysNoQuota && showsPct));
  ```
- 🔴 **그 검사가 「모순이 날 수 있는 자리」에서 도는지 물어라.** 갈래가 표면(셸·PWA·탭)에
  달렸으면 **그 표면을 만들어서** 재야 한다 — 안 그러면 초록은 「없다」가 아니라 **「안 봤다」**이다.
  실측: 모순 검사를 넣고도 헤드리스가 늘 브라우저 탭이라 **문제가 날 수 없는 자리에서 통과**했다.
  `window.Capacitor`를 심어 앱이 자기를 셸이라 믿게 만들고 실제 렌더러로 그렸다(M-0113).
- **모순을 만들 수 없게 만드는 것이 검사보다 낫다**(§7 2층). 두 문장이 **한 값에서 파생**되면
  모순이 구조적으로 불가능하다 — `levelFromMetrics`가 그 형태이고, 판정문을 등급이 아니라
  **이유**에서 만든 것(`storageHeadline`)도 같은 수다.

### 이 조항이 기대는 세 층 (§7)

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터 |
| ② 구조 | 두 문장을 **한 값에서 파생**시켜 모순을 불가능하게 | `levelFromMetrics` · `storageHeadline` · 유니온 타입 |
| ③ 기계 | `npm run brief` ⑨가 착수마다 네 축을 묻는다 · 라이브 검사가 화면 전체에서 대조 | `scripts/brief.mjs` · `verify-*-live.mjs` |

**정직한 한계**: 기계는 *"두 문장이 뜻으로 어긋난다"*를 일반적으로 못 본다 — 잡히는 것은
**내가 짝을 지어 준 쌍**뿐이다. 그래서 ①이 묻고 ②가 줄이고 ③은 **이미 아는 쌍의 재발**만 막는다.
새 쌍을 찾는 일은 여전히 사람 몫이고, 이 앱에서 그 검출기는 **사용자의 실기기 화면**이다(§10).

## 미완성 작업을 발견했을 때 (§16 — 사용자 지시 2026-08-06)

> *"어차피 내가 지시한거야. 미완성작업 마무리 안 했다면 **내게 질문해서 마무리 지어야 해**."*
> 그리고: *"**이건 어떤 인공지능이 봐도 수행할 수 있게** 헌법에 명시해주세요."*

**왜 헌법인가**: 이 규율이 스킬 문서(`gates-mechanization-dev`)에만 있었다. 그 문서는
게이트·프롬프트를 고칠 때만 로드되므로, **화면을 고치러 온 AI는 영원히 안 읽는다.**
그런데 미완성은 영역을 가리지 않고 생긴다 — 그러니 **모든 AI가 읽는 자리**에 있어야 한다.

**무엇이 미완성인가**: 커밋 안 된 작업트리 · 푸시 안 된 커밋 · 열린 Draft PR ·
`origin/main`에 없는 브랜치 커밋. 남이 남겼든 내가 남겼든 같다.

**언제 도는가**: **세션을 시작할 때 한 번, 닫기 전에 한 번.** 둘 다 필수다.
🔴 **이 저장소는 squash 머지라 브랜치를 `origin/main`에서 다시 세우는 것이 정례다**
— 그 순간 안 물어본 미완성은 **소리 없이 사라진다.**

### ① 찾는다 — 인상이 아니라 명령으로

```bash
git status --porcelain                      # 커밋 안 된 작업트리
git stash list                              # 숨겨 둔 작업
git log --oneline origin/main..HEAD         # 푸시·머지 안 된 내 커밋
git branch -r --format='%(refname:short)'   # 남이 두고 간 브랜치
#   각 브랜치마다: git log --oneline origin/main..origin/<브랜치>
#   그 커밋이 **내용까지** 남았는지는 diff로 본다(squash 머지면 커밋만 남고 내용은 main에 있다):
#   git diff --stat origin/main origin/<브랜치> -- src scripts docs
```

**판독**: 삭제가 삽입보다 압도적이면 그 브랜치는 main보다 **뒤**다(= 이미 흡수됨).
확신이 안 서면 그 브랜치에만 있는 파일을 하나 골라 main에 있는지 본다.

### ② 보존한다 — 지우지도, 완성인 척 커밋하지도 않는다

```bash
git add -A && git commit -m "chore(wip): <무엇을 하다 말았는지> — 미완성 · 머지 금지"
git push -u origin <브랜치>
```

🔴 **`wip:` 접두사는 쓸 수 없다** — `.githooks/commit-msg`의 허용 type
(`feat|fix|security|refactor|test|docs|build|chore`)에 없어 **커밋이 거부된다.**
반드시 **`chore(wip):`** 형태로 쓰고, 본문 또는 제목에 **「미완성 · 머지 금지」**를 넣는다
(훅이 이 표식을 강제한다 — 없으면 RED).

🔴 **작업트리는 인계 수단이 아니다.** 컨테이너가 재활용되면 사라진다. 푸시까지 해야 보존이다.

### ③ 잰다 — "거의 다 된 것 같다"는 판단이 아니라 인상이다

두 층으로 가른다. 층마다 뜻이 다르다.

| 명령 | 통과하면 | 실패하면 |
|---|---|---|
| `npx vitest run <새 모듈>` | 순수 로직은 섰다 | 설계가 아직 안 섰다 — 원저자 의도를 먼저 읽어라 |
| `npx tsc --noEmit` | 배선도 섰다 | **배선만 미완이다**(가장 흔하다) |

실측(M-0111): 유닛 16건 통과 · `tsc` 3곳 실패 → *배선만* 남은 상태였다.
그 3곳의 정체는 **인자 하나를 지우면서 호출부 8곳 중 5곳만 고친 것** — §7의 최빈형이다.
**인자·필드를 지우는 정리는 컴파일러가 형제를 전수로 뽑아 주는 자리다. 손으로 세지 마라.**

### ④ 🔴 묻는다 — 이 단계를 건너뛰면 ①~③이 전부 무의미하다

> *"작업트리에 미완성 X가 있습니다(유닛 N건 통과 · `tsc` M곳 실패 = 배선만 미완).
>  이어서 마무리할까요?"*

- **저장소에 있는 미완성은 예외 없이 사용자 지시에서 나온 것이다.** 남의 작업이라 조심할
  이유는 있어도 **주인이 없는 작업은 아니다.** 판단할 사람은 언제나 사용자다.
- 「안전하게 세워 뒀다」를 **「방치했다」의 다른 이름**으로 쓰지 마라.
- 🔴 **미완성을 발견했는데 안 물었다면, 그건 내가 만든 미완성이다.**

### ⑤ 맡으면 릴리스까지 간다

배선만 고치고 커밋해 두는 것은 **또 하나의 미완성**이다. 「완료의 정의」를 그대로 탄다:
버전·CHANGELOG → build → 전체 harness → 화면 열어 보기(§13) → 머지 → **배포 그린 확인**.

### ⑥ 설계 판단이 이미 코드에 적혀 있으면 다시 하지 않는다

이 저장소는 *왜 그렇게 했는지*를 머리말에 적는다. 이어받는 사람이 설계를 다시 고르면
**원저자의 근거가 조용히 사라지고**, 그 근거는 대개 사용자와의 대화에서 나온 것이라
다시 만들 수 없다. 이어받는 사람의 일은 보통 **배선 마무리와 릴리스 절차**다.

### 이 조항이 기대는 세 층 (§7)

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터에 자동 심김 |
| ② 구조 | `npm run brief`가 **착수마다** 미완성을 세어 보여주고 ④를 상기시킨다 | `scripts/brief.mjs` ⑧ |
| ③ 기계 | `chore(wip):` 커밋에 「머지 금지」 표식이 없으면 **커밋 자체가 거부**된다 | `.githooks/commit-msg` |

**정직한 한계**: 기계는 *"물었는가"*를 못 본다 — 그건 대화이지 파일이 아니다. 그래서
③은 **보존의 형식**만 지키고, ④는 이 조항과 ②의 상기뿐이다. 그 대신 ②를 착수 절차(§9)에
붙여 **안 읽고는 일을 시작할 수 없게** 했다.

## 하네스·릴리스 법 (§18 — 사용자 지시 2026-08-06 · 외부 문서 「Harness and Release Law」 검토 도입)

> 사용자가 외부 프롬프트(HRL v1.1 · 모델 무관)를 가져와 *"우리 앱에도 적극 도입 검토하자"*고 했다.
> 조문별로 대조해 보니 **절반 이상은 이 헌법에 이미 있었다.** 그래서 **복사하지 않는다** —
> §2가 말하듯 **손편집 중복 자체가 결함**이고, 복사하면 정본이 둘이 되어 갈라진다.
>
> **여기에는 우리에게 비어 있던 것만** 적고, 이미 있는 것은 **가리키기만** 한다.

### 이미 있는 것 (여기 다시 적지 않는다)

| HRL 조문 | 우리 자리 |
|---|---|
| 검사 목록 원장 하나 | `registry.gen.ts` · `check-registry-gen`(§7 2층) |
| 못 잰 것은 통과가 아니다 | **§15** 「각 초록의 뜻」 · `gates`가 안 잰 이름을 나열 |
| 주입증명 | **§4** · **§11**(게이트도 검사받는다) |
| 버전 산술 · 변경이력 | **§15** 「변경 축적과 릴리스 묶음」 · `check-doc-counts` |
| 묶기 | **§15** ②③ |
| 초록은 증명이 아니다 · 머지 ≠ 배포 | **§15** · **「완료의 정의」** |

### §18-A. 🔴 검증 명령을 파이프에 넣지 않는다 — 판정은 **종료코드**로

```bash
npm run harness 2>&1 | grep -E "FAIL"        # ✗ 종료코드는 grep의 것이다
npm run harness > /tmp/r.log 2>&1; echo "EXIT=$?"; tail -20 /tmp/r.log   # ✓
npm run harness && echo OK || echo FAILED                                 # ✓ 종료코드를 본다
```

**왜 이 조문이 첫 번째인가**: 이 저장소가 **실제로 가장 많이 어긴 것**이기 때문이다.
2026-08-06 세션에서 이 형태를 수십 번 썼다 — 출력 문자열을 눈으로 읽어서 살았지,
**종료코드로 판정한 적이 없다.** 초록을 보고 싶은 사람과 초록을 만들어 주는 파이프가 만나면
그 검사는 정보가 없다.

**기계 층**: 게이트로는 원리적으로 못 잡는다(파일이 아니라 **내가 치는 명령**에서 깨진다) →
**PreToolUse(Bash) 훅**이 차단한다(`scripts/hook-verify-exit.mjs` · S-09). 자체검사 양성 5·음성 6.

**대리 지표 금지도 같은 조문이다.** *"X가 끝나면 Y가 생긴다"*가 참이어도 **"Y가 생기면 X가 끝났다"**는
거짓일 수 있다. 역이 성립하지 않는 신호로 완료를 판정하지 마라 — **결론을 직접 물어라**
(워크플로면 run conclusion, 스크립트면 종료코드).

### §18-B. 재실행 범위 — 실패한 그룹만, 그리고 **그 검사가 이번 변경을 보는가**

- 검사 하나가 빨간불이면 **그 검사가 속한 그룹만** 다시 돈다. 멀쩡히 통과한 그룹을
  「불안해서」 다시 돌리지 않는다.
- 🔴 **재실행 전에 물어라**: *이 검사가 이번 변경을 볼 수 있는가?* 바꾼 심볼·파일로 검사 소스를
  `grep`해서 **0건이면 모집단이 0**이고, 나올 초록은 「위반 없음」이 아니라 **「아무것도 안 봄」**이다.
  이건 §17의 *"그 검사가 「문제가 날 수 있는 자리」에서 도는지 물어라"*와 같은 질문이다.
- 우리 가장 비싼 그룹은 `verify-*-live`(브라우저)다. **그 층이 빨간불일 때만** 다시 돈다.

### §18-C. 병렬이 기본 — 직렬은 **의존이 있을 때만**

낭비되는 시간의 대부분은 대기가 아니라 **병렬로 할 수 있는 것을 줄 세운 것**이다.

이 원칙은 조사·검사·배포뿐 아니라 **앱 런타임 오케스트레이션과 동기화 도메인**에도 그대로
적용한다. 같은 단계의 작업이 서로의 결과를 입력으로 쓰지 않으면 병렬이 기본이다.

**직렬 판정 기준(이것만)**: 앞의 결과가 뒤의 입력인가? 순서가 바뀌면 뒤의 증거가 무효가 되는가?

```
소스·버전 확정 → build        번들이 그 소스를 품는다
build → harness·live          증거는 그 번들을 포함한 트리에서 나와야 한다
CI 초록 → 머지 → 배포
```

**그 밖에서 「앞의 것이 끝나야 안심된다」로 줄 세우는 것은 의존이 아니라 습관이다.**
병렬로 해야 하는 것: 조사·탐색 · 리뷰 렌즈 · 서로 다른 파일을 보는 검사 · 독립 배포 표면.

🔴 **직렬 경계에는 구체적 사유가 필수다.** `안전을 위해`·`기존 순서라서`·`한 번에 하나씩`은
사유가 아니다. 다음 둘 중 하나를 **정확한 값·자원·증거 이름으로** 적는다.

1. 앞 단계의 어떤 출력이 뒤 단계의 어떤 입력이 되는가.
2. 어떤 공유 자원이나 read-back 증거가 겹쳐, 순서를 바꾸면 무엇이 무효가 되는가.

코드 오케스트레이션은 가능하면 병렬 그룹과 직렬 간선을 **타입 있는 실행 계획 데이터**로 둔다.
첫 단계 이후의 각 간선은 `reason` 같은 필수 필드를 가져야 하며, 새 형제가 들어올 때 병렬 그룹과
의존성을 선택하지 않으면 컴파일·게이트가 실패해야 한다. 산문 주석만으로 줄 세우지 않는다.

🔴 **병렬 실패도 합류(join) 뒤에 보고한다.** 부수효과가 있는 형제 중 하나가 실패했다고 즉시
반환하면 나머지 쓰기는 뒤에서 계속되어 다음 단계·다음 실행과 겹친다. 같은 단계의 모든 형제를
성공/실패로 정산한 뒤 실패를 반환한다. 조기 reject를 완료 경계로 쓰지 않는다.

병렬 전 확인 셋: ①**같은 자원을 쓰는가**(겹치면 병렬이 아니라 직렬이거나 격리)
②**결과를 합칠 자리와 실패 처리를 먼저 정했는가** ③**침묵을 관측할 수 있는가**
(못 하면 대리 지표에 속는다 — §18-A).

PR과 완료 보고에는 **병렬 그룹**과 **직렬 간선+각 사유**를 적고, 요약은
**「직렬 N구간 + 병렬 M구간」**으로 쓴다. 「28분 걸렸다」는 정보가 없다.

### §18-D. 자동화도 하나의 표면이다

러너·크론·백그라운드 잡은 **사람과 같은 권한으로 같은 브랜치·파일**을 쓴다.
맡긴 동안 그 자원을 건드리지 마라 — 급하면 끝난 뒤에 한다.

이 저장소에 걸리는 자리: **APK 워크플로가 도는 동안 같은 브랜치에 푸시하지 않는다**
(러너가 `[rejected] fetch first`로 죽으면 정상적으로 만들어진 산출물이 통째로 버려진다).

🔴 **규칙의 범위를 예시로만 적지 마라 — 예시 밖은 지켜지지 않는다.**

### §18-E. 산출물이 품는 소스가 바뀌면 산출물을 다시 만든다

**패키징 산출물은 소스 확정이 끝난 뒤에 만든다.** 만든 뒤 소스를 또 바꾸면 해시·버전이 낡는다.

```
소스·생성물·버전·이력 확정 → 좁은 검사 → 커밋 → 산출물 빌드 → 전체 검사 한 번 → 머지 → 배포
```

⚠️ **문서만 고쳐도 소스가 바뀔 수 있다** — 생성물(문서 목록·버전·레지스트리)이 소스 안에 박혀
있기 때문이다. 판정 기준은 「거동이 바뀌었나」가 아니라 **「그 파일이 실제로 바뀌었나」**이고,
**기계에게 묻는다.**

**우리 형태**: 이 저장소는 APK를 **커밋하지 않고** 고정 릴리스에 올리므로, 원문의
`check-release-order.mjs`(산출물 커밋 뒤 소스 커밋 검출)를 그대로 쓸 대상이 없다.
같은 근본형은 이미 세 곳이 지킨다 — `verify-*-live`의 **dist 낡음 차단(exit 2)** ·
`gen-registry` · `check-adapter-parity`. **비어 있는 것은 APK 쪽**이고, 그건
`android-apk-dev` 헌장이 다룬다.

### §18-F. 🔴 앱과 서버가 따로 배포되면 **제공자를 하위 호환으로 먼저 배포하고 정확한 실물을 되읽는다**

앱이 기대하는 Edge Function·DB capability·외부 저장소 계약이 바뀌면, 앱과 서버를 같은
릴리스라고 불러도 **실제로는 서로 다른 시각에 배포되는 두 표면**이다. `FN_VERSION=6` 같은
프로토콜 번호는 *무엇을 할 줄 아는가*를 말할 뿐, **오늘 고친 소스가 운영에 올라갔는가**를
증명하지 않는다. 같은 버전 안의 수정·재시도·timeout·CORS·진단 필드도 배포 누락될 수 있다.

그래서 소비자 앱보다 제공자 표면을 먼저 배포하는 릴리스는 다음 순서를 지킨다.

1. **정본과 생성물을 먼저 고정한다.** 프로토콜·필수 연산·논리 소스 해시의 정본을 하나만 두고,
   Edge 상수와 앱 계약은 거기서 생성한다. 생성 뒤 Edge 본문이 바뀌면 해시부터 다시 만든다.
2. **새 제공자는 구형 앱을 계속 받아야 한다.** Edge 선배포 창에서 현재 운영 앱의 요청이 깨지면
   선배포가 아니라 즉시 장애다. 하위 호환이 불가능한 변경은 capability 세대나 단계적 migration을
   별도로 설계하고 같은 릴리스에 숨기지 않는다.
3. **제공자를 먼저 배포한 뒤 운영 응답을 직접 읽는다.** 배포 명령 성공이나 함수 revision 증가는
   대리 지표다(§18-A). 운영 `capabilities`의 **정확한 소스 해시**, 프로토콜, 필수 연산, 필요한
   시크릿 준비 상태를 릴리스 계약과 대조한다.
4. **운영 대조가 통과한 뒤에만 Ready PR·앱 배포를 연다.** 불일치·조회 실패·환경변수 부재는
   `unknown` 또는 실패이지 통과가 아니다. 앱을 먼저 올려 사용자 기기에서 시험하거나 사용자
   자료를 고쳐서 확인하지 않는다.
5. **앱 배포 뒤 두 표면을 다시 되읽는다.** Pages/APK 버전과 서버 계약이 모두 기대값이어야
   완료다. 하나만 초록이면 §15가 말하는 배포 완료가 아니다.

**현재 적용 표면 — `media-sign`.** `schemas/sync-release-contract.json`이 protocol·필수 op·
LF 정규화 논리 소스 SHA-256의 정본이다. `check-sync-release-contract`는 정본↔Edge↔앱 생성물을,
`verify-sync-release-live`는 운영 `capabilities`를 대조한다. Ready PR의 Required harness와 Pages
배포가 같은 운영 대조를 통과해야 한다. 기기의 「동기화 출고 점검」은 같은 계약을 읽기 전용으로
판정하며, 사용자 자료를 수정·삭제하는 복구 버튼을 갖지 않는다.

새 독립 서버 표면이 생기면 `schemas/release-profile.json`에 영향 조건·배포 주체·권위 있는
read-back을 등록하고, 위와 동등한 **정적 동일성 검사 + 운영 실물 검사**를 갖추기 전에는
앱 릴리스의 전제라고 부르지 않는다.

### §18-G. 🔴 게이트는 **자기가 사는 세계**를 가정하지 않는다 — 없는 세계에서 한 번 돌려 본다

> **실측(2026-08-09 · M-0135)**: 로컬 하네스가 **전부 초록**인 커밋이 CI에서 죽었다.
> 범인은 그날 내가 만든 게이트였고, 하는 일은 한 줄이었다 — `git diff origin/main...HEAD`.
> 로컬에는 `origin/main`이 있다. `actions/checkout`의 얕은 체크아웃에는 **없다.**

**왜 조항이어야 하나**: 🔴 **로컬 하네스는 이 부류를 원리적으로 못 잡는다 — 자기가 사는
세계에서 도니까.** 그래서 「조심하자」로는 절대 안 지켜지고, 검출은 언제나 CI가 한다.
그런데 CI에서 잡히면 이미 릴리스 묶음 한복판이라 §18-H의 값까지 함께 치른다.

**정확히 무엇이 결함이었나 — 죽음을 위반으로 보고했다.** 이 저장소의 계약은 이미 셋이다:

```
exit 0 = 통과      exit 1 = 재보고 위반을 찾음      exit 2 = 전제 미충족(SKIP)
```

전제가 없을 때 게이트가 **예외를 스택째 뱉으면** 종료코드는 1이 되고, 하네스는 그것을
「위반을 찾았다」로 적는다. 그 문장은 거짓이다 — 게이트는 판정한 게 아니라 **죽었다**.
`unknown`을 통과로 반올림하지 않는 규율(§8)의 **거울상**이다: 모르는 것을 *위반*으로도
반올림하지 않는다.

**규칙.**

1. **전제가 없으면 판정하지 말고 말한다.** 없는 세계를 만나면 `exit 2`(SKIP)로 나가거나,
   그 창만 빼고 판정하고 「확인 불가: …는 재지 못했습니다」를 화면에 적는다.
2. **게이트마다 가정하는 세계를 원장에 적는다.** 어휘는 동결이고(`baseRef`·`dist`·
   `globalSkills`·`network`·`browser`·`runtimeEnv`), **아무것도 가정하지 않으면 빈 문자열을
   적는다** — 빈칸과 「아직 안 봤다」는 다른 말이므로 누락은 RED다.
3. 🔴 **선언은 산문이므로 믿지 않는다 — 벗겨서 돌려 본다.** 얕은 체크아웃과 같은 모양의
   **빈 세계 사본**(작업트리·색인·HEAD는 있고 `origin/*`·`dist/`는 없음)을 만들어 싼 게이트를
   전부 한 번 돌린다. 집에서는 초록인데 거기서 빨간불이면, 그 게이트는 세계를 가정한 것이다.
4. **새 게이트도 예외가 아니다.** 사본은 **추적 파일 + 아직 커밋 안 한 파일**을 함께 옮긴다 —
   이 검사가 가장 필요한 순간이 *방금 만든 게이트*이기 때문이다(M-0135가 그랬다).

**이 조항이 기대는 세 층 (§7)**

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터 |
| ② 구조 | `GATE_WORLD` 원장 — 없는 게이트는 RED이므로 **다음 형제가 자동으로 따라온다** | `src/app/gates.ts` |
| ③ 기계 | **빈 세계 시연** — 매 실행마다 카나리아(`rev-parse origin/main`)로 *세계가 실제로 벗겨졌는지* 먼저 증명하고, 그 위에서 게이트를 돌린다 | `check-env-assumption` |

**정직한 한계**: 벗길 수 있는 세계는 둘(`baseRef`·`dist`)뿐이다. 나머지 넷은
**재지 않았고**, 원장 선언으로만 안다 — 판정문이 그렇게 말한다. 그리고 빈 세계는 게이트가
**죽지 않는지**까지만 본다. 판정 *내용*이 옳은지는 여전히 보지 못한다.

### §18-H. 🔴 릴리스 검증이 시작되면 저장소를 **얼린다** — 다시 여는 것은 결정이고, 결정은 세어진다

> **실측(2026-08-09 · v2.07)**: CI가 네 번 돌았다. 필요한 것은 **한 번**이었다.
>
> | run | 결과 | 커밋 | 시각 |
> |---|---|---|---|
> | #552 | **failure** | 04b3abfb | 18:33 → 18:37 |
> | #553 | **cancelled** | b8ff17fb | 18:41 → 18:43 |
> | #554 | success | 2d672fdc | 18:43 → 18:47 |
> | #555 | success | 06c7d668 | 19:11 → 19:14 |
>
> 검증 자체는 **3~4분**이다. 첫 Ready부터 마지막 초록까지는 **41분**이 걸렸다.
> 사용자가 물었다 — *"왜 이렇게 배포가 길어지니?"* 답은 CI가 아니라 **나**였다.
> #552는 §18-G가 막을 결함이고, **#553·#555는 순전히 내가 검증 중에 커밋을 얹어서 생겼다.**

**왜 조항이 필요한가 — 값이 안 보였다.** §15가 이미 *"하네스 뒤 코드를 바꾸면 묶음이
깨진다"*고 적어 뒀다. 그런데 조항은 **커밋을 치는 순간에 아무 말도 하지 않는다.**
그리고 `concurrency: cancel-in-progress`가 켜져 있으므로, 「커밋 하나 더」의 값은
커밋 하나가 아니라 **돌고 있는 검증을 버리고 처음부터**다. 안 보이는 값은 계산되지 않는다.

**규칙.**

1. **릴리스 검증을 시작할 때 얼린다**(`npm run release:arm <후보>`). 얼어 있는 동안
   **새 이력을 만드는 것**(`commit`·`merge`·`rebase`·`cherry-pick`·`revert`)이 막힌다.
   **조사·읽기·후보 push는 자유다** — 막으면 사람이 장치를 꺼 버리고, 꺼진 장치는 없는
   장치다. 🔴 첫 실사용에서 `push`까지 막았다가 **릴리스 후보를 올리는 일 자체가 막혔다**
   (v2.08). 규율이 막으려는 것은 push가 아니라 **검증 뒤에 생긴 새 커밋**이고, 커밋이
   막혀 있는 동안 push가 나를 수 있는 것은 이미 검증된 이력뿐이다.
2. **다시 여는 것은 금지가 아니라 기록이다**(`npm run release:reopen -- "<이유>"`).
   빨간 게이트를 고치려고 다시 여는 것은 **정당하다.** 다만 이유를 적고, 그 순간
   묶음은 「다시 열렸다」로 **세어진다**. 이유 없는 예외를 막고 세는 것은 이 저장소가
   이미 쓰는 형태다(§7의 `NO_OP_REQUIRED`).
3. **급하지 않은 변경은 배포 확인 뒤에 따로 올린다.** 후속과제·문서 정리가 여기 해당한다 —
   v2.07에서 CI를 두 번 더 돌린 것이 정확히 이 부류였다.
4. **완료 보고에 재개방 횟수를 적는다.** 「41분 걸렸다」는 정보가 없다.
   **「검증 1회 + 재개방 N회」**가 정보다(§18-C의 「직렬 N구간 + 병렬 M구간」과 같은 규율).
5. **묶음을 닫는 것은 배포 그린 확인 뒤다**(`npm run release:close`). 머지가 아니라 배포다
   (「완료의 정의」).

**이 조항이 기대는 세 층 (§7)**

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터 |
| ② 구조 | 묶음의 경계를 **물건으로** 만든다 — 상태와 재개방 이유가 파일에 남는다 | `scripts/release-lock.mjs` |
| ③ 기계 | **PreToolUse(Bash) 훅이 얼어 있는 동안 커밋을 차단**하고, 막힐 때 그 값(취소·재시작)을 화면에 적는다 | `scripts/hook-release-freeze.mjs` · `check-hooks-wired` |

**정직한 한계**: 이 잠금은 **AI의 명령**만 막는다(PreToolUse 훅). 사람이 터미널에서 직접
치는 것은 보지 못한다. 그리고 잊고 넘어간 잠금이 다음 사람을 영원히 막지 않도록
**3시간이 지나면 스스로 풀린다** — 판정 불가는 통과시킨다는 다른 훅들과 같은 자세다.

### 그대로 받지 않은 것 — 우리 조건이 다르다

- **「전체 검사는 CI에서, 로컬은 좁게」**: 원문은 CI가 더 싸다는 실측 위에 서 있다. 이 저장소는
  **Actions 한도 초과로 배포가 막힌 적이 있어**(§15) 로컬이 더 싼 자리일 수 있다. 그래서
  「기본은 CI」를 받지 않고 **§15의 릴리스 묶음**을 유지한다. 다만 **같은 커밋을 세 번 재지 않는다**는
  원문의 핵심은 §15가 이미 말한다.

### 이 조항이 기대는 세 층 (§7)

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터 |
| ② 구조 | 파이프 대신 로그+`EXIT=$?`가 기본 관용구 | 검증 명령을 적는 모든 자리 |
| ③ 기계 | **PreToolUse(Bash) 훅이 파이프를 차단**하고, 독립 서버 표면은 정적·운영 대조가 앱 릴리스를 차단한다 | `scripts/hook-verify-exit.mjs` · `check-sync-release-contract` · `verify-sync-release-live` |

**정직한 한계**: 훅은 **파이프 형태**만 본다. 일반 코드 전부에서 「병렬로 할 수 있는 것을
줄 세웠는가」는 여전히 판단 영역이다. 다만 동기화 오케스트레이션은 `syncPlan.ts`의 실행 계획과
`check-sync-parallelism`이 push 3단계·pull 1단계·직렬 사유·join-all을 기계로 잠근다.

## 사용자 대면 언어 — 한국어로만 (§19 — 사용자 지시 2026-08-12)

> *"모든 진행답변은 한국어로만..영어, 한자, 중국어는 절대 엄격히 금지.
> 특히 중국어는 내가 이 계정 쓰는 한 영구히 금지"*
>
> 범위는 같은 사용자가 곧바로 좁혀 줬다: *"코딩을 위한 명령어 이런건 제외죠. 나한테 설명하는 것들만"*

### 무엇이 대상인가

**사용자에게 가는 산문 전부** — 설명·보고·질문·요약·판정 문장. 한국어로만 쓴다.
영어 문장·한자·중국어를 섞지 않는다.
🔴 **중국어는 이 계정을 쓰는 한 영구 금지다** — 기한이 붙은 규칙이 아니므로 다음 세션에서 다시 묻지 마라.

### 무엇이 대상이 아닌가 — 기술 식별자는 번역하지 않는다

바꾸면 **실행되지 않거나 저장소에서 찾을 수 없는 것**은 원래 표기 그대로 둔다.

| 그대로 두는 것 | 예 |
|---|---|
| 명령어 | `npm run gates` · `git status --porcelain` |
| 파일·경로 | `docs/BACKLOG.md` · `src/app/changelog.ts` |
| 코드 심볼 | `levelFromMetrics` · `deleted_at` · `NO_OP_REQUIRED` |
| 식별자 | 과제 T-019 · 실수 M-0135 · 결정 ADR-0060 · 커밋 `84d8bb2` |
| 원문 인용 | 오류 메시지·로그·게이트 출력 — 번역하면 검색이 안 된다 |

**코드·주석·커밋 메시지·문서 본문도 이 조항의 대상이 아니다.** 특히 커밋 메시지는
`.githooks/commit-msg`가 `feat|fix|security|refactor|test|docs|build|chore` 접두사를 강제하므로
형식을 바꾸면 **커밋 자체가 거부된다**. 이 저장소의 기존 관행을 유지한다.

### 이 조항이 기대는 층 (§7) — 그리고 왜 한 층뿐인가

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터에 자동 심김 |
| ② 구조 | **없다** — 아래 「정직한 한계」 |
| ③ 기계 | **없다** — 아래 「정직한 한계」 |

🔴 **정직한 한계**: 게이트는 **파일**을 본다. 그런데 이 조항이 지배하는 것은 **대화 출력**이고
그것은 저장소에 없다 — 어떤 검사도 잴 수 없다. §16이 *"기계는 「물었는가」를 못 본다"*고
적은 것과 같은 부류다. 그래서 이 조항에는 **읽고 지키는 것 말고 다른 층이 없다.**
그 대신 정본 하나에 적어 두 AI가 **같은 문장**을 읽게 했다(§14).
어긴 것을 잡는 유일한 검출기는 **사용자**다.

## 무엇이 좋아졌는지 쉬운 말로 설명한다 (§20 — 사용자 지시 2026-08-12)

> *"완료되면 어떻게 기능이 좋아진거지 초등학생도 이해하기 쉽게 설명바랍니다.
> 모든 기능을 업데이트하고 나면 어떻게 좋아졌는지 쉬운 언어로 설명하도록"*

### 규칙

기능 변경·업데이트를 마치면 기술 보고와 **별도로** 「무엇이 좋아졌는가」를 쉬운 말로 적는다.
기술 보고를 쉽게 고쳐 쓰는 것이 아니라, **한 절을 따로 둔다** — 섞으면 결국 둘 다 어려워진다.

1. **초등학생이 읽어도 이해되는 문장**으로 쓴다. 전문 용어를 쓰지 않고, 꼭 필요하면 괄호로 풀어 준다.
2. **앱을 쓰는 사람의 자리**에서 쓴다. 무엇을 고쳤는지가 아니라 **이제 무엇이 달라지는지**를 말한다.
   - ✗ 「로그아웃 잠금을 데이터 관리 화면으로 수평전개했다」
   - ✓ 「이제 로그아웃하면 다른 사람이 내 기록을 못 봐요」
3. 🔴 **좋아진 것이 없으면 없다고 쓴다.** 문서·검사만 바꾼 작업은 사용자 화면이 그대로다.
   그때 「좋아졌다」고 쓰는 것은 비타협 원칙 #4(정직한 완료) 위반이다 —
   **「이번엔 화면이 바뀌지 않았어요」**가 정직한 문장이다.
4. **못 고친 것·남은 것도 같은 쉬운 말로 적는다.** 「미검증」을 기술 용어로만 남기면
   사용자는 그것이 **자기에게** 무슨 뜻인지 모른다. 모르면 판단할 수 없다(§12).

### 어디에 적는가

- **대화 보고** — 기술 보고 끝에 「쉬운 말로」 절을 둔다.
- **사용자 화면** — `src/app/changelog.ts`의 `notes`가 이미 이 규율의 자리다. 그건 개발자 메모가
  아니라 앱의 「개발자 정보」 화면에 **그대로 보이는 사용자 문장**이다. 어려운 말을 적으면 사용자가 읽는다.

### 이 조항이 기대는 층 (§7)

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터 |
| ② 구조 | 사용자 문장이 **한 파일에서만** 나온다 — 화면이 그 파일을 읽으므로 고칠 곳도 읽어 볼 곳도 한 곳이다 | `src/app/changelog.ts` → `aboutApp.ts` |
| ③ 기계 | **없다** — 아래 한계 |

**정직한 한계**: 「이 문장이 쉬운가」는 기계가 판정하지 못한다. §10이 *"이 문장이 사용자에게
이해되는가는 기계가 판정하지 못한다"*고 이미 적은 바로 그 자리다. 검출기는 사용자다.
구조(②)가 하는 일은 *"쉽다"의 보증*이 아니라 **어려운 말이 여러 곳으로 흩어지는 것의 차단**이다.

## 머지 단계에 들어가면 배포를 멈추지 않는다 (§21 — 사용자 지시 2026-08-12)

> *"실패하더라도 배포 강행 알지? 다음판에 고치는거야. 머지단계로 진입하면
> 심각한 데이터 유실 문제 아닌한 무조건 배포 강행"*

**왜**: 이 저장소의 릴리스는 **멈춰 서는 데서 비싸진다.** v2.07은 검증 자체가 3~4분인데 첫
Ready부터 마지막 초록까지 **41분**이 걸렸고(§18-H), 그 대부분은 CI가 아니라 **판단을 다시
여는 시간**이었다. 그리고 이 앱은 사용자 한 사람이 쓰는 개인 여행기록 앱이다 — 완벽한 초록을
기다리는 값이 **고친 것을 사용자 기기에 못 올리는 값**보다 큰 경우가 실제로 있다.

### 언제부터인가 — 「머지 단계 진입」의 정의

**릴리스 후보를 정하고 `npm run release:arm`으로 얼린 뒤, Required CI에 판정을 물은 시점부터**다.
그 전(조사·구현·축적)에는 이 조항이 걸리지 않는다 — 거기서는 여전히 고치고 다시 잰다.

### 규칙

1. **머지 단계에 들어갔으면 빨간 검사가 있어도 머지하고 배포한다.** 남은 결함은 **다음 판**에서 고친다.
2. 🔴 **예외는 둘뿐이다.**
   - **사용자 기억의 유실·손상** — 자료가 지워지거나 덮어써지거나 복구할 수 없게 되는 부류.
     비타협 원칙 #1은 속도와 맞바꿀 대상이 아니다.
   - **§0 절대 위반**(비밀키 노출·RLS 미검증 배포 등). 이건 애초에 릴리스 대상이 아니다.
   - 화면 문구·미관·검사 인프라·환경 특유의 실패는 **예외가 아니다.** 강행한다.
3. 🔴 **강행의 대가는 「기록」이다.** 빨간 채로 배포했으면 **같은 판에서** 그 항목을
   `docs/BACKLOG.md`에 **다음 판 우선 과제로 올린다.** 올리지 않으면 그건 강행이 아니라 **은폐**이고,
   §16이 말하는 「내가 만든 미완성」이 된다.
4. 🔴 **강행은 검사 면제가 아니라 판단의 이전이다.** 완료 보고에 **「하네스 N/M으로 배포함」**과
   빨간 항목의 이름을 적는다. 「통과했다」로 반올림하는 순간 비타협 원칙 #4(정직한 완료)를 어긴다.
5. **그래도 빨간 것이 무엇인지는 본다.** 강행은 *"보지 않아도 된다"*가 아니라
   *"보고 나서, 데이터 유실이 아니면 간다"*이다 — 5초면 갈린다.

### 「완료의 정의」와의 관계

「완료의 정의」는 **전체 하네스 통과**를 머지 조건으로 적고 있다. §21은 **머지 단계에 한해 그
조건을 사용자 판단으로 대체한다.** 나머지 순서(버전 확정 → arm → build → 검증 → 머지 → **배포
그린 확인** → close)는 그대로다 — 특히 **배포 그린 확인은 면제되지 않는다.** 강행은 「검사를
건너뛰는 것」이 아니라 「빨간 검사를 알고 넘어가는 것」이다.

### 이 조항이 기대는 층 (§7)

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터 |
| ② 구조 | 재개방 횟수가 파일에 남아 「멈춰 선 값」이 보인다 | `scripts/release-lock.mjs` |
| ③ 기계 | **없다** — 아래 한계 |

**정직한 한계**: 「이게 데이터 유실급인가」는 기계가 판정하지 못한다. 그건 **결함의 뜻**을 읽는
일이고, 이 저장소에서 그 자리는 언제나 사람이었다(§10의 마지막 층). 그래서 이 조항은
**판단을 없애지 않고 기본값을 바꾼다** — 예전 기본값은 「멈춘다」였고, 이제는 「간다, 단 유실이면 멈춘다」이다.

## 기다릴 때 눈을 감고 있지 않는다 — 30초 (§22 — 사용자 지시 2026-08-12)

> *"30초마다 확인하는거 맞지?? 마냥 대기타는거 아니지?"*
> 그리고 요점을 다시 좁혀 줬다: *"내용을 보지 말라는게 아냐..지금처럼 **잠수**가 문제라는거지..
> 예상이 1분 짜리 ci면 1번 확인이 되겠지만 무슨 사유로 10분 지나면?? 이런 규칙 없음
> 너의 현재 규정으로는 **7분 멍 때리는 거**"*

**왜**: 이 조항이 다루는 것은 「시간이냐 내용이냐」가 **아니다.** 그건 §18-C와 T-016·T-018이
이미 정리했다(고정 대기 126곳 → 0). 여기서 막는 것은 **눈을 감고 있는 구간의 길이**다.
예상 1분짜리에 5분을 자면, 그게 30초 만에 끝났든 10분째 멈춰 있든 **나는 똑같이 모른다.**
그리고 실제로 그랬다 — 2026-08-12 v2.19 배포에서 Pages `deploy` job이 러너를 못 받아
**5분 4초를 대기열에 있었다.** 그때 210초·300초로 자고 있었다면 그 사실 자체를 못 봤다.

### 규칙

1. 🔴 **외부 상태를 기다릴 때 한 번의 대기는 30초를 넘기지 않는다.** CI·배포·러너 배정·
   원격 큐처럼 **내가 못 만드는 상태**가 대상이다. 예상 소요가 얼마든 상관없다 —
   예상은 정상일 때의 값이고, 이 조항은 **비정상일 때를 위해 있다.**
2. **매 확인은 진행 여부를 말한다.** 「아직」이 아니라 **무엇이 아직인지**를 적는다
   (`deploy job 대기열 5분째 · 러너 미배정`). 그래야 멈춘 것과 도는 것이 갈린다.
3. **끝났으면 즉시 다음으로 간다.** 남은 대기를 소진하지 않는다.
4. 🔴 **「계속 확인하겠다」고 말했으면 실제로 그래야 한다.** 말과 행동이 다르면 그건
   사용자에게 가는 **거짓 문장**이고, 비타협 원칙 #4와 §8을 정면으로 어긴다.
   실제로 어겼다 — *"계속 확인하겠다"*고 적어 놓고 210초·300초 통짜로 잤다.
5. **내가 시작한 작업(로컬 빌드·하네스)은 이 조항의 대상이 아니다.** 그건 끝나면 스스로
   알려 주므로 기다림이 아니라 실행이다. 이 조항은 **바깥 세계**를 향한 것이다.

### 이 조항이 기대는 층 (§7)

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 조항 | 이 절 | `docs/CONSTITUTION.md` → 두 어댑터(코덱스도 같은 글자를 읽는다) |
| ② 구조 | **없다** — 아래 한계 |
| ③ 기계 | **없다** — 아래 한계 |

**정직한 한계**: 게이트는 **파일**을 본다. 그런데 이 조항이 지배하는 것은 **내가 대기를 거는
방식**이고 그건 저장소에 남지 않는다 — §19(언어)·§16(물었는가)과 같은 부류다. 없는 검사를
만들어 초록을 띄우면 그 초록의 뜻은 「지켰다」가 아니라 **「안 봤다」**이다. 그래서 층은
하나뿐이고, 어긴 것을 잡는 검출기는 **사용자**다. 실제로 이 조항은 사용자가 두 번 물어서 생겼다.

## 문서 지도 (SSOT)


**`docs/HANDOFF_CODEX.md`(처음 들어온 AI를 위한 완전 인계서 — 맥락 없이도 이어서 일할 수 있게)** · **`docs/HARNESS_RELEASE_LAW.md`(🔴 생성물 — 릴리스 규율 통합본. 정본은 이 파일의 §18·§15·완료의 정의·검증 명령이고, `node scripts/gen-harness-law.mjs`가 심는다. 다른 앱에 이식할 때 건네줄 파일 하나)** · **`docs/모듈별 설계서/README.md`(🔴 생성물 — 실행 코드에서 추출한 모듈별 파일·API·의존·I/O·테스트·해시. 손편집 금지, `npm run gen:module-designs`)** · `docs/PROJECT_SPEC.md`(요구사항·최상위) · `ARCHITECTURE.md` · `DATA_MODEL.md` · `SECURITY.md` · `PRIVACY.md` · `SYNC_PROTOCOL.md` · `MEDIA_PIPELINE.md` · `DEPLOYMENT.md`(배포 계약) · `AGENT_REGISTRY.md` · `LESSONS.md` · `ROADMAP.md` · `TEST_PLAN.md` · `ROUND_TRIP_TEST_BLUEPRINT.md`(운영 쓰기 왕복검사의 다른 앱 이식 계약) · `DIAGNOSTIC_TOOL_DESIGN_BLUEPRINT.md`(앱 상태 확인 전체 시스템의 다른 앱 이식 계약) · `DECISIONS.md` · `ASSUMPTIONS.md` · `HANDOFF.md`(Records 계층 영구 이력 — PR·커밋·날짜별 시간순 근거. 재사용 가능한 교훈은 스킬 문서·`records/coding-mistakes.md`로 이관하고, 이 문서는 그 근거를 계속 쌓는다. 이 프로젝트엔 "인수인계 완료" 시점이 없어 — 매 AI 세션 경계가 곧 인계 지점이라 — 폐기 대상이 아니다. ADR-0059) · `CHANGELOG.md` · `REPOSITORY_AUDIT.md` · `CONFLICT_REPORT.md` · `ACTIVE_TASKS.md` · `BACKLOG.md`(🔴 미완료 과제 단일 정본 — 상태는 여기에만 적고 다른 문서는 T-번호로 링크만. `npm run brief`가 열린 과제를 매 착수마다 띄운다) · `DISASTER_RECOVERY.md`(백업·복원 계약 — 전용 감사 에이전트가 참조). v0.2 원본은 `docs/reference/v0.2/`.
충돌하면 공유 문서(SPEC)가 이긴다. 특정 AI 도구 대화가 아니라 이 문서들이 기준이다.

<!--CONSTITUTION:END-->
