---
name: gates-mechanization-dev
description: 게이트·기계화 개발 프롬프트 — scripts/*.mjs(harness·check-*·gen-registry)와 app/registry.gen.ts·blueprint.ts·gates.ts를 만들거나 수정하기 전에 반드시 로드한다. 비공허 게이트 작성법·SSOT→생성→게이트 흐름·EXCLUDE 규율·오탐(산문 충돌) 사례를 담은 작업 헌장. 새 게이트 추가, 자동 집계, 손편집 중복 제거 작업 시 사용.
---

# 게이트·기계화 개발 프롬프트 (Gates & Mechanization Dev Charter)

이 저장소의 품질은 사람의 주의력이 아니라 **기계가 막는 것**에서 나온다.
원칙의 정본은 `CLAUDE.md`(§4 게이트는 비공허하게 · §6 결함군 승격)와 `docs/LESSONS.md` §7이고,
이 문서는 **작업 헌장**(게이트를 실제로 쓰는 법)이다.

## 0. 파일 지도

| 파일 | 역할 |
|---|---|
| `scripts/harness.mjs` | **게이트 목록의 SSOT**. 여기 한 줄 추가 = 게이트 등록 |
| `scripts/check-*.mjs` | 개별 게이트(정적/생성물 대조/동적) |
| `scripts/gen-registry.mjs` | SSOT들 → `src/app/registry.gen.ts` 자동 집계 + 문서 마커 재심기 |
| `src/app/registry.gen.ts` | **GENERATED — 손편집 금지** |
| `src/app/gates.ts` | 게이트 한 줄 설명·카테고리(화면 표시용 편집 메타) |
| `src/app/blueprint.ts` | 설계 개요도(배선맵) 선언 — `check-blueprint`가 현실과 대조 |
| `scripts/verify-editor-live.mjs` | 라이브 렌더(선택 게이트, 브라우저 필요) |

## 1. 기계화의 기본형

> **SSOT 하나 → 파생물 자동 생성 → 게이트가 "커밋본 == 재생성본"을 강제**

- 같은 사실이 2곳 이상에 나오면 **손편집 중복 자체가 결함**이다. 한 곳을 정본으로 정하고 나머지는 파생하거나 지운다.
- 숫자를 손으로 세지 않는다(게이트 수·에이전트 수·화면 수…). `gen-registry`가 실제 파일에서 집계한다.
- 문서 산문에 숫자를 넣고 싶으면 `<!--reg:키-->값<!--/reg-->` 마커로 심고 `check-doc-counts`가 대조하게 한다.

## 2. 게이트 작성 규칙

1. **비공허 자체검사를 내장한다**: 게이트 파일 안에서 **알려진 실패를 주입해 잡히는지** 먼저 확인하고, 못 잡으면 스스로 throw 한다.
   ```js
   (() => {
     const bad = findViolations('명백히 잘못된 입력');
     if (bad.length === 0) throw new Error('SELF-TEST 실패: … (게이트 공허).');
   })();
   ```
2. **실제 결함을 주입해 RED를 재현한다**: 자체검사만으로는 부족하다. 진짜 소스를 잠깐 망가뜨려 게이트가 RED를 내는지 보고 되돌린다(이 확인 없이 "게이트 추가함"이라 쓰지 말 것).
3. **순수 함수로 분리한다**: 위반 탐지 로직을 `export function find*(text, label)` 형태로 빼면 자체검사가 그걸 직접 호출할 수 있다.
4. **실패 메시지에 고치는 법을 적는다**: 무엇이 왜 틀렸는지 + **다음에 뭘 실행하면 되는지**(예: `node scripts/gen-registry.mjs로 재생성 후 커밋`).
5. **harness에 등록**하고 `src/app/gates.ts`에 설명·카테고리를 추가한다(화면이 자동으로 반영).
6. **새 외부 의존을 추가하면 게이트도 같은 커밋에 갱신**: CSP 호스트 → `index.html` + `check-csp`의 `REQUIRED`. 새 제공자 키 → `check-secret-leak`의 형태 패턴. 안 하면 게이트가 그 의존을 보증하지 못한다.

## 2-B. 게이트도 결함을 갖는다 — 2026-07-26에 **세 가지 형태**를 다 겪었다

§2.2("실제 결함을 주입해 RED를 재현한다")는 이미 적혀 있었다. 그런데 하루에 게이트 세 개가
서로 **다른 방식으로** 공허하거나 틀렸다. 형태를 알아야 막을 수 있다.

### ① 넓히면 다시 공허해진다 — **등록부에 항목을 추가하면 그 항목으로 주입을 새로 하라**

`check-report-fields`에 `EnvReport`를 추가했다. 통과했다. **그런데 주입해도 RED가 안 났다.**
원인: 그 게이트는 *"중첩 객체는 바깥 필드만 센다 — 안쪽은 바깥이 소비되면 따라온다"*고
가정했는데, `device`는 `device.ua`로 소비되지만 `device.platform`은 죽어 있었다.
**바깥이 살아 있어도 안쪽은 죽을 수 있다.** 가정이 틀렸다.

> 처음 만들 때 한 주입 검증은 **그때의 등록 대상에 대해서만** 유효하다.
> 등록부(REPORTS·ROUTES·SURFACES…)에 한 줄 추가하는 것은 **새 게이트를 만드는 것과 같다.**

### ② 셀프테스트가 **옛 전제를 못박고 있다**

위 가정은 셀프테스트에 *"중첩 객체 안쪽 필드는 세지 않는다(바깥이 소비되면 따라온다)"*라는
**정상 케이스**로 박혀 있었다. 그래서 로직을 고치자 셀프테스트가 먼저 깨졌다 — 좋은 일이다.
전제가 바뀌면 **셀프테스트 케이스를 뒤집는 것이 첫 작업**이다. 통과시키려고 로직을 되돌리지 마라.

고친 뒤 즉시 진짜를 하나 더 잡았다(`sw.scope`도 계산만 하고 안 쓰고 있었다).
**공허했던 게이트를 고치면 밀린 결함이 쏟아진다** — 그게 공허했다는 증거다.

### ③ 오탐도 게이트의 결함이다

`check-edge-fn-ops`의 `op === '(\w+)'` 정규식이 `typeof body.op === 'string'`까지 세서
`string`을 op으로 신고했다. 부정 후방탐색`(?<![.\w])`으로 좁혔다.

> 오탐은 "게이트가 빡빡하다"가 아니라 **틀린 것**이다. 사람이 무시하기 시작하면 그 게이트는 죽는다.

### 실행 규칙 (이 셋을 다 막는 최소 절차)

```
□ 게이트를 **만들 때**   : 알려진 실패 주입 → RED 확인 → 복원
□ 게이트를 **넓힐 때**   : 새로 추가한 대상으로 주입 → RED 확인 → 복원   ← 오늘 빠뜨렸던 단계
□ 게이트를 **고칠 때**   : 셀프테스트가 옛 전제를 담고 있지 않은지 먼저 본다
□ 통과했을 때           : 정상 입력도 넣어 **초록이 뜨는지** 본다(오탐 확인)
□ 결함을 고쳤을 때       : **결함이 있던 옛 코드를 되돌려 넣어** RED를 확인한다  ← §2-E
```

> 마지막 줄이 §2-E에서 얻은 것이다. "새 검사를 썼다"는 **결함을 잡는다**와 다르다.
> 옛 코드로 RED가 안 나면 그 검사는 **결함이 아니라 부품을 재고 있는 것**이다.

## 2-E. **부품은 다 맞는데 조립이 틀렸다** — 유닛 12건이 통과한 채로 결함이 배포됐다

2026-07-26 M-0033. 복원 결함을 고치며 유닛 **12건**을 새로 썼고 전부 초록이었다. 그리고
**결함은 그대로 배포됐다.** 사용자 실기기에서 같은 증상이 다시 났다.

검사가 잘못 쓰인 게 아니다. **검사가 부품만 봤다:**

| 부품 | 검사했나 | 실제로 옳았나 |
|---|---|---|
| `requestUnpurge`가 의사를 남기는가 | ✅ | ✅ |
| `applyPurgedLedger`가 대기 id를 건너뛰는가 | ✅ | ✅ |
| `pushUnpurges`가 되읽어 확인하는가 | ✅ | ✅ |
| **그 셋을 부르는 쪽이 올바른 순서·경계로 부르는가** | ❌ | ❌ ← 여기가 결함 |

결함은 **`requestUnpurge`를 트랜잭션 밖에서 부른 것**, 즉 *부품 사이의 이음매*에 있었다.
부품별 유닛은 이음매를 원리적으로 못 본다 — 각 부품은 자기 몫을 정확히 해냈으니까.

### 그래서 무엇을 재야 했나

> **같은 이름의 검사가 두 가지를 잰다. 사고가 난 쪽을 재라.**

- ❌ 「복원이 의사를 **남기는가**」 — 부서진 코드에서도 **통과한다**(의사는 남았다. 늦었을 뿐).
- ✅ 「복원이 행과 의사를 **같이 커밋하는가**」 — 의사 쓰기를 실패시키면 **행도 없어야** 한다.
  옛 배치를 주입하면 행이 커밋된 채 남아 **RED**가 난다. 이게 사고의 상태 그 자체다.

두 문장은 거의 같아 보이는데, 하나는 공허하고 하나는 결함을 잡는다. 차이는 **"무엇이 함께
성립해야 하는가"**를 재느냐다.

### 이음매를 재는 법 (부품 유닛으로는 안 되는 것들)

| 이음매 | 재는 법 |
|---|---|
| **원자성** — A와 B가 한 커밋인가 | **B를 실패시키고 A도 없는지** 본다. 함께 실패해야 함께 성공한 것이다 |
| **순서** — A가 B보다 먼저인가 | 두 번째를 **부르면 안 되는 상태**로 두고(예: 서버 포트가 던지게) 첫 번째만으로 끝나는지 본다 |
| **호출 여부** — 정말 부르긴 하는가 | 그 의존을 **부분 모킹**해서 호출을 관측한다(`vi.mock` + `importActual`) |

`tests/unit/restoreAtomicity.test.ts`가 이 셋 중 첫째의 표본이다.

> **주입 규칙은 여기서도 같다(§2-B).** 새 검사를 쓰면 **결함이 있던 옛 코드를 되돌려 넣어
> RED를 확인**한다. 확인하지 않으면 "부품만 재는 검사"를 또 하나 늘린 것일 수 있다.

## 2-C. 래칫은 우회하지 말고 **덜어내라** — 게이트가 설계를 밀어준다

`check-fn-size`(최상위 함수 길이 래칫)가 `renderHome` 251→261줄을 막았다. 늘어난 건 주석
10줄뿐이었지만 래칫은 한 방향이다. 우회(주석 줄이기·예외 추가) 대신 헤더 조립을
`buildHeader()`로 뽑아내자 235줄이 됐고 **구조가 더 읽기 쉬워졌다.**

> 래칫에 걸리면 그건 "게이트가 방해한다"가 아니라 **"지금이 쪼갤 때"**라는 신호다.
> 줄어들면 게이트가 새 값으로 낮추라고 알려준다 — 그 커밋에서 함께 낮춘다.

## 2-D. 게이트는 **CI가 쓰는 Node에서** 돌아야 한다 (2026-07-26 · 배포 2회 연속 실패)

`check-fn-size`가 `fs.globSync`(**Node 22+**)를 썼다. 로컬(22)에서는 통과, CI(20)에서는
`SyntaxError`로 **게이트가 실행조차 못 하고** 죽었다. harness가 FAIL을 내므로 그 뒤 모든
배포가 막혔다.

> **돌지 못하는 게이트는 없는 게이트보다 나쁘다.** 없으면 그 자리만 비지만, 죽으면 전부를 막는다.

그리고 **아무도 몰랐다** — 병합 후 배포 결론을 확인하지 않아 빨간불이 두 번 쌓였다.

**규칙 셋:**

1. **게이트 스크립트는 표준 API만 쓴다.** 편한 최신 API(`globSync`·`structuredClone` 등)를 쓰기
   전에 "CI Node에 이게 있나"를 묻는다. 없으면 `readdirSync` 같은 오래된 것으로 쓴다.
2. **Node 판은 `.nvmrc` 한 곳이 정한다.** 워크플로는 숫자를 박지 말고 `node-version-file: '.nvmrc'`,
   `package.json`의 `engines.node`도 같은 판. `check-node-version`이 셋의 일치를 강제한다.
3. **의심되면 그 판으로 직접 돌려라.** 이 환경에는 `/opt/node20`·`/opt/node22`가 있다:
   ```bash
   /opt/node20/bin/node scripts/harness.mjs   # CI가 보는 것과 같은 결과
   ```

**그리고 병합했으면 배포 결론을 확인한다.** merge 성공 ≠ 배포 성공.

## 3. EXCLUDE(제외) 규율

게이트에 예외를 넣는 순간 게이트는 그만큼 무뎌진다. 그래서:
- **근거 주석을 반드시 함께** 쓴다(왜 이건 검사 대상이 아닌가).
- 데이터 테이블 제외는 자문 한 문장을 통과해야 한다 — **"이걸 잃으면 사용자의 기억이 사라지나?"** 그렇다면 제외 금지.
- 제외를 넣은 뒤 **실제 결함이 여전히 RED로 잡히는지 재확인**한다(제외가 게이트를 무디게 만들지 않았음을 증명).
- 예외는 줄 단위 옵트아웃(`// not-a-date`)이나 명시적 파일 집합으로 좁힌다. 디렉터리 통째 제외는 최후 수단.

## 4. 과거 결함 등록부

| 사례 | 근본형 | 재발 방지 |
|---|---|---|
| 가이드 화면이 "6게이트·26에이전트"를 손 스냅샷으로 적어 실제(10·28)와 어긋남 | 손으로 센 숫자는 반드시 낡는다 | `gen-registry` 자동 집계 + `check-registry-gen`(커밋본==재생성본) | <!--hist-->
| 문서 산문 숫자가 실제와 드리프트 | 산문은 게이트 밖이라 방치됨 | `<!--reg:키-->` 마커 + `check-doc-counts` |
| `check-migration-grants`가 처음엔 공허 — 느슨한 정규식이 **문장 경계를 넘어** 다른 테이블의 GRANT를 매칭 | 정규식이 구조를 모른다 | 문장 단위 파싱 + 주입 RED 확인 |
| `check-report-fields`를 `EnvReport`로 **넓혔더니** 그 대상에 대해 공허 | 넓히기 = 새 게이트인데 주입을 안 했다 | §2-B ① — 등록부에 한 줄 추가할 때마다 주입 |
| 같은 게이트의 **셀프테스트가 틀린 전제**를 정상 케이스로 못박음 | 전제가 바뀌어도 셀프테스트는 안 따라간다 | §2-B ② — 전제가 바뀌면 케이스부터 뒤집는다 |
| `check-edge-fn-ops` 정규식이 `typeof body.op === 'string'`을 op으로 셈 | 오탐 | 부정 후방탐색으로 좁힘. **오탐도 결함이다** |
| **게이트가 자기를 설명하는 산문을 오탐(2회 재발)** — 마커 문법을 예시로 적었더니 실제 마커로 인식(seq32), ISO 절단 금지를 설명했더니 위반으로 인식(seq38) | **검사 대상의 문법을 문서가 리터럴로 포함하면 오탐** | ①주석·문자열을 코드에서 분리(`codePart`) ②서사 데이터 파일(`researchLog`·`changelog`)은 정적 스캔 제외 ③문서 예시는 한글 키 등 매칭 밖 표기 사용 |
| CI가 UTC라 시간대 결함이 유닛으로 안 보임 | **테스트 환경이 특정 조건에 고정**되면 그 축의 결함은 구조적으로 안 보인다 | `check-timezone`이 유닛을 Asia/Seoul·Pacific/Honolulu에서 재실행(동/서 양방향) |
| 새 테이블 추가 시 백업·배선맵에서 조용히 누락될 뻔 | 완전성이 사람 기억 의존 | `check-backup-coverage`·`check-blueprint`가 db.ts를 진실원으로 대조 |
| **"숫자를 손으로 세지 않는다"를 적어 놓고 다섯 곳에서 어김**(건강검진 2026-07-26) — HANDOFF 인계 요약이 게이트 12개를 나열(실제 22)·"마이그레이션 0001–0010"(실제 16)·"v0.44"(실제 0.99), 스킬 3곳과 제안서가 "12게이트" | **조항(1층)과 마커 구조(2층)는 이미 있었다. 없던 것은 이탈을 잡는 층뿐이다.** 산문은 게이트 밖이라 계속 방치된다 | `check-hand-counts` — 지시문서에서 손편집 게이트 개수를 금지하고 마커를 강제. `check-doc-counts`가 그 마커 값을 대조해 **둘이 고리를 닫는다** | <!--hist-->
| `gates.ts` 게이트 설명이 12개에서 멈춰 게이트 10개가 가이드 화면에 **이름만** 뜸 | 목록은 자동 파생인데 **거기 붙는 사람의 말**은 수동이라 따로 낡는다 | `check-registry-gen`이 게이트↔설명·분류를 **양방향** 대조(누락·잔존 둘 다 RED) | <!--hist-->
| **오탐 3회차** — `§4 게이트는 비공허하게`의 절 번호를 게이트 개수로 인식 | §2·§4 오탐과 같은 형: **검사 대상의 표기를 문서가 리터럴로 포함한다.** 이번엔 문법이 아니라 *절 참조 표기*였다 | 숫자 앞 `§`·`#` 제외 + 과거 기록은 줄 단위 옵트아웃 `<!--hist-->`(§3 규율) |
| **라이브 검사가 앱이 아니라 브라우저를 재고 있었다**(서비스워커, 3중 교란) — ①브라우저 HTTP 캐시가 서버 요청을 막아 워커를 무력화해도 통과 ②경로 필터가 `/assets/`인데 서버 기록은 `assets/`라 **매칭 0건**으로 늘 통과 ③`setOffline(true)`은 페이지만 막고 **워커의 요청은 그대로 나간다**(오프라인 중 서버가 5건 수신) | **라이브 검사는 "무엇을 재고 있는지"가 정적 게이트보다 훨씬 불투명하다.** 통과는 앱이 옳다는 뜻이 아니라 *교란 요인이 대신 만족시켰다*는 뜻일 수 있다 | 서버가 `no-store` 응답 + 셀렉터를 실측 문자열에 맞춤 + **오프라인은 서버를 실제로 닫아서** 만든다. 검사가 진짜 조건이었는지 확인하는 **메타 검사**를 함께 둔다(「오프라인 검사가 진짜 오프라인이었다」) |
| **식별자 정규식이 ASCII만 받아 한글 이름을 조용히 통과**(하루에 2회 — `check-registry-gen`의 Record 키, `check-fn-size`의 함수명) | **`[A-Za-z_$]`·`[\w-]`는 한글을 안 받는다.** 안 세어진 대상은 "위반 없음"으로 보고되므로 **누락이 통과로 반올림**된다 | 키·식별자 매칭은 `[^']+`(따옴표 안 전부) 또는 `\p{L}`+`u` 플래그로. **주입시험에 비ASCII 사례를 반드시 넣는다** — 둘 다 그때만 드러났다 |

**교훈의 형**: *환경이 고정되면 그 축의 결함은 안 보인다* — 시간대(해결), 그 밖에 로케일·화면 폭·네트워크 상태·기기 성능도 같은 부류다. 새 축을 고정한 채 테스트하고 있지 않은지 주기적으로 의심할 것.

## 5. 검증 레시피

```
npm run harness                 # 전체 게이트(개수는 여기 적지 않는다 — 돌리면 나온다)
node scripts/check-<name>.mjs   # 개별
node scripts/gen-registry.mjs   # 파생물 재생성(+ 문서 마커 재심기)
node scripts/verify-editor-live.mjs   # 라이브 렌더(선택)
```
- 게이트를 추가·수정했으면 **① 자체검사 통과 ② 실제 결함 주입 시 RED ③ 복원 시 GREEN** 세 가지를 모두 보이고 보고한다.
- `gen-registry` 실행 후에는 `registry.gen.ts` 변경분을 **함께 커밋**한다(안 하면 `check-registry-gen`이 RED).

## 6. 변경 후 의무

- `changelog.ts` +0.01 · `researchLog.ts` · `docs/HANDOFF.md` · 새 교훈은 **이 문서 §4에 행 추가**
- 게이트 개수·이름은 문서에 손으로 적지 않는다 — `registry.gen.ts`에서 파생하거나 마커를 쓴다.
  `check-hand-counts`가 강제한다. 과거 기록(그때 몇 개였다)은 그 줄에 `<!--hist-->`를 붙여 제외하되,
  **제외는 역사에만** 쓴다 — 지금 상태를 말하는 문장에 붙이면 규율이 죽는다.
- `docs/HANDOFF.md` 인계 요약에 **게이트 이름을 나열하지 않는다.** 예전에 12개가 나열돼 있었고
  실제가 23개가 될 때까지 아무도 못 고쳤다(새 AI가 "여기부터 읽는다"고 지정된 자리였다).


## 게이트가 못 잡는 것 (2026-07-26, CLAUDE.md §10)

하루에 결함 6건을 겪고 분류했다. **정적 게이트가 잡은 것은 0건**이었다 — 전부 사용자의 실기기 화면에서 나왔다. 사후에 셋으로 갈렸다.

| 부류 | 예 | 처방 |
|---|---|---|
| **계약** | 새 테이블/연산의 권한 누락(M-0020·M-0024) | **정적 게이트.** 코드가 하는 일과 서버가 허락하는 일을 대조 |
| **상태 의존** | 저장소 혼재(M-0019), 옛 방식 잔재(M-0023) | **진단 지표**(런타임). + 착수 전 실서버 스냅샷(§9 4단계) |
| **전달** | 판정 문장이 엉뚱(M-0021), 확인해놓고 화면에 안 알림(M-0022) | 사용자 문장을 **순수 함수로 분리** + `check-report-fields` + **실기기** |

**게이트를 만들 때 물을 것**: *이 결함은 어느 부류인가?* 상태 의존인데 정적 게이트를 만들면 공허해지고(코드엔 증거가 없으므로 늘 통과한다), 전달 결함인데 자료구조를 검사하면 오늘의 M-0022처럼 **숫자는 다 맞는데 화면은 틀린** 상태를 그대로 통과시킨다.

**게이트 자신이 공허한지 반드시 확인한다.** 2026-07-26 하루에 **두 번** 게이트가 공허한 채로 만들어졌다:
- `check-verdict-symmetry`의 `guide-card-ic`가 `guide-card-icon`을 부분문자열로 포함해 원래 결함을 통과시켰다(M-0011).
- `deleteOpsAreGranted`의 느슨한 정규식이 **문장 경계를 넘어** 다른 테이블의 grant를 물어, 검사 대상 테이블의 권한을 지워도 통과했다(M-0024).

둘 다 **비공허 확인(알려진 실패 주입)에서만** 드러났다. 만들자마자 주입하지 않으면 게이트는 장식이다.
