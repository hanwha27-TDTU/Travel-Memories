-- 0031_videos_comment_fix.sql · 표 comment가 실제 저장 계약과 어긋난 것을 바로잡는다.
--
-- 🔴 왜 새 migration인가: 0030의 `comment on table`은 **운영 DB에 박혀 있다.** 그러니
--    0030 파일의 주석만 고치면 절반이다 — 다음 사람이 psql로 표를 들여다보면 여전히
--    틀린 문장을 읽는다(§17 문서↔코드는 파일만의 이야기가 아니다).
--
-- 무엇이 틀렸나: 0030은 *"포스터는 로컬 파생 캐시"*라고 적었는데, 실제 구현은 포스터를
--    `videoPosterStoragePath()`로 만든 **별도 R2 객체**로 올리고 **경량 영상과 포스터
--    두 바이트가 모두 되읽혀야** 동기화 op을 완료한다(`services/sync.ts`).
--
-- 🔴 왜 이 오류가 위험한가: 이 문장은 **데이터 생명주기 계약**이다. 「포스터는 로컬 캐시」로
--    읽은 사람이 나중에 R2 정리를 만들면 **포스터 객체를 고아로 오판해 지운다** —
--    사용자에게는 재생 전 썸네일이 통째로 사라지는 것으로 보인다(비타협 원칙 #1의 표면).
--
-- 구조 변경 없음: 열·인덱스·정책·권한을 건드리지 않는다. 표 설명 문자열 하나만 바꾼다.
-- 되돌리기: 0030의 원래 문장으로 같은 `comment on table`을 다시 실행하면 된다.

comment on table journey.videos is
  '사용자 영상의 경량본 메타데이터. 바이트는 비공개 R2에 **두 객체**로 — 경량 영상과 포스터가 각각 별도 키이며 둘 다 되읽혀야 동기화가 완료된다. 하드 삭제는 영구삭제 경로만 허용.';
