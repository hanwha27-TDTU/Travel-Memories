---
shape: tree
registry_prefix: BKL
---
# BACKLOG · Bugeon Journey — 미완료 과제 단일 정본(SSOT)

> 🔴 **미완료 과제의 상태는 이 파일 한 곳에만 산다.** 다른 문서(ROADMAP·HANDOFF·ASSUMPTIONS 등)는
> 과제를 언급할 때 **T-번호로 링크만** 하고 상태를 다시 적지 않는다 — 두 곳에 적힌 상태는
> 반드시 갈라진다(ROADMAP이 실제로 그렇게 낡았다 · 2026-08-03 교차검증, HANDOFF-0057).
>
> **왜 이 문서는 같은 운명(낡음)을 피하는가** — 조항이 아니라 구조로(§7 3층):
> ① 이 조항 ② `npm run brief`가 **모든 착수 브리핑에 열린 과제를 자동으로 띄운다**(안 읽고는
> 일을 시작할 수 없다) ③ `check-doc-governance`가 문서 지도 등록을 강제한다.
>
> **규칙**
> - 과제를 끝낸 커밋이 **같은 커밋에서** 그 행을 「완료 아카이브」로 옮긴다(나중에 옮기는 것은 없다).
> - 검증 증거(PR·HANDOFF 번호·read-back) 없이 완료로 옮기지 않는다(정직한 완료).
> - 새 미완료가 생기면(작업 중 발견 포함) 같은 커밋에서 행을 추가한다.
> - 상태 값은 `대기`(착수 가능) · `막힘-실기기`(사용자 기기 필요) · `막힘-사용자`(사용자 결정/조작 필요) 셋뿐.

## BKL-1 열린 과제

> **축: 아직 닫히지 않은 것** — 행 하나 = 과제 하나. 상태는 `대기`·`막힘-실기기`·`막힘-사용자` 셋뿐이다.
> **근거: 손목록** — 과제는 작업 중에 사람이 발견해 적는다. 파생할 원장이 없다(그래서 `BKL-9.1`).


| ID | 과제 | 상태 | 무엇이 필요한가 | 추가일 | 근거 |
|---|---|---|---|---|---|
| T-038 | 🗺️ **행정안전부 정부지도 운영 승인키 적용과 카카오 실패 왕복 확인** | 막힘-사용자 | 보기 전용 정부지도 어댑터·WGS84→EPSG:5179 변환·`Kakao → 정부지도 → MapLibre` 폴백·배포 변수 `VITE_JUSO_MAP_KEY`·초보자 가이드는 키 없이도 준비했다. 현재 「도로명주소 검색 API」만 승인이고 「지도제공 검색 API」는 처리 중이므로 운영 키를 대신 쓰지 않는다. **완료 조건**: 지도제공 검색 API 승인 뒤 GitHub Repository Variable에 값을 등록하고, 새 배포에서 한국 카카오 정상 우선·카카오 실패 시 정부지도·정부지도 실패 시 MapLibre를 라이브로 확인한다. 좌표제공 검색 API는 지도 표시의 선행조건이 아니다. | 2026-08-14 | 주소기반산업지원서비스 API 인증키 관리 화면 · 공식 지도 API 안내서 |
| T-037 | 🛡️ **Tauri의 Linux GTK 전이 의존성 `glib 0.18.5` 보안 업데이트 경로** | 대기 | Dependabot가 `glib >= 0.20.0`을 요구하지만 현재 Tauri 2.11.5 → wry/webkit2gtk/gtk 0.18 계열에서는 `0.18.5`보다 높게 해석할 수 없어 `security_update_not_possible`로 끝난다. `cargo tree --target all -i glib`로 Linux GTK 전이 경로임을 확인했으며 현재 배포 표면인 Windows 실행 파일에는 이 경로가 연결되지 않는다. **완료 조건**: 호환되는 Tauri/wry/GTK 계열이 나온 뒤 업그레이드하고 Cargo lock/tree·Windows 빌드·설치본 검증을 통과한다. 그 전에는 Windows 런타임 노출이라고 과장하지 않되 자동 알림도 숨기지 않는다. | 2026-08-14 | Dependabot runs 31735070817·31753963952 · `cargo tree --target all -i glib` |
| T-036 | 🗺️ **TomTom 운영 키 설정과 실제 중앙아시아 지도 확인** | 막힘-실기기 | 사용자가 TomTom의 `Map Display API` 포함 키를 GitHub Repository Variable `VITE_TOMTOM_API_KEY`에 등록했고 Domain whitelist를 `hanwha27-tdtu.github.io,localhost,127.0.0.1`로 제한했다. 값은 출력하지 않았다. 2026-08-14 운영 Pages의 기존 TSMU 장소 칩을 직접 열어 지도·확대는 확인했지만 OSM 귀속이 보여, 칩만 `LocalPlace.countryCode`를 버리는 실제 결함을 발견해 v2.38에서 수정했다. 결정적 라이브는 연결된 UZ 장소 칩의 `provider=tomtom`과 가짜 TomTom 타일 12건을 실제 클릭으로 확인했다. v2.39 Pages 배포와 운영 version read-back은 끝났다. **남은 완료 조건**: 우즈베키스탄 Android 실기기에서 TomTom 귀속·실제 타일·팬·핀치를 확인한다. | 2026-08-13 | ADR-0070 · HANDOFF-0184 · HANDOFF-0191 · 운영 화면 실측 |
| T-028 | 🕰 날짜 정합성의 남은 구멍 — 「그럴듯하게 틀린 날짜」 | 대기 | `check-date-freshness`(§18-I)는 **이틀 이상 낡은 것**과 **미래 날짜**만 잡는다. 어제 날짜로 적으면 시간대 허용치(±1일) 안이라 통과한다. 🔴 **안 해도 결함이 아니다** — 허용치를 좁히면 KST/UTC 하루 차이가 정상인데도 RED가 나고, 오탐 많은 게이트는 사람이 무시해서 죽는다(§11 ③). 좁히려면 먼저 **릴리스 시각의 지역 시간대를 어디서 읽을지**를 정해야 하고(지금은 정본이 없다), 그게 이 과제의 실제 내용이다. 그때까지는 판정문이 허용치를 스스로 밝히는 것이 정직한 상태다. | 2026-08-13 | §18-I 「정직한 한계」 · HANDOFF-0173 |
| T-033 | ✉️ **`0002` 마이그레이션의 하드코딩 소유자 이메일** | 대기 | `supabase/migrations/0002_journey_invite_only.sql`이 소유자 이메일을 **SQL 안에 문자열로** 들고 있다. 저장소가 공개로 전환되어 그 값은 이제 누구나 볼 수 있으며, 계정이 바뀌면 **조용히 아무도 초대되지 않는** 상태가 된다. **완료 조건**: 값을 설정 테이블·환경으로 옮기거나, 옮기지 않기로 한다면 **왜 안전한지를 그 파일에 적는다**(이유 없는 예외는 결함 · §7). 🔴 **되돌리기 위험**: 초대 정책을 건드리면 로그인이 막힐 수 있으므로 RLS 침투 검증을 함께 돌린다. **선행조건**: `supabase-security-dev` 헌장 정독. | 2026-08-13 | 외부 리뷰 #10 · HANDOFF-0176 · 공개 전환 HANDOFF-0194 |
| T-034 | 🧱 **거대 파일 분할 — 검사할 수 없는 면적을 줄인다** | 대기 | `tripDetail.ts` 약 3,426줄 · `diagnostics.ts` 약 2,869 · `sync.ts` 약 2,673 · `app.css` 약 2,293. 길이 자체가 결함은 아니지만 **결함이 숨는 면적**이고, 순수 로직이 DOM 클로저에 갇혀 유닛을 못 붙인다(M-0022가 그 자리에서 났다). 🔴 **한 번에 쪼개지 않는다** — 살아 있는 앱에 위험하다. `check-fn-size` 래칫이 이미 한 방향으로 밀고 있으므로, **작업하다 걸릴 때마다 그 자리의 중복을 덜어내는 것**이 정상 진행이다(`gates-mechanization-dev` §5-A-1에 방법이 있다). **완료 조건**: 이 행은 래칫 값이 의미 있게 내려가면 닫는다 — 목표 수치를 지금 정하지 않는다(정하면 그 숫자를 맞추려고 우회한다). | 2026-08-13 | 외부 리뷰 #7 · HANDOFF-0176 |
| T-041 | 👆 **작은 조작 버튼의 터치 표적 전수 심사 — 형제가 더 있다** | 대기 | T-035를 하며 ✕·닫기 버튼을 전수로 재니 **미달이 더 있었다**: `.pick-x` 22×22 · `.photo-del` 26×26 · `.pe-close` 34×34 · `.map-close` 36×36 · `.single-photo-moment-close` 38×38(반면 `.photo-viewer-close`·`.video-viewer-close`는 44×44, `.guide-close`는 이미 `--touch-target-min` 토큰을 쓴다). 🔴 **한 번에 넓히지 않는다** — T-035 실측이 보여줬듯 그냥 44px로 키우면 **옆 버튼을 덮고**, 보이지 않는 파괴적 표적이 다른 것 위에 얹히는 것은 미달보다 나쁘다(`.chip-x` 머리주석의 사고가 그것이다). **완료 조건**: 자리마다 ①실제 Chromium에서 히트 영역과 **덮는 조작 요소 0건**을 함께 재고 ②넓힐 수 없는 자리는 **이유를 그 자리에 적는다**(§7 — 이유 없는 예외는 결함). T-035가 세운 계약(「세로 44px + 덮음 0건」·토큰 파생)이 이 과제의 방법이다. **선행조건**: 없음. | 2026-08-14 | T-035 작업 중 형제 전수 실측 · HANDOFF-0199 |
| T-042 | 🧪 **`verify-editor-live`의 잔여 흔들림이 재발했다** | 대기 | T-031 검증 중 같은 트리에서 **3회 중 2회** 서로 다른 검사 1건이 추가로 실패했다(1회차 「휴지통 일괄삭제」에서 timeout 사망 · 2회차 「홈 트리 ✕ 해제」 · 3회차는 대조군과 똑같이 깨끗). 같은 커밋의 대조군 worktree는 **2/2 깨끗**이었으므로 「내 변경 탓이 아니다」를 단정하지 않는다 — 표본이 작고 원인을 못 봤다(§8: 모르는 것은 확인 불가). T-014·T-018이 이 흔들림을 한 번 닫았으므로 **재발**이다. **완료 조건**: 실패한 두 자리에서 무엇을 기다리는지 확인하고(시간이 아니라 내용 — M-0119), 재현되면 그 자리를 고치고, 재현되지 않으면 **몇 회 돌려 안 났는지를 숫자로** 남긴다. | 2026-08-14 | HANDOFF-0199 · 실행 로그 4회 대조 |

> **T-019~T-024의 출처와 한계**: 2026-08-12 Codex Security Deep Scan이 장경로 도구 실패와 사용자 중단으로
> 정식 완료되지 않았고, 회수된 discovery 후보 21개를 빠뜨리지 않도록 여섯 실행 단위로 묶었다.
> 이것들은 **확정 취약점 목록이 아니다.** Claude는 각 과제에서 기존 ADR·실수 원장·코드·운영 환경을 먼저
> 대조하고, 확인된 사실·미검증·기각을 가른다. 감사는 전역 `bg-codex-autorouter`의 **30분 상한**에 따라
> 한 구간씩 제안·승인·중간보고하며 다음 구간을 자동 시작하지 않는다.

> 🔴 **T-010·T-011·T-012·T-013은 사용자 지시 하나에서 나온 형제들이다**(2026-08-05:
> *"니가 못보는 건 당연히 모두 넣어야 해..빼면 안 됨"*). T-010·T-011은 v1.81, T-013은 v1.90,
> T-012는 2026-08-10 구현으로 모두 닫혔다. 덮은 도구 id는 `src/domain/diagGroups.ts`의
> `BLIND_SPOTS`가 정본이고 `check-diag-blindspots`가 없는 도구·이유 없는 미구현을 RED로 잡는다.
>
> **다섯 번째 형제는 여기 없다**: 「게이트가 실제로 돌았는가」는 **앱이 원리적으로 못 잰다**
> (앱은 CI를 볼 수 없다). 그건 과제가 아니라 **경계**이므로 등록부에만 두고 백로그에 올리지 않는다 —
> 올리면 영원히 안 끝나는 행이 된다.

## BKL-2 완료 아카이브 (최신이 위 · 증거 필수)

> **축: 닫힌 것** — T번호 역순. 증거 열이 비면 그 행은 아카이브에 들어올 수 없다(정직한 완료).
> **근거: 손목록** — 닫은 사람이 같은 커밋에서 옮긴다.


| ID | 과제 | 종결 방식 | 완료일 | 증거 |
|---|---|---|---|---|
| T-035 | 👆 **칩 닫기(✕) 버튼이 44px 터치 표적에 못 미친다** | 해냄(완료조건을 실측으로 개정 — 44×44가 아니라 **세로 44 + 덮음 0**) | 2026-08-14 | M-0162 · HANDOFF-0199. 실측: `.place-picked`의 `.chip-clear`는 **24×24px**이고, 원문이 말한 대로 44×44로 넓히면 바로 옆 **「좌표 복사」(`.place-picked-copy`)를 덮는다**(`elementFromPoint` 9점으로 확인). 복사하려던 손이 위치를 해제하는 것은 미달보다 나쁘므로 **가로는 넓히지 않고 세로로만** `--touch-target-min`까지 넓혔다(투명 `::after` — 보이는 크기 24px 유지, 24×44 = 1,056px²로 약 1.8배). 라이브 2건 신설(세로 44 이상 + 보이는 크기 24 · 덮는 조작 요소 0건)에 **주입 2방향 RED**: 확장 되돌림 → 24×24 FAIL · 가로까지 확장 → `place-picked-copy` 덮음 FAIL. 수정 전후 배지 캡처가 동일해 시각 밀도 변화 없음을 확인했다. 나머지 형제 미달은 T-041로 분리했다. |
| T-031 | 🚪 **죽은 라우트 셋 — 그리고 그것을 지키는 유닛이 공허하다** | 해냄(②지움 — 화면이 없었다) | 2026-08-14 | M-0161 · HANDOFF-0198. `trips`·`map`·`settings`는 `navigate(...)` 호출이 **0건**이었고 화면도 없었다(여행 목록은 홈 · 지도는 상세 안 · 설정에 해당하는 것은 홈의 오버레이). 셋을 지우고 라우트 ↔ URL 세그먼트를 `ROUTE_SEGMENTS` 한 값으로 모아 `pathToRoute`·새 순수 함수 `routeToPath`가 둘 다 거기서 파생되게 했다. `main.ts`의 `default`에는 `never` 가드를 두어 **case 없는 새 라우트는 컴파일이 안 된다**(주입 `TS2322` RED 확인). `check-screen-lifecycle`에 검사 ④를 신설해 모집단을 `ROUTE_SEGMENTS`에서 AST로 파생하고, 실제 파일 주입 2방향(가드 삭제 · 화면 없는 라우트 되살림) 모두 `exit 1` RED → 원복 GREEN. 유닛은 전수 왕복으로 바꾸고 옛 셋이 홈으로 폴백함을 명시적으로 잠갔다(17건). |
| T-040 | 🛡️ **공개 저장소 전환 뒤 GitHub 보안 기능·노출면 재판정** | 해냄 | 2026-08-14 | HANDOFF-0195·0196 · CodeQL run 31793866669 · 공개 저장소 보호 설정 read-back. CodeQL 기본 설정(원격 위협 모델)·Secret Scanning·Push Protection을 활성화했고 기존 비밀 유출 게이트·Dependabot를 대체하지 않는다. 첫 CodeQL 12개 알림 중 CI 권한 최소화와 확인된 High 경로를 PR #284에서 처리했다. 남은 `glib` 전이 의존성은 T-037, SQL 소유자 이메일은 T-033으로 분리해 추적한다. |
| T-039 | 📦 **APK·Windows 설치파일의 비로그인 공개 다운로드 경로** | 해냄(사용자 승인 공개 전환) | 2026-08-14 | 사용자가 소스·이력 공개 영향을 들은 뒤 공개 전환을 승인했다. `gh repo view`는 `visibility=PUBLIC`·`isPrivate=false`를 되읽었다. 인증 없는 `curl -L --fail`이 고정 URL의 APK **7,719,557바이트**와 Windows 설치파일 **4,694,686바이트**를 각각 HTTP 200으로 끝까지 받았다. HANDOFF-0194. |
| T-029 | 🌙 날짜 판정을 「지금」이 아니라 **「도착 시각」** 기준으로 | 해냄 | 2026-08-14 | `RELEASE_ARRIVAL_TAIL_MINUTES=13`은 v2.27의 PR CI 시작→Pages 배포 완료 실측 13.0분을 근거로 한다. 얼린 릴리스의 최신 changelog 날짜를 `(현재 + 13분)` UTC 날짜와 정확히 대조한다. 한낮·자정 13분 전/후·23:59의 오늘 날짜 RED·내일 날짜 GREEN을 셀프테스트와 별도 결함 주입으로 확인했고, 전체 검증은 HANDOFF-0190에 기록한다. |
| T-032 | 🔐 Android·Windows OAuth 콜백의 host·path 제한 | 해냄 | 2026-08-13 | ADR-0072 · HANDOFF-0188. 실제 코드는 스킴만 검사했고 Android manifest도 스킴만 선언한 사실을 먼저 확인했다. 공용 `authCodeFromUrl`이 host·path·빈 code를 거부하고 사유를 사용자 알림으로 남긴다. Android intent-filter는 host까지 제한했다. Windows도 같은 parser를 쓰며 scoped opener·deep-link·single-instance 순서를 게이트가 고의 결함으로 검증한다. Android 새 APK와 Windows 새 설치본 적용은 각 네이티브 산출물 배포 뒤다. |
| T-026 | 🗝 국내 장소 제공자(카카오) 실제 연결 | 해냄 | 2026-08-13 | ADR-0069 · HANDOFF-0183 · v2.34. GitHub 변수 `VITE_KAKAO_JAVASCRIPT_KEY`와 Supabase 시크릿 `KAKAO_REST_KEY`의 이름 존재를 값 노출 없이 되읽었다. 한국 좌표 Kakao·해외/혼합 MapLibre·Kakao 실패 MapLibre 복귀를 순수 판정과 브라우저 라이브로 잠갔고, CSP 결함 주입 RED→복원 GREEN을 확인했다. 운영 Pages의 실제 응답은 배포 워크플로와 `version.json` read-back으로 닫는다. |
| T-030 | 🧠 대용량 백업의 기기 메모리 — 주장 전에 잰다 | 해냄(보수 상한) | 2026-08-13 | ADR-0068 · HANDOFF-0179 · v2.32. 파생 사진 50/200/500장 production 경로를 평문·암호화로 계측: 평문 sampled RSS 약 2.02~2.04×, 암호화 process max RSS 약 4.00~5.02×. 전체 입력·엔트리 복사 제거. Android는 `file.arrayBuffer()` 전 최대 32MiB, 기기 값은 더 낮추는 데만 사용. 초과 File의 arrayBuffer 0회·ZIP 엔트리 무복사 유닛. **Android WebView 실기기 PSS·원본/영상-heavy 표본은 사용자 경계이며 안전 보증 아님** |
| T-021 | 🧠 대용량 백업·원본사진 메모리 고갈 경계 | 해냄 | 2026-08-12 | M-0147 · HANDOFF-0157 · v2.19. 🔴 후보보다 무거운 것이 나왔다 — 큰 JSON 백업은 OOM이 아니라 **조용히 빈 문자열**이 되어 앱이 멀쩡한 백업을 「형식 아님」이라 말했다(V8 문자열 상한 `2^29-24`, 한 바이트로 갈림을 실측). 이제 크기 때문임을 말하고 ZIP을 안내하며, 상한 안이어도 「바이트는 있는데 글자 0」이면 파일 탓을 하지 않는다(read-back). Android는 두 벌 손코드를 `readBytes()` 하나로 합치고 `maxMemory()/4` 상한 — 한 장이 약 3.3배를 쓰므로. 초과·OOM은 그 장만 건너뛰고 사유를 남기며, 웹은 빈 바이트 장을 파이프라인에 넣지 않는다. 유닛 7건·주입 3건 RED→GREEN·전체 하네스 통과. **실기기 저사양 확인은 사용자 경계** |
| T-022 | 🧯 인증된 Edge Function 자원 남용 상한 | 해냄 | 2026-08-12 | ADR-0065 · HANDOFF-0159·0160·0162. `geocode`에 질의 길이 100자 + 사용자별 300회/분(사람이 원리적으로 못 닿고 폭주는 즉시 걸리는 자리 — 실측 근거는 ADR-0065)을 넣고 **운영 배포 완료**(version 1→2 · `verify_jwt: false` 유지 · 소스 되읽기로 핵심 표지 대조). `media-sign`은 **안 하기로 결정** — 위협이 「이미 초대받은 계정」을 요구하고, 소스 해시가 릴리스 계약에 묶여 주석 한 줄도 §18-F 배포 창을 요구하며, 키 경계는 이미 지켜진다. 사유는 `supabase-security-dev` 헌장에 적어 그 파일을 만지는 누구든 brief가 먼저 읽힌다. 🔴 **남은 것은 사용자 화면 확인 하나** — 이 샌드박스가 `*.supabase.co`를 막아 호출로 재지 못했다 |
| T-027 | 🧪 자체검사 실패를 던져서 알리는 게이트를 판정 형태로 | 해냄 | 2026-08-13 | M-0153 · HANDOFF-0173 · v2.27. 🔴 **12개가 아니라 블록 14개였다**(두 파일이 자체검사 블록을 둘씩 갖고 있었다 — 세어 보고서 알았다). 최상위 IIFE와 맨블록 두 모양을 다뤘고, **자체검사 던짐이 든 블록만** 감싼 뒤 파일마다 감싸지지 않은 던짐이 남았는지 검증했다(0건). 이제 **63개 전부** 자체검사 실패를 `exit 2` 판정으로 알린다. 축이 꽉 찼으므로 래칫의 뜻이 「늘려 가는 목표」에서 **「되돌아감의 차단」**으로 바뀌었다 — 새 게이트가 던지는 채로 들어오면 즉시 RED. 주입 3방향(전환된 IIFE 파손 → 판정+exit 2 · 두 블록짜리의 블록 파손 → 판정+exit 2 · 전환 되돌림 → 래칫 RED 62<63) |
| T-025 | 🔇 앱이 「못 물어봤다」를 「없다」로 접지 않게 | 해냄 | 2026-08-12 | M-0148 · HANDOFF-0165 · v2.21. `ProviderProbe` 유니온으로 두 상태를 **타입에서** 갈랐다 — 세 번째 상태를 만들려면 `asked`를 적을 수밖에 없어 다음 사람이 또 `[]`로 접을 수 없다(§7 2층). 판정 문장은 순수 함수 `placeProviderView`로 뽑아 「환경·기능 지원」 도구의 지표로 낸다. 못 물어본 것은 `unknown`이고 종류까지 가른다(오류=transient · 모양 불일치=structural). 🔴 검색 화면은 **조용한 것이 맞다**(Nominatim이 답한다) — 말하는 일은 진단의 몫이다. 유닛 7건 + 라이브 4건, **두 층 모두 옛 동작 주입으로 RED 확인**. 원인을 못박지 않는 문장인지도 유닛·라이브가 함께 잠근다(§8 · M-0056) |
| T-024 | 🔎 세션 잔존·진단 redaction·SQL/감사 에이전트 guardrail | 해냄(일부는 **안 하기로 결정**) | 2026-08-12 | ADR-0067 · v2.20. **넷을 각각 쟀다.** ①진단 redaction: `{"api_key": "…"}` 형태만 그대로 샜다(실측) → 따옴표를 허용하고 값 끝에서 닫는 기호를 제외 · 주입 RED→GREEN. ②「데이터 관리」 세션 잔존: 잠금이 **여는 순간에만** 걸려 열어 둔 화면이 우회로였다 → `onAuthChange`가 **첫 확인과 같은 함수**를 타게 함(M-0060) · 상태가 그대로면 조기 반환이라 토큰 갱신이 화면을 흔들지 않는다. ③`WHERE` 없는 `UPDATE`: `hook-sql-safe`에 검출 추가(양성 2·음성 2로 비공허 확인 · v2.20 이전 커밋). 🔴 ④**여행 상세 세션 잔존은 안 한다** — 후보는 사실이지만 정상 사건(토큰 갱신)이 작업 중 화면 이동을 일으킬 수 있고, 그 화면은 **로컬 기록 읽기**라 파괴적 쓰기와 같은 무게로 잠그지 않는다. ⑤**감사 에이전트 `Bash`는 결함이 아니었다** — 여덟 중 여섯이 이미 「Write/Edit 없음」으로 정확히 적고 있었고, 빼면 침투·측정이 죽는다. 느슨했던 한 곳만 형제와 같은 문장으로 맞추고 **빼지 말라는 이유를 그 자리에 남겼다** |
| T-023 | 🤖 Android·CI·배포 보안 경계 재검증 | **안 하기로 결정** | 2026-08-12 | ADR-0066 · 사용자 결정. 🔴 **후보 하나는 사실이 아니었다** — 「host 전체 `allowNavigation`」은 실제로 `["hanwha27-tdtu.github.io"]` 자기 호스트 하나뿐이다(후보를 믿었으면 없는 결함을 고칠 뻔했다). 나머지(APK 워크플로 `contents: write`·수동 Pages non-main·debug WebView)는 전부 **저장소 쓰기 권한이 있어야 성립**하고 비공개·단일 소유자에서 그건 사용자 자신이다. 고정 debug 키는 ADR-0039가 이미 결정. `allowBackup=true`는 **일부러 끄지 않는다** — 자동 백업이라는 복구 계층을 없앨 수 있고(원칙 #1), debug 사이드로드라 `run-as`로 이미 읽히므로 큰 문을 두고 작은 문만 잠그는 모양이다. 실제 백업 범위는 실기기 미측정이며 그 불확실성이 곧 「건드리지 않는다」의 근거다 |
| T-020 | 🔐 로그아웃 상태 데이터 관리 권한 경계 | 해냄 | 2026-08-12 | ADR-0063 · HANDOFF-0154(구현)·0155(라이브). 사용자 선택 2안 — 내보내기는 열고 복원·영구삭제·클라우드 교체는 로그인 필요. 판정은 `canRunLocalDataAction` 한 곳, 적용은 `HubCard.needs` **필수 필드**라 새 카드가 분류를 빠뜨리면 컴파일 실패(주입 `TS2741` 확인). 모름은 잠김으로 접는다. 새 게이트 `verify-authgate-live`가 가짜 클라우드 설정 픽스처로 **잠금 갈래를 실제로 그려** 12/12 — 잠긴 카드는 눌러도 안 열리고 백업 카드는 열린다. 주입 2종 RED(잠금 무력화 5건 · 전제 파괴 시 재지 않고 실패). 범위 밖으로 남긴 위치관리대장·진단 복구·환율은 `needs: null`이며 「안전」이 아니라 「미심사」다 |
| T-019 | 👤 계정 전환 시 로컬 Dexie·sync queue 소유 경계 | **안 하기로 결정** | 2026-08-12 | ADR-0062 · 사용자 확인("번갈아 쓸일은 없어요"). HANDOFF-0152에서 조건부 사실로 확정 — 전환 대상 계정에 최종본이 **있으면** `replaceLocalSnapshot`이 이전 행·큐를 전부 치우고 push하지 않으며(안전), **없을 때만** `setLegacyBaseline`이 이전 행을 새 기준 세대로 찍고 큐를 남긴다. 위험 갈래는 로컬 행이 남의 것일 때만 열리므로 단일 계정 기기에서는 성립하지 않는다. 🔴 고치면 안 되는 이유가 따로 있다 — 로컬을 안 지우는 것은 오프라인 우선 계약이라 선의로 잠그면 로그인 전 기록이 사라진다. 남은 unknown 3건은 「확인함」이 아니라 「물을 조건이 사라짐」이며, 계정이 둘이 되면 새 과제로 다시 열고 그것부터 잰다 |
| T-007 | Phase 7 — AI 확장(요약·태그·검색·OCR, `ai_artifacts` 테이블) | **안 하기로 결정** | 2026-08-11 | ADR-0060 · 사용자 결정("007은 폐기하자"). 관측된 필수 불편 없이 개인정보 외부 전송·제공자 비용·환각 검증·새 데이터 생명주기 복잡도를 도입하지 않는다. 구체적 불편이 새로 관측되면 전체 Phase를 되살리지 않고 별도 과제로 다시 심사 |
| T-017 | 📤 **§18-G·§18-H를 공통 정본(HRL)에 올릴지 사용자 승인** | 해냄 | 2026-08-10 | HANDOFF-0135 · 사용자 승인("유일한 실패를 해결하자")에 따라 공용 정본 `e273327`의 HRL-18(산출물 전제)·HRL-19(위험 기반 하네스)·HRL-20(브라우저 실패 격리)과 Windows 공급망 경계를 프로젝트 vendor/lock·release profile·Codex/Claude 전역 4개 설치본에 공식 동기화. 공용 스킬 자체검사·설치 왕복, `check-shared-skill-contract`, 전체 하네스 64/64 GREEN |
| T-018 | ⏱️ **남은 고정 대기 126곳을 「내용 기다리기」로** | 해냄 | 2026-08-10 | HANDOFF-0133 · 실행 `waitForTimeout` **126→0**, 래칫 기준도 0으로 하향. 각 자리를 DOM·canvas·IndexedDB·파일 처리·네트워크 소비 같은 완료 사실로 바꾸고 390/390 유지. 같은 계측에서 137.4s→**63.4s(약 54% 단축)**. Windows 계측기의 0건 성공·실패 후 미종료도 M-0137로 함께 고침 |
| T-012 | 🛡️ **백업 복원 왕복 — 내려받은 파일이 실제로 다시 읽혀 원래 기록으로 돌아오는가** | 해냄 | 2026-08-10 | HANDOFF-0134 · 사용자 자료를 복제하지 않는 진단 trip 1건 전용 파일을 내려받고, 선택 파일 SHA-256 대조→production 병합→선택 메타 포함 전 필드 read-back 뒤 바깥 transaction을 의도적으로 abort. 성공 뒤 trip·queue·purged 잔재 0건. import 전 실패는 기존 상태를 정리하지 않으며, 통과 증거는 현재 앱·백업 형식에 결합되고 24시간 뒤 만료 |
| T-016 | ⏱️ **브라우저 게이트 시간 — BGT-2의 재는 순서를 우리 앱에서 실행** | 해냄 | 2026-08-09 | HANDOFF-0131 · 🔴 **답을 베끼지 않았고, 베꼈으면 틀렸다.** 원문 BGT의 답은 「폰트 격리」인데 실측하니 우리 병목이 아니었다. 게이트를 고치지 않고 playwright를 감싸 성분별로 쟀다(그래야 다른 측정이 되지 않는다): 전체 128.6s 중 **고정 대기 137.3s/351회**(겹쳐 돌아 합이 벽시계를 넘는다) · 페이지 열기 8.2s/35회 · 내용 기다리기 10.4s/188회 · evaluate 2.8s/382회 — **사실상 자고 있는 검사**였다. 🔴 근본형은 §7이다: M-0119가 *"시간이 아니라 **내용**을 기다림"*을 이미 적었는데 **한 곳만 고쳐졌고 형제 166곳이 그대로 잤다.** 처방: 클릭 직후 300ms 이하 38곳을 `settle()`(rAF×2 — 시계가 아니라 **브라우저의 렌더 파이프라인**이 값을 정한다)로 바꿔 128.6s→**121.3s**, **390/390 PASS 유지**. 400ms 이상은 진짜 비동기일 수 있어 손대지 않았다(T-018). 재발 방지는 문서가 아니라 게이트다 — 고정 대기는 **오류도 경고도 안 내고 그냥 느려지므로**: `check-live-sleep`(모집단을 harness 원장에서 파생 · 래칫 한 방향 · 주입 RED 확인) |
| T-015 | 🛡️ **게이트가 환경을 가정하는 결함군 차단** — 로컬 초록·CI 빨강을 원리적으로 막는다 | 해냄 | 2026-08-09 | HANDOFF-0129 · 헌법 **§18-G** 신설 + `check-env-assumption`. 🔴 **선언만으로 끝내지 않았다** — 얕은 체크아웃과 같은 모양의 **빈 세계 사본**(작업트리·색인·HEAD 있음 / `origin/*`·`dist/` 없음)을 만들어 싼 게이트 **55개를 실제로 돌린다**(§18-C에 따라 병렬 — 3.2초). 원장은 `GATE_WORLD` 63개 전수 선언(빈 문자열도 선언이라 누락은 RED). **비공허 증명 3중**: ①매 실행마다 카나리아(`rev-parse origin/main`)가 죽는지 먼저 확인 — 안 죽으면 *"빈 세계가 공허합니다"*로 RED ②카나리아를 무력화하는 주입 → RED 확인 ③멀쩡한 게이트에 M-0135를 재현 주입 → 이름 대며 RED. **실제 수확**: `check-gate-integrity`가 빈 세계에서 **죽었다** — 「없는 파일」을 말하는 정직한 절이 있었는데 그 **앞**에서 `readFileSync`가 스택째 터져 거기까지 못 갔다(순서가 결함이었다). 정직한 한계: 벗기는 세계는 둘(baseRef·dist)뿐이고 나머지 넷은 **재지 않았다**고 판정문이 말한다 |
| T-014 | 🧪 `verify-editor-live`가 가끔 흔들린다(341/348 ↔ 348/348) | 해냄 | 2026-08-06 | HANDOFF-0110 · M-0119 · 🔴 **원인은 「측정이 이르다」가 아니라 「누르지 않았다」였다** — `?.click()`이 대상이 없을 때 **조용히 무산**되고, 검사는 그 빈 화면을 `rows=0`·`dom=0`으로 보고했다(놓친 **행동**을 측정**값**으로 반올림). 실증: ①첫 클릭 하나만 무산시키자 harness가 냈던 **그 7건이 같은 값으로** 재현 ②`goto`를 `waitUntil:'commit'`으로 바꿔 「DOM 이전」을 결정적으로 만들자 **옛 코드 11 FAIL / 새 코드 352/352 PASS**. 수정: 조용한 무산 제거(없으면 이름을 대며 실패) · 시간이 아니라 **내용**을 기다림 · 두 벌 손으로 적힌 여는 절차를 `openGuideCard()` 하나로(§7 2층) · **전제 검사 4건 신설**(「그 화면이 실제로 열렸다」 — §4) · 지도 타일도 `waitUntil(tileZooms.length > 0)`로 |
| T-013 | 📡 서버 트리거가 `updated_at`을 덮어써 LWW 판정을 흔들지 않는가 | 해냄 | 2026-08-06 | HANDOFF-0108 · 🔴 **운영 DB 실측 + 앱이 스스로 재는 단계 둘**. 실측: `journey.set_updated_at()`이 `new.updated_at := now()`로 **무조건 덮어쓴다.** 다만 트리거는 이름 순으로 돌아 `a_sync_write_guard`가 **먼저** 보므로 LWW·OCC 판정은 **이 기기가 보낸 값**으로 내려지고, push의 read-back이 서버 값을 받아 적어(`updatedAt: serverTrip.updatedAt`) 양쪽이 같아진다. 🔴 **그 「같아짐」을 코드가 아니라 서버에서 재게 했다** — 「왕복 시험」에 `update`(이미 있는 행 고치기 = UPDATE 트리거가 도는 **유일한** 자리) → `stampRead`(이 기기와 서버가 같은 수정 시각인가, `compareInstants`로 표기가 아니라 의미 비교) 두 단계 추가. **만들기만 재면 트리거가 안 도는 자리에서 초록이 나고 그 뜻은 「없다」가 아니라 「안 봤다」**이다(§17) |
| T-005 | 태블릿 저장소 보호(persist) 적용 | **경계로 재분류** | 2026-08-06 | 🔴 과제가 아니라 **경계**였다 — 안드로이드 WebView가 `persist()`에 허락을 주지 않는다(사용자 태블릿 실측: 앱이 시작할 때 요청 → 거절, HANDOFF-0106). 그 표면에서 이 값이 무엇을 뜻하는지는 **잴 방법이 앱에 없다**. `BLIND_SPOTS`에 `coveredBy: null` + 이유와 함께 등록했다(이 문서의 「다섯 번째 형제」 규칙 — 올리면 영원히 안 끝나는 행이 된다). 사용자에게 유효한 답은 화면이 한다: 물어본 시각·거절 사실 + 백업 안내 |
| T-010 | 🖼️ 파일 실물 — 사진·소리가 저장소에 실제로 있는가 | 해냄 | 2026-08-05 | HANDOFF-0095 · 🔴 **조사 중 정정**: 서버 실재는 「저장 상태」의 `auditMediaFiles`(r2ListObjects 전수 대조)가 **이미 덮고 있었다** — 등록부가 `null`로 잘못 적혀 있었다. 새 도구는 **이 기기 바이트**를 덮는다. 남은 조각(서버 바이트를 받아 여는 것)은 이유와 함께 등록부에 새로 올렸다 |
| T-011 | 🖼️ 사진·소리가 실제로 열리고 재생되는가 | 해냄 | 2026-08-05 | HANDOFF-0095 · 「파일 실물」 도구(`fileRealityVerdict`+`fileReality`) · 유닛 23건 · 라이브 E⓪~E⑩ 11건(**깨진 WebP를 실제로 심어** 잡히는지 확인) · 주입 2종 RED → 원복 GREEN |
| T-004 | Supabase leaked-password 보호 켜기 | 해냄 | 2026-08-05 | 사용자가 대시보드 Auth Providers → Email → "Prevent use of leaked passwords" 토글을 켜고 저장. `get_advisors(security)` 재조회로 `auth_leaked_password_protection` WARN 소멸을 확인(읽기 전용, 무변경) |
| T-008 | 운영 응답에 `frame-ancestors 'none'` 등 보안 헤더 적용 | **안 하기로 결정** | 2026-08-05 | **사용자 결정(ADR-0048)**: GitHub Pages 유지·헤더 지원 호스팅 이전 안 함으로 확정 — 적용 자체를 하지 않기로 종결. 잔여 완화는 meta CSP + no-referrer 유지 |
| T-009 | 비공개 저장소 GitHub CodeQL·Secret Scanning 활성화 | **안 하기로 결정** | 2026-08-05 | **사용자 결정(ADR-0047)**: 비공개 유지·Advanced Security 미구매로 확정 — 활성화 자체를 하지 않기로 종결. 잔여 방어층은 custom secret gate+Dependabot+npm audit+SHA pinning 유지 |
| T-006 | 개발자 정보 표기값 확정 | 해냄 | 2026-08-03 | HANDOFF-0065 · **사용자 확정**("김부건 (Kim Bugeon) · Tashkent State Medical University" 채택). 화면의 값이 이미 같아 코드 변경 없음 · ASSUMPTIONS A-015/Q6 반영 |
| T-003 | hook 기반 강제 규칙(S-09) 구현 | 해냄 | 2026-08-03 | HANDOFF-0064 · PreToolUse 2종(파괴적 SQL·자격증명 쓰기) + `check-hooks-wired` 게이트. 파이프 테스트 차단/통과 양방향 · 빈 hooks·죽은 참조 주입 각각 RED |
| T-001 | 실제 2기기 canonical 게시/소비 왕복 검증 | 해냄 | 2026-08-03 | HANDOFF-0062 · 게시: `legacy`→`0fe9bb91…`·operation/device 기록·서버 행 정확히 64 · 소비: 폰이 휴지통 18→5로 정합·무결성 64건·**서버 무변경(0 upsert)** |
| T-002 | authenticated R2 PUT/GET/delete 왕복 검증 | 해냄 | 2026-08-03 | HANDOFF-0061 · 실기기 2대 왕복(올리기: R2 파일 45→46이 기록 46과 짝 일치 · 받기: 다른 기기에서 사진 실제 렌더 · 지우기: 원장 4→7·서버 행 0·R2 잔재 0) |
| T-A03 | journey FK 커버링 인덱스 10건 (migration 0028) | 해냄 | 2026-08-03 | PR #174 · HANDOFF-0057 · advisor read-back(unindexed FK 0 · 행수 무변경) |
| T-A02 | ROADMAP·ASSUMPTIONS 문서 부채 정리(스캐폴딩 시절 표기·해결된 열린 질문) | 해냄 | 2026-08-03 | PR #174 · HANDOFF-0057 |
| T-A01 | 로그인된 태블릿에서 v1.64 삭제 동기화 진단 5건 해소 확인 | 해냄 | 2026-08-03 | PR #173 · HANDOFF-0056 · [진단 요약 복사] 실측 |

## BKL-9 이 문서가 못 보는 것

> **축: 원리적 한계와, 트리로 세우자 드러난 어긋남** — 적히지 않은 과제 · 한 서랍에 담긴 세 성격 · 형제와 다른 행.
> **근거: 손목록** — 사각지대는 정의상 파생할 수 없다.

### BKL-9.1 적히지 않은 과제는 이 문서가 모른다

이 원장은 **적은 것만** 안다. 「나중에 하자」로 넘어간 것이 여기 없으면 어느 층도 그것을 보지 못한다. 그래서 규칙이 *"작업 중 발견 포함, 같은 커밋에서 행을 추가한다"*이다.

### BKL-9.2 「완료 아카이브」가 성격이 다른 셋을 한 서랍에 담았다 — **닫음(2026-08-09)**

트리로 세우자 드러났다. `BKL-2`의 행들은 **종결 방식이 세 가지**인데 표에는 그 구분이 없었다.

| 종결 방식 | 예 |
|---|---|
| 실제로 해냈다 | T-001·T-002·T-003·T-004·T-006·T-010·T-011·T-013·T-014·T-A01~A03 |
| **안 하기로 결정했다** | T-008·T-009 (둘 다 ADR로 종결 — 적용 자체를 안 함) |
| **과제가 아니었다(경계로 재분류)** | T-005 |

「해냈다」와 「안 하기로 했다」와 「애초에 과제가 아니었다」는 다음 사람에게 **전혀 다른 뜻**인데, 증거 열을 끝까지 읽어야만 갈렸다.

**닫은 방법**: `BKL-2`에 **종결 방식** 열을 세웠다. 이제 한 눈에 갈린다. 발견은 구조를 세운 판(`fea2987` 직전)에 기록됐고 닫음은 이 판이다 — 커밋을 나눠 「원래 그랬는지 내가 그랬는지」를 diff가 말하게 했다(TRE-3.2의 목적).

### BKL-9.3 T-005만 형제와 열 수가 달랐다 — **닫음(2026-08-09)**

`BKL-2`의 헤더와 15개 행은 열이 4개인데 **`T-005`만 5개**다(`**경계로 재분류**`가 완료일 자리에 들어가 날짜가 증거 칸으로 밀렸다). 표로 볼 때는 한 줄이 어긋난 것으로만 보이지만, 형제를 나란히 세우면 **하나만 모양이 다르다**는 것이 바로 보인다.

**닫은 방법**: `BKL-9.2`의 종결 방식 열을 세우자 **저절로 맞았다** — T-005는 애초에 그 열을 쓰고 있었고, 열이 없던 것은 나머지 15행이었다. 「하나만 어긋난 행」이 아니라 **「하나만 미래의 형식을 쓰고 있던 행」**이었다.

