# BACKLOG · Bugeon Journey — 미완료 과제 단일 정본(SSOT)

> 🔴 **미완료 과제의 상태는 이 파일 한 곳에만 산다.** 다른 문서(ROADMAP·HANDOFF·ASSUMPTIONS 등)는
> 과제를 언급할 때 **T-번호로 링크만** 하고 상태를 다시 적지 않는다 — 두 곳에 적힌 상태는
> 반드시 갈라진다(ROADMAP이 실제로 그렇게 낡았다 · 2026-08-03 교차검증, HANDOFF-0057).
>
> **왜 이 문서는 같은 운명(낡음)을 피하는가** — 조항이 아니라 구조로(§7 3층):
> ① 이 조항 ② `npm run brief`가 **모든 착수 브리핑에 열린 과제를 자동으로 띄운다**(안 읽고는
> 일을 시작할 수 없다) ③ `check-doc-governance`가 문서 지도 등록을 강제한다.
>
> **규칙**
> - 과제를 끝낸 커밋이 **같은 커밋에서** 그 행을 「완료 아카이브」로 옮긴다(나중에 옮기는 것은 없다).
> - 검증 증거(PR·HANDOFF 번호·read-back) 없이 완료로 옮기지 않는다(정직한 완료).
> - 새 미완료가 생기면(작업 중 발견 포함) 같은 커밋에서 행을 추가한다.
> - 상태 값은 `대기`(착수 가능) · `막힘-실기기`(사용자 기기 필요) · `막힘-사용자`(사용자 결정/조작 필요) 셋뿐.

## 열린 과제

| ID | 과제 | 상태 | 무엇이 필요한가 | 추가일 | 근거 |
|---|---|---|---|---|---|
| T-012 | 🛡️ **백업 복원 왕복** — 내보낸 파일이 다시 읽혀 원래 기록으로 돌아오는가 | 대기 | 쓰기 시험. 「왕복 시험」의 안전 경계를 그대로 물려받을 것(자기가 만든 id만·정리 실패는 `leftover`로 말함) | 2026-08-05 | 같은 등록부 항목 |
| T-014 | 🧪 `verify-editor-live`가 **가끔 흔들린다** — 같은 커밋에서 348/348과 341/348이 번갈아 나온다 | 대기 | 흔들린 것은 두 무리다: ①외부 지도 타일 조회(네트워크) ②지연 로드 화면이 아직 안 그려진 채 재는 것(`dom=0`). **흔들리는 게이트는 약한 게이트다**(§11 ③의 이웃 — 사람이 빨강을 무시하기 시작한다). 화면이 그려질 때까지 기다리는 조건을 넣고, 외부 타일은 전제 미충족(SKIP)과 위반(FAIL)을 가른다 | 2026-08-06 | v1.90 릴리스 중 실측(harness 1회차 341/348 · 단독 3회 348/348 · 2회차 harness 전부 통과) |
| T-007 | Phase 7 — AI 확장(요약·태그·검색·OCR, `ai_artifacts` 테이블) | 대기 | MVP 안정화 후 별도 branch (계획된 미착수) | 2026-07-22 | ROADMAP Phase 7 · 비타협 원칙 #2 |

> 🔴 **T-012·T-013은 사용자 지시 하나에서 나온 형제들이다**(2026-08-05: *"니가 못보는 건 당연히
> 모두 넣어야 해..빼면 안 됨"*). 같은 지시에서 나온 T-010·T-011은 v1.81에서, **T-013은 v1.90**에서 닫혔다.
> 지금 상태는 「뺀 것」이 아니라 **「이유와 함께 등록해 둔 것」**이다 —
> `src/domain/diagGroups.ts`의 `BLIND_SPOTS`에 `pendingReason`과 함께 있고,
> `check-diag-blindspots`가 이유 없는 미구현을 RED로 잡는다.
>
> **다섯 번째 형제는 여기 없다**: 「게이트가 실제로 돌았는가」는 **앱이 원리적으로 못 잰다**
> (앱은 CI를 볼 수 없다). 그건 과제가 아니라 **경계**이므로 등록부에만 두고 백로그에 올리지 않는다 —
> 올리면 영원히 안 끝나는 행이 된다.

## 완료 아카이브 (최신이 위 · 증거 필수)

| ID | 과제 | 완료일 | 증거 |
|---|---|---|---|
| T-013 | 📡 서버 트리거가 `updated_at`을 덮어써 LWW 판정을 흔들지 않는가 | 2026-08-06 | HANDOFF-0108 · 🔴 **운영 DB 실측 + 앱이 스스로 재는 단계 둘**. 실측: `journey.set_updated_at()`이 `new.updated_at := now()`로 **무조건 덮어쓴다.** 다만 트리거는 이름 순으로 돌아 `a_sync_write_guard`가 **먼저** 보므로 LWW·OCC 판정은 **이 기기가 보낸 값**으로 내려지고, push의 read-back이 서버 값을 받아 적어(`updatedAt: serverTrip.updatedAt`) 양쪽이 같아진다. 🔴 **그 「같아짐」을 코드가 아니라 서버에서 재게 했다** — 「왕복 시험」에 `update`(이미 있는 행 고치기 = UPDATE 트리거가 도는 **유일한** 자리) → `stampRead`(이 기기와 서버가 같은 수정 시각인가, `compareInstants`로 표기가 아니라 의미 비교) 두 단계 추가. **만들기만 재면 트리거가 안 도는 자리에서 초록이 나고 그 뜻은 「없다」가 아니라 「안 봤다」**이다(§17) |
| T-005 | 태블릿 저장소 보호(persist) 적용 | **경계로 재분류** | 2026-08-06 | 🔴 과제가 아니라 **경계**였다 — 안드로이드 WebView가 `persist()`에 허락을 주지 않는다(사용자 태블릿 실측: 앱이 시작할 때 요청 → 거절, HANDOFF-0106). 그 표면에서 이 값이 무엇을 뜻하는지는 **잴 방법이 앱에 없다**. `BLIND_SPOTS`에 `coveredBy: null` + 이유와 함께 등록했다(이 문서의 「다섯 번째 형제」 규칙 — 올리면 영원히 안 끝나는 행이 된다). 사용자에게 유효한 답은 화면이 한다: 물어본 시각·거절 사실 + 백업 안내 |
| T-010 | 🖼️ 파일 실물 — 사진·소리가 저장소에 실제로 있는가 | 2026-08-05 | HANDOFF-0095 · 🔴 **조사 중 정정**: 서버 실재는 「저장 상태」의 `auditMediaFiles`(r2ListObjects 전수 대조)가 **이미 덮고 있었다** — 등록부가 `null`로 잘못 적혀 있었다. 새 도구는 **이 기기 바이트**를 덮는다. 남은 조각(서버 바이트를 받아 여는 것)은 이유와 함께 등록부에 새로 올렸다 |
| T-011 | 🖼️ 사진·소리가 실제로 열리고 재생되는가 | 2026-08-05 | HANDOFF-0095 · 「파일 실물」 도구(`fileRealityVerdict`+`fileReality`) · 유닛 23건 · 라이브 E⓪~E⑩ 11건(**깨진 WebP를 실제로 심어** 잡히는지 확인) · 주입 2종 RED → 원복 GREEN |
| T-004 | Supabase leaked-password 보호 켜기 | 2026-08-05 | 사용자가 대시보드 Auth Providers → Email → "Prevent use of leaked passwords" 토글을 켜고 저장. `get_advisors(security)` 재조회로 `auth_leaked_password_protection` WARN 소멸을 확인(읽기 전용, 무변경) |
| T-008 | 운영 응답에 `frame-ancestors 'none'` 등 보안 헤더 적용 | 2026-08-05 | **사용자 결정(ADR-0048)**: GitHub Pages 유지·헤더 지원 호스팅 이전 안 함으로 확정 — 적용 자체를 하지 않기로 종결. 잔여 완화는 meta CSP + no-referrer 유지 |
| T-009 | 비공개 저장소 GitHub CodeQL·Secret Scanning 활성화 | 2026-08-05 | **사용자 결정(ADR-0047)**: 비공개 유지·Advanced Security 미구매로 확정 — 활성화 자체를 하지 않기로 종결. 잔여 방어층은 custom secret gate+Dependabot+npm audit+SHA pinning 유지 |
| T-006 | 개발자 정보 표기값 확정 | 2026-08-03 | HANDOFF-0065 · **사용자 확정**("김부건 (Kim Bugeon) · Tashkent State Medical University" 채택). 화면의 값이 이미 같아 코드 변경 없음 · ASSUMPTIONS A-015/Q6 반영 |
| T-003 | hook 기반 강제 규칙(S-09) 구현 | 2026-08-03 | HANDOFF-0064 · PreToolUse 2종(파괴적 SQL·자격증명 쓰기) + `check-hooks-wired` 게이트. 파이프 테스트 차단/통과 양방향 · 빈 hooks·죽은 참조 주입 각각 RED |
| T-001 | 실제 2기기 canonical 게시/소비 왕복 검증 | 2026-08-03 | HANDOFF-0062 · 게시: `legacy`→`0fe9bb91…`·operation/device 기록·서버 행 정확히 64 · 소비: 폰이 휴지통 18→5로 정합·무결성 64건·**서버 무변경(0 upsert)** |
| T-002 | authenticated R2 PUT/GET/delete 왕복 검증 | 2026-08-03 | HANDOFF-0061 · 실기기 2대 왕복(올리기: R2 파일 45→46이 기록 46과 짝 일치 · 받기: 다른 기기에서 사진 실제 렌더 · 지우기: 원장 4→7·서버 행 0·R2 잔재 0) |
| T-A03 | journey FK 커버링 인덱스 10건 (migration 0028) | 2026-08-03 | PR #174 · HANDOFF-0057 · advisor read-back(unindexed FK 0 · 행수 무변경) |
| T-A02 | ROADMAP·ASSUMPTIONS 문서 부채 정리(스캐폴딩 시절 표기·해결된 열린 질문) | 2026-08-03 | PR #174 · HANDOFF-0057 |
| T-A01 | 로그인된 태블릿에서 v1.64 삭제 동기화 진단 5건 해소 확인 | 2026-08-03 | PR #173 · HANDOFF-0056 · [진단 요약 복사] 실측 |
