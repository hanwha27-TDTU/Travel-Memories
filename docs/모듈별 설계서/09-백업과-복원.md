---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 백업·복원·ZIP 무결성 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

전체 백업 직렬화, ZIP/암호화, 형식 검증, 복원 병합과 왕복 진단을 소유한다.

- 모듈 ID: `backup-restore`
- 관측 파일: 5개 · 논리 줄: 1682 · 최상위 선언: 105개(외부 공개 50개)
- 코드 내용 SHA-256: `21b6e1475b63ffa9e725cdbc14f00c4dcf62c0c7c93f0dc1c78adea40f2c1ac1`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/services/backup.ts`

### 다른 모듈로 나가는 정적 의존

- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 2건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 2건
- 앱 셸과 라우팅(`app-shell`): import/re-export 1건
- 오디오 노트(`audio-notes`): import/re-export 1건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 1건
- 플랫폼 런타임·파일 저장·업데이트(`platform-runtime`): import/re-export 1건
- 오프라인 동기화와 정본 전환(`sync-canonical`): import/re-export 1건
- 휴지통·복원·영구삭제(`trash-purge`): import/re-export 1건

### 외부 패키지/런타임 의존

- `dexie`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/services/backup.ts` | 945 | 27/54 | src/offline/db.ts<br>src/sync/merge.ts<br>src/services/zip.ts<br>src/services/backupCrypto.ts<br>src/services/purge.ts<br>src/domain/time.ts<br>src/domain/media/naming.ts<br>src/domain/audio/note.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localPlaces, localTrips, localVideos; file/blob bytes; transactional persistence; deletion lifecycle; platform bridge |
| `src/services/backupCrypto.ts` | 69 | 3/9 | — | integrity/crypto |
| `src/services/backupMeta.ts` | 44 | 5/6 | — | browser key/value storage |
| `src/services/backupRoundTrip.ts` | 384 | 7/25 | src/offline/db.ts<br>src/app/changelog.ts<br>src/domain/time.ts<br>src/services/backup.ts<br>src/services/capacitorShell.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localPlaces, localTrips, localVideos; file/blob bytes; integrity/crypto; transactional persistence; deletion lifecycle; browser key/value storage |
| `src/services/zip.ts` | 240 | 8/11 | — | file/blob bytes; integrity/crypto |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/services/backup.ts` | export | const | `BACKUP_APP_TAG` | — | — |
| `src/services/backup.ts` | export | const | `BACKUP_APP_FILENAME` | — | — |
| `src/services/backup.ts` | export | const | `BACKUP_VERSION` | — | — |
| `src/services/backup.ts` | export | const | `MAX_BACKUP_IMPORT_BYTES` | — | Browser restore reads the selected file into memory and may create a second buffer while decrypting. Refuse inputs that cannot be handled predictably before allocating or touching… |
| `src/services/backup.ts` | export | const | `UNKNOWN_ANDROID_BACKUP_IMPORT_BYTES` | — | — |
| `src/services/backup.ts` | internal | const | `ANDROID_IMPORT_HEAP_FRACTION` | — | — |
| `src/services/backup.ts` | export | function | `backupImportLimitBytes(androidShell: boolean, jsHeapSizeLimit?: number, androidMaxMemory?: number): number` | [jsHeapSizeLimit, androidMaxMemory] .filter, Number.isFinite, Math.min, Math.floor | Android WebView에서 파일을 읽기 전 적용할 기기별 ZIP/JSON 복원 상한 (T-030). production ZIP 경로의 불필요한 whole-file 복사를 제거한 뒤 Node 진단에서 peak ArrayBuffer는 입력의 약 2배였다. Android WebView와 실제 IndexedDB struc… |
| `src/services/backup.ts` | export | const | `MAX_BACKUP_JSON_TEXT_BYTES` | — | JSON 백업 한 덩이가 문자열이 될 수 있는 최대 바이트 수 (T-021 · M-0147). 🔴 이 숫자는 취향이 아니라 실측 이다. V8의 문자열 최대 길이는 `2^29 - 24`이고, 그 한 글자를 넘기면 `TextDecoder().decode()`가 던지지 않고 빈 문자열을 돌려준다 : 536,870,888 바… |
| `src/services/backup.ts` | export | function | `decodeBackupJsonBytes(bytes: Uint8Array): string` | new TextDecoder().decode | JSON 백업 바이트를 문자열로 만든다. 조용한 실패를 조용히 두지 않는다. 상한 검사만으로 끝내지 않고 되읽어 확인한다 (§2 read-back) — 상한이 미래에 바뀌거나 다른 엔진에서 다른 자리에 절벽이 있어도, 「바이트는 있는데 글자가 0」이면 그건 우리가 못 읽은 것이지 파일이 깨진 것이 아니다. 그 둘을 같은… |
| `src/services/backup.ts` | internal | function | `assertSupportedBackupVersion(value: unknown): asserts value is number` | Number.isInteger, String | — |
| `src/services/backup.ts` | internal | function | `isRecord(value: unknown): value is Record<string, unknown>` | Array.isArray | — |
| `src/services/backup.ts` | internal | function | `assertSyncRow(value: unknown, domain: string, index: number): asserts value is SyncMeta & { id: string }` | isRecord, value.id.trim, Number.isFinite, Date.parse, Number.isInteger | — |
| `src/services/backup.ts` | internal | function | `assertRows(rows: CollectedRows): void` | Array.isArray, value.forEach, assertSyncRow, Number.isFinite, ['title', 'startDate', 'endDate', 'status'].forEach, ['tripId', 'occurredAt', 'title', 'note', 'emotion', 'place…, ['momentId', 'tripId', 'mime', 'takenAt'].forEach, ['width', 'height', 'bytesOriginal', 'bytesDisplay'].forEach, ['momentId', 'tripId', 'originalCurrency', 'category', 'not…, requireFinite | — |
| `src/services/backup.ts` | export | function | `assertBackupImportSize(bytes: number, maxBytes = MAX_BACKUP_IMPORT_BYTES): void` | Number.isFinite, Math.floor, mb.toLocaleString | — |
| `src/services/backup.ts` | export | async function | `readBackupFileWithinLimit(file: Pick<File, 'size' \\| 'arrayBuffer'>, maxBytes: number): Promise<ArrayBuffer>` | assertBackupImportSize, file.arrayBuffer | The single selected-file read gate: size rejection must happen before allocation. |
| `src/services/backup.ts` | export | interface | `CollectedRows` | — | 백업에 담기는 로컬 전 테이블(사용자 데이터). 두 형식이 이 한 곳에서만 읽는다. |
| `src/services/backup.ts` | export | interface | `BackupStats` | — | — |
| `src/services/backup.ts` | export | interface | `ImportResult` | — | — |
| `src/services/backup.ts` | export | typealias | `BackupFileFormat` | — | — |
| `src/services/backup.ts` | export | function | `backupFilename(now: Date, format: BackupFileFormat, options: { encrypted?: boolean; suffix?: string } = {}): string` | String(n).padStart, String, now.getFullYear, p, now.getMonth, now.getDate, now.getHours, now.getMinutes, options.suffix.replace(/[^\p{L}\p{N}_-]+/gu, '_').replace, options.suffix.replace | 사용자 대면 백업 파일명의 SSOT. 기본 규칙은 사용자가 정한 `날짜_시간_앱이름`이며, 형식·암호화·진단 표식은 그 뒤에만 붙는다. 화면마다 문자열을 조립하면 ZIP/JSON/진단 파일이 다시 서로 다른 순서로 갈라진다(§7). |
| `src/services/backup.ts` | internal | function | `statsOf(rows: CollectedRows): BackupStats` | rows.media.filter, rows.audio.filter, (rows.videos ?? []).filter | — |
| `src/services/backup.ts` | export | async function | `exportCollectRows(): Promise<CollectedRows>` | db, Promise.all, d.localTrips.toArray, d.localMoments.toArray, d.localMedia.toArray, d.localExpenses.toArray, d.localAudio.toArray, d.localVideos.toArray, d.localPlaces.toArray | export 수집: 전 테이블(tombstone 포함)을 로컬에서 읽는다. |
| `src/services/backup.ts` | internal | typealias | `BackupDomain` | — | 백업 복원이 다루는 도메인 이름. `enqueue`·`mergeDomain`이 같은 어휘를 쓴다. |
| `src/services/backup.ts` | internal | async function | `mergeDomain(table: Table<T, string>, incoming: readonly T[], entityType: BackupDomain, enqueue: (entityType: BackupDomain, row: SyncMeta & { id: string }) => Promise<…): Promise<number>` | mergeDecision, table.get, table.put, enqueue | 한 도메인의 행들을 병합한다 — 여섯 곳에 손으로 쓰던 같은 루프를 한 곳으로 . 🔴 왜 뽑았나(§7 2층 · §11 「게이트가 설계를 밀어준다」): 예전에는 도메인마다 루프를 손으로 복사했고, 그 안에는 `enqueue`를 부르는 줄 이 있었다. 오디오를 추가할 때 그 줄이 빠져 "복원한 녹음이 이 기기에만 남는" … |
| `src/services/backup.ts` | export | async function | `importMergeRows(incoming: CollectedRows): Promise<ImportResult>` | db, incoming.trips.map, incoming.moments.map, incoming.media.map, incoming.expenses.map, (incoming.audio ?? []).map, (incoming.videos ?? []).map, (incoming.places ?? []).map, Promise.all, d.localTrips.toArray | import 병합: 재구성된 행(미디어 blob 복원 완료)을 mergeDecision으로만 반영한다. 절대 로컬을 통째로 덮어쓰지 않는다 — 빈-데이터 가드로 로컬을 지키고, 각 행은 LWW+tombstone. |
| `src/services/backup.ts` | internal | function | `bytesToBase64(bytes: Uint8Array): string` | String.fromCharCode, bytes.subarray, btoa | — |
| `src/services/backup.ts` | internal | async function | `blobToDataUrl(blob: Blob): Promise<string>` | blob.arrayBuffer, bytesToBase64 | — |
| `src/services/backup.ts` | internal | function | `b64ToBlob(dataUrl: string): Blob` | dataUrl.indexOf, dataUrl.slice, /data:([^;]+)/.exec, atob, bin.charCodeAt | — |
| `src/services/backup.ts` | internal | async function | `blobBytes(blob: Blob): Promise<Uint8Array<ArrayBuffer>>` | blob.arrayBuffer | — |
| `src/services/backup.ts` | internal | typealias | `AudioExport` | — | 오디오 노트의 백업 표현 — blob은 data URL 문자열로. 사진(MediaExport)과 같은 형태. |
| `src/services/backup.ts` | internal | typealias | `VideoExport` | — | — |
| `src/services/backup.ts` | internal | typealias | `MediaExport` | — | — |
| `src/services/backup.ts` | export | interface | `BackupFile` | — | — |
| `src/services/backup.ts` | export | async function | `serializeJson(rows: CollectedRows): Promise<string>` | Promise.all, blobToDataUrl, Promise.resolve, mediaOut.push, audioOut.push, videoOut.push, new Date().toISOString, JSON.stringify | 순수: CollectedRows → JSON 문자열(사진 base64 내장). db 접근 없음(왕복 드릴 가능). |
| `src/services/backup.ts` | export | function | `deserializeJson(text: string): CollectedRows` | assertBackupImportSize, JSON.parse, Array.isArray, assertSupportedBackupVersion, mediaExport.map, b64ToBlob, (Array.isArray(parsed.audio) ? parsed.audio : []).map, (Array.isArray(parsed.videos) ? parsed.videos : []).map, assertRows | 순수: JSON 문자열 → CollectedRows(미디어 blob 복원). db 접근 없음. |
| `src/services/backup.ts` | internal | const | `ZIP_FORMAT` | — | — |
| `src/services/backup.ts` | internal | const | `PLACES_JSON` | — | 최상위 장소 파일 — 장소는 여행의 자식이 아니라 사용자 소유라 폴더 밖에 산다(0022). |
| `src/services/backup.ts` | internal | const | `ORPHAN_FOLDER` | — | — |
| `src/services/backup.ts` | internal | typealias | `MediaMetaEntry` | — | — |
| `src/services/backup.ts` | internal | interface | `TripBundle` | — | — |
| `src/services/backup.ts` | internal | interface | `ZipManifest` | — | — |
| `src/services/backup.ts` | internal | function | `tripFolderName(trip: LocalTrip): string` | tripFolder | ZIP 안 여행 폴더명 — R2 폴더와 같은 규칙 을 쓴다(naming.ts 한 곳). |
| `src/services/backup.ts` | internal | function | `extForMime(mime: string): string` | — | — |
| `src/services/backup.ts` | internal | const | `PURPOSE` | — | — |
| `src/services/backup.ts` | export | async function | `serializeZip(rows: CollectedRows, includeOriginals = true): Promise<Blob>` | rows.trips.map, folderOf.set, tripFolderName, tripById.has, folderOf.get, bundles.get, tripById.get, bundles.set, rows.moments.map, bundleFor | 순수: CollectedRows → ZIP Blob(여행 폴더 + trip.json + photos/ 실제 파일). db 접근 없음. |
| `src/services/backup.ts` | internal | typealias | `ZipFilesByName` | — | — |
| `src/services/backup.ts` | internal | function | `readZipManifest(files: ZipEntry[], byName: ZipFilesByName): ZipManifest` | byName.get, JSON.parse, new TextDecoder().decode, assertSupportedBackupVersion, Array.isArray, manifest.folders.some, manifest.folders.map, byName.has, files.filter, file.name.endsWith | — |
| `src/services/backup.ts` | internal | function | `readZipPlaces(byName: ZipFilesByName, backupVersion: number): LocalPlace[]` | byName.get, JSON.parse, new TextDecoder().decode, Array.isArray | — |
| `src/services/backup.ts` | export | function | `deserializeZip(buf: ArrayBuffer): CollectedRows` | assertBackupImportSize, unzip, files.map, readZipManifest, readZipPlaces, f.name.endsWith, f.name.slice, f.name.lastIndexOf, JSON.parse, dec.decode | 순수: ZIP → CollectedRows(사진 파일에서 blob 재구성). db 접근 없음. |
| `src/services/backup.ts` | export | async function | `exportBackup(_includePhotos = true, passphrase?: string): Promise<{ blob: Blob; stats: BackupStats }>` | exportCollectRows, serializeJson, encryptBytes, new TextEncoder().encode, statsOf | 단일 JSON 백업. passphrase가 있으면 AES-GCM 봉투로 암호화. |
| `src/services/backup.ts` | export | async function | `exportBackupZip(includeOriginals = true, passphrase?: string): Promise<{ blob: Blob; stats: BackupStats }>` | exportCollectRows, serializeZip, encryptBytes, blobBytes, statsOf | 여행별 폴더 ZIP 백업. passphrase가 있으면 ZIP 바이트 전체를 AES-GCM 봉투로 암호화. |
| `src/services/backup.ts` | export | async function | `importBackup(text: string): Promise<ImportResult>` | importMergeRows, deserializeJson | — |
| `src/services/backup.ts` | export | async function | `importBackupZip(buf: ArrayBuffer): Promise<ImportResult>` | importMergeRows, deserializeZip | — |
| `src/services/backup.ts` | export | async function | `importBackupAuto(buf: ArrayBuffer, passphrase?: string): Promise<ImportResult & { needsPassphrase?: boolean }>` | assertBackupImportSize, isEncryptedEnvelope, decryptBytes, bytes.buffer.slice, looksLikeZip, bytes.subarray, importBackupZip, importBackup, decodeBackupJsonBytes | 파일 바이트로 형식을 자동 감지해 복원. 순서: 암호화 봉투 → (복호) → ZIP(PK) → JSON. 암호화 봉투인데 passphrase가 없으면 needsPassphrase로 UI에 알린다(복원 안 함). |
| `src/services/backupCrypto.ts` | internal | const | `MAGIC` | — | — |
| `src/services/backupCrypto.ts` | internal | const | `SALT_LEN` | — | — |
| `src/services/backupCrypto.ts` | internal | const | `IV_LEN` | — | — |
| `src/services/backupCrypto.ts` | internal | const | `PBKDF2_ITERS` | — | — |
| `src/services/backupCrypto.ts` | internal | function | `subtle(): SubtleCrypto` | — | — |
| `src/services/backupCrypto.ts` | internal | async function | `deriveKey(passphrase: string, salt: Uint8Array<ArrayBuffer>): Promise<CryptoKey>` | subtle().importKey, subtle, new TextEncoder().encode, subtle().deriveKey | — |
| `src/services/backupCrypto.ts` | export | function | `isEncryptedEnvelope(bytes: Uint8Array): boolean` | — | 첫 바이트가 암호화 봉투(MAGIC)인지. import 자동 감지용. |
| `src/services/backupCrypto.ts` | export | async function | `encryptBytes(plain: Uint8Array<ArrayBuffer>, passphrase: string): Promise<Uint8Array<ArrayBuffer>>` | crypto.getRandomValues, deriveKey, subtle().encrypt, subtle, out.set | plaintext 바이트 → 암호화 봉투 바이트. |
| `src/services/backupCrypto.ts` | export | async function | `decryptBytes(envelope: Uint8Array<ArrayBuffer>, passphrase: string): Promise<Uint8Array<ArrayBuffer>>` | isEncryptedEnvelope, envelope.slice, deriveKey, subtle().decrypt, subtle | 암호화 봉투 바이트 → plaintext 바이트. 암호가 틀리면 throw. |
| `src/services/backupMeta.ts` | internal | const | `KEY` | — | — |
| `src/services/backupMeta.ts` | export | function | `recordBackupNow(): void` | localStorage.setItem, new Date().toISOString | — |
| `src/services/backupMeta.ts` | export | function | `getLastBackupAt(): string \\| null` | localStorage.getItem | — |
| `src/services/backupMeta.ts` | export | function | `backupAgeDays(iso: string \\| null, now = Date.now()): number \\| null` | Date.now, Date.parse, Number.isNaN, Math.max, Math.floor | 마지막 백업으로부터 지난 일수(정수). 없거나 파싱 불가면 null. |
| `src/services/backupMeta.ts` | export | const | `STALE_DAYS` | — | 사람이 읽는 신선도 문구. STALE_DAYS 이상이면 권고를 붙일 수 있게 stale 플래그도 준다. |
| `src/services/backupMeta.ts` | export | function | `backupFreshness(iso: string \\| null, now = Date.now()): { text: string; stale: boolean; never: boolean }` | Date.now, backupAgeDays | — |
| `src/services/backupRoundTrip.ts` | export | const | `BACKUP_ROUND_TRIP_TITLE_PREFIX` | — | — |
| `src/services/backupRoundTrip.ts` | export | const | `BACKUP_ROUND_TRIP_MAX_AGE_MS` | — | — |
| `src/services/backupRoundTrip.ts` | internal | const | `PENDING_KEY` | — | — |
| `src/services/backupRoundTrip.ts` | internal | const | `LAST_RUN_KEY` | — | — |
| `src/services/backupRoundTrip.ts` | internal | interface | `PendingBackupRoundTrip` | — | — |
| `src/services/backupRoundTrip.ts` | export | interface | `BackupRoundTripRun` | — | — |
| `src/services/backupRoundTrip.ts` | export | interface | `BackupRoundTripFile` | — | — |
| `src/services/backupRoundTrip.ts` | internal | const | `volatilePending: PendingBackupRoundTrip \\| null` | — | — |
| `src/services/backupRoundTrip.ts` | internal | const | `volatileLastRun: BackupRoundTripRun \\| null` | — | — |
| `src/services/backupRoundTrip.ts` | internal | function | `readJson(key: string): T \\| null` | localStorage.getItem, JSON.parse | — |
| `src/services/backupRoundTrip.ts` | internal | function | `writeJson(key: string, value: unknown): void` | localStorage.setItem, JSON.stringify | — |
| `src/services/backupRoundTrip.ts` | internal | function | `pendingRun(): PendingBackupRoundTrip \\| null` | readJson | — |
| `src/services/backupRoundTrip.ts` | export | function | `lastBackupRoundTrip(): BackupRoundTripRun \\| null` | readJson, pendingRun, Date.now, Date.parse, Number.isFinite | — |
| `src/services/backupRoundTrip.ts` | internal | function | `saveLastRun(run: BackupRoundTripRun): void` | writeJson | — |
| `src/services/backupRoundTrip.ts` | internal | function | `restoreTables(d: JourneyDB)` | — | — |
| `src/services/backupRoundTrip.ts` | internal | function | `fixtureTrip(now: Date): LocalTrip` | now.toISOString, localDate, crypto.randomUUID, at.slice(0, 19).replace, at.slice | — |
| `src/services/backupRoundTrip.ts` | internal | function | `statsOf(rows: CollectedRows): BackupStats` | — | — |
| `src/services/backupRoundTrip.ts` | internal | async function | `sha256Hex(bytes: ArrayBuffer): Promise<string>` | crypto.subtle.digest, [...digest].map((b) => b.toString(16).padStart(2, '0')).join, [...digest].map, b.toString(16).padStart, b.toString | — |
| `src/services/backupRoundTrip.ts` | export | async function | `createBackupRoundTripFile(): Promise<BackupRoundTripFile>` | fixtureTrip, fixtureRows, serializeZip, blob.arrayBuffer, backupFilename, sha256Hex, video.blob.arrayBuffer, video.posterBlob.arrayBuffer, now.toISOString, writeJson | production ZIP 형식과 같은 진단 전용 ZIP 을 만든다. 고유 시험 trip·moment·video 한 건만 담으므로 사용자의 사진·소리·영상·위치·메모가 시험 파일로 한 번 더 복제되지 않는다. |
| `src/services/backupRoundTrip.ts` | internal | class | `VerifiedRollback` | super | — |
| `src/services/backupRoundTrip.ts` | internal | function | `fixtureRows(fixture: LocalTrip, now: Date): CollectedRows` | now.toISOString, crypto.randomUUID | — |
| `src/services/backupRoundTrip.ts` | internal | function | `sameRecord(actual: object, expected: object, ignored = new Set<string>()): boolean` | Object.keys, ignored.has, Object.is | — |
| `src/services/backupRoundTrip.ts` | internal | async function | `artifactIds(pending: PendingBackupRoundTrip): Promise<string[]>` | db, Promise.all, d.localTrips.get, d.localMoments.get, d.localVideos.get, ids.map, d.syncQueue.where('entityId').equals(id).toArray, d.syncQueue.where('entityId').equals, d.syncQueue.where, d.purgedIds.get | — |
| `src/services/backupRoundTrip.ts` | internal | async function | `cleanupExactFixture(pending: PendingBackupRoundTrip): Promise<string[]>` | db, d.transaction, d.localTrips.get, trip?.title.startsWith, d.localVideos.delete, d.localMoments.delete, d.localTrips.delete, d.syncQueue.where('entityId').equals(id).primaryKeys, d.syncQueue.where('entityId').equals, d.syncQueue.where | 실패 경로에서도 이 시험의 정확한 세 id만 치운다. 접두사 검색으로 넓게 지우지 않는다. |
| `src/services/backupRoundTrip.ts` | export | async function | `runBackupFileRoundTrip(file: File): Promise<BackupRoundTripRun>` | pendingRun, new Date().toISOString, androidRuntimeMaxMemory, readBackupFileWithinLimit, backupImportLimitBytes, shellState, rendererHeapSizeLimit, sha256Hex, deserializeZip, rows.trips.find | 사용자가 고른 그 파일 의 해시를 확인하고, 실제 복원 경로를 돈 뒤 전체를 abort한다. 성공 조건은 import 응답이 아니라 트랜잭션 안 read-back + abort 뒤 잔재 0건이다. |
| `src/services/zip.ts` | export | interface | `ZipEntry` | — | — |
| `src/services/zip.ts` | internal | const | `CRC_TABLE` | — | — |
| `src/services/zip.ts` | export | function | `crc32(bytes: Uint8Array): number` | — | — |
| `src/services/zip.ts` | internal | const | `UTF8` | — | — |
| `src/services/zip.ts` | internal | const | `DOS_DATE` | — | — |
| `src/services/zip.ts` | export | const | `ZIP_MAX_TOTAL_BYTES` | — | 이 라이터가 담을 수 있는 한계 — ZIP64 미지원(2026-07-27 명시). 크기·오프셋 필드가 전부 `setUint32`(4 GiB)이고 엔트리 수는 `setUint16`(65,535)이다. 넘으면 오류가 아니라 잘못된 값이 기록된다 — 열리는 것처럼 보이다가 복원에서 깨지는 조용한 손상 이다. 사진 규모에서는 … |
| `src/services/zip.ts` | export | const | `ZIP_MAX_ENTRIES` | — | — |
| `src/services/zip.ts` | export | function | `zipCapacityError(entries: { name: string; data: { length: number } }[]): string \\| null` | entries.length.toLocaleString, ZIP_MAX_ENTRIES.toLocaleString, entries.reduce, (total / 1024 ** 3).toFixed | 담을 수 있는가. 못 담으면 사람이 읽는 이유 를 준다(개수·크기를 숫자로 밝힌다). |
| `src/services/zip.ts` | export | function | `zipStore(entries: ZipEntry[]): Blob` | zipCapacityError, seenNames.has, seenNames.add, UTF8.encode, crc32, lh.setUint32, lh.setUint16, locals.push, ch.setUint32, ch.setUint16 | — |
| `src/services/zip.ts` | export | function | `looksLikeZip(bytes: Uint8Array): boolean` | — | ZIP 매직바이트(PK\x03\x04) 여부 — import에서 JSON/ZIP 구분용. |
| `src/services/zip.ts` | export | function | `unzip(buf: ArrayBuffer): ZipEntry[]` | view.getUint32, view.getUint16, dec.decode, bytes.subarray, seenNames.has, seenNames.add, name.endsWith, crc32, out.push | store ZIP을 엔트리 배열로 푼다. 중앙 디렉터리를 진실원으로 순회한다. |

## 5. 자동 검증 연결

- `tests/unit/audioNote.test.ts`
- `tests/unit/backupFileRoundTrip.test.ts`
- `tests/unit/backupJsonTextLimit.test.ts`
- `tests/unit/backupMeta.test.ts`
- `tests/unit/backupNaming.test.ts`
- `tests/unit/backupRoundtrip.test.ts`
- `tests/unit/cascadeOps.test.ts`
- `tests/unit/contractGaps.test.ts`
- `tests/unit/placeSiblingDiscipline.test.ts`
- `tests/unit/restoreAtomicity.test.ts`
- `tests/unit/restoreDrill.test.ts`
- `tests/unit/zip.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/services/backup.ts` — 945줄
- `src/services/backupRoundTrip.ts` — 384줄
- `src/services/zip.ts` — 240줄
- `src/services/backupCrypto.ts` — 69줄
- `src/services/backupMeta.ts` — 44줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
