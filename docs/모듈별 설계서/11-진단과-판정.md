---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 진단·무결성·사용자 판정 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

진단 자료 수집, 정상/할 일/문제/확인 불가 판정, 보고서와 진단 화면을 소유한다.

- 모듈 ID: `diagnostics`
- 관측 파일: 22개 · 논리 줄: 6764 · 최상위 선언: 259개(외부 공개 172개)
- 코드 내용 SHA-256: `01cd95829405018a4c7fee45720008a4ad317f9ddd0229cb21bc983d82708e34`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/diagGroups.ts`

### 다른 모듈로 나가는 정적 의존

- 오프라인 동기화와 정본 전환(`sync-canonical`): import/re-export 12건
- 인증·Supabase 클라이언트·동의(`auth-security`): import/re-export 11건
- 화면·오버레이·상호작용(`ui-shell`): import/re-export 6건
- 앱 셸과 라우팅(`app-shell`): import/re-export 5건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 5건
- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 4건
- 휴지통·복원·영구삭제(`trash-purge`): import/re-export 4건
- 백업·복원·ZIP 무결성(`backup-restore`): import/re-export 3건
- 지도·장소·지오코딩(`map-place`): import/re-export 2건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 2건
- 플랫폼 런타임·파일 저장·업데이트(`platform-runtime`): import/re-export 2건
- 영상 처리 파이프라인(`video-pipeline`): import/re-export 2건

### 외부 패키지/런타임 의존

- `@supabase/supabase-js`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/diagGroups.ts` | 304 | 7/7 | — | — |
| `src/domain/diagnosticReport.ts` | 26 | 1/6 | — | — |
| `src/domain/fileRealityVerdict.ts` | 220 | 9/15 | — | — |
| `src/domain/gateControlView.ts` | 93 | 7/7 | — | — |
| `src/domain/integrity.ts` | 322 | 13/20 | src/domain/time.ts | deletion lifecycle |
| `src/domain/placeProviderVerdict.ts` | 74 | 2/5 | src/services/geocode.ts | — |
| `src/domain/placeZombieVerdict.ts` | 58 | 3/4 | — | deletion lifecycle |
| `src/domain/roundTripVerdict.ts` | 209 | 14/14 | — | deletion lifecycle |
| `src/domain/serverContractVerdict.ts` | 231 | 9/9 | — | — |
| `src/domain/sessionVerdict.ts` | 158 | 8/8 | — | — |
| `src/services/diagnostics.ts` | 283 | 9/12 | src/offline/db.ts<br>src/domain/integrity.ts<br>src/domain/time.ts<br>src/domain/syncTombstoneVerdict.ts<br>src/services/purge.ts<br>src/services/supabase/client.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localPlaces, localTrips, localVideos; Supabase table: purged_ids, sync_meta; deletion lifecycle |
| `src/services/envReport.ts` | 212 | 8/10 | — | integrity/crypto; transactional persistence; browser media worker/codec; DOM interaction; browser key/value storage |
| `src/services/fileReality.ts` | 195 | 4/10 | src/offline/db.ts<br>src/domain/fileRealityVerdict.ts | Dexie: localAudio, localMedia, localVideos; file/blob bytes; transactional persistence; DOM interaction; browser key/value storage |
| `src/services/placeZombieAudit.ts` | 89 | 2/5 | src/domain/placeZombieVerdict.ts<br>src/offline/db.ts<br>src/services/canonicalSync.ts<br>src/services/supabase/client.ts | Dexie: localPlaces; Supabase table: sync_meta; deletion lifecycle |
| `src/services/roundTrip.ts` | 271 | 3/10 | src/offline/db.ts<br>src/services/supabase/client.ts<br>src/services/auth.ts<br>src/services/autoSync.ts<br>src/services/trips.ts<br>src/domain/time.ts<br>src/domain/roundTripVerdict.ts | Dexie: localTrips; Supabase table: purged_ids, trips; transactional persistence; deletion lifecycle; browser key/value storage |
| `src/services/serverContract.ts` | 153 | 2/6 | src/services/supabase/client.ts<br>src/services/auth.ts<br>src/domain/serverContractVerdict.ts | — |
| `src/services/sessionState.ts` | 61 | 1/2 | src/services/supabase/client.ts<br>src/domain/authGate.ts<br>src/domain/sessionVerdict.ts | — |
| `src/services/syncReleaseDiagnostics.ts` | 23 | 1/1 | src/domain/syncProgress.ts<br>src/services/r2.ts<br>src/services/autoSync.ts<br>src/services/supabase/client.ts<br>src/domain/syncReleaseVerdict.ts | — |
| `src/ui/panels/diagnostics.ts` | 2870 | 51/71 | src/domain/diagGroups.ts<br>src/domain/diagnosticReport.ts<br>src/domain/fileRealityVerdict.ts<br>src/services/fileReality.ts<br>src/domain/time.ts<br>src/ui/dom.ts<br>src/services/diagnostics.ts<br>src/services/sync.ts<br>src/domain/integrity.ts<br>src/domain/syncStatusVerdict.ts<br>src/domain/syncReleaseVerdict.ts<br>src/domain/syncTombstoneVerdict.ts<br>src/services/envReport.ts<br>src/services/capacitorShell.ts<br>src/domain/persistAdvice.ts<br>src/app/errorLog.ts<br>src/app/changelog.ts<br>src/services/autoSync.ts<br>src/services/syncReleaseDiagnostics.ts<br>src/services/storeState.ts<br>src/services/r2.ts<br>src/services/purge.ts<br>src/services/supabase/client.ts<br>src/services/geocode.ts<br>src/domain/placeProviderVerdict.ts<br>src/services/placeZombieAudit.ts<br>src/services/backup.ts<br>src/services/fileSave.ts<br>src/services/backupRoundTrip.ts<br>src/services/backupMeta.ts<br>src/services/trashState.ts<br>src/domain/trashVerdict.ts<br>src/services/roundTrip.ts<br>src/services/videoRoundTrip.ts<br>src/services/serverContract.ts<br>src/domain/serverContractVerdict.ts<br>src/services/sessionState.ts<br>src/domain/deviceFleetVerdict.ts<br>src/domain/sessionVerdict.ts<br>src/domain/authGate.ts<br>src/domain/roundTripVerdict.ts<br>src/domain/videoRoundTripVerdict.ts<br>src/app/deviceId.ts<br>src/ui/panels/verdict.ts | file/blob bytes; integrity/crypto; transactional persistence; deletion lifecycle; browser media worker/codec; DOM interaction |
| `src/ui/panels/verdict.ts` | 414 | 15/25 | src/ui/dom.ts | file/blob bytes; deletion lifecycle; DOM interaction |
| `src/ui/screens/diagnosticsHub.ts` | 375 | 2/10 | src/ui/dom.ts<br>src/ui/panels/diagnostics.ts<br>src/domain/diagGroups.ts<br>src/ui/panels/verdict.ts | DOM interaction |
| `src/ui/screens/mechChecks.ts` | 123 | 1/2 | src/ui/dom.ts<br>src/app/registry.gen.ts<br>src/app/gates.ts | DOM interaction |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/diagGroups.ts` | export | const | `DIAG_GROUPS` | — | — |
| `src/domain/diagGroups.ts` | export | typealias | `DiagGroup` | — | — |
| `src/domain/diagGroups.ts` | export | interface | `GroupMeta` | — | — |
| `src/domain/diagGroups.ts` | export | const | `GROUP_META: Record<DiagGroup, GroupMeta>` | — | Record이므로 그룹을 추가하면 컴파일러가 여기로 데려온다 (§7 2층). |
| `src/domain/diagGroups.ts` | export | interface | `BlindSpot` | — | — |
| `src/domain/diagGroups.ts` | export | const | `BLIND_SPOTS: BlindSpot[]` | — | 🔴 개발 환경이 못 보는 것 전수. 빼지 않는다 — 빼는 순간 다음 사람은 그런 사각지대가 있는 줄도 모른다(실제로 이 저장소는 그 형태로 여러 번 넘어졌다: M-0019 저장소 혼재 · M-0023 옛 방식 잔재 · M-0059 채팅 업로드가 GPS를 0으로 덮음 — 전부 "내 환경에서는 안 보이는 것" 이었다). |
| `src/domain/diagGroups.ts` | export | function | `blindSpotsForTool(toolId: string): BlindSpot[]` | BLIND_SPOTS.filter | 도구 상세·보고서·게이트가 같은 책임 관계를 읽는다. |
| `src/domain/diagnosticReport.ts` | internal | const | `JWT` | — | — |
| `src/domain/diagnosticReport.ts` | internal | const | `BEARER` | — | — |
| `src/domain/diagnosticReport.ts` | internal | const | `SECRET_PAIR` | — | — |
| `src/domain/diagnosticReport.ts` | internal | const | `EMAIL` | — | — |
| `src/domain/diagnosticReport.ts` | internal | const | `URL` | — | — |
| `src/domain/diagnosticReport.ts` | export | function | `redactDiagnosticText(value: string): string` | value .replace(BEARER, 'Bearer [비밀값 숨김]') .replace(JWT, '[비…, value .replace(BEARER, 'Bearer [비밀값 숨김]') .replace, value .replace | 진단 보고서의 최종 반출 경계. 원문을 변형하지 않는 것이 목적이 아니라, 지원에 필요한 판정·개수는 남기고 계정·서버·인증을 식별할 수 있는 값은 내보내지 않는 것이 목적이다. |
| `src/domain/fileRealityVerdict.ts` | export | interface | `KindTally` | — | 한 종류(사진·소리·영상)의 로컬 바이트 실측. |
| `src/domain/fileRealityVerdict.ts` | export | interface | `OpenSweep` | — | [열어 보기]가 실제로 디코드해 본 결과. 한 번도 안 했으면 `null`. |
| `src/domain/fileRealityVerdict.ts` | export | interface | `FileRealityInput` | — | — |
| `src/domain/fileRealityVerdict.ts` | export | typealias | `FileLevel` | — | — |
| `src/domain/fileRealityVerdict.ts` | export | interface | `FileMetric` | — | — |
| `src/domain/fileRealityVerdict.ts` | internal | const | `KO` | — | — |
| `src/domain/fileRealityVerdict.ts` | internal | function const | `n(v: number)` | — | 「N개」 — 숫자 0도 값이다(금액·개수에서 0을 falsy로 뭉개지 않는다). |
| `src/domain/fileRealityVerdict.ts` | internal | function const | `gaOf(unit: string)` | /[가-힣]$/.test, unit.charCodeAt | 🔴 「개」 뒤에는 「가」다 — 실기기 화면에 「71개 이 모두 성해요」가 그대로 나갔다 (2026-08-05 사용자 캡처 · M-0110). 조사를 문자열에 손으로 박으면 앞말이 바뀔 때 어색해진다. 여기서는 앞말이 언제나 「개」(받침 없음)라 「가」로 고정이지만, 왜 고정인지 를 적어 둔다 — 다음 사람이 다른 단위… |
| `src/domain/fileRealityVerdict.ts` | internal | function | `emptyMetric(kind: 'photo' \\| 'audio' \\| 'video', t: KindTally): FileMetric` | n | 한 종류의 「바이트가 비어 있는 기록」 지표. |
| `src/domain/fileRealityVerdict.ts` | internal | function | `openMetric(sweep: OpenSweep \\| null, totalNow: number): FileMetric` | n, uncheckedSinceSweep | 「열어 봤는가 · 열렸는가」 지표. 세 상태를 가른다 — 안 해 봤다 / 열어 봤고 다 열렸다 / 안 열리는 것이 있다. 첫째를 `ok`로 반올림하면 그게 거짓말이고, `unknown`으로 두면 정상 화면이 매번 이상해 보인다 (M-0035). 사용자가 한 번 누르면 해소되는 일 이므로 `todo`가 정확하다. |
| `src/domain/fileRealityVerdict.ts` | internal | function | `uncheckedSinceSweep(sweep: OpenSweep \\| null, totalNow: number): number` | — | 심각도 합성 — 손으로 적지 않는다(자기모순 방지). |
| `src/domain/fileRealityVerdict.ts` | export | function | `worstOf(levels: readonly FileLevel[]): FileLevel` | levels.includes | — |
| `src/domain/fileRealityVerdict.ts` | export | function | `fileRealityMetrics(i: FileRealityInput): FileMetric[]` | emptyMetric, openMetric | — |
| `src/domain/fileRealityVerdict.ts` | export | function | `fileRealityHeadline(i: FileRealityInput): string` | uncheckedSinceSweep, parts.push, n, parts.join, gaOf | 판정 한 문장. 🔴 문장을 손으로 쓰지 않고 무리별 개수에서 만든다 (§7-C — M-0021이 「엉뚱한 곳을 가리킨」 형태). 그리고 한정을 생략하지 않는다 : 이 도구가 본 것은 이 기기에 있는 바이트 뿐이므로 문장이 그 경계를 말한다. |
| `src/domain/fileRealityVerdict.ts` | export | function | `fileRealityScopeNote(): string` | — | 🔴 이 도구가 다루지 않는 것 — 화면에 반드시 함께 나간다(§7-C). "이 문장이 다루지 않는 것은 무엇인가?" 두 가지다: ① 다른 기기에만 있는 바이트 — 여기선 볼 수 없다(M-0048: 「이 기기에 없다」는 「없다」가 아니다) ② 서버에 있는 바이트 — 전수로 내려받아 열어 보면 사진 앱에서 egress가 … |
| `src/domain/gateControlView.ts` | export | interface | `GateControlRow` | — | 세 축 — 이름은 `check-gate-control`의 축과 같은 말을 쓴다(두 곳이 다른 말을 하면 안 된다). |
| `src/domain/gateControlView.ts` | export | interface | `AxisSummary` | — | — |
| `src/domain/gateControlView.ts` | export | const | `GATE_CONTROL_DISCLAIMER` | — | 🔴 표 맨 위에 반드시 나가는 문장 — 이 표가 무엇이고 무엇이 아닌지 . |
| `src/domain/gateControlView.ts` | export | const | `MISSING_SHOWN_MAX` | — | 화면에 이름을 보일 최대 개수. 넘으면 「외 N개」로 접는다. |
| `src/domain/gateControlView.ts` | export | function | `capMissing(names: readonly string[]): { shown: string[]; more: number }` | names.slice, Math.max | 목록을 잘라 보여줄 것과 남은 수로 나눈다 — 자른 사실을 잃지 않는다 (§5 3항). |
| `src/domain/gateControlView.ts` | export | function | `summarizeGateControl(rows: readonly GateControlRow[]): AxisSummary[]` | capMissing, rows.filter((r) => !pick(r)).map, rows.filter, pick, axis | — |
| `src/domain/gateControlView.ts` | export | function | `gateControlHeadline(axes: readonly AxisSummary[]): string` | axes.reduce | 한 줄 요약 — 가장 약한 축 을 말한다. 🔴 평균이나 합계를 쓰지 않는다. 세 축 중 하나만 낮아도 그 자리가 약점이고, 평균은 그것을 가린다(§8 — 모르는 것·약한 것을 정상으로 반올림하지 않는다). |
| `src/domain/integrity.ts` | export | interface | `IdCheckRow` | — | — |
| `src/domain/integrity.ts` | export | interface | `TripRowLike` | — | — |
| `src/domain/integrity.ts` | export | interface | `MomentRowLike` | — | — |
| `src/domain/integrity.ts` | export | interface | `MediaRowLike` | — | — |
| `src/domain/integrity.ts` | export | interface | `PlaceRowLike` | — | 장소는 여행의 자식이 아니라 사용자 소유(부모 없음). 좌표 필드명은 latitude/longitude다. |
| `src/domain/integrity.ts` | export | interface | `ExpenseRowLike` | — | — |
| `src/domain/integrity.ts` | export | interface | `AudioRowLike` | — | — |
| `src/domain/integrity.ts` | export | interface | `IntegritySnapshot` | — | 점검 대상 스냅샷. ⚠️ 2026-07-27: 소리가 여기 없던 동안, 무결성 점검은 소리를 못 봤다 — 부모 없는 소리도, 지운 순간에 살아남은 소리도, 옛 표기로 박힌 `recordedAt`도 전부 「0건」으로 보고했다. 그건 정상이라는 뜻이 아니라 안 봤다는 뜻 이었고, 지표가 자기 시야의 경계를 밝히지 않으면 그… |
| `src/domain/integrity.ts` | export | typealias | `Severity` | — | — |
| `src/domain/integrity.ts` | export | interface | `IntegrityFinding` | — | — |
| `src/domain/integrity.ts` | export | interface | `IntegrityReport` | — | — |
| `src/domain/integrity.ts` | internal | const | `UUID_RE` | — | — |
| `src/domain/integrity.ts` | internal | function const | `short(id: string)` | id.slice | — |
| `src/domain/integrity.ts` | export | const | `CHECK_COUNT` | — | 점검 분류 수 — 화면이 "점검 N개 분류"를 손으로 세지 않도록 여기서 파생한다. |
| `src/domain/integrity.ts` | internal | typealias | `AddFinding` | — | `checkIntegrity` 안의 발견 등록 함수 — 점검을 곁 함수로 뽑을 때 그대로 넘긴다. |
| `src/domain/integrity.ts` | internal | function | `parentChecks(s: IntegritySnapshot, add: AddFinding): void` | s.trips.map, s.moments.map, s.moments.filter((m) => m.deletedAt === null).map, s.moments.filter, s.media.filter((m) => m.deletedAt === null).map, s.media.filter, s.expenses.filter((e) => e.deletedAt === null).map, s.expenses.filter, s.audio.filter((a) => a.deletedAt === null).map, s.audio.filter | 부모-자식 참조 점검 두 가지. 자식 종류가 늘면 여기 한 곳만 늘어난다. 왜 뽑았나(2026-07-27): 소리를 자식으로 더하자 `checkIntegrity`가 래칫(`check-fn-size`)을 넘었다. 주석을 지워 줄이는 대신 응집된 덩어리를 뽑았고 결과가 더 낫다 — 자식 목록이 한 배열로 모여, 다음 형제를… |
| `src/domain/integrity.ts` | internal | function | `timeChecks(all: IdCheckRow[], add: AddFinding): void` | add, all.filter((r) => (compareInstants(r.createdAt, r.updatedAt…, all.filter, compareInstants, instantFieldsOf(r).some, instantFieldsOf, isCanonicalInstant, all.filter(badFormat).map | 시각 점검 두 가지. 내용(순간)과 형식(표기)을 나눈다 — 하나로 묶었다가 사고가 났다. ── M-0034 (2026-07-27 사용자 실기기) ───────────────────────────────── 예전 판정은 `r.createdAt > r.updatedAt`, 즉 문자열 대소 였다. 그래서 서버에서 온 `20… |
| `src/domain/integrity.ts` | internal | function | `coordRangeHits(moments: MomentRowLike[], media: MediaRowLike[], places: PlaceRowLike[]): string[]` | Math.abs, moments.filter((m) => bad(m.placeLat, m.placeLng)).map, moments.filter, bad, media.filter((m) => bad(m.gpsLat, m.gpsLng)).map, media.filter, places.filter((p) => bad(p.latitude, p.longitude)).map, places.filter | ±90/±180을 넘는 좌표를 세 도메인의 실제 필드 에서 모은다(M-2). 순수 — 유닛이 직접 잰다. |
| `src/domain/integrity.ts` | export | function | `checkIntegrity(s: IntegritySnapshot): IntegrityReport` | findings.push, hits.slice(0, 3).map, hits.slice, s.trips.filter, s.moments.filter, s.media.filter, s.expenses.filter, s.places.filter, s.media.map, parentChecks | 무결성 점검. 활성 행만 대상으로 한다 — tombstone은 이미 "없는 것"이라 참조가 끊겨도 정상이다. |
| `src/domain/integrity.ts` | internal | function | `sevRank(s: Severity): number` | — | — |
| `src/domain/placeProviderVerdict.ts` | internal | const | `LABEL: Record<string, string>` | — | 사용자에게 보이는 제공자 이름 — 코드 이름을 그대로 내보내지 않는다. |
| `src/domain/placeProviderVerdict.ts` | internal | interface | `ViewBase` | — | — |
| `src/domain/placeProviderVerdict.ts` | export | typealias | `PlaceProviderView` | — | 🔴 유니온으로 둔다 — `unknownKind`에 기본값을 주지 않는다. 기본값이 있으면 새 갈래가 그것을 안 적어도 컴파일되고, 그게 이 규율이 조용히 빠지는 길이다(M-0060 · verdict의 `Metric`이 같은 이유로 유니온이다). |
| `src/domain/placeProviderVerdict.ts` | internal | const | `EXPECTED` | — | 이 지표가 정상이라고 부르는 상태 — 물어볼 수 있었다 (붙었는지와 무관하다). |
| `src/domain/placeProviderVerdict.ts` | export | function | `placeProviderView(probe: ProviderProbe): PlaceProviderView` | probe.providers.map, names.join | — |
| `src/domain/placeZombieVerdict.ts` | export | interface | `PlaceZombieRow` | — | — |
| `src/domain/placeZombieVerdict.ts` | export | interface | `PlaceZombieCandidate` | — | — |
| `src/domain/placeZombieVerdict.ts` | internal | function | `keyOf(row: PlaceZombieRow): string \\| null` | row.name.trim().replace(/\s+/g, ' ').toLocaleLowerCase, row.name.trim().replace, row.name.trim, Number.isFinite, row.latitude.toFixed, row.longitude.toFixed | — |
| `src/domain/placeZombieVerdict.ts` | export | function | `findPlaceZombieCandidates(rows: readonly PlaceZombieRow[]): PlaceZombieCandidate[]` | keyOf, active.push, deletedByKey.get, ids.push, deletedByKey.set, active .flatMap(({ id, key }) => { const deletedIds = delet…, active .flatMap, [...deletedIds].sort, a.activeId.localeCompare | 삭제 표식이 남아 있는 같은 장소가 새 활성 UUID로 재등록된 후보를 찾는다. 반환 단위는 "활성 행" 하나다. 같은 활성 행에 삭제 표식이 여럿 있어도 한 후보로 묶어 중복 경보를 만들지 않는다. |
| `src/domain/roundTripVerdict.ts` | export | const | `ROUND_TRIP_STEPS` | — | 왕복의 단계 — 순서가 계약이다. 앞이 실패하면 뒤는 돌지 않는다. |
| `src/domain/roundTripVerdict.ts` | export | typealias | `RoundTripStep` | — | — |
| `src/domain/roundTripVerdict.ts` | export | const | `STEP_LABEL: Record<RoundTripStep, string>` | — | 사람이 읽는 단계 이름 — 화면이 손으로 다시 적지 않게 여기 한 곳(§7). |
| `src/domain/roundTripVerdict.ts` | export | const | `STEP_PROVES: Record<RoundTripStep, string>` | — | 각 단계가 무엇을 증명하는가. 이걸 못 쓰면 그 단계는 시험이 아니라 절차다 — 사용자가 「왜 이 줄이 여기 있는지」를 알 수 있어야 결과를 해석할 수 있다. 🔴 여기에 마크다운을 쓰지 마라. 이 문자열은 `textContent`로 그려지므로 ` `가 화면에 그대로 찍힌다(§5 7항). 실제로 첫 판에 ` 실제 행 `… |
| `src/domain/roundTripVerdict.ts` | export | const | `STEP_STAGE: Record<RoundTripStep, string \\| null>` | — | 🔴 판정 한 문장이 부르는 단계 이름 — 없으면 앞 단계에 묻어간다(되읽기·뒷정리). ── 왜 이 표가 생겼나 (2026-08-06 · 사용자 태블릿 캡처) ────────────────────── T-013으로 단계를 8 → 10으로 늘렸는데 판정문은 손으로 쓴 옛 문장 그대로였다: 화면 위: 「저장 → 서버 → 삭… |
| `src/domain/roundTripVerdict.ts` | export | function | `stageNames(): string[]` | ROUND_TRIP_STEPS.map((s) => STEP_STAGE[s]).filter, ROUND_TRIP_STEPS.map | 판정문이 부를 단계 이름들 — 손으로 나열하지 않는다(위 표가 정본). |
| `src/domain/roundTripVerdict.ts` | export | typealias | `StepState` | — | — |
| `src/domain/roundTripVerdict.ts` | export | interface | `StepResult` | — | — |
| `src/domain/roundTripVerdict.ts` | export | interface | `RoundTripRun` | — | — |
| `src/domain/roundTripVerdict.ts` | export | typealias | `RoundTripLevel` | — | — |
| `src/domain/roundTripVerdict.ts` | export | interface | `RoundTripView` | — | — |
| `src/domain/roundTripVerdict.ts` | export | function | `firstFailure(run: RoundTripRun): StepResult \\| null` | run.steps.find | 첫 실패 단계 — 없으면 null. 「어디서 끊겼나」가 이 도구의 답이다. |
| `src/domain/roundTripVerdict.ts` | export | function | `roundTripView(run: RoundTripRun \\| null): RoundTripView` | firstFailure, run.steps.filter, stageNames().join, stageNames | 아직 한 번도 안 돌린 상태는 정상이 아니라 「확인 불가」 다(§8 — 미검사를 통과로 적는 것은 거짓말이다). 「문제 없음」과 「안 재봤음」을 같은 초록으로 칠하면 그 초록은 아무 뜻도 없다. |
| `src/domain/roundTripVerdict.ts` | export | const | `ROUND_TRIP_TITLE_PREFIX` | — | 시험용 여행 제목의 접두사. 🔴 숨기지 않고 알아보게 만든다. 홈 목록에서 감추면 정리가 실패했을 때 사용자가 그 사실을 영영 모른다 — 조용한 잔재가 나중에 「이 여행 뭐지?」가 되는 것보다, 제목이 스스로 정체를 밝히는 편이 정직하다(§8 — 모르는 걸 아는 척하지 않는다의 역방향). 정상 흐름에서는 몇 초 안에 … |
| `src/domain/serverContractVerdict.ts` | export | typealias | `PageSizeReading` | — | ① 「한 번에 받는 행수」 판독. 숫자 하나로 뭉개지 않는다 (§7-J — 부재는 한 방향으로만 재면 거짓이 될 수 있다). 갈래를 유니온으로 두면 새 갈래가 생길 때 `switch`가 컴파일 오류로 끌려 나온다 — 누락이 조용히 지나가지 않는 구조다(§7 2층). |
| `src/domain/serverContractVerdict.ts` | export | typealias | `ContractLevel` | — | — |
| `src/domain/serverContractVerdict.ts` | export | interface | `ContractMetricView` | — | — |
| `src/domain/serverContractVerdict.ts` | export | interface | `ServerContractObservation` | — | 관측값 — 서비스가 실제 서버에 물어서 채운다. 🔴 각 필드의 `null`은 0이 아니라 「못 쟀음」 이다(§8). 조회 실패와 「0건」을 같은 값으로 접으면 서버가 이상한데 화면이 조용해진다. |
| `src/domain/serverContractVerdict.ts` | export | function | `pageSizeMetric(o: ServerContractObservation): ContractMetricView` | — | ① 서버가 한 번에 주는 행수가 앱의 가정 이상인가. |
| `src/domain/serverContractVerdict.ts` | export | function | `paginationMetric(o: ServerContractObservation): ContractMetricView` | — | ② 페이지를 나눠 받을 때 사이가 빠지지 않는가. |
| `src/domain/serverContractVerdict.ts` | export | function | `anonBlockMetric(o: ServerContractObservation): ContractMetricView` | — | ③ 로그인 없는 요청이 실제로 막히는가 — 이 앱에서 가장 무거운 지표 . 뚫렸다면 다른 사람이 내 여행·사진·위치를 볼 수 있다는 뜻이다(§0 개인자료 기본 비공개). 그래서 「모름」과 「막힘」을 절대 같게 칠하지 않는다. |
| `src/domain/serverContractVerdict.ts` | export | function | `serverContractMetrics(o: ServerContractObservation): ContractMetricView[]` | anonBlockMetric, pageSizeMetric, paginationMetric | 세 지표를 계약된 순서로. 화면 자리도 계약이다(§7 사용자 대면 대칭). |
| `src/domain/serverContractVerdict.ts` | export | function | `serverContractHeadline(o: ServerContractObservation): string` | serverContractMetrics, ms.filter, anonBlockMetric | 판정 한 문장 — 무리별 개수에서 만든다(§7-C 「엉뚱한 곳을 가리킴」 방지). 지표를 늘렸는데 문장만 옛 지표를 가리키던 사고가 이 저장소에서 두 번 났다. |
| `src/domain/sessionVerdict.ts` | export | typealias | `SessionLevel` | — | — |
| `src/domain/sessionVerdict.ts` | export | interface | `SessionMetricView` | — | — |
| `src/domain/sessionVerdict.ts` | export | const | `WANTED_SESSION_DAYS` | — | 사용자가 요청한 유지 기간(2026-08-05). 이보다 짧으면 자주 다시 로그인하게 된다. |
| `src/domain/sessionVerdict.ts` | export | interface | `SessionObservation` | — | 관측값 — 서비스가 채운다. 🔴 `null`은 0이 아니라 「모름」 이다. 만료 시각을 못 읽었는데 「곧 끊긴다」고 말하면 사용자가 멀쩡한 세션을 의심하고, 「괜찮다」고 말하면 진짜 만료를 놓친다(§8). |
| `src/domain/sessionVerdict.ts` | export | function | `sessionLifetimeMetric(o: SessionObservation): SessionMetricView` | Math.floor | ① 로그인이 얼마나 유지되는가. |
| `src/domain/sessionVerdict.ts` | export | function | `lockWiringMetric(o: SessionObservation): SessionMetricView` | [!o.lockWired ? '여행 목록' : '', !o.deepLinkGuardWired ? '딥링크'…, missing.join | ②③ 로그아웃 잠금 배선이 살아 있는가. 지금 가려져 있는가를 묻지 않는다 — 로그인 중에는 안 가려지는 것이 정상이다. 묻는 것은 "로그아웃하면 가려질 배선이 코드에 있는가" 이고, 그건 앱이 자기 자신을 봐서 안다. |
| `src/domain/sessionVerdict.ts` | export | function | `sessionMetrics(o: SessionObservation): SessionMetricView[]` | sessionLifetimeMetric, lockWiringMetric | 두 지표를 계약된 순서로. |
| `src/domain/sessionVerdict.ts` | export | function | `sessionHeadline(o: SessionObservation): string` | sessionMetrics, ms.filter, lockWiringMetric | 판정 한 문장 — 무리별 개수에서 만든다(§7-C). |
| `src/services/diagnostics.ts` | export | interface | `DiagnosedSyncItem` | — | — |
| `src/services/diagnostics.ts` | export | interface | `SyncDiagnosis` | — | — |
| `src/services/diagnostics.ts` | export | const | `ITEM_CAP` | — | 목록 상한 — 이보다 많으면 개수로만 알린다. 화면이 목록으로 뒤덮이면 수리 버튼에 못 닿는다. |
| `src/services/diagnostics.ts` | export | function | `stuckReasonOf(lastError: string \\| undefined, lastStatus: number \\| undefined): string` | — | 실패 한 건의 사유 문장. 순수 함수 — 화면에 나가는 말은 유닛이 직접 돌린다(§10 ③). 🔴 모르면 모른다고 적는다 (비타협 원칙 #4). 옛 큐는 사유를 아예 안 갖고 있었으므로 그때 만들어진 항목은 `lastError`도 `lastStatus`도 없다 — 그걸 「알 수 없는 오류」 같은 그럴듯한 문장으로 채우… |
| `src/services/diagnostics.ts` | export | async function | `diagnoseSync(): Promise<SyncDiagnosis>` | db, Promise.all, d.syncQueue.toArray, d.purgedIds.count, PURGE_DOMAINS.map, DOMAIN_PURGE[domain].table().toArray, DOMAIN_PURGE[domain].table, stuckReasonOf, reasonCount.set, reasonCount.get | — |
| `src/services/diagnostics.ts` | internal | interface | `TombstoneMarkerRow` | — | — |
| `src/services/diagnostics.ts` | export | interface | `TombstoneAuditPort` | — | — |
| `src/services/diagnostics.ts` | internal | const | `AUDIT_CHUNK` | — | PostgREST URL 상한과 기본 행 상한을 동시에 피한다. |
| `src/services/diagnostics.ts` | internal | function | `chunks(xs: T[], size: number): T[][]` | out.push, xs.slice | — |
| `src/services/diagnostics.ts` | export | function | `tombstoneAuditRemote(client: JourneyClient): TombstoneAuditPort` | client.from('sync_meta').select('canonical_version').maybeS…, client.from('sync_meta').select, client.from, chunks, client .from(DOMAIN_PURGE[domain].remoteTable) .select('id,…, client .from(DOMAIN_PURGE[domain].remoteTable) .select, client .from, isoInstant, out.push, isoInstantOrNull | 읽기 전용 서버 대조 포트. 한 테이블이라도 실패하면 호출부가 전체를 확인 불가로 둔다. |
| `src/services/diagnostics.ts` | export | async function | `auditOpLessTombstones(port: TombstoneAuditPort, locals: TombstoneSyncRef[]): Promise<TombstoneSyncFinding[]>` | port.generation, byDomain.set, byDomain.get(local.domain as PurgeDomain)?.push, byDomain.get, Promise.all, PURGE_DOMAINS.map, port.markers, rows.map, port.purgedIds, locals.map | 큐 없는 tombstone을 서버 증거와 대조한다. 읽기 전용 이며 일부 성공을 정상으로 반올림하지 않는다. |
| `src/services/diagnostics.ts` | export | async function | `loadIntegritySnapshot(): Promise<IntegritySnapshot>` | db, Promise.all, d.localTrips.toArray, d.localMoments.toArray, d.localMedia.toArray, d.localExpenses.toArray, d.localAudio.toArray, d.localVideos.toArray, d.localPlaces.toArray | 무결성 점검이 볼 로컬 스냅샷을 한 곳에서 만든다. 왜 여기로 뽑았나(§7 2층, 2026-07-27): 이 `Promise.all([...])`는 `diagnostics.ts` 안에 두 번 손으로 적혀 있었다 (진단 카드용·요약 텍스트용). 도메인을 하나 더할 때 둘 다 고쳐야 하는 구조는 반드시 한쪽을 빠뜨린다 — … |
| `src/services/envReport.ts` | export | interface | `EnvReport` | — | — |
| `src/services/envReport.ts` | internal | function | `has(fn: () => boolean): boolean` | fn | 기능 하나를 안전하게 탐지한다 — 탐지 자체가 예외를 던지면 '미지원'으로 본다. |
| `src/services/envReport.ts` | export | async function | `collectEnv(appVersion: string): Promise<EnvReport>` | has, navigator.storage.estimate, navigator.storage.persisted, navigator.serviceWorker.getRegistration, navigator.userAgent.slice, navigator.languages?.join, new Date().toISOString, new Date().getTimezoneOffset, Intl.DateTimeFormat().resolvedOptions, Intl.DateTimeFormat | — |
| `src/services/envReport.ts` | export | function | `evictionRisk(r: EnvReport): { level: 'ok' \\| 'info' \\| 'error'; text: string }` | Math.round | 축출 위험 판정 — 남은 여유와 persist 상태로 사람이 읽을 결론을 만든다. |
| `src/services/envReport.ts` | export | async function | `requestPersist(): Promise<boolean>` | navigator.storage?.persist | 브라우저에 "이 데이터를 함부로 지우지 말라"고 요청한다. 거절될 수 있다(브라우저 재량). |
| `src/services/envReport.ts` | internal | const | `AUTO_PERSIST_KEY` | — | 설치된 표면에서 앱이 스스로 한 번 저장소 보호를 요청한다(T-005 · §12). 조용하다: 성공해도 실패해도 화면에 아무것도 띄우지 않는다 — 사용자가 시작한 행동이 아니므로 결과를 들이밀지 않는다. 상태는 진단 「저장소 안전」이 언제든 말해 준다(그게 그 도구의 일이다). 🔴 시도했다는 사실만 남긴다. 「허락받았… |
| `src/services/envReport.ts` | export | interface | `PersistAsk` | — | 🔴 물어봤는가, 그리고 뭐라고 답했는가 (2026-08-06 · T-005 · 사용자 태블릿). 예전에는 「시도했다」만 남겼다. 그러니 화면이 「미적용」이라고만 말할 수 있었고, 사용자는 "앱이 물어보긴 한 건가?" 를 알 길이 없었다 — 앱이 아는 것을 안 말하던 자리 (§12). 그 상태로는 T-005가 영원히 「… |
| `src/services/envReport.ts` | export | function | `lastPersistAsk(): PersistAsk \\| null` | localStorage.getItem, JSON.parse | — |
| `src/services/envReport.ts` | export | function | `rememberPersistAsk(granted: boolean, auto: boolean): void` | localStorage.setItem, JSON.stringify, new Date().toISOString | — |
| `src/services/envReport.ts` | export | async function | `autoRequestPersistOnce(): Promise<void>` | import, navigator.storage.persisted, localStorage.getItem, shouldAutoRequestPersist, persistSurface, localStorage.setItem, JSON.stringify, new Date().toISOString, rememberPersistAsk, requestPersist | — |
| `src/services/fileReality.ts` | internal | const | `SWEEP_KEY` | — | 스윕 결과를 기억하는 자리. 비싼 관측을 계기가 있을 때만 재는 규율은 `syncParity`와 같다(§7). |
| `src/services/fileReality.ts` | export | async function | `tallyLocalFiles(): Promise<{ photo: KindTally; audio: KindTally; video: KindTally }>` | Promise.all, db().localMedia.toArray, db, db().localAudio.toArray, db().localVideos.toArray, media.filter, audio.filter, video.filter | 이 기기의 사진·소리·영상 바이트를 센다 (디코드 없음 — 허브가 열릴 때마다 도는 층이라 싸야 한다). 🔴 휴지통도 센다. 되살릴 수 있어야 하므로 휴지통 항목의 바이트도 성해야 한다 — 「지운 것이니 깨져도 된다」는 순간, 복원은 빈 껍데기를 되살린다. |
| `src/services/fileReality.ts` | internal | async function | `opensAsImage(blob: Blob): Promise<boolean>` | createImageBitmap, bmp.close | 사진 한 장이 실제로 열리는가. `createImageBitmap`을 쓰는 이유: 진짜 디코더를 통과시킨다. `new Image()` + `src`는 브라우저마다 게으르게 동작할 수 있고, 크기만 읽고 넘어가면 깨진 바이트를 통과시킨다 (그러면 이 검사가 공허해진다 · §4). 디코드에 실패하면 예외가 난다 — 그게 우… |
| `src/services/fileReality.ts` | internal | async function | `opensAsAudio(blob: Blob, mime: string): Promise<boolean>` | URL.createObjectURL, el.removeAttribute, resolve, done, Number.isFinite, setTimeout, URL.revokeObjectURL | 소리 하나가 실제로 열리는가. `decodeAudioData`를 쓰지 않는 이유: 그건 전체를 PCM으로 펼친다 — 20초 녹음 수십 개면 메모리가 크게 튄다. 여기서 묻는 것은 "재생 가능한 형식인가" 이므로 `HTMLAudioElement`가 메타데이터를 읽어 길이를 알아내는지 까지만 본다. 못 읽으면 `error`… |
| `src/services/fileReality.ts` | internal | async function | `opensAsVideo(blob: Blob, mime: string): Promise<boolean>` | URL.createObjectURL, document.createElement, el.removeAttribute, el.load, resolve, done, Number.isFinite, setTimeout, URL.revokeObjectURL | 영상은 전체 프레임을 펼치지 않고 재생기가 메타데이터를 읽는지 확인한다. |
| `src/services/fileReality.ts` | internal | function const | `shortId(id: string)` | id.slice | 진단이 기억하는 id 길이 — 앞 8자(§6 개인정보 규율). |
| `src/services/fileReality.ts` | export | async function | `sweepOpenFiles(now = new Date().toISOString()): Promise<OpenSweep>` | new Date().toISOString, Promise.all, db().localMedia.toArray, db, db().localAudio.toArray, db().localVideos.toArray, opensAsImage, failed.push, shortId, opensAsAudio | 🔴 전수로 열어 본다. 사용자가 [열어 보기]를 눌렀을 때만 돈다. 순차로 도는 이유: 수백 장을 한꺼번에 디코드하면 저사양 기기에서 메모리가 터진다 (이 저장소는 저메모리 검토를 사진 변경의 필수 조건으로 둔다). 느린 대신 안전하다 — 그리고 이건 사용자가 명시적으로 누른 관측이라 몇 초는 받아들일 수 있다. |
| `src/services/fileReality.ts` | export | function | `lastSweep(): OpenSweep \\| null` | localStorage.getItem, JSON.parse, Array.isArray | 마지막 스윕 — 없거나 형식이 깨졌으면 `null`(모른다). |
| `src/services/fileReality.ts` | internal | function | `rememberSweep(s: OpenSweep): void` | localStorage.setItem, JSON.stringify | — |
| `src/services/fileReality.ts` | export | function | `__resetFileRealityForTests(): void` | localStorage.removeItem | 테스트용 초기화(모듈 밖 상태를 갖는 모듈의 의무). |
| `src/services/placeZombieAudit.ts` | internal | interface | `RemotePlaceRow` | — | — |
| `src/services/placeZombieAudit.ts` | export | interface | `PlaceZombieAudit` | — | — |
| `src/services/placeZombieAudit.ts` | internal | async function | `generation(client: JourneyClient): Promise<string>` | client.from('sync_meta').select('canonical_version').maybeS…, client.from('sync_meta').select, client.from | — |
| `src/services/placeZombieAudit.ts` | internal | function | `remotePlace(row: RemotePlaceRow): PlaceZombieRow \\| null` | — | — |
| `src/services/placeZombieAudit.ts` | export | async function | `auditPlaceZombies(client: JourneyClient): Promise<PlaceZombieAudit>` | generation, Promise.all, db().localPlaces.toArray, db, fetchAllRows, localRows.map, remote.data.map(remotePlace).filter, remote.data.map, findPlaceZombieCandidates | canonical_version 전후를 함께 읽어 한 세대의 스냅샷만 판정한다. 중간에 다른 기기가 최종본을 바꾸면 후보를 추측하지 않고 caller가 unknown으로 보인다. |
| `src/services/roundTrip.ts` | internal | const | `LAST_RUN_KEY` | — | — |
| `src/services/roundTrip.ts` | export | function | `lastRoundTrip(): RoundTripRun \\| null` | localStorage.getItem, JSON.parse, Array.isArray | 마지막 시험 결과 — `localStorage`에 둔다. 캐시 성격(기억 자체가 아님)이라 사이트 데이터를 지워 사라져도 무해하고, Dexie에 쓰면 백업 대상 판단이 흐려진다(`backupMeta.ts`와 같은 규율). |
| `src/services/roundTrip.ts` | internal | function | `saveRun(run: RoundTripRun): void` | localStorage.setItem, JSON.stringify | — |
| `src/services/roundTrip.ts` | internal | async function | `runStep(step: RoundTripStep, fn: () => Promise<string \\| void>): Promise<StepResult>` | Date.now, fn | 한 단계를 재고 결과를 기록한다. 던지지 않는다 — 실패도 값 으로 흐른다. 문자열을 돌려주면 그건 「통과했지만 이런 걸 봤다」는 관측이다(`note`). |
| `src/services/roundTrip.ts` | internal | async function | `readServerTrip(id: string): Promise<{ deleted_at: string \\| null; updated_at: string } \\| null>` | supabase, c.from('trips').select('id, deleted_at, updated_at').eq('id…, c.from('trips').select('id, deleted_at, updated_at').eq, c.from('trips').select, c.from | 서버에서 여행 한 건을 되읽는다. 없으면 null. |
| `src/services/roundTrip.ts` | internal | async function | `purgedOnServer(id: string): Promise<boolean>` | supabase, c.from('purged_ids').select('id').eq('id', id).maybeSingle, c.from('purged_ids').select('id').eq, c.from('purged_ids').select, c.from, Boolean | 영구삭제 원장에 id가 있는가 — 좀비 차단의 근거가 실제로 남았는지. |
| `src/services/roundTrip.ts` | export | async function | `cleanupRoundTripLeftovers(): Promise<string[]>` | db, d.localTrips.toArray, all.filter, t.title.startsWith, softDeleteTripLocalFirst, purgeTripPermanently, d.localTrips.get, stillThere.push | 시험용 여행이 이 기기에 남아 있으면 치운다. 접두사가 붙은 것만 대상으로 한다. 정리는 멱등이어야 한다 — 여러 번 눌러도 안전하고, 실패해도 다음 시도가 이어받는다. |
| `src/services/roundTrip.ts` | export | async function | `runRoundTrip(): Promise<RoundTripRun>` | new Date().toISOString, ROUND_TRIP_STEPS.slice, steps.push, skip, supabase, bail, saveRun, currentUser, at.slice(0, 19).replace, at.slice | 왕복 한 판을 돈다. 던지지 않는다 — 어디서 끊겼는지가 결과이므로, 예외로 나가면 그 정보가 사라진다(§13 4항 ③ — 실패 사유를 조용히 삼키지 않는다). 앞 단계가 실패하면 뒤는 `skipped`다. 「안 쟀다」와 「실패했다」는 다른 말이고, 둘을 같은 빨강으로 칠하면 사용자가 원인을 엉뚱한 데서 찾는다(§8). |
| `src/services/roundTrip.ts` | internal | async function | `updateAndStamp(id: string, stamp: string): Promise<StepResult[]>` | runStep, updateTripLocalFirst, db().localTrips.get, db, requestSync, readServerTrip, compareInstants | ④ 이미 있는 행을 고친다 → ⑤ 그 수정 시각을 이 기기와 서버가 같은 값으로 들고 있는가. ── 이 두 단계가 왜 있나 (T-013 · 2026-08-06) ──────────────────────────────── 운영 DB 실측: `journey.set_updated_at()`은 `new.updated_at :=… |
| `src/services/roundTrip.ts` | internal | function | `skip(step: RoundTripStep): StepResult` | — | — |
| `src/services/serverContract.ts` | export | const | `ASSUMED_PAGE_SIZE` | — | 앱이 페이지네이션에 쓰는 크기. `canonicalSync.ts`의 `PAGE_SIZE`와 같아야 한다 — 다르면 이 도구가 엉뚱한 가정을 재게 된다. 🔴 손으로 맞춘 값이다. 저쪽이 바뀌면 여기도 바뀌어야 하고, 그 짝은 `check-page-size-parity`가 정적으로 대조한다(§7 2층 — 산문 약속은 반드… |
| `src/services/serverContract.ts` | internal | const | `PROBE_TABLE` | — | 실측용 테이블 — 여행은 어느 계정에나 있고 행이 가장 적어 egress가 싸다. |
| `src/services/serverContract.ts` | internal | async function | `measurePageSize(c: JourneyClient): Promise<PageSizeReading>` | c .from(PROBE_TABLE) .select('id', { count: 'exact' }) .ord…, c .from(PROBE_TABLE) .select, c .from | ① 서버가 한 번에 주는 최대 행수. 앱의 가정(`ASSUMED_PAGE_SIZE`)만큼 요청해 실제로 몇 행이 왔는지 본다. 자료가 그보다 적으면 상한을 알 수 없으므로(요청보다 적게 온 것이 정상) `null`을 준다 — 「자료가 적어서 못 쟀다」와 「서버가 잘라서 줬다」는 다른 말이다(§8). |
| `src/services/serverContract.ts` | internal | async function | `checkPaginationSeam(c: JourneyClient): Promise<{ sound: boolean \\| null; note: string \\| null }>` | c.from(PROBE_TABLE).select('id').order('id', { ascending: t…, c.from(PROBE_TABLE).select('id').order, c.from(PROBE_TABLE).select, c.from, Promise.all, q, rowsA[1].id.slice, rowsB[0].id.slice | ② 페이지 경계에서 행이 빠지지 않는가. 재는 법: `[0..1]`과 `[1..2]`를 따로 받아 겹치는 자리(index 1)가 같은 행인지 본다. 같으면 서버의 정렬·오프셋이 두 요청 사이에 일관된 것이고, 다르면 그 사이로 행이 빠지거나 겹칠 수 있다. 행이 3개 미만이면 경계가 생기지 않으므로 `null`(못 쟀음… |
| `src/services/serverContract.ts` | internal | async function | `probeAnonAccess(): Promise<{ blocked: boolean \\| null; rows: number \\| null; note: string \\| null }>` | createClient, anon.from(PROBE_TABLE).select('id').limit, anon.from(PROBE_TABLE).select, anon.from | ③ 🔴 로그인 없는 요청이 실제로 막히는가 — 이 앱에서 가장 무거운 실측 . 세션이 붙지 않은 새 클라이언트 를 만들어 같은 테이블을 조회한다. 정상이면 RLS가 0행을 주거나 권한 오류를 낸다. 행이 오면 그건 남의 자료가 보인다 는 뜻이다(§0). 왜 새 클라이언트인가: 기존 `supabase()`는 세션을 물고… |
| `src/services/serverContract.ts` | export | async function | `collectServerContract(): Promise<ServerContractObservation>` | isConfigured, supabase, Boolean, currentUser, probeAnonAccess, Promise.all, measurePageSize, checkPaginationSeam | 세 실측을 모은다. 실패는 예외가 아니라 값 으로 흐른다. |
| `src/services/sessionState.ts` | internal | const | `PERSIST_SESSION` | — | 앱이 세션을 저장하도록 설정돼 있는가. `createJourneyClient`가 `persistSession: true`로 고정돼 있으므로 이 값은 상수다. 그래도 상수로 두지 않고 여기서 한 번 이름을 붙이는 이유: 나중에 설정이 바뀌면 이 한 줄이 바뀌고 진단이 따라온다. 손으로 `true`를 적어 두면 설정이 꺼져… |
| `src/services/sessionState.ts` | export | async function | `collectSessionState(): Promise<SessionObservation>` | isConfigured, authGateMismatches, supabase, c.auth.getSession, Math.max, Math.floor, Date.now, Boolean | — |
| `src/services/syncReleaseDiagnostics.ts` | export | async function | `collectSyncReleaseObservation(client: JourneyClient): Promise<SyncReleaseObservation>` | r2Capabilities, syncStatus, syncProgressLabel, new Date().toISOString | — |
| `src/ui/panels/diagnostics.ts` | export | const | `FILE_ID_CAP` | — | 접힌 출처에 보일 사진 id 상한. 넘으면 "외 N건 생략"이라고 적는다 (조용히 자르지 않는다). |
| `src/ui/panels/diagnostics.ts` | export | const | `PLACE_ZOMBIE_ID_CAP` | — | 개인정보를 보이지 않는 범위에서 후보 ID를 보여 줄 상한. |
| `src/ui/panels/diagnostics.ts` | export | const | `EXPECTED_FN_VERSION` | — | 이 앱이 기대하는 사진 저장소 함수 판. 서버가 이보다 낮으면 새 지표를 믿을 수 없다. 함수에 연산을 추가할 때 `FN_VERSION`과 함께 올린다 — 안 올리면 화면이 낡음을 못 알아본다. |
| `src/ui/panels/diagnostics.ts` | internal | const | `APP_VERSION` | — | 화면에 표시할 앱 버전 — changelog가 SSOT(손으로 적지 않는다). |
| `src/ui/panels/diagnostics.ts` | internal | function | `bytes(n: number \\| null): string` | (n / 1024).toFixed, (n / (1024 * 1024)).toFixed, (n / (1024 * 1024 * 1024)).toFixed | 사람이 읽는 바이트. |
| `src/ui/panels/diagnostics.ts` | internal | function | `table(rows: [string, string][], whenEmpty: string): HTMLElement` | el, r.append, t.appendChild | 근거 표. 비었을 때 무슨 뜻인지는 부르는 쪽이 말한다 — `whenEmpty`는 선택이 아니다. 왜 필수인가(2026-07-27 사용자 지적): 예전엔 비면 「없음」 한 단어만 찍었다. 그래서 접힌 제목은 「사진 파일 9개(2.1 MB) · 기록 9개」 인데 펼치면 「없음」 이었다 — 사용자가 물었다: "사진파일 9개… |
| `src/ui/panels/diagnostics.ts` | internal | function const | `countMap(o: Record<string, number>)` | Object.entries(o) .filter(([, n]) => n > 0) .map(([k, n]) =…, Object.entries(o) .filter(([, n]) => n > 0) .map, Object.entries(o) .filter, Object.entries | — |
| `src/ui/panels/diagnostics.ts` | internal | interface | `MetricSource` | — | 도메인 판정 뷰의 공통 모양 — 인라인 타입으로 두면 게이트가 지표 리터럴로 오인한다(§11 ③). |
| `src/ui/panels/diagnostics.ts` | internal | function | `metricFromView(label: string, v: MetricSource, kind: UnknownKind): Metric` | — | 도메인 판정 뷰 → `Metric`. 🔴 `kind`에 기본값이 없다 — 새 도구가 안 적으면 컴파일되지 않는다. 왜: `확인 불가`가 구조적 (이 기기에선 원리적으로 못 잼)인지 일시적 (재보면 알 수 있음)인지에 따라 총괄 판정에 넣을지가 갈린다(사용자 지적 2026-08-06). 기본값을 두면 그 판단을 안 하고… |
| `src/ui/panels/diagnostics.ts` | export | async function | `storageProbe(): Promise<Verdict>` | collectEnv, evictionRisk, Math.round, persistSurface, persistIsMeaningful, lastPersistAsk, persistAskNote, localDateTime, bytes, levelOr | — |
| `src/ui/panels/diagnostics.ts` | export | const | `STUCK_STATES` | — | 대기열을 두 가지로 쪼갠다 — 이게 옛 화면의 핵심 결함이었다. "대기 중인 작업 3건"은 아직 안 보낸 것 과 보내다 막힌 것 을 한 숫자에 섞었다. 앞은 동기화를 누르면 풀리는 '할 일'이고, 뒤는 눌러도 안 풀리는 '문제'다. 사용자가 해야 할 행동이 정반대다. |
| `src/ui/panels/diagnostics.ts` | export | function | `stuckMeaning(reasons: ReadonlyArray<{ reason: string; count: number }>): string` | reasons.slice(0, 2).map((r) => `${r.reason} (${r.count}건)`)…, reasons.slice(0, 2).map, reasons.slice | 「막힌 작업」의 설명 문장. 순수 함수 — 사용자에게 가는 말은 유닛이 직접 돌린다(§10 ③). 🔴 왜 사유를 붙이나(2026-08-05 · M-0107): 옛 문장은 "[실패 재시도]를 눌러 주세요" 한 줄이었다. 그런데 사용자 기기의 13건은 눌러도 절대 풀리지 않는 부류였다(부모가 영구삭제돼 서버에 그 관계가 … |
| `src/ui/panels/diagnostics.ts` | internal | interface | `LoadedTombstoneAudit` | — | — |
| `src/ui/panels/diagnostics.ts` | internal | async function | `loadTombstoneAudit(d: SyncDiagnosis): Promise<LoadedTombstoneAudit>` | d.opLessItems.map, supabase, unknown, currentUserSafe, auditOpLessTombstones, tombstoneAuditRemote | 로컬 숫자를 서버 판정으로 바꾼다. 한 요청이라도 실패하면 일부 결과를 정상으로 쓰지 않는다. 데이터는 읽기만 하며, 로그인 전·오프라인에는 로컬 tombstone을 그대로 보존한다. |
| `src/ui/panels/diagnostics.ts` | internal | function | `syncExplanationItems(d: SyncDiagnosis, findings: TombstoneSyncFinding[])` | findings.map, d.itemCandidates.filter, byKey.get, isConfirmedTombstone, candidates.slice, Math.max | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `syncProbe(): Promise<Verdict>` | diagnoseSync, loadTombstoneAudit, tombstoneSyncPresentation, syncExplanationItems, Object.entries(d.queue.byState) .filter(([s]) => STUCK_STAT…, Object.entries(d.queue.byState) .filter, Object.entries, STUCK_STATES.has, stuckMeaning, metricFromView | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `syncReleaseProbe(): Promise<Verdict>` | supabase, syncStatus, collectSyncReleaseObservation, new Date().toISOString, syncReleaseMetrics, views.map, metricFromView, levelFromMetrics, syncReleaseHeadline, table | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `placeZombieProbe(): Promise<Verdict>` | supabase, currentUserSafe, auditPlaceZombies, levelFromMetrics, sample.slice, Math.max, table, shown.map, candidate.activeId.slice, candidate.deletedIds.map((id) => id.slice(0, 8)).join | 이전 결함은 tombstone을 "없는 장소"로 오인해 새 UUID를 만들고, 그 UUID를 다시 업로드하면서 삭제 승리를 피해 갔다. 이 도구는 그 흔적을 서버와 로컬에서 읽기만 한다. 같은 곳을 사용자가 의도적으로 다시 등록했을 가능성도 있으므로 후보를 자동 삭제하지 않는다. 자동 정리는 또 다른 데이터 유실을 만드… |
| `src/ui/panels/diagnostics.ts` | export | async function | `integrityProbe(): Promise<Verdict>` | checkIntegrity, loadIntegritySnapshot, r.findings.filter, nowFinds.reduce, nowFinds.map((f) => `${f.title}: ${f.detail}`).join, nowFinds.map, prevFinds.reduce, prevFinds.map((f) => `${f.title}: ${f.detail}`).join, prevFinds.map, levelFromMetrics | — |
| `src/ui/panels/diagnostics.ts` | internal | const | `ESSENTIAL` | — | 없으면 앱이 제 기능을 못 하는 것 vs 없어도 대체 경로가 있는 것 — 같은 무게로 다루면 안 된다. |
| `src/ui/panels/diagnostics.ts` | export | async function | `environmentProbe(): Promise<Verdict>` | collectEnv, placeProviderView, ensureProviders, supabase, Object.entries(env.features) .filter(([, ok]) => !ok) .map, Object.entries(env.features) .filter, Object.entries, missing.filter, ESSENTIAL.has, Object.keys | — |
| `src/ui/panels/diagnostics.ts` | export | function | `errorProbe(): Promise<Verdict>` | recentErrors, levelFromMetrics, table, errs.map, e.at.slice, v.actions.push, clearErrors, Promise.resolve | — |
| `src/ui/panels/diagnostics.ts` | export | function | `classifyOrphanFiles(orphans: string[], purgedLedger: string[], restorePending: ReadonlySet<string>): { leftover: string[]; restoring: string[]; unexplained: string[] }` | orphans.filter, led.has, restorePending.has | 기록 없는 사진 파일 을 세 갈래로 가른다. 순수 함수 — 유닛이 모든 갈래를 직접 돌린다(§10 ③). 2026-07-26 사용자 실기기: 서버 행이 전부 0인데 R2에 파일 3개가 남았다. 영구삭제 시 바이트 삭제는 최선노력 이라(실패해도 op을 지운다) 재시도 기회가 없고, 그래서 잔재가 생긴다. 한 숫자에 두면 … |
| `src/ui/panels/diagnostics.ts` | export | function | `classifyMissingFiles(missing: string[], serverTombstoned: string[], localBytes: ReadonlySet<string>, otherDevices: number): { recoverable: string[]; clearable: string[]; atRisk: string[] }` | missing.filter, localBytes.has, dead.has | 파일이 없는 기록 을 세 갈래로 가른다. `classifyOrphanFiles`의 반대 방향이고, 가르는 질문은 하나뿐 이다 — 이 기기에 사본이 있는가. 🔴 왜 이 질문이 먼저인가(2026-07-28 사용자 실기기, M-0046): 예전에는 묻지 않고 tombstone 여부만 봤다. 그래서 로컬 휴지통에 멀쩡히 있는… |
| `src/ui/panels/diagnostics.ts` | export | function | `strandedMetric(stranded: number): Metric` | — | 전파되지 않은 영구삭제 지표 — 내가 지웠다고 믿는데(로컬 표식) 서버엔 tombstone으로 남은 것. 로컬 표식 때문에 휴지통에도 안 보이므로 어디서도 손댈 수 없는 상태 다 — 앱이 말해주지 않으면 영원히 남는다. (형제인 `blockedByLedgerMetric`은 처음부터 함수였는데 이쪽만 `storeState… |
| `src/ui/panels/diagnostics.ts` | export | function | `blockedByLedgerMetric(blocked: number, restoringFiles: number): Metric` | — | — |
| `src/ui/panels/diagnostics.ts` | export | interface | `StoreBecauseInput` | — | `storeBecause` 입력 — 판정 문장과 같은 자료 를 받는다(다른 것을 보면 두 문장이 갈라진다). |
| `src/ui/panels/diagnostics.ts` | export | interface | `StoreHeadlineInput` | — | — |
| `src/ui/panels/diagnostics.ts` | export | function | `storeHeadline(i: StoreHeadlineInput): string` | i.fileBad.filter, bad.reduce, bad.map((f) => f.noun).join, bad.map | — |
| `src/ui/panels/diagnostics.ts` | export | function | `storeBecause(i: StoreBecauseInput): string` | i.fileBad.reduce | 판정 문장 밑의 설명 한 줄. `storeHeadline`의 형제이고, 같은 규칙을 지나야 한다 (§7). 🔴 왜 순수 함수로 뽑았나(2026-08-01 사용자 실기기): 이 문장은 호출부에 삼항 연산자로 박혀 있었고 , 거기서 `fileBad`(배열)를 불리언으로 썼다. `fileBadByNoun`은 명사당 한 칸을… |
| `src/ui/panels/diagnostics.ts` | internal | function | `renameDeviceAction(): Action` | customDeviceName, setCustomDeviceName, detectDeviceLabel, readDeviceHints | 이 기기 이름 바꾸기. 왜(2026-07-26 사용자 실기기): 태블릿이 「PC · Linux · Chrome」으로 서버에 찍혔다. UA만 보는 한 이건 고칠 수 없다 — Chrome의 「데스크톱 사이트」가 UA를 통째로 바꿔 보내기 때문이다 (`deviceId.ts` 머리말). 감지를 아무리 다듬어도 사용자보다 정확할… |
| `src/ui/panels/diagnostics.ts` | internal | interface | `StoreActionsInput` | — | 「저장 상태」의 행동 버튼들 . 왜 따로 두나: `storeStateProbe`가 지표를 만들고 판정 문장을 고르고 버튼까지 짓느라 계속 커졌고, 길이 래칫이 이를 막았다(큰 함수는 결함이 숨을 면적이다). 판정(무엇이 문제인가)과 행동(무엇을 눌러 고치나)은 원래 다른 일이므로 여기서 갈라 둔다. primary는 하나… |
| `src/ui/panels/diagnostics.ts` | internal | function | `storeCleanupActions(i: StoreActionsInput): Action[]` | r2AbortMultipart, r2ListObjects, r2DeleteMany, purgeServerOnly, requestSync, syncStatus | 「저장 상태」의 치우기 버튼들 — 이미 자료가 없는 잔재만 다룬다. 되살리기·전파와 갈라 둔 이유는 성격이 정반대이기 때문이다. 위쪽 둘은 자료를 지키는 일이고 여기 셋은 찌꺼기를 버리는 일이다. 한 함수에 섞여 있으면 "지우는 버튼"과 "살리는 버튼"이 같은 코드 덩어리 안에서 서로의 조건을 보게 된다 — 실제로 복원… |
| `src/ui/panels/diagnostics.ts` | internal | function | `storeActions(i: StoreActionsInput): Action[]` | requeueMissingBytes, requestSync, syncStatus, requeueUnpropagatedPurges, storeCleanupActions, renameDeviceAction | — |
| `src/ui/panels/diagnostics.ts` | internal | function | `fileAuditEvidence(noun: string, fa: MediaFileAudit, totalBytes: number \\| null): { label: string; build: () => HTMLElement }` | bytes, ids.slice, rows.push, id.slice, show, table | 「사진 파일 N개 · 기록 M개 — 짝이 안 맞는 것은?」 근거 표. 제목이 표의 내용을 예고해야 한다(2026-07-27 사용자 지적). 예전엔 제목이 "9개 · 9개" 인데 펼치면 「없음」 이었다 — 제목은 가진 것 을 세고 표는 어긋난 것 을 담는데 그 한정을 아무도 말하지 않았다. 형제인 「서버에 남는 표식 N건… |
| `src/ui/panels/diagnostics.ts` | export | function | `fileAuditMetrics(i: { /** 사용자에게 보일 종류 이름(「사진」·「소리」). 라벨·설명 문장이 전부 이걸 쓴다. */ noun: string; fa: Me…): { /** 이 감사가 다룬 종류 이름. **지표와 함께 다닌다** — 갈라지면 M-0048이 재발한다. */ noun: string; metrics: Metric[]; lefto…` | classifyOrphanFiles, classifyMissingFiles, levelOr | 파일 대조 지표를 만드는 한 곳. 사진·소리·영상이 같은 규율을 지나게 한다(§7 2층). 왜 함수로 뽑았나(2026-07-27): 소리가 서버로 가면서 대조 대상이 둘이 됐다. 이 80줄을 손으로 한 번 더 쓰면 그 순간 드리프트가 시작된다 — 이 저장소가 이미 세 번 겪은 모양이다(§7 머리말). 지금은 `noun`… |
| `src/ui/panels/diagnostics.ts` | export | function | `fileBadByNoun(audits: ReadonlyArray<{ noun: string; metrics: Metric[] }>): Array<{ noun: string; n: number }>` | audits.map, a.metrics.filter | 파일 대조에서 어긋난 지표를 종류별로 센다 — 판정 문장이 이름을 부를 수 있게. 🔴 왜 순수 함수인가(M-0048): 예전엔 `metrics.slice(...)`로 한 숫자에 합쳐 놓고 문장은 「사진 파일에…」로 못 박혀 있었다. 소리를 형제로 들이자 숫자는 소리까지 셌는데 이름은 안 따라왔다 — 사진이 9:9로 멀… |
| `src/ui/panels/diagnostics.ts` | export | function | `countComparisonMetrics(cmp: StoreComparison): Metric[]` | PURGE_DOMAINS.map, out.push | 개수 대조 지표 — 도메인별 활성 수와 휴지통 수를 서버와 나란히 놓는다. 순수 함수. (파일 대조와 함께 `storeStateProbe` 안에 있었는데 `check-fn-size` 래칫에 걸려 뽑아냈다. 경계가 맞아떨어졌다 — 여기는 몇 개인가 를 묻고, `fileAuditMetrics`는 짝이 맞는가 를 묻는다. 다… |
| `src/ui/panels/diagnostics.ts` | internal | function | `storeFileAudits(cmp: StoreComparison, restorePending: Set<string>, otherDevices: number)` | fileAuditMetrics | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `storeStateProbe(): Promise<Verdict>` | supabase, currentUserSafe, deviceLabel, shortDeviceId, renameDeviceAction, unionListings, r2ListObjects, compareStore, storeStateRemote, pendingUnpurgeIds | — |
| `src/ui/panels/diagnostics.ts` | internal | async function | `currentUserSafe(): Promise<{ id: string } \\| null>` | import, currentUser | 로그인 조회 실패를 오류로 만들지 않는다(로그아웃과 구분이 안 되는 상황은 '없음'으로). |
| `src/ui/panels/diagnostics.ts` | export | function | `bannerLevelOf(per: { level: Level; unknownKind: UnknownKind }[]): Level` | per.filter, worst, counted.map | 🔴 배너가 쓸 총괄 등급 — 구조적 확인 불가를 뺀 것 (사용자 지적 2026-08-06). 순수 함수인 이유는 §4다: 라이브에서 이 갈래를 재려면 다른 이상이 하나도 없는 상태가 필요한데 실제 앱에는 늘 무언가가 켜져 있어, 라이브만 두면 가려져서 통과 한다. |
| `src/ui/panels/diagnostics.ts` | export | function | `rollupBanner(i: { bannerLevel: Level; badLabels: string[]; todoLabels: string[]; structuralC…): string` | i.badLabels.join, i.todoLabels.join | — |
| `src/ui/panels/diagnostics.ts` | export | interface | `RollupEntry` | — | — |
| `src/ui/panels/diagnostics.ts` | export | interface | `RollupResult` | — | — |
| `src/ui/panels/diagnostics.ts` | export | interface | `DiagnosticProgress` | — | — |
| `src/ui/panels/diagnostics.ts` | internal | async function | `probeTool(t: DiagTool): Promise<RollupEntry>` | t.probe, unknownIsStructural | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `runDiagnosticToolSet(wanted: DiagTool[], onProgress?: (progress: DiagnosticProgress) => void): Promise<RollupEntry[]>` | Promise.all, wanted.map, probeTool, onProgress | — |
| `src/ui/panels/diagnostics.ts` | internal | async function | `runTools(ids: string[], onProgress?: (progress: DiagnosticProgress) => void): Promise<RollupEntry[]>` | runDiagnosticToolSet, CORE_TOOLS.filter, ids.includes | — |
| `src/ui/panels/diagnostics.ts` | internal | function | `resultFromEntries(per: RollupEntry[]): RollupResult` | per.filter, worst, per.map, bannerLevelOf, new Date().toISOString | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `rollup(onProgress?: (progress: DiagnosticProgress) => void): Promise<RollupResult>` | resultFromEntries, runTools, CORE_TOOLS.map | — |
| `src/ui/panels/diagnostics.ts` | export | function | `failedAsyncToolIds(result: RollupResult): string[]` | CORE_TOOLS.map, result.per .filter((entry) => entry.level === 'problem' \\|\…, result.per .filter, modes.get | 재실행 가치가 있는 원격·비동기 도구 중 문제/일시적 확인 불가만 고른다. |
| `src/ui/panels/diagnostics.ts` | export | async function | `retryFailedDiagnostics(previous: RollupResult, onProgress?: (progress: DiagnosticProgress) => void): Promise<{ result: RollupResult; retried: number }>` | failedAsyncToolIds, runTools, fresh.map, previous.per.map, replacements.get, resultFromEntries | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `copyDiagSummary(snapshot?: RollupResult): Promise<string>` | summaryText, navigator.clipboard.writeText | 요약을 클립보드로. 결과 문장을 돌려준다 — 부르는 쪽이 그대로 화면에 띄운다(§13 4항: 누르면 결과가 화면에 나와야 한다). 실패도 조용히 삼키지 않고 다음 행동을 말한다(§7-D). |
| `src/ui/panels/diagnostics.ts` | export | async function | `summaryText(snapshot?: RollupResult): Promise<string>` | Promise.all, collectEnv, diagnoseSync, rollup, checkIntegrity, loadIntegritySnapshot, recentErrors, Object.entries(o) .filter(([, n]) => n > 0) .map(([k, n]) =…, Object.entries(o) .filter(([, n]) => n > 0) .map, Object.entries(o) .filter | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `summaryProbe(): Promise<Verdict>` | rollup, roll.per.map, metricFromView, roll.per.filter, copyDiagSummary, el, summaryText().then, summaryText | — |
| `src/ui/panels/diagnostics.ts` | export | function | `backupHeadline(input: { cloud: boolean; stale: boolean; roundTrip: 'pass' \\| 'fail' \\| null; }): string` | — | — |
| `src/ui/panels/diagnostics.ts` | export | function | `backupBecause(input: { cloud: boolean; stale: boolean; roundTrip: 'pass' \\| 'fail' \\| null; r…): string` | — | — |
| `src/ui/panels/diagnostics.ts` | export | function | `backupProbe(): Promise<Verdict>` | getLastBackupAt, backupFreshness, lastBackupRoundTrip, isConfigured, new Date(roundTrip.at).toLocaleString, levelFromMetrics, backupHeadline, backupBecause, exportBackupZip, saveBackupBlob | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `trashProbe(): Promise<Verdict>` | collectTrashState, trashMetrics, views.map, metricFromView, rows.slice(0, TRASH_ITEM_CAP).map, rows.slice, shown.push, evidence.push, table, cap | — |
| `src/ui/panels/diagnostics.ts` | export | function | `roundTripProbe(): Promise<Verdict>` | lastRoundTrip, roundTripView, metricFromView, evidence.push, table, run.steps.map, ROUND_TRIP_STEPS.map, runRoundTrip, actions.push, cleanupRoundTripLeftovers | — |
| `src/ui/panels/diagnostics.ts` | export | function | `videoRoundTripProbe(): Promise<Verdict>` | lastVideoRoundTrip, videoRoundTripView, evidence.push, table, run.steps.map, VIDEO_ROUND_TRIP_STEPS.map, runVideoRoundTrip, actions.push, cleanupVideoRoundTripLeftovers, Promise.resolve | 영상 바이트 형제의 독립 왕복 — 자동 probe는 마지막 결과만 읽고, 버튼에서만 서버에 쓴다. |
| `src/ui/panels/diagnostics.ts` | export | async function | `serverContractProbe(): Promise<Verdict>` | collectServerContract, serverContractMetrics, views.map, metricFromView, levelFromMetrics, serverContractHeadline | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `sessionProbe(): Promise<Verdict>` | collectSessionState, sessionMetrics, views.map, metricFromView, levelFromMetrics, sessionHeadline, table, AUTH_GATE_CASES.map | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `fileRealityProbe(): Promise<Verdict>` | tallyLocalFiles, lastSweep, fileRealityMetrics(input).map, fileRealityMetrics, metricFromView, levelFromMetrics, fileRealityHeadline, fileRealityScopeNote, sweepOpenFiles, table | — |
| `src/ui/panels/diagnostics.ts` | export | async function | `deviceFleetProbe(): Promise<Verdict>` | supabase, currentUserSafe, compareStore, storeStateRemote, cmp.devices.map, isConfigured, Boolean, new Date().toISOString, fleetMetrics, views.map | — |
| `src/ui/panels/diagnostics.ts` | export | interface | `DiagTool` | — | — |
| `src/ui/panels/diagnostics.ts` | export | const | `CORE_TOOLS: DiagTool[]` | — | 판정 롤업에 참여하는 도구 — 요약 자신은 롤업 대상이 아니다(자기참조가 된다). |
| `src/ui/panels/diagnostics.ts` | export | const | `SUMMARY_TOOL: DiagTool` | — | — |
| `src/ui/panels/diagnostics.ts` | export | const | `DIAG_TOOLS: DiagTool[]` | — | — |
| `src/ui/panels/diagnostics.ts` | export | function | `renderDiagTool(t: DiagTool): HTMLElement` | blindSpotsForTool, renderTool, t.probe, spots.map((spot) => `${spot.id}(${spot.kind === 'structural…, spots.map | 도구 하나를 그린다 — 모든 도구가 같은 렌더러를 통과한다 . |
| `src/ui/panels/verdict.ts` | export | typealias | `Level` | — | 판정 4단. 3단(정상/주의/문제)이 아니라 4단인 이유는 정직함 때문이다(비타협 원칙 #4). 진단에는 현재 증거로는 알 수 없는 상태가 실제로 있다. op 없는 tombstone은 로컬만으로 구분할 수 없어 서버 행·영구삭제 원장까지 대조하고, 거기에도 없거나 조회가 불완전할 때만 unknown이다 (M-0008·M… |
| `src/ui/panels/verdict.ts` | export | interface | `LevelMeta` | — | — |
| `src/ui/panels/verdict.ts` | export | const | `LEVELS: Record<Level, LevelMeta>` | — | — |
| `src/ui/panels/verdict.ts` | export | function | `worst(levels: Level[]): Level` | levels.reduce | 여러 판정의 롤업 — 가장 급한 것이 이긴다. 빈 배열은 '정상'이 아니라 '확인 불가'다. |
| `src/ui/panels/verdict.ts` | export | typealias | `UnknownKind` | — | 🔴 `확인 불가`가 왜 확인 불가인가 — 두 가지는 사용자에게 뜻이 정반대다 (사용자 지적 2026-08-06: "구조적으로 확인불가이면 확인불가로 판정은 하되 비정상으로 분류하면 안 될 거 같아요 " ). · `structural` — 이 기기·이 배포에서는 원리적으로 잴 수 없다. 다시 눌러도 안 된다. 예: AP… |
| `src/ui/panels/verdict.ts` | internal | interface | `MetricBase` | — | 지표의 공통 부분 — 등급과 무관하게 언제나 필요한 것. |
| `src/ui/panels/verdict.ts` | export | typealias | `Metric` | — | 지표 하나 — 기대값이 필수다 . 선택 필드로 두면 빠뜨린 지표가 조용히 섞여 들어와 "값만 있고 기준은 없는" 옛 화면으로 되돌아간다. 기대값을 못 쓰는 지표라면 그건 지표가 아니라 맥락 이다 → `context`로 보낸다. 그리고 `unknown`이면 왜 모르는지의 종류 (`unknownKind`)도 필수다 — 위 … |
| `src/ui/panels/verdict.ts` | export | function | `levelOr(isUnknown: boolean, kind: UnknownKind, otherwise: 'ok' \\| 'todo' \\| 'problem'): { level: 'unknown'; unknownKind: UnknownKind } \\| { level: 'ok' \\| 'todo' \\| 'problem' }` | — | 지표 리터럴에서 등급 부분만 만든다 — `...levelOr(조건, 종류, 아니면)`으로 펼쳐 쓴다. 왜: 유니온 때문에 손으로 쓰면 세 줄짜리 삼항이 지표마다 반복된다. 한 곳에 두면 짧아질 뿐 아니라, `kind`를 안 넘기면 컴파일되지 않는다 는 계약이 그대로 유지된다(기본값 금지). |
| `src/ui/panels/verdict.ts` | export | function | `unknownIsStructural(metrics: Metric[]): boolean` | metrics.filter, unknowns.every | — |
| `src/ui/panels/verdict.ts` | internal | interface | `ActionBase` | — | — |
| `src/ui/panels/verdict.ts` | internal | interface | `TextAction` | — | — |
| `src/ui/panels/verdict.ts` | internal | interface | `FileAction` | — | — |
| `src/ui/panels/verdict.ts` | export | typealias | `Action` | — | 입력 종류가 늘어나도 모든 진단 도구가 같은 렌더러·접근성·read-back 규율을 물려받는다. |
| `src/ui/panels/verdict.ts` | internal | function | `isFileAction(action: Action): action is FileAction` | — | — |
| `src/ui/panels/verdict.ts` | export | interface | `Evidence` | — | 접힌 '출처' 층 — 목록·기술코드·원문 덤프는 전부 여기로 내려간다. |
| `src/ui/panels/verdict.ts` | export | interface | `Verdict` | — | — |
| `src/ui/panels/verdict.ts` | export | function | `emptyVerdict(level: Level, headline: string): Verdict` | — | 판정 없이 만들 수 있는 빈 골격 — 도구가 필드를 빠뜨리지 않게. |
| `src/ui/panels/verdict.ts` | export | function | `levelFromMetrics(metrics: Metric[]): Level` | worst, metrics.map | 지표들에서 전체 판정을 파생한다. 손으로 level을 적으면 지표는 빨간데 headline은 초록인 화면이 만들어진다(자기모순). 파생하면 그 모순이 구조적으로 불가능해진다. |
| `src/ui/panels/verdict.ts` | internal | function | `badge(level: Level): HTMLElement` | el, b.append | — |
| `src/ui/panels/verdict.ts` | internal | function | `metricCard(m: Metric): HTMLElement` | el, top.append, badge, now.append, exp.append, cmp.append, card.append, card.appendChild | 이상 지표 하나 = 카드. 기준 대조 가 카드의 본문이다. |
| `src/ui/panels/verdict.ts` | internal | function | `quietBlock(ok: Metric[]): HTMLElement` | el, s.append, d.appendChild, r.append, mini.appendChild | 정상 지표들 — 나열하지 않는다 . 한 줄 + 접힌 미니표. |
| `src/ui/panels/verdict.ts` | export | interface | `ToolView` | — | — |
| `src/ui/panels/verdict.ts` | internal | function | `showActionFailure(message: HTMLElement, error: Error, reread: () => void): void` | reread | — |
| `src/ui/panels/verdict.ts` | internal | function | `appendAction(actionBar: HTMLElement, message: HTMLElement, action: Action, reread: () => void): void` | el, isFileAction, field.setAttribute, action.input.initial, actionBar.appendChild, button.setAttribute, field?.addEventListener, button.click, button.addEventListener, applyText | 액션 한 개의 입력·버튼·실패·read-back 배선. renderTool 안에 두면 입력 종류가 늘 때마다 판정 렌더 전체가 커지고, 도구별로 파일 UI를 따로 만들게 된다. 모든 입력은 이 문을 지난다. |
| `src/ui/panels/verdict.ts` | export | function | `renderTool(v: ToolView): HTMLElement` | el, wrap.setAttribute, head.append, badge, card.append, wrap.append, head.replaceChildren, v .probe() .then((r) => { card.className = `vd-card vd-card…, v .probe() .then, v .probe | 도구 하나를 그린다 — 모든 진단 도구가 이 함수를 통과한다 . 계약상 중요한 것 두 가지: - 계산 전에는 절대 '정상'을 그리지 않는다. 빈 화면에 초록을 먼저 칠하면 그건 거짓말이다. - 액션 실행 후에는 다시 판정한다 . 고쳤다고 말만 하고 화면이 옛 값을 들고 있으면 사용자는 고쳐졌는지 알 수 없다("read-… |
| `src/ui/screens/diagnosticsHub.ts` | internal | function | `card(t: DiagTool, onOpen: (t: DiagTool) => void): { btn: HTMLButtonElement; slot: HTMLElement }` | el, btn.setAttribute, ic.setAttribute, mid.append, slot.append, btn.append, btn.addEventListener, onOpen | 허브 카드 하나 — 구조는 [가이드]·[데이터 관리]와 같은 계약 (ic / mid / 우측 슬롯)을 지킨다. |
| `src/ui/screens/diagnosticsHub.ts` | internal | function | `renderByPath(body: HTMLElement, onOpen: (t: DiagTool) => void): Map<string, HTMLElement>` | DIAG_TOOLS.filter, el, head.setAttribute, head.append, body.appendChild, BLIND_SPOTS.filter, why.map((b) => b.what).join, why.map, card, slots.set | 🔴 도구를 경로축 단계로 묶어 그린다 (v1.79). 사용자가 진단을 여는 이유는 늘 "어디서 끊겼나?" 이므로, 목록이 기억이 지나는 순서 로 서면 위에서부터 좁힐 수 있다. v1.76에서 이 분류(`DIAG_GROUPS`)를 만들어 놓고 도구에는 안 걸어서 목록이 평평한 채로 남아 있었다 — 분류는 사각지대 등록부… |
| `src/ui/screens/diagnosticsHub.ts` | internal | function | `dot(level: Level): HTMLElement` | el, d.setAttribute | — |
| `src/ui/screens/diagnosticsHub.ts` | internal | function | `hubHeader(): { header: HTMLElement; closeBtn: HTMLButtonElement }` | el, titleWrap.append, closeBtn.setAttribute, header.append | 허브 머리말 — 제목·설명·닫기. 조립만 하고 배선은 부르는 쪽이 한다. |
| `src/ui/screens/diagnosticsHub.ts` | internal | function | `renderDetail(body: HTMLElement, t: DiagTool, onBack: () => void): void` | body.replaceChildren, el, back.addEventListener, bar.append, body.append, renderDiagTool, back.focus | 도구 하나를 상세로 그린다 — 뒤로 가기는 허브 홈 이지 화면 닫기가 아니다. |
| `src/ui/screens/diagnosticsHub.ts` | internal | function | `rollupActions(actions: { recheckAll: (progress: (value: DiagnosticProgress) => void) => Promi…): { nodes: HTMLElement[]; runInitial: () => void }` | el, recheck.setAttribute, retry.setAttribute, copy.setAttribute, note.setAttribute, bar.append, applyText, buttons.forEach, say, run((progress) => say(`${progress.completed}/${progress.tot… | 총괄 판정 바로 아래의 [🔄 일괄 점검] · [📋 결과 복사] (사용자 제안 2026-08-06: "상단에 일괄 점검 버튼 누르고 점검결과를 바로 옆에 버튼을 만들어서 복사할 수 있도록" ). 왜 위인가: 예전엔 복사가 맨 아래 「진단 요약 복사」 도구 안 에 있었다. 문제가 보이는 순간(맨 위)과 전달 수단(맨 아… |
| `src/ui/screens/diagnosticsHub.ts` | export | interface | `DiagHubOptions` | — | 진단 허브를 연다. `toolId`를 주면 그 도구로 바로 들어간다 (허브 홈을 거치지 않는다). 왜(사용자 제안 2026-07-26): 첫 화면의 「동기화 대기 13건」 칩이 말만 하고 갈 곳을 주지 않았다. 사용자는 [데이터 관리] → [진단 도구] → [동기화 상태]를 스스로 찾아 들어가야 했다. 진단 §7-B와 … |
| `src/ui/screens/diagnosticsHub.ts` | internal | function | `trapDialogFocus(modal: HTMLElement, event: KeyboardEvent): void` | [...modal.querySelectorAll<HTMLElement>('button:not([disabl…, modal.querySelectorAll, node.getClientRects, event.preventDefault, last.focus, first.focus | — |
| `src/ui/screens/diagnosticsHub.ts` | internal | function | `wireDialogClose(overlay: HTMLElement, modal: HTMLElement, closeBtn: HTMLButtonElement, afterClose: () => void): () => void` | overlay.remove, document.removeEventListener, afterClose, close, trapDialogFocus, closeBtn.addEventListener, overlay.addEventListener, document.addEventListener | — |
| `src/ui/screens/diagnosticsHub.ts` | export | function | `openDiagnosticsHub(toolId?: string, opts?: DiagHubOptions): void` | el, overlay.setAttribute, hubHeader, body.replaceChildren, bTop.append, banner.append, banner.setAttribute, body.appendChild, renderByPath, bDot.setAttribute | — |
| `src/ui/screens/mechChecks.ts` | internal | const | `FLOW: [string, string][]` | — | — |
| `src/ui/screens/mechChecks.ts` | export | function | `openMechChecks(): void` | el, overlay.setAttribute, tw.append, closeBtn.setAttribute, header.append, modal.append, overlay.appendChild, body.appendChild, FLOW.forEach, step.append | — |

## 5. 자동 검증 연결

- `tests/unit/audioSiblingDiscipline.test.ts`
- `tests/unit/backupVerdict.test.ts`
- `tests/unit/diagnoseItems.test.ts`
- `tests/unit/diagnosticReport.test.ts`
- `tests/unit/diagnosticRun.test.ts`
- `tests/unit/fileRealityVerdict.test.ts`
- `tests/unit/gateControlView.test.ts`
- `tests/unit/instantFormat.test.ts`
- `tests/unit/integrity.test.ts`
- `tests/unit/mediaFileAudit.test.ts`
- `tests/unit/placeProviderVerdict.test.ts`
- `tests/unit/placeZombieVerdict.test.ts`
- `tests/unit/rollupBanner.test.ts`
- `tests/unit/roundTripVerdict.test.ts`
- `tests/unit/serverContractVerdict.test.ts`
- `tests/unit/sessionVerdict.test.ts`
- `tests/unit/stuckReason.test.ts`
- `tests/unit/syncTombstoneVerdict.test.ts`
- `tests/unit/verdict.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/ui/panels/diagnostics.ts` — 2870줄
- `src/ui/panels/verdict.ts` — 414줄
- `src/ui/screens/diagnosticsHub.ts` — 375줄
- `src/domain/integrity.ts` — 322줄
- `src/domain/diagGroups.ts` — 304줄
- `src/services/diagnostics.ts` — 283줄
- `src/services/roundTrip.ts` — 271줄
- `src/domain/serverContractVerdict.ts` — 231줄
- `src/domain/fileRealityVerdict.ts` — 220줄
- `src/services/envReport.ts` — 212줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
