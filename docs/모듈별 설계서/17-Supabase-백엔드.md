---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# Supabase 스키마·RLS·Edge Function 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

서버 스키마, migration, RLS/grant와 독립 배포 Edge Function을 소유한다.

- 모듈 ID: `supabase-backend`
- 관측 파일: 42개 · 논리 줄: 4192 · 최상위 선언: 74개(외부 공개 47개)
- 코드 내용 SHA-256: `48d65daa9f512719f87d8ea31fc87af8edcf913b27f254d616f5e622b92e4ef7`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `supabase/functions/geocode/index.ts`
- `supabase/functions/media-sign/index.ts`
- `supabase/migrations/0001_journey_schema_trips.sql`
- `supabase/migrations/0002_journey_invite_only.sql`
- `supabase/migrations/0003_journey_moments.sql`
- `supabase/migrations/0004_journey_zombie_guard.sql`
- `supabase/migrations/0005_journey_moments_place_coords.sql`
- `supabase/migrations/0006_journey_media.sql`
- `supabase/migrations/0007_journey_media_storage.sql`
- `supabase/migrations/0008_journey_expenses.sql`
- `supabase/migrations/0009_journey_expenses_category_note.sql`
- `supabase/migrations/0010_journey_media_storage_delete.sql`
- `supabase/migrations/0011_journey_purged_at.sql`
- `supabase/migrations/0012_journey_purged_ids.sql`
- `supabase/migrations/0013_purged_ids_grants.sql`
- `supabase/migrations/0014_retire_journey_media_bucket.sql`
- `supabase/migrations/0015_journey_hard_delete_grants.sql`
- `supabase/migrations/0016_drop_journey_media_bucket.sql`
- `supabase/migrations/0017_unpurge_for_restore.sql`
- `supabase/migrations/0018_rls_initplan_and_fn_hygiene.sql`
- `supabase/migrations/0019_journey_audio.sql`
- `supabase/migrations/0020_audio_purged_guard.sql`
- `supabase/migrations/0021_trip_time_zone.sql`
- `supabase/migrations/0022_journey_places.sql`
- `supabase/migrations/0023_moments_place_link.sql`
- `supabase/migrations/0024_media_gps.sql`
- `supabase/migrations/0025_purged_reinsert_user_scope.sql`
- `supabase/migrations/0026_reject_stale_sync_writes.sql`
- `supabase/migrations/0027_canonical_sync_meta.sql`
- `supabase/migrations/0028_journey_fk_covering_indexes.sql`
- `supabase/migrations/0029_media_sort_order.sql`
- `supabase/migrations/0030_journey_videos.sql`
- `supabase/migrations/0031_videos_comment_fix.sql`
- `supabase/migrations/20260813113101_add_moment_companion_names.sql`
- `supabase/tests/canonical_sync_meta.sql`
- `supabase/tests/rls_attack_media_expenses.sql`
- `supabase/tests/rls_attack_moments.sql`
- `supabase/tests/rls_attack_trips.sql`
- `supabase/tests/rls_invite_only_trips.sql`
- `supabase/tests/stale_sync_write_guard.sql`
- `supabase/tests/storage_delete_rls.sql`
- `supabase/tests/zombie_guard.sql`

### 다른 모듈로 나가는 정적 의존

- 다른 소유 모듈로 나가는 정적 import가 관측되지 않았다.

### 외부 패키지/런타임 의존

- Node 내장 또는 저장소 내부 모듈만 정적으로 import한다.

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `supabase/functions/geocode/index.ts` | 486 | 14/26 | — | network fetch |
| `supabase/functions/media-sign/index.ts` | 761 | 33/48 | — | network fetch; integrity/crypto; deletion lifecycle |
| `supabase/migrations/0001_journey_schema_trips.sql` | 78 | 0/0 | — | SQL table: trips; SQL function: set_updated_at; deletion lifecycle |
| `supabase/migrations/0002_journey_invite_only.sql` | 65 | 0/0 | — | SQL table: allowed_users; SQL function: is_allowed; deletion lifecycle |
| `supabase/migrations/0003_journey_moments.sql` | 57 | 0/0 | — | SQL table: moments; deletion lifecycle |
| `supabase/migrations/0004_journey_zombie_guard.sql` | 36 | 0/0 | — | SQL function: prevent_zombie_resurrection; deletion lifecycle |
| `supabase/migrations/0005_journey_moments_place_coords.sql` | 6 | 0/0 | — | SQL table: moments |
| `supabase/migrations/0006_journey_media.sql` | 43 | 0/0 | — | SQL table: media; deletion lifecycle |
| `supabase/migrations/0007_journey_media_storage.sql` | 16 | 0/0 | — | deletion lifecycle |
| `supabase/migrations/0008_journey_expenses.sql` | 38 | 0/0 | — | SQL table: expenses; deletion lifecycle |
| `supabase/migrations/0009_journey_expenses_category_note.sql` | 6 | 0/0 | — | SQL table: expenses |
| `supabase/migrations/0010_journey_media_storage_delete.sql` | 7 | 0/0 | — | deletion lifecycle |
| `supabase/migrations/0011_journey_purged_at.sql` | 31 | 0/0 | — | SQL table: expenses, media, moments, trips; deletion lifecycle |
| `supabase/migrations/0012_journey_purged_ids.sql` | 79 | 0/0 | — | SQL table: expenses, media, moments, purged_ids, trips; SQL function: block_purged_reinsert; deletion lifecycle |
| `supabase/migrations/0013_purged_ids_grants.sql` | 39 | 0/0 | — | deletion lifecycle |
| `supabase/migrations/0014_retire_journey_media_bucket.sql` | 32 | 0/0 | — | — |
| `supabase/migrations/0015_journey_hard_delete_grants.sql` | 51 | 0/0 | — | deletion lifecycle |
| `supabase/migrations/0016_drop_journey_media_bucket.sql` | 38 | 0/0 | — | deletion lifecycle |
| `supabase/migrations/0017_unpurge_for_restore.sql` | 73 | 0/0 | — | SQL function: unpurge_ids; deletion lifecycle |
| `supabase/migrations/0018_rls_initplan_and_fn_hygiene.sql` | 169 | 0/0 | — | deletion lifecycle |
| `supabase/migrations/0019_journey_audio.sql` | 125 | 0/0 | — | SQL table: audio; deletion lifecycle |
| `supabase/migrations/0020_audio_purged_guard.sql` | 31 | 0/0 | — | deletion lifecycle |
| `supabase/migrations/0021_trip_time_zone.sql` | 24 | 0/0 | — | SQL table: moments, trips |
| `supabase/migrations/0022_journey_places.sql` | 200 | 0/0 | — | SQL table: places; deletion lifecycle |
| `supabase/migrations/0023_moments_place_link.sql` | 44 | 0/0 | — | SQL table: moments; deletion lifecycle |
| `supabase/migrations/0024_media_gps.sql` | 40 | 0/0 | — | SQL table: media |
| `supabase/migrations/0025_purged_reinsert_user_scope.sql` | 47 | 0/0 | — | SQL function: block_purged_reinsert |
| `supabase/migrations/0026_reject_stale_sync_writes.sql` | 109 | 0/0 | — | SQL function: guard_sync_write; deletion lifecycle |
| `supabase/migrations/0027_canonical_sync_meta.sql` | 342 | 0/0 | — | SQL table: audio, expenses, media, moments, places, sync_meta, trips; SQL function: ensure_sync_meta, guard_canonical_sync_write, publish_canonical_snapshot; deletion lifecycle |
| `supabase/migrations/0028_journey_fk_covering_indexes.sql` | 35 | 0/0 | — | deletion lifecycle |
| `supabase/migrations/0029_media_sort_order.sql` | 40 | 0/0 | — | Dexie: localMedia; SQL table: media |
| `supabase/migrations/0030_journey_videos.sql` | 141 | 0/0 | — | SQL table: videos; SQL function: publish_canonical_snapshot; deletion lifecycle |
| `supabase/migrations/0031_videos_comment_fix.sql` | 20 | 0/0 | — | — |
| `supabase/migrations/20260813113101_add_moment_companion_names.sql` | 209 | 0/0 | — | SQL table: moments; SQL function: publish_canonical_snapshot; deletion lifecycle |
| `supabase/tests/canonical_sync_meta.sql` | 188 | 0/0 | — | deletion lifecycle |
| `supabase/tests/rls_attack_media_expenses.sql` | 53 | 0/0 | — | deletion lifecycle |
| `supabase/tests/rls_attack_moments.sql` | 56 | 0/0 | — | — |
| `supabase/tests/rls_attack_trips.sql` | 100 | 0/0 | — | deletion lifecycle |
| `supabase/tests/rls_invite_only_trips.sql` | 60 | 0/0 | — | — |
| `supabase/tests/stale_sync_write_guard.sql` | 158 | 0/0 | — | deletion lifecycle |
| `supabase/tests/storage_delete_rls.sql` | 24 | 0/0 | — | deletion lifecycle |
| `supabase/tests/zombie_guard.sql` | 35 | 0/0 | — | deletion lifecycle |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `supabase/functions/geocode/index.ts` | internal | const | `DENO` | — | Deno 런타임 전역. Node에서는 undefined → serve를 호출하지 않고 순수 함수만 노출한다. |
| `supabase/functions/geocode/index.ts` | internal | function const | `envGet(k: string)` | DENO?.env.get | — |
| `supabase/functions/geocode/index.ts` | export | const | `FN_VERSION` | — | 함수 판(version) — 앱이 「서버 함수가 낡았다」를 스스로 말할 수 있게(M-0031 규율). |
| `supabase/functions/geocode/index.ts` | export | const | `FN_OPS` | — | 이 함수가 처리하는 op. `check-edge-fn-ops`가 구현과 이 목록의 어긋남을 양방향으로 잡는다. |
| `supabase/functions/geocode/index.ts` | internal | const | `LIMIT` | — | 결과 개수 상한 — 남의 서버에 예의를 지키고 화면도 감당 가능하게. |
| `supabase/functions/geocode/index.ts` | export | interface | `NormalizedRow` | — | — |
| `supabase/functions/geocode/index.ts` | internal | function const | `num(v: unknown)` | v.trim, Number, Number.isFinite | — |
| `supabase/functions/geocode/index.ts` | internal | function const | `str(v: unknown)` | v.trim | — |
| `supabase/functions/geocode/index.ts` | export | function | `splitKoreanAddress(addr: string \\| null): { region: string \\| null; city: string \\| null }` | addr.trim().split, addr.trim | 한국 주소 문자열 → 시·도 / 시·군·구. 국내 제공자는 구조화 주소를 조각으로 주지 않고 한 줄 문자열 로 준다 (`"서울 종로구 동숭동 1-1"`). 조각이 필요한 이유는 저장 때문이다 — 주소 표기는 행정구역 개편으로 바뀌지만 어느 시·구였는지 는 기억의 일부다. 앞 두 토큰만 본다. 더 깊이 파면 동·리·번지까… |
| `supabase/functions/geocode/index.ts` | export | function | `kakaoAddressRank(addressType: string \\| null): number \\| null` | — | 카카오 주소검색의 `address_type` → Nominatim 랭크 자. 근거(카카오 문서의 뜻을 우리 등급으로 옮긴 것): · `ROAD_ADDR`/`REGION_ADDR` = 건물번호까지 확정된 주소 → 건물(28+) · `ROAD` = 도로명만 → 길(26) · `REGION` = 지역명만 → 동네(22) 모르… |
| `supabase/functions/geocode/index.ts` | export | function | `normalizeKakaoKeyword(json: unknown): NormalizedRow[]` | Array.isArray, num, str, splitKoreanAddress, out.push, out.slice | 카카오 키워드(장소) 검색 응답 → 표준 행. 키워드 검색의 결과는 정의상 POI(가게·건물·시설) 이므로 건물 등급으로 옮긴다. 이건 추측이 아니라 그 엔드포인트의 계약이다 — 주소검색은 아래 함수가 따로 다룬다. |
| `supabase/functions/geocode/index.ts` | export | function | `normalizeKakaoAddress(json: unknown): NormalizedRow[]` | Array.isArray, num, str, splitKoreanAddress, out.push, kakaoAddressRank, out.slice | 카카오 주소 검색 응답 → 표준 행. 랭크는 `address_type`이 정한다(위 함수 참조). |
| `supabase/functions/geocode/index.ts` | export | function | `normalizeVworld(json: unknown, asked: 'place' \\| 'address'): NormalizedRow[]` | Array.isArray, num, str, splitKoreanAddress, out.push, out.slice | VWorld 검색 응답 → 표준 행. VWorld는 `type=place`(POI)와 `type=address`(주소)를 나눠 부르며, 응답 모양은 같다 (`response.result.items[]`). 그래서 랭크는 무엇을 물었는지 로 정한다. |
| `supabase/functions/geocode/index.ts` | export | function | `availableProviders(env: (k: string) => string \\| undefined): string[]` | env, out.push | 설정된 시크릿으로부터 지금 실제로 쓸 수 있는 제공자 목록. 없으면 빈 배열. |
| `supabase/functions/geocode/index.ts` | internal | const | `CORS: Record<string, string>` | — | 🔴 이 함수는 다른 출처 (앱은 ` .github.io`, 함수는 ` .supabase.co`)에서 불린다. 그러면 브라우저가 본 요청 앞에 사전요청(OPTIONS) 을 먼저 보내는데, 그 요청에는 본문이 없어 아래 `req.json()`이 던지고 400 이 나갔다. 본 요청은 나가지도 못했다. 실측(2026-08-1… |
| `supabase/functions/geocode/index.ts` | internal | function const | `json(body: unknown, status = 200)` | JSON.stringify | — |
| `supabase/functions/geocode/index.ts` | internal | async function | `verifyUser(req: Request): Promise<string \\| null>` | req.headers.get, envGet, auth.startsWith, fetch, r.json | 요청자의 JWT를 직접 확인한다(플랫폼 설정에 기대지 않는다 — media-sign과 같은 규율). 🔴 예전에는 `boolean`을 돌려줬다. 이제 사용자 id 를 돌려준다 — 속도 한도를 걸려면 「누가」가 필요한데, 이 호출이 이미 사용자 본문을 받아오므로 왕복을 더 늘리지 않는다. 실패·불명은 `null`이고, 그… |
| `supabase/functions/geocode/index.ts` | export | const | `MAX_QUERY_LEN` | — | 질의 길이 상한. 실제 주소·상호는 이보다 훨씬 짧다 — 넘는 것은 검색이 아니라 부하다. |
| `supabase/functions/geocode/index.ts` | export | const | `RATE_WINDOW_MS` | — | 한 사용자에게 허용하는 창의 길이와 횟수. 🔴 이 숫자는 「악의를 막는 선」이 아니라 「폭주를 잡는 선」이다 (사용자 결정 2026-08-12 · ADR-0065). 이 함수는 초대제 뒤에 있어서 부르려면 이미 허용목록 안의 계정 이어야 한다 — 혼자 쓰는 앱에서 그건 사용자 자신이다. 그래서 남는 실제 위험은 악의가… |
| `supabase/functions/geocode/index.ts` | export | const | `RATE_MAX_PER_WINDOW` | — | — |
| `supabase/functions/geocode/index.ts` | export | interface | `RateState` | — | — |
| `supabase/functions/geocode/index.ts` | export | function | `allowRequest(prev: RateState \\| undefined, now: number, max = RATE_MAX_PER_WINDOW, windowMs = RATE_WINDOW_MS): { ok: boolean; next: RateState; retryAfterSec: number }` | Math.max, Math.ceil | 순수 판정 — 이 요청을 받아도 되는가. 상태를 돌려주고 저장은 호출부가 한다. 순수하니 유닛이 시계를 직접 먹여 경계를 잰다(§10 ③ — 재려면 순수해야 한다). |
| `supabase/functions/geocode/index.ts` | internal | const | `rateState` | — | 이 인스턴스의 카운터. 위 주석대로 전역 보증이 아니다. |
| `supabase/functions/geocode/index.ts` | internal | async function | `isInvited(req: Request): Promise<boolean>` | req.headers.get, envGet, auth.startsWith, fetch, r.json | 🔐 초대제 확인 — media-sign과 같은 규율 . 인증만으로는 부족하다. journey 테이블은 RLS로 초대제(`journey.is_allowed()`)를 걸지만, 지오코딩은 DB를 거치지 않으므로 그 방어가 빠진다 — 허용목록 밖 계정도 우리 지도 API 키로 검색을 돌릴 수 있었다. 허용목록 SSOT(`jo… |
| `supabase/functions/geocode/index.ts` | internal | async function | `searchKakao(key: string, q: string): Promise<NormalizedRow[]>` | Promise.all, fetch(`${base}/keyword.json?size=${LIMIT}&query=${encodeURI…, fetch, encodeURIComponent, r.json, fetch(`${base}/address.json?size=${LIMIT}&query=${encodeURI…, normalizeKakaoKeyword, normalizeKakaoAddress | — |
| `supabase/functions/geocode/index.ts` | internal | async function | `searchVworld(key: string, q: string): Promise<NormalizedRow[]>` | String, p.set, fetch(`https://api.vworld.kr/req/search?${p.toString()}`).c…, fetch, p.toString, normalizeVworld, r.json().catch, r.json, Promise.all, call | — |
| `supabase/functions/media-sign/index.ts` | internal | const | `DENO` | — | Deno 런타임 전역. Node에서는 undefined → serve를 호출하지 않고 순수 함수만 노출한다. |
| `supabase/functions/media-sign/index.ts` | internal | function const | `envGet(k: string)` | DENO?.env.get | — |
| `supabase/functions/media-sign/index.ts` | export | const | `ALGO` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `REGION` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `SERVICE` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `EXPIRES_SEC` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `UUID_RE` | — | — |
| `supabase/functions/media-sign/index.ts` | internal | const | `CORS: Record<string, string>` | — | — |
| `supabase/functions/media-sign/index.ts` | export | interface | `R2Env` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `R2_SECRET_NAMES` | — | — |
| `supabase/functions/media-sign/index.ts` | internal | function | `r2Env(): R2Env \\| null` | R2_SECRET_NAMES.map, envGet, accountId.trim, bucket.trim, accessKeyId.trim, secretAccessKey.trim | — |
| `supabase/functions/media-sign/index.ts` | internal | const | `enc` | — | — |
| `supabase/functions/media-sign/index.ts` | internal | function | `hex(buf: ArrayBuffer): string` | Array.from(new Uint8Array(buf)) .map((b) => b.toString(16).…, Array.from(new Uint8Array(buf)) .map, Array.from, b.toString(16).padStart, b.toString | — |
| `supabase/functions/media-sign/index.ts` | internal | async function | `hmac(key: ArrayBuffer \\| Uint8Array, data: string): Promise<ArrayBuffer>` | crypto.subtle.importKey, crypto.subtle.sign, enc.encode | — |
| `supabase/functions/media-sign/index.ts` | internal | async function | `sha256hex(s: string): Promise<string>` | hex, crypto.subtle.digest, enc.encode | — |
| `supabase/functions/media-sign/index.ts` | export | function | `uriEncode(s: string): string` | encodeURIComponent(s).replace, encodeURIComponent, c.charCodeAt(0).toString(16).toUpperCase, c.charCodeAt(0).toString, c.charCodeAt | RFC 3986. encodeURIComponent가 남기는 !'() 까지 인코딩한다(S3 정규화 요구). |
| `supabase/functions/media-sign/index.ts` | export | function | `amzDateOf(ms: number): string` | new Date(ms).toISOString().replace(/[:-]/g, '').replace, new Date(ms).toISOString().replace, new Date(ms).toISOString | ISO 문자열 → SigV4 basic 형식(YYYYMMDDTHHMMSSZ). |
| `supabase/functions/media-sign/index.ts` | export | async function | `presign(env: R2Env, method: 'PUT' \\| 'GET' \\| 'DELETE', key: string, nowMs: number, extraQuery: [string, string][] = []): Promise<string>` | amzDateOf, amzDate.slice, uriEncode, key.split('/').map(uriEncode).join, key.split('/').map, key.split, ( [ ['X-Amz-Algorithm', ALGO], ['X-Amz-Credential', `${env.…, String, [method, canonicalUri, canonicalQuery, `host:${host}\n`, 'h…, [ALGO, amzDate, scope, await sha256hex(canonicalRequest)].j… | presigned URL 생성. 헤더는 host만 서명하므로 브라우저가 Content-Type을 덧붙여도 서명이 깨지지 않는다(쿼리 서명에서 미서명 헤더는 검증 대상이 아니다). `key`가 빈 문자열이면 버킷 자체 를 가리킨다(ListObjectsV2용). 이때 `extraQuery`로 `list-type=2`·`pr… |
| `supabase/functions/media-sign/index.ts` | export | function | `safeRest(rest: string): string \\| null` | KNOWN_EXT_RE.test, rest.split, /[\u0000-\u001f\\]/.test | 객체 키의 접미 (사용자 폴더 뒤쪽) 검증. 2026-07-27 이전엔 함수가 `{sub}/{mediaId}.webp`를 통째로 만들었다. 클라이언트가 키에 전혀 손댈 수 없다는 뜻이라 단순하고 강했다. 그런데 사용자가 "폴더는 여행 제목, 파일명은 날짜_시간_제목_id" 를 원했고, 함수는 여행 제목도 촬영시각도 모른… |
| `supabase/functions/media-sign/index.ts` | export | function | `objectKey(userId: string, rest: string): string` | — | 객체 키. 사용자 폴더는 검증된 sub 에서만 나온다(클라이언트 입력 아님). 앱의 `mediaStoragePath`와 형식이 같아야 한다 — `tests/unit/mediaNaming`이 왕복으로 잠근다. |
| `supabase/functions/media-sign/index.ts` | export | const | `LIST_PAGE_SIZE` | — | 한 번에 받을 객체 수와 페이지 상한. 개인 여행 앱이라 수천 장이 상한이면 충분하고, 무한 루프를 구조적으로 막는다. 상한에 걸리면 `truncated`로 반드시 말한다 — 조용히 자르고 "고아 0건"이라 판정하면 그건 거짓말이다(비타협 원칙 #4). |
| `supabase/functions/media-sign/index.ts` | export | const | `LIST_MAX_PAGES` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `PHOTO_EXTS` | — | 이 앱이 만드는 확장자. `domain/media/naming.ts`의 `AUDIO_EXTS`와 같은 목록이어야 한다 — 앱이 `.webm`으로 올리는데 여기서 거부하면 소리는 영영 서버에 못 간다. 두 파일은 다른 배포 단위(브라우저/Deno)에 살아 손으로 맞출 수 없으므로 `tests/unit/audioRowmap… |
| `supabase/functions/media-sign/index.ts` | export | const | `AUDIO_EXTS` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `VIDEO_EXTS` | — | — |
| `supabase/functions/media-sign/index.ts` | internal | const | `KNOWN_EXT_RE` | — | — |
| `supabase/functions/media-sign/index.ts` | internal | const | `AUDIO_EXT_RE` | — | — |
| `supabase/functions/media-sign/index.ts` | export | function | `isAudioKey(key: string): boolean` | AUDIO_EXT_RE.test, isVideoKey | 이 키는 소리인가. 종류는 확장자가 말한다 (사진과 같은 폴더에 살기 때문에). |
| `supabase/functions/media-sign/index.ts` | export | function | `isVideoKey(key: string): boolean` | /__video__(?:op_[0-9a-f]{32}__)?[0-9a-f]{32}\.(?:mp4\\|webm)… | `.webm`은 소리에도 쓰이므로 영상 이름의 명시적 표식으로 가른다. `.mp4`도 같은 규칙을 쓴다. |
| `supabase/functions/media-sign/index.ts` | export | function | `isVideoPosterKey(key: string): boolean` | /__video_poster__(?:op_[0-9a-f]{32}__)?[0-9a-f]{32}\.webp$/… | 영상 표시용 포스터는 본체와 같은 operation/id fence를 쓰되 별도 WebP 객체로 보관한다. |
| `supabase/functions/media-sign/index.ts` | export | function | `groupKeysByMediaId(keys: string[]): Map<string, string[]>` | mediaIdOfKey, grouped.set, grouped.get | 같은 media id에 본체·포스터·옛 operation 사본이 함께 있어도 하나도 덮어쓰지 않는다. |
| `supabase/functions/media-sign/index.ts` | export | const | `FN_VERSION` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `FN_OPS` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `FN_SOURCE_SHA256` | — | — |
| `supabase/functions/media-sign/index.ts` | export | const | `DELETE_MANY_MAX` | — | 한 번에 지울 수 있는 최대 개수 — 요청 하나가 무한정 길어지지 않게. |
| `supabase/functions/media-sign/index.ts` | export | function | `xmlUnescape(s: string): string` | s .replace(/&lt;/g, '<') .replace(/&gt;/g, '>') .replace(/&…, s .replace(/&lt;/g, '<') .replace(/&gt;/g, '>') .replace, s .replace(/&lt;/g, '<') .replace, s .replace | XML 엔티티 되돌리기. 우리 키엔 없지만 없다고 가정하지 않는다. |
| `supabase/functions/media-sign/index.ts` | export | interface | `ListPage` | — | — |
| `supabase/functions/media-sign/index.ts` | export | function | `parseListXml(xml: string): ListPage` | xml.matchAll, block.match, keys.push, xmlUnescape, Number, prefixes.push, /<IsTruncated>\s*true\s*<\/IsTruncated>/i.test, xml.match | ListObjectsV2 응답 XML에서 키와 다음 토큰만 뽑는다. DOM 파서 없이 정규식을 쓰는 이유: Deno 런타임에 XML 파서가 없고, S3 응답은 스키마가 고정된 기계 생성물이라 `<Contents><Key>…</Key>` 형태가 변하지 않는다. 순수 함수라 유닛이 실제 응답 형태로 직접 돌린다 — 서명과 … |
| `supabase/functions/media-sign/index.ts` | export | function | `countOutside(page: ListPage, myPrefix: string): number` | page.prefixes.filter | 내 폴더 밖에 몇 개가 있나 — 개수만 센다. 순수 함수라 유닛이 직접 돌린다. 왜 개수만인가(2026-07-26 사용자 요청): 진단이 「사진 파일 0개」라고 말할 때 그건 "내 폴더에는 0개" 라는 뜻이지 "버킷이 비었다"가 아니다. 그 한정을 화면이 말하지 않아 사용자가 대시보드를 직접 열어 확인해야 했다(M-00… |
| `supabase/functions/media-sign/index.ts` | export | interface | `MultipartUpload` | — | 미완료 멀티파트 업로드 목록 파싱(`GET /{bucket}?uploads`). 왜 필요한가(2026-07-26 사용자 실기기): 버킷 최상위가 완전히 비었는데 대시보드는 2.87MB를 계속 보여줬다. 사용자가 물었다 — "설마 휴지통 이런 데 간 거 아님?" R2에 휴지통은 없다. 그러나 미완료 멀티파트 업로드 는 실… |
| `supabase/functions/media-sign/index.ts` | export | function | `parseMultipartXml(xml: string): MultipartUpload[]` | xml.matchAll, block.match, out.push, xmlUnescape | — |
| `supabase/functions/media-sign/index.ts` | export | function | `mediaIdOfKey(key: string): string \\| null` | key.split('/').pop, key.split, last.replace, base.split('__').pop, base.split, /^[0-9a-f]{32}$/i.test, tail.toLowerCase, h.slice, UUID_RE.test | 키에서 mediaId를 뽑는다. 폴더(uid) 부분은 버린다 — 응답에 다시 담지 않기 위해서다. 우리 형식이 아니면 null(호출부가 따로 세어 보고한다 — 조용히 버리지 않는다). |
| `supabase/functions/media-sign/index.ts` | internal | function | `publishableKey(req: Request): string \\| null` | envGet, req.headers.get | auth API 호출에 쓸 publishable(anon) 키. 프로젝트가 새 키 체계(`SUPABASE_PUBLISHABLE_KEYS`)를 쓰면 `SUPABASE_ANON_KEY`가 주입되지 않을 수 있다 → 그때는 요청에 실려온 apikey 헤더 를 쓴다. 안전한 이유: 신원은 apikey가 아니라 JWT 에서 나오… |
| `supabase/functions/media-sign/index.ts` | internal | async function | `verifiedUserId(req: Request): Promise<string \\| null>` | req.headers.get, auth.startsWith, envGet, publishableKey, fetch, r.json, UUID_RE.test | — |
| `supabase/functions/media-sign/index.ts` | internal | async function | `isInvited(req: Request): Promise<boolean>` | req.headers.get, auth.startsWith, envGet, publishableKey, fetch, r.json | 🔐 초대제 확인 — 인증(sub 위조 차단)만으로는 부족하다. journey 테이블은 RLS로 `auth.uid() = user_id AND journey.is_allowed()`를 이중으로 건다(0002). 그런데 R2 서명 경로는 DB를 거치지 않으므로 그 두 번째 방어(초대제)가 통째로 빠진다 — 허용목록 밖의 … |
| `supabase/functions/media-sign/index.ts` | internal | function | `json(body: unknown, status = 200): Response` | JSON.stringify | — |
| `supabase/functions/media-sign/index.ts` | internal | async function | `listKeysOnce(env: R2Env, prefix: string): Promise<string[] \\| null>` | presign, Date.now, String, fetch, parseListXml, r.text | 내 폴더의 키 한 페이지를 읽는다. 실패하면 null — 빈 배열로 반올림하지 않는다 (빈 목록은 "없다"이고 null은 "모른다"이다. 둘을 섞으면 지우지도 않은 것을 지웠다고 말한다). `deleteMany`의 선행 조회 와 되읽기 가 이 한 곳을 쓴다 — 두 곳에 손으로 쓰면 한쪽만 고쳐지는 날이 온다(§7). |
| `supabase/functions/media-sign/index.ts` | export | async function | `handler(req: Request): Promise<Response>` | json, req.json, r2Env, R2_SECRET_NAMES.filter, envGet, new Date().toISOString, verifiedUserId, isInvited, String, q.push | — |

## 5. 자동 검증 연결

- `tests/unit/audioRowmap.test.ts`
- `tests/unit/geocodeProxy.test.ts`
- `tests/unit/geocodeRateLimit.test.ts`
- `tests/unit/mediaFileAudit.test.ts`
- `tests/unit/mediaNaming.test.ts`
- `tests/unit/videoPolicy.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `supabase/functions/media-sign/index.ts` — 761줄
- `supabase/functions/geocode/index.ts` — 486줄
- `supabase/migrations/0027_canonical_sync_meta.sql` — 342줄
- `supabase/migrations/20260813113101_add_moment_companion_names.sql` — 209줄
- `supabase/migrations/0022_journey_places.sql` — 200줄
- `supabase/tests/canonical_sync_meta.sql` — 188줄
- `supabase/migrations/0018_rls_initplan_and_fn_hygiene.sql` — 169줄
- `supabase/tests/stale_sync_write_guard.sql` — 158줄
- `supabase/migrations/0030_journey_videos.sql` — 141줄
- `supabase/migrations/0019_journey_audio.sql` — 125줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
