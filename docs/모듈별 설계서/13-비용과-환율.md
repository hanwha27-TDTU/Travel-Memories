---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 비용·통화·환율 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

비용 행, 금액 표시, 기준통화 환산과 환율 공급 경계를 소유한다.

- 모듈 ID: `expense-fx`
- 관측 파일: 4개 · 논리 줄: 565 · 최상위 선언: 43개(외부 공개 30개)
- 코드 내용 SHA-256: `f549fb35460560613eadb72f77d86eb19132243cfbff530b025588b37975d96a`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/expense/format.ts`

### 다른 모듈로 나가는 정적 의존

- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 2건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 1건

### 외부 패키지/런타임 의존

- Node 내장 또는 저장소 내부 모듈만 정적으로 import한다.

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/expense/format.ts` | 121 | 8/11 | — | — |
| `src/domain/expense/fx.ts` | 128 | 9/9 | src/domain/time.ts | — |
| `src/services/expenses.ts` | 158 | 7/9 | src/offline/db.ts | Dexie: localExpenses; deletion lifecycle |
| `src/services/fx.ts` | 158 | 6/14 | src/offline/db.ts<br>src/domain/expense/fx.ts | Dexie: localFxRates; network fetch; browser key/value storage |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/expense/format.ts` | export | interface | `CurrencyMeta` | — | — |
| `src/domain/expense/format.ts` | export | const | `CURRENCIES: readonly CurrencyMeta[]` | — | 지원 통화. 자주 쓰는 통화를 위에, 그 아래로 세계 통화(코드 알파벳순). 표시 소수자릿수는 통화 관례를 따른다(KRW·JPY·UZS·IDR·KZT·TWD·VND=0). 심볼이 없으면(빈 문자열) 금액 뒤에 통화 코드를 붙인다(정직: 심볼 위조 안 함). 일부 통화는 관례상 심볼을 뒤에 둔다(suffix: UZS so… |
| `src/domain/expense/format.ts` | export | const | `DEFAULT_CURRENCY` | — | — |
| `src/domain/expense/format.ts` | internal | const | `NO_FLAG_PREFIX` | — | 국기를 못 만드는 통화 접두어 — ISO 4217의 'X..'는 초국가·특수코드(XAF·XOF·XDR 등)라 나라가 없다. |
| `src/domain/expense/format.ts` | export | function | `currencyFlag(code: string): string` | code.slice(0, 2).toUpperCase, code.slice, /^[A-Z]{2}$/.test, NO_FLAG_PREFIX.test, String.fromCodePoint, [...cc].map, ch.charCodeAt | 통화코드 → 국기 이모지. 손으로 36개를 적지 않는다 (§7): ISO 4217 통화코드의 앞 두 글자가 곧 ISO 3166-1 alpha-2 국가코드라서 코드에서 파생한다(KRW→KR→🇰🇷, EUR→EU→🇪🇺). 새 통화를 CURRENCIES에 넣으면 국기도 자동으로 따라온다. 만들 수 없으면 빈 문자열(위조… |
| `src/domain/expense/format.ts` | export | function | `currencyLabel(c: CurrencyMeta): string` | currencyFlag, [flag, c.symbol, c.code].filter(Boolean).join, [flag, c.symbol, c.code].filter | 선택 UI용 한 줄 라벨: '🇰🇷 ₩ KRW' / 심볼 없으면 '🇦🇪 AED'. |
| `src/domain/expense/format.ts` | internal | function | `meta(code: string): CurrencyMeta \\| undefined` | CURRENCIES.find | — |
| `src/domain/expense/format.ts` | internal | function | `groupThousands(intPart: string): string` | intPart.replace | — |
| `src/domain/expense/format.ts` | export | function | `formatMoney(amount: number, currency: string): string` | meta, Number.isFinite, safe.toFixed, fixed.split, groupThousands | 금액+통화 → 표시 문자열. 알 수 없는 통화(또는 심볼 없는 통화)는 코드를 뒤에 붙인다(정직: 심볼 위조 안 함). |
| `src/domain/expense/format.ts` | export | function | `sumByCurrency(items: readonly { originalAmount: number; originalCurrency: string }[]): Record<string, number>` | Number.isFinite | 통화별 합계. 서로 다른 통화는 섞지 않는다(환율 없이 합산 금지 — 정직). |
| `src/domain/expense/format.ts` | export | function | `formatTotals(totals: Record<string, number>): string[]` | Object.keys(totals) .sort() .map, Object.keys(totals) .sort, Object.keys, formatMoney | 통화별 합계를 표시 문자열 배열로. 예: ['₩120,000', '$15.00']. |
| `src/domain/expense/fx.ts` | export | interface | `FxRateTable` | — | 특정 날짜·기준통화의 환율 표. rates[X] = 1 base 당 X의 수량. |
| `src/domain/expense/fx.ts` | export | function | `fxKey(date: string, base: string): string` | base.toUpperCase | 캐시 키 — 날짜+기준통화 하나당 표 하나. |
| `src/domain/expense/fx.ts` | export | function | `rateOf(table: FxRateTable, currency: string): number \\| null` | currency.toUpperCase, table.base.toUpperCase, Number.isFinite | 표 안에서 통화 하나의 비율. 기준통화 자신은 1. 없으면 null(위조하지 않는다). |
| `src/domain/expense/fx.ts` | export | function | `convertAmount(amount: number, from: string, to: string, table: FxRateTable): number \\| null` | Number.isFinite, from.toUpperCase, to.toUpperCase, rateOf | from 통화의 금액을 to 통화로 환산. 표 하나로 양방향(외화→원화, 원화→외화) 모두 처리한다. 값을 만들 수 없으면 null (0이나 추정치로 얼버무리지 않는다 — 정직). |
| `src/domain/expense/fx.ts` | export | function | `isFxFresh(table: FxRateTable, todayDate: string, nowIso: string, ttlHours = 6): boolean` | new Date(table.fetchedAt).getTime, new Date(nowIso).getTime, Number.isNaN | 캐시 신선도. 과거 날짜 표는 영구 유효 (확정된 기준환율은 안 바뀐다). 오늘 날짜 표만 ttl 시간이 지나면 다시 받는다. |
| `src/domain/expense/fx.ts` | export | function | `fxDateFor(occurredAtIso: string, todayDate: string, offsetMin: number): string` | atOffset | 요청할 환율 날짜를 정한다. 미래 날짜는 존재하지 않으므로 오늘로 당긴다. (사용자가 발생 시각을 미래로 적어둔 경우를 조용히 실패시키지 않기 위함) @param offsetMin 여행지 오프셋(분). 비용의 「사용일」은 그 돈을 쓴 자리의 달력 날짜다 — 기기 기준으로 재면 시차만큼 하루가 밀려 엉뚱한 날의 기준환율 … |
| `src/domain/expense/fx.ts` | export | function | `unitRate(from: string, to: string, table: FxRateTable): number \\| null` | convertAmount | 1 from = ? to (단위 환율). 환산 상세를 사용자가 검산할 수 있게 노출하는 값. |
| `src/domain/expense/fx.ts` | export | function | `formatRate(n: number): string` | Number.isFinite, n.toLocaleString, n.toPrecision, s.includes, s.replace(/0+$/, '').replace, s.replace | 환율 숫자 표시 — 통화 소수자릿수(KRW=0)를 쓰면 0.1233이 "₩0"으로 뭉개지므로 별도 규칙. 1 이상은 소수 2~4자리, 1 미만은 유효숫자 4자리(작은 환율도 의미 있는 자리까지 보인다). |
| `src/domain/expense/fx.ts` | export | function | `convertTotals(totals: Record<string, number>, base: string, table: FxRateTable): { total: number; skipped: string[] }` | Object.entries, convertAmount, skipped.push | 통화별 합계를 기준통화 하나로 환산해 더한다. 환산 불가 통화는 skipped에 남긴다(숨기지 않음). |
| `src/services/expenses.ts` | internal | function | `uuid(): string` | crypto.randomUUID | — |
| `src/services/expenses.ts` | internal | function | `expenseOp(operationId: string, entityId: string, operationType: SyncQueueItem['operationType'], createdAt: string): SyncQueueItem` | — | 'expense' 동기화 op 하나. moments와 동일 형태. |
| `src/services/expenses.ts` | export | interface | `CreateExpenseInput` | — | — |
| `src/services/expenses.ts` | export | async function | `createExpenseLocalFirst(input: CreateExpenseInput): Promise<LocalExpense>` | new Date().toISOString, uuid, input.category?.trim, input.note?.trim, db, d.transaction, d.localExpenses.add, d.syncQueue.add, expenseOp, d.localExpenses.get | 비용 생성 — 내구성 로컬 커밋 + read-back. 원금액은 양수만 허용(제약). |
| `src/services/expenses.ts` | export | interface | `UpdateExpensePatch` | — | — |
| `src/services/expenses.ts` | export | async function | `updateExpenseLocalFirst(id: string, patch: UpdateExpensePatch): Promise<LocalExpense>` | db, d.localExpenses.get, new Date().toISOString, uuid, patch.category.trim, patch.note.trim, d.transaction, d.localExpenses.put, d.syncQueue.add, expenseOp | 비용 수정 — version+1·updatedAt 갱신(LWW). 금액은 양수 유지. |
| `src/services/expenses.ts` | export | async function | `softDeleteExpenseLocalFirst(id: string): Promise<void>` | db, d.localExpenses.get, new Date().toISOString, uuid, d.transaction, d.localExpenses.put, d.syncQueue.add, expenseOp | 비용 삭제 — tombstone(§0). 삭제도 동기화되도록 큐 op를 넣는다. |
| `src/services/expenses.ts` | export | async function | `restoreExpenseLocalFirst(id: string): Promise<void>` | db, d.localExpenses.get, new Date().toISOString, uuid, d.transaction, d.localExpenses.put, d.syncQueue.add, expenseOp | 비용 되살리기(실행취소) — 삭제와 대칭 . version+1로 LWW에서 최신이 이긴다. 2026-07-25 감사 F4: trips·moments·media에는 restore가 있는데 비용만 없어, 개별 비용 삭제는 되돌릴 수 없었다. 도메인 간 생명주기 대칭이 깨져 있던 자리다. |
| `src/services/expenses.ts` | export | async function | `listExpensesByTrip(tripId: string): Promise<LocalExpense[]>` | db().localExpenses.where('tripId').equals(tripId).toArray, db().localExpenses.where('tripId').equals, db().localExpenses.where, db, rows.filter | 여행의 활성 비용(통계·합계용). tombstone 제외. |
| `src/services/fx.ts` | internal | const | `BASE_KEY` | — | — |
| `src/services/fx.ts` | export | const | `DEFAULT_FX_BASE` | — | — |
| `src/services/fx.ts` | internal | function | `safeGet(key: string): string \\| null` | localStorage.getItem | — |
| `src/services/fx.ts` | export | function | `fxBase(): string` | (safeGet(BASE_KEY) \\|\\| DEFAULT_FX_BASE).toUpperCase, safeGet | 표시 기준통화(사용자 선호 — 기억 데이터가 아니라 표시 설정이라 localStorage). |
| `src/services/fx.ts` | export | function | `setFxBase(code: string): void` | localStorage.setItem, code.toUpperCase | — |
| `src/services/fx.ts` | export | function | `todayDate(): string` | localDate, new Date().toISOString | 오늘(로컬) 날짜. |
| `src/services/fx.ts` | internal | typealias | `Provider` | — | — |
| `src/services/fx.ts` | internal | async function | `fetchJson(url: string, timeoutMs = 8000): Promise<unknown \\| null>` | setTimeout, ctrl.abort, fetch, clearTimeout, res.json | — |
| `src/services/fx.ts` | internal | function | `normalizeRates(raw: unknown): Record<string, number>` | Object.entries, Number, Number.isFinite, k.toUpperCase | 응답의 통화 맵을 대문자 키 + 유한 양수만 남겨 정규화(신뢰 경계). |
| `src/services/fx.ts` | internal | function const | `fawaz(date, base, today): Provider` | base.toLowerCase, fetchJson, normalizeRates, Object.keys, base.toUpperCase, new Date().toISOString | 주 제공자 — fawazahmed0/currency-api. 과거 날짜는 URL에 날짜를 고정해 받는다. |
| `src/services/fx.ts` | internal | function const | `frankfurter(date, base, today): Provider` | fetchJson, base.toUpperCase, normalizeRates, Object.keys, new Date().toISOString | 보조 제공자 — Frankfurter(ECB). 주요 통화 품질은 높으나 지원 통화가 적다. |
| `src/services/fx.ts` | internal | const | `PROVIDERS: Provider[]` | — | — |
| `src/services/fx.ts` | export | async function | `cachedTable(date: string, base: string): Promise<FxRateTable \\| null>` | db().localFxRates.get, db, fxKey | 캐시에서만 읽는다(네트워크 없음) — 렌더 경로에서 즉시 표시용. |
| `src/services/fx.ts` | export | async function | `ensureTable(date: string, base = fxBase()): Promise<FxRateTable \\| null>` | fxBase, todayDate, cachedTable, isFxFresh, new Date().toISOString, p, db().localFxRates.put, db, fxKey | 표를 확보한다: 신선한 캐시가 있으면 그대로, 없으면 제공자에서 받아 캐시에 넣는다. 네트워크가 없거나 전부 실패하면 오래된 캐시라도 반환 (있으면) — 없으면 null. 어떤 경우에도 예외를 던지지 않는다(환율은 보조 정보이며 앱을 막지 않는다). |

## 5. 자동 검증 연결

- `tests/unit/expenseFormat.test.ts`
- `tests/unit/fx.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/services/expenses.ts` — 158줄
- `src/services/fx.ts` — 158줄
- `src/domain/expense/fx.ts` — 128줄
- `src/domain/expense/format.ts` — 121줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
