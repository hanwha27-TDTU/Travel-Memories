---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 앱 셸과 라우팅 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

부팅, 화면 라우팅, 앱 공통 등록부·자가평가·변경이력과 최상위 조립을 소유한다.

- 모듈 ID: `app-shell`
- 관측 파일: 18개 · 논리 줄: 5748 · 최상위 선언: 123개(외부 공개 94개)
- 코드 내용 SHA-256: `4f235bca7665445a27171b39447e9e93d1cb989ee4912c691b807c54fec5f5f0`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/main.ts`

### 다른 모듈로 나가는 정적 의존

- 화면·오버레이·상호작용(`ui-shell`): import/re-export 6건
- 인증·Supabase 클라이언트·동의(`auth-security`): import/re-export 3건
- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 2건
- 오프라인 동기화와 정본 전환(`sync-canonical`): import/re-export 1건

### 외부 패키지/런타임 의존

- Node 내장 또는 저장소 내부 모듈만 정적으로 import한다.

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/app/agents.ts` | 92 | 4/4 | — | transactional persistence; deletion lifecycle |
| `src/app/apk.ts` | 99 | 7/8 | — | platform bridge |
| `src/app/blueprint.ts` | 144 | 11/11 | src/offline/db.ts | transactional persistence; deletion lifecycle |
| `src/app/changelog.ts` | 2515 | 6/6 | — | file/blob bytes; integrity/crypto; deletion lifecycle; platform bridge |
| `src/app/constitution.gen.ts` | 41 | 4/4 | — | transactional persistence |
| `src/app/deviceId.ts` | 204 | 12/18 | — | platform bridge; browser key/value storage |
| `src/app/errorLog.ts` | 69 | 5/9 | — | transactional persistence; DOM interaction |
| `src/app/gates.ts` | 407 | 7/7 | — | deletion lifecycle |
| `src/app/hashchain.ts` | 76 | 7/10 | — | integrity/crypto |
| `src/app/platformMap.gen.ts` | 82 | 2/2 | — | — |
| `src/app/playbook.ts` | 136 | 4/10 | src/app/apk.ts<br>src/app/changelog.ts | — |
| `src/app/registry.gen.ts` | 207 | 1/1 | — | deletion lifecycle |
| `src/app/researchLog.ts` | 1076 | 1/1 | src/app/hashchain.ts | file/blob bytes; integrity/crypto; transactional persistence; deletion lifecycle; browser media worker/codec; platform bridge; browser key/value storage |
| `src/app/router.ts` | 87 | 6/10 | — | DOM interaction |
| `src/app/screenSession.ts` | 71 | 4/4 | — | — |
| `src/app/selfEval.ts` | 320 | 12/12 | src/app/registry.gen.ts | deletion lifecycle |
| `src/app/syncReleaseContract.gen.ts` | 23 | 1/1 | — | deletion lifecycle |
| `src/main.ts` | 99 | 0/5 | src/ui/styles/fonts.css<br>src/ui/styles/tokens.css<br>src/ui/styles/app.css<br>src/app/router.ts<br>src/ui/screens/home.ts<br>src/ui/screens/tripDetail.ts<br>src/ui/theme.ts<br>src/offline/db.ts<br>src/app/errorLog.ts<br>src/services/autoSync.ts<br>src/services/auth.ts<br>src/services/supabase/client.ts<br>src/domain/authGate.ts<br>src/app/screenSession.ts | DOM interaction |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/app/agents.ts` | export | typealias | `AgentCategory` | — | — |
| `src/app/agents.ts` | export | const | `AGENT_CATEGORY_LABEL: Record<AgentCategory, string>` | — | — |
| `src/app/agents.ts` | export | const | `AGENT_DESC: Record<string, string>` | — | 에이전트 한 줄 설명 — 에이전트마다 반드시 하나씩 (없으면 화면에 이름만 뜬다). |
| `src/app/agents.ts` | export | const | `AGENT_CATEGORY: Record<string, AgentCategory>` | — | 에이전트 분류 — 가이드 화면이 이 값으로 묶어 그린다. |
| `src/app/apk.ts` | internal | const | `REPO_SLUG` | — | owner/repo — 다운로드 주소와 API 주소가 같은 저장소를 가리키게 하는 한 곳(SSOT). |
| `src/app/apk.ts` | export | const | `APK_RELEASE_TAG` | — | CI가 덮어쓰는 고정 릴리스 태그 — 워크플로의 `gh release upload` 대상과 1:1. |
| `src/app/apk.ts` | export | const | `APK_LATEST_URL` | — | 항상 최신 APK를 주는 고정 주소. GitHub 릴리스 자산은 공개 저장소라 로그인 없이 받아진다. |
| `src/app/apk.ts` | export | const | `APK_RELEASE_API` | — | 🔴 릴리스 설명(body) 을 읽는 API 주소 — 셸이 새 APK를 감지하는 유일한 경로. 함정 D: 릴리스 '자산'(releases/download/…)은 302 리다이렉트에 CORS 헤더가 없어 웹뷰 fetch가 조용히 막힌다. api.github.com은 `Access-Control-Allow-Origin: `… |
| `src/app/apk.ts` | export | function | `parseShellBuild(body: string): number \\| null` | body.match, JSON.parse, Number, Number.isFinite | 릴리스 설명의 `shell-version` 마커에서 versionCode를 뽑는다. 순수 — 유닛이 직접 돌린다. 워크플로(android-apk.yml)가 심는 형식과 계약 : `<!-- shell-version: {"versionCode": 42} -->`. 마커가 없거나(옛 릴리스) 깨졌으면 null → 배너를 띄우… |
| `src/app/apk.ts` | export | const | `APK_INSTALL_STEPS: ReadonlyArray<{ title: string; desc: string }>` | — | 설치 순서 — 초등학생도 따라 할 수 있게 (사용자 지시). 화면(가이드)은 이 데이터를 그대로 그린다(§10 ③ — 사용자에게 가는 문장은 데이터로 두고 유닛이 직접 읽는다). 전문 용어 금지: 유닛이 「아티팩트·CI·사이드로드·딥링크」 같은 말이 섞이면 RED를 낸다. |
| `src/app/apk.ts` | export | const | `APP_ICONS: ReadonlyArray<{ key: string; label: string }>` | — | 🎨 앱 아이콘 목록 (ADR-0038) — 키·라벨·순서의 SSOT. `key`는 네이티브 `IconSwitcherPlugin.ALIASES`의 키와 글자 단위로 같아야 한다 (전환기 동작). 썸네일은 `public/icons/app-icons/{key}.png`. 첫째(passport)가 기본. 셸(APK)에서만 전… |
| `src/app/apk.ts` | export | const | `APK_FACTS: ReadonlyArray<string>` | — | 설치 안내 아래 붙는 사실 문장들 — 역시 데이터로 두고 유닛이 읽는다. |
| `src/app/blueprint.ts` | export | typealias | `StorageForm` | — | — |
| `src/app/blueprint.ts` | export | interface | `SourceDomain` | — | 발전원 = 데이터 도메인. implemented=Dexie 테이블 실장 여부(계획 도메인은 false). |
| `src/app/blueprint.ts` | export | const | `SOURCES: readonly SourceDomain[]` | — | — |
| `src/app/blueprint.ts` | export | const | `SHAPING: readonly { code: string; label: string; sub: string }[]` | — | ② 다듬기 — 저장·동기화 전 공통 처리(merge.ts·rowmap.ts의 실제 단계). |
| `src/app/blueprint.ts` | export | const | `STORAGE: readonly { code: string; label: string; sub: string }[]` | — | ③ 보관·동기화 — 로컬 우선 → 클라우드 맞춤 → 삭제는 휴지통(sync.ts의 실제 자세). |
| `src/app/blueprint.ts` | export | interface | `ScreenNode` | — | ④ 말단 = 화면. feeds=소비하는 발전원 키(게이트가 file 존재를 검증). |
| `src/app/blueprint.ts` | export | const | `SCREENS: readonly ScreenNode[]` | — | — |
| `src/app/blueprint.ts` | export | interface | `CheckGroup` | — | — |
| `src/app/blueprint.ts` | export | interface | `SelfCheck` | — | — |
| `src/app/blueprint.ts` | export | function | `selfCheck(): SelfCheck` | SOURCES.filter, SCREENS.some, sc.feeds.includes, synced.filter, built.filter, fed, gaps.push, SOURCES.filter((s) => !s.implemented).map, groups.reduce, Math.round | — |
| `src/app/blueprint.ts` | export | async function | `liveCounts(): Promise<Record<string, number>>` | db, Promise.all, SOURCES.filter((s) => s.localTable).map, SOURCES.filter, d[s.localTable!].filter((r) => (r as { deletedAt: string \\|…, d[s.localTable!].filter | 실카운트: Dexie 실제 목록에서 활성(미삭제) 개수. implemented 도메인만. |
| `src/app/changelog.ts` | export | interface | `ChangeEntry` | — | — |
| `src/app/changelog.ts` | export | const | `DEVELOPER` | — | 개발자 정보(공개 페이지 노출용). 개인 이메일 등 민감정보는 넣지 않는다(PRIVACY). |
| `src/app/changelog.ts` | export | const | `CHANGELOG: ChangeEntry[]` | — | 최신이 맨 앞. 각 항목 = 실제 병합된 릴리스. |
| `src/app/changelog.ts` | export | const | `APP_VERSION` | — | 현재 앱 버전(최신 항목). |
| `src/app/changelog.ts` | export | const | `LAST_MODIFIED` | — | 코드 최종 수정일(최신 항목 날짜). |
| `src/app/changelog.ts` | export | const | `FIRST_DEV_DATE` | — | 최초 개발일. |
| `src/app/constitution.gen.ts` | export | interface | `ConstitutionItem` | — | — |
| `src/app/constitution.gen.ts` | export | const | `PRINCIPLES: readonly ConstitutionItem[]` | — | 비타협 원칙 — 목적의 일부이지, 목적과 맞바꿀 대상이 아니다. |
| `src/app/constitution.gen.ts` | export | const | `DISCIPLINE: readonly ConstitutionItem[]` | — | 실행 규율 — 품질은 모델이 아니라 규율에서 나온다. |
| `src/app/constitution.gen.ts` | export | const | `NEVER_DO: readonly string[]` | — | 절대 위반 금지 (§0). |
| `src/app/deviceId.ts` | internal | const | `KEY` | — | — |
| `src/app/deviceId.ts` | internal | const | `NAME_KEY` | — | — |
| `src/app/deviceId.ts` | export | const | `DEVICE_NAME_MAX` | — | 사용자가 지은 이름의 길이 상한 — 진단 표 한 줄을 넘기지 않을 만큼. |
| `src/app/deviceId.ts` | internal | const | `memoryFallback: string \\| null` | — | 이 기기의 안정적 id. localStorage가 막히면 세션 동안만 유지되는 임시 id를 쓴다. |
| `src/app/deviceId.ts` | export | function | `deviceId(): string` | localStorage.getItem, crypto.randomUUID, localStorage.setItem | — |
| `src/app/deviceId.ts` | export | function | `shortDeviceId(id: string = deviceId()): string` | deviceId, id.slice | 화면·목록에 쓰는 짧은 id — 전체를 보여줄 이유가 없다. |
| `src/app/deviceId.ts` | export | interface | `DeviceHints` | — | 판별에 쓰는 실측 신호 묶음 . 순수 함수가 브라우저를 직접 만지지 않게 하려고 분리했다 — 그래야 유닛이 「데스크톱 모드 태블릿」 같은 상황을 실제로 돌려볼 수 있다(§10 ③). |
| `src/app/deviceId.ts` | export | function | `readDeviceHints(): DeviceHints` | matchMedia | 브라우저에서 신호를 읽는다. 없는 값은 보수적으로(터치 없음) 둔다. |
| `src/app/deviceId.ts` | internal | function | `kindFromUa(ua: string): string \\| null` | /iPad\\|Tablet/i.test, /iPhone/i.test, /Android/i.test, /Mobi/i.test | UA가 스스로 밝힌 기기 종류. 못 밝히면 `null`(= 데스크톱 계열 UA). |
| `src/app/deviceId.ts` | internal | function | `osFromUa(ua: string): string` | /Android/i.test, /iPhone\\|iPad\\|iOS/i.test, /Mac OS X/i.test, /Windows/i.test, /Linux/i.test | — |
| `src/app/deviceId.ts` | internal | function | `browserFromUa(ua: string): string` | /Edg\//i.test, /SamsungBrowser/i.test, /OPR\//i.test, /Firefox/i.test, /Chrome/i.test, /Safari/i.test | — |
| `src/app/deviceId.ts` | export | function | `detectDeviceLabel(h: DeviceHints): string` | kindFromUa, browserFromUa, osFromUa | 감지된 기본 이름 — `"태블릿 · Android · Chrome"`. 순수 함수. UA 문자열 전체가 아니라 분류만 뽑는다(개인정보 최소화). 판별이 안 되면 '알 수 없음'으로 남긴다 — 추측해서 틀린 이름을 붙이면 사용자가 자기 기기를 못 알아본다. UA가 데스크톱이라는데 터치 신호가 모순되면 「터치 기기」에서 멈… |
| `src/app/deviceId.ts` | export | function | `normalizeDeviceName(raw: string): string` | raw.replace(/#/g, ' ').replace(/\s+/g, ' ').trim().slice, raw.replace(/#/g, ' ').replace(/\s+/g, ' ').trim, raw.replace(/#/g, ' ').replace, raw.replace | 저장 가능한 형태로 다듬는다. `#`는 반드시 없앤다 — 스탬프의 구분자라 이름에 들어가면 `parseDeviceStamp`가 이름 뒷부분을 id로 읽는다(경계 문자를 데이터가 밟는 고전적 결함). |
| `src/app/deviceId.ts` | export | function | `customDeviceName(): string \\| null` | localStorage.getItem, normalizeDeviceName | 사용자가 지은 이름. 없으면 `null`(= 감지값을 쓴다). |
| `src/app/deviceId.ts` | export | function | `setCustomDeviceName(raw: string): string \\| null` | normalizeDeviceName, localStorage.setItem, localStorage.removeItem | 이름을 정한다. 빈 문자열이면 지우고 자동 감지로 되돌린다 (되돌릴 길 없는 설정을 만들지 않는다). 저장이 막힌 브라우저에서는 조용히 실패하지 않고 알린다 — 사용자는 이름이 바뀐 줄 알 테니까. |
| `src/app/deviceId.ts` | export | function | `deviceLabel(hints: DeviceHints = readDeviceHints()): string` | readDeviceHints, customDeviceName, detectDeviceLabel | 사람이 알아보는 이 기기 이름. 사용자가 지은 이름이 있으면 그것이 이긴다. 인자를 주면 그 신호로 감지만 한다(유닛·진단 미리보기용). |
| `src/app/deviceId.ts` | export | function | `deviceStamp(): string` | deviceLabel, shortDeviceId | 서버 행에 넣는 값 — `라벨#짧은id`. 한 칸에 담아 컬럼을 늘리지 않는다. |
| `src/app/deviceId.ts` | export | function | `parseDeviceStamp(stamp: string): { label: string; id: string }` | stamp.lastIndexOf, stamp.slice | 서버에서 읽은 값을 사람이 읽는 형태로 되돌린다. 형식이 달라도 깨지지 않는다. |
| `src/app/errorLog.ts` | export | interface | `AppError` | — | — |
| `src/app/errorLog.ts` | internal | const | `MAX` | — | — |
| `src/app/errorLog.ts` | internal | const | `buf: AppError[]` | — | — |
| `src/app/errorLog.ts` | internal | const | `installed` | — | — |
| `src/app/errorLog.ts` | internal | function | `push(kind: AppError['kind'], message: string, stack?: string): void` | (stack ?? '') .split('\n') .slice(1, 4) .map((l) => l.trim(…, (stack ?? '') .split('\n') .slice(1, 4) .map, (stack ?? '') .split('\n') .slice, (stack ?? '') .split, l.trim, buf.push, new Date().toISOString, String(message).slice, String, where.slice | — |
| `src/app/errorLog.ts` | export | function | `installErrorLog(): void` | window.addEventListener, push, String, console.error.bind, args.map((a) => (a instanceof Error ? a.message : String(a)…, args.map, orig | 앱 시작 시 1회. 기존 콘솔 동작을 막지 않는다(가로채되 그대로 통과시킨다). |
| `src/app/errorLog.ts` | export | function | `recentErrors(): AppError[]` | [...buf].reverse | — |
| `src/app/errorLog.ts` | export | function | `errorCount(): number` | — | — |
| `src/app/errorLog.ts` | export | function | `clearErrors(): void` | — | — |
| `src/app/gates.ts` | export | typealias | `GateCategory` | — | — |
| `src/app/gates.ts` | export | const | `CATEGORY_LABEL: Record<GateCategory, string>` | — | — |
| `src/app/gates.ts` | export | const | `GATE_DESC: Record<string, string>` | — | 게이트 한 줄 설명 — 게이트마다 반드시 하나씩. 설명 없는 게이트는 가이드 화면에 이름만 뜬다. 사용자에게 `check-edge-fn-ops`는 아무 뜻도 아니므로, 그건 "무엇을 지키는지 말하지 않는 안전장치"다(§8 — 도구는 관측이 아니라 판정을 한다). 실제로 이 표는 12개에서 멈춰 있었고 게이트가 22개로 … |
| `src/app/gates.ts` | export | const | `GATE_AXIS: Record<string, string>` | — | 게이트 약속 원장 — 「이 검사는 무엇을 세지 않고 단언하는가」. ── 왜 설명 말고 축이 또 필요한가 ──────────────────────────────────────────── `GATE_DESC`는 무엇을 보는가 를 말한다. 그런데 개수만 세는 게이트는 어느 저장소에서나 「위반 0」과 「아무것도 안 봤다」에 같… |
| `src/app/gates.ts` | export | const | `GATE_WORLD: Record<string, string>` | — | 게이트 세계 원장 — 「이 검사는 자기가 사는 세계에서 무엇을 가정하는가」(헌법 §18-G). ── 왜 이게 필요했나 (M-0135) ──────────────────────────────────────────────── 로컬 하네스가 전부 초록 인 커밋이 CI에서 죽었다. 범인은 `git diff origin/main… |
| `src/app/gates.ts` | export | const | `GATE_CATEGORY: Record<string, GateCategory>` | — | 게이트 카테고리. 없으면 'static'으로 본다. 목록은 registry.gen에서 오므로 개수는 파생. |
| `src/app/gates.ts` | export | function | `categoryOf(gate: string): GateCategory` | — | — |
| `src/app/hashchain.ts` | export | const | `GENESIS` | — | 체인 시작 앵커(제네시스). |
| `src/app/hashchain.ts` | export | interface | `ChainInput` | — | — |
| `src/app/hashchain.ts` | export | interface | `ChainLink` | — | — |
| `src/app/hashchain.ts` | internal | function | `toHex(buf: ArrayBuffer): string` | Array.from(new Uint8Array(buf)) .map((b) => b.toString(16).…, Array.from(new Uint8Array(buf)) .map, Array.from, b.toString(16).padStart, b.toString | — |
| `src/app/hashchain.ts` | internal | function | `canonical(input: ChainInput): string` | JSON.stringify | 결정론적 직렬화(키 순서 고정) — 해시 재현성을 위해 필드 순서를 명시한다. |
| `src/app/hashchain.ts` | internal | async function | `sha256Hex(s: string): Promise<string>` | crypto.subtle.digest, new TextEncoder().encode, toHex | — |
| `src/app/hashchain.ts` | export | function | `hashchainSupported(): boolean` | — | 지원 여부(보안 컨텍스트 + subtle 존재). |
| `src/app/hashchain.ts` | export | async function | `buildChain(inputs: ChainInput[]): Promise<ChainLink[]>` | sha256Hex, canonical, links.push | 입력들을 순서대로 해시체인으로 묶는다(각 hash = SHA256(prevHash \\| canonical)). |
| `src/app/hashchain.ts` | export | async function | `verifyChain(links: ChainLink[]): Promise<boolean>` | sha256Hex, canonical | 체인 내부 정합성 검증: 각 링크가 prevHash와 자기 입력으로 재계산한 해시와 일치하고, 링크 간 prevHash 연결이 끊기지 않았는지 확인. 하나라도 어긋나면 false. |
| `src/app/hashchain.ts` | export | function | `anchorHash(links: ChainLink[]): string` | — | 마지막 앵커 해시(체인 전체를 대표). 빈 체인이면 GENESIS. |
| `src/app/platformMap.gen.ts` | export | interface | `PlatformRow` | — | — |
| `src/app/platformMap.gen.ts` | export | const | `PLATFORM_MAP: PlatformRow[]` | — | — |
| `src/app/playbook.ts` | export | const | `PLAYBOOK_VERSION` | — | 플레이북 버전 = 앱 버전(changelog 맨 앞). 별도로 적지 않는다 — 그 순간 두 진실이 생긴다. |
| `src/app/playbook.ts` | internal | const | `APP_NAME` | — | 앱 이름(SSOT) — 다른 앱으로 재현할 때 여기만 바뀌면 문서 전체가 따라온다. |
| `src/app/playbook.ts` | export | function | `playbookFilename(ext: 'html' \\| 'md'): string` | APP_NAME.replace | 내려받을 때의 파일 이름. 버전이 이름에 박혀 어느 버전 가이드인지 파일만 봐도 안다. |
| `src/app/playbook.ts` | internal | const | `INTRO` | — | — |
| `src/app/playbook.ts` | internal | const | `WHY` | — | — |
| `src/app/playbook.ts` | internal | const | `REUSE_NOTE` | — | — |
| `src/app/playbook.ts` | internal | function | `parts()` | APK_INSTALL_STEPS.map | 순서·사실 데이터를 한 곳에서 꺼낸다(두 형식이 같은 재료를 쓰게). |
| `src/app/playbook.ts` | export | function | `buildPlaybookMarkdown(): string` | parts, lines.push, lines.join | Markdown — 가장 단순·이식성 최고. 다른 앱/사람이 그대로 복사해 재현하기 좋다. |
| `src/app/playbook.ts` | internal | function | `esc(s: string): string` | s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace, s.replace(/&/g, '&amp;').replace, s.replace | — |
| `src/app/playbook.ts` | export | function | `buildPlaybookHtml(): string` | parts, steps .map((s) => `<li><b>${esc(s.title)}</b><span>${esc(s.…, steps .map, esc, facts.map((f) => `<li>${esc(f)}</li>`).join, facts.map | 자체완결 HTML — 한 파일로 예쁘게 렌더되고 오프라인에서도 열린다(외부 자원 없음). |
| `src/app/registry.gen.ts` | export | const | `REGISTRY` | — | — |
| `src/app/researchLog.ts` | export | const | `RESEARCH_LOG: ChainInput[]` | — | 연구노트 항목. seq는 append-only 순번(재배열 금지 — 해시체인이 순서에 의존). |
| `src/app/router.ts` | export | typealias | `Route` | — | — |
| `src/app/router.ts` | export | interface | `TripNavigationTarget` | — | — |
| `src/app/router.ts` | internal | typealias | `RenderFn` | — | — |
| `src/app/router.ts` | internal | const | `BASE` | — | — |
| `src/app/router.ts` | internal | const | `ROUTES: Record<string, Route>` | — | — |
| `src/app/router.ts` | internal | function | `relSegments(pathname: string, base: string): string[]` | pathname.startsWith, pathname.slice, pathname.replace, rel.replace(/\/+$/, '').split, rel.replace | — |
| `src/app/router.ts` | export | function | `pathToRoute(pathname: string, base: string = BASE): Route` | relSegments | 경로 → 라우트. base 주입 가능한 순수함수(tests/unit에서 직접 테스트 — 미러 금지). |
| `src/app/router.ts` | export | function | `pathToParam(pathname: string, base: string = BASE): string \\| undefined` | relSegments | 경로의 파라미터(2번째 세그먼트). 예: /trip/abc → 'abc'. 없으면 undefined. |
| `src/app/router.ts` | export | function | `searchToTripTarget(search: string): TripNavigationTarget \\| undefined` | params.getAll(key).filter, params.getAll, one | Empty or repeated query values are ambiguous, so the detail screen receives no target. |
| `src/app/router.ts` | export | function | `createRouter(render: RenderFn): { navigate: (route: Route, param?: string, target?: TripNavigationTarget) => void; start: () => voi…` | render, pathToRoute, pathToParam, searchToTripTarget, window.dispatchEvent, query.set, window.history.pushState, apply, window.addEventListener | — |
| `src/app/screenSession.ts` | export | typealias | `ScreenDispose` | — | 화면이 라우터에게 돌려주는 정리 함수. 반환형이 계약이라 새 화면도 내놓을 수밖에 없다(§7 2층). |
| `src/app/screenSession.ts` | export | interface | `ScreenHandle` | — | — |
| `src/app/screenSession.ts` | export | interface | `ScreenSession` | — | — |
| `src/app/screenSession.ts` | export | function | `createScreenSession(): ScreenSession` | disposeCurrent, dispose | 화면 세션을 만든다. 상태를 모듈 전역이 아니라 클로저에 둔다 — 그래야 유닛이 서로 간섭하지 않고 여러 개를 나란히 세워 볼 수 있다. |
| `src/app/selfEval.ts` | export | typealias | `Grade` | — | — |
| `src/app/selfEval.ts` | export | interface | `FiveW1H` | — | — |
| `src/app/selfEval.ts` | export | interface | `EvalItem` | — | — |
| `src/app/selfEval.ts` | export | const | `EVAL_ITEMS: EvalItem[]` | — | 평가 항목. 손으로 적되 근거를 함께 적는다. 자동 집계 가능한 부분(게이트 수 등)은 `REGISTRY`에서 파생해 드리프트를 막는다. |
| `src/app/selfEval.ts` | export | const | `GRADE_BANDS: readonly { min: number; grade: Grade; cls: string }[]` | — | 등급 경계 — 화면과 문서가 같은 기준을 쓰도록 여기서만 정의한다. 🔴 판정과 범례 가 같은 표를 읽게 만든 이유: 예전엔 `gradeOf`가 if 사슬이고 가이드 화면의 범례는 「세계적 95+ · 우수 85+ …」를 손으로 적어 있었다. 경계를 하나 옮기면 화면이 조용히 거짓말을 한다 — 그리고 그건 게이트가 못 본… |
| `src/app/selfEval.ts` | export | function | `gradeOf(score: number): Grade` | GRADE_BANDS.find | — |
| `src/app/selfEval.ts` | export | function | `gradeClass(score: number): string` | GRADE_BANDS.find | 등급 배지 CSS 클래스(화면이 등급→클래스를 손으로 사슬 잇지 않게). |
| `src/app/selfEval.ts` | export | function | `gradeLegend(): string` | GRADE_BANDS.map((b, i) => Number.isFinite(b.min) ? `${b.gra…, GRADE_BANDS.map, Number.isFinite | 범례 문장 — 「세계적 95+ · … · 취약 <50」을 표에서 조립한다(손으로 적지 않는다). |
| `src/app/selfEval.ts` | export | const | `CRITICAL_OPEN: { code: string; why: string }[]` | — | 미해결 치명결함 — 있으면 종합점수에 상한을 건다. 지금은 없음(있으면 여기 등록). |
| `src/app/selfEval.ts` | export | const | `CRITICAL_CAP` | — | 치명결함이 하나라도 열려 있으면 종합은 이 값을 넘을 수 없다. |
| `src/app/selfEval.ts` | export | interface | `EvalSummary` | — | — |
| `src/app/selfEval.ts` | export | function | `summarize(items: EvalItem[] = EVAL_ITEMS): EvalSummary` | items.reduce, Math.round, Math.min, gradeOf | — |
| `src/app/syncReleaseContract.gen.ts` | export | const | `SYNC_RELEASE_CONTRACT` | — | — |
| `src/main.ts` | internal | const | `appEl` | — | — |
| `src/main.ts` | internal | const | `root: HTMLElement` | — | — |
| `src/main.ts` | internal | async function | `guardTripDetail(): Promise<boolean>` | isConfigured, canViewLocalRecords, currentUser | 로그아웃(클라우드 모드) 중엔 딥링크(`/trip/<id>` 북마크·뒤로가기)로도 이 기기의 로컬 여행이 안 보이게 한다 — 홈 화면 잠금(signedOutLockState, home.ts)과 같은 경계 다. 홈만 가려도 이 라우트가 그대로 열려 있으면 그 자체로 우회로가 된다. |
| `src/main.ts` | internal | const | `screens` | — | — |
| `src/main.ts` | internal | const | `router: ReturnType<typeof createRouter>` | — | — |

## 5. 자동 검증 연결

- `tests/unit/apkGuide.test.ts`
- `tests/unit/appUpdate.test.ts`
- `tests/unit/blueprint.test.ts`
- `tests/unit/hashchain.test.ts`
- `tests/unit/logIntegrity.test.ts`
- `tests/unit/placeSiblingDiscipline.test.ts`
- `tests/unit/playbook.test.ts`
- `tests/unit/router.test.ts`
- `tests/unit/screenSession.test.ts`
- `tests/unit/storeState.test.ts`
- `tests/unit/syncReleaseVerdict.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/app/changelog.ts` — 2515줄
- `src/app/researchLog.ts` — 1076줄
- `src/app/gates.ts` — 407줄
- `src/app/selfEval.ts` — 320줄
- `src/app/registry.gen.ts` — 207줄
- `src/app/deviceId.ts` — 204줄
- `src/app/blueprint.ts` — 144줄
- `src/app/playbook.ts` — 136줄
- `src/app/apk.ts` — 99줄
- `src/main.ts` — 99줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
