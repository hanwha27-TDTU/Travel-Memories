---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 검증 게이트·생성기·릴리스 시스템 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

게이트 원장, 생성물, CI, 배포 워크플로, 릴리스 프로필과 공급망 검증을 소유한다.

- 모듈 ID: `quality-release`
- 관측 파일: 119개 · 논리 줄: 27240 · 최상위 선언: 1389개(외부 공개 324개)
- 코드 내용 SHA-256: `241c489906d292bb9da1f30f85192b7a68d64644858a15118d956c80825cddad`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `.githooks/commit-msg`
- `.github/workflows/android-apk.yml`
- `.github/workflows/ci.yml`
- `.github/workflows/deploy-pages.yml`
- `.github/workflows/windows-installer.yml`
- `.nvmrc`
- `index.html`
- `package-lock.json`
- `package.json`
- `public/manifest.webmanifest`
- `public/sw.js`
- `schemas/agent-report.schema.json`
- `schemas/codex-shared-skills-lock.json`
- `schemas/release-profile.json`
- `schemas/sync-release-contract.json`
- `scripts/build-windows.mjs`
- `scripts/check-adapter-parity.mjs`
- `scripts/check-apk-release-link.mjs`
- `scripts/check-backup-coverage.mjs`
- `scripts/check-base-consistency.mjs`
- `scripts/check-blueprint.mjs`
- `scripts/check-bytes-upload-symmetry.mjs`
- `scripts/check-ci-policy.mjs`
- `scripts/check-constitution-gen.mjs`
- `scripts/check-csp.mjs`
- `scripts/check-current-doc-facts.mjs`
- `scripts/check-date-freshness.mjs`
- `scripts/check-dependabot-policy.mjs`
- `scripts/check-diag-blindspots.mjs`
- `scripts/check-doc-counts.mjs`
- `scripts/check-doc-governance.mjs`
- `scripts/check-doc-tree.mjs`
- `scripts/check-domain-symmetry.mjs`
- `scripts/check-domain-wiring.mjs`
- `scripts/check-edge-cors.mjs`
- `scripts/check-edge-fn-ops.mjs`
- `scripts/check-enforcement-parity.mjs`
- `scripts/check-env-assumption.mjs`
- `scripts/check-env-wiring.mjs`
- `scripts/check-exif-order.mjs`
- `scripts/check-exif-strip-on-share.mjs`
- `scripts/check-fn-size.mjs`
- `scripts/check-font-subsets.mjs`
- `scripts/check-gate-integrity.mjs`
- `scripts/check-hand-counts.mjs`
- `scripts/check-hooks-wired.mjs`
- `scripts/check-input-closeout.mjs`
- `scripts/check-instant-normalization.mjs`
- `scripts/check-lazy-screens.mjs`
- `scripts/check-live-coverage.mjs`
- `scripts/check-live-sleep.mjs`
- `scripts/check-migration-grants.mjs`
- `scripts/check-module-design-docs.mjs`
- `scripts/check-no-synthetic-italic.mjs`
- `scripts/check-node-version.mjs`
- `scripts/check-page-size-parity.mjs`
- `scripts/check-platform-map.mjs`
- `scripts/check-production-artifacts.mjs`
- `scripts/check-purge-scope.mjs`
- `scripts/check-real-coord.mjs`
- `scripts/check-recovery-path.mjs`
- `scripts/check-registry-gen.mjs`
- `scripts/check-report-fields.mjs`
- `scripts/check-review-tier.mjs`
- `scripts/check-schema-parity.mjs`
- `scripts/check-screen-lifecycle.mjs`
- `scripts/check-secret-leak.mjs`
- `scripts/check-self-eval.mjs`
- `scripts/check-shared-skill-contract.mjs`
- `scripts/check-skill-routing.mjs`
- `scripts/check-step-duration.mjs`
- `scripts/check-sw.mjs`
- `scripts/check-sync-parallelism.mjs`
- `scripts/check-sync-release-contract.mjs`
- `scripts/check-timezone.mjs`
- `scripts/check-tooling-registry.mjs`
- `scripts/check-ui-color-token.mjs`
- `scripts/check-update-signal.mjs`
- `scripts/check-verdict-symmetry.mjs`
- `scripts/check-version-ssot.mjs`
- `scripts/check-windows-shell.mjs`
- `scripts/check-workflow-pins.mjs`
- `scripts/compare-geocoders.mjs`
- `scripts/gen-all.mjs`
- `scripts/gen-font-subsets.py`
- `scripts/gen-module-design-docs.mjs`
- `scripts/gen-sync-release-contract.mjs`
- `scripts/gen-version-file.mjs`
- `scripts/generate-icons.mjs`
- `scripts/harness.mjs`
- `scripts/hook-release-freeze.mjs`
- `scripts/hook-secret-guard.mjs`
- `scripts/hook-sql-safe.mjs`
- `scripts/hook-verify-exit.mjs`
- `scripts/known.mjs`
- `scripts/measure-backup-zip-memory.mjs`
- `scripts/measure-live-time.mjs`
- `scripts/print-app-version.mjs`
- `scripts/verify-authgate-live.mjs`
- `scripts/verify-diagnostics-live.mjs`
- `scripts/verify-editor-live.mjs`
- `scripts/verify-sync-release-live.mjs`
- `tsconfig.json`
- `vite.config.ts`

### 다른 모듈로 나가는 정적 의존

- 다른 소유 모듈로 나가는 정적 import가 관측되지 않았다.

### 외부 패키지/런타임 의존

- `typescript`
- `vite`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `.githooks/commit-msg` | 47 | 0/0 | — | — |
| `.github/workflows/android-apk.yml` | 87 | 0/0 | — | platform bridge; workflow jobs: workflow_dispatch, push, apk |
| `.github/workflows/ci.yml` | 140 | 0/0 | — | workflow jobs: pull_request, fast-gates, harness, live-render |
| `.github/workflows/deploy-pages.yml` | 69 | 0/0 | — | workflow jobs: push, workflow_dispatch, build, deploy |
| `.github/workflows/windows-installer.yml` | 95 | 0/0 | — | integrity/crypto; workflow jobs: workflow_dispatch, push, installer |
| `.nvmrc` | 2 | 0/0 | — | — |
| `index.html` | 28 | 0/0 | — | — |
| `package-lock.json` | 2081 | 0/0 | — | platform bridge |
| `package.json` | 59 | 0/0 | — | — |
| `public/manifest.webmanifest` | 32 | 0/0 | — | — |
| `public/sw.js` | 134 | 0/5 | — | network fetch; deletion lifecycle; DOM interaction |
| `schemas/agent-report.schema.json` | 105 | 0/0 | — | — |
| `schemas/codex-shared-skills-lock.json` | 46 | 0/0 | — | — |
| `schemas/release-profile.json` | 541 | 0/0 | — | integrity/crypto; deletion lifecycle; platform bridge |
| `schemas/sync-release-contract.json` | 21 | 0/0 | — | deletion lifecycle |
| `scripts/brief.mjs` | 635 | 7/11 | — | deletion lifecycle; platform bridge; browser key/value storage |
| `scripts/build-windows.mjs` | 20 | 0/4 | scripts/lib/app-version.mjs | — |
| `scripts/check-adapter-parity.mjs` | 120 | 1/7 | scripts/gen-adapters.mjs<br>scripts/gen-harness-law.mjs | — |
| `scripts/check-apk-release-link.mjs` | 88 | 4/7 | — | platform bridge |
| `scripts/check-backup-coverage.mjs` | 137 | 0/14 | scripts/gate-selftest-lib.mjs | Dexie: localTrips; transactional persistence |
| `scripts/check-base-consistency.mjs` | 61 | 0/8 | — | — |
| `scripts/check-blueprint.mjs` | 141 | 0/19 | scripts/gate-selftest-lib.mjs | transactional persistence |
| `scripts/check-bytes-upload-symmetry.mjs` | 160 | 5/14 | — | deletion lifecycle |
| `scripts/check-ci-policy.mjs` | 200 | 5/9 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-constitution-gen.mjs` | 85 | 0/5 | scripts/gen-constitution.mjs<br>scripts/gate-selftest-lib.mjs | — |
| `scripts/check-csp.mjs` | 103 | 0/8 | — | — |
| `scripts/check-current-doc-facts.mjs` | 129 | 5/14 | — | — |
| `scripts/check-date-freshness.mjs` | 218 | 7/12 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-dependabot-policy.mjs` | 32 | 1/4 | — | — |
| `scripts/check-diag-blindspots.mjs` | 286 | 8/31 | — | — |
| `scripts/check-doc-counts.mjs` | 80 | 1/6 | scripts/gen-registry.mjs | — |
| `scripts/check-doc-governance.mjs` | 158 | 3/12 | scripts/gen-adapters.mjs | — |
| `scripts/check-doc-tree.mjs` | 178 | 4/9 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-domain-symmetry.mjs` | 273 | 6/13 | — | Dexie: localAudio, localX; transactional persistence; deletion lifecycle |
| `scripts/check-domain-wiring.mjs` | 62 | 0/10 | — | — |
| `scripts/check-edge-cors.mjs` | 143 | 3/7 | — | — |
| `scripts/check-edge-fn-ops.mjs` | 226 | 4/12 | — | network fetch |
| `scripts/check-enforcement-parity.mjs` | 188 | 3/9 | — | — |
| `scripts/check-env-assumption.mjs` | 323 | 11/14 | scripts/check-gate-promise.mjs<br>scripts/gate-selftest-lib.mjs | — |
| `scripts/check-env-wiring.mjs` | 105 | 3/9 | — | — |
| `scripts/check-exif-order.mjs` | 228 | 5/12 | — | — |
| `scripts/check-exif-strip-on-share.mjs` | 102 | 3/6 | scripts/gate-selftest-lib.mjs | file/blob bytes |
| `scripts/check-fn-size.mjs` | 233 | 1/9 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-font-subsets.mjs` | 104 | 2/12 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-gate-control.mjs` | 573 | 13/19 | — | — |
| `scripts/check-gate-integrity.mjs` | 238 | 5/21 | — | — |
| `scripts/check-gate-promise.mjs` | 142 | 4/10 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-hand-counts.mjs` | 134 | 3/12 | — | — |
| `scripts/check-hooks-wired.mjs` | 158 | 2/9 | — | — |
| `scripts/check-input-closeout.mjs` | 182 | 3/14 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-instant-normalization.mjs` | 219 | 4/12 | scripts/gate-selftest-lib.mjs | deletion lifecycle |
| `scripts/check-lazy-screens.mjs` | 159 | 2/12 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-live-coverage.mjs` | 223 | 2/11 | — | — |
| `scripts/check-live-sleep.mjs` | 119 | 3/6 | — | — |
| `scripts/check-migration-grants.mjs` | 482 | 10/18 | — | SQL table: allowed_users, foo, ghost, purged_ids; SQL function: f; deletion lifecycle |
| `scripts/check-module-design-docs.mjs` | 79 | 1/5 | scripts/module-design-docs-lib.mjs<br>scripts/gate-selftest-lib.mjs | deletion lifecycle |
| `scripts/check-no-synthetic-italic.mjs` | 88 | 2/7 | — | — |
| `scripts/check-node-version.mjs` | 136 | 4/11 | — | — |
| `scripts/check-page-size-parity.mjs` | 75 | 2/8 | — | — |
| `scripts/check-platform-map.mjs` | 55 | 0/4 | scripts/gen-platform-map.mjs<br>scripts/gate-selftest-lib.mjs | — |
| `scripts/check-production-artifacts.mjs` | 58 | 1/7 | — | — |
| `scripts/check-purge-scope.mjs` | 207 | 6/17 | — | SQL table: X; deletion lifecycle |
| `scripts/check-real-coord.mjs` | 83 | 1/6 | — | — |
| `scripts/check-recovery-path.mjs` | 151 | 3/8 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-registry-gen.mjs` | 151 | 1/14 | scripts/gen-registry.mjs<br>scripts/gate-selftest-lib.mjs | — |
| `scripts/check-report-fields.mjs` | 182 | 6/10 | — | — |
| `scripts/check-review-tier.mjs` | 219 | 4/10 | scripts/gate-selftest-lib.mjs | DOM interaction |
| `scripts/check-schema-parity.mjs` | 139 | 0/13 | scripts/gate-selftest-lib.mjs | SQL table: X |
| `scripts/check-screen-lifecycle.mjs` | 329 | 3/13 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-secret-leak.mjs` | 106 | 0/8 | scripts/lib/secret-patterns.mjs | network fetch |
| `scripts/check-self-eval.mjs` | 113 | 3/9 | — | — |
| `scripts/check-shared-skill-contract.mjs` | 302 | 2/32 | scripts/gate-selftest-lib.mjs | platform bridge |
| `scripts/check-skill-routing.mjs` | 190 | 3/11 | scripts/brief.mjs | platform bridge |
| `scripts/check-step-duration.mjs` | 156 | 4/9 | scripts/gate-selftest-lib.mjs | deletion lifecycle |
| `scripts/check-sw.mjs` | 185 | 3/10 | scripts/gate-selftest-lib.mjs | deletion lifecycle; DOM interaction |
| `scripts/check-sync-parallelism.mjs` | 89 | 1/9 | — | — |
| `scripts/check-sync-release-contract.mjs` | 80 | 1/4 | scripts/gate-selftest-lib.mjs<br>scripts/sync-release-contract-lib.mjs | — |
| `scripts/check-timezone.mjs` | 196 | 2/19 | scripts/gate-selftest-lib.mjs | — |
| `scripts/check-tooling-registry.mjs` | 96 | 1/6 | scripts/gate-selftest-lib.mjs | platform bridge |
| `scripts/check-ui-color-token.mjs` | 93 | 1/10 | — | — |
| `scripts/check-update-signal.mjs` | 107 | 4/11 | — | network fetch; DOM interaction |
| `scripts/check-verdict-symmetry.mjs` | 833 | 16/27 | — | deletion lifecycle; DOM interaction |
| `scripts/check-version-ssot.mjs` | 211 | 7/18 | scripts/gate-selftest-lib.mjs | platform bridge |
| `scripts/check-windows-shell.mjs` | 94 | 2/7 | — | platform bridge |
| `scripts/check-workflow-pins.mjs` | 35 | 1/4 | — | — |
| `scripts/compare-geocoders.mjs` | 188 | 0/10 | — | network fetch |
| `scripts/gate-selftest-lib.mjs` | 44 | 1/1 | — | — |
| `scripts/gen-adapters.mjs` | 112 | 6/7 | — | — |
| `scripts/gen-all.mjs` | 132 | 4/9 | — | — |
| `scripts/gen-constitution.mjs` | 132 | 8/12 | — | — |
| `scripts/gen-font-subsets.py` | 166 | 0/0 | — | — |
| `scripts/gen-harness-law.mjs` | 134 | 5/7 | scripts/gen-adapters.mjs | — |
| `scripts/gen-module-design-docs.mjs` | 23 | 1/1 | scripts/module-design-docs-lib.mjs | — |
| `scripts/gen-platform-map.mjs` | 193 | 3/9 | — | Supabase table: x; integrity/crypto; transactional persistence; deletion lifecycle |
| `scripts/gen-registry.mjs` | 198 | 5/9 | scripts/check-gate-control.mjs | — |
| `scripts/gen-sync-release-contract.mjs` | 30 | 0/7 | scripts/sync-release-contract-lib.mjs | — |
| `scripts/gen-version-file.mjs` | 24 | 0/1 | — | — |
| `scripts/generate-icons.mjs` | 52 | 0/7 | — | — |
| `scripts/harness.mjs` | 305 | 1/10 | — | deletion lifecycle |
| `scripts/hook-release-freeze.mjs` | 81 | 1/2 | scripts/release-lock.mjs | — |
| `scripts/hook-secret-guard.mjs` | 85 | 3/6 | scripts/lib/secret-patterns.mjs | — |
| `scripts/hook-sql-safe.mjs` | 155 | 6/9 | — | SQL table: trips; deletion lifecycle |
| `scripts/hook-verify-exit.mjs` | 192 | 2/8 | — | — |
| `scripts/known.mjs` | 313 | 5/12 | — | — |
| `scripts/lib/app-version.mjs` | 13 | 2/2 | — | — |
| `scripts/lib/secret-patterns.mjs` | 54 | 4/4 | — | — |
| `scripts/live-browser-lib.mjs` | 187 | 6/6 | — | DOM interaction |
| `scripts/measure-backup-zip-memory.mjs` | 210 | 0/13 | — | file/blob bytes; transactional persistence; deletion lifecycle |
| `scripts/measure-live-time.mjs` | 118 | 0/8 | — | — |
| `scripts/module-design-docs-lib.mjs` | 556 | 11/33 | — | file/blob bytes; integrity/crypto; transactional persistence; deletion lifecycle; browser media worker/codec; DOM interaction; platform bridge; browser key/value storage |
| `scripts/print-app-version.mjs` | 10 | 0/0 | scripts/lib/app-version.mjs | — |
| `scripts/release-lock.mjs` | 234 | 10/15 | — | — |
| `scripts/sync-release-contract-lib.mjs` | 57 | 9/11 | — | integrity/crypto |
| `scripts/verify-authgate-live.mjs` | 235 | 1/17 | scripts/live-browser-lib.mjs<br>scripts/gate-selftest-lib.mjs | — |
| `scripts/verify-diagnostics-live.mjs` | 1210 | 0/35 | scripts/live-browser-lib.mjs<br>scripts/gate-selftest-lib.mjs | file/blob bytes; transactional persistence; deletion lifecycle; DOM interaction; platform bridge; browser key/value storage |
| `scripts/verify-editor-live.mjs` | 5888 | 0/324 | scripts/live-browser-lib.mjs<br>scripts/gen-registry.mjs<br>scripts/gen-constitution.mjs<br>scripts/gate-selftest-lib.mjs | network fetch; file/blob bytes; integrity/crypto; transactional persistence; deletion lifecycle; browser media worker/codec; DOM interaction; platform bridge; browser key/value storage |
| `scripts/verify-sync-release-live.mjs` | 93 | 1/10 | scripts/gate-selftest-lib.mjs | network fetch |
| `tsconfig.json` | 23 | 0/0 | — | — |
| `vite.config.ts` | 21 | 1/1 | — | — |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `public/sw.js` | internal | const | `CACHE` | — | — |
| `public/sw.js` | internal | const | `SHELL_URL` | — | — |
| `public/sw.js` | internal | const | `MAX_ENTRIES` | — | — |
| `public/sw.js` | internal | async function | `trim(cache)` | cache.keys, cache.delete | 상한을 넘으면 오래된 것부터 버린다. |
| `public/sw.js` | internal | function | `isCacheable(response)` | Boolean | 캐시에 넣어도 되는 응답인가. 부분/오류/불투명 응답을 넣으면 그 뒤로 계속 그걸 내준다. |
| `scripts/brief.mjs` | internal | const | `ROOT` | — | — |
| `scripts/brief.mjs` | export | const | `SKILL_ROUTES` | — | 경로 → 필수 스킬 문서. 이 표가 SSOT다 — 어느 문서를 읽을지 기억에 맡기지 않는다. `check-skill-routing` 게이트가 "src의 모든 영역이 표에 걸리는가"를 검사한다. |
| `scripts/brief.mjs` | export | const | `POSTMORTEMS` | — | 필독 사후분석 — 특정 영역을 건드리기 전에 반드시 읽어야 하는 사고 기록. 왜 스킬 문서와 따로 두나(2026-07-26): 스킬 문서는 "이렇게 하라" 를 말하고, 사후분석은 "이렇게 하다 이렇게 됐다" 를 말한다. 둘은 다른 종류의 지식이고, 후자가 없으면 앞의 규칙이 왜 있는지 몰라 상황이 조금만 달라져도 어긴다… |
| `scripts/brief.mjs` | export | const | `NO_SKILL_REQUIRED` | — | 스킬 문서가 필요 없는 영역 — 이유를 반드시 적는다 (이유 없는 제외는 결함, §7). |
| `scripts/brief.mjs` | export | function | `routePath(path)` | path.replaceAll | Windows의 `path.relative()`가 돌려주는 `\`를 라우팅 표의 `/` 표기로 맞춘다. |
| `scripts/brief.mjs` | export | function | `skillsFor(paths)` | out.has, out.set, out.get(skill).includes, out.get, out.get(skill).push, routePath, /^\.claude\/skills\/([^/]+)\/SKILL\.md$/.exec, add, r.match.test | — |
| `scripts/brief.mjs` | export | function | `siblingsOf(path)` | join, dirname, existsSync, readdirSync(dir) .filter((f) => /\.(ts\\|mjs\\|css\\|sql)$/.te…, readdirSync(dir) .filter, readdirSync, /\.(ts\\|mjs\\|css\\|sql)$/.test, routePath | §7이 요구하는 산출물 — 형제 목록을 디렉터리에서 뽑는다 (손으로 세지 않는다). |
| `scripts/brief.mjs` | export | function | `openBacklogRows(text)` | text.search, text.indexOf, text.slice, rest.search, rest.slice, openSection.split('\n').filter, openSection.split, /^\\\|\s*T-[A-Z0-9-]+\s*\\\|/.test | BACKLOG의 완료 아카이브를 열린 과제로 다시 살리지 않고 BKL-1 표만 읽는다. |
| `scripts/brief.mjs` | internal | function | `changedPaths()` | execSync, out .split('\n') .map((l) => l.slice(3).trim()) .filter(Boo…, out .split('\n') .map((l) => l.slice(3).trim()) .filter, out .split('\n') .map, out .split, l.slice(3).trim, l.slice, p.endsWith | — |
| `scripts/brief.mjs` | internal | function | `pastMistakes(paths)` | join, existsSync, readFileSync, paths.map, p.split('/').pop().replace, p.split('/').pop, p.split, src.split(/\n## /).slice, src.split, block.split | 이 영역에서 과거에 낸 실수 — 같은 자리에서 두 번 넘어지지 않게. |
| `scripts/brief.mjs` | internal | const | `isMain` | — | — |
| `scripts/build-windows.mjs` | internal | const | `raw` | — | — |
| `scripts/build-windows.mjs` | internal | const | `parts` | — | — |
| `scripts/build-windows.mjs` | internal | const | `version` | — | — |
| `scripts/build-windows.mjs` | internal | const | `result` | — | — |
| `scripts/check-adapter-parity.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-adapter-parity.mjs` | internal | const | `GENERATED_DOCS` | — | 헌법에서 절만 골라 생성하는 문서들. 새 문서가 생기면 여기 한 줄(§7 2층). |
| `scripts/check-adapter-parity.mjs` | export | function | `block(text)` | text.indexOf, text.slice(i + BEGIN.length, j).trim, text.slice | 마커 사이만 뽑는다. 없으면 null. 순수 함수라 셀프테스트가 직접 돌린다(§4). |
| `scripts/check-adapter-parity.mjs` | internal | const | `constitution` | — | — |
| `scripts/check-adapter-parity.mjs` | internal | const | `problems` | — | — |
| `scripts/check-adapter-parity.mjs` | internal | const | `blocks` | — | — |
| `scripts/check-adapter-parity.mjs` | internal | const | `lines` | — | — |
| `scripts/check-apk-release-link.mjs` | internal | function const | `read(path)` | readFileSync | — |
| `scripts/check-apk-release-link.mjs` | export | function | `androidWorkflowProblems(text)` | /permissions:\s*\n\s*contents:\s*write/.test, p.push, /gh release upload apk-latest [^\n]*--clobber/.test, /if:\s*github\.ref == 'refs\/heads\/main'/.test, /APP_VERSION_CODE=\$\{\{ github\.run_number \}\}/.test, /shell-version:/.test | — |
| `scripts/check-apk-release-link.mjs` | export | function | `windowsWorkflowProblems(text)` | /permissions:\s*\n\s*contents:\s*write/.test, p.push, /gh release upload \$tag \$exe \$sha --clobber/.test, /\$tag = 'windows-latest'/.test, /Bugeon-Journey-Windows-x64-setup\.exe/.test, /github\.ref == 'refs\/heads\/main'/.test, /Get-FileHash[\s\S]*gh release download[\s\S]*Get-FileHash/…, /npm run windows:build/.test | — |
| `scripts/check-apk-release-link.mjs` | export | function | `constantsProblems(apk, installers)` | /APK_RELEASE_TAG = 'apk-latest'/.test, /app-debug\.apk/.test, p.push, /APK_RELEASE_API/.test, /export function parseShellBuild/.test, /WINDOWS_RELEASE_TAG = 'windows-latest'/.test, /WINDOWS_ASSET_NAME = 'Bugeon-Journey-Windows-x64-setup\.ex…, /WINDOWS_LATEST_URL[\s\S]*releases\/download/.test, /APK_LATEST_URL/.test | — |
| `scripts/check-apk-release-link.mjs` | export | function | `guideProblems(text)` | /orderedInstallerGuides/.test, p.push, /label: '설치파일 다운로드'/.test, /github\.com\/.+releases\/download/.test | — |
| `scripts/check-apk-release-link.mjs` | internal | function | `selfTest()` | androidWorkflowProblems, windowsWorkflowProblems, constantsProblems, guideProblems, goodAndroid.replace, goodWindows.replace, goodInstallers.replace, checks.some, console.error, process.exit | — |
| `scripts/check-apk-release-link.mjs` | internal | const | `problems` | — | — |
| `scripts/check-backup-coverage.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-backup-coverage.mjs` | internal | const | `DB_FILE` | — | — |
| `scripts/check-backup-coverage.mjs` | internal | const | `BACKUP_FILE` | — | — |
| `scripts/check-backup-coverage.mjs` | internal | const | `EXCLUDE` | — | — |
| `scripts/check-backup-coverage.mjs` | internal | function | `parseTableNames(src)` | re.exec, names.push | db.ts에서 Dexie 테이블 프로퍼티명을 뽑는다: `localTrips!: Table<...>`. |
| `scripts/check-backup-coverage.mjs` | internal | function | `splitByRole(src)` | declRe.exec, marks.push, src.slice, marks[i].name.toLowerCase, name.includes, exportBodies.push, importBodies.push | backup.ts를 함수 단위로 가른다: 각 `function <name>(` 선언부터 다음 선언 직전까지를 본문으로. 역할은 이름으로 분류(export/import). 반환: { exportBodies:[], importBodies:[] }. |
| `scripts/check-backup-coverage.mjs` | internal | function | `referencesTable(body, table)` | new RegExp(`\\.${table}\\b`).test | — |
| `scripts/check-backup-coverage.mjs` | internal | function | `anyRefs(bodies, table)` | bodies.some, referencesTable | — |
| `scripts/check-backup-coverage.mjs` | internal | function | `findGaps(tables, exportBodies, importBodies)` | EXCLUDE.has, anyRefs, violations.push | 대조: 제외를 뺀 모든 테이블이 export-role·import-role 양쪽에 존재하는지. 위반 목록 반환. |
| `scripts/check-backup-coverage.mjs` | internal | function | `selfTest()` | findGaps, v.some, x.includes | — |
| `scripts/check-backup-coverage.mjs` | internal | const | `tables` | — | — |
| `scripts/check-backup-coverage.mjs` | internal | const | `{ exportBodies, importBodies }` | — | — |
| `scripts/check-backup-coverage.mjs` | internal | const | `violations` | — | — |
| `scripts/check-backup-coverage.mjs` | internal | const | `covered` | — | — |
| `scripts/check-base-consistency.mjs` | internal | function | `baseFromViteConfig(src)` | src.match | — |
| `scripts/check-base-consistency.mjs` | internal | function | `check(viteSrc, manifestText, indexHtml)` | baseFromViteConfig, base.startsWith, base.endsWith, problems.push, JSON.parse, indexHtml.includes | 위반 목록 반환(빈 배열 = 통과). |
| `scripts/check-base-consistency.mjs` | internal | const | `VITE_FIX` | — | — |
| `scripts/check-base-consistency.mjs` | internal | const | `MANI_FIX` | — | — |
| `scripts/check-base-consistency.mjs` | internal | const | `HTML_FIX` | — | — |
| `scripts/check-base-consistency.mjs` | internal | const | `selfCases` | — | — |
| `scripts/check-base-consistency.mjs` | internal | const | `brokenSelf` | — | — |
| `scripts/check-base-consistency.mjs` | internal | const | `problems` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `BP` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `DB` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `SYNC` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `SCREENS_DIR` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `DOMAIN_DIR` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `EXCLUDE_TABLES` | — | — |
| `scripts/check-blueprint.mjs` | internal | function | `parseSources(src)` | /export const SOURCES[\s\S]*?\n\];/.exec, block[0].split, /key:\s*'([^']+)'/.exec, /implemented:/.test, out.push, /table:\s*'([^']+)'/.exec, /implemented:\s*true/.test, /localTable:\s*'([^']+)'/.exec, /hasRowmap:\s*true/.test, /hasSync:\s*true/.test | blueprint.ts에서 SOURCES 객체들을 뽑는다. |
| `scripts/check-blueprint.mjs` | internal | function | `parseScreens(src)` | /export const SCREENS[\s\S]*?\n\];/.exec, re.exec, files.push | — |
| `scripts/check-blueprint.mjs` | internal | function | `parseDbTables(src)` | re.exec, EXCLUDE_TABLES.has, names.push | db.ts의 Dexie 테이블 프로퍼티명(syncQueue 제외). |
| `scripts/check-blueprint.mjs` | internal | function | `findViolations(sources, screenFiles, dbTables, syncText)` | sources.filter((s) => s.localTable).map, sources.filter, byLocal.get, v.push, existsSync, join, new RegExp(`['"\\.]${s.table}\\b`).test | — |
| `scripts/check-blueprint.mjs` | internal | function | `selfTest()` | findViolations, v.some, x.includes, parseDbTables, parsed.includes | — |
| `scripts/check-blueprint.mjs` | internal | const | `bpSrc` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `sources` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `screenFiles` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `dbTables` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `syncText` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `violations` | — | — |
| `scripts/check-blueprint.mjs` | internal | const | `built` | — | — |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | const | `SYNC` | — | — |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | const | `PURGE` | — | — |
| `scripts/check-bytes-upload-symmetry.mjs` | export | function | `uploadGuards(text)` | re.exec, text.slice, Math.max, before.matchAll, out.push | 업로드 호출을 감싼 `if` 조건을 뽑는다. 반환: [{ cond, call }]. 방향이 중요하다 — 업로드 호출을 먼저 찾고 거꾸로 올라간다. 처음엔 `if`부터 훑어 본문을 봤는데, 여는 중괄호 다음의 줄바꿈에서 본문이 끊겨 아무것도 못 잡았다 (자체검사가 그 자리에서 RED를 냈다 — §4가 없었다면 공허한 채로… |
| `scripts/check-bytes-upload-symmetry.mjs` | export | function | `findHandRolledGuards(text)` | uploadGuards(text) .filter( (g) => !g.cond.includes('mustUp…, uploadGuards(text) .filter, uploadGuards, g.cond.includes, g.cond.trim, /const\s+uploadsBytes\s*=\s*mustUploadBytes\s*\(/.test | 손으로 판정한 자리를 찾는다. 반환이 비면 통과. |
| `scripts/check-bytes-upload-symmetry.mjs` | export | function | `countByteDomains(purgeText)` | purgeText.match | `hasRemoteBytes: true`인 도메인 수를 센다(손으로 적지 않는다 — 등록부에서 뽑는다). |
| `scripts/check-bytes-upload-symmetry.mjs` | export | function | `countGateCalls(text)` | text.match | 공용 문을 지나는 호출 수. |
| `scripts/check-bytes-upload-symmetry.mjs` | export | function | `operationFencedDomains(text)` | [...text.matchAll(/operationStoragePath\s*\(\s*stablePath\s…, text.matchAll | DB OCC보다 먼저 올리는 바이트가 작업별 불변 키를 쓰는 도메인 목록(M-0087). |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | function | `selfTest()` | findHandRolledGuards, fails.push, countByteDomains, countGateCalls, operationFencedDomains, fenced.has | — |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | const | `selfFails` | — | — |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | const | `sync` | — | — |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | const | `problems` | — | — |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | const | `domains` | — | — |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | const | `calls` | — | — |
| `scripts/check-bytes-upload-symmetry.mjs` | internal | const | `fenced` | — | — |
| `scripts/check-ci-policy.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-ci-policy.mjs` | internal | const | `CI` | — | — |
| `scripts/check-ci-policy.mjs` | internal | const | `DEPLOY` | — | — |
| `scripts/check-ci-policy.mjs` | export | function | `triggerBlock(yaml)` | /^on:\s*$([\s\S]*?)^(?=[a-z][\w-]*:)/m.exec | 트리거 블록(`on:` ~ 다음 최상위 키)만 잘라낸다 — 주석의 낱말을 트리거로 오탐하지 않게. |
| `scripts/check-ci-policy.mjs` | export | function | `stripComments(yaml)` | String(yaml) .split('\n') .filter((l) => !/^\s*#/.test(l)) …, String(yaml) .split('\n') .filter, String(yaml) .split, String, /^\s*#/.test | 주석 줄을 걷어낸다(설명문이 판정을 흔들지 않게 — 오탐도 결함 · §2-B ③). |
| `scripts/check-ci-policy.mjs` | export | function | `jobBody(yaml, job)` | String(yaml).split, String, lines.findIndex, new RegExp(`^\\s{2}${job}:\\s*$`).test, /^\s{2}[\w-]+:\s*$/.test, /^[\w-]+:/.test, body.push, body.join | `jobs:` 아래 2칸 들여쓴 job 블록의 본문을 뽑는다. 🔴 정규식으로 하려다 내 셀프테스트에 잡혔다 : `(?=…\\|$)`에 `m` 플래그가 붙으면 `$`가 모든 줄 끝에 매치돼 lazy 캡처가 첫 줄에서 끝난다. 줄 단위로 세는 편이 정확하고 읽힌다. |
| `scripts/check-ci-policy.mjs` | export | function | `commandsInOrder(body, first, second)` | body.indexOf | 한 job 안에서 두 npm 명령이 실제 run 순서대로 놓였는지 판정한다. |
| `scripts/check-ci-policy.mjs` | export | function | `findPolicyViolations(ciYaml, deployYaml)` | stripComments, triggerBlock, /^\s*push:/m.test, bad.push, /^\s*pull_request:/m.test, /ready_for_review/.test, jobBody, /if:[^\n]*draft\s*==\s*false/.test, commandsInOrder, /npm run verify:sync-release-live/.test | §15 위반을 모두 찾아 문자열 배열로. 빈 배열이면 통과. |
| `scripts/check-ci-policy.mjs` | internal | const | `violations` | — | — |
| `scripts/check-constitution-gen.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-constitution-gen.mjs` | internal | const | `OUT` | — | — |
| `scripts/check-constitution-gen.mjs` | internal | const | `expected` | — | — |
| `scripts/check-constitution-gen.mjs` | internal | const | `actual` | — | — |
| `scripts/check-constitution-gen.mjs` | internal | const | `d` | — | — |
| `scripts/check-csp.mjs` | internal | const | `REQUIRED` | — | — |
| `scripts/check-csp.mjs` | internal | const | `FORBIDDEN` | — | — |
| `scripts/check-csp.mjs` | internal | function | `parseCsp(content)` | content.split, part.trim().split(/\s+/).filter, part.trim().split, part.trim, tokens.slice | meta CSP content 문자열 → { 지시어: [소스...] } |
| `scripts/check-csp.mjs` | internal | function | `checkHtml(html)` | html.match, parseCsp, Object.entries, problems.push, csp[dir].includes, csp[dir]?.includes | html 텍스트를 검사해 위반 목록을 반환(빈 배열 = 통과). |
| `scripts/check-csp.mjs` | internal | const | `GOOD` | — | — |
| `scripts/check-csp.mjs` | internal | const | `selfCases` | — | — |
| `scripts/check-csp.mjs` | internal | const | `brokenSelf` | — | — |
| `scripts/check-csp.mjs` | internal | const | `problems` | — | — |
| `scripts/check-current-doc-facts.mjs` | export | const | `DOMAIN_WORDS` | — | 도메인 원장(SYNC_DOMAINS) ↔ 문서에 쓰는 우리말 이름. 🔴 새 도메인이 원장에 생기면 여기 이름을 적어야 통과한다. 안 적으면 RED — 「필수 시나리오에 소리가 없다」(TPL-9.2)가 정확히 그 형태로 생겼다. 오디오는 도메인 원장에는 들어왔는데 검사 계획에는 아무도 안 넣었고, 손목록이라 빈칸이 안 … |
| `scripts/check-current-doc-facts.mjs` | export | function | `declaredTestDirs(testPlan)` | [...testPlan.matchAll(/`tests\/([a-z0-9]+)`/g)].map, testPlan.matchAll | TEST_PLAN이 선언한 `tests/<층>`을 뽑는다. 선언했으면 실재해야 한다. 🔴 첫 판은 `[a-z]+`였고 그래서 `tests/e2e`를 통째로 못 읽었다 (숫자가 들어 있다). 하필 실재하지 않는 그 층을 못 읽으니, 게이트는 조용히 「위반 없음」을 냈을 것이다 — 좁은 문자 클래스가 형제 하나를 지우는 … |
| `scripts/check-current-doc-facts.mjs` | export | function | `scenarioSection(testPlan)` | testPlan.indexOf, testPlan.slice | 필수 시나리오 절(TPL-2)만 잘라낸다 — 다른 절에 낱말이 있다고 덮인 것이 아니다. |
| `scripts/check-current-doc-facts.mjs` | export | function | `syncDomainsOf(source)` | /export const SYNC_DOMAINS\s*=\s*\[([^\]]*)\]/.exec, [...m[1].matchAll(/'([a-z]+)'/g)].map, m[1].matchAll | `export const SYNC_DOMAINS = [...]` 한 줄에서만 뽑는다. 못 찾으면 던진다(조용한 0 금지). |
| `scripts/check-current-doc-facts.mjs` | export | function | `currentDocProblems({ roadmap, dataModel, testPlan, handoff }, latestMigration, world = {})` | declaredTestDirs, testDirs.includes, problems.push, scenarioSection, section.includes, roadmap.includes, dataModel.includes, /운영 미적용/.test, testPlan.indexOf, handoff.includes | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `good` | — | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `injected` | — | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `WORLD_OK` | — | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `goodPlan` | — | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `axisCases` | — | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `axisBroken` | — | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `migrations` | — | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `latestMigration` | — | — |
| `scripts/check-current-doc-facts.mjs` | internal | const | `problems` | — | — |
| `scripts/check-date-freshness.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-date-freshness.mjs` | internal | const | `CHANGELOG` | — | — |
| `scripts/check-date-freshness.mjs` | internal | const | `LOCK` | — | — |
| `scripts/check-date-freshness.mjs` | export | const | `TOLERANCE_DAYS` | — | 시간대 때문에 정상적으로 생기는 하루 차이는 위반이 아니다(머리말 참조). |
| `scripts/check-date-freshness.mjs` | export | const | `RELEASE_ARRIVAL_TAIL_MINUTES` | — | 릴리스가 CI를 지나 Pages에 도착하기까지의 꼬리. 2026-08-13 v2.27 실측은 13.0분이었다 (PR CI 시작 → 배포 완료; harness 2.2분 · 머지→배포 0.8분 · 나머지는 판단 시간). 추측한 25분이 아니라 측정값을 쓴다. 이 값은 도착 보증이 아니라 날짜를 무엇으로 적을지 알려 주는 기… |
| `scripts/check-date-freshness.mjs` | internal | const | `DATE_RE` | — | `YYYY-MM-DD`. 앞뒤에 숫자가 붙은 것(버전·해시 조각)은 날짜가 아니다. |
| `scripts/check-date-freshness.mjs` | export | function | `daysBetween(a, b)` | Math.round, Date.parse | 두 `YYYY-MM-DD` 사이의 일수(a - b). 문자열만 받는다 — 시계는 호출자가 준다. |
| `scripts/check-date-freshness.mjs` | export | function | `expectedReleaseDate(now, tailMinutes = RELEASE_ARRIVAL_TAIL_MINUTES)` | new Date(now.getTime() + tailMinutes * 60_000).toISOString(…, new Date(now.getTime() + tailMinutes * 60_000).toISOString, now.getTime | 예상 릴리스 도착 시각의 UTC 날짜. 시계는 호출자가 넣어 재현 가능하게 둔다. |
| `scripts/check-date-freshness.mjs` | export | function | `findFutureDates(text, file, today)` | text.matchAll, daysBetween, out.push | A) 미래 날짜를 찾는다. 허용치를 넘는 것만 위반이다. 🔴 생성 블록·인용은 가리지 않는다 — 미래 날짜는 어디에 있든 틀린 값이기 때문이다. |
| `scripts/check-date-freshness.mjs` | export | function | `newestRelease(src)` | /version:\s*'([\d.]+)'\s*,\s*\n\s*date:\s*'(\d{4}-\d{2}-\d{… | changelog에서 맨 앞(최신) 항목의 버전과 날짜를 뽑는다. 못 읽으면 `null`. |
| `scripts/check-date-freshness.mjs` | export | function | `findStaleReleaseDate(newest, arrivalDate, armed)` | — | B) 릴리스가 얼려 있을 때만 잰다 — 최신 항목이 예상 도착일 것 인가. @returns 위반 문자열 배열(빈 배열이면 통과) · 얼려 있지 않으면 빈 배열 |
| `scripts/check-date-freshness.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-dependabot-policy.mjs` | export | function | `dependabotProblems(text)` | text.matchAll, ecosystems .filter(([, , block]) => !/open-pull-requests-li…, ecosystems .filter, /open-pull-requests-limit:\s*0\b/.test | — |
| `scripts/check-dependabot-policy.mjs` | internal | const | `good` | — | — |
| `scripts/check-dependabot-policy.mjs` | internal | const | `bad` | — | — |
| `scripts/check-dependabot-policy.mjs` | internal | const | `problems` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-diag-blindspots.mjs` | export | function | `parseBlindSpots(src)` | src.indexOf, src.slice, body.matchAll, chunk.match, items.push, str, /\bpendingReason:/.test | `BLIND_SPOTS` 배열의 항목을 소스에서 파싱한다(런타임 import 없이 — 게이트는 빌드 전에도 돈다). |
| `scripts/check-diag-blindspots.mjs` | export | function | `parseGroups(src)` | src.match, [...m[1].matchAll(/'([a-z]+)'/g)].map, m[1].matchAll | 그룹 목록을 소스에서 뽑는다. |
| `scripts/check-diag-blindspots.mjs` | export | function | `parseToolIds(src)` | src.match, [...m[0].matchAll(/\bid: '([\w-]+)'/g)].map, m[0].matchAll | 도구 등록부(`CORE_TOOLS`)의 id 목록. |
| `scripts/check-diag-blindspots.mjs` | export | function | `parseToolGroups(src)` | src.match, [...m[0].matchAll(/\bid: '([\w-]+)',\s*(?:group: '([a-z]+)'…, m[0].matchAll | 도구의 `{id, group}` 쌍. `group`이 없으면 `null`로 들어온다(그게 잡을 대상이다). |
| `scripts/check-diag-blindspots.mjs` | export | function | `checkToolGroups(toolGroups, groups, items)` | problems.push, groups.includes, toolGroups.some, (items ?? []).some | 🔴 분류가 도구까지 닿았는가 — F·G (2026-08-05 추가). 왜 생겼나: v1.76이 경로축 8단계를 만들고 인계에 "재분류했다" 고 적었는데, 실제로는 사각지대 등록부에만 걸렸고 도구에는 안 걸렸다. `DIAG_GROUPS`를 `src` 전체에서 찾으면 자기 파일 밖에서 쓰는 곳이 하나도 없었고, 허브는 도… |
| `scripts/check-diag-blindspots.mjs` | export | function | `parseGroupIcons(src)` | src.match, [...m[0].matchAll(/(\w+): \{ icon: '([^']+)'/g)].map, m[0].matchAll | `GROUP_META`의 `{group, icon}` 쌍. |
| `scripts/check-diag-blindspots.mjs` | export | function | `checkGroupIcons(icons)` | problems.push, /^\p{Emoji_Presentation}$/u.test, icon.endsWith | 🔴 단계 아이콘이 흑백 두부로 그려지지 않는가 — H (2026-08-05, 육안에서 나옴). 유니코드에는 기본이 글자 표현인 이모지 가 있다(`Emoji_Presentation=No`). `🛡`·`🖼`·`☁`가 그렇다 — 뒤에 이형자 선택자 U+FE0F를 안 붙이면 흑백 윤곽선 으로 그려진다. 실제로 허브를 캡처… |
| `scripts/check-diag-blindspots.mjs` | export | function | `checkBlindSpots(items, groups, toolIds, toolGroups = [])` | problems.push, items.forEach, it.what.slice, seenIds.has, seenIds.add, groups.includes, ['structural', 'transient'].includes, toolIds.includes, toolGroups.find | 순수 검사 — 셀프테스트가 가짜 입력으로 직접 돌린다. |
| `scripts/check-diag-blindspots.mjs` | internal | const | `G` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `T` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `ok` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `parseFixture` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `parsedLf` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `parsedCrlf` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `selfCases` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `selfToolGroups` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `brokenSelf` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `pending` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `groupCases` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `iconCases` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `brokenIcons` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `brokenGroups` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `groupsSrc` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `toolsSrc` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `items` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `groups` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `toolIds` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `toolGroups` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `problems` | — | — |
| `scripts/check-diag-blindspots.mjs` | internal | const | `covered` | — | — |
| `scripts/check-doc-counts.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-doc-counts.mjs` | export | function | `appVersionPlacementProblems(entries)` | text.split(/\r?\n/).entries, text.split, /<!--reg:appVersion-->.*?<!--\/reg-->/.test, line.includes, found.push, found.filter((x) => !x.allowed).map, found.filter, bad.push | 현재 버전 마커를 과거 커밋·배포 기록에 붙이면 생성 때마다 역사가 바뀐다(M-0086). |
| `scripts/check-doc-counts.mjs` | internal | const | `reg` | — | — |
| `scripts/check-doc-counts.mjs` | internal | const | `problems` | — | — |
| `scripts/check-doc-counts.mjs` | internal | const | `markerCount` | — | — |
| `scripts/check-doc-counts.mjs` | internal | const | `docs` | — | — |
| `scripts/check-doc-governance.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-doc-governance.mjs` | internal | const | `CONSTITUTION` | — | — |
| `scripts/check-doc-governance.mjs` | internal | const | `NO_MAP_REQUIRED` | — | 문서 지도에 없어도 되는 `docs/ .md` — 이유를 반드시 적는다 (비면 규율이 죽는다). 여기 한 줄은 "이 문서는 다음 사람이 몰라도 된다" 는 선언이다. 그러니 이유는 왜 몰라도 손해가 없는가 를 적어야 한다. |
| `scripts/check-doc-governance.mjs` | internal | const | `KNOWN_INSTRUCTION_FILES` | — | 이 저장소가 아는 AI 지시문 파일 들. 있으면 반드시 어댑터로 등록돼야 한다. 손으로 적는 목록이지만 없는 파일은 검사하지 않으므로 오탐이 없고, 새 도구를 도입할 때 여기 한 줄을 더하는 것이 곧 "그 도구도 같은 계약을 읽게 하라"는 신호가 된다. |
| `scripts/check-doc-governance.mjs` | export | function | `docMapSection(text)` | /## 문서 지도 \(SSOT\)([\s\S]*?)(?=\n## \\|$)/.exec | 문서 지도 절만 잘라낸다. 순수 함수라 셀프테스트가 직접 돌린다(§4). |
| `scripts/check-doc-governance.mjs` | export | function | `unmapped(docs, mapText, excused)` | docs.filter, excused.has, mapText.includes | 지도에 안 실린 문서를 고른다. |
| `scripts/check-doc-governance.mjs` | export | function | `unregistered(present, adapters)` | present.filter, adapters.includes | 등록되지 않은 지시문 파일을 고른다. |
| `scripts/check-doc-governance.mjs` | internal | const | `problems` | — | — |
| `scripts/check-doc-governance.mjs` | internal | const | `con` | — | — |
| `scripts/check-doc-governance.mjs` | internal | const | `mapText` | — | — |
| `scripts/check-doc-governance.mjs` | internal | const | `docs` | — | — |
| `scripts/check-doc-governance.mjs` | internal | const | `present` | — | — |
| `scripts/check-doc-tree.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-doc-tree.mjs` | internal | const | `SHAPES` | — | — |
| `scripts/check-doc-tree.mjs` | export | function | `governedDocs(root)` | readdirSync, join, e.isDirectory, walk, e.name.endsWith, out.push, out.sort | governed 문서 집합 — `docs/ .md` 전부. 모집단을 손으로 적지 않는다(HRL-1). |
| `scripts/check-doc-tree.mjs` | export | function | `parseFrontmatter(text)` | text.replace, text.startsWith, text.indexOf, text.slice(4, end).split, text.slice, /^([a-z_]+):\s*(.*)$/.exec, line.trim, m[2].trim | frontmatter를 읽는다. 없으면 `null` — 미선언은 실패 이지 통과가 아니다. |
| `scripts/check-doc-tree.mjs` | export | function | `findTreeProblems(rel, body)` | body.split, lines.forEach, /^##\s+\S/.test, /^###/.test, majors.push, line.slice(3).trim, line.slice, problems.push, majors.forEach, lines.slice | 트리 규칙 다섯을 판정한다 — 순수 함수(자체검사가 가짜 문서로 부른다). 🔴 축·근거는 「대분류 자신의 머리말」에서만 인정한다 (TRE-2.4). Medical Note의 첫 판은 자식(`###`)의 축이 부모를 대신 만족시켜 축을 지워도 초록 이었다. 판정 범위를 첫 중분류 앞으로 좁히지 않으면 이 게이트는 조용히… |
| `scripts/check-doc-tree.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-doc-tree.mjs` | internal | const | `OK_DOC` | — | — |
| `scripts/check-doc-tree.mjs` | export | function | `selfTest()` | findTreeProblems, OK_DOC.replace, OK_DOC.replace('## X-9 이 문서가 못 보는 것', '## X-9 남은 이야기').repl…, findTreeProblems('t.md', doc).some, p.includes, parseFrontmatter | — |
| `scripts/check-doc-tree.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-domain-symmetry.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-domain-symmetry.mjs` | internal | const | `SERVICES` | — | — |
| `scripts/check-domain-symmetry.mjs` | internal | const | `NO_OP_REQUIRED` | — | op를 만들지 않아도 되는 함수 — 이유를 반드시 적는다 (비면 규율이 죽는다). 헌법 §7(2026-07-27): 대칭이 기본값이고, 비대칭은 설계 단계에서 심사한 예외 만 허용한다. 이 표가 그 심사 결과를 담는 자리다. 여기 한 줄을 적는 것은 "형제와 다르게 두겠다"는 선언이므로, 이유는 무엇이 다른가 가 아니라… |
| `scripts/check-domain-symmetry.mjs` | export | function | `bodyOf(src, afterParams)` | src.slice | 함수 본문을 중괄호 균형으로 잘라낸다. ⚠️ 이 게이트 자신의 결함(2026-07-25): 처음에는 인자 목록 뒤 첫 `{` 를 본문 시작으로 봤다. 그런데 반환 타입이 `Promise<{ a: string[] }>`처럼 객체면 그 중괄호를 본문으로 착각해, 멀쩡한 함수를 "op를 안 만든다"고 오탐 했다(moments… |
| `scripts/check-domain-symmetry.mjs` | internal | function | `afterParams(src, openParen)` | — | 인자 목록 여는 괄호 위치 → 닫는 괄호 다음 위치. |
| `scripts/check-domain-symmetry.mjs` | export | function | `writesLocal(body)` | /\b(?:d\.local\w+\\|db\(\)\.local\w+\\|local\w+\\|localTable\\|… | 로컬 테이블에 쓰는가. 이것이 "상태를 바꾸는 함수"의 정의다 — 이름이 아니라 행동. Dexie 쓰기 메서드를 테이블스러운 수신자(`d.localX`·`table`·`localTable`) 위에서 찾는다. `new Set(...).delete(x)` 같은 자료구조 조작을 쓰기로 세지 않으려고 수신자를 제한한다. |
| `scripts/check-domain-symmetry.mjs` | export | const | `LIFECYCLE_NAME` | — | 이름으로 보는 생명주기 짝(A 검사 전용). |
| `scripts/check-domain-symmetry.mjs` | export | function | `mutationsIn(src)` | src.matchAll, bodyOf, afterParams, writesLocal, LIFECYCLE_NAME.test, out.push | 파일에서 로컬 테이블에 쓰는 export 함수를 찾아 {name, body} 목록을 만든다. 생명주기 이름(softDelete·restore로 시작)은 쓰기가 없어도(위임형) 짝 검사를 위해 포함한다. |
| `scripts/check-domain-symmetry.mjs` | export | function | `enqueuesOp(body)` | /syncQueue\.(add\\|bulkAdd)/.test, /\bops\b\s*[:=]/.test, /childOp\(/.test, body.matchAll, /^(softDelete\\|restore\\|create\\|update\\|add\\|purge)\w+\\|\w*… | 큐에 op를 넣는 코드가 있는가(직접 add / ops 배열 / 헬퍼 위임 전부 인정). 위임도 인정한다 : 본문이 다른 변경 함수를 부르면 op 책임은 그쪽에 있다 (`restoreTripFromTrash` → `restoreTripLocalFirst`). 위임을 못 알아보면 게이트가 멀쩡한 함수를 오탐하고, 오탐은 … |
| `scripts/check-domain-symmetry.mjs` | export | function | `checkSources(files)` | mutationsIn, muts.map, n.startsWith, n.replace, names.has, problems.push, NO_OP_REQUIRED.has, enqueuesOp | 대칭·op 검사 → 문제 목록(빈 배열 = 통과). |
| `scripts/check-domain-symmetry.mjs` | internal | const | `files` | — | — |
| `scripts/check-domain-symmetry.mjs` | internal | const | `problems` | — | — |
| `scripts/check-domain-symmetry.mjs` | internal | const | `total` | — | — |
| `scripts/check-domain-wiring.mjs` | internal | function | `tablesFromDoc(md)` | md.matchAll, m[1].split, tables.push | DATA_MODEL.md에서 테이블 헤딩(## snake_case[ / snake_case][ (주석)])을 추출. |
| `scripts/check-domain-wiring.mjs` | internal | function | `tablesFromRegistry(src)` | [...src.matchAll(/table:\s*'([a-z_]+)'/g)].map, src.matchAll | registry.ts에서 table: '...' 선언을 추출. |
| `scripts/check-domain-wiring.mjs` | internal | function | `compare(docTables, regTables)` | problems.push, regTables.includes, docTables.includes, regTables.filter, regTables.indexOf, dup.join | 두 목록 대조. 견고한 추출기 규율: 한쪽이 비정상적으로 적으면 파서 실패로 간주. |
| `scripts/check-domain-wiring.mjs` | internal | const | `DOC_FIX` | — | — |
| `scripts/check-domain-wiring.mjs` | internal | const | `REG_FIX` | — | — |
| `scripts/check-domain-wiring.mjs` | internal | const | `selfCases` | — | — |
| `scripts/check-domain-wiring.mjs` | internal | const | `brokenSelf` | — | — |
| `scripts/check-domain-wiring.mjs` | internal | const | `docTables` | — | — |
| `scripts/check-domain-wiring.mjs` | internal | const | `regTables` | — | — |
| `scripts/check-domain-wiring.mjs` | internal | const | `problems` | — | — |
| `scripts/check-edge-cors.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-edge-cors.mjs` | export | function | `stripComments(src)` | src.replace(/\/\*[\s\S]*?\*\//g, ' ').replace, src.replace | 주석을 지운다. 이 게이트를 만들자마자 내 주석에 걸렸다 — 규칙을 설명하려고 적은 `req.json()` 한 조각이 코드보다 앞서 있어 순서 판정이 뒤집혔다(§11 ③ 오탐도 결함이다). |
| `scripts/check-edge-cors.mjs` | export | function | `answersPreflight(src)` | stripComments, code.search | 사전요청을 본문을 읽기 전에 답하는가. 순서가 틀리면 `req.json()`이 먼저 던진다. 파일 전체에서 잰다 — 진입 방식이 두 가지이기 때문이다(`serve(async …)` 직접 전달 · 이름 붙인 `handler`를 나중에 `serve(handler)`). 핸들러를 찾아 자르는 방식은 후자를 놓쳤다. |
| `scripts/check-edge-cors.mjs` | export | function | `hasCorsHeaders(src)` | stripComments, /['"]Access-Control-Allow-Origin['"]/.test, /['"]Access-Control-Allow-Headers['"]/.test, /['"]Access-Control-Allow-Methods['"]/.test | 사전요청에 필요한 세 헤더를 다 적었는가. 하나라도 빠지면 브라우저가 거절한다. |
| `scripts/check-edge-cors.mjs` | internal | function | `selfTest()` | answersPreflight, hasCorsHeaders, cases.filter, fn, console.error, process.exit | — |
| `scripts/check-edge-cors.mjs` | internal | const | `fns` | — | — |
| `scripts/check-edge-cors.mjs` | internal | const | `problems` | — | — |
| `scripts/check-edge-fn-ops.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-edge-fn-ops.mjs` | internal | const | `FN_PATH` | — | — |
| `scripts/check-edge-fn-ops.mjs` | export | function | `handledOps(src)` | [...src.matchAll(/(?<![.\w])op === '(\w+)'/g)].map, src.matchAll | 핸들러가 실제로 분기하는 op 이름. `body.op === 'string'`(typeof 비교)까지 세면 거짓 경보가 난다 — 실제로 처음 짰을 때 그렇게 걸렸다. 앞에 점이나 글자가 없는 `op` 만 센다. |
| `scripts/check-edge-fn-ops.mjs` | export | function | `declaredOps(src)` | src.match, [...(m[1] ?? '').matchAll(/'(\w+)'/g)].map, (m[1] ?? '').matchAll | `export const FN_OPS = [...] as const;` 에서 선언된 op 이름. 못 찾으면 null. |
| `scripts/check-edge-fn-ops.mjs` | export | function | `audit(src)` | declaredOps, problems.push, handledOps, declared.has, handled.has, /export const FN_VERSION = \d+;/.test | — |
| `scripts/check-edge-fn-ops.mjs` | export | function | `inviteAudit(src)` | /async function isInvited\s*\(/.test, /rest\/v1\/rpc\/is_allowed/.test, /if\s*\(\s*!\s*\(\s*await\s+isInvited\s*\(\s*req\s*\)\s*\)\…, problems.push | 🔐 초대제 형태검사 — Edge Function이 인증만 하고 초대제를 빠뜨리지 않았는가. 왜 생겼나(H-1 · 전수감사 2026-08-01): journey 테이블은 RLS로 `auth.uid() = user_id AND journey.is_allowed()`를 이중으로 건다(0002). 그런데 R2 서명·지오코딩은… |
| `scripts/check-edge-fn-ops.mjs` | internal | const | `selfTestCount` | — | — |
| `scripts/check-edge-fn-ops.mjs` | internal | const | `inviteSelfTestCount` | — | — |
| `scripts/check-edge-fn-ops.mjs` | internal | function | `edgeFunctionPaths()` | join, existsSync, readdirSync(dir, { withFileTypes: true }) .filter((e) => e.…, readdirSync(dir, { withFileTypes: true }) .filter, readdirSync, e.isDirectory | 🔴 대상은 손으로 세지 않고 디렉터리에서 뽑는다 (§7 — 형제 목록을 손으로 세지 마라). 2026-07-30까지 이 게이트는 `media-sign` 한 개를 상수로 갖고 있었다. 그래서 두 번째 함수(`geocode`)를 만드는 순간, 새 형제는 형제들이 지키는 계약을 하나도 안 받은 채 태어날 수 있었다 — `c… |
| `scripts/check-edge-fn-ops.mjs` | internal | const | `targets` | — | — |
| `scripts/check-edge-fn-ops.mjs` | internal | const | `failed` | — | — |
| `scripts/check-edge-fn-ops.mjs` | internal | const | `opTotal` | — | — |
| `scripts/check-enforcement-parity.mjs` | internal | const | `GITHOOKS` | — | — |
| `scripts/check-enforcement-parity.mjs` | export | const | `HOOK_REGISTRY` | — | Claude 전용 강제층의 등록부 — 누구를 묶는가 와 다른 AI는 무엇이 대신하는가 . 🔴 새 훅을 만들면 여기에도 등록해야 한다. 안 하면 RED다. 이유 없는 비대칭은 결함이고, 이유를 적어야 다음 사람이 판단할 수 있다 (§7). |
| `scripts/check-enforcement-parity.mjs` | export | function | `executableGitHooks(dir, statFn, listFn)` | listFn, files.filter, statFn, join, execFileSync, /^100755\s/.test | `.githooks/`에서 실행 가능한 훅 목록. |
| `scripts/check-enforcement-parity.mjs` | export | function | `wiresGitHooks(pkg)` | /core\.hooksPath/.test, /\.githooks/.test | `prepare`가 `core.hooksPath`를 `.githooks`로 배선하는가. |
| `scripts/check-enforcement-parity.mjs` | internal | function | `selfTest()` | executableGitHooks, p.endsWith, wiresGitHooks, cases.filter, fn, console.error, process.exit | — |
| `scripts/check-enforcement-parity.mjs` | internal | const | `problems` | — | — |
| `scripts/check-enforcement-parity.mjs` | internal | const | `hooks` | — | — |
| `scripts/check-enforcement-parity.mjs` | internal | const | `claudeHooks` | — | — |
| `scripts/check-enforcement-parity.mjs` | internal | const | `onlyClaude` | — | — |
| `scripts/check-env-assumption.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-env-assumption.mjs` | export | const | `WORLDS` | — | 🔴 세계 어휘는 동결이다. 자유 서술로 두면 「음... 환경 좀 탐」 같은 값이 들어오고, 그 순간 원장은 소음이 된다. 새 세계가 필요하면 여기에 벗기는 방법과 함께 추가한다. `strippable`이 참인 것만 아래 빈 세계가 실제로 벗긴다. 나머지는 선언으로만 안다 — 그 사실을 판정문에 적는다(§8: 모르는 것… |
| `scripts/check-env-assumption.mjs` | export | const | `PROBE_EXCLUDE` | — | 빈 세계에서 안 돌리는 게이트와 그 이유. 🔴 이유 없는 제외는 결함이다(§7). `slow: true`와 `node scripts/ .mjs`가 아닌 항목은 아래 `probeTargets`가 규칙으로 거른다 — 여기 적는 것은 그 규칙으로 안 걸러지는 개별 예외 뿐이고, 지금은 둘이다. |
| `scripts/check-env-assumption.mjs` | export | function | `crashed(stderr)` | /^\s+at\s+\S+/m.test, String | node가 삼키지 못한 예외 를 뱉었는가. 스택 프레임 한 줄이면 충분하다. |
| `scripts/check-env-assumption.mjs` | export | function | `verdictOf(name, declared, barren, homeStatus = 0)` | crashed, [0, 1, 2].includes, declared.filter, Object.entries(WORLDS).filter(([, v]) => v.strippable).map(…, Object.entries(WORLDS).filter(([, v]) => v.strippable).map, Object.entries(WORLDS).filter, Object.entries | 한 게이트의 빈 세계 결과를 판정한다 — 순수 함수(자체검사가 가짜 결과로 부른다). @param declared 원장이 이 게이트에 적어 둔 세계 이름들 @param barren `{ status, stderr }` — 빈 세계에서 실제로 돌린 결과 @param homeStatus 같은 게이트를 집(작업 저장소) 에서… |
| `scripts/check-env-assumption.mjs` | export | function | `ledgerProblems(gateNames, worlds)` | problems.push, String(raw).split(/\s+/).filter, String(raw).split, String, Object.keys(WORLDS).join, Object.keys, gateNames.includes | 원장 자체의 결함(누락·어휘 밖·유령)을 본다 — 순수 함수. |
| `scripts/check-env-assumption.mjs` | export | function | `probeTargets(entries, exclude = PROBE_EXCLUDE)` | entries .filter((g) => !g.slow && /^node scripts\/[\w-]+\.m…, entries .filter, /^node scripts\/[\w-]+\.mjs$/.test | harness 등록부에서 빈 세계에 넣을 게이트만 고른다 — 규칙으로 거르고, 예외는 이유와 함께. |
| `scripts/check-env-assumption.mjs` | export | function | `buildBarrenWorld(root)` | mkdtempSync, join, tmpdir, execFileSync('git', ['ls-files', '-z', '--cached', '--other…, execFileSync, mkdirSync, dirname, copyFileSync, git, existsSync | 추적 파일을 작업트리 그대로 옮긴 임시 사본을 만든다. HEAD가 아니라 지금 고치고 있는 판을 재야 한다 — 안 그러면 방금 만든 게이트는 이 검사를 영원히 안 받는다. 🔴 벗길 것만 벗긴다. 첫 판은 `git init`만 하고 끝냈고, 그래서 추적 파일이 0개인 세계가 됐다 — `actions/checkout`이 … |
| `scripts/check-env-assumption.mjs` | export | function | `directoryLinkType(platform)` | — | Windows의 directory symlink는 개발자 모드/관리자 권한을 요구한다. junction은 같은 역할을 권한 없이 한다. |
| `scripts/check-env-assumption.mjs` | export | function | `runGate(dir, name)` | spawnSync | 게이트 하나를 어느 세계에서 돌린다(동기 — 카나리아·집 재측정처럼 드문 자리에서만). |
| `scripts/check-env-assumption.mjs` | export | async function | `runGatesInParallel(dir, names, limit)` | Math.max, Math.min, cpus, out.set, spawn, p.stderr.on, p.stdout.on, p.on, resolve, Promise.all | 🔴 빈 세계의 게이트들은 병렬이 기본이다 (§18-C). 서로의 결과를 입력으로 쓰지 않고, 같은 자원을 쓰지도 않는다(각자 읽기 전용으로 사본을 볼 뿐이다) — 줄 세울 이유가 없다. 첫 판은 55개를 줄 세웠고 편집 루프가 6초에서 16초가 됐다. "낭비되는 시간의 대부분은 대기가 아니라 병렬로 할 수 있는 것을 … |
| `scripts/check-env-assumption.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-env-assumption.mjs` | export | function | `selfTest()` | ledgerProblems, directoryLinkType, ledgerProblems(n, w).some, p.includes, verdictOf, String(verdictOf('g', d, r, home)).includes, String, probeTargets, targets.join | — |
| `scripts/check-env-assumption.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-env-wiring.mjs` | internal | const | `WORKFLOW` | — | — |
| `scripts/check-env-wiring.mjs` | internal | const | `EXCLUDE` | — | 대조에서 제외할 이름. 반드시 근거를 남긴다(비면 규율이 산다). |
| `scripts/check-env-wiring.mjs` | internal | function | `tsFiles(dir)` | readdirSync, join, statSync(p).isDirectory, statSync, out.push, tsFiles, name.endsWith | src/ 아래 .ts 파일 전부. |
| `scripts/check-env-wiring.mjs` | export | function | `envNamesInCode(text)` | [...text.matchAll(/import\.meta\.env\.(VITE_[A-Z0-9_]+)/g)]…, text.matchAll | 텍스트에서 `import.meta.env.VITE_X` 이름을 모은다. |
| `scripts/check-env-wiring.mjs` | export | function | `envNamesInWorkflow(text)` | text.matchAll, out.add | 워크플로 텍스트에서 build 단계 env에 배선된 `VITE_X: ${{ vars.VITE_X }}` 이름을 모은다. |
| `scripts/check-env-wiring.mjs` | export | function | `compare(code, wf)` | n.startsWith, problems.push, n.slice, EXCLUDE.has, wf.has, code.has | 두 집합을 비교해 문제 목록을 만든다(빈 배열 = 통과). |
| `scripts/check-env-wiring.mjs` | internal | const | `code` | — | — |
| `scripts/check-env-wiring.mjs` | internal | const | `wf` | — | — |
| `scripts/check-env-wiring.mjs` | internal | const | `problems` | — | — |
| `scripts/check-exif-order.mjs` | internal | const | `MEDIA` | — | — |
| `scripts/check-exif-order.mjs` | internal | const | `SCREEN` | — | — |
| `scripts/check-exif-order.mjs` | export | function | `sliceFunctionBody(text, name)` | text.indexOf, text.slice | `addPhotoToMoment` 본문만 잘라낸다(다른 함수의 호출 순서에 속지 않게). |
| `scripts/check-exif-order.mjs` | export | function | `findOrderViolations(body)` | body.indexOf, out.push, /readPhotoMeta\(\s*([A-Za-z_$][\w$]*)/.exec | 순서·인자 위반을 찾는다. 반환이 비면 통과. |
| `scripts/check-exif-order.mjs` | export | function | `findGalleryDefaultViolations(text)` | re.exec, /GALLERY/i.test, out.push, literal.includes | 🔴 사진 입력칸의 기본 `accept`가 갤러리 선택기로 내려가 있지 않은가(M-0064). 라이브 검사도 이걸 재지만(`verify-editor-live`), 라이브는 빠른 차선에서 건너뛴다. 이 결함의 값이 나흘이었으므로 건너뛸 수 없는 층 에도 둔다(SKIP은 통과가 아니다 — §11). |
| `scripts/check-exif-order.mjs` | export | function | `findPickViolations(text)` | [...text.matchAll(/readPhotoMeta\(\s*([A-Za-z_$][\w$]*)/g)]…, text.matchAll, out.push, /edit\\|result\\|blob\\|compressed/i.test | 고를 때 메타를 읽는 곳이 편집기 산출물을 읽고 있지 않은가. |
| `scripts/check-exif-order.mjs` | export | function | `findExifWindowViolations(text)` | re.exec, out.push | E. 🔴 EXIF 읽기 창을 손 매직넘버로 쓰지 않는가 (H-2 · 2026-08-01). 저장 경로(media.ts)와 🔬 관측 창(tripDetail.ts)이 서로 다른 크기(256KB vs 512KB)를 손으로 썼더니, 그 사이에 EXIF가 앉은 사진에서 🔬는 "위치 있음 → 앱 결함"이라 말하고 저장은 좌표… |
| `scripts/check-exif-order.mjs` | internal | function | `selfTest()` | findOrderViolations, fails.push, findPickViolations, findGalleryDefaultViolations, findExifWindowViolations | — |
| `scripts/check-exif-order.mjs` | internal | const | `selfFails` | — | — |
| `scripts/check-exif-order.mjs` | internal | const | `screenText` | — | — |
| `scripts/check-exif-order.mjs` | internal | const | `mediaText` | — | — |
| `scripts/check-exif-order.mjs` | internal | const | `problems` | — | — |
| `scripts/check-exif-strip-on-share.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-exif-strip-on-share.mjs` | export | function | `uploadsOriginalBytes(src)` | src.matchAll, args.split, RAW.test, bad.push, second.trim | 업로드 호출이 원본 blob 을 넘기고 있지 않은가. 넘기면 EXIF가 그대로 서버로 간다. |
| `scripts/check-exif-strip-on-share.mjs` | export | function | `derivesViaCanvas(src)` | /canvas\.toBlob\(/.test, bad.push, /return\s*\{\s*blob:\s*original\b/.test | 파생본 생성이 canvas 재인코딩을 지나는가. 지나지 않으면 원본 메타가 살아남는다. |
| `scripts/check-exif-strip-on-share.mjs` | export | function | `privacyDocReferences(doc)` | /check-exif-strip-on-share/.test | 문서가 이 게이트를 계속 부르는가(왕복). 문서에서 사라지면 게이트만 남아 이유를 잃는다. |
| `scripts/check-exif-strip-on-share.mjs` | internal | function const | `read(rel)` | existsSync, join, readFileSync | — |
| `scripts/check-exif-strip-on-share.mjs` | internal | const | `violations` | — | — |
| `scripts/check-fn-size.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-fn-size.mjs` | internal | const | `LIMIT` | — | 새로 만드는 최상위 함수의 상한. 이보다 길게 쓰려면 쪼개거나, 근거와 함께 LEGACY에 올린다. |
| `scripts/check-fn-size.mjs` | internal | const | `LEGACY` | — | 이미 있던 큰 함수와 현재 길이. 값은 줄어들기만 한다(늘리려면 리뷰에서 근거를 대라). 항목을 지우는 것은 자유 — LIMIT 아래로 내려왔다는 뜻이다. |
| `scripts/check-fn-size.mjs` | export | function | `topLevelFunctions(source, fileName = 'source.ts')` | ts.createSourceFile, sourceFile.getLineAndCharacterOfPosition, node.getStart, out.push, ts.isFunctionDeclaration, add, ts.isVariableStatement, ts.isIdentifier, ts.isArrowFunction, ts.isFunctionExpression | 최상위 함수의 (이름, 줄수)를 센다 — 순수 함수라 자체검사가 직접 부른다. 세는 법: 열 0에서 시작하는 선언부터, 열 0의 닫는 중괄호까지. 이 저장소는 최상위 함수가 항상 열 0에서 닫히므로 결정적이다(파서 없이도 흔들리지 않는다). 🔴 두 형태를 모두 센다 (전수 감사 정리): 예전엔 `function foo… |
| `scripts/check-fn-size.mjs` | internal | function | `collectTs(dir)` | readdirSync, join, e.isDirectory, out.push, collectTs, e.name.endsWith | src 아래 모든 `.ts`를 모은다. `fs.globSync`를 쓰지 않는다 — 그건 Node 22+ API인데 CI는 Node를 따로 핀하므로, 로컬(22)에서는 통과하고 CI(20)에서는 게이트가 실행조차 못 하고 죽었다 (2026-07-26, 배포 2회 연속 실패). 돌지 못하는 게이트는 없는 게이트보다 나쁘다 … |
| `scripts/check-fn-size.mjs` | internal | const | `files` | — | — |
| `scripts/check-fn-size.mjs` | internal | const | `problems` | — | — |
| `scripts/check-fn-size.mjs` | internal | const | `seen` | — | — |
| `scripts/check-fn-size.mjs` | internal | const | `tracked` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `CSS` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `FONT_DIR` | — | — |
| `scripts/check-font-subsets.mjs` | export | function | `parseFaces(css)` | css.matchAll, body.match, body.match(/unicode-range:\s*([^;]+);/)?.[1]?.trim, out.push, /font-display:\s*swap/.test | @font-face 블록에서 (파일명, unicode-range)를 선언 순서대로 뽑는다(순수). |
| `scripts/check-font-subsets.mjs` | export | function | `rangeSpan(range)` | range.split, part.trim().match, part.trim, parseInt | unicode-range 문자열이 덮는 코드포인트 수(대략 — 구간 합). |
| `scripts/check-font-subsets.mjs` | internal | const | `css` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `faces` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `problems` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `referenced` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `GENERATION_SOURCE` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `coarse` | — | — |
| `scripts/check-font-subsets.mjs` | internal | const | `precise` | — | — |
| `scripts/check-gate-control.mjs` | internal | const | `isMain` | — | 이 파일을 직접 실행했는가. `gen-registry.mjs`가 검출 함수를 import 해서 쓰므로, import만으로 검사가 돌면 안 된다 — 판정 로직은 한 곳에만 있어야 한다(§2 · 손편집 중복 금지). |
| `scripts/check-gate-control.mjs` | internal | const | `HARNESS` | — | — |
| `scripts/check-gate-control.mjs` | export | const | `HAS_CONTROL_BASELINE` | — | 🔴 래칫 기준선 둘 — 오직 올리기만 한다. 내리는 커밋은 없다. 왜 둘인가(2026-08-12 이 게이트를 만든 다음 판에 발견): 첫 판은 `--selftest` 플래그 만 셌는데, 세어 보니 대조군을 갖고도 플래그를 안 단 게이트가 14개 였다. 즉 이 게이트는 「대조군 보유」가 아니라 「독립 실행 가능」을 재면… |
| `scripts/check-gate-control.mjs` | export | const | `RUNNABLE_BASELINE` | — | — |
| `scripts/check-gate-control.mjs` | export | const | `CLEAN_EXIT_BASELINE` | — | 🔴 세 번째 축(2026-08-12 · M-0152) — 대조군이 실패를 「판정」으로 알리는가. 모든 게이트가 대조군을 갖고 있다. 남은 문제는 알리는 방식 이었다: `throw`로 죽으면 스택과 함께 `exit 1`이 되고, 하네스는 그것을 「위반을 찾았다」로 적는다. 자체검사 실패는 위반이 아니라 「이 게이트를 믿… |
| `scripts/check-gate-control.mjs` | export | function | `gateScripts(harnessSrc)` | harnessSrc.matchAll, /\bname:\s*'([^']+)'/.exec, /\bcmd:\s*'([^']+)'/.exec, /node\s+(scripts\/[\w.-]+\.mjs)/.exec, out.push | 하네스에 등록된 게이트 중 `node scripts/ .mjs`로 도는 것의 스크립트 경로를 뽑는다. 🔴 `name`이 첫 필드라고 가정하지 않는다 (2026-08-13 · M-0155). 예전 정규식은 `{` 바로 뒤의 `name:`만 봤다. 그런데 라이브 게이트 셋은 `{ slow: true, name: … }`로… |
| `scripts/check-gate-control.mjs` | export | function | `workflowScripts(workflowSrcs, pkgScripts)` | src.matchAll, out.set, /node\s+(scripts\/[\w.-]+\.mjs)/.exec, [...out.values()] .filter((script) => /^scripts\/(check\\|ve…, [...out.values()] .filter, out.values, /^scripts\/(check\\|verify)-/.test, script.replace | 🔴 하네스가 게이트의 전부가 아니다 (2026-08-13 · 사용자 질문 "숨겨진 게이트가 더 있으면?" ). 실측하니 `verify-sync-release-live`는 하네스에 없고 `ci.yml`·`deploy-pages.yml`이 `npm run verify:sync-release-live`로 직접 부른다. 고아… |
| `scripts/check-gate-control.mjs` | export | function | `gatePopulation(root = '.')` | join, existsSync, readdirSync(wfDir).filter((f) => f.endsWith('.yml')).map, readdirSync(wfDir).filter, readdirSync, f.endsWith, readFileSync, JSON.parse, gateScripts, workflowScripts | 🔴 모집단을 계산하는 한 곳 (2026-08-13 · M-0157). 왜 함수로 뽑았나: 예전엔 `gen-registry`와 이 게이트가 모집단을 따로 계산했다. 그래서 이 게이트가 모집단을 하네스 ∪ 워크플로로 넓혔을 때 생성기는 안 따라왔고 , 화면은 68 , 게이트는 69 를 말했다 — 사용자가 "게이트 카운터 … |
| `scripts/check-gate-control.mjs` | export | function | `stripComments(src)` | stripTemplates, src.replace(/\/\*[\s\S]*?\*\//g, ' ').replace, src.replace | 주석을 지운다. 🔴 이 게이트가 첫 판에 여기서 뚫렸다 — 호출을 `// selfTest();`로 주석 처리해도 대조군이 살아 있다고 셌다. 오늘 `check-edge-cors`에서 똑같이 당한 자리를 그대로 반복한 것이다(§7 — 한 곳에서 옳은 것은 형제 전부에서 옳다). |
| `scripts/check-gate-control.mjs` | export | function | `stripTemplates(src)` | src.replace | 백틱 템플릿 문자열을 지운다. 🔴 실제로 여기서 뚫렸다 (2026-08-13 · M-0155). 라이브 게이트에 대조군을 자동으로 넣다가 삽입이 `const FIXTURE = \`…\`` 안으로 들어갔다. 그 문자열은 픽스처 앱의 소스가 되므로 게이트에서는 한 줄도 실행되지 않는다. 그런데 이 검출기는 텍스트로만 봐서… |
| `scripts/check-gate-control.mjs` | export | function | `declaresSelftest(src)` | /process\.argv\.includes\(\s*['"]--selftest['"]\s*\)/.test, stripComments | 이 스크립트가 `--selftest`를 인자로 받아 처리 하는가(주석에 적힌 말이 아니라 코드). |
| `scripts/check-gate-control.mjs` | export | function | `hasControl(src)` | stripComments, hasSelfTestFunction, hasInlineSelfCheck | 대조군을 갖고 있는가 — 플래그와 무관하다. `selfTest` 함수가 정의돼 있고 실제로 불리는가 를 본다. 정의만 하고 안 부르면 그건 대조군이 아니라 죽은 코드 이고, 그 게이트는 여전히 공허하다. 인자를 받는 형태(`selfTest(server)`)도 센다 — 살아 있는 실제 자료를 먹이는 대조군이다. |
| `scripts/check-gate-control.mjs` | internal | function | `hasSelfTestFunction(code)` | /function\s+selfTest\s*\(/.test, /(^\\|[^\w.])selfTest\s*\([^)]*\)\s*;/m.test, code.replace | ① 이름 붙인 형태 — `function selfTest()`를 정의하고 실제로 부른다 . |
| `scripts/check-gate-control.mjs` | internal | function | `hasInlineSelfCheck(code)` | announceStyle | ② 블록 형태 — 이름 없는 `{ … }` 안에서 사례를 돌리고 실패하면 `exit(2)`로 나간다. 🔴 이 갈래를 놓쳐서 이 게이트는 대조군 41개를 「없음」으로 셌다 (2026-08-12 · M-0151). `check-ui-color-token`은 양성·음성 6사례를 갖고도 「없음」이었다. 검출기가 한 가지 모양… |
| `scripts/check-gate-control.mjs` | internal | const | `SELF_TEST_WORDING` | — | 자체검사를 말하는 낱말. 🔴 이 저장소는 네 가지를 쓴다 — 하나만 알면 형제를 못 본다(M-0151). |
| `scripts/check-gate-control.mjs` | export | function | `announceStyle(code)` | /runSelfTest\s*\(/.test, /throw\s+new\s+Error\(\s*['"`]\s*(SELF-TEST\\|셀프테스트\\|자체검사\\|자…, SELF_TEST_WORDING.test, /process\.exit\(\s*2\s*\)/.test, /process\.exit\(\s*1\s*\)/.test | 자체검사 실패를 어떻게 알리는가 — 한 게이트는 정확히 하나 의 값을 갖는다. 🔴 왜 술어 여럿이 아니라 값 하나인가(2026-08-12 · M-0153): 예전 판은 술어 둘 (`declaresSelfCheckExit`·`throwsOnSelfCheck`)을 `if / else if`로 이어 붙였다. 그래서 둘 다 … |
| `scripts/check-gate-control.mjs` | export | function | `declaresSelfCheckExit(code)` | announceStyle | 자체검사 실패를 판정으로 알린다 — 사유를 적고 `exit 2`(전제 미충족)로 나간다. |
| `scripts/check-gate-control.mjs` | export | function | `throwsOnSelfCheck(code)` | announceStyle | 자체검사 실패를 던져서 알린다 — `throw new Error('SELF-TEST 실패: …')`. 🔴 대조군은 있지만 알리는 방식이 §18-G에 어긋난다 : 던지면 스택과 함께 `exit 1`이 되고, 하네스는 그것을 「위반을 찾았다」 로 적는다. 그런데 자체검사 실패는 위반이 아니라 「이 게이트를 믿을 수 없다」… |
| `scripts/check-gate-control.mjs` | internal | function | `selfTest()` | gateScripts, gateScripts(harness).some, readFileSync, /gatePopulation\s*\(/.test, /gateControl\s*=\s*gateScripts\s*\(/.test, workflowScripts, hasControl, declaresSelftest, announceStyle, cases.filter | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-gate-integrity.mjs` | export | function | `listCheckFiles(dir, wired = new Set())` | readdirSync(dir) .filter((f) => f.startsWith('check-') && f…, readdirSync(dir) .filter, readdirSync, f.startsWith, f.endsWith, f.slice, [...new Set([...onDisk, ...wired])].sort | 감사 대상 파일 목록. 🔴 모집단을 파일명 glob으로 잡으면 이름을 안 따르는 형제가 통째로 빠진다 (2026-08-09 실측). 예전 판은 `check- .mjs`만 셌고, 그래서 "52개 전부 배선+대조군 보유" 라는 초록을 내면서 하네스에 배선된 게이트 셋을 한 번도 안 봤다 — `known.mjs`와, 하필 … |
| `scripts/check-gate-integrity.mjs` | export | function | `wiredScriptFiles(harnessSrc)` | [...harnessSrc.matchAll(/cmd:\s*'[^']*scripts\/([A-Za-z0-9_…, harnessSrc.matchAll | harness.mjs 소스에서 실제로 실행되는 스크립트 파일명(확장자 제외) 집합을 뽑는다. `name:` 필드가 아니라 `cmd:`의 실제 경로에서 뽑는다 — 표시 이름과 파일명이 다를 수 있다 (예: `check-known-index`는 `scripts/known.mjs --selftest`를 돈다. 이름=파일명을 … |
| `scripts/check-gate-integrity.mjs` | export | function | `hasSelfTestMarker(src)` | /(셀프테스트\\|자체검사\\|자기점검\\|SELF-TEST)[\s\S]{0,10}(실패\\|공허\\|건)/.test, /주입[\s\S]{0,20}(판정\\|검증\\|확인\\|실패\\|RED)/.test | §4 규율의 실행 흔적 — "셀프테스트/자체검사/자기점검/SELF-TEST" + "실패/공허" 근접 등장. 🔴 어휘를 좁게 잡으면 멀쩡한 대조군을 「없음」으로 읽는다 (2026-08-09). 이 저장소는 같은 규율을 네 가지 말로 쓴다 — 셀프테스트·자체검사·자기점검·SELF-TEST. 그리고 라이브 검사는 아예 「주… |
| `scripts/check-gate-integrity.mjs` | export | function | `hasNegativeCase(src)` | [ /\b(expectClean\\|clean\\|ok\\|valid\\|pass)\s*:\s*false/, /R…, p.test | 🔴 셀프테스트에 「실패를 기대하는 케이스」가 있는가 (2026-08-05 사용자 질문에서 강화). `hasSelfTestMarker`는 "셀프테스트라는 말이 나온다" 까지만 안다. 통과 케이스만 있고 실패 케이스가 없어도 그 문구는 붙는다 — 그러면 대조군이 있다고 보고되면서 실제로는 한쪽만 재는 셈이다. 대조군의 본… |
| `scripts/check-gate-integrity.mjs` | export | function | `findProblems(files, wired, srcOf)` | wired.has, problems.push, srcOf, hasSelfTestMarker, hasNegativeCase | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `GOOD_SRC` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `NO_TEST_SRC` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `PASS_ONLY_SRC` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `selfCases` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | function | `scriptsDir0()` | join | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `brokenSelf` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `scriptsDir` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `harnessSrcForFiles` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `files` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `harnessSrc` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `wired` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | function const | `srcOf(f)` | readFileSync, join | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `problems` | — | — |
| `scripts/check-gate-integrity.mjs` | internal | const | `missing` | — | — |
| `scripts/check-gate-promise.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-gate-promise.mjs` | internal | const | `AXIS_DEBT_BASELINE` | — | 🔴 동결 부채 목록 — 축을 아직 못 적은 게이트. 여기 이름을 새로 넣지 마라. 넣으면 이 게이트가 잡는다(래칫은 한 방향). 지금은 비어 있고, 비어 있는 것이 목표 상태다. |
| `scripts/check-gate-promise.mjs` | internal | const | `AXIS_FORM` | — | 축이 「무엇을 세지 않는가」를 실제로 말하는가. 이 문체가 이 원장의 유일한 이빨이다. |
| `scripts/check-gate-promise.mjs` | internal | const | `AXIS_MIN` | — | — |
| `scripts/check-gate-promise.mjs` | export | function | `recordKeys(source, constName)` | source.replace, source.search, source.indexOf, [...source.slice(open, close).matchAll(/^\s*(?:'([^']+)'\\|(…, source.slice(open, close).matchAll, source.slice | gates.ts의 Record 리터럴에서 키를 뽑는다(형제 게이트 check-registry-gen과 같은 규율). |
| `scripts/check-gate-promise.mjs` | export | function | `recordValues(source, constName)` | source.replace, source.search, source.indexOf, source.slice, body.matchAll, (m[3] ?? '').replace(/^\s*'\\|'\s*$/g, '').replace, (m[3] ?? '').replace | 키 → 값(문자열). 여러 줄에 걸친 값도 받는다. |
| `scripts/check-gate-promise.mjs` | export | function | `findProblems(gateNames, axes, debt)` | AXIS_DEBT_BASELINE.includes, problems.push, (axes[name] ?? '').trim, debt.includes, AXIS_FORM.test, Object.keys, gateNames.includes | 순수 판정 — 자체검사가 가짜 입력으로 부른다. |
| `scripts/check-gate-promise.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-gate-promise.mjs` | export | function | `selfTest()` | findProblems, recordKeys(crlf, 'GATE_AXIS').join, recordKeys, recordValues, findProblems(n, a, d).some, p.includes | — |
| `scripts/check-gate-promise.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-hand-counts.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-hand-counts.mjs` | internal | const | `HAND_COUNT_RE` | — | — |
| `scripts/check-hand-counts.mjs` | internal | const | `HIST_OPT_OUT` | — | 줄 단위 옵트아웃. 과거 기록 한 줄에 붙인다(§3 EXCLUDE 규율 — 근거를 곁에 쓴다). |
| `scripts/check-hand-counts.mjs` | export | function | `stripMarkers(text)` | text.replace | 마커(<!--reg:...-->값<!--/reg-->)를 지운 본문. 마커로 심은 값은 이미 대조되므로 검사 대상이 아니다. |
| `scripts/check-hand-counts.mjs` | export | function | `findHandCounts(text)` | String(text).split, String, raw.includes, stripMarkers(raw).matchAll, stripMarkers, out.push, m[0].trim | 손편집 게이트 개수 위반 목록(순수 — 자체검사가 직접 부른다). |
| `scripts/check-hand-counts.mjs` | export | function | `handoffSummary(text)` | text.indexOf, text.slice, rest.search, rest.slice | HANDOFF에서 '인계 요약' 블록만 잘라낸다(그 아래 Phase 기록은 역사라 검사 제외). |
| `scripts/check-hand-counts.mjs` | internal | const | `targets` | — | 검사 대상: 지금 상태를 지시하는 문서들. |
| `scripts/check-hand-counts.mjs` | internal | const | `handoffPath` | — | — |
| `scripts/check-hand-counts.mjs` | internal | const | `handoffRaw` | — | — |
| `scripts/check-hand-counts.mjs` | internal | const | `summary` | — | — |
| `scripts/check-hand-counts.mjs` | internal | const | `skillsDir` | — | — |
| `scripts/check-hand-counts.mjs` | internal | const | `problems` | — | — |
| `scripts/check-hooks-wired.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-hooks-wired.mjs` | internal | const | `SETTINGS` | — | — |
| `scripts/check-hooks-wired.mjs` | internal | const | `REQUIRED` | — | 필수 훅 — 이유 없이 빼면 결함이다(§7). 새 훅을 더하면 여기에도 등록한다. |
| `scripts/check-hooks-wired.mjs` | export | function | `collectHooks(settings)` | Object.entries, Array.isArray, out.push, String | settings.json에서 (event, matcher, command) 삼중항을 전부 뽑는다. |
| `scripts/check-hooks-wired.mjs` | export | function | `findMissing(hooks, required = REQUIRED)` | required.filter, hooks.some, h.matcher.includes, h.command.includes | 필수 훅이 전부 등록됐는지. 빠진 것들을 반환(빈 배열이면 통과). |
| `scripts/check-hooks-wired.mjs` | internal | const | `settings` | — | — |
| `scripts/check-hooks-wired.mjs` | internal | const | `hooks` | — | — |
| `scripts/check-hooks-wired.mjs` | internal | const | `bad` | — | — |
| `scripts/check-hooks-wired.mjs` | internal | const | `{ execFileSync }` | — | — |
| `scripts/check-input-closeout.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-input-closeout.mjs` | internal | const | `PROFILE` | — | — |
| `scripts/check-input-closeout.mjs` | internal | const | `WRITER_RE` | — | 재생성 writer로 인정하는 이름꼴. 디렉터리가 모집단의 출처다(손목록 금지). |
| `scripts/check-input-closeout.mjs` | internal | const | `WRITE_HINT` | — | check 명령에 쓰기가 섞였는가 . 마감은 writer를 실행하지 않는다(HRL-17). 🔴 `gen-` 자체도 막는다 — `node scripts/gen-adapters.mjs --check` 같은 형태로 writer를 「검사 모드」라 부르며 부르는 길을 열면, 그 플래그가 언젠가 조용히 쓰기로 돌아온다. |
| `scripts/check-input-closeout.mjs` | export | function | `discoverWriters(dir)` | readdirSync(dir).filter((f) => WRITER_RE.test(f)).sort, readdirSync(dir).filter, readdirSync, WRITER_RE.test | — |
| `scripts/check-input-closeout.mjs` | export | function | `auditLedger(writers, ledger)` | Array.isArray, errors.push, listed.has, listed.set, listed.keys, writers.includes, writer.replace, auditEntry | 원장이 온전한지 판정한다 — 순수 함수(자체검사가 부른다). @param writers 디렉터리에서 뽑은 실제 writer 파일명 목록 @param ledger 프로필의 regenerationLedger 항목 배열 |
| `scripts/check-input-closeout.mjs` | internal | function | `auditEntry(writer, entry)` | entry.check.trim, entry.excludedReason.trim, errors.push, WRITE_HINT.test | 항목 하나의 계약: check가 있거나, 이유를 적은 제외이거나. 이유 없는 제외는 결함이다(§7). |
| `scripts/check-input-closeout.mjs` | internal | function | `fingerprint()` | git | 작업트리 지문 — 추적 파일은 내용 해시 로, 미추적 파일은 이름 으로 본다. 🔴 미추적을 이름만 보는 것은 의도적이다: 마감이 새 파일을 만들어 놓고 조용히 넘어가는 것을 잡으면 충분하고, 로그·캐시 내용까지 해시하면 검사가 자기 소음에 걸린다. |
| `scripts/check-input-closeout.mjs` | internal | function | `git(args)` | execFileSync | — |
| `scripts/check-input-closeout.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-input-closeout.mjs` | export | function | `selfTest()` | auditLedger, errs.some, e.includes | — |
| `scripts/check-input-closeout.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-input-closeout.mjs` | internal | function | `runCloseout(checks)` | fingerprint, entry.check.split, execFileSync, failures.push, fail | 마감 실행 — 읽기 전용 check를 돌리고 전후 트리 지문 으로 「안 건드렸다」를 검증한다. |
| `scripts/check-input-closeout.mjs` | internal | function | `fail(errors)` | console.error, process.exit | — |
| `scripts/check-instant-normalization.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-instant-normalization.mjs` | internal | const | `DOMAIN` | — | — |
| `scripts/check-instant-normalization.mjs` | export | function | `fromRowFunctions(source)` | re.exec, source.slice, rest.search, out.push, m[3].trim, rest.slice | 서버 행 → 로컬 행 변환 함수를 찾아 (이름, 반환형, 본문)을 뽑는다. `from…Row(r: XRow): T {` 형태만 본다 — snake_case 행을 받는 것이 곧 "서버 경계"의 정의다. |
| `scripts/check-instant-normalization.mjs` | export | function | `violationsIn(source, rel)` | fromRowFunctions, /\bWithInstants</.test, bad.push, fn.body.matchAll, WRAPPED.test, fn.body.slice | 한 파일의 위반 목록. 반환형과 본문 두 가지를 본다(하나만 보면 우회로가 남는다). ⚠️ 컬럼 목록을 손으로 적지 않는다 (자기점검 2026-07-27에 이걸로 뚫렸다). 처음엔 `created_at`·`updated_at`·`deleted_at` 셋만 검사했다. 그래서 같은 커밋에서 `occurred_at`(순간의 … |
| `scripts/check-instant-normalization.mjs` | export | function | `backupViolations(src)` | [...src.matchAll(/incoming\.(\w+)/g)].map, src.matchAll, tables // `incoming.X.map(...)`와 `(incoming.X ?? []).map(..…, new RegExp(`incoming\\.${t}(?:\\s*\\?\\?\\s*\\[\\])?\\s*\\)… | 백업 복원이 실제로 정규화를 호출하는가. ⚠️ 처음엔 파일에 `withCanonicalStamps`라는 글자가 있는지만 봤다. 그러면 import 한 줄만 남기고 호출 4개를 전부 지워도 초록 이다 — 자기점검에서 주입해 확인했다. 게이트를 만들었다는 것과 그 자리가 지켜진다는 것은 다르다(§11). 이제 `incomi… |
| `scripts/check-instant-normalization.mjs` | export | function | `stringTimeComparisons(files)` | rel.endsWith, src.split('\n').forEach, src.split, /^\s*(\/\/\\|\*\\|\/\*)/.test, /not-an-instant/.test, LOCALE.test, bad.push, RELOP.test | 시각을 문자열로 비교하지 않는가 (논리적 충돌 점검 2026-07-27). 정규화(입구)만 고치고 비교(출구)를 두면 방어선이 하나뿐 이다. 옛 표기가 한 줄만 남아도 순서가 흔들린다. 실제로 `mergeDecision`은 `compareInstants`로 고쳤는데 타임라인 정렬만 `localeCompare`로 남아 있… |
| `scripts/check-instant-normalization.mjs` | internal | function | `walk(dir)` | readdirSync, join, statSync(p).isDirectory, statSync, out.push, walk, p.endsWith | — |
| `scripts/check-instant-normalization.mjs` | internal | const | `files` | — | — |
| `scripts/check-instant-normalization.mjs` | internal | const | `violations` | — | — |
| `scripts/check-instant-normalization.mjs` | internal | const | `fnCount` | — | — |
| `scripts/check-instant-normalization.mjs` | internal | const | `srcFiles` | — | — |
| `scripts/check-instant-normalization.mjs` | internal | const | `merge` | — | — |
| `scripts/check-lazy-screens.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-lazy-screens.mjs` | internal | const | `CORE_SCREENS` | — | 첫 로드에 있어도 되는 화면. 라우터가 경로에 따라 즉시 그리는 두 화면뿐이다. |
| `scripts/check-lazy-screens.mjs` | export | function | `staticRelativeImports(source)` | source.matchAll, /^\s+type\s/.test, clause.match, braced[1] .split(',') .map((s) => s.trim()) .filter, braced[1] .split(',') .map, braced[1] .split, s.trim, names.every, /^type\s/.test, out.push | 정적(런타임에 남는) 상대 import 명세자만 뽑는다 — 순수 함수라 자체검사가 직접 부른다. 제외: `import type ...`(빌드에서 지워짐) · 중괄호 안이 전부 `type X`인 import · `import()`(동적). |
| `scripts/check-lazy-screens.mjs` | internal | function | `resolveTs(fromFile, spec)` | resolve, dirname, join, existsSync, cand.endsWith | fromFile 기준 상대 명세자를 실제 .ts 파일 경로로. 없으면 null. |
| `scripts/check-lazy-screens.mjs` | export | function | `reachableFrom(entry, readFile = (p) => readFileSync(p, 'utf8'))` | readFileSync, stack.pop, relative, seen.has, seen.add, readFile, staticRelativeImports, resolveTs, stack.push | entry에서 정적으로 도달하는 .ts 파일 집합(ROOT 상대 경로). |
| `scripts/check-lazy-screens.mjs` | internal | const | `reachable` | — | — |
| `scripts/check-lazy-screens.mjs` | internal | const | `SCREEN_DIR` | — | — |
| `scripts/check-lazy-screens.mjs` | internal | const | `allScreens` | — | — |
| `scripts/check-lazy-screens.mjs` | internal | function | `isLeak(rel)` | rel.startsWith, CORE_SCREENS.includes, rel.slice | 첫 로드에 있으면 안 되는 파일: 보조 화면 + 진단 패널(무거운 판정 로직). |
| `scripts/check-lazy-screens.mjs` | internal | const | `leaked` | — | — |
| `scripts/check-lazy-screens.mjs` | internal | const | `missingCore` | — | — |
| `scripts/check-lazy-screens.mjs` | internal | const | `lazyCount` | — | — |
| `scripts/check-live-coverage.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-live-coverage.mjs` | internal | const | `UI_DIRS` | — | — |
| `scripts/check-live-coverage.mjs` | internal | const | `SCRIPTS` | — | — |
| `scripts/check-live-coverage.mjs` | internal | const | `NO_LIVE_REQUIRED` | — | 라이브 검사가 없어도 되는 화면 — 이유를 반드시 적는다 (비면 규율이 죽는다). 여기 한 줄을 적는 것은 "이 화면은 사람이 눈으로 안 봐도 된다" 는 선언이다. 그러니 이유는 왜 그 화면이 틀려도 사용자가 손해를 안 보는가 를 적어야 한다. "아직 못 했다"도 정당한 사정이 될 수 있지만(§7 3항), 그건 면허가 … |
| `scripts/check-live-coverage.mjs` | internal | function | `allScreens()` | readdirSync, f.endsWith, out.push, out.sort | 화면 목록 — 손으로 세지 않는다. 디렉터리가 곧 등록부다. `blueprint.ts`의 `SCREENS`를 쓰지 않는 이유: 그건 손으로 선언한 목록 이라 화면을 만들고 등록을 빠뜨리면 여기서도 함께 빠진다 — 두 누락이 서로를 가려 준다. 디렉터리는 거짓말을 못 한다(§7 "손으로 세지 말고 등록부/디렉터리에서 뽑는… |
| `scripts/check-live-coverage.mjs` | export | function | `parseCovers(src)` | src.matchAll, m[1].split, s.trim, out.push | 라이브 스크립트가 자기가 여는 화면을 스스로 밝힌다. 선언을 별도 파일에 두지 않는 이유: 그 파일은 스크립트와 따로 낡는다(§1 SSOT). |
| `scripts/check-live-coverage.mjs` | export | function | `checkCoverage(screens, coversByScript, excused)` | screens.includes, problems.push, covered.set, covered.has, excused.has, excused.keys | 검사 본체 — 순수 함수라 셀프테스트가 직접 돌린다(§4). |
| `scripts/check-live-coverage.mjs` | internal | const | `screens` | — | — |
| `scripts/check-live-coverage.mjs` | internal | const | `coversByScript` | — | — |
| `scripts/check-live-coverage.mjs` | internal | const | `problems` | — | — |
| `scripts/check-live-coverage.mjs` | internal | const | `coveredCount` | — | — |
| `scripts/check-live-sleep.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-live-sleep.mjs` | export | const | `SLEEP_BASELINE` | — | 🔴 동결 기준선 — 숫자를 올리지 마라. 줄일 때만 고친다. 값은 T-018 완료 상태다. 편집기 라이브 게이트는 모든 고정 대기를 실제 완료 조건으로 바꿨다. |
| `scripts/check-live-sleep.mjs` | export | function | `countSleeps(source)` | source .split('\n') .filter((l) => !l.trim().startsWith('//…, source .split('\n') .filter, source .split, l.trim().startsWith, l.trim, l.match | 소스에서 고정 대기 호출부 수를 센다. 주석 속 글자는 세지 않는다(오탐은 틀린 게이트다 · §11 ③). |
| `scripts/check-live-sleep.mjs` | export | function | `findProblems(counts, baseline = SLEEP_BASELINE)` | Object.entries, problems.push, Object.keys | 순수 판정 — 자체검사가 가짜 입력으로 부른다. |
| `scripts/check-live-sleep.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-live-sleep.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-migration-grants.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-migration-grants.mjs` | internal | const | `DIR` | — | — |
| `scripts/check-migration-grants.mjs` | export | const | `NO_GRANT_REQUIRED` | — | 이 규칙에서 빠지는 테이블과 그 이유 . 이유 없는 제외는 결함이다(CLAUDE.md §7). 비면 규율이 죽으므로, 새로 넣을 때는 반드시 한 줄 근거를 함께 쓴다. |
| `scripts/check-migration-grants.mjs` | export | function | `stripSql(src)` | src.replace(/--[^\n]*/g, '').replace, src.replace | 주석을 걷어낸다 — 주석 속 예시 SQL을 실제 문장으로 읽지 않게. |
| `scripts/check-migration-grants.mjs` | export | function | `createdTables(sql)` | [...stripSql(sql).matchAll(/create\s+table\s+(?:if\s+not\s+…, stripSql(sql).matchAll, stripSql, m[1].toLowerCase | `create table [if not exists] journey.X` 에서 X들을 뽑는다. |
| `scripts/check-migration-grants.mjs` | export | function | `hasGrant(allSql, table)` | re.test | 이 테이블에 `authenticated` 권한을 주는 grant가 어디엔가 있는가. |
| `scripts/check-migration-grants.mjs` | export | function | `policiesGateInvite(allSql, table)` | stripSql(allSql).matchAll, stripSql, /is_allowed\s*\(/i.test, bad.push | 이 테이블의 정책들이 초대제(`is_allowed()`)를 결합하는가. 정책이 하나라도 초대제 없이 남아 있으면 허용목록 밖 사용자가 그 경로로 들어온다. |
| `scripts/check-migration-grants.mjs` | export | function | `deleteOpsAreGranted(srcFiles, allSql)` | srcFiles.map(([, t]) => t).join, srcFiles.map, /\.delete\(\)/.test, stripSql(allSql) .split(';') .map((x) => x.trim().replace(/…, stripSql(allSql) .split(';') .map, stripSql(allSql) .split, stripSql, x.trim().replace, x.trim, stmts.some | 코드가 하는 연산을 서버가 허락하는가 (M-0024, 2026-07-26). ADR-0030에서 영구삭제를 "tombstone 유지" → "행 하드 삭제"로 바꿨는데, 서버에 DELETE 권한도 정책도 주지 않았다. `pushPurges`는 원장 기록(INSERT)까지 성공하고 그 다음 삭제에서 막혔다 — 사용자 화면엔… |
| `scripts/check-migration-grants.mjs` | export | function | `auditMigrations(files)` | files.map(([, s]) => s).join, files.map, createdTables, seen.has, seen.add, hasGrant, problems.push, policiesGateInvite, /is_allowed\s*\(/i.test, [...stripSql(allSql).matchAll(new RegExp(`create\\s+policy[… | — |
| `scripts/check-migration-grants.mjs` | export | function | `latestPolicyDefs(files)` | stripSql(src).matchAll, stripSql, out.set, m[2].toLowerCase, m[1].toLowerCase | 정책의 최종 정의 만 모은다. 마이그레이션은 추가 전용이라 옛 정의가 파일에 그대로 남아 있다 — 전체를 훑어 위반을 세면 역사를 결함으로 신고하는 오탐 이 된다(§2-B ③). 같은 이름의 정책이 여러 번 재정의되면 파일 순서상 마지막 것이 서버의 현실 이므로 그것만 판정 대상이다. |
| `scripts/check-migration-grants.mjs` | internal | function | `bareCallRemains(body, callRe)` | new RegExp(callRe, 'i').test, body.replace | 스칼라 서브쿼리로 감싼 호출을 지운 뒤에도 맨 호출 이 남는가. |
| `scripts/check-migration-grants.mjs` | export | function | `auditPolicyShape(files)` | latestPolicyDefs, bareCallRemains, problems.push, /is_allowed\s*\(/i.test, /\bto\s+authenticated\b/i.test | 정책 선언의 모양 을 잠근다(2026-07-27 건강진단 · advisor `auth_rls_initplan` 18건). 세 가지를 본다. 셋 다 "지금은 무해한데 조용히 어긋나는" 부류라 사람 눈으로는 안 잡힌다: ① `auth.uid()`·`is_allowed()`를 행마다 재평가하지 않는가. 둘 다 STABLE이므… |
| `scripts/check-migration-grants.mjs` | export | function | `auditDefinerSearchPath(files)` | stripSql, clean.matchAll, state.set, m[1].toLowerCase, /security\s+definer/i.test, head.match, state.get, Object.assign, problems.push | `SECURITY DEFINER` 함수의 search_path가 빈 문자열로 고정돼 있는가 (헌장 §2 불변식 #5). 이 규칙은 헌장에 처음부터 적혀 있었다. 그런데 `block_purged_reinsert()`(0012)와 `unpurge_ids()`(0017)는 `journey, public`으로 태어났고, 202… |
| `scripts/check-migration-grants.mjs` | internal | const | `POLICY_GOOD` | — | 계약을 전부 지키는 정책 표본. 셀프테스트 기준은 살아 있는 파일이 아니라 인라인 표본 이다 — 실제 파일을 기준으로 삼으면 그 파일이 바뀔 때 셀프테스트가 조용히 뜻을 바꾼다. |
| `scripts/check-migration-grants.mjs` | internal | const | `selfTestCount` | — | — |
| `scripts/check-migration-grants.mjs` | internal | const | `files` | — | — |
| `scripts/check-migration-grants.mjs` | internal | const | `srcFiles` | — | — |
| `scripts/check-migration-grants.mjs` | internal | const | `problems` | — | — |
| `scripts/check-module-design-docs.mjs` | export | function | `findDrift(expected, actual)` | actual.has, problems.push, actual.get, actual.keys, expected.has | — |
| `scripts/check-module-design-docs.mjs` | internal | const | `collected` | — | — |
| `scripts/check-module-design-docs.mjs` | internal | const | `expected` | — | — |
| `scripts/check-module-design-docs.mjs` | internal | const | `actual` | — | — |
| `scripts/check-module-design-docs.mjs` | internal | const | `problems` | — | — |
| `scripts/check-no-synthetic-italic.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-no-synthetic-italic.mjs` | internal | const | `CSS_DIR` | — | — |
| `scripts/check-no-synthetic-italic.mjs` | export | const | `ALLOWED` | — | 근거 있는 예외. 비면 규율이 죽는 게 아니라 비어 있는 것이 정상 이다 — 이 앱의 화면 문자열은 전부 한글이므로 지금은 예외가 없다. (라틴 전용 요소가 생기면 셀렉터와 이유를 여기 적는다.) |
| `scripts/check-no-synthetic-italic.mjs` | export | function | `findItalics(css)` | css.replace, noComments.split, line.match, (sel[1] ?? '').trim, /font-style\s*:\s*(italic\\|oblique)/i.test, out.push, line.trim, out.filter | `font-style: italic\\|oblique` 선언을 찾는다. 주석 안은 세지 않는다. |
| `scripts/check-no-synthetic-italic.mjs` | internal | const | `selfTestCount` | — | — |
| `scripts/check-no-synthetic-italic.mjs` | internal | const | `files` | — | — |
| `scripts/check-no-synthetic-italic.mjs` | internal | const | `problems` | — | — |
| `scripts/check-node-version.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-node-version.mjs` | export | function | `hardcodedNodeVersions(yml)` | [...yml.replace(/^\s*#.*$/gm, '').matchAll(/node-version:\s…, yml.replace(/^\s*#.*$/gm, '').matchAll, yml.replace | 워크플로 본문에서 하드코딩된 node-version을 찾는다(주석은 제외). |
| `scripts/check-node-version.mjs` | export | function | `usesVersionFile(yml)` | /node-version-file:\s*['"]?\.nvmrc/.test | 워크플로가 `.nvmrc`를 참조하는가. |
| `scripts/check-node-version.mjs` | export | function | `engineMajor(range)` | /^>=\s*(\d+)/.exec, String(range ?? '').trim, String | `>=22` / `>=20.19` 같은 하한에서 메이저만 뽑는다. 형식이 낯설면 null. |
| `scripts/check-node-version.mjs` | export | function | `audit({ nvmrc, engines, workflows })` | problems.push, /^\d+$/.test, nvmrc.trim, engineMajor, /setup-node/.test, hardcodedNodeVersions, hard.join, usesVersionFile | — |
| `scripts/check-node-version.mjs` | internal | const | `selfTestCount` | — | — |
| `scripts/check-node-version.mjs` | internal | const | `WF_DIR` | — | — |
| `scripts/check-node-version.mjs` | internal | const | `workflows` | — | — |
| `scripts/check-node-version.mjs` | internal | const | `nvmrcPath` | — | — |
| `scripts/check-node-version.mjs` | internal | const | `pkg` | — | — |
| `scripts/check-node-version.mjs` | internal | const | `problems` | — | — |
| `scripts/check-page-size-parity.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-page-size-parity.mjs` | export | function | `readNumericConst(src, name)` | src.match, Number | `const NAME = 123;` 형태에서 숫자를 뽑는다(export 여부 무관). |
| `scripts/check-page-size-parity.mjs` | export | function | `comparePageSizes(real, assumed)` | problems.push | 순수 검사 — 셀프테스트가 가짜 입력으로 직접 돌린다. |
| `scripts/check-page-size-parity.mjs` | internal | const | `selfCases` | — | — |
| `scripts/check-page-size-parity.mjs` | internal | const | `brokenSelf` | — | — |
| `scripts/check-page-size-parity.mjs` | internal | const | `real` | — | — |
| `scripts/check-page-size-parity.mjs` | internal | const | `assumed` | — | — |
| `scripts/check-page-size-parity.mjs` | internal | const | `problems` | — | — |
| `scripts/check-platform-map.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-platform-map.mjs` | internal | const | `OUT` | — | — |
| `scripts/check-platform-map.mjs` | internal | const | `expected` | — | — |
| `scripts/check-platform-map.mjs` | internal | const | `actual` | — | — |
| `scripts/check-production-artifacts.mjs` | export | function | `productionArtifactProblems(viteSource, distFiles = [])` | /build\s*:\s*\{[\s\S]*?sourcemap\s*:\s*false/.test, problems.push, /\.map$/i.test | — |
| `scripts/check-production-artifacts.mjs` | internal | function | `filesBelow(dir, prefix = '')` | existsSync, readdirSync, entry.isDirectory, out.push, filesBelow, join | — |
| `scripts/check-production-artifacts.mjs` | internal | const | `good` | — | — |
| `scripts/check-production-artifacts.mjs` | internal | const | `bad` | — | — |
| `scripts/check-production-artifacts.mjs` | internal | const | `selfTests` | — | — |
| `scripts/check-production-artifacts.mjs` | internal | const | `failed` | — | — |
| `scripts/check-production-artifacts.mjs` | internal | const | `problems` | — | — |
| `scripts/check-purge-scope.mjs` | internal | const | `PURGE` | — | — |
| `scripts/check-purge-scope.mjs` | internal | const | `MIGRATIONS` | — | — |
| `scripts/check-purge-scope.mjs` | internal | function const | `rowmapPath(domain)` | — | — |
| `scripts/check-purge-scope.mjs` | internal | const | `PARENT_KEY` | — | 부모 → 자식 테이블에서 그 부모를 가리키는 컬럼. purge.ts의 `PARENT_KEY`와 같아야 한다. |
| `scripts/check-purge-scope.mjs` | internal | const | `PARENTS` | — | — |
| `scripts/check-purge-scope.mjs` | internal | function | `domainBlocks(src)` | src.slice, src.indexOf, re.exec, keys.push, keys.map, body.slice | DOMAIN_PURGE 객체 본문을 도메인 블록으로 쪼갠다. |
| `scripts/check-purge-scope.mjs` | export | function | `parseCascadeParents(src)` | domainBlocks, block.match, [...m[1].matchAll(/'(\w+)'/g)].map, m[1].matchAll | 각 도메인의 `cascadeParents` 선언을 뽑는다. |
| `scripts/check-purge-scope.mjs` | export | function | `parseRemoteTables(src)` | domainBlocks, block.match | 각 도메인의 서버 테이블 이름. migration의 테이블 이름과 도메인을 잇는 유일한 다리다. |
| `scripts/check-purge-scope.mjs` | export | function | `parseCascadeEdges(sqlFiles)` | sql.replace, re.exec, starts.push, lower.slice, rf.exec, edges.push | migration SQL에서 `journey.X → journey.Y on delete cascade` 직접 간선을 뽑는다. `create table journey.X (` 블록 안의 `references journey.Y (…) on delete cascade`를 본다. `auth.users` 참조는 대상이 아니다(사… |
| `scripts/check-purge-scope.mjs` | export | function | `ancestorsOf(table, edges, seen = new Set())` | edges.filter, seen.has, seen.add, ancestorsOf | 간선의 전이 폐포 — 「나를 데려가는 모든 조상 테이블」. 여행→순간→사진이 두 단계다. |
| `scripts/check-purge-scope.mjs` | export | function | `rowmapHasColumn(domain, column)` | new RegExp(`\\b${column}\\s*:`).test, readFileSync, rowmapPath | rowmap 파일이 서버 행 타입에 그 컬럼을 선언하는가. |
| `scripts/check-purge-scope.mjs` | export | function | `scopeProblems(declared, remoteTables, edges, hasColumnFn)` | Object.entries, problems.push, ancestorsOf, PARENTS.filter((p) => anc.has(PARENT_KEY[p].table)).sort, PARENTS.filter, anc.has, [...parents].sort, fromSchema.join, fromCode.join, hasColumnFn | 불일치 목록을 돌려준다(비어 있으면 통과). |
| `scripts/check-purge-scope.mjs` | internal | function | `selfTest()` | cols[d].includes, scopeProblems, ancestorsOf('media', edges).has, ancestorsOf, Object.keys, parseCascadeParents, readFileSync, readMigrationEdges, cases.filter, fn | — |
| `scripts/check-purge-scope.mjs` | internal | function | `readMigrationEdges()` | readdirSync(MIGRATIONS) .filter((f) => f.endsWith('.sql')) …, readdirSync(MIGRATIONS) .filter, readdirSync, f.endsWith, readFileSync, parseCascadeEdges | — |
| `scripts/check-purge-scope.mjs` | internal | const | `src` | — | — |
| `scripts/check-purge-scope.mjs` | internal | const | `declared` | — | — |
| `scripts/check-purge-scope.mjs` | internal | const | `problems` | — | — |
| `scripts/check-real-coord.mjs` | internal | const | `HOME_FILE` | — | — |
| `scripts/check-real-coord.mjs` | internal | const | `COORD_ZERO` | — | — |
| `scripts/check-real-coord.mjs` | export | function | `coordZeroLines(text)` | text.split, line.trimStart, trimmed.startsWith, COORD_ZERO.exec, /lat/i.test, /lng/i.test, out.push, line.trim | 한 소스에서 좌표 0,0 리터럴을 쓰는 줄들을 찾는다(주석 줄은 제외). |
| `scripts/check-real-coord.mjs` | internal | function | `selfTest()` | coordZeroLines, cases.filter, fn, console.error, process.exit | — |
| `scripts/check-real-coord.mjs` | internal | const | `files` | — | — |
| `scripts/check-real-coord.mjs` | internal | const | `problems` | — | — |
| `scripts/check-recovery-path.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-recovery-path.mjs` | internal | const | `ROUTES` | — | — |
| `scripts/check-recovery-path.mjs` | export | function | `globToRe(glob)` | '.+^${}()\\|[]\\'.includes | glob 하나를 정규식으로. ` `는 경로 구분자를 넘고, ` `는 안 넘는다. 🔴 첫 판은 치환을 이어 붙여 만들었다가 `supabase/migrations/` 밑 파일을 하나도 못 잡았다 (끝의 ` `를 「디렉터리 경계」로만 읽었다). 자체검사가 그 자리에서 잡았다 — 갈래 판정기가 조용히 두 갈래짜리가 될 뻔한 … |
| `scripts/check-recovery-path.mjs` | export | function | `routeFor(path, classes)` | globToRe(glob).test, globToRe | 경로 하나의 수습 경로를 정한다 — 순수 함수(자체검사가 부른다). 순서가 곧 우선순위다: userData가 먼저 걸리면 나머지는 보지 않는다(위 🔴). |
| `scripts/check-recovery-path.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-recovery-path.mjs` | internal | const | `CLASSES` | — | — |
| `scripts/check-recovery-path.mjs` | export | function | `selfTest()` | routeFor | — |
| `scripts/check-recovery-path.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `OUT` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `expected` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `actual` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `reg` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `gatesTs` | — | — |
| `scripts/check-registry-gen.mjs` | export | function | `recordKeys(source, constName)` | source.search, source.indexOf, source.slice, [...body.matchAll(/^\s*(?:'([^']+)'\\|([A-Za-z_$][\w$]*))\s*…, body.matchAll | gates.ts의 한 Record 리터럴에서 키를 뽑는다(순수 — 자체검사가 직접 부른다). 따옴표 키는 따옴표 안 전체 를 받는다. `[\w-]+`로 좁히면 ASCII 밖 키가 조용히 안 뽑히고, 안 뽑힌 키는 "없는 게이트가 남아 있다" 검사를 통과해 버린다(주입해 보고 발견한 구멍). 🔴 이름은 경계까지 맞춘다.… |
| `scripts/check-registry-gen.mjs` | internal | const | `described` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `categorized` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `metaProblems` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `agentsTs` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `agentDescribed` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `agentCategorized` | — | — |
| `scripts/check-registry-gen.mjs` | internal | const | `agentProblems` | — | — |
| `scripts/check-report-fields.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-report-fields.mjs` | internal | function const | `read(rel)` | existsSync, join, readFileSync | — |
| `scripts/check-report-fields.mjs` | export | const | `REPORTS` | — | 검사할 보고용 구조체 등록부. "보고용"이란 사용자에게 보여주려고 만든 것 이다 — 계산 중간값이나 내부 상태가 아니라. 그래서 소비처는 화면이거나, 사람이 읽는 문장을 만드는 함수다. |
| `scripts/check-report-fields.mjs` | export | const | `UNCONSUMED_OK` | — | 근거 있는 제외. 비면 규율이 죽는다 — 새로 넣을 때는 한 줄 이유를 함께 쓴다. |
| `scripts/check-report-fields.mjs` | export | function | `stripComments(src)` | src.replace(/\/\/[^\n]*/g, '').replace, src.replace | 주석을 걷어낸다 — 주석 속 필드 언급을 "소비"로 세지 않게. |
| `scripts/check-report-fields.mjs` | export | function | `fieldsOf(src, name)` | stripComments(src).match, stripComments, body.split, line.trim, t.match, fields.push, (f[2] ?? '').match, (inline[1] ?? '').split, part.trim().match, part.trim | `export interface X { … }` 본문에서 필드 이름을 뽑는다. 한 줄짜리 중첩 객체의 안쪽까지 센다. ⚠️ 처음엔 "중첩 객체는 바깥 필드만 센다 — 그 안쪽은 바깥이 소비되면 따라온다" 고 가정했다. 그 가정은 2026-07-26에 틀린 것으로 드러났다. 기기 이름표를 공용 것으로 바꾸자 `EnvRep… |
| `scripts/check-report-fields.mjs` | export | function | `isConsumed(consumerSrc, field)` | stripComments, field.includes, field.slice, field.lastIndexOf, new RegExp(`\\.${field}\\b`).test, new RegExp(`\\{[^}]*\\b${field}\\b[^}]*\\}\\s*=`).test | 소비처 어딘가에서 `.field`로 읽거나 구조분해로 꺼내는가. |
| `scripts/check-report-fields.mjs` | export | function | `audit(reports, readFn)` | readFn, fieldsOf, problems.push, r.consumers.map(readFn).join, r.consumers.map, isConsumed | — |
| `scripts/check-report-fields.mjs` | internal | const | `selfTestCount` | — | — |
| `scripts/check-report-fields.mjs` | internal | const | `problems` | — | — |
| `scripts/check-review-tier.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-review-tier.mjs` | export | const | `SIGNALS` | — | 리뷰를 부르는 신호. 이름을 남기는 이유: 「무엇 때문에 필수인지」를 사람이 알아야 한다. |
| `scripts/check-review-tier.mjs` | internal | const | `COSMETIC` | — | 면제가 가능한 「보기만 바꾸는」 변경. 신호가 0건일 때만 본다. |
| `scripts/check-review-tier.mjs` | internal | const | `SAYS_NOTHING` | — | 빈 줄·괄호만 있는 줄 — 판정에 아무 말도 보태지 않는다. |
| `scripts/check-review-tier.mjs` | export | function | `tierFor(files, uiMatch)` | files.filter, uiMatch, f.lines.filter, (f.generatedLines ?? []).includes, SAYS_NOTHING.test, re.test, found.add, COSMETIC.test | 변경 줄 묶음에서 티어를 판정한다 — 순수 함수(자체검사가 부른다). @param files [{ path, lines: string[], generatedLines: string[] }] |
| `scripts/check-review-tier.mjs` | export | function | `parseDiff(text)` | text.split, raw.startsWith, raw.slice(4).trim, raw.slice, rest.startsWith, rest.slice, files.push, cur.lines.push | unified diff에서 파일별 추가/변경 줄을 뽑는다. 🔴 모르는 헤더를 만나면 던진다 — 조용히 직전 파일에 붙이지 않는다. |
| `scripts/check-review-tier.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-review-tier.mjs` | internal | function const | `isUi(p)` | /^src\/ui\/.*\.(ts\\|css)$/.test | — |
| `scripts/check-review-tier.mjs` | export | function | `selfTest()` | tierFor, t, parseDiff | — |
| `scripts/check-review-tier.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-schema-parity.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-schema-parity.mjs` | internal | const | `MIG_DIR` | — | — |
| `scripts/check-schema-parity.mjs` | internal | const | `DOMAIN_DIR` | — | — |
| `scripts/check-schema-parity.mjs` | internal | const | `ROW_TO_TABLE` | — | — |
| `scripts/check-schema-parity.mjs` | internal | const | `SQL_KEYWORDS` | — | — |
| `scripts/check-schema-parity.mjs` | internal | function | `parseServerColumns()` | (tables[t] ??= new Set()).add, readdirSync(MIG_DIR).filter((f) => f.endsWith('.sql')).sort, readdirSync(MIG_DIR).filter, readdirSync, f.endsWith, readFileSync, join, createRe.exec, m[2].split, raw.trim().replace | 마이그레이션 SQL 전체에서 journey.<table> → Set(컬럼명) 을 만든다. |
| `scripts/check-schema-parity.mjs` | internal | function | `parseRowmapInterfaces()` | readdirSync, join, existsSync, readFileSync, ifaceRe.exec, m[2].split, raw.trim().match, raw.trim, fields.push, found.push | 각 rowmap의 `interface XRow { ... }` 필드명을 뽑는다. |
| `scripts/check-schema-parity.mjs` | internal | function | `findDrift(server, rowmaps)` | violations.push, cols.has | 대조: rowmap 필드가 서버 컬럼의 부분집합인지. 위반 목록 반환. |
| `scripts/check-schema-parity.mjs` | internal | function | `selfTest(server)` | findDrift, v.some, x.includes | — |
| `scripts/check-schema-parity.mjs` | internal | const | `server` | — | — |
| `scripts/check-schema-parity.mjs` | internal | const | `rowmaps` | — | — |
| `scripts/check-schema-parity.mjs` | internal | const | `violations` | — | — |
| `scripts/check-schema-parity.mjs` | internal | const | `summary` | — | — |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `ENTRY` | — | — |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `ROUTER` | — | — |
| `scripts/check-screen-lifecycle.mjs` | export | function | `routeNames(source, fileName = 'router.ts')` | ts.createSourceFile, ts.isVariableDeclaration, ts.isIdentifier, ts.isAsExpression, ts.isTypeAssertionExpression, ts.isObjectLiteralExpression, init.properties .filter((p) => ts.isPropertyAssignment(p)) …, init.properties .filter, ts.isPropertyAssignment, ts.isStringLiteral | 라우트 모집단을 `router.ts`의 `ROUTE_SEGMENTS` 객체 리터럴에서 뽑는다. 🔴 정규식이 아니라 AST로 읽는다 — 주석에 적힌 옛 라우트 이름(`'map'`·`'settings'`)이 이 파일에 그대로 남아 있어, 문자열 검색이면 주석을 모집단으로 셀 뻔했다. |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `SCREEN_CALL` | — | 화면을 그리는 호출로 보는 이름 규칙. 이 저장소의 관행(`renderHome`·`renderTripDetail`)이다. |
| `scripts/check-screen-lifecycle.mjs` | export | function | `analyzeRouter(source, fileName = 'main.ts')` | ts.createSourceFile, ts.isCallExpression, ts.isIdentifier, ts.isArrowFunction, ts.isFunctionExpression, routerBodies.push, ts.forEachChild, findRouter, sf.getLineAndCharacterOfPosition, node.getStart | 라우터 콜백 안의 화면 호출을 전부 찾아 어떻게 불렸는지 분류한다. 반환: `{ found:boolean, calls:[…], switches:[…] }` - `found` — 라우터 콜백 자체를 찾았는가(못 찾으면 판정 불가) - `mounted` — `screen.mount(...)`의 인자로 들어갔는가 - `inA… |
| `scripts/check-screen-lifecycle.mjs` | export | function | `violations({ found, calls, switches }, routes, file = ENTRY)` | Array.isArray, out.push, routes.filter, s.cases.includes, missing.map((m) => `'${m}'`).join, missing.map | 분석 결과 → 사람이 읽는 위반 목록. 순수 함수라 자체검사가 직접 먹인다. `routes`는 `router.ts`에서 파생한 라우트 전부 다. 기본값을 두지 않는다 — 기본값은 옛 전제를 못박고, 그 순간 이 검사는 「지금의 라우트」가 아니라 「내가 아는 라우트」를 잰다(§11 ②). |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `src` | — | — |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `routes` | — | — |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `analysis` | — | — |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `result` | — | — |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `{ calls }` | — | — |
| `scripts/check-screen-lifecycle.mjs` | internal | const | `asyncN` | — | — |
| `scripts/check-secret-leak.mjs` | internal | const | `SKIP_PREFIX` | — | — |
| `scripts/check-secret-leak.mjs` | internal | const | `BINARY_EXT` | — | — |
| `scripts/check-secret-leak.mjs` | internal | function | `selfTest()` | fakeJwt, 'A'.repeat, '-'.repeat, scanText, broken.push, console.error, broken.join, process.exit | — |
| `scripts/check-secret-leak.mjs` | internal | function | `trackedFiles()` | execFileSync, out.split('\0').filter, out.split | — |
| `scripts/check-secret-leak.mjs` | internal | function | `distFiles(dir = 'dist')` | existsSync, readdirSync, join, statSync(p).isDirectory, statSync, acc.push, distFiles | — |
| `scripts/check-secret-leak.mjs` | internal | const | `hits` | — | — |
| `scripts/check-secret-leak.mjs` | internal | function | `scanFile(path)` | readFileSync, scanText, console.error | — |
| `scripts/check-secret-leak.mjs` | internal | const | `targets` | — | — |
| `scripts/check-self-eval.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-self-eval.mjs` | internal | const | `FILE` | — | — |
| `scripts/check-self-eval.mjs` | export | const | `EVIDENCE_CAP` | — | 증거수준별 점수 상한 — 근거 없이 높은 점수를 못 쓰게 한다. |
| `scripts/check-self-eval.mjs` | internal | const | `W_KEYS` | — | — |
| `scripts/check-self-eval.mjs` | export | function | `parseItems(src)` | src.matchAll, src.slice, chunk.match, Number, chunk.indexOf, chunk.slice, str, items.push, num | selfEval.ts 소스에서 항목을 파싱한다(런타임 import 없이 — 게이트는 빌드 전에도 돌아야 한다). |
| `scripts/check-self-eval.mjs` | export | function | `checkItems(items)` | items.reduce, problems.push, it.gap.trim, it.w[k].trim, items.map((i) => i.n).sort, items.map, ns.join | — |
| `scripts/check-self-eval.mjs` | internal | const | `items` | — | — |
| `scripts/check-self-eval.mjs` | internal | const | `problems` | — | — |
| `scripts/check-self-eval.mjs` | internal | const | `total` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `SOURCE` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `APPROVED_COMMIT` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `EXPECTED_SKILLS` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | function | `markedBlock(text, start, end)` | text.indexOf, text.slice | — |
| `scripts/check-shared-skill-contract.mjs` | internal | function | `normalizedText(text)` | String(text \\|\\| '').replaceAll('\r\n', '\n').trim, String(text \\|\\| '').replaceAll, String | — |
| `scripts/check-shared-skill-contract.mjs` | internal | function | `jobBlock(workflow, id)` | workflow.replaceAll, workflow.indexOf, workflow.slice, rest.search, rest.slice | — |
| `scripts/check-shared-skill-contract.mjs` | internal | function | `requiredJobIds(workflow)` | workflow.replaceAll, [...normalized.matchAll(/^ ([a-zA-Z0-9_-]+):\n/gm)].map, normalized.matchAll, ids.filter((id) => jobBlock(normalized, id).includes('githu…, ids.filter, jobBlock(normalized, id).includes, jobBlock | — |
| `scripts/check-shared-skill-contract.mjs` | internal | function | `commandParts(command)` | String(command \\|\\| '').split('&&').map((part) => part.trim…, String(command \\|\\| '').split('&&').map, String(command \\|\\| '').split, String, part.trim | — |
| `scripts/check-shared-skill-contract.mjs` | export | function | `workflowProblems(workflows)` | jobBlock, fast.includes, problems.push, harness.includes, live.includes, workflows.pages.includes, workflows.apk.includes, workflows.windows.includes, workflows.supabaseGuide.includes | — |
| `scripts/check-shared-skill-contract.mjs` | export | function | `validateContract({ lock, profile, vendorHashes, adapterText, workflows, installedHashes = null, …)` | errors.push, /^[a-f0-9]{64}$/.test, String, Array.isArray, entries.map((entry) => entry.name).sort, entries.map, JSON.stringify, auditInstalled, markedBlock, normalizedText | — |
| `scripts/check-shared-skill-contract.mjs` | internal | function | `auditInstalled(tool, entry, hashes, errors, unknowns)` | unknowns.push, errors.push | 전역 설치본 한 건을 판정한다. 🔴 미설치와 변조는 다른 사실이다 (2026-08-09 · §8 「모르는 것은 확인 불가다」). 예전 판은 `null !== 해시`가 참이라는 이유만으로 설치되지 않은 것을 「해시가 lock과 다름」 이라고 보고했다. 그 문장을 읽은 사람은 변조를 의심하며 해시를 뒤지지, 설치를 하지 … |
| `scripts/check-shared-skill-contract.mjs` | internal | function | `selfTest(lock, profile, vendorHashes, adapterText, workflows)` | validateContract, normalErrors.join, '0'.repeat, x.lock.skills.pop, x.profile.groups.pop, 'f'.repeat, x.workflows.ci.replace, x.workflows.apk.replaceAll, x.workflows.windows.replace, x.profile.releaseNodes.filter | — |
| `scripts/check-shared-skill-contract.mjs` | internal | function const | `readJson(path)` | JSON.parse, readFileSync | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `lock` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `profile` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `vendorHashes` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `adapterPath` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `adapterText` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `workflows` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | function | `installedHashesAt(root)` | Object.fromEntries, lock.skills.map, join, existsSync, contentHash | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `installedHashes` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `codexSkills` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `claudeHashes` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `claudeSkills` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `globalBlock` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `globalAgents` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `statePath` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `installState` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `currentLawHosts` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `commonLawCopies` | — | — |
| `scripts/check-shared-skill-contract.mjs` | internal | const | `result` | — | — |
| `scripts/check-skill-routing.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-skill-routing.mjs` | internal | function | `walk(dir, ext, out = [])` | existsSync, readdirSync, join, statSync(p).isDirectory, statSync, walk, ext.test, out.push, routePath, relative | — |
| `scripts/check-skill-routing.mjs` | internal | const | `SCAN` | — | 훑는 영역. `src/`만 보던 것을 넓혔다 (2026-07-27 배포 중 실물로 드러남). `.github/workflows/`를 고치는데 `npm run brief`가 "먼저 정독할 스킬 문서"를 하나도 주지 않았다. 라우팅 표에 그 경로가 없었고, 이 게이트는 `src/`만 훑었으므로 누락이 조용히 통과 했다 — … |
| `scripts/check-skill-routing.mjs` | export | function | `unrouted(paths, routes, excluded)` | paths .map(routePath) .filter, paths .map, excluded.has, routes.some, r.match.test | — |
| `scripts/check-skill-routing.mjs` | export | function | `orphanSkills(routes, available)` | routes.map, available.filter, used.has | — |
| `scripts/check-skill-routing.mjs` | export | function | `deadLinks(routes, available)` | [...new Set(routes.map((r) => r.skill))].filter, routes.map, have.has | — |
| `scripts/check-skill-routing.mjs` | internal | const | `selfTestCount` | — | — |
| `scripts/check-skill-routing.mjs` | internal | const | `skillsDir` | — | — |
| `scripts/check-skill-routing.mjs` | internal | const | `available` | — | — |
| `scripts/check-skill-routing.mjs` | internal | const | `problems` | — | — |
| `scripts/check-skill-routing.mjs` | internal | const | `files` | — | — |
| `scripts/check-step-duration.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-step-duration.mjs` | internal | const | `MIN_SAMPLES` | — | — |
| `scripts/check-step-duration.mjs` | export | function | `thresholdOf(baselineSec)` | Math.max, Math.round | 판정 임계 — 이 식이 정본이다. 프로필의 thresholdSec은 이 식의 캐시 이고, 어긋나면 실패다. |
| `scripts/check-step-duration.mjs` | export | function | `auditBaselines(table)` | Array.isArray, problems.push, thresholdOf, Math.min, Math.max, Object.keys | 기준선 표가 판정에 쓸 만한가 — 순수 함수(자체검사가 부른다). |
| `scripts/check-step-duration.mjs` | export | function | `judgeRun(table, jobsPayload)` | table.steps.map, (job?.name ?? '').split, byStep.get, Math.round, Date.parse, verdicts.push | 실제 실행 판정 — jobs 응답에서 단계 소요를 뽑아 기준선과 견준다. |
| `scripts/check-step-duration.mjs` | internal | const | `SELF` | — | — |
| `scripts/check-step-duration.mjs` | internal | const | `GOOD` | — | — |
| `scripts/check-step-duration.mjs` | export | function | `selfTest()` | auditBaselines, JSON.parse, JSON.stringify, thresholdOf, clone, mutate, auditBaselines(t).some, p.includes, judgeRun | — |
| `scripts/check-step-duration.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-sw.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-sw.mjs` | internal | const | `SW` | — | — |
| `scripts/check-sw.mjs` | internal | const | `MAIN` | — | — |
| `scripts/check-sw.mjs` | export | function | `codeOnly(source)` | source.replace(/\/\*[\s\S]*?\*\//g, ' ').replace, source.replace | 주석만 걷어낸 코드. 문자열은 남긴다 — 여기서 찾는 증거(호스트 이름·'GET')가 바로 문자열 리터럴이기 때문이다. 문자열까지 지웠다가 호스트 검사가 통째로 공허해지는 것을 자체검사가 잡았다(이 게이트를 만들며 실제로 낸 실수). 오탐의 원인은 문자열이 아니라 규칙을 서술하는 머리주석 이므로 그쪽만 지운다. |
| `scripts/check-sw.mjs` | export | function | `swViolations(source, externalHosts)` | codeOnly, /url\.origin\s*!==\s*self\.location\.origin/.test, bad.push, host.replace(/^https?:\/\//, '').replace(/^\*\./, '').repla…, host.replace(/^https?:\/\//, '').replace, host.replace, code.includes, /method\s*!==\s*'GET'/.test, /const\s+CACHE\s*=/.test, /v\d+/.test | sw.js 코드가 지켜야 할 계약 위반 목록(순수 — 자체검사가 직접 부른다). |
| `scripts/check-sw.mjs` | export | function | `registrationViolations(mainSource)` | codeOnly, /serviceWorker\s*\.\s*register\s*\(/.test, bad.push, /import\.meta\.env\.PROD/.test, /BASE_URL/.test | 등록 코드가 실제로 있는가(M-0015: 만들어 놓고 안 부름). |
| `scripts/check-sw.mjs` | internal | function | `externalHostsFromCsp()` | readFileSync, join, src.match, [...block.matchAll(/'([^']+)'/g)].map((m) => m[1]).filter, [...block.matchAll(/'([^']+)'/g)].map, block.matchAll, h.startsWith | check-csp.mjs의 connect-src를 외부 호스트 SSOT로 읽는다(손으로 적지 않는다). |
| `scripts/check-sw.mjs` | internal | const | `GOOD_SAMPLE` | — | — |
| `scripts/check-sw.mjs` | internal | const | `GOOD_MAIN` | — | — |
| `scripts/check-sw.mjs` | internal | const | `problems` | — | — |
| `scripts/check-sync-parallelism.mjs` | internal | const | `PLAN` | — | — |
| `scripts/check-sync-parallelism.mjs` | internal | const | `SYNC` | — | — |
| `scripts/check-sync-parallelism.mjs` | internal | function | `between(src, start, end)` | src.indexOf, src.slice | — |
| `scripts/check-sync-parallelism.mjs` | export | function | `parallelismProblems(planSrc, syncSrc)` | between, [...push.matchAll(/reason:\s*\n?\s*'([^']+)'/g)].map, push.matchAll, /id:\s*'roots'[\s\S]*?domains:\s*\['trip',\s*'place'\]/.test, problems.push, /id:\s*'moments'[\s\S]*?domains:\s*\['moment'\]/.test, /id:\s*'moment-children'[\s\S]*?domains:\s*\['media',\s*'ex…, reasons.some, reason.includes, /id:\s*'all-domains'[\s\S]*?domains:\s*SYNC_DOMAINS/.test | — |
| `scripts/check-sync-parallelism.mjs` | internal | const | `goodPlan` | — | — |
| `scripts/check-sync-parallelism.mjs` | internal | const | `goodSync` | — | — |
| `scripts/check-sync-parallelism.mjs` | internal | const | `cases` | — | — |
| `scripts/check-sync-parallelism.mjs` | internal | const | `broken` | — | — |
| `scripts/check-sync-parallelism.mjs` | internal | const | `problems` | — | — |
| `scripts/check-sync-release-contract.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-sync-release-contract.mjs` | export | function | `auditArtifacts({ contract, edgeSource, clientSource })` | edgeSourceSha256, problems.push, renderEdgeContractBlock, normalizeLf(edgeSource).includes, normalizeLf, renderClientContract, /sourceSha256:\s*FN_SOURCE_SHA256/.test, Number.isInteger | — |
| `scripts/check-sync-release-contract.mjs` | internal | const | `contract` | — | — |
| `scripts/check-sync-release-contract.mjs` | internal | const | `problems` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `SRC` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `VITEST` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `TZS` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `SLICE_DATE` | — | ISO 일시에서 날짜를 잘라내는 패턴. 날짜는 localDate()로만 만든다. |
| `scripts/check-timezone.mjs` | internal | const | `OPT_OUT` | — | 이 줄은 날짜가 아니라는 명시적 예외(해시 등). 남용 방지를 위해 줄 단위로만 허용. |
| `scripts/check-timezone.mjs` | internal | const | `MEMORY_FIELDS` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `DEVICE_FNS` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `DEVICE_OK` | — | 이 자리는 정말 기기 시계가 맞다는 명시적 예외. 이유를 함께 적게 한다(빈 예외는 결함). |
| `scripts/check-timezone.mjs` | internal | const | `MEMORY_IN_DEVICE_FN` | — | — |
| `scripts/check-timezone.mjs` | export | function | `findDeviceClockViolations(text, label)` | text.split('\n').forEach, text.split, codePart, MEMORY_IN_DEVICE_FN.test, DEVICE_OK.test, out.push, line.trim | 기억 필드를 기기 로컬 함수에 넣는 줄을 찾는다(순수 — 자체검사가 직접 호출). |
| `scripts/check-timezone.mjs` | internal | const | `PROSE_FILES` | — | — |
| `scripts/check-timezone.mjs` | internal | function | `walk(dir)` | readdirSync, join, statSync(p).isDirectory, statSync, out.push, walk, /\.ts$/.test | — |
| `scripts/check-timezone.mjs` | internal | function | `codePart(line)` | line.trim, t.startsWith, line.indexOf, line.slice | 주석을 걷어내고 코드 부분만 남긴다(계약을 설명하는 주석까지 잡지 않도록). |
| `scripts/check-timezone.mjs` | export | function | `findSliceViolations(text, label)` | text.split('\n').forEach, text.split, SLICE_DATE.test, codePart, OPT_OUT.test, out.push, line.trim | 소스 텍스트에서 위반 줄을 찾는다(순수 — 자체검사가 이걸 직접 호출). |
| `scripts/check-timezone.mjs` | internal | const | `violations` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `deviceClock` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `runs` | — | — |
| `scripts/check-timezone.mjs` | internal | const | `bad` | — | — |
| `scripts/check-tooling-registry.mjs` | export | function | `validateRegistry(candidate)` | problems.push, Array.isArray, candidate.tools.entries, tool.id.trim, ids.has, ids.add, tool.command.trim, tool.purpose.trim | — |
| `scripts/check-tooling-registry.mjs` | internal | function | `selfTest()` | validateRegistry, failures.push, invalidCases.forEach | — |
| `scripts/check-tooling-registry.mjs` | internal | const | `registry` | — | — |
| `scripts/check-tooling-registry.mjs` | internal | const | `registryProblems` | — | — |
| `scripts/check-tooling-registry.mjs` | internal | const | `failures` | — | — |
| `scripts/check-tooling-registry.mjs` | internal | const | `skips` | — | — |
| `scripts/check-ui-color-token.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-ui-color-token.mjs` | internal | const | `UI_DIR` | — | — |
| `scripts/check-ui-color-token.mjs` | internal | const | `HEX_RE` | — | 따옴표로 감싼 6자리 hex(#rrggbb). 3자리는 `#118`(PR 번호) 오탐이 많아 6자리만 본다. |
| `scripts/check-ui-color-token.mjs` | internal | const | `ALLOW` | — | — |
| `scripts/check-ui-color-token.mjs` | export | function | `violations(src)` | src.split, HEX_RE.test, ALLOW.test, out.push, line.trim | 한 소스에서 (줄번호, 내용) 위반 목록. 순수 함수라 자체검사가 직접 부른다. |
| `scripts/check-ui-color-token.mjs` | internal | const | `selfTestCount` | — | — |
| `scripts/check-ui-color-token.mjs` | internal | function | `collectTs(dir)` | join, existsSync, readdirSync, e.isDirectory, out.push, collectTs, e.name.endsWith, e.name.includes | — |
| `scripts/check-ui-color-token.mjs` | internal | const | `files` | — | — |
| `scripts/check-ui-color-token.mjs` | internal | const | `failed` | — | — |
| `scripts/check-ui-color-token.mjs` | internal | const | `checked` | — | — |
| `scripts/check-update-signal.mjs` | export | function | `pkgProblems(text)` | /"build":[^\n]*gen-version-file\.mjs/.test, p.push | — |
| `scripts/check-update-signal.mjs` | export | function | `genProblems(text)` | text.includes, p.push, /changelog\.ts/.test | — |
| `scripts/check-update-signal.mjs` | export | function | `appProblems(appText, mainText)` | appText.includes, p.push, /wireAutoUpdate/.test, appText.match, /\btryApply\s*\(/.test | — |
| `scripts/check-update-signal.mjs` | export | function | `swProblems(text)` | /version\.json[^\n]*\)\s*\)?\s*return;\\|endsWith\('\/versio…, p.push | — |
| `scripts/check-update-signal.mjs` | internal | const | `GOOD_PKG` | — | — |
| `scripts/check-update-signal.mjs` | internal | const | `GOOD_GEN` | — | — |
| `scripts/check-update-signal.mjs` | internal | const | `GOOD_APP` | — | — |
| `scripts/check-update-signal.mjs` | internal | const | `GOOD_MAIN` | — | — |
| `scripts/check-update-signal.mjs` | internal | const | `GOOD_SW` | — | — |
| `scripts/check-update-signal.mjs` | internal | function | `selfTest()` | pkgProblems, genProblems, appProblems, swProblems, appProblems(GOOD_APP.replace('typedSinceSafePoint = false;'…, GOOD_APP.replace, m.includes, cases.filter, fn, console.error | — |
| `scripts/check-update-signal.mjs` | internal | const | `problems` | — | — |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `RICH_TEXT_RENDERER` | — | 강조(` … `)를 안전하게 렌더하는 유일한 경로 . 이게 있어야 문자열 쪽에 규칙을 걸 필요가 없다. ⚠️ 이 게이트 자신의 결함(M-0012): 처음엔 "이 6개 파일에서 ` ` 금지"로 만들었다. 대상 파일을 손으로 골랐기 때문에 dataManager·r2Setup·changelog가 통째로 빠졌고, 사용자 화면에… |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `NO_LEGACY_ROWS` | — | 판정 렌더러를 우회하면 안 되는 파일과, 거기서 금지된 옛 나열형 클래스. |
| `scripts/check-verdict-symmetry.mjs` | export | function | `metricsMissingExpected(src)` | src.matchAll, src.slice, obj.includes, Math.max, /:\s*MetricSource\b/.test, /\b(interface\s+\w+\\|type\s+\w+\s*=)\s*$/.test, new RegExp(`[{,]\\s*${k}\\s*[,}]`).test, ['label', 'expected', 'level'].filter( (k) => !has(k) && !(…, ['label', 'expected', 'level'].filter, has | `actual:` 를 가진 객체 리터럴은 `expected:` 와 `level:` 도 가져야 한다. 객체 경계는 중괄호 깊이로 잡는다(중첩 리터럴을 부모로 오인하지 않게). |
| `scripts/check-verdict-symmetry.mjs` | export | function | `markdownBypass(src)` | src .replace(/\/\*[\s\S]*?\*\//g, ' ') .replace, src .replace, noComments.matchAll, /\*\*[^*]+\*\*/.test, bad.push, body.slice | 강조를 담은 문자열을 `.textContent =` 로 직접 넣으면 별표가 그대로 찍힌다(실제 결함). 문자열을 금지하는 대신 우회 경로 를 금지한다 — 강조는 el()/applyText()/setNote()가 처리한다. 주석은 제외한다(주석의 강조는 화면에 안 나온다). |
| `scripts/check-verdict-symmetry.mjs` | export | function | `emptyTextIsMeaningless(src)` | src.indexOf, /[\w$.]/.test, src.slice, /,\s*(['"`])((?:\\.\\|(?!\1)[\s\S])*?)\1\s*,?\s*$/.exec, m[2].trim, bad.push, /^(없음\\|없습니다\\|-\\|—\\|N\/A\\|해당\s*없음\\|0개\\|0건)$/.test | 빈 근거 표가 무엇이 없는지 말하는가. 2026-07-27 사용자: "사진파일 9개라고 적혀있는데 밑에 '없음' 이건 뭘 의미?" 접힌 제목은 가진 것 (파일 9개·기록 9개)을 세고, 펼친 표는 어긋난 것 을 담는다. 그 한정을 아무도 말하지 않아서, 표가 비면 「없음」 한 단어만 남았다 — 사용자는 그게 "사진이 없… |
| `scripts/check-verdict-symmetry.mjs` | export | function | `richTextRendererIntact(src)` | src.replace(/\/\*[\s\S]*?\*\//g, ' ').replace, src.replace, /export function applyText\(/.test, bad.push, /createElement\('strong'\)/.test, /innerHTML/.test, /if \(text !== undefined\) applyText\(node, text\)/.test | 안전 렌더러가 살아 있는가 — 이게 사라지면 위 규칙 전체가 무너진다. |
| `scripts/check-verdict-symmetry.mjs` | export | function | `guideCardContract(src)` | /'guide-card(?: \\|')/.test, new RegExp(`['"\\s]${c}(?=['"\\s])`).test, ['guide-card-ic', 'guide-card-mid'].filter, hasClass, missing.join | — |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `TOOL_FIELDS` | — | — |
| `scripts/check-verdict-symmetry.mjs` | export | function | `toolRegistryComplete(src)` | src.matchAll, src.slice, TOOL_FIELDS.filter, obj.includes, bad.push, missing.join | — |
| `scripts/check-verdict-symmetry.mjs` | export | function | `legacyRows(src, banned)` | banned.filter((c) => src.includes(`'${c}`) \\|\\| src.include…, banned.filter, src.includes | — |
| `scripts/check-verdict-symmetry.mjs` | export | function | `purgeOrderContract(syncSrc)` | syncSrc.match, src.indexOf, Math.min, ['remote.hardDelete(', 'remote.hardDeleteFamily('].map(at).…, ['remote.hardDelete(', 'remote.hardDeleteFamily('].map, at, bad.push | 영구삭제는 되돌릴 수 없는 유일한 경로다. 여기선 "무엇을 하는가"보다 "어떤 순서로 하는가" 가 안전을 결정하고, 순서는 코드를 훑어봐선 틀린 줄 모른다 — 그래서 게이트가 본다. · 원장(`ledgerAdd`)을 지우기 전에 적어야 한다. 뒤집히면 그 틈에 밀린 편집을 가진 다른 기기가 자기 사본을 다시 올려 좀비가… |
| `scripts/check-verdict-symmetry.mjs` | export | function | `runSyncAppliesLedger(syncSrc)` | syncSrc.match, /applyPurgedLedger\(/.test, bad.push, /ledgerAll\(/.test | 다른 기기의 영구삭제는 원장으로만 배운다 — 서버 행이 없으므로 pull은 그 사실을 볼 수 없다. `runSync`가 원장을 적용하지 않으면 지운 여행이 그 기기 휴지통에 영원히 남는다. |
| `scripts/check-verdict-symmetry.mjs` | export | function | `checkCountMatches(src)` | src.match, Number | `CHECK_COUNT`가 실제 점검 수와 맞는가 (자기점검 2026-07-27). 화면은 「기록 N건을 11가지 기준 으로 확인했어요」라고 말한다. 그 숫자는 `integrity.ts`에 손으로 적힌 상수인데, 진짜 값은 `add()` 호출 수 다. 두 곳에 같은 사실이 있으면 드리프트는 시간 문제다 — M-0001이… |
| `scripts/check-verdict-symmetry.mjs` | export | function | `noDroppedPurgedAtColumn(files)` | src.split, /purged_at/.test, /^\s*(\/\/\\|\*\\|\/\*)/.test, /purged_ids/.test, bad.push | — |
| `scripts/check-verdict-symmetry.mjs` | export | function | `listPrefixIsServerBuilt(fnSrc)` | fnSrc.match, /const prefix = `\$\{userId\}\//.test, bad.push, fnSrc.replace(/\/\/[^\n]*/g, '').replace, fnSrc.replace, new RegExp(`body\\.${f}\\b`).test, new RegExp(`\\b${f}\\?:\\s*unknown`).test, /return json\(\s*\{[^}]*\burl\b/.test, /truncated/.test | 목록 조회의 범위는 서버가 못박는다 (supabase-security-dev §2.8, 2026-07-26 신설). 단건 op(put/get/delete)는 키를 서버가 만들어 남의 폴더를 못 가리킨다. 그런데 목록은 prefix 하나로 버킷 전체가 열린다 — R2엔 RLS가 없으므로 이 한 줄이 마지막 벽이다. 그래서… |
| `scripts/check-verdict-symmetry.mjs` | export | function | `overlayContract(css)` | css.match, /overflow-y:\s*auto/.test, bad.push, /place-items:\s*center/.test, /align-items:\s*center/.test, /env\(safe-area-inset/.test, /dvh/.test, css.matchAll, /(?:max-)?height:[^;]*\b\d+vh/.test, /position:\s*fixed/.test | 실제 사고(2026-07-26, 사용자 실기기 가로 태블릿): 사진 편집 모달의 상하가 잘리고 여백이 없었다 . 세 오버레이가 같은 규칙을 각자 구현했고 셋이 서로 달랐다 — `.guide-overlay`만 배웠고 `.pe-overlay`는 못 배웠다(§7의 교과서적 사례). 두 가지가 원인이다: ① `vh` — 모바일… |
| `scripts/check-verdict-symmetry.mjs` | export | function | `semanticTintContrast(css)` | css.matchAll, m[1].trim().split('\n').pop().trim, m[1].trim().split('\n').pop, m[1].trim().split, m[1].trim, new RegExp(`background[^;]*--sem-${hue}\\)`).test, new RegExp(`(?:^\\|[;{])\\s*color:\\s*var\\(--sem-${hue}\\)`…, bad.push | 실제 사고(2026-07-26): 시맨틱색 12~15% 틴트 배경 위에 같은 색 원색 텍스트 를 쓰면 라이트 테마에서 대비가 2.5~3.0:1로 무너진다(필요 4.5:1). 다크에서는 통과라 다크에서만 확인하면 안 보인다 (LESSONS §5 전형). v0.69에서 이 부류를 고쳤는데 손으로 고른 규칙만 고쳤다. 이번에… |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `MUTATING_IMPORTS` | — | 실제 사고(2026-07-26): `dataManager.ts`가 `runSync`를 아예 import하지 않아 휴지통 복원· 영구삭제·백업 복원 뒤에 동기화가 일어나지 않았다. 영구삭제 전파(ADR-0027)를 만들어 놓고도 그 op가 큐에 앉아만 있었다 — 사용자가 2기기 테스트를 했다면 실패했을 것이다. 근본형은 … |
| `scripts/check-verdict-symmetry.mjs` | export | function | `screensRequestSync(files)` | src.replace(/\/\*[\s\S]*?\*\//g, ' ').replace, src.replace, [...code.matchAll(/import\s*\{([^}]*)\}\s*from/g)].map((m) …, [...code.matchAll(/import\s*\{([^}]*)\}\s*from/g)].map, code.matchAll, MUTATING_IMPORTS.test, /(^\\|[,\s])requestSync(Soon)?([,\s]\\|$)/.test, /requestSync(Soon)?\s*\(/.test, bad.push | — |
| `scripts/check-verdict-symmetry.mjs` | export | function | `noDirectRunSync(files)` | /from '[^']*services\/sync'/.test, /\brunSync\b/.test, bad.push | 동기화 실행 규칙은 autoSync 한 곳에만 — 화면이 runSync를 직접 부르면 중복·오류삼킴이 재발한다. |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `selfTestCount` | — | 셀프테스트 통과 수 — 손으로 세지 않는다. 아래 로그가 이 값을 읽는다. |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `problems` | — | — |
| `scripts/check-verdict-symmetry.mjs` | internal | function const | `read(rel)` | readFileSync, join | — |
| `scripts/check-verdict-symmetry.mjs` | internal | function | `walk(dir, out = [])` | readdirSync, join, statSync(p).isDirectory, statSync, walk, f.endsWith, out.push | — |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `cardFiles` | — | — |
| `scripts/check-verdict-symmetry.mjs` | internal | const | `scanned` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `CHANGELOG` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `REGISTRY` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `GRADLE` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `APK_WF` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `WINDOWS_BUILD` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `WINDOWS_VERSION_READER` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `WINDOWS_WF` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `TAURI_CONFIG` | — | — |
| `scripts/check-version-ssot.mjs` | internal | const | `TAURI_CARGO` | — | — |
| `scripts/check-version-ssot.mjs` | export | function | `ssotVersion(src)` | /version: '([\d.]+)'/.exec | changelog 맨 앞 항목의 버전 — 이 저장소의 버전 정본. |
| `scripts/check-version-ssot.mjs` | export | function | `registryVersion(src)` | /appVersion:\s*'([\d.]+)'/.exec | 생성물이 심어 둔 앱 버전. |
| `scripts/check-version-ssot.mjs` | export | function | `gradleVersionNameProblems(src)` | /versionName\s+(.+)/.exec, m[1].trim, /APP_VERSION_NAME/.test, /:\s*'([^']*)'/.exec, /^\d+(\.\d+)*$/.test | 안드로이드 `versionName`이 주입받는 형태 인가. 🔴 「숫자를 손으로 박은 것」만 위반이다. 로컬 대체값(`'0.0-local'` 같은 비-버전 문자열)은 괜찮다 — 배포본과 헷갈릴 수 없기 때문이다. |
| `scripts/check-version-ssot.mjs` | export | function | `workflowInjects(src)` | /-PAPP_VERSION_NAME/.test | 워크플로가 그 값을 실제로 넘기는가 (주석에 적힌 말이 아니라 명령). |
| `scripts/check-version-ssot.mjs` | export | function | `windowsVersionProblems(buildScript, configText, cargoText)` | /readAppVersion/.test, /--config/.test, /JSON\.stringify\(\{ version \}\)/.test, problems.push, JSON.parse, /^version = "0\.0\.0"$/m.test | — |
| `scripts/check-version-ssot.mjs` | export | function | `windowsReleaseVersionProblems(readerScript, workflowText)` | /readAppVersion/.test, problems.push, /node scripts\/print-app-version\.mjs/.test, /IsNullOrWhiteSpace\(\$version\)/.test | — |
| `scripts/check-version-ssot.mjs` | export | function | `docsClaimPackageJsonIsSsot(text)` | /`?package\.json`?\s*(의\s*)?version\s*[—-]\s*손편집 금지/.test | 지시문서가 `package.json`을 버전 정본이라 부르는가 — 그 문장이 이 사고의 출발이었다. |
| `scripts/check-version-ssot.mjs` | internal | const | `isMain` | — | — |
| `scripts/check-windows-shell.mjs` | internal | const | `ROOT` | — | — |
| `scripts/check-windows-shell.mjs` | internal | const | `OAUTH_URL` | — | — |
| `scripts/check-windows-shell.mjs` | export | function | `auditWindowsShell({ pkg, config, capability, cargo, rust, androidManifest })` | problems.push, permissions.filter, permissions.find, textPermissions.includes, permissions.some, p.startsWith, cargo.includes, rust.indexOf, rust.includes, androidManifest.includes | — |
| `scripts/check-windows-shell.mjs` | internal | function | `fixtures()` | — | — |
| `scripts/check-windows-shell.mjs` | export | function | `runSelfTest()` | fixtures, structuredClone, broad.capability.permissions.push, auditWindowsShell | — |
| `scripts/check-windows-shell.mjs` | internal | const | `input` | — | — |
| `scripts/check-windows-shell.mjs` | internal | const | `problems` | — | — |
| `scripts/check-workflow-pins.mjs` | export | function | `unpinnedActions(text)` | [...text.matchAll(/^\s*-?\s*uses:\s*([^\s#]+).*$/gm)] .map(…, [...text.matchAll(/^\s*-?\s*uses:\s*([^\s#]+).*$/gm)] .map, text.matchAll, /@[0-9a-f]{40}$/i.test | — |
| `scripts/check-workflow-pins.mjs` | internal | const | `good` | — | — |
| `scripts/check-workflow-pins.mjs` | internal | const | `bad` | — | — |
| `scripts/check-workflow-pins.mjs` | internal | const | `problems` | — | — |
| `scripts/compare-geocoders.mjs` | internal | const | `QUERIES` | — | — |
| `scripts/compare-geocoders.mjs` | internal | function const | `sleep(ms)` | setTimeout | — |
| `scripts/compare-geocoders.mjs` | internal | function | `rankGrade(placeRank)` | Number.isFinite | 등급 자 — src/domain/place/precision.ts와 같은 경계 를 쓴다(다르면 비교가 거짓말이 된다). |
| `scripts/compare-geocoders.mjs` | internal | function | `meters(a, b)` | Math.cos, Math.round, Math.sqrt | — |
| `scripts/compare-geocoders.mjs` | internal | async function | `askNominatim(q)` | fetch, r.json, (Array.isArray(rows) ? rows : []).map, Array.isArray, String(x.display_name ?? '').split, String, Number, rankGrade | — |
| `scripts/compare-geocoders.mjs` | internal | async function | `askKakao(q, key)` | fetch, encodeURIComponent, r.json, (j.documents ?? []).map, Number | — |
| `scripts/compare-geocoders.mjs` | internal | async function | `askVworld(q, key)` | fetch, r.json, items.map, Number | — |
| `scripts/compare-geocoders.mjs` | internal | const | `providers` | — | — |
| `scripts/compare-geocoders.mjs` | internal | const | `stats` | — | — |
| `scripts/compare-geocoders.mjs` | internal | const | `firsts` | — | — |
| `scripts/gate-selftest-lib.mjs` | export | function | `runSelfTest(gateName, fn)` | fn, String, console.error, process.exit | 자체검사를 돌리고, 실패하면 사유를 적고 `exit 2`로 나간다. 던지든(`throw`) 실패 목록을 돌려주든(`string[]`) 둘 다 받는다 — 기존 게이트들이 두 형태로 나뉘어 있고, 그 안에서 무엇을 검사하는지는 건드리지 않는 것 이 이 전환의 핵심이기 때문이다(§11 ②: 통과시키려고 판정 로직을 되돌리지 … |
| `scripts/gen-adapters.mjs` | internal | const | `ROOT` | — | — |
| `scripts/gen-adapters.mjs` | export | const | `SOURCE` | — | — |
| `scripts/gen-adapters.mjs` | export | function | `stripFrontmatter(text)` | text.match, text.slice, rest.match, rest.slice | 문서 트리 규율의 frontmatter(`shape:` 등)는 문서 메타이지 계약 본문이 아니다. 걷어내지 않으면 두 어댑터 머리에 `--- shape: prose-debt ---`가 실려 나가고, 그러면 두 AI가 읽는 「공통 계약」에 자기 문서의 관리 메타가 섞인다(2026-08-09 실측 — 이 게이트가 즉시 RE… |
| `scripts/gen-adapters.mjs` | export | const | `BEGIN` | — | — |
| `scripts/gen-adapters.mjs` | export | const | `END` | — | — |
| `scripts/gen-adapters.mjs` | export | const | `ADAPTERS` | — | 어댑터 목록. 새 도구(다른 AI)가 생기면 여기 한 줄 이고 나머지는 따라온다(§7 2층). |
| `scripts/gen-adapters.mjs` | export | function | `render(adapterText, constitutionText)` | adapterText.indexOf, adapterText.slice, stripFrontmatter(constitutionText).trim, stripFrontmatter | 마커 사이를 갈아 끼운 전체 텍스트를 만든다. 순수 함수 — 게이트가 이걸 그대로 불러 「커밋본 == 재생성본」을 대조한다(두 곳에 로직을 손으로 구현하면 그 순간 갈라진다). |
| `scripts/gen-all.mjs` | internal | const | `ROOT` | — | — |
| `scripts/gen-all.mjs` | export | const | `CHAIN` | — | 실행 순서. 각 간선에 사유가 필수다 (§18-C — `안전을 위해`·`기존 순서라서`는 사유가 아니다). 사유는 「앞의 어떤 출력이 뒤의 어떤 입력이 되는가」로만 쓴다. |
| `scripts/gen-all.mjs` | export | const | `SKIP` | — | 여기서 안 도는 생성기와 그 이유 . 이유 없는 제외는 결함이다(§7). |
| `scripts/gen-all.mjs` | export | function | `discoverGenerators(dir)` | readdirSync(dir) .filter((f) => f.startsWith('gen-') && (f.…, readdirSync(dir) .filter, readdirSync, f.startsWith, f.endsWith | 디렉터리의 생성기 전부. 손으로 세지 않는다(M-0155 — 「전부」라는 낱말이 다섯 번 거짓이었다). |
| `scripts/gen-all.mjs` | export | function | `coverageGaps(found, chain = CHAIN, skip = SKIP)` | chain.map, skip.keys, found.filter, planned.has, [...planned].filter((p) => !foundSet.has(p)).sort, [...planned].filter, foundSet.has | 모집단 ↔ (CHAIN ∪ SKIP)의 어긋남. 양방향으로 본다 — 없는 것을 돌리려 해도 결함이다. |
| `scripts/gen-all.mjs` | internal | function | `selfTest()` | coverageGaps, fails.push, extra.unregistered.includes, gone.missing.includes, discoverGenerators(join(ROOT, 'scripts')).includes, discoverGenerators, join | 자체검사 — 알려진 실패를 주입해 잡히는지 본다(§4). 실패하면 exit 2(전제 미충족). |
| `scripts/gen-all.mjs` | internal | const | `problems` | — | — |
| `scripts/gen-all.mjs` | internal | const | `found` | — | — |
| `scripts/gen-all.mjs` | internal | const | `{ unregistered, missing }` | — | — |
| `scripts/gen-constitution.mjs` | internal | const | `ROOT` | — | — |
| `scripts/gen-constitution.mjs` | export | const | `SRC` | — | — |
| `scripts/gen-constitution.mjs` | internal | const | `OUT` | — | — |
| `scripts/gen-constitution.mjs` | export | function | `plain(md)` | md .replace(/\*\*(.+?)\*\*/g, '$1') .replace(/`([^`]+)`/g, …, md .replace(/\*\*(.+?)\*\*/g, '$1') .replace, md .replace | 마크다운 장식을 걷어낸다(화면은 textContent로만 넣으므로 마크업이 그대로 보인다). |
| `scripts/gen-constitution.mjs` | export | function | `sectionBody(text, heading)` | text.split, lines.findIndex, l.trim, heading.match, lines.slice, line.match, out.push, out.join | `## 제목` / `### 제목` 아래 본문(다음 같은/상위 수준 제목 전까지)을 돌려준다. |
| `scripts/gen-constitution.mjs` | export | function | `numberedItems(body)` | (body ?? '').split, line.match, rest.match, out.push, plain | `1. 제목 본문` 꼴 항목들 → [{title, body}]. 제목이 없으면 전체가 title. |
| `scripts/gen-constitution.mjs` | export | function | `bulletItems(body)` | (body ?? '') .split(/\r?\n/) .filter((l) => /^- \S/.test(l)…, (body ?? '') .split(/\r?\n/) .filter, (body ?? '') .split, /^- \S/.test, plain, l.slice | `- 문장` 꼴 항목들 → [문장]. 인용(>)·중첩은 세지 않는다. |
| `scripts/gen-constitution.mjs` | export | function | `parse(text)` | numberedItems, sectionBody, bulletItems | 헌법 텍스트 → 화면이 쓸 자료구조(순수 — 게이트가 그대로 부른다). |
| `scripts/gen-constitution.mjs` | internal | function const | `q(s)` | s.replace(/\\/g, '\\\\').replace, s.replace | — |
| `scripts/gen-constitution.mjs` | export | function | `render(data)` | items.map((it) => ` { title: ${q(it.title)}, body: ${q(it.b…, items.map, q, items.map((s) => ` ${q(s)},`).join, pairs, strs | 결정적 TS 파일 문자열(손편집 금지 헤더 포함). |
| `scripts/gen-constitution.mjs` | export | function | `collect()` | parse, readFileSync, join | SSOT를 읽어 파싱한다. |
| `scripts/gen-constitution.mjs` | internal | const | `isMain` | — | — |
| `scripts/gen-harness-law.mjs` | internal | const | `ROOT` | — | — |
| `scripts/gen-harness-law.mjs` | export | const | `SOURCE` | — | — |
| `scripts/gen-harness-law.mjs` | export | const | `OUT` | — | — |
| `scripts/gen-harness-law.mjs` | export | const | `SECTIONS` | — | 모을 절과 왜 모으는가 . 🔴 이유 없는 항목은 두지 않는다 — 「릴리스 관련 같아서」로 넣기 시작하면 이 문서는 헌법의 사본이 되고, 사본은 읽히지 않는다. `heading`은 `## `로 시작하는 절 제목의 접두사 다(제목에 조항 번호·날짜가 붙어 자라므로 전체 일치로 잡으면 헌법을 고칠 때마다 여기가 조용히 빈다… |
| `scripts/gen-harness-law.mjs` | export | function | `sectionOf(text, headingPrefix)` | text.split, lines.findIndex, l.startsWith, lines[i].startsWith, lines.slice(start, end).join('\n').trimEnd, lines.slice(start, end).join, lines.slice | 헌법에서 `## ` 절 하나를 통째로 잘라낸다. 못 찾으면 `null`(호출부가 RED로 판정). |
| `scripts/gen-harness-law.mjs` | internal | const | `HEADER` | — | — |
| `scripts/gen-harness-law.mjs` | export | function | `render(constitutionText)` | stripFrontmatter, sectionOf, parts.push, parts.join | 통합본 전체 텍스트를 만든다 — 순수 함수 (게이트가 그대로 불러 대조한다). |
| `scripts/gen-module-design-docs.mjs` | export | function | `writeModuleDesignDocs()` | collectModuleDesigns, expectedDocuments, mkdirSync, readdirSync, existing.endsWith, documents.has, rmSync, join, writeFileSync | — |
| `scripts/gen-platform-map.mjs` | internal | const | `ROOT` | — | — |
| `scripts/gen-platform-map.mjs` | internal | const | `OUT` | — | — |
| `scripts/gen-platform-map.mjs` | internal | function const | `read(rel)` | existsSync, join, readFileSync | — |
| `scripts/gen-platform-map.mjs` | internal | function const | `code(rel)` | read(rel).replace(/\/\/[^\n]*/g, '').replace, read(rel).replace, read | 주석을 걷어낸 코드 — 주석 속 옛 서술을 "현재 사실"로 읽지 않게(M-0006의 화석 방지). |
| `scripts/gen-platform-map.mjs` | export | const | `ROWS` | — | 표의 행 정의. 각 행은 자기 답을 코드에서 찾아온다 — 여기에 정답을 적어두지 않는다. `probe()`는 `{ where, note }`를 돌려주거나, 판정할 수 없으면 null을 돌려준다(그 경우 생성기가 멈춘다 — 모르는 것을 아는 척 적느니 빌드를 세우는 게 낫다, 비타협 원칙 #4). |
| `scripts/gen-platform-map.mjs` | internal | function | `migrationText()` | join, existsSync, readdirSync(dir) .filter((f) => f.endsWith('.sql')) .map((f…, readdirSync(dir) .filter((f) => f.endsWith('.sql')) .map, readdirSync(dir) .filter, readdirSync, f.endsWith, readFileSync | — |
| `scripts/gen-platform-map.mjs` | export | function | `collect()` | ROWS.map, r.probe | — |
| `scripts/gen-platform-map.mjs` | export | function | `render(rows)` | rows .map( (r) => ` {\n id: '${r.id}',\n what: '${r.what}',…, rows .map | — |
| `scripts/gen-platform-map.mjs` | internal | const | `isMain` | — | — |
| `scripts/gen-registry.mjs` | internal | const | `ROOT` | — | — |
| `scripts/gen-registry.mjs` | internal | const | `OUT` | — | — |
| `scripts/gen-registry.mjs` | export | const | `DOC_FILES` | — | 문서 산문 카운트 마커를 심을 파일들(있는 것만). SSOT는 registry(collect()). 🔴 어댑터(`CLAUDE.md`·`AGENTS.md`)는 여기 넣지 않는다. 그 두 파일의 마커 사이는 `docs/CONSTITUTION.md`에서 생성되므로, 마커를 심어야 할 자리는 정본 하나 다. 어댑터에 직접 심… |
| `scripts/gen-registry.mjs` | internal | const | `MARKER_RE` | — | 마커: <!--reg:KEY-->값<!--/reg-->. KEY는 registry 카운트 키. |
| `scripts/gen-registry.mjs` | export | function | `findMarkers(text, reg)` | text.matchAll, Object.prototype.hasOwnProperty.call, String, out.push | text 안의 모든 마커를 [{key, value, ok, expected}]로. reg가 있으면 대조도 채운다. |
| `scripts/gen-registry.mjs` | export | function | `patchMarkers(text, reg)` | text.replace, Object.prototype.hasOwnProperty.call, String | 마커 값을 reg 카운트로 다시 심는다(순수). 알 수 없는 KEY는 그대로 둔다. |
| `scripts/gen-registry.mjs` | export | function | `collect()` | readFileSync, join, [...harness.matchAll(/name:\s*'([^']+)'/g)].map, harness.matchAll, readdirSync(join(ROOT, dir)).filter, readdirSync, f.endsWith, changelog.match, readdirSync(agentDir) .filter((f) => f.endsWith('.md')) .fi…, readdirSync(agentDir) .filter | SSOT들에서 카운트·목록을 실측한다. |
| `scripts/gen-registry.mjs` | export | function | `render(reg)` | reg.gates.map((g) => ` '${g}',`).join, reg.gates.map, reg.agents.map((a) => ` '${a}',`).join, reg.agents.map, reg.gateControl .map((g) => ` { name: '${g.name}', control:…, reg.gateControl .map | 결정적 TS 파일 문자열(손편집 금지 헤더 포함). |
| `scripts/gen-registry.mjs` | internal | const | `isMain` | — | — |
| `scripts/gen-sync-release-contract.mjs` | internal | const | `ROOT` | — | — |
| `scripts/gen-sync-release-contract.mjs` | internal | const | `CONTRACT_PATH` | — | — |
| `scripts/gen-sync-release-contract.mjs` | internal | const | `EDGE_PATH` | — | — |
| `scripts/gen-sync-release-contract.mjs` | internal | const | `CLIENT_PATH` | — | — |
| `scripts/gen-sync-release-contract.mjs` | internal | const | `contract` | — | — |
| `scripts/gen-sync-release-contract.mjs` | internal | const | `edgeSource` | — | — |
| `scripts/gen-sync-release-contract.mjs` | internal | const | `edgeBlock` | — | — |
| `scripts/gen-version-file.mjs` | internal | const | `version` | — | — |
| `scripts/generate-icons.mjs` | internal | const | `CHROMIUM` | — | — |
| `scripts/generate-icons.mjs` | internal | const | `SVG` | — | — |
| `scripts/generate-icons.mjs` | internal | const | `BG` | — | — |
| `scripts/generate-icons.mjs` | internal | const | `svgDataUri` | — | — |
| `scripts/generate-icons.mjs` | internal | function | `page(size, { maskable = false } = {})` | Math.round | size×size 아이콘 페이지. maskable은 안전영역(중앙 80%)에 축소 배치 + 전면 배경. |
| `scripts/generate-icons.mjs` | internal | const | `OUT` | — | — |
| `scripts/generate-icons.mjs` | internal | const | `work` | — | — |
| `scripts/harness.mjs` | internal | const | `CAVEAT_RE` | — | — |
| `scripts/harness.mjs` | internal | const | `SELF_PROOF_RE` | — | 자기증명 안내문은 한계가 아니다 — 자기가 잘 돈다는 자랑 이다. 「주입증명 … 미설치 1은 확인 불가로 갈림」처럼 낱말만 겹치는 줄을 한계로 읽으면 목록이 소음이 되고, 소음이 된 목록은 사람이 안 읽는다(§11 ③ — 오탐은 틀린 게이트다). |
| `scripts/harness.mjs` | export | function | `caveatsOf(text)` | text.trim, text .split('\n') // 게이트가 이미 붙인 들여쓰기·화살표를 벗긴다. 안 벗기면 `↳ ↳`가…, text .split, l.trim().replace(/^[↳·⚠️\s]+/, '').trim, l.trim().replace, l.trim, SELF_PROOF_RE.test, CAVEAT_RE.test | 게이트 출력에서 한계를 말한 줄 만 고른다 — 순수 함수(자체검사가 부른다). |
| `scripts/harness.mjs` | internal | const | `FAST` | — | — |
| `scripts/harness.mjs` | internal | const | `gates` | — | — |
| `scripts/harness.mjs` | internal | const | `EXIT_PRECONDITION` | — | 전제 미충족을 뜻하는 종료코드. 라이브 게이트들이 이 값으로 자기 전제를 알린다. 🔴 이 값은 `optional`일 때만 SKIP이다. 아래 분기의 `g.optional &&`가 그 경계이고, 그건 실수가 아니라 계약이다 — 정적 게이트 15종은 셀프테스트가 공허할 때 같은 `2`를 낸다(저장소 관용구). Requir… |
| `scripts/harness.mjs` | internal | const | `failed` | — | — |
| `scripts/harness.mjs` | internal | const | `skipped` | — | — |
| `scripts/harness.mjs` | internal | const | `notMeasured` | — | — |
| `scripts/harness.mjs` | internal | const | `partial` | — | 통과했지만 스스로 「일부는 못 쟀다」고 말한 게이트. 마지막 판정문이 이것을 센다. |
| `scripts/hook-release-freeze.mjs` | export | function | `selfTest()` | judgeSelfTest, Date.now, blockedBy, JSON.parse, parse, JSON.stringify | 훅의 자체검사(§4). 판정의 양성·음성 주입은 `release-lock.mjs`가 갖고 있고 여기서 다시 세지 않는다 — 같은 사실을 두 곳에서 세면 갈라진다(§2 SSOT). 대신 이 파일만이 아는 것을 못박는다: 훅이 실제로 부르는 경로 (stdin JSON → 명령 문자열 → 판정)가 이어져 있는가. |
| `scripts/hook-release-freeze.mjs` | internal | const | `isMain` | — | — |
| `scripts/hook-secret-guard.mjs` | internal | const | `SKIP_PREFIX` | — | — |
| `scripts/hook-secret-guard.mjs` | export | function | `writtenText(toolInput)` | parts.push, parts.join | 훅 입력에서 「이번에 쓰이는 텍스트」를 모은다. Write=content, Edit=new_string. |
| `scripts/hook-secret-guard.mjs` | export | function | `isGuardedPath(filePath)` | filePath.replace(/\\/g, '/').replace, filePath.replace, SKIP_PREFIX.some, rel.startsWith | 이 경로가 검사 대상인가(docs/는 제외 — 계약 문서는 형태를 서술한다). |
| `scripts/hook-secret-guard.mjs` | export | function | `selfTest()` | fakeJwt, scanText, writtenText, isGuardedPath | — |
| `scripts/hook-secret-guard.mjs` | internal | async function | `readStdin()` | chunks.push, Buffer.concat(chunks).toString, Buffer.concat | — |
| `scripts/hook-secret-guard.mjs` | internal | async function | `main()` | selfTest, readStdin, JSON.parse, isGuardedPath, scanText, writtenText, [...new Set(findings.map((f) => f.name))].join, findings.map, console.error, process.exit | — |
| `scripts/hook-sql-safe.mjs` | internal | const | `DESTRUCTIVE` | — | — |
| `scripts/hook-sql-safe.mjs` | export | function | `stripSqlComments(sql)` | String(sql) .replace(/\/\*[\s\S]*?\*\//g, ' ') .replace, String(sql) .replace, String | SQL 주석을 걷어낸 본문(주석 안의 낱말을 파괴 동사로 오탐하지 않게 — 오탐도 결함). |
| `scripts/hook-sql-safe.mjs` | export | function | `isRollbackWrapped(sql)` | stripSqlComments(sql).trim().toLowerCase, stripSqlComments(sql).trim, stripSqlComments, /^begin\s*;/.test, /\brollback\s*;?\s*$/.test, /\bcommit\b/.test | `BEGIN … ROLLBACK`으로 감싼 사전검증인가(운영 무변경 · 표준 절차). |
| `scripts/hook-sql-safe.mjs` | export | function | `hasReasonedOverride(sql)` | /--\s*sql-safe-ok\s*:\s*(\S.*)/i.exec, String, Boolean, m[1].trim | `-- sql-safe-ok: <이유>` — 이유가 비어 있지 않아야 통과(이유 없는 예외는 결함). |
| `scripts/hook-sql-safe.mjs` | export | function | `findDestructiveSql(sql)` | sql.trim, isRollbackWrapped, hasReasonedOverride, stripSqlComments, new RegExp(d.re.source, d.re.flags).test, hits.push, hasWhereLessUpdate | 파괴적 구문을 모두 찾아 이름 배열로. 통과 사유가 있으면 빈 배열. |
| `scripts/hook-sql-safe.mjs` | export | function | `hasWhereLessUpdate(body)` | String(body).split, String, stmt.trim, /^update\b/i.test, /\bset\b/i.test, /\bwhere\b/i.test | WHERE 없는 `UPDATE`를 문장 단위로 찾는다 (T-024 · 2026-08-12). 정규식 하나로 풀지 않는다. 전방탐색을 겹친 한 줄 정규식으로도 되지만, 문장을 `;`로 갈라 각각 「update로 시작하고 where가 없다」만 보면 읽는 사람이 무엇을 막는지 한눈에 알고, 다음 사람이 고칠 때 겁내지 않는다… |
| `scripts/hook-sql-safe.mjs` | export | function | `selfTest()` | findDestructiveSql, must, pass | — |
| `scripts/hook-sql-safe.mjs` | internal | async function | `readStdin()` | chunks.push, Buffer.concat(chunks).toString, Buffer.concat | — |
| `scripts/hook-sql-safe.mjs` | internal | async function | `main()` | selfTest, JSON.parse, readStdin, findDestructiveSql, console.error, hits.join, process.exit | — |
| `scripts/hook-verify-exit.mjs` | internal | const | `VERIFY` | — | 종료코드로 판정해야 하는 명령들. 이름이 아니라 하는 일 로 고른다. 🔴 줄 맨 앞(또는 `;`·`&&`·`(` 바로 뒤)에 올 때만 명령으로 본다. 산문 속에 인용된 같은 글자를 명령으로 읽으면 오탐이 나고, 오탐은 빡빡한 훅이 아니라 틀린 훅 이다(§11 ③). |
| `scripts/hook-verify-exit.mjs` | internal | const | `SAFE_TAIL` | — | 이 파이프 뒤쪽은 종료코드를 삼키지 않는다 고 볼 수 있는가. `tee`는 앞 명령의 출력을 그대로 흘리고 종료코드도 자기 것이지만, 적어도 출력을 자르지 않는다 . 그래도 판정은 여전히 tee의 것이므로 허용하지 않는다 — 예외를 만들면 「이건 괜찮겠지」가 늘어난다(§7: 예외는 이유와 함께 등록하거나 아예 두지 않는… |
| `scripts/hook-verify-exit.mjs` | internal | function | `isProse(line)` | /^[-*>#]/.test, line.includes, line.startsWith | 산문 줄인가 — 히어독·문서 본문이 명령 문자열에 섞여 들어온다. 🔴 이 판정은 실제 오탐에서 나왔다 (2026-08-06): ADR 본문의 한 줄 (이 규율을 설명하는 문장)이 차단당해, 규율을 적는 일 자체를 규율이 막았다. 게다가 훅이 Bash를 막으니 자기 수리 경로까지 막혔다 — Write/Edit로 고쳐야 … |
| `scripts/hook-verify-exit.mjs` | internal | function | `pipedCommand(head)` | head .split(/;\\|&&\\|\\\|\\\|\\|\(/) .map((x) => x.trim()) .fil…, head .split(/;\\|&&\\|\\\|\\\|\\|\(/) .map, head .split, x.trim | 파이프에 실제로 물려 있는 명령 하나를 뽑는다. 🔴 예전 판은 파이프 앞의 조각을 전부 검사했다. 그래서 이 형태가 막혔다: node scripts/verify-x.mjs > run.log 2>&1; echo "EXIT=$?"; grep FAIL run.log \\| head -30 여기서 파이프에 물린 것은 `grep… |
| `scripts/hook-verify-exit.mjs` | export | function | `pipesVerification(command)` | command.split, raw.trim, isProse, line.indexOf, SAFE_TAIL.some, re.test, pipedCommand, line.slice, VERIFY.find, String | 명령 문자열에서 「검증 명령을 파이프에 넣었는가」를 판정한다 — 순수 함수(자체검사가 부른다). |
| `scripts/hook-verify-exit.mjs` | internal | const | `SELF_COUNTS` | — | — |
| `scripts/hook-verify-exit.mjs` | export | function | `selfTest()` | pipesVerification | — |
| `scripts/hook-verify-exit.mjs` | internal | const | `isMain` | — | — |
| `scripts/known.mjs` | internal | const | `ROOT` | — | — |
| `scripts/known.mjs` | internal | const | `CODE_ROOTS` | — | 뒤질 코드 뿌리. 문서가 아닌 층 — 여기가 M-0064에서 빠졌다. |
| `scripts/known.mjs` | internal | const | `CODE_EXT` | — | — |
| `scripts/known.mjs` | internal | function | `walk(dir, out = [])` | readdirSync, n.startsWith, join, statSync(p).isDirectory, statSync, walk, CODE_EXT.test, out.push | @typedef {{ kind: string, title: string, file: string, line: number, body: string }} Entry |
| `scripts/known.mjs` | export | function | `splitSections(text, file, kind, headingRe = /^#{2,3} (.+)$/)` | text.split, lines.slice(start, end).join('\n').trim, lines.slice(start, end).join, lines.slice, out.push, headingRe.exec, push, m[1].replace(/\*\*/g, '').trim, m[1].replace | `## 제목` 또는 `# 제목`으로 갈린 절들. 문서 세 층이 같은 모양을 쓴다. |
| `scripts/known.mjs` | export | function | `extractRedComments(text, file)` | text.split, /^\s*(\/\/\\|\*\\|\/\*\\|--)/.test, lines[i].includes, isComment, lines.slice(a, b + 1).join, lines.slice, (lines[i].replace(/^\s*(\/\/\\|\*\\|\/\*\\|--)\s*/, '').replac…, lines[i].replace(/^\s*(\/\/\\|\*\\|\/\*\\|--)\s*/, '').replace…, lines[i].replace(/^\s*(\/\/\\|\*\\|\/\*\\|--)\s*/, '').replace, lines[i].replace | 🔴가 붙은 주석 덩어리 를 뽑는다. 왜 주석만인가: 코드 본문까지 넣으면 낱말이 흔해 순위가 무너진다(`accept`는 어디에나 있다). 이 저장소에서 왜 그렇게 했는지 는 거의 항상 주석에 있고, 그게 조사할 때 필요한 것이다. 왜 🔴만인가: 표시가 곧 "이건 위험하다·이유가 있다" 는 저자의 선언이다. 전부 뽑으… |
| `scripts/known.mjs` | export | function | `collectEntries(root = ROOT)` | readFileSync, join, read, out.push, splitSections, readdirSync, walk, t.includes, extractRedComments, relative | 네 층을 전부 모은다. |
| `scripts/known.mjs` | export | function | `rank(entries, terms)` | terms.map((t) => t.toLowerCase()).filter, terms.map, t.toLowerCase, entries .map((e) => { const hay = `${e.title}\n${e.body}`.t…, entries .map, `${e.title}\n${e.body}`.toLowerCase, low.filter, hay.includes, a.file.localeCompare, e.kind.startsWith | 낱말이 몇 개나 들어갔는지로 매긴다. 전부 들어간 것이 위로. 일부러 단순하게 둔다 — 정교한 점수는 왜 이게 위인지 를 사람이 못 읽는다. 「7개 중 3개」는 읽을 수 있고, 읽을 수 있어야 틀렸을 때 의심할 수 있다 (§8 — 도구는 판정을 하되 근거를 준다). |
| `scripts/known.mjs` | internal | function | `evidenceLine(body, terms)` | body.split, lines.findIndex, l.toLowerCase().includes, l.toLowerCase, t.toLowerCase, lines[i].replace(/^\s*(\/\/\\|\*\\|\/\*\\|--)\s*/, '').trim().…, lines[i].replace(/^\s*(\/\/\\|\*\\|\/\*\\|--)\s*/, '').trim, lines[i].replace, lines[0]?.trim().slice, lines[0]?.trim | 덩어리에서 낱말이 처음 나오는 줄 — 사람이 눈으로 확인할 근거 한 줄. |
| `scripts/known.mjs` | export | function | `selfTest(root = ROOT)` | splitSections, fails.push, extractRedComments, red[0].body.includes, rank, crowd.push, rank(crowd, ['사진', '위치']).some, collectEntries, all.some, e.kind.startsWith | — |
| `scripts/known.mjs` | internal | const | `isMain` | — | — |
| `scripts/known.mjs` | internal | const | `argv` | — | — |
| `scripts/lib/app-version.mjs` | export | function | `parseAppVersion(source)` | source.match | changelog 맨 앞 항목에서 앱 버전을 읽는다. 모든 배포 표면이 이 파서를 공유한다. |
| `scripts/lib/app-version.mjs` | export | function | `readAppVersion(path = 'src/app/changelog.ts')` | parseAppVersion, readFileSync | — |
| `scripts/lib/secret-patterns.mjs` | export | const | `PATTERNS` | — | — |
| `scripts/lib/secret-patterns.mjs` | export | function | `isServiceRoleJwt(candidate)` | JSON.parse, Buffer.from(candidate.split('.')[1], 'base64url').toString, Buffer.from, candidate.split | JWT 후보 문자열의 payload role이 service_role인지 판정(anon/publishable 허용). |
| `scripts/lib/secret-patterns.mjs` | export | function | `scanText(text)` | text.matchAll, isServiceRoleJwt, findings.push, m[0].slice | 텍스트에서 시크릿 형태를 모두 찾아 [{name, sample}]로 반환. 모든 매치를 개별 판정. |
| `scripts/lib/secret-patterns.mjs` | export | function | `fakeJwt(role)` | Buffer.from(JSON.stringify(o)).toString, Buffer.from, JSON.stringify, b64u | 셀프테스트용 가짜 JWT 조립(리터럴 금지 — 위 주석 참조). |
| `scripts/live-browser-lib.mjs` | export | async function | `launchLiveBrowser(chromium, { gate, cleanup, launchOptions = {} })` | chromium.launch, console.error, String(e).split, String, cleanup, process.exit | 라이브 게이트용 Chromium을 띄운다. @param chromium playwright의 chromium (호출부가 이미 import한 것을 넘긴다) @param opts.gate 게이트 이름 — 실패 문장에 그대로 나간다 @param opts.cleanup 실패 시 정리(서버 close·임시 산출물 삭제). 게이트… |
| `scripts/live-browser-lib.mjs` | export | function | `makeCheck(sink = console.log)` | sink | 라이브 게이트의 판정 기록기를 만든다. @param {(line: string) => void} [sink] 출력 함수(기본 `console.log`) @returns 라벨·통과여부·상세를 받는 `check` 함수. `check.stats()`로 집계를 읽는다. |
| `scripts/live-browser-lib.mjs` | export | function | `proveCheckCounts()` | makeCheck, lines.push, check, check.stats, lines[0].startsWith, lines[1].startsWith, lines[1].includes | 위 기록기가 실제로 실패를 세는지 증명한다 — 라이브 게이트의 대조군(§4). 실패하면 던진다. 호출부는 `runSelfTest`로 감싸서 사유를 적고 `exit 2`로 나간다(§18-G). |
| `scripts/live-browser-lib.mjs` | export | const | `XSCROLL_OPT_OUT` | — | 의도된 가로 스크롤은 이 표식으로 뺀다 — 표식 없는 넘침은 전부 위반이다. |
| `scripts/live-browser-lib.mjs` | export | function | `scanOverflowContainers(page, tolerance = 2)` | page.evaluate, document.querySelectorAll, el.closest, el.tagName.toLowerCase, el.className.trim, el.className.trim().split(/\s+/).slice(0, 3).join, el.className.trim().split(/\s+/).slice, el.className.trim().split, out.push, getComputedStyle | 열려 있는 화면의 모든 요소 를 훑어 가로로 넘치는 컨테이너를 찾는다. 손으로 고른 선택자 목록을 쓰지 않는다 — 그런 목록은 자라지 않아서 , 새 화면이 생기면 조용히 범위 밖이 된다(이 저장소가 반복해서 당한 형태). @param page playwright 페이지 @param tolerance 반올림 여유(기본 2… |
| `scripts/live-browser-lib.mjs` | export | async function | `proveOverflowScanner(page)` | scanOverflowContainers, page.evaluate, document.createElement, box.appendChild, document.body.appendChild, document.getElementById('__overflow_probe__')?.remove, document.getElementById | 대조군(§4) — 스캐너가 실제로 넘침을 잡는지 진짜 DOM에 심어서 증명한다. 🔴 순수 함수를 시험하는 것으로는 부족하다. 이 검사의 위험은 판정식이 아니라 측정이 실제 화면에서 도는가 이고, 그건 심어 봐야만 안다. 심은 것은 반드시 되돌린다 — 대조군이 본 검사를 오염시키면 안 된다. |
| `scripts/measure-backup-zip-memory.mjs` | internal | const | `DISPLAY_BYTES` | — | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | const | `THUMB_BYTES` | — | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | const | `DEFAULT_COUNTS` | — | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | function | `mib(bytes)` | Math.round | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | function | `snapshot(stage)` | process.memoryUsage, mib, process.resourceUsage | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | function | `forceGc()` | globalThis.gc | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | function | `seededBytes(length, seed)` | — | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | async function | `loadBackupModule()` | createServer, vite.ssrLoadModule, vite.close | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | function | `fixtureRows(count)` | Array.from, String(index).padStart, String, seededBytes | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | async function | `generate(count, output, encrypted)` | loadBackupModule, backup.serializeZip, fixtureRows, blob.arrayBuffer, crypto.encryptBytes, writeFile, process.stdout.write, JSON.stringify, mib, vite.close | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | async function | `measure(count, inputPath, encrypted)` | import, loadBackupModule, forceGc, snapshot, readFile, stages.push, backup.importBackupAuto, process.stdout.write, JSON.stringify, mib | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | function | `runChild(args)` | spawn, process.cwd, child.stdout.setEncoding, child.stdout.on, child.on, reject, args.join, resolve, stdout.trim().split(/\r?\n/).filter(Boolean).map, stdout.trim().split(/\r?\n/).filter | — |
| `scripts/measure-backup-zip-memory.mjs` | internal | async function | `main()` | process.argv.slice, generate, Number, measure, process.argv.includes, process.argv.slice(2).filter, countArgs.map, counts.some, Number.isInteger, mkdtemp | — |
| `scripts/measure-live-time.mjs` | internal | const | `chromium` | — | — |
| `scripts/measure-live-time.mjs` | internal | const | `t0` | — | — |
| `scripts/measure-live-time.mjs` | internal | const | `stat` | — | — |
| `scripts/measure-live-time.mjs` | internal | const | `origLaunch` | — | — |
| `scripts/measure-live-time.mjs` | internal | function | `report()` | Date.now, ((x / total) * 100).toFixed, console.error, (total / 1000).toFixed, (stat.launchMs / 1000).toFixed, pct, (stat.gotoMs / 1000).toFixed, (stat.sleepMs / 1000).toFixed, (stat.waitForMs / 1000).toFixed, (stat.evalMs / 1000).toFixed | — |
| `scripts/measure-live-time.mjs` | internal | const | `origExit` | — | — |
| `scripts/measure-live-time.mjs` | internal | const | `targetArg` | — | — |
| `scripts/measure-live-time.mjs` | internal | const | `target` | — | — |
| `scripts/module-design-docs-lib.mjs` | export | const | `ROOT` | — | — |
| `scripts/module-design-docs-lib.mjs` | export | const | `OUTPUT_DIR` | — | — |
| `scripts/module-design-docs-lib.mjs` | export | const | `GENERATOR_VERSION` | — | — |
| `scripts/module-design-docs-lib.mjs` | internal | const | `MODULES` | — | — |
| `scripts/module-design-docs-lib.mjs` | export | const | `MODULE_CATALOG` | — | — |
| `scripts/module-design-docs-lib.mjs` | internal | const | `CODE_EXTENSIONS` | — | — |
| `scripts/module-design-docs-lib.mjs` | internal | const | `INCLUDED_EXTENSIONS` | — | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `slash(path)` | path.replaceAll | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `walk(dir)` | existsSync, readdirSync, join, entry.isDirectory, out.push, walk | — |
| `scripts/module-design-docs-lib.mjs` | export | function | `isSourceInput(path)` | slash, relative, p.startsWith, INCLUDED_EXTENSIONS.has, extname, ['.rs', '.toml', '.json'].includes, ['.ts', '.sql', '.json'].includes, ['.mjs', '.js', '.py'].includes, ['.yml', '.yaml'].includes, [ 'package.json', 'package-lock.json', 'vite.config.ts', 't… | — |
| `scripts/module-design-docs-lib.mjs` | export | function | `sourceInputs(root = ROOT)` | roots.flatMap, walk, join, existsSync, files.push, [...new Set(files.filter(isSourceInput))].sort, files.filter, slash(relative(root, a)).localeCompare, slash, relative | — |
| `scripts/module-design-docs-lib.mjs` | export | function | `moduleIdOf(input)` | slash, input.startsWith, relative, p.startsWith, /^(scripts\\|\.github\/workflows\\|\.githooks\\|schemas)\//.te…, /^(package(?:-lock)?\.json\\|vite\.config\.ts\\|tsconfig\.jso…, /\/rowmap\.ts$/.test, /^src\/services\/(?:sync\\|syncParity\\|syncPlan\\|canonicalSy…, /^src\/domain\/(?:deviceFleetVerdict\\|sync[^/]*)\.ts$/.test, /^src\/(?:media\/(?:editor-core\\|pixelops)\\|ui\/photoEditor… | 파일의 단일 소유자. 경로 이름은 기능의 증거가 아니라 소유 경계 다. 실제 동작은 아래 AST· I/O 추출이 재며, 새 파일이 어느 경계에도 없으면 null로 닫힌다. |
| `scripts/module-design-docs-lib.mjs` | internal | function | `logical(text)` | text.replaceAll | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `hashFiles(files)` | createHash, hash.update, slash, relative, logical, readFileSync, hash.digest | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `scriptKind(path)` | extname, ['.js', '.mjs', '.cjs'].includes | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `oneLine(text, max = 220)` | text.replace(/\s+/g, ' ').replaceAll('\\|', '\\\\|').trim, text.replace(/\s+/g, ' ').replaceAll, text.replace, clean.slice | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `hasExport(node)` | Boolean, node.modifiers?.some | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `docOf(node, sf)` | ts.getJSDocCommentsAndTags(node).map((d) => d.getText(sf)).…, ts.getJSDocCommentsAndTags(node).map, ts.getJSDocCommentsAndTags, d.getText, oneLine, docs.replace(/^\/\*\*?\\|\*\/$/g, '').replace, docs.replace | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `callsOf(node, sf)` | ts.isCallExpression, calls.add, oneLine, child.expression.getText, ts.forEachChild, [...calls].slice | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `declarationsOf(sf)` | hasExport, ts.isFunctionDeclaration, node.parameters.map((p) => oneLine(p.getText(sf), 80)).join, node.parameters.map, oneLine, p.getText, out.push, node.modifiers?.some, node.type.getText, callsOf | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `importsOf(sf)` | ts.isImportDeclaration, ts.isStringLiteral, imports.push, ts.isExportDeclaration | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `resolveImport(fromFile, specifier, allPaths)` | specifier.startsWith, resolve, dirname, ['.ts', '.mts', '.cts', '.js', '.mjs', '.cjs', '.json'].map, ['index.ts', 'index.js', 'index.mjs'].map, join, candidates.find, allPaths.has | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `matches(text, regex)` | [...text.matchAll(regex)].map((m) => m[1]).filter, [...text.matchAll(regex)].map, text.matchAll | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `signalsOf(path, text)` | [...new Set([ ...matches(text, /\b(?:db\(\)\\|d)\.(local[A-Z…, matches, [...new Set(matches(text, /\.from\(\s*['"]([^'"]+)['"]\s*\)…, [...new Set(matches(text, /\b(?:create\s+table(?:\s+if\s+no…, [...new Set(matches(text, /\bcreate\s+(?:or\s+replace\s+)?f…, signals.push, localTables.join, serverTables.join, sqlTables.join, sqlFunctions.join | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `analyzeFile(full, allPaths)` | slash, relative, logical, readFileSync, text.split, CODE_EXTENSIONS.has, extname, ts.createSourceFile, scriptKind, declarationsOf | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `collectTestEvidence(allPaths, moduleOfPath)` | walk(join(ROOT, 'tests')).filter, walk, join, CODE_EXTENSIONS.has, extname, MODULE_CATALOG.map, logical, readFileSync, ts.createSourceFile, scriptKind | — |
| `scripts/module-design-docs-lib.mjs` | export | function | `collectModuleDesigns()` | sourceInputs, inputs.filter, moduleIdOf, unknown.map((f) => ` - ${slash(relative(ROOT, f))}`).join, unknown.map, slash, relative, inputs.map, resolve, analyzeFile | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `md(value)` | value.replaceAll | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `renderFileInventory(module)` | module.files .map((file) => { const publicCount = file.decl…, module.files .map, file.declarations.filter, file.localImports.map((d) => slash(relative(ROOT, d.resolve…, file.localImports.map, slash, relative, md, file.signals.join | — |
| `scripts/module-design-docs-lib.mjs` | internal | function | `renderDeclarations(module)` | rows.push, md, item.calls.join, rows.join | — |
| `scripts/module-design-docs-lib.mjs` | export | function | `renderModuleDoc(module, catalog = MODULE_CATALOG)` | catalog.map, [...module.files].sort((a, b) => b.lines - a.lines).slice, [...module.files].sort, module.entryPoints.map((p) => `- \`${p}\``).join, module.entryPoints.map, module.edges.map(([id, count]) => `- ${titleOf.get(id) ?? i…, module.edges.map, titleOf.get, module.externalImports.map((p) => `- \`${p}\``).join, module.externalImports.map | — |
| `scripts/module-design-docs-lib.mjs` | export | function | `renderIndex(collected)` | collected.modules.reduce, collected.modules.flatMap, m.edges.map, collected.modules.map((m, index) => `\\| ${index + 1} \\| ${m…, collected.modules.map, encodeURI, m.hash.slice, graph.join | — |
| `scripts/module-design-docs-lib.mjs` | export | function | `expectedDocuments(collected = collectModuleDesigns())` | collectModuleDesigns, renderIndex, documents.set, renderModuleDoc | — |
| `scripts/release-lock.mjs` | internal | const | `ROOT` | — | — |
| `scripts/release-lock.mjs` | export | const | `LOCK_PATH` | — | — |
| `scripts/release-lock.mjs` | export | const | `STALE_MS` | — | 잠금이 낡으면 스스로 풀린다 . 세션이 끊기거나 릴리스를 잊고 넘어가면, 얼어붙은 저장소가 다음 사람의 모든 커밋을 막는다 — 그건 이 장치가 막으려던 것보다 큰 사고다. (`hook-secret-guard`·`hook-verify-exit`과 같은 자세: 판정 불가는 통과시킨다 .) |
| `scripts/release-lock.mjs` | export | const | `MIN_REASON` | — | 이유 없는 재개방을 막는다 — 「그냥」·「하나만」은 이유가 아니다. |
| `scripts/release-lock.mjs` | export | const | `EMPTY` | — | — |
| `scripts/release-lock.mjs` | export | function | `nextState(state, action, arg, now)` | arg.trim | 상태 전이 — 순수 함수(자체검사가 가짜 시각으로 부른다). @returns `{ state }` 또는 `{ error }` |
| `scripts/release-lock.mjs` | export | function | `blockedBy(command, state, now)` | command.split, raw.trim, isProse, line.split(/;\\|&&\\|\\\|\\\|\\|\\\|/).map, line.split, x.trim, FROZEN.some, re.test | 지금 이 명령을 막아야 하는가 — 순수 함수. `null`이면 통과. |
| `scripts/release-lock.mjs` | internal | const | `FROZEN` | — | 얼어 있는 동안 막는 명령. 새 이력을 만드는 것 만 고른다 — 조사·읽기는 자유여야 한다(막으면 사람이 잠금을 꺼 버리고, 꺼진 장치는 없는 장치다). 🔴 `git push`는 여기 없다 — 첫 실사용에서 이 장치가 자기 발을 밟았다 (2026-08-09, v2.08). 첫 판은 push도 막았고, 그래서 릴리스 후… |
| `scripts/release-lock.mjs` | export | function | `frozenWords(frozen = FROZEN)` | frozen.map, /git\\s\+([\w-]+)/.exec, String | 막는 명령의 이름들 — 화면 문장이 이 원장에서 나온다 (손으로 적으면 갈라진다 · §17). |
| `scripts/release-lock.mjs` | export | function | `isProse(line)` | /^[-*>#]/.test, line.includes, line.startsWith | 산문 줄인가 — 히어독·문서 본문이 명령 문자열에 섞여 들어온다(hook-verify-exit과 같은 규율). |
| `scripts/release-lock.mjs` | export | function | `readLock()` | existsSync, JSON.parse, readFileSync | — |
| `scripts/release-lock.mjs` | internal | function | `writeLock(state)` | rmSync, writeFileSync, JSON.stringify | — |
| `scripts/release-lock.mjs` | internal | const | `SELF` | — | — |
| `scripts/release-lock.mjs` | export | function | `selfTest()` | nextState, String(nextState(st, act, arg, T0).error ?? '').includes, String, blockedBy, label.startsWith, frozenWords, words.includes, words.some, w.includes, words.join | — |
| `scripts/release-lock.mjs` | internal | const | `isMain` | — | — |
| `scripts/sync-release-contract-lib.mjs` | export | const | `EDGE_BEGIN` | — | — |
| `scripts/sync-release-contract-lib.mjs` | export | const | `EDGE_END` | — | — |
| `scripts/sync-release-contract-lib.mjs` | internal | const | `EDGE_OMITTED` | — | — |
| `scripts/sync-release-contract-lib.mjs` | export | function | `normalizeLf(text)` | String(text).replace, String | — |
| `scripts/sync-release-contract-lib.mjs` | export | function | `logicalEdgeSource(source)` | normalizeLf, escapeRegExp, generated.test, lf.replace | — |
| `scripts/sync-release-contract-lib.mjs` | export | function | `sha256(text)` | createHash('sha256').update(normalizeLf(text), 'utf8').dige…, createHash('sha256').update, createHash, normalizeLf | — |
| `scripts/sync-release-contract-lib.mjs` | export | function | `edgeSourceSha256(source)` | sha256, logicalEdgeSource | — |
| `scripts/sync-release-contract-lib.mjs` | export | function | `renderEdgeContractBlock(contract)` | contract.mediaSign.requiredOps.map((op) => `'${op}'`).join, contract.mediaSign.requiredOps.map, [ EDGE_BEGIN, '// GENERATED by scripts/gen-sync-release-con… | — |
| `scripts/sync-release-contract-lib.mjs` | export | function | `replaceEdgeContractBlock(source, block)` | normalizeLf, escapeRegExp, generated.test, lf.replace, legacy.test | — |
| `scripts/sync-release-contract-lib.mjs` | export | function | `renderClientContract(contract)` | JSON.stringify | — |
| `scripts/sync-release-contract-lib.mjs` | internal | function | `escapeRegExp(value)` | value.replace | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `ROOT` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `TMP` | — | 임시 산출물. 점으로 시작해 어느 게이트의 스캔에도 걸리지 않는다(진단 라이브와 같은 규율). |
| `scripts/verify-authgate-live.mjs` | internal | const | `VITE` | — | `npx`는 셸 shim이라 `execFileSync`의 계약이 아니다 — Windows에서 ENOENT로 층 전체가 SKIP된다(M-0091). |
| `scripts/verify-authgate-live.mjs` | internal | const | `PORT` | — | — |
| `scripts/verify-authgate-live.mjs` | export | function | `outputPathForRequest(outputRoot, requestUrl)` | decodeURIComponent, pathname.replace, resolve, candidate.startsWith | Resolve only a file contained in the temporary build output. Invalid encodings and traversal are rejected. |
| `scripts/verify-authgate-live.mjs` | internal | const | `chromium` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `SRC` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `FIXTURE` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `MIME` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `OUTPUT_ROOT` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `server` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `failed` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `checked` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | function const | `check(label, ok, detail = '')` | console.log | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `browser` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `page` | — | — |
| `scripts/verify-authgate-live.mjs` | internal | const | `consoleErrors` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `ROOT` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `DIST` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `BASE` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `TMP` | — | 주입 층이 쓰는 임시 산출물. 점으로 시작해 어느 게이트의 스캔에도 걸리지 않는다. |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `VITE` | — | `npx`/`npx.cmd`는 셸 shim이라 `execFileSync`의 네이티브 실행 파일 계약이 아니다. Windows Node 24에서 실제로 `spawnSync npx ENOENT`가 나 진단 층 전체가 SKIP됐다(M-0091). `npm run build`가 쓰는 같은 Vite JS 진입점을 현재 Node로… |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `chromium` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | function | `newestMs(dir)` | readdirSync, e.name.startsWith, join, Math.max, e.isDirectory, newestMs, statSync | 🔴 `dist`가 소스보다 낡았으면 멈춘다 — `verify-editor-live`와 같은 이유, 같은 규율 . 빌드가 실패해도 옛 dist는 남으므로 "빌드했겠지"가 조용히 성립한다. 낡은 번들을 재는 검사는 공허한 게이트다(§4). 규칙을 두 파일에 손으로 구현한 셈이지만, 그 규칙이 막는 사고 가 파일마다 따로 … |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `results` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | function const | `check(name, ok, extra = '')` | results.push, console.log | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `MIME` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | function | `serveDir(dir, base, port)` | createServer, p.startsWith, res.writeHead(404).end, res.writeHead, p.slice, readFile, join, res.writeHead(200, { 'cache-control': 'no-store', 'content-…, extname, srv.listen | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `FIXTURE` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `appServer` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `fixServer` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `browser` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `errors` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `page` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | async function | `openDiagnosticsHub(pg)` | pg.goto, pg.waitForTimeout, pg.getByRole('button', { name: /데이터 관리/ }).first().click, pg.getByRole('button', { name: /데이터 관리/ }).first, pg.getByRole, pg.locator('[data-hub-card="diagnostics"]').click, pg.locator, pg.waitForSelector | 앱을 열고 데이터 관리 → 진단 도구 까지 간다. 안정 hook을 먼저 쓴다. C층도 같은 경로를 쓰므로 한 곳에만 구현한다 (§7 2층) — 두 곳에 손으로 적으면 허브 진입이 바뀌는 날 한쪽만 고쳐진다. |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `toolNames` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `b` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `stuckView` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `d` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `beforeProbes` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `c1` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `c2` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `fileEmpty` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `fileInput` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `fileRead` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `firstTool` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `seeded` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `fileCard` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `hasFileTool` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `storeCard` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `hasStore` | — | — |
| `scripts/verify-diagnostics-live.mjs` | internal | const | `fails` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `chromium` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `DIST` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `BASE` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function | `assertDistFresh()` | readdirSync, e.name.startsWith, join, Math.max, e.isDirectory, newest, statSync, resolve, dirname, fileURLToPath | 🔴 `dist`가 지금 소스보다 낡았으면 멈춘다. 2026-07-27에 이 구멍을 밟았다: 알려진 실패를 주입했는데 빌드가 타입 오류로 죽었고 , 그래서 `dist`는 옛 번들 그대로였다 — 라이브 검사는 초록을 냈다. 주입 검증이 통과했으니 "검사가 살아 있다"고 믿을 뻔했다. 낡은 번들을 재는 검사는 공허한 게이트… |
| `scripts/verify-editor-live.mjs` | internal | const | `MIME` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `served` | — | 서버가 실제로 파일을 내준 경로 기록. 서비스워커 검사가 이걸 센다 — 브라우저 쪽 `fromServiceWorker()`는 "워커가 처리했다"일 뿐 "네트워크를 안 탔다"가 아니다. |
| `scripts/verify-editor-live.mjs` | internal | function | `makeServer(log)` | createServer, p.startsWith, res.writeHead(404).end, res.writeHead, p.slice, log.push, readFile, join, res.writeHead(200, { ...headers, 'content-type': MIME[extna…, extname | dist를 BASE 경로로 서빙하는 서버를 만든다(오프라인 검사가 자기 서버를 따로 띄운다). |
| `scripts/verify-editor-live.mjs` | internal | const | `server` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `results` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function const | `check(name, ok, extra = '')` | results.push, console.log | — |
| `scripts/verify-editor-live.mjs` | internal | async function | `observeViewerObjectUrls(targetPage)` | targetPage.evaluate, URL.createObjectURL.bind, window.__liveOriginalCreateObjectURL, window.__liveObjectUrlBlobs.set | — |
| `scripts/verify-editor-live.mjs` | internal | async function | `stopObservingViewerObjectUrls(targetPage)` | targetPage.evaluate | — |
| `scripts/verify-editor-live.mjs` | internal | const | `browser` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `page` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `PNG_1X1` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tileZooms` | — | 요청된 타일의 확대수준(z) 기록. 왜 이걸 재는가(2026-07-30 M-0050): 「지도가 부정확하다」 신고의 절반은 좌표가 아니라 확대수준 이었다 — 지점이 하나뿐일 때 zoom이 초기값 10에 남아, 핀 하나가 김포~남양주가 다 보이는 화면 위에 떠 있었다. MapLibre의 `map` 객체는 화면 밖(클로저 … |
| `scripts/verify-editor-live.mjs` | internal | const | `kakaoSdkRequests` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tomtomTileRequests` | — | — |
| `scripts/verify-editor-live.mjs` | internal | async function | `waitUntil(fn, timeoutMs = 15000, stepMs = 100)` | Date.now, fn, setTimeout | Node 쪽 조건이 참이 될 때까지 기다린다 — 못 채워도 던지지 않는다. 던지지 않는 이유(§4): 이 함수를 쓰는 자리는 대개 「0건이면 아래 판정이 공허하다」를 재는 전제 검사다. 조건이 끝내 거짓이면 그건 판정할 값 이지 사고가 아니다 — 검사가 그 사실을 화면에 적어야 한다. 시간으로 자면 그 0이 「없다」인지… |
| `scripts/verify-editor-live.mjs` | internal | async function | `settle(p = page)` | p.evaluate, requestAnimationFrame | 클릭 뒤 화면이 실제로 다시 그려질 때까지 기다린다 — 시계가 아니라 브라우저의 렌더 파이프라인에. ── 왜 (T-016 · BGT-2 실측 2026-08-09) ───────────────────────────────────── 이 검사의 시간이 어디로 가는지 성분별로 쟀다(게이트를 고치지 않고 playwright를 … |
| `scripts/verify-editor-live.mjs` | internal | const | `TOUCH_TARGET_EXCEPTIONS` | — | 44px 표적 검사에서 빼는 것과 그 이유 . 🔴 한 곳에만 둔다 — `coverSweep`이 화면마다 쓰고 홈 전수 검사도 쓴다. 두 벌로 두면 한쪽만 고쳐지는 날이 온다(§2 · §7 2층). 🔴 이유 없는 제외는 결함이다 (§7). |
| `scripts/verify-editor-live.mjs` | internal | async function | `coverSweep(label, rootSelector, exceptions = TOUCH_TARGET_EXCEPTIONS)` | page.viewportSize, page.setViewportSize, settle, page.evaluate, document.querySelector, n.getClientRects, getComputedStyle, n.tagName.toLowerCase, n.className.trim().split, n.className.trim | 🔴 폴드 커버 폭(344px)에서 지금 열려 있는 화면을 훑는다 (T-043 · 2026-08-15). 왜 헬퍼인가: 사용자가 요청한 커버 화면 최적화에서 홈만 재고 나머지는 「안 봤다」 로 남았다. 화면마다 손으로 측정을 쓰면 항목이 갈라지므로, 재는 것을 한 곳에서 정한다 (§7 2층). 재는 것(`ui-respo… |
| `scripts/verify-editor-live.mjs` | internal | async function | `closeButtonTarget(selector)` | page.evaluate, document.querySelector, n.getBoundingClientRect, getComputedStyle, Math.abs, parseFloat, g, document.elementFromPoint(cx + dx, cy + dy)?.closest, document.elementFromPoint, n.contains | 닫기 버튼의 누를 수 있는 넓이 와 그 영역이 덮는 다른 조작 요소 를 함께 잰다(T-041). 🔴 둘을 함께 재는 것이 계약이다. 넓히기만 재면 이웃의 터치를 훔치면서 통과하고, 덮음만 재면 아무것도 안 넓히고 통과한다 — M-0162에서 전자를 실제로 만들었다. 셋(`.pe-close`·`.map-close`·`.… |
| `scripts/verify-editor-live.mjs` | internal | function const | `defaultReverseRoute(route)` | route.fulfill, JSON.stringify | — |
| `scripts/verify-editor-live.mjs` | internal | const | `errors` | — | — |
| `scripts/verify-editor-live.mjs` | internal | async function | `createTripFromModal(title, start = '', end = '')` | page.locator('.trip-form .btn-primary').click, page.locator, modal.waitFor, modal.locator('input').first().fill, modal.locator('input').first, modal.locator, modal.locator('input[type="date"]').nth(0).fill, modal.locator('input[type="date"]').nth, modal.locator('input[type="date"]').nth(1).fill, modal.locator('button[type="submit"]').click | — |
| `scripts/verify-editor-live.mjs` | internal | const | `delN0` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `delN1` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `delN2` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `titleBadgeOrder` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bpStages` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bpScore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bpTrip` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `dmVisual` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `fxOpts` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `fxLabels` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noFlag` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `fxDefault` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `fxSaved` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `prSeed` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `prRows` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `prFiltered` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `prCountText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `ambSwap` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `ambWhy` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `beforeSwap` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `afterSwap` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `afterSwapBtn` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `clearSwap` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `backupPanelText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `backupShellContext` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `backupShellPage` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `canonicalText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `canonicalConfirm` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `warningStatus` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `r2Steps` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `r2Danger` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `r2Text` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `probeText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mcSteps` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mcBadge` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mcBadgeN` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mcGates` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mcCatNames` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `seoulPicker` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mapCloseTarget` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function const | `uzReverseRoute(route)` | route.fulfill, JSON.stringify | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tomtomBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tashkentPicker` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function | `withExifDateTime(jpegBytes, dt)` | Buffer.from, ascii, Buffer.alloc, b.writeUInt16BE, b.writeUInt32BE, tiff.push, u16, u32, Buffer.concat, src.subarray | EXIF `DateTimeOriginal`을 실제로 품은 JPEG 을 만든다. 캔버스가 만든 JPEG에는 EXIF가 없다. 그래서 "사진에서 시각을 읽는다"를 라이브에서 잴 방법이 없었고, 실제로 그 자리에 옛 전제를 못박은 검사 가 하나 있었다(EXIF 없는 파일인데 사진 근거가 나오길 기대했다 — 파일 수정시각으로 … |
| `scripts/verify-editor-live.mjs` | internal | function | `withExifGps(jpegBytes, dt, lat, lng)` | Buffer.alloc, b.writeUInt16BE, b.writeUInt32BE, Buffer.from, Math.abs, Math.floor, Math.round, Buffer.concat, u32, u16 | EXIF GPS를 품은 JPEG 을 만든다 (2026-07-30 · 사용자 제안 「사진에서 장소를 가져오자」). 왜 필요한가: 캔버스 JPEG에는 GPS가 없다. 픽스처 없이 이 기능을 라이브로 재려면 DOM에 값을 주입 해야 하는데, 그러면 재는 것이 앱의 EXIF 파서 가 아니라 내 주입 이 된다 — 공허한 검사다(… |
| `scripts/verify-editor-live.mjs` | internal | const | `imgBuf` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `buf` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `peCloseTarget` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `valTexts` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `origPressed` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `sticky` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `actionsSticky` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `skipAll` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `before` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bright` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `valAfter` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `origAfter` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `after` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `cmp` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `back` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `undoEnabled` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `valUndo` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pxUndo` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `undoDisabled` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `dotShown` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `dialogSeen` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `stillOpen` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `thumbs` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `firstThumb` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `counterInit` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `counterNext` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `visiblePhotoBytes` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `photoDownloadStarted` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `photoDownload` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `photoDownloadPath` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `savedPhotoBytes` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `beforeViewerViewport` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `narrowViewer` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `counterWrap` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `vbox` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zoomedCls` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zoomedScale` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `resetScale` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tallBuf` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `clip` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function const | `previewPixel()` | page.$eval, c.getContext('2d').getImageData(1, 1, 1, 1).data.join, c.getContext('2d').getImageData, c.getContext | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zoomBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pxBeforeWheel` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `cbox` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zoomAfterWheel` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pxBeforePinch` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zoomAfterPinch` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pxPrePersp` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `quadShown` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `perspBarShown` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `polyBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `polyAfter` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `edgeBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `edgeTouch` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `quadCdp` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function const | `quadTouch(type, x, y)` | quadCdp.send | — |
| `scripts/verify-editor-live.mjs` | internal | const | `edgeAfter` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function const | `close(a, b, tolerance = 0.2)` | Math.abs | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bottomDx2` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bottomDy2` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bottomDx3` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bottomDy3` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `edgeMovedTogether` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `quadGone` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pxPersp` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pxUndoPersp` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `wideBuf` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `fit` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `injected` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `approxText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `expandedBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `detail` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `expandedAfter` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `hiddenAgain` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `clearBeforeGps` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `fromPhoto` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `firstGpsPreview` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `keptPlace` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `t035` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `lrSeed` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `offBox` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function const | `offBoxWatch(req)` | req.url, u.startsWith, offBox.push | — |
| `scripts/verify-editor-live.mjs` | internal | const | `lrTyped` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `lrMiss` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `lrCoord` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `lrPicked` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noExif` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `acceptBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `chooserOpened` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `acceptAfter` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `hereApplied` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `hereDenied` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `timeOnly` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `probe` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `consented` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `namedFromPhoto` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noteField` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `placeCleared` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `moneyChip` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noteCleared` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zoneSug` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `afterApply` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `editCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `addRowAlignment` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `beforeEdit` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `editFilled` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coordCopy` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coordText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `copiedCoord` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `keptCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `kept2` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noGpsCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noGpsToast` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `toastFit` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coordOnly` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function const | `openEditPanel()` | page.evaluate, document.querySelector('[data-zone-input]')?.closest, document.querySelector, page.locator('.hero-edit').first().click, page.locator('.hero-edit').first, page.locator, settle | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zoneUi` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zonePick` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `zoneRestored` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `homeZoneBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `clock0` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `afterFix` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `homePrev` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noFreeText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `preview` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `clock1` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `restored` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `audioSeed` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `chipGeo` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `widthBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `playState` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `afterEnd` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `placeSeed` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `seededCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `seededChip` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `seededMapButton` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `chipIsButton` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `ext` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mapProviderSeen` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `openedAll` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `g` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `y` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `nv` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `kk` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tomtomChipSeed` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tomtomSeededCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tomtomChipButton` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tomtomChipBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tomtomChipProvider` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `memoryFields` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coverViewport` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coverCreateActions` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `emotionButton` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `emotionOn` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `emotionOff` | — | — |
| `scripts/verify-editor-live.mjs` | internal | async function | `layoutAt(w, h)` | page.setViewportSize, settle, page.evaluate, window.scrollTo, document.querySelector('.detail-compose')?.getBoundingClien…, document.querySelector, document.querySelector('.timeline-wrap')?.getBoundingClient…, Math.abs, Math.round, document.querySelector('.screen-detail').getBoundingClientR… | — |
| `scripts/verify-editor-live.mjs` | internal | const | `wide` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `edge` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `on` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `narrow` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noOverlap` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noteBox` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mobileSyncFeedback` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `TREE_FIXTURE_IDS` | — | — |
| `scripts/verify-editor-live.mjs` | internal | async function | `seedTreeFixtures(targetPage)` | targetPage.evaluate, new Date().toISOString, indexedDB.open, req.result.transaction, tx.objectStore, st.put, resolve, reject | — |
| `scripts/verify-editor-live.mjs` | internal | async function | `homeTreeAt(w, h, targetPage = page)` | targetPage.setViewportSize, settle, targetPage.evaluate, window.scrollTo, document.querySelector, fold?.querySelector, fold?.getBoundingClientRect, main?.getBoundingClientRect, Boolean, Math.abs | — |
| `scripts/verify-editor-live.mjs` | internal | const | `treeWide` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noteAct` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noteAct2` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `jumped` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `expectTool` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `hub` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `toolIds` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tool` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `summaryTxt` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `store` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `rename` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `renamed` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `reverted` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bulkTrashPurge` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bulkTrashConfirm` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `trashText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `trashNote` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `aboutTxt` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `when0` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pick0` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `whenNoExif` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `whenExif` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `kept` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pick1` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `pick2` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `placeClear` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `grades` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `source` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `road` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bldg` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noneChips` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `opened` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `afterPick` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `afterPoint` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `savedRow` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `afterSaved` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coordSeen` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coordBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `swapExists` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coordAfter` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `linkSeen` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `noneText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `revBadge` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `keptName` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `dropCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `dropBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `dropUi` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `dropAfter` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `singleDropMomentBefore` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `singleCloseTarget` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `singleComposerOpened` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `singleDropSaved` | — | — |
| `scripts/verify-editor-live.mjs` | internal | function const | `clickGuideCard(needle)` | page .waitForFunction( (t) => [...document.querySelectorAll…, page .waitForFunction, [...document.querySelectorAll('.guide-card')].some, document.querySelectorAll, c.textContent?.includes, page.evaluate, [...document.querySelectorAll('.guide-card')].find, el.click | 🔴 가이드 카드 하나를 실제로 열고, 열린 것을 확인한 뒤 돌려준다 (T-014 · 2026-08-06). ── 왜 이 함수가 생겼나 ───────────────────────────────────────────────────── 같은 커밋에서 348/348과 341/348이 번갈아 나왔다. 원인은 「측정이 이르다」가… |
| `scripts/verify-editor-live.mjs` | internal | function const | `openGuideCard(label)` | page.goto, page.click, clickGuideCard, page .waitForFunction( () => (document.querySelector('.guid…, page .waitForFunction, document.querySelector, check | — |
| `scripts/verify-editor-live.mjs` | internal | const | `installerGuide` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `mapGuide` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `windowsAuthGuide` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `plat` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `headM` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `PLACE_RECORD_IDS` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `placeRecordSeed` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `unlinkedText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `orphanMomentCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `bulkAddressButton` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `emptyPlaceButton` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `unusedSelectAll` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `recordModalText` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `modalMediaOrder` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `unusedDelete` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `targetCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `linkedNameReadBack` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tinyVideoDataUrl` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `tinyVideoBuffer` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `videoCard` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `phoneAttachmentActions` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `coverAttachmentActions` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `desktopAttachmentActions` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `videoReadBack` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `videoViewer` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `visibleVideoBytes` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `videoDownloadStarted` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `videoDownload` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `videoDownloadPath` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `savedVideoBytes` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `headerSeeded` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `targetSweep` | — | — |
| `scripts/verify-editor-live.mjs` | internal | const | `fails` | — | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `ROOT` | — | — |
| `scripts/verify-sync-release-live.mjs` | export | function | `liveContractProblems(contract, capability)` | Number.isFinite, problems.push, String, Array.isArray, expected.requiredOps.filter, ops.includes, missing.join, String(capability?.sourceSha256 ?? '없음').slice, expected.sourceSha256.slice | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `url` | — | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `key` | — | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `controller` | — | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `timer` | — | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `response` | — | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `capability` | — | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `contract` | — | — |
| `scripts/verify-sync-release-live.mjs` | internal | const | `problems` | — | — |
| `vite.config.ts` | export | const | `BASE` | — | — |

## 5. 자동 검증 연결

- 이 모듈 파일을 정적으로 import하는 유닛 테스트가 관측되지 않았다. 이는 미검증 판정이 아니라 **직접 import 증거 없음**이다.

## 6. 복구 시 먼저 볼 큰 파일

- `scripts/verify-editor-live.mjs` — 5888줄
- `package-lock.json` — 2081줄
- `scripts/verify-diagnostics-live.mjs` — 1210줄
- `scripts/check-verdict-symmetry.mjs` — 833줄
- `scripts/brief.mjs` — 635줄
- `scripts/check-gate-control.mjs` — 573줄
- `scripts/module-design-docs-lib.mjs` — 556줄
- `schemas/release-profile.json` — 541줄
- `scripts/check-migration-grants.mjs` — 482줄
- `scripts/check-screen-lifecycle.mjs` — 329줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
