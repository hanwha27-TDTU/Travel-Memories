---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 사진편집기 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

비파괴 편집 상태, 픽셀 연산, 브러시·필터 UI와 편집 결과 렌더를 소유한다.

- 모듈 ID: `photo-editor`
- 관측 파일: 3개 · 논리 줄: 1800 · 최상위 선언: 65개(외부 공개 44개)
- 코드 내용 SHA-256: `093f2dbf83b9f2c06cffe8c77c897d9f3ce9a7c2aa8adc006aeb94223aa75d86`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/media/editor-core.ts`

### 다른 모듈로 나가는 정적 의존

- 화면·오버레이·상호작용(`ui-shell`): import/re-export 1건

### 외부 패키지/런타임 의존

- Node 내장 또는 저장소 내부 모듈만 정적으로 import한다.

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/media/editor-core.ts` | 455 | 31/32 | src/media/pixelops.ts | DOM interaction |
| `src/media/pixelops.ts` | 292 | 10/10 | — | — |
| `src/ui/photoEditor.ts` | 1053 | 3/23 | src/ui/dom.ts<br>src/media/editor-core.ts<br>src/media/pixelops.ts | file/blob bytes; deletion lifecycle; DOM interaction |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/media/editor-core.ts` | export | typealias | `CropAspect` | — | — |
| `src/media/editor-core.ts` | export | interface | `FreeCrop` | — | 자유 크롭 사각형(회전 후 기하 공간, 0..1 정규화). |
| `src/media/editor-core.ts` | export | interface | `HealPoint` | — | 잡티 제거 지점(기하 공간 정규화 좌표 + 반경은 이미지 너비 비율). |
| `src/media/editor-core.ts` | export | interface | `QuadPoint` | — | 원근 보정 모서리점(회전 후 기하 공간, 0..1 정규화). |
| `src/media/editor-core.ts` | export | typealias | `Quad` | — | 원근 보정 사각형 — TL, TR, BR, BL 순서 고정. 이 사다리꼴을 직사각형으로 편다. |
| `src/media/editor-core.ts` | export | typealias | `QuadEdge` | — | — |
| `src/media/editor-core.ts` | export | interface | `EditState` | — | — |
| `src/media/editor-core.ts` | export | const | `DEFAULT_EDIT: EditState` | — | — |
| `src/media/editor-core.ts` | export | function | `freshEdit(): EditState` | — | 초기 상태 사본(배열 공유 방지). |
| `src/media/editor-core.ts` | export | const | `PRESETS: Record<string, Partial<EditState>>` | — | 절제된 프리셋(사진 원색 존중 — 감성 과잉 금지). |
| `src/media/editor-core.ts` | export | function | `isIdentity(s: EditState): boolean` | isNoAdjust | — |
| `src/media/editor-core.ts` | export | function | `aspectRatio(aspect: CropAspect, srcW: number, srcH: number): number` | — | — |
| `src/media/editor-core.ts` | export | function | `rotateHeals90(heals: HealPoint[]): HealPoint[]` | heals.map | 회전(90° CW)·반전 시 정규화 좌표 변환 — 기존 잡티·자유크롭 위치를 보존한다. |
| `src/media/editor-core.ts` | export | function | `flipHealsH(heals: HealPoint[]): HealPoint[]` | heals.map | — |
| `src/media/editor-core.ts` | export | function | `rotateFreeCrop90(fc: FreeCrop): FreeCrop` | — | — |
| `src/media/editor-core.ts` | export | function | `flipFreeCropH(fc: FreeCrop): FreeCrop` | — | — |
| `src/media/editor-core.ts` | export | function | `rotateQuad90(q: Quad): Quad` | r | 90° CW 회전 시 quad 변환 — 점 회전 후 TL·TR·BR·BL 의미가 유지되게 순서도 재배열. |
| `src/media/editor-core.ts` | export | function | `flipQuadH(q: Quad): Quad` | f | 좌우 반전 시 quad 변환 — TL↔TR, BL↔BR. |
| `src/media/editor-core.ts` | export | function | `moveQuadEdge(q: Quad, edge: QuadEdge, dx: number, dy: number): Quad` | Math.max, Math.min, q.map | 원근 보정의 한 변을 통째로 평행 이동한다. edge는 위·오른쪽·아래·왼쪽(0..3)이며, 두 끝점 중 하나라도 화면 밖으로 나가지 않는 범위에서 dx/dy를 함께 클램프한다. 선분 모양은 유지되고 인접한 두 변만 따라 움직인다. |
| `src/media/editor-core.ts` | export | function | `quadOutputDims(q: Quad, W: number, H: number): { w: number; h: number }` | Math.hypot, d, Math.max, Math.round | 원근 보정 출력 크기(px): 마주보는 변 길이의 평균으로 대상 직사각형을 정한다 (문서 스캐너 관례 — 실물 비율에 근사). |
| `src/media/editor-core.ts` | export | function | `squareToQuadCoeffs(q: Quad, W: number, H: number): number[]` | Math.abs | 단위 정사각형(0..1)² → quad(px 공간) 사영 변환 계수 [a..h] (Heckbert 방식). 출력 픽셀 (s,t) → 원본 px: x=(a·s+b·t+c)/(g·s+h·t+1), y=(d·s+e·t+f)/(g·s+h·t+1). 이것이 곧 "펴기"의 역매핑이다(출력에서 원본을 찾아 샘플). |
| `src/media/editor-core.ts` | export | typealias | `FreeCropDragMode` | — | — |
| `src/media/editor-core.ts` | export | function | `resizeFreeCrop(f: FreeCrop, mode: FreeCropDragMode, dx: number, dy: number, minSize: number): FreeCrop` | Math.max, Math.min, mode.includes | 자유 크롭 드래그(순수): 시작 사각형 f를 mode 방향으로 (dx,dy)만큼 끌었을 때의 새 사각형. 모서리 리사이즈에서 반대쪽 변은 절대 움직이지 않는다 — 경계 클램프를 x/w가 아니라 좌·우(상·하) "변" 좌표에 각각 적용해, 서쪽 핸들을 경계 밖으로 끌면 동쪽 변이 따라 늘어나던 결함을 원천 차단한다. |
| `src/media/editor-core.ts` | export | function | `resolveWindow(W: number, H: number, s: Pick<EditState, 'aspect' \\| 'zoom' \\| 'panX' \\| 'panY' \\| 'freeCrop'>): { x: number; y: number; w: number; h: number }` | cropWindow | 현재 상태의 크롭 창(회전 후 기하 공간 px). 자유 크롭이 있으면 그것을 우선. |
| `src/media/editor-core.ts` | export | function | `rotatedDims(w: number, h: number, rotate90: number): { w: number; h: number }` | — | 회전(90 단위) 후 원본 축 크기. |
| `src/media/editor-core.ts` | export | function | `angleCoverScale(w: number, h: number, angleDeg: number): number` | Math.abs, Math.max, Math.cos, Math.sin | 각도 보정 시 빈 모서리가 없도록 하는 확대 배율. 근사식: cos\\|θ\\| + sin\\|θ\\|·max(가로세로비, 세로가로비). |
| `src/media/editor-core.ts` | export | function | `cropWindow(W: number, H: number, aspect: CropAspect, zoom: number, panX: number, panY: number): { x: number; y: number; w: number; h: number }` | aspectRatio | 크롭 창 계산(순수): 회전 후 이미지(W,H) 안에서 aspect·zoom·pan에 따른 소스 사각형. |
| `src/media/editor-core.ts` | export | function | `geoDims(srcW: number, srcH: number, s: Pick<EditState, 'rotate90' \\| 'quad'>): { w: number; h: number }` | rotatedDims, quadOutputDims | 기하 공간(gd) 크기 — 좌표 계약의 단일 진실원. `heals`·`freeCrop`의 0..1 좌표가 어느 공간의 것인지를 정하는 함수다. `quad`(원근 펴기)가 있으면 펴기까지 적용된 크기이고, 없으면 회전 후 크기(rd)와 같다. ⚠️ 이 함수가 생긴 이유(2026-07-27 이식 명세 작성 중 발견): ba… |
| `src/media/editor-core.ts` | export | function | `screenToGeo(u: number, v: number, srcW: number, srcH: number, s: EditState, brushPct?: number): { x: number; y: number; r: number }` | geoDims, resolveWindow | 미리보기 화면의 상대 좌표(u,v ∈ 0..1) → 기하 공간 정규화 좌표 . 미리보기는 크롭 창을 꽉 채워 그리므로, 화면 비율을 창 안 위치로 되돌린 뒤 gd로 정규화한다. bake의 heal 재투영(`(hp.x gd.w - win.x)/win.w outW`)의 정확한 역함수 다 — 그래서 두 식은 반드시 같은 `g… |
| `src/media/editor-core.ts` | internal | function | `mulberry32(seed: number): () => number` | Math.imul | 결정적 PRNG(mulberry32). 그레인이 미리보기 갱신마다 어른거리지 않게 고정 시드로 쓴다. |
| `src/media/editor-core.ts` | export | function | `bakeToCanvas(src: ImageBitmap \\| HTMLImageElement \\| HTMLCanvasElement, srcW: number, srcH: number, s: EditState, maxEdge: number): HTMLCanvasElement` | rotatedDims, geoDims, resolveWindow, Math.min, Math.max, document.createElement, Math.round, g.getContext, gx.translate, gx.rotate | 편집 상태를 적용해 새 canvas를 만든다(원본 불변). maxEdge: 출력 긴 변 제한(미리보기 소형 / 저장 대형). |
| `src/media/editor-core.ts` | export | function | `autoHealPoints(data: Uint8ClampedArray, cw: number, ch: number, srcW: number, srcH: number, s: EditState): HealPoint[]` | detectBlemishes(data, cw, ch).map, detectBlemishes, screenToGeo | 자동 잡티 — 미리보기 픽셀에서 얼룩을 찾아 상태 좌표(gd 0..1)의 heal 점 으로 돌려준다. 🔴 화면→상태 변환을 여기서 `screenToGeo`로 지나게 한 이유: 수동 탭과 같은 함수 를 써야 자동과 수동이 같은 자리에 찍힌다. 두 곳에 손으로 구현하면 드리프트는 시간 문제다(§7 2층). 이 파일의 §4… |
| `src/media/pixelops.ts` | export | interface | `ColorAdjust` | — | — |
| `src/media/pixelops.ts` | export | const | `NO_ADJUST: ColorAdjust` | — | — |
| `src/media/pixelops.ts` | export | function | `isNoAdjust(a: ColorAdjust): boolean` | — | — |
| `src/media/pixelops.ts` | export | function | `applyColorAdjust(data: Uint8ClampedArray, adj: ColorAdjust): void` | isNoAdjust, Math.pow | RGBA 배열에 색 조정을 제자리 적용. |
| `src/media/pixelops.ts` | export | function | `sharpen(data: Uint8ClampedArray, width: number, height: number, amount: number): Uint8ClampedArray` | — | 언샤프 마스크(3×3 블러와의 차이를 amount만큼 증폭). amount 0..1. 별도 출력 버퍼를 반환한다(입력 불변). |
| `src/media/pixelops.ts` | export | function | `healSpot(data: Uint8ClampedArray, width: number, height: number, cx: number, cy: number, r: number): void` | Math.min, Math.max, Math.round, Math.cos, Math.sin, ring.push, Math.floor, Math.ceil, Math.hypot | 스팟 힐링(잡티 제거): (cx,cy) 반경 r 안을 주변 링 샘플로 자연스럽게 메꾼다. 링(r×1.25)에서 16방향 샘플을 뽑아 역거리제곱 가중 보간 + 가장자리 감쇠 블렌딩. 제자리 수정(순수 픽셀 연산 — DOM 없음). |
| `src/media/pixelops.ts` | export | function | `grain(data: Uint8ClampedArray, amount: number, rand: () => number = Math.random): void` | rand | 필름 그레인(모노 노이즈 가산). amount 0..1. rand 주입 가능(테스트 결정성). |
| `src/media/pixelops.ts` | export | function | `warpPerspective(src: Uint8ClampedArray, sw: number, sh: number, dw: number, dh: number, coeffs: number[]): Uint8ClampedArray<ArrayBuffer>` | Math.floor | 원근 워프(역매핑 + bilinear): 출력 (dw×dh)의 각 픽셀 (s,t)∈(0..1)²를 coeffs(단위정사각형→원본 px 사영계수 [a..h])로 원본 좌표에 투영해 샘플한다. 순수 함수 — 출력 크기에서만 루프(§2-4), 원본 배열 불변. 경계 밖은 가장자리 클램프. |
| `src/media/pixelops.ts` | export | interface | `DetectedSpot` | — | 자동 잡티 감지 결과 — 0..1 정규화 좌표(감지한 캔버스 기준)와 반경(폭 대비 비율). |
| `src/media/pixelops.ts` | export | function | `detectBlemishes(data: Uint8ClampedArray, width: number, height: number, opts: { maxSpots?: number; strength?: number } = {}): DetectedSpot[]` | Math.min, Math.max, Math.round, Math.hypot, lum, Math.sqrt, stat, found.push, found.sort, kept.some | 자동 잡티 감지 — 주변보다 어두운 작은 얼룩 을 찾는다. ── 왜 「어두운 것만」인가 ──────────────────────────────────────────────────── 잡티·점·먼지는 주변 피부·배경보다 어둡다. 밝은 쪽까지 잡으면 하이라이트·반사·치아 가 걸려 사람 얼굴이 뭉개진다. 한쪽만 보는 것은 성… |
| `src/ui/photoEditor.ts` | internal | const | `PREVIEW_MAX` | — | — |
| `src/ui/photoEditor.ts` | internal | const | `FAST_MAX` | — | — |
| `src/ui/photoEditor.ts` | internal | const | `OUTPUT_MAX` | — | — |
| `src/ui/photoEditor.ts` | internal | const | `MIN_CROP` | — | — |
| `src/ui/photoEditor.ts` | internal | interface | `SliderSpec` | — | — |
| `src/ui/photoEditor.ts` | internal | const | `SLIDERS: SliderSpec[]` | — | — |
| `src/ui/photoEditor.ts` | internal | const | `ASPECTS: { key: CropAspect; label: string }[]` | — | — |
| `src/ui/photoEditor.ts` | internal | const | `COLOR_ZERO` | — | 프리셋이 관리하는 색·효과 값(0 = 변화 없음). 프리셋 적용·원본 비교의 공통 초기값. |
| `src/ui/photoEditor.ts` | internal | async function | `decodeBitmap(file: Blob): Promise<{ bmp: ImageBitmap \\| HTMLImageElement; w: number; h: number }>` | createImageBitmap, URL.createObjectURL, img.decode, URL.revokeObjectURL | — |
| `src/ui/photoEditor.ts` | export | interface | `EditorResult` | — | — |
| `src/ui/photoEditor.ts` | export | interface | `EditorOpts` | — | — |
| `src/ui/photoEditor.ts` | internal | function | `cloneState(s: EditState): EditState` | s.heals.map, s.quad.map | — |
| `src/ui/photoEditor.ts` | internal | interface | `PerspectiveOverlay` | — | — |
| `src/ui/photoEditor.ts` | internal | function | `createPerspectiveOverlay(): PerspectiveOverlay` | el, document.createElementNS, svg.setAttribute, svg.appendChild, Array.from, line.setAttribute, String, box.appendChild | — |
| `src/ui/photoEditor.ts` | internal | function | `freshQuadDraft(): Quad` | — | — |
| `src/ui/photoEditor.ts` | internal | function | `syncPerspectiveOverlay(overlay: PerspectiveOverlay, draft: Quad, show: boolean): void` | overlay.polygon.setAttribute, draft.map((p) => `${p.x * 100},${p.y * 100}`).join, draft.map, overlay.edges.forEach, line.setAttribute, String, draft.forEach | — |
| `src/ui/photoEditor.ts` | internal | function | `fmtSliderVal(spec: SliderSpec, v: number): string` | v.toFixed, Math.round, String | 슬라이더 값 표시: 각도는 °, 나머지는 -100..100 정수(0=원본). |
| `src/ui/photoEditor.ts` | internal | function | `readPreviewPixels(preview: HTMLCanvasElement): ImageData \\| null` | preview.getBoundingClientRect, document.createElement, Math.max, Math.round, cv.getContext, ctx.drawImage, ctx.getImageData | 미리보기 캔버스의 현재 픽셀. 자동 보정이 「지금 화면에 보이는 것」을 재게 한다(WYSIWYG). |
| `src/ui/photoEditor.ts` | internal | function | `makeFitMode(fitBtn: HTMLButtonElement, stage: HTMLElement, canvasWrap: HTMLElement, store: (m: 'contain' \\| 'width') => void, repaint: () => void): (on: boolean) => void` | store, fitBtn.setAttribute, String, stage.classList.toggle, canvasWrap.classList.toggle, repaint | 표시 방식(높이 맞춤 ↔ 폭 채우기) 전환기를 만든다 — 바꾸는 길은 이 하나뿐 이다. 🔴 예전엔 이 로직이 클릭 핸들러 안에 손으로 박혀 있었다. 그래서 [초기화]가 재사용할 수 없었고, `fitMode`는 `EditState` 밖 변수라 `Object.assign(state, DEFAULT_EDIT)`도 못 건드렸… |
| `src/ui/photoEditor.ts` | internal | function | `makeBrushSlider(initial: number, onChange: (v: number) => void): HTMLLabelElement` | el, String, brush.setAttribute, brush.addEventListener, onChange, Number, wrap.append | 잡티 브러시 크기 슬라이더. 값 변경만 바깥에 알린다(표시 로직은 여기 가둔다). |
| `src/ui/photoEditor.ts` | internal | function | `makeAutoHealButton(d: { preview: HTMLCanvasElement; healHint: HTMLElement; state: EditState; srcW:…): HTMLButtonElement` | el, btn.addEventListener, readPreviewPixels, autoHealPoints, d.pushHistory, d.state.heals.push, d.repaint | 「✨ 자동 보정」 버튼 — 어두운 작은 얼룩을 찾아 한 번에 지운다. 🔴 셋을 함께 지킨다. ①실행 직전 이력을 남겨 ↺ 한 번으로 전부 원복되게 하고 ②결과를 문장으로 말한다 (조용히 아무 일도 안 일어나면 사용자는 고장으로 읽는다 · §8) ③찾은 게 없으면 그 사실도 말한다 — 「없음」과 「안 해봄」을 같은 침묵… |
| `src/ui/photoEditor.ts` | internal | function | `makeDiscardButton(onDiscard: () => void): HTMLButtonElement` | el, btn.addEventListener, confirm, onDiscard | 「이 사진 버리기」 버튼. 🔴 되돌릴 수 없는 방향이라 묻는다 (닫기 보호와 같은 자세). 그리고 문장이 반드시 「기기의 원본 파일은 지워지지 않는다」 를 말한다 — 이 앱에서 「버린다」는 낱말은 사용자에게 원본 삭제로 읽힐 수 있고, 그건 §0이 절대 금지하는 일이다. |
| `src/ui/photoEditor.ts` | export | async function | `openPhotoEditor(file: Blob, fileLabel: string, opts: EditorOpts = {}): Promise<EditorResult>` | decodeBitmap, cloneState, freshEdit, el, overlay.setAttribute, overlay.appendChild, head.appendChild, undoAllBtn.setAttribute, undoAllBtn.addEventListener, undoLast | — |

## 5. 자동 검증 연결

- `tests/unit/contractGaps.test.ts`
- `tests/unit/detect-blemishes.test.ts`
- `tests/unit/perspective.test.ts`
- `tests/unit/pixelops.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/ui/photoEditor.ts` — 1053줄
- `src/media/editor-core.ts` — 455줄
- `src/media/pixelops.ts` — 292줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
