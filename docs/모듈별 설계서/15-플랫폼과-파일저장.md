---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 플랫폼 런타임·파일 저장·업데이트 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

브라우저/Capacitor 차이, 파일 저장 read-back, 앱·셸 업데이트와 기기 경계를 소유한다.

- 모듈 ID: `platform-runtime`
- 관측 파일: 12개 · 논리 줄: 916 · 최상위 선언: 65개(외부 공개 37개)
- 코드 내용 SHA-256: `cfc133c1426175202230e1cf58e6a008378a8229469068a6bbaed947ded1dd5c`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src-tauri/build.rs`
- `src-tauri/capabilities/main.json`
- `src-tauri/Cargo.toml`
- `src-tauri/src/lib.rs`
- `src-tauri/src/main.rs`
- `src-tauri/tauri.conf.json`
- `src/services/appUpdate.ts`
- `src/services/shellUpdate.ts`

### 다른 모듈로 나가는 정적 의존

- 앱 셸과 라우팅(`app-shell`): import/re-export 2건
- 화면·오버레이·상호작용(`ui-shell`): import/re-export 1건

### 외부 패키지/런타임 의존

- `@tauri-apps/api/core`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src-tauri/build.rs` | 4 | 0/0 | — | — |
| `src-tauri/capabilities/main.json` | 19 | 0/0 | — | — |
| `src-tauri/Cargo.toml` | 21 | 0/0 | — | — |
| `src-tauri/src/lib.rs` | 19 | 0/0 | — | — |
| `src-tauri/src/main.rs` | 4 | 0/0 | — | — |
| `src-tauri/tauri.conf.json` | 45 | 0/0 | — | — |
| `src/services/appUpdate.ts` | 148 | 6/9 | src/app/registry.gen.ts | network fetch; DOM interaction; browser key/value storage |
| `src/services/capacitorShell.ts` | 113 | 10/10 | src/domain/persistAdvice.ts<br>src/services/nativePlatform.ts | integrity/crypto; platform bridge |
| `src/services/fileSave.ts` | 258 | 10/23 | src/services/capacitorShell.ts | file/blob bytes; integrity/crypto; DOM interaction; platform bridge |
| `src/services/nativePhotos.ts` | 139 | 5/9 | src/services/capacitorShell.ts | file/blob bytes; DOM interaction; platform bridge |
| `src/services/nativePlatform.ts` | 28 | 4/4 | — | platform bridge |
| `src/services/shellUpdate.ts` | 118 | 2/10 | src/services/capacitorShell.ts<br>src/app/apk.ts | network fetch; DOM interaction; platform bridge |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/services/appUpdate.ts` | export | const | `CURRENT_VERSION: string` | — | 실행 중인 번들의 버전. 🔴 `changelog`를 통째로 들이지 않는다 (2026-08-13 · 외부 리뷰 지적). 예전엔 `import { CHANGELOG }` 후 `CHANGELOG[0].version` 한 글자를 읽었는데, 그 파일은 222KB · 2,476줄 이고 이 모듈은 production 시작 경로에서… |
| `src/services/appUpdate.ts` | export | const | `CHECK_MIN_MS` | — | 확인 사이 최소 간격 — 복귀 연타가 네트워크를 두드리지 않게. |
| `src/services/appUpdate.ts` | export | function | `isNewer(latest: string, current: string): boolean` | v.split('.').map, v.split, Number.parseInt, parse, Math.max | `1.47` vs `1.46` — 점 구분 숫자 비교(자릿수 달라도 안전). 순수: 유닛이 직접 돌린다. |
| `src/services/appUpdate.ts` | export | function | `shouldCheck(lastAt: number \\| null, now: number): boolean` | — | 지금 확인해도 되나(스로틀). 순수. |
| `src/services/appUpdate.ts` | export | function | `safeToReload(hasOverlay: boolean, typedSinceSafePoint: boolean): boolean` | — | 지금 새로고침해도 안전한가. 순수(인자로 받은 사실만 본다). — 열린 창(overlay)이 있으면 안 된다: 백업 진행·편집기 등 중간 상태가 산다. — 지난 안전지점(화면 이동) 이후 입력이 있었으면 안 된다: 아직 저장 안 된 글일 수 있다. |
| `src/services/appUpdate.ts` | internal | interface | `VersionSignal` | — | — |
| `src/services/appUpdate.ts` | internal | async function | `fetchLatest(base: string): Promise<string \\| null>` | fetch, Date.now, res.json | 배포된 신호 파일을 묻는다. 실패(오프라인·404)는 null — 갱신은 속도지 기능이 아니다. |
| `src/services/appUpdate.ts` | internal | const | `ATTEMPT_KEY` | — | 같은 버전으로 두 번 새로고침하지 않기 위한 표식(세션 한정) — CDN이 늦으면 루프가 된다. |
| `src/services/appUpdate.ts` | export | function | `wireAutoUpdate(): void` | safeToReload, document.querySelector, sessionStorage.getItem, sessionStorage.setItem, location.reload, Date.now, shouldCheck, tryApply, fetchLatest(import.meta.env.BASE_URL).then, fetchLatest | 자동 갱신을 배선한다(main.ts에서 1회). 시점: ① 시작 직후 ② 화면 복귀(visibilitychange — 미뤄둔 적용). 적용: 안전하면 즉시 location.reload(), 아니면 pending으로 들고 다음 시점에 다시 시도. |
| `src/services/capacitorShell.ts` | export | typealias | `ShellState` | — | — |
| `src/services/capacitorShell.ts` | export | function | `shellPlugin(name: string): T \\| null` | capacitorBridge | 셸 안에서만 플러그인을 돌려준다. 크롬에서는 언제나 null. |
| `src/services/capacitorShell.ts` | export | function | `shellState(): ShellState` | capacitorBridge | 어느 문인가 — 🔬 관측 창의 「경로」 줄(M-0069)과 로그인 리다이렉트 분기가 읽는다. 기준 플러그인은 OriginalPhotos다: 셸의 존재 이유가 그 문이므로(ADR-0036), 셸인데 그 문이 없으면 「옛 APK」(shell-no-plugin)로 적는다 — 재설치가 답인 상태다. |
| `src/services/capacitorShell.ts` | export | function | `rendererHeapSizeLimit(): number \\| undefined` | Number.isFinite | Chromium/WebView가 공개하는 renderer JS heap 한도. 없으면 추측하지 않고 undefined. |
| `src/services/capacitorShell.ts` | export | interface | `IconSwitcher` | — | 🎨 런처 아이콘 전환기 (ADR-0038) — 셸에만 있다. 웹/PWA에서는 `null`(설치 시 아이콘이 고정되므로 원리적으로 불가). 화면은 이 값이 null이면 아이콘 섹션 자체를 그리지 않는다 — 크롬에서 「안 되는 버튼」을 보여주지 않는다(§5). |
| `src/services/capacitorShell.ts` | export | function | `iconSwitcher(): IconSwitcher \\| null` | shellPlugin | — |
| `src/services/capacitorShell.ts` | export | interface | `BackupFileWriter` | — | Android에 큰 앱 파일을 청크로 쓰고, 닫은 뒤 다시 읽어 검증하는 셸 문. wire 이름은 구형 APK 호환 때문에 유지한다. |
| `src/services/capacitorShell.ts` | export | function | `backupFileWriter(): BackupFileWriter \\| null` | shellPlugin | — |
| `src/services/capacitorShell.ts` | export | async function | `androidRuntimeMaxMemory(): Promise<number \\| undefined>` | backupFileWriter, read, Number.isFinite | Read the native Android process budget; an absent/old bridge stays unknown. |
| `src/services/capacitorShell.ts` | export | function | `persistSurface(): PersistSurface` | nativePlatform, shellState, window.matchMedia | 🔎 지금 어느 문으로 실행 중인가 — 셸/설치된 PWA/브라우저 탭(T-005). 왜 여기인가: 셸 감지가 이미 이 파일의 SSOT다(머리말). 「설치됐는가」를 다른 파일에서 따로 읽기 시작하면 감지 규칙이 손으로 두 벌이 되고, 그게 M-0060의 형태다 — 같은 판정을 두 번 쓰면 갈라진다. 그래서 표면 판정 전체… |
| `src/services/fileSave.ts` | internal | const | `CHUNK_BYTES` | — | — |
| `src/services/fileSave.ts` | export | typealias | `FileSaveReceipt` | — | — |
| `src/services/fileSave.ts` | export | typealias | `BackupSaveReceipt` | — | 기존 백업 호출부의 공개 타입 이름은 호환을 위해 유지한다. |
| `src/services/fileSave.ts` | export | interface | `FileSaveOptions` | — | — |
| `src/services/fileSave.ts` | internal | interface | `WritableFileHandle` | — | — |
| `src/services/fileSave.ts` | internal | interface | `SavePickerWindow` | — | — |
| `src/services/fileSave.ts` | internal | function | `bytesToBase64(bytes: Uint8Array): string` | String.fromCharCode, bytes.subarray, btoa | — |
| `src/services/fileSave.ts` | internal | async function | `sha256Hex(bytes: ArrayBuffer): Promise<string>` | crypto.subtle.digest, [...digest].map((byte) => byte.toString(16).padStart(2, '0'…, [...digest].map, byte.toString(16).padStart, byte.toString | — |
| `src/services/fileSave.ts` | internal | async function | `exactBlobParity(a: Blob, b: Blob): Promise<boolean>` | Math.min, Promise.all, a.slice(offset, end).arrayBuffer, a.slice, b.slice(offset, end).arrayBuffer, b.slice | — |
| `src/services/fileSave.ts` | internal | async function | `saveWithAndroid(blob: Blob, filename: string, mime: string, destination: 'downloads' \\| 'picker'): Promise<FileSaveReceipt \\| null>` | backupFileWriter, writer.begin, blob.slice(offset, Math.min(blob.size, offset + CHUNK_BYTES…, blob.slice, Math.min, writer.append, bytesToBase64, sha256Hex, writer.finish, writer.abort | — |
| `src/services/fileSave.ts` | internal | function | `extensionOf(filename: string): string` | /\.[a-z0-9]+$/i.exec, match[0]!.toLowerCase | — |
| `src/services/fileSave.ts` | internal | const | `MIME_EXTENSIONS: Readonly<Record<string, readonly string[]>>` | — | — |
| `src/services/fileSave.ts` | internal | function | `assertMimeExtension(mime: string, extension: string): void` | allowed?.includes | — |
| `src/services/fileSave.ts` | internal | async function | `saveWithBrowserPicker(blob: Blob, filename: string, mime: string, description: string): Promise<FileSaveReceipt \\| null>` | extensionOf, picker.call, handle.createWritable, writable.write, writable.close, writable.abort, handle.getFile, exactBlobParity | — |
| `src/services/fileSave.ts` | internal | function | `requestBrowserDownload(blob: Blob, filename: string): FileSaveReceipt` | URL.createObjectURL, document.createElement, document.body.appendChild, anchor.click, anchor.remove, setTimeout, URL.revokeObjectURL | — |
| `src/services/fileSave.ts` | export | async function | `saveFileBlob(blob: Blob, filename: string, options: FileSaveOptions): Promise<FileSaveReceipt>` | extensionOf, options.mime.split(';', 1)[0]?.trim, options.mime.split, blob.type.split(';', 1)[0]?.trim, blob.type.split, assertMimeExtension, saveWithAndroid, saveWithBrowserPicker, requestBrowserDownload | 백업·사진·영상이 공유하는 저장 문. Android와 지원 브라우저는 쓴 파일을 되읽고, 일반 브라우저의 anchor fallback은 저장 요청으로만 판정한다. |
| `src/services/fileSave.ts` | internal | async function | `saveBackupBlobAt(blob: Blob, filename: string, androidDestination: 'downloads' \\| 'picker'): Promise<BackupSaveReceipt>` | filename.endsWith, saveFileBlob | — |
| `src/services/fileSave.ts` | export | async function | `saveBackupBlob(blob: Blob, filename: string): Promise<BackupSaveReceipt>` | saveBackupBlobAt | Android는 취소될 수 있는 선택기 없이 Download/Bugeon Journey에 저장하고, 그 밖의 표면은 가능한 저장 문을 쓴다. |
| `src/services/fileSave.ts` | export | async function | `saveBackupBlobToChosenFolder(blob: Blob, filename: string): Promise<BackupSaveReceipt>` | saveBackupBlobAt | Android에서 사용자가 다른 폴더를 직접 고르고 싶을 때만 SAF 선택기를 연다. |
| `src/services/fileSave.ts` | export | function | `backupSaveMessage(receipt: BackupSaveReceipt): string` | — | — |
| `src/services/fileSave.ts` | export | typealias | `MediaSaveKind` | — | — |
| `src/services/fileSave.ts` | export | function | `mediaSaveMessage(receipt: FileSaveReceipt, kind: MediaSaveKind): string` | — | 뷰어 저장 결과 문장은 호출부에서 조립하지 않고 이 순수 함수 한 곳에서 판정한다. |
| `src/services/fileSave.ts` | export | function | `mediaSaveErrorMessage(kind: MediaSaveKind, error: unknown): string` | error.message.trim | — |
| `src/services/nativePhotos.ts` | internal | interface | `OriginalPhotosBridge` | — | 셸이 주입하는 브리지의 모양 — 플러그인(OriginalPhotosPlugin.java)과 1:1이다. |
| `src/services/nativePhotos.ts` | internal | function | `bridge(): OriginalPhotosBridge \\| null` | shellPlugin | 셸 안인가. 크롬에서는 null → 이 모듈 전체가 조용히 비켜선다. |
| `src/services/nativePhotos.ts` | internal | const | `lastPick` | — | 파일 이름 → 네이티브 문이 말한 사실. 같은 이름을 다시 고르면 최신 것으로 덮인다. |
| `src/services/nativePhotos.ts` | internal | const | `skipped: { name: string; reason: string }[]` | — | 네이티브가 바이트를 못 준 장들 (T-021). 비어 있는 것이 정상 이고, 남아 있으면 그만큼 사진이 안 들어온 것이다 — 개수가 0이라고 말하지 말고 이유를 보여주기 위한 자리다. |
| `src/services/nativePhotos.ts` | export | function | `skippedPicks(): readonly { name: string; reason: string }[]` | — | 이번 선택에서 바이트를 못 받은 장들. 화면이 「왜 안 들어왔는지」를 말할 때 읽는다. |
| `src/services/nativePhotos.ts` | export | function | `pickedVia(name: string): { original: boolean; reason: string } \\| null` | lastPick.get | 이 파일이 네이티브 문을 거쳤다면 그때의 사실을, 아니면 null. null인데 shellState()가 'shell'이면 — 셸인데 시스템 선택기 경로로 들어온 것이다. |
| `src/services/nativePhotos.ts` | export | function | `b64ToFile(name: string, b64: string): File` | atob, bin.charCodeAt | base64 → File. 순수(브라우저 전역만) — 유닛이 직접 돌린다. MIME은 일부러 비워 둔다 : `readPhotoMeta`가 바이트를 직접 판별한다(M-0067 — 선택기의 MIME 주장을 믿지 않는 것이 이 경로의 규율이고, 여기서 지어내면 그 규율을 다시 어긴다). |
| `src/services/nativePhotos.ts` | export | async function | `pickIntoInput(input: HTMLInputElement): Promise<boolean>` | bridge, b.pick, Array.from, dt.items.add, skipped.push, b64ToFile, lastPick.set, input.dispatchEvent | 네이티브 선택기로 고른 사진들을 기존 input에 덧붙이고 change를 쏜다. @returns 셸 밖(크롬)이면 false — 호출부는 아무것도 안 해도 된다(기존 경로가 그대로 돈다). |
| `src/services/nativePhotos.ts` | export | function | `wireNativeIntake(input: HTMLInputElement): void` | input.addEventListener, bridge, e.preventDefault, pickIntoInput | 사진 입력칸을 셸에 잇는다 — 생성 폼·편집 폼이 같은 문 을 지난다(§7). capture 단계에서 가로채는 이유: input.click()은 label 경유·[🖼️ 갤러리에서]의 `wireAltPick` 경유 등 여러 길로 온다. 셸 안에서는 어느 길로 와도 시스템 선택기 대신 네이티브 문이 열려야 한다 — 셸에서… |
| `src/services/nativePlatform.ts` | export | typealias | `NativePlatform` | — | — |
| `src/services/nativePlatform.ts` | export | interface | `CapacitorGlobal` | — | — |
| `src/services/nativePlatform.ts` | export | function | `capacitorBridge(): CapacitorGlobal \\| null` | cap?.isNativePlatform | Android 브리지 원본. 브라우저와 Tauri에서는 null이다. |
| `src/services/nativePlatform.ts` | export | function | `nativePlatform(): NativePlatform` | capacitorBridge, isTauri | 실행 표면 판정. Tauri가 Android 브리지보다 먼저 자신을 가장하지 않는다는 전제를 두지 않는다. |
| `src/services/shellUpdate.ts` | internal | interface | `AppInfoPlugin` | — | 셸 App 플러그인의 최소 모양 — 여기서는 현재 build(versionCode)만 읽는다. |
| `src/services/shellUpdate.ts` | internal | const | `CHECK_MIN_MS` | — | 확인 사이 최소 간격(복귀 연타가 API를 두드리지 않게). 무인증 GitHub API는 시간당 60회. |
| `src/services/shellUpdate.ts` | internal | const | `BANNER_ID` | — | — |
| `src/services/shellUpdate.ts` | internal | const | `dismissedBuild: number \\| null` | — | — |
| `src/services/shellUpdate.ts` | internal | const | `lastCheckAt: number \\| null` | — | — |
| `src/services/shellUpdate.ts` | internal | async function | `currentBuild(): Promise<number \\| null>` | shellPlugin, app.getInfo, Number, Number.isFinite | 지금 설치된 셸의 versionCode. 셸이 아니면(크롬) null. |
| `src/services/shellUpdate.ts` | internal | async function | `remoteBuild(): Promise<number \\| null>` | fetch, res.json, parseShellBuild | 릴리스 설명의 최신 셸 versionCode. 못 읽으면 null(오프라인·마커 없음·차단). |
| `src/services/shellUpdate.ts` | internal | function | `showBanner(build: number): void` | document.getElementById, document.createElement, bar.setAttribute, x.setAttribute, x.addEventListener, bar.remove, bar.append, document.body.appendChild | 배너를 그린다(이미 있으면 다시 만들지 않는다). [업데이트]=외부 다운로드, [✕]=세션 닫기. |
| `src/services/shellUpdate.ts` | export | async function | `checkShellUpdate(): Promise<void>` | currentBuild, Date.now, remoteBuild, showBanner | 한 번 확인해 필요하면 배너를 띄운다. 셸에서만 동작. |
| `src/services/shellUpdate.ts` | export | function | `wireShellUpdate(): void` | shellPlugin, checkShellUpdate, document.addEventListener | main.ts에서 1회 배선 — 시작 + 복귀(visible) 때 확인. 크롬에서는 조용히 비켜선다. |

## 5. 자동 검증 연결

- `tests/unit/appUpdate.test.ts`
- `tests/unit/fileSave.test.ts`
- `tests/unit/nativePhotos.test.ts`
- `tests/unit/nativePlatform.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/services/fileSave.ts` — 258줄
- `src/services/appUpdate.ts` — 148줄
- `src/services/nativePhotos.ts` — 139줄
- `src/services/shellUpdate.ts` — 118줄
- `src/services/capacitorShell.ts` — 113줄
- `src-tauri/tauri.conf.json` — 45줄
- `src/services/nativePlatform.ts` — 28줄
- `src-tauri/Cargo.toml` — 21줄
- `src-tauri/capabilities/main.json` — 19줄
- `src-tauri/src/lib.rs` — 19줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
