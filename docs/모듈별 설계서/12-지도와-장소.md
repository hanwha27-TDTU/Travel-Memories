---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 지도·장소·지오코딩 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

좌표 유효성, 장소 레지스트리, 지도 표시, 외부 지도와 지오코더 경계를 소유한다.

- 모듈 ID: `map-place`
- 관측 파일: 19개 · 논리 줄: 4567 · 최상위 선언: 235개(외부 공개 144개)
- 코드 내용 SHA-256: `142f3b3c72def0561c9e6adcc087754af11f439627f8a9f2cbd9304624954865`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/place/photoProbe.ts`

### 다른 모듈로 나가는 정적 의존

- 화면·오버레이·상호작용(`ui-shell`): import/re-export 4건
- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 2건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 2건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 2건
- 앱 셸과 라우팅(`app-shell`): import/re-export 1건
- 인증·Supabase 클라이언트·동의(`auth-security`): import/re-export 1건

### 외부 패키지/런타임 의존

- `proj4`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/place/coordInput.ts` | 238 | 8/14 | — | — |
| `src/domain/place/externalMap.ts` | 200 | 7/11 | src/domain/place/coordInput.ts | — |
| `src/domain/place/geojson.ts` | 52 | 6/6 | src/domain/place/coordInput.ts | — |
| `src/domain/place/here.ts` | 95 | 4/4 | src/domain/place/precision.ts | — |
| `src/domain/place/jusoMap.ts` | 23 | 2/4 | src/domain/place/mapProvider.ts | — |
| `src/domain/place/mapProvider.ts` | 93 | 7/10 | — | — |
| `src/domain/place/photoHint.ts` | 160 | 5/5 | src/domain/place/coordInput.ts | — |
| `src/domain/place/photoProbe.ts` | 115 | 5/6 | src/media/exif.ts | integrity/crypto |
| `src/domain/place/precision.ts` | 194 | 14/17 | — | — |
| `src/domain/place/provider.ts` | 143 | 11/16 | — | — |
| `src/domain/place/registry.ts` | 273 | 17/18 | src/domain/place/coordInput.ts<br>src/domain/time.ts | transactional persistence; deletion lifecycle |
| `src/services/externalMapConsent.ts` | 41 | 2/3 | src/domain/place/externalMap.ts<br>src/services/consent.ts | browser key/value storage |
| `src/services/geocode.ts` | 429 | 22/32 | src/domain/place/precision.ts<br>src/domain/place/provider.ts | network fetch; Edge Function invoke |
| `src/services/here.ts` | 72 | 2/3 | src/domain/place/here.ts | — |
| `src/services/mapRenderer.ts` | 500 | 4/30 | src/domain/place/precision.ts<br>src/domain/place/mapProvider.ts<br>src/domain/place/jusoMap.ts | DOM interaction |
| `src/services/places.ts` | 683 | 22/29 | src/offline/db.ts<br>src/domain/place/rowmap.ts<br>src/domain/place/provider.ts<br>src/domain/time.ts<br>src/services/geocode.ts<br>src/domain/place/coordInput.ts<br>src/domain/media/order.ts | Dexie: localMedia, localMoments, localPlaces, localTrips; file/blob bytes; transactional persistence; deletion lifecycle |
| `src/ui/externalMapRow.ts` | 78 | 2/2 | src/ui/dom.ts<br>src/domain/place/externalMap.ts<br>src/services/externalMapConsent.ts | DOM interaction |
| `src/ui/screens/mapView.ts` | 439 | 3/12 | src/ui/dom.ts<br>src/domain/place/geojson.ts<br>src/domain/place/externalMap.ts<br>src/domain/place/mapProvider.ts<br>src/services/mapRenderer.ts<br>src/ui/externalMapRow.ts<br>src/services/here.ts<br>src/services/geocode.ts<br>src/domain/place/here.ts | file/blob bytes; DOM interaction |
| `src/ui/screens/placeRegistry.ts` | 739 | 1/13 | src/ui/dom.ts<br>src/services/places.ts<br>src/services/here.ts<br>src/domain/place/here.ts<br>src/domain/place/coordInput.ts<br>src/ui/lazyScreens.ts<br>src/app/router.ts<br>src/domain/place/registry.ts | file/blob bytes; deletion lifecycle; DOM interaction |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/place/coordInput.ts` | export | interface | `ParsedCoord` | — | 좌표 하나를 읽어낸 결과. 읽지 못했으면 호출부가 null을 받는다. |
| `src/domain/place/coordInput.ts` | internal | function const | `isLat(n: number)` | Number.isFinite | — |
| `src/domain/place/coordInput.ts` | internal | function const | `isLng(n: number)` | Number.isFinite | — |
| `src/domain/place/coordInput.ts` | export | function | `isRealCoord(lat: unknown, lng: unknown): boolean` | isLat, isLng | 🔴 「이게 지구 위의 진짜 좌표인가」의 단일 판정 — 이 앱에서 이 질문에 답하는 유일한 함수다. 왜 하나여야 하나(H-3 · 2026-08-01 감사·확정): 같은 판정이 일곱 곳에 손으로 있었고 이미 세 가지 강도로 갈라져 있었다 — 어떤 곳은 `Number.isFinite`로 NaN을 막고 어떤 곳은 안 막아 `… |
| `src/domain/place/coordInput.ts` | export | function | `isUsableCoord(lat: number, lng: number): boolean` | isRealCoord | 좌표쌍이 지구 위에 있는가(붙여넣기 파서용 별칭 — 인자가 이미 number인 경로). |
| `src/domain/place/coordInput.ts` | export | function | `orderPair(a: number, b: number): ParsedCoord \\| null` | isUsableCoord | 순서를 모르는 두 수 → 좌표. 위도는 ±90을 넘을 수 없다는 물리적 제약 이 대부분의 모호함을 없앤다. 한국 좌표 (위도 33~39 · 경도 124~132)는 경도가 90을 넘으므로 언제나 명확하다 — 사용자가 어느 순서로 붙여넣든 옳게 읽힌다. |
| `src/domain/place/coordInput.ts` | export | function | `swapCoord(c: ParsedCoord): ParsedCoord` | — | 위·경도를 뒤바꾼다. 화면의 [바꾸기] 버튼이 쓴다 — 뒤바꾼 뒤에는 더 이상 모호하지 않다. |
| `src/domain/place/coordInput.ts` | internal | function | `parseDmsPart(text: string): number \\| null` | /(-?\d+(?:\.\d+)?)\s*[°d:\s]\s*(?:(\d+(?:\.\d+)?)\s*['′m:\s…, text.trim, Number, Number.isFinite, /[SW]/i.test, Math.abs | 도·분·초 한 조각 → 십진수. `37°35'13.2"N` 형태. 못 읽으면 null. |
| `src/domain/place/coordInput.ts` | export | function | `parseDms(text: string): ParsedCoord \\| null` | /[NSEW]/i.test, text.split(/(?<=[NSEW])\s*[,/]?\s*/i).filter, text.split, parseDmsPart, /[NS]/i.test, isUsableCoord | 도분초 표기 → 좌표. `37°35'13.2"N 127°00'05.8"E` N/S/E/W가 있으면 순서가 확정된다 (어느 쪽이 위도인지 글자가 말한다) — 모호하지 않다. |
| `src/domain/place/coordInput.ts` | internal | function | `parseKnownUrl(text: string): ParsedCoord \\| null` | isUsableCoord, /yandex\.[a-z.]+\/maps[^\s]*?[?&]ll=(-?\d+(?:\.\d+)?)[,%2C]…, build, Number, /naver\./i.test, /[?&]lat=(-?\d+(?:\.\d+)?)/i.exec, /[?&]lng=(-?\d+(?:\.\d+)?)/i.exec, /kakao\.[a-z.]+\/link\/map\/[^,]*,(-?\d+(?:\.\d+)?),(-?\d+(…, /openstreetmap\.[a-z.]+[^\s]*?#map=\d+(?:\.\d+)?\/(-?\d+(?:…, /google\.[a-z.]+\/maps\\|maps\.google\./i.test | URL 쿼리/경로에서 이름 있는 좌표를 읽는다. 이름이 순서를 말하므로 모호하지 않다 . |
| `src/domain/place/coordInput.ts` | internal | function | `firstNumberPair(text: string): [number, number] \\| null` | /(-?\d{1,3}(?:\.\d+)?)\s*[,/\s]\s*(-?\d{1,3}(?:\.\d+)?)/.ex…, Number, Number.isFinite | 문자열에서 숫자쌍 하나를 뽑는다(구분자는 쉼표·공백·슬래시). 못 찾으면 null. |
| `src/domain/place/coordInput.ts` | export | function | `parseCoordinateInput(raw: string): ParsedCoord \\| null` | (raw ?? '').trim, parseKnownUrl, parseDms, /^[a-z]+:\/\//i.test, /\.[a-z]{2,}\//i.test, /^[-\d.,/\s°'"′″NSEWnsew]+$/.test, firstNumberPair, orderPair | 사용자가 붙여넣은 무엇이든 → 좌표(또는 null). 순서가 중요하다: 이름 있는 형식(URL·도분초)을 먼저 본다. 그것들은 순서를 확정해 주므로 모호함이 없다. 평문 숫자쌍은 마지막이고, 그때만 `ambiguous`가 될 수 있다. 좌표가 아닌 평범한 장소 이름(「대학로」·「Roma Termini」)에는 반드시 nu… |
| `src/domain/place/coordInput.ts` | internal | const | `SOURCE_LABEL: Record<ParsedCoord['source'], string>` | — | — |
| `src/domain/place/coordInput.ts` | export | function | `coordInputLabel(c: ParsedCoord): string` | c.lat.toFixed, c.lng.toFixed | 사용자에게 나가는 한 문장 — 순수 함수로 뽑아 유닛이 잰다 (§10 ③). 🔴 모호했으면 반드시 말한다. 「위도 37.587 · 경도 127.0016으로 읽었어요」라고 적어야 사용자가 틀린 걸 알아채고 뒤바꿀 수 있다. 조용히 추측하면 기억이 엉뚱한 곳에 남는다. |
| `src/domain/place/externalMap.ts` | export | typealias | `MapProviderId` | — | 제공자 식별자. 동의 저장 키와 검사에서 쓰므로 문자열을 손으로 다시 적지 않는다. |
| `src/domain/place/externalMap.ts` | export | interface | `PlaceLike` | — | — |
| `src/domain/place/externalMap.ts` | export | interface | `ExternalMapTarget` | — | — |
| `src/domain/place/externalMap.ts` | export | function | `isUsableCoord(lat: number \\| null, lng: number \\| null): boolean` | isRealCoord | 좌표가 지구 위의 값인가 — 단일 판정(isRealCoord)에 위임한다 (H-3). 🔴 예전엔 이 함수가 유한·범위·0,0 검사를 손으로 다시 구현 했다(감사가 센 7곳의 8번째 — `coordInput.ts`의 판정과 갈라질 수 있었다). 이름은 이 층의 어휘(외부 지도 링크)라 남기되 몸은 공용 함수를 부른다. … |
| `src/domain/place/externalMap.ts` | internal | function | `n6(v: number): string` | String, Number, v.toFixed | 좌표 표기 — 소수 6자리면 약 10cm다. 그 이상은 정밀도를 가장 하는 것이다. |
| `src/domain/place/externalMap.ts` | internal | function | `commaSafe(name: string): string` | name.replace(/,/g, ' ').trim, name.replace | 카카오 링크는 경로가 쉼표로 나뉜다 (`/link/map/이름,위도,경도`). 그래서 이름에 쉼표가 있으면 좌표 자리가 밀려 엉뚱한 곳이 열린다 — 지어낸 위험이 아니라 「서울시청, 본관」 같은 흔한 이름에서 바로 난다. 쉼표를 공백으로 바꿔 자리를 지킨다. |
| `src/domain/place/externalMap.ts` | internal | interface | `Provider` | — | — |
| `src/domain/place/externalMap.ts` | internal | const | `PROVIDERS: readonly Provider[]` | — | 제공자 등록부. 여기 한 줄이 곧 버튼 하나 다 — 화면이 목록을 손으로 다시 적지 않는다(§7). ⚠️ 각 URL 형식의 출처와 검증 상태는 `docs/DECISIONS.md`(ADR-0031)에 적었다. 구글만 실기기에서 좌표까지 확인됐고(2026-07-28 사용자), 나머지 셋은 문서 기준 이다. |
| `src/domain/place/externalMap.ts` | export | const | `MAP_PROVIDER_IDS: readonly MapProviderId[]` | — | 제공자 목록(화면이 순서를 정하지 않게 여기서 준다). |
| `src/domain/place/externalMap.ts` | export | function | `externalMapTargets(place: PlaceLike): ExternalMapTarget[]` | place.name.trim, isUsableCoord, out.push, p.byCoords, n6, p.byName | 이 장소를 각 지도에서 열 링크 . 좌표도 이름도 없으면 빈 배열 (열 곳이 없다고 정직하게). 좌표가 있으면 좌표로 집는다(정확). 없으면 이름으로 검색하되 그 사실을 caveat에 담는다 — 사용자 결정(2026-07-27): "이름으로 검색해 연다. 단 이름으로 찾은 결과라고 화면에 밝힌다." |
| `src/domain/place/externalMap.ts` | export | function | `externalMapConsentText(t: ExternalMapTarget): string` | [ `${t.company} 지도로 이동할까요?`, '', `${t.sends}가 ${t.company}에… | 처음 한 번 보여줄 확인 문구. 무엇이 어디로 나가는지 를 `sends`·`company`에서 파생한다 — 문구를 손으로 다시 적으면 링크가 바뀔 때 문장만 옛말로 남는다(SSOT). |
| `src/domain/place/geojson.ts` | export | interface | `MediaGps` | — | — |
| `src/domain/place/geojson.ts` | export | interface | `LocatedPoint` | — | — |
| `src/domain/place/geojson.ts` | export | function | `momentCoord(mediaList: readonly MediaGps[]): { lat: number; lng: number } \\| null` | isRealCoord | 순간의 대표 좌표 = GPS가 있는 첫 사진. 없으면 null(대략위치를 지어내지 않음). |
| `src/domain/place/geojson.ts` | export | interface | `GeoFeature` | — | — |
| `src/domain/place/geojson.ts` | export | interface | `GeoFeatureCollection` | — | — |
| `src/domain/place/geojson.ts` | export | function | `toFeatureCollection(points: readonly LocatedPoint[]): GeoFeatureCollection` | points.map | 위치 순간 목록 → GeoJSON FeatureCollection. 좌표는 [lng, lat]. |
| `src/domain/place/here.ts` | export | typealias | `HereFail` | — | 위치를 못 얻은 이유. 각각 사용자가 할 일이 다르므로 뭉개지 않는다(§8). |
| `src/domain/place/here.ts` | export | function | `hereVerdict(accuracyM: number \\| null): PrecisionVerdict` | Number.isFinite, Math.round, precisionFromSpan | GPS 정확도(m) → 이미 있는 정밀도 체계 로 판정. 왜 새로 만들지 않는가: 화면은 이미 「📍 건물·지점 · 약 50m」/「⚠ 길 전체를 가리켜요」를 말할 줄 안다. 위치 출처가 검색이든 지도든 GPS든 사용자가 알아야 하는 것은 같다 — 이 좌표가 얼마나 좁은 곳을 가리키는가 . 여기서 등급을 새로 정의하면 같… |
| `src/domain/place/here.ts` | export | function | `hereLabel(coord: { lat: number; lng: number }, accuracyM: number \\| null): string` | coord.lat.toFixed, coord.lng.toFixed, Number.isFinite, Math.round, (m / 1000).toFixed | 배지에 적을 근거 — 숫자 좌표를 함께 보여준다 (사용자 2026-07-31: "사진 업로드와 동시에 내게 좌표를 제공해주면 내가 직접 입력하면 되지 않아?" ). 왜 숫자를 노출하나: 사용자의 목적이 "무료 지도 자체가 위치가 정확하지 않아서 GPS 도움을 받으려는 것" 이다. 그러면 앱이 「위치 지정됨」이라고만 말하… |
| `src/domain/place/here.ts` | export | function | `hereFailMessage(fail: HereFail): string` | — | 실패 사유 → 다음에 할 일이 담긴 한 문장(§12 — 「안 됩니다」로 끝내지 않는다). 넷을 나눠 두는 이유: 권한을 껐을 때와 실내라 못 잡을 때는 사용자가 할 일이 정반대 다. 하나로 뭉개면 권한을 켜야 할 사람이 계속 버튼만 누르고, 잠시 기다리면 될 사람이 설정을 뒤진다. |
| `src/domain/place/jusoMap.ts` | internal | const | `WGS84` | — | — |
| `src/domain/place/jusoMap.ts` | internal | const | `JUSO_GRS80` | — | — |
| `src/domain/place/jusoMap.ts` | export | interface | `JusoMapPoint` | — | — |
| `src/domain/place/jusoMap.ts` | export | function | `toJusoMapPoint(coord: MapCoordinate): JusoMapPoint` | Number.isFinite, proj4 | — |
| `src/domain/place/mapProvider.ts` | export | typealias | `MapDisplayProvider` | — | — |
| `src/domain/place/mapProvider.ts` | export | interface | `MapCoordinate` | — | — |
| `src/domain/place/mapProvider.ts` | export | interface | `MapProviderConfig` | — | — |
| `src/domain/place/mapProvider.ts` | internal | const | `TOMTOM_PILOT_COUNTRIES` | — | TomTom 시범 적용국. 넓은 사각형 추측 대신 확인된 국가 코드로만 고른다. |
| `src/domain/place/mapProvider.ts` | internal | interface | `CoordinateBox` | — | — |
| `src/domain/place/mapProvider.ts` | internal | const | `KOREA_MAP_BOXES: readonly CoordinateBox[]` | — | 대한민국 지도 표시용 작동 경계. 국경을 판정하거나 사용자 기록을 분류하는 데이터가 아니다. 카카오 지도 표시 여부만 고른다. 본토·서남해/제주·울릉/독도를 나눠, 하나의 큰 사각형이 주변 국가를 과하게 삼키지 않게 한다. |
| `src/domain/place/mapProvider.ts` | export | function | `isKoreaMapCoordinate(coord: MapCoordinate): boolean` | Number.isFinite, KOREA_MAP_BOXES.some | — |
| `src/domain/place/mapProvider.ts` | export | function | `displayProviderForPoints(points: readonly MapCoordinate[], config: MapProviderConfig): MapDisplayProvider` | points.every, points.map, displayProviderForPicker, providers.every | 여행 지도는 표시할 점이 전부 한국일 때만 카카오를 쓴다. 여러 나라가 섞인 여행을 카카오에 억지로 넣지 않고 기존 세계지도(MapLibre)로 보존한다. |
| `src/domain/place/mapProvider.ts` | export | function | `displayProviderForPicker(initial: MapCoordinate \\| null, countryCode: string \\| null, config: MapProviderConfig): MapDisplayProvider` | isKoreaMapCoordinate, countryCode?.trim().toLowerCase, countryCode?.trim, TOMTOM_PILOT_COUNTRIES.has | 한국은 좌표로, TomTom 시범국은 확인된 국가 코드로 판정한다. |
| `src/domain/place/mapProvider.ts` | export | function | `displayProviderFallbackOrder(preferred: MapDisplayProvider, includeJusoAfterKakao = false): readonly MapDisplayProvider[]` | — | 지역 지도 실패 시 기존 MapLibre로 닫히도록 하는 순서도 한 곳에서 파생한다. |
| `src/domain/place/photoHint.ts` | export | interface | `PhotoMetaLike` | — | `readPhotoMeta`가 돌려주는 것 중 이 판단에 필요한 부분만. |
| `src/domain/place/photoHint.ts` | export | interface | `PhotoHint` | — | — |
| `src/domain/place/photoHint.ts` | export | function | `photoHintOf(metas: readonly PhotoMetaLike[]): PhotoHint` | metas.filter, isRealCoord, located .map((m) => ({ m, t: m.takenAt ? Date.parse(m.taken…, located .map, Date.parse, Number.isNaN, metas.find | 고른 사진들에서 위치·시간대 힌트 를 뽑는다. 🔴 여러 장이면 가장 이른 촬영시각 의 사진을 쓴다(사용자 지적 2026-07-31: "사진여러장이면 가장 앞 시간 사진을 기준으로" ). 처음엔 「고른 순서의 첫 장」이었다. 그런데 시각은 이미 「가장 이른 것」을 쓰고 있었다 (`guessOccurredAt` — 순간의… |
| `src/domain/place/photoHint.ts` | export | function | `photoPlaceLabel(hint: PhotoHint): string` | hint.coord.lat.toFixed, hint.coord.lng.toFixed | 「사진 위치에서 가져왔다」는 근거 한 줄. 좌표가 없으면 빈 문자열 (§8 침묵이 정상). 시각 쪽(`guessOccurredAt`의 `label`)과 같은 어휘 를 쓴다 — 같은 성격의 추측이 화면에서 다른 말투로 나오면 사용자는 둘을 다른 것으로 읽는다(§7 사용자 대면 대칭). |
| `src/domain/place/photoHint.ts` | export | function | `photoPlaceNotice(hint: PhotoHint): string` | — | 🔴 좌표를 못 얻었을 때 하는 말 — 「왜 없는지」와 「그럼 어떻게 하는지」까지. ───────────────────────────────────────────────────────────────────────────── 왜 이 함수가 생겼나 (사용자 2026-07-31: "아직도 해결이 안되네..어려운 기능인건가요?… |
| `src/domain/place/photoProbe.ts` | export | interface | `PhotoProbeFacts` | — | — |
| `src/domain/place/photoProbe.ts` | internal | function | `comma(n: number): string` | n.toLocaleString | `2,918,760` — 폰 파일 관리자가 보여주는 숫자와 눈으로 맞춰 보게. |
| `src/domain/place/photoProbe.ts` | export | function | `photoProbeLine(f: PhotoProbeFacts): string` | comma, f.probe.app1.join | 관측 한 줄. 판정어(「~때문입니다」)를 쓰지 않는다 — 이 함수는 원인을 모른다. 순수 함수인 이유(§10 ③): 사용자에게 나가는 문장은 유닛이 직접 돌려야 한다. 이 저장소는 자료구조가 옳은데 화면 문장만 틀린 결함을 세 번 냈다(M-0022·M-0046·M-0061). |
| `src/domain/place/photoProbe.ts` | export | function | `photoProbeNext(p: ExifProbe): string` | — | 다음에 무엇을 하면 되는지 한 줄. 관측에서 갈라지는 행동만 말한다. 🔴 원인을 말하지 않는다. "선택기가 지웠어요" 는 M-0056에서 철회했고 "파일에 원래 없었어요" 는 M-0059에서 무너졌다. 행동은 원인을 몰라도 고를 수 있다. |
| `src/domain/place/photoProbe.ts` | export | interface | `PhotoPathFacts` | — | 관측 창이 넘겨주는 경로 사실 — services/nativePhotos가 기록한 것을 그대로 받는다. |
| `src/domain/place/photoProbe.ts` | export | function | `photoProbePath(f: PhotoPathFacts): string` | — | 바이트가 어느 문으로 들어왔는지 한 줄. 순수 함수(§10 ③) — 이 문장도 사용자에게 간다. 🔴 원인 단정어를 쓰지 않는다(§8 — 유닛이 잠근다). `reason`은 플러그인이 적은 사실 문자열이라 가공 없이 그대로 내보낸다(가공하면 대조할 수 없다 — 해시와 같은 규율). |
| `src/domain/place/precision.ts` | export | typealias | `PlacePrecision` | — | 결과가 가리키는 대상의 크기 등급. 'unknown'은 정상으로 반올림하지 않은 상태다(§8). |
| `src/domain/place/precision.ts` | export | typealias | `BBox` | — | [남, 북, 서, 동] — Nominatim boundingbox의 순서 그대로(문자열로 온다). |
| `src/domain/place/precision.ts` | export | interface | `PrecisionVerdict` | — | — |
| `src/domain/place/precision.ts` | internal | const | `METERS_PER_DEGREE` | — | 위도 1도의 대략 거리(m). 경도는 위도에 따라 줄어든다(cos 보정). |
| `src/domain/place/precision.ts` | export | function | `bboxSpanMeters(bbox: BBox \\| null): number \\| null` | [south, north, west, east].every, Number.isFinite, Math.abs, Math.cos, Math.max, Math.round | boundingbox → 대상이 걸친 대략 범위(m). 가로·세로 중 큰 쪽 을 쓴다. 왜 큰 쪽인가: 대학로처럼 가늘고 긴 대상은 짧은 변만 보면 "정밀하다"고 오판한다. 정밀도는 가장 나쁜 축 이 결정한다(작은 쪽으로 반올림하면 앱이 아는 척하게 된다). |
| `src/domain/place/precision.ts` | export | function | `precisionFromRank(placeRank: number \\| null): PlacePrecision` | Number.isFinite | Nominatim `place_rank` → 등급. 출처는 Nominatim의 주소 랭크 체계다(낮을수록 넓다): ~16 국가·광역, 17–21 도시·읍면, 22–25 동네·구역, 26–27 도로, 28+ 건물·지점. 범위로 잡는 이유는 제공자가 세부 값을 조정해도 등급이 흔들리지 않게 하기 위해서다. |
| `src/domain/place/precision.ts` | export | function | `precisionFromSpan(spanMeters: number \\| null): PlacePrecision` | Number.isFinite | 범위(m) → 등급. `place_rank`가 없는 제공자(B단계에서 붙는 국내 제공자 등)를 위한 경로다. 경계값은 등급의 대표 크기 에서 왔다: 건물 100m, 도로 2km, 동네 8km, 도시 40km. |
| `src/domain/place/precision.ts` | export | function | `classifyPlace(input: { placeRank: number \\| null; bbox: BBox \\| null }): PrecisionVerdict` | bboxSpanMeters, precisionFromRank, precisionFromSpan | 결과 하나의 정밀도 판정. 랭크가 있으면 랭크가 이긴다 — 랭크는 제공자가 말한 의미 (이건 도로다)이고, 범위는 우리가 잰 크기 다. 작은 도로는 범위만 보면 건물처럼 보이는데, 그래도 도로는 도로다 (어느 지점인지 여전히 알 수 없다). 둘 다 없으면 'unknown'으로 남긴다 — 모르는 것을 정밀한 쪽으로 반올림… |
| `src/domain/place/precision.ts` | internal | const | `PRECISIONS: readonly PlacePrecision[]` | — | — |
| `src/domain/place/precision.ts` | export | function | `verdictFromStored(precision: string \\| null, spanMeters: number \\| null): PrecisionVerdict` | PRECISIONS.find, Number.isFinite | 저장해 둔 등급 문자열 → 판정. 🔴 왜 필요한가(2026-07-30 라이브 검사가 잡았다): 장소 라이브러리에서 다시 고른 장소는 이미 등급을 알고 있는데 화면이 그걸 말하지 않고 있었다. 처음 고를 때는 「길 전체를 가리켜요」라고 해놓고, 같은 장소를 두 번째로 고르면 조용해졌다 — 같은 사실에 대해 앱이 두 번 … |
| `src/domain/place/precision.ts` | export | function | `spanText(spanMeters: number \\| null): string \\| null` | Number.isFinite, Math.max, Math.round, (spanMeters / 1_000).toFixed | 범위(m) → 사람이 읽는 거리. 모르면 null(«대략»이라고도 말하지 않는다). |
| `src/domain/place/precision.ts` | internal | const | `PRECISION_NOUN: Record<PlacePrecision, string>` | — | — |
| `src/domain/place/precision.ts` | export | function | `precisionNoun(p: PlacePrecision): string` | — | 등급의 짧은 이름(칩·배지용). |
| `src/domain/place/precision.ts` | export | function | `precisionGlyph(v: PrecisionVerdict): string` | — | 등급 글리프. 색은 늘 글리프와 함께 (헌법 §7 사용자 대면 대칭) — 색만으로 구분하면 색각 이상·흑백 인쇄·저대비 환경에서 정보가 통째로 사라진다. '?'와 '⚠'를 나누는 이유: §8은 모르는 것 과 문제인 것 을 반올림해 섞지 말라고 한다. 넓은 대상은 우리가 알고 넓다고 말하는 것이고, unknown은 재지 … |
| `src/domain/place/precision.ts` | export | function | `precisionLabel(v: PrecisionVerdict): string` | spanText | 사용자에게 나가는 한 문장 — 순수 함수로 뽑아 유닛이 잰다 (§10 ③). M-0022의 교훈이 이 자리다: 자료구조가 옳아도 화면에 나가는 문장이 틀리면 그건 결함이고, 유닛 15건이 전부 통과해도 아무도 못 잡는다. 그래서 문장을 DOM에서 떼어 놓는다. |
| `src/domain/place/precision.ts` | export | function | `needsRefine(v: PrecisionVerdict): boolean` | — | 지도 미세조정을 권할 것인가. 'point'가 아니면 어느 지점인지 앱이 모른다. 그 상태로 저장해도 되지만, 사용자가 나중에 "왜 엉뚱한 데 찍혔지?"라고 묻게 된다 — 그 질문이 나오기 전에 앱이 먼저 말한다. 'unknown'도 포함한다: 모르는 것은 정밀한 쪽으로 반올림하지 않는다. |
| `src/domain/place/precision.ts` | export | function | `zoomForSpan(spanMeters: number \\| null): number` | Number.isFinite | 이 결과를 지도에 띄울 때의 확대수준. 🔴 이 함수가 생긴 이유가 원래 신고의 절반이다. `mapView`가 지점이 하나뿐일 때만 zoom을 손대지 않아(`setCenter`만 호출) 초기값 10 — 김포~남양주가 다 보이는 화면 — 위에 핀 하나가 떠 있었다. 형제(목록 행 클릭)는 `flyTo({zoom:14})`… |
| `src/domain/place/provider.ts` | export | typealias | `ProviderId` | — | — |
| `src/domain/place/provider.ts` | internal | const | `PROVIDER_LABEL: Record<ProviderId, string>` | — | 사용자에게 보이는 제공자 이름 — 어디서 온 답인지 숨기지 않는다 (§8 출처 표시). |
| `src/domain/place/provider.ts` | export | function | `providerLabel(id: ProviderId): string` | — | — |
| `src/domain/place/provider.ts` | export | const | `FALLBACK_PROVIDER: ProviderId` | — | 키가 필요 없어 언제나 쓸 수 있는 제공자. 설정이 하나도 없어도 앱이 도는 근거다. |
| `src/domain/place/provider.ts` | internal | const | `KOREA_ONLY: ReadonlySet<ProviderId>` | — | 국내 전용 제공자(해외 질의에 쓰면 답이 없거나 엉뚱하다). |
| `src/domain/place/provider.ts` | export | function | `isKoreaOnly(id: ProviderId): boolean` | KOREA_ONLY.has | — |
| `src/domain/place/provider.ts` | export | function | `hasHangul(text: string): boolean` | /[ㄱ-ㆎ가-힣]/.test | 한글(음절·자모)이 하나라도 있는가. |
| `src/domain/place/provider.ts` | internal | const | `KR_BOX` | — | 대한민국 본토 + 제주 + 울릉/독도를 넉넉히 덮는 상자. 정밀한 국경이 목적이 아니다 — "국내 제공자에게 먼저 물어볼 가치가 있는가" 만 가르면 된다. 넓게 잡아 틀려도 손해가 없다: 국내 제공자가 답을 못 주면 Nominatim으로 내려가기 때문이다. |
| `src/domain/place/provider.ts` | export | function | `inKoreaBox(p: { lat: number; lng: number } \\| null \\| undefined): boolean` | Number.isFinite | — |
| `src/domain/place/provider.ts` | export | function | `looksKorean(query: string, near?: { lat: number; lng: number } \\| null): boolean` | hasHangul, inKoreaBox | 이 질의가 한국을 가리키는가 . 두 단서를 쓴다. ①질의에 한글이 있다 ②지금 보고 있는 위치가 한국 안이다. 둘 중 하나면 충분하다 — 「Gyeongbokgung」처럼 로마자로 적은 국내 검색(①이 거짓)과 「스타벅스」처럼 어디에나 있는 이름(②가 결정)을 둘 다 살리기 위해서다. 틀려도 안전한 판정이다. 틀리게 국내… |
| `src/domain/place/provider.ts` | export | function | `providerOrder(query: string, opts: { available: readonly ProviderId[]; near?: { lat: number; lng: number } \…): ProviderId[]` | looksKorean, isKoreaOnly, order.includes, order.push | 물어볼 순서를 정한다. @param available 지금 실제로 쓸 수 있는 제공자(키·프록시가 설정된 것). Nominatim은 늘 포함. 규칙: - 한국 질의: 설정된 국내 제공자 먼저, 그 다음 Nominatim. - 해외 질의: 국내 제공자를 아예 부르지 않는다 — 답이 없을 걸 알면서 부르는 것은 사용자의 시… |
| `src/domain/place/provider.ts` | export | function | `roughDistanceMeters(a: { lat: number; lng: number }, b: { lat: number; lng: number }): number` | Math.cos, Math.sqrt | 두 좌표 사이 대략 거리(m). 중복 판정에만 쓰므로 평면 근사로 충분하다. |
| `src/domain/place/provider.ts` | internal | const | `SAME_PLACE_METERS` | — | 같은 곳으로 볼 거리(m). 건물 하나 크기 — 이보다 가까우면서 이름도 같으면 같은 곳이다. |
| `src/domain/place/provider.ts` | internal | interface | `Dedupable` | — | — |
| `src/domain/place/provider.ts` | export | function | `normalizeName(name: string): string` | name .toLowerCase() .replace(/\([^)]*\)/g, '') .replace, name .toLowerCase() .replace, name .toLowerCase | 비교용으로 이름을 정규화 — 공백·괄호주석·점을 무시한다(『스타벅스 종로점』 vs 『스타벅스종로점』). |
| `src/domain/place/provider.ts` | export | function | `dedupePlaces(items: readonly T[]): T[]` | normalizeName, out.some, roughDistanceMeters, out.push | 여러 제공자의 결과를 순서를 지키며 합치고 중복을 접는다. 왜 순서를 지키나: 앞선 제공자가 더 잘 아는 쪽이다(한국 질의라면 국내 제공자). 뒤에서 온 같은 장소가 앞을 덮으면 라우팅이 무의미해진다. 그래서 먼저 온 것이 이긴다. 왜 이름만으로 접지 않나: 「대학로」는 서울에도 있고 다른 도시에도 있다. 이름이 같아도… |
| `src/domain/place/registry.ts` | export | interface | `RegistryPlace` | — | 대장이 다루는 장소의 최소 모양 — 화면·유닛이 이 형태만 알면 된다. |
| `src/domain/place/registry.ts` | export | function | `searchRegistry(places: readonly T[], query: string): T[]` | places.filter, query.trim().toLowerCase, query.trim, alive.filter, [p.name, p.city, p.country, p.formattedAddress].filter(Bool…, [p.name, p.city, p.country, p.formattedAddress].filter, hay.includes | 이름 부분일치 검색. 사용자 요청: "장소 필드에 입력할 때 한글자라도 동일하면 그 글자가 포함된 장소들은 다 나오게" 🔴 상한을 두지 않는다. 기존 순간 편집 화면은 `.slice(0, 5)`로 잘랐는데, 그러면 「타」를 쳤을 때 여섯 번째 장소는 존재하지 않는 것과 같다. 개인 여행 앱이라 장소 수가 수백 규모이므… |
| `src/domain/place/registry.ts` | export | function | `liveLookupNote(query: string, total: number): string \\| null` | query.trim | 순간 편집의 장소 칸에서 치는 동안 대장이 못 찾았을 때 할 말. 못 찾았으면 `null`이 아니라 문장을 준다 — 침묵은 「그런 곳은 없다」로 읽히는데, 대장이 아는 것은 내가 담아 둔 곳뿐 이다 (§7-C 「한정을 생략」). 그래서 시야의 경계를 밝히고 다음에 할 일 을 함께 준다(막다른 길 금지). 순수 함수인 이… |
| `src/domain/place/registry.ts` | export | function | `shortAddress(p: { country: string \\| null; city: string \\| null }): string \\| null` | [p.country, p.city].filter, Boolean, parts.join | 국가·도시까지만 — 사용자 요청 ④ (예: `우즈베키스탄 · 타슈켄트`). 없으면 null. |
| `src/domain/place/registry.ts` | export | interface | `CoordView` | — | 좌표 한 벌의 표시 정보. |
| `src/domain/place/registry.ts` | export | interface | `CoordPreview` | — | 지금 좌표와 뒤집은 좌표를 나란히 준다. 화면이 둘을 함께 보여주고 사용자가 고른다. |
| `src/domain/place/registry.ts` | internal | function | `view(lat: number, lng: number): CoordView` | isUsableCoord, lat.toFixed, lng.toFixed | — |
| `src/domain/place/registry.ts` | export | function | `coordPreview(lat: number, lng: number): CoordPreview` | view | 좌표 미리보기. 위도는 ±90을 넘을 수 없다는 물리 제약 이 대부분의 모호함을 없앤다 — 한국 좌표 (경도 124~132)는 뒤집으면 위도가 90을 넘어 불가능하므로 `ambiguous: false`다. 타슈켄트(41.3, 69.2)처럼 둘 다 90 미만이면 원리적으로 모호하고, 그때는 주소가 답한다 . |
| `src/domain/place/registry.ts` | export | function | `sortRegistry(places: readonly T[]): T[]` | [...places].sort, a.name.localeCompare | 대장 목록의 정렬 — 이름 가나다순. 왜 최신순이 아닌가: 대장은 찾으러 오는 화면 이다(사진의 위치를 고치려고 온다). 「언제 만들었나」가 아니라 「이름이 무엇인가」로 찾으므로 가나다순이 맞다. |
| `src/domain/place/registry.ts` | export | function | `needsAddress(p: RegistryPlace): boolean` | — | 주소를 아직 못 받은 장소 — 대장이 「주소 불러오기」를 권할 대상. |
| `src/domain/place/registry.ts` | export | interface | `MomentLike` | — | 순간 쪽에서 이 판단에 필요한 부분만. |
| `src/domain/place/registry.ts` | export | function | `momentsUsingPlace(moments: readonly T[], place: { id: string; name: string }): { linked: T[]; sameName: T[] }` | moments.filter, alive.filter, linked.map, linkedIds.has, m.placeName.trim, place.name.trim | 이 장소를 쓰는 순간들 — 두 갈래로 나눠 센다. 🔴 왜 나누는가(§8 — 모르는 것을 아는 척하지 않는다): · `linked` = `placeId`가 이 장소를 가리킨다. 확실하다. · `sameName` = 링크는 없고 이름만 같다. 같은 곳일 수도 있고 아닐 수도 있다 — 「집」이라 적은 곳이 서울 집일 수도 … |
| `src/domain/place/registry.ts` | export | function | `usageLabel(u: { linked: unknown[]; sameName: unknown[] }): string \\| null` | — | 「이 장소를 쓰는 순간」 개수 한 줄. 🔴 확실한 것과 짐작을 한 문장 안에서도 구분해 말한다 . `null`이면 쓰는 순간이 없다는 뜻. |
| `src/domain/place/registry.ts` | export | interface | `PlaceRecordMoment` | — | 장소 기록 모달의 최소 투영. 사진 제목은 별도 필드가 아니라 순간 제목이다. |
| `src/domain/place/registry.ts` | export | interface | `PlaceRecordTripGroup` | — | — |
| `src/domain/place/registry.ts` | export | function | `unlinkedPlaceMoments(moments: readonly T[], query: string): T[]` | query.trim().toLocaleLowerCase, query.trim, moments .filter((moment) => { if (moment.deletedAt !== null…, moments .filter, moment.placeName.trim, [moment.placeName, moment.title, moment.tripTitle] .join(' …, [moment.placeName, moment.title, moment.tripTitle] .join, compareInstants, a.id.localeCompare | 이름은 순간에 남았지만 장소 대장 ID가 없는 활성 순간. 자동으로 같은 이름의 장소에 붙이지 않는다 — 「집」처럼 동명이 많고, 좌표도 없으면 동일성을 증명할 수 없다. 대신 대장에서 빠짐없이 보여 주고 해당 순간으로 이동시켜 사용자가 올바른 장소를 한 번 고르게 한다. |
| `src/domain/place/registry.ts` | export | function | `placeRecordGroups(moments: readonly T[], place: { id: string; name: string }): { linked: PlaceRecordTripGroup<T>[]; sameName: PlaceRecordTripGroup<T>[] }` | byTrip.get, existing.moments.push, byTrip.set, byTrip.values, momentsUsingPlace, group | 기존 장소 판정을 재사용해 확실한 링크와 이름만 같은 추정을 여행별로 묶는다. |
| `src/domain/place/registry.ts` | export | function | `unusedRegistryPlaces(places: readonly T[], moments: readonly PlaceRecordMoment[]): T[]` | places.filter, placeRecordGroups | "사진이 없는 장소" 정리창에 보여 줄, 직접 연결된 순간이 없는 장소. 사진이 없는 순간도 사용자의 글 기록이므로 `media` 개수로 판단하지 않는다. 이름만 같은 순간 역시 장소 ID 연결의 증거가 아니며, 이 목록에 남더라도 그 순간을 지우거나 연결하지 않는다. 실제 삭제 직전에는 서비스가 같은 조건을 Dexie… |
| `src/services/externalMapConsent.ts` | internal | function | `keyOf(id: MapProviderId): string` | — | 제공자별 키. 옛 키(`bugeon:externalMapOk`)는 일부러 읽지 않는다 — 그걸 구글 동의로 물려받으면 "구글에만 동의했는데 넷 다 열림"이 되진 않지만, 사용자가 하나뿐이던 시절 에 누른 확인을 지금의 넷 중 하나로 해석하는 셈이라 다시 묻는 쪽이 정직하다. 비용은 한 번의 확인이다. |
| `src/services/externalMapConsent.ts` | export | function | `hasAgreedExternalMap(id: MapProviderId): boolean` | hasAgreed, keyOf | 이 기기에서 이 제공자에 대해 이미 확인했는가. |
| `src/services/externalMapConsent.ts` | export | function | `rememberAgreedExternalMap(id: MapProviderId): void` | rememberAgreed, keyOf | — |
| `src/services/geocode.ts` | export | interface | `AddressParts` | — | 구조화 주소 조각. 없는 것은 null — 빈 문자열로 채우지 않는다(§8 「모르는 것」). |
| `src/services/geocode.ts` | export | interface | `PlaceResult` | — | — |
| `src/services/geocode.ts` | internal | const | `NOMINATIM` | — | — |
| `src/services/geocode.ts` | export | interface | `GeocodeOptions` | — | — |
| `src/services/geocode.ts` | internal | const | `BIAS_HALF_DEGREE` | — | 편향 상자의 반변(도). 약 55km — "지금 있는 도시" 정도를 우선한다. |
| `src/services/geocode.ts` | export | function | `buildNominatimUrl(query: string, opts: GeocodeOptions = {}): string` | String, query.trim, Number.isFinite, params.set, [lng - BIAS_HALF_DEGREE, lat + BIAS_HALF_DEGREE, lng + BIAS…, params.toString | 검색 질의 → Nominatim URL(순수). 한국어 우선, 소량 결과, 정밀도 근거 포함. |
| `src/services/geocode.ts` | internal | function | `num(v: unknown): number \\| null` | Number.isFinite, v.trim, Number | 문자열/숫자 아무거나 → 유한 숫자 또는 null. 외부 데이터는 형태를 믿지 않는다. |
| `src/services/geocode.ts` | internal | function | `str(v: unknown): string \\| null` | v.trim | — |
| `src/services/geocode.ts` | export | function | `parseBBox(v: unknown): BBox \\| null` | Array.isArray, v.map, nums.some | Nominatim `boundingbox`(["남","북","서","동"] 문자열 4개) → BBox. 하나라도 깨지면 null. |
| `src/services/geocode.ts` | export | function | `parseAddress(v: unknown): AddressParts` | str, cc.toLowerCase, first | Nominatim `address` 객체 → 구조화 주소(순수). 한국 주소가 어느 키로 오는지가 일정하지 않다 — 서울은 `city`, 광역시 밖은 `county`, 구는 `borough`/`city_district`/`suburb` 중 하나로 온다. 그래서 후보 목록의 첫 값 을 쓴다. 못 찾으면 null이다(억지로… |
| `src/services/geocode.ts` | export | function | `adminLabel(a: AddressParts): string \\| null` | [a.region, a.city, a.district].filter, seen.some, s.includes, p.includes, seen.push, seen.join | 'kr' + 시·도 + 구 → "서울특별시 종로구" 같은 짧은 행정 라벨. 없으면 null. |
| `src/services/geocode.ts` | internal | function | `parseRow(row: Record<string, unknown>): PlaceResult \\| null` | num, str, displayName.split(',')[0]?.trim, displayName.split, parseBBox, classifyPlace, parseAddress | Nominatim 한 행 → PlaceResult. 좌표가 깨졌으면 null(버린다). |
| `src/services/geocode.ts` | export | function | `parseNominatimResults(json: unknown): PlaceResult[]` | Array.isArray, parseRow, out.push | Nominatim JSON 응답 → PlaceResult[](순수). 좌표 유효한 것만. |
| `src/services/geocode.ts` | internal | const | `NOMINATIM_REVERSE` | — | — |
| `src/services/geocode.ts` | export | function | `buildNominatimReverseUrl(lat: number, lng: number): string` | String, params.toString | 좌표 → 그 자리의 주소(역지오코딩) URL(순수). `zoom=18`은 건물 단위 다. 더 낮추면 동네 이름이 나오고, 더 올리면 없는 정밀도를 요구하게 된다. 지도에서 콕 찍은 지점에 이름을 붙이는 것이 목적이므로 건물이 맞다. |
| `src/services/geocode.ts` | export | function | `parseReverseResult(json: unknown): PlaceResult \\| null` | Array.isArray, parseNominatimResults | 역지오코딩 응답 → PlaceResult(순수). 배열이 아니라 객체 하나 로 온다. 바다 한가운데처럼 아무것도 없는 곳은 `{ error: … }`를 준다 — 그때는 null 이다. 이름을 지어내지 않는다(§8). 호출부는 그냥 좌표만 쓰고 이름은 사용자가 적는다. |
| `src/services/geocode.ts` | export | async function | `reverseGeocode(lat: number, lng: number): Promise<PlaceResult \\| null>` | Number.isFinite, fetch, buildNominatimReverseUrl, parseReverseResult, res.json | 좌표 → 그 자리의 이름·주소. 실패·결과없음은 null (throw하지 않는다). 🔴 왜 throw하지 않나: 이건 거들어 주는 기능 이다. 사용자는 이미 지도에서 지점을 확정했고 좌표는 손에 있다. 이름을 못 붙였다고 그 저장을 막으면, 편의 기능이 기억을 막는 셈이 된다(§0의 정신 — 앱이 사용자를 이기지 않는… |
| `src/services/geocode.ts` | export | async function | `searchNominatim(query: string, opts: GeocodeOptions = {}): Promise<PlaceResult[]>` | query.trim, fetch, buildNominatimUrl, parseNominatimResults(await res.json()).map, parseNominatimResults, res.json | 장소 검색(Nominatim 단독) — 실패 시 throw(호출부에서 안내). |
| `src/services/geocode.ts` | internal | const | `PROXY_FN` | — | — |
| `src/services/geocode.ts` | internal | interface | `ProxyRow` | — | Edge Function이 돌려주는 표준 행 — supabase/functions/geocode의 NormalizedRow와 같은 모양. |
| `src/services/geocode.ts` | export | function | `parseProxyRows(json: unknown, fallbackProvider: ProviderId): PlaceResult[]` | Array.isArray, num, str, parseBBox, out.push, classifyPlace, parseAddress | 프록시 행 → PlaceResult(순수). 🔴 정밀도는 여기서 다시 판정한다 — 서버가 보낸 등급을 그대로 믿지 않는다. 등급 규칙은 앱에 한 벌만 있어야 하고(§7 2층), 그래야 Nominatim과 국내 제공자가 같은 자 로 재진다. 서버가 옛 판이어도 앱의 자가 바뀌면 화면은 같이 바뀐다. |
| `src/services/geocode.ts` | internal | const | `cachedProbe: ProviderProbe \\| null` | — | 사용 가능 제공자를 세션에 한 번만 묻는다. 매 검색마다 `capabilities`를 되물으면 검색 한 번에 왕복이 둘이 된다. 이 값은 서버 시크릿 구성이라 세션 중에 바뀌지 않는다 — 바뀌었다면 새로고침이 정상적인 반영 경로다. |
| `src/services/geocode.ts` | export | async function | `ensureProviders(client: ProxyClient \\| null): Promise<ProviderProbe>` | proxyProviders | — |
| `src/services/geocode.ts` | export | function | `resetProviderCache(): void` | — | 검사용 — 캐시를 비운다. 제품 코드에서 부르지 않는다. |
| `src/services/geocode.ts` | export | typealias | `ProbeFailure` | — | 🔴 「물어봤더니 없다」와 「못 물어봤다」는 다른 사실이다 (§8 · T-025 · M-0148). 예전에는 둘 다 `[]`였다. 그래서 `geocode`가 CORS 때문에 브라우저에서 아예 안 불리는 상태가 오래 조용했다 — 앱은 「국내 제공자가 없나 보다」로 접고 Nominatim으로 내려갔고, 화면에는 아무 말도 … |
| `src/services/geocode.ts` | export | typealias | `ProviderProbe` | — | — |
| `src/services/geocode.ts` | export | function | `providersOf(probe: ProviderProbe): readonly ProviderId[]` | — | 검색 경로가 쓰는 목록. 못 물어본 상태는 빈 목록으로 취급한다 — 검색은 조용히 계속된다. |
| `src/services/geocode.ts` | export | async function | `proxyProviders(client: ProxyClient \\| null): Promise<ProviderProbe>` | client.functions.invoke, Array.isArray, list.filter | 프록시에 붙어 있는 국내 제공자 목록. 🔴 실패해도 검색 화면은 조용하다 — Nominatim이 답하므로 사용자에게 알릴 일이 아니다. 그러나 진단은 조용하면 안 된다. 그래서 사유를 잃지 않고 돌려준다(§8). |
| `src/services/geocode.ts` | export | interface | `ProxyClient` | — | 프록시 호출에 필요한 최소 모양만 요구한다(전체 Supabase 타입에 묶이지 않게). |
| `src/services/geocode.ts` | internal | async function | `searchViaProxy(client: ProxyClient, provider: ProviderId, query: string): Promise<PlaceResult[]>` | client.functions.invoke, parseProxyRows | — |
| `src/services/geocode.ts` | export | interface | `RoutedOptions` | — | — |
| `src/services/geocode.ts` | export | async function | `searchPlaces(query: string, opts: RoutedOptions = {}): Promise<PlaceResult[]>` | query.trim, providerOrder, searchNominatim, searchViaProxy, dedupePlaces, String | 장소 검색 — 순서대로 묻고, 답이 있으면 멈춘다. 정직한 설계 선택 두 가지: 1. Nominatim은 언제나 마지막에 있다. 국내 제공자가 없거나·실패하거나·빈 결과를 줘도 앱은 답을 낸다. 설정 0에서 동작하는 것이 기본값이고, 국내 제공자는 개선이지 전제가 아니다. 2. 앞선 제공자가 답을 주면 거기서 멈춘다. … |
| `src/services/here.ts` | export | interface | `HereReading` | — | — |
| `src/services/here.ts` | internal | const | `FAIL_BY_CODE: Record<number, HereFail>` | — | — |
| `src/services/here.ts` | export | function | `readHere(timeoutMs = 12_000): Promise<HereReading>` | Promise.resolve, resolve, setTimeout, finish, navigator.geolocation.getCurrentPosition, clearTimeout, Number.isFinite | 지금 위치를 한 번 읽는다. @param timeoutMs 기다릴 시간. 기본 12초 — 실외 GPS 최초 고정(cold fix)은 10초를 넘기는 일이 흔하다. 너무 짧게 잡으면 되는 상황을 안 된다고 말하게 된다. `enableHighAccuracy: true`인 이유: 이 값은 기억이 찍힐 자리 다. wifi 측위… |
| `src/services/mapRenderer.ts` | export | interface | `RenderPoint` | — | — |
| `src/services/mapRenderer.ts` | export | interface | `MapRendererHandle` | — | — |
| `src/services/mapRenderer.ts` | internal | interface | `JourneyRenderOptions` | — | — |
| `src/services/mapRenderer.ts` | internal | interface | `PickerRenderOptions` | — | — |
| `src/services/mapRenderer.ts` | internal | const | `SINGLE_POINT_ZOOM` | — | — |
| `src/services/mapRenderer.ts` | internal | const | `KAKAO_SINGLE_POINT_LEVEL` | — | — |
| `src/services/mapRenderer.ts` | internal | const | `LOAD_TIMEOUT_MS` | — | — |
| `src/services/mapRenderer.ts` | internal | async function | `renderJusoJourney(options: JourneyRenderOptions): Promise<MapRendererHandle>` | abortError, document.createElement, iframe.setAttribute, encodeURIComponent, sdkUrl.replaceAll, options.container.replaceChildren, options.points.map, toJusoMapPoint, iframe.contentWindow?.postMessage, window.setTimeout | — |
| `src/services/mapRenderer.ts` | internal | const | `OSM_STYLE` | — | — |
| `src/services/mapRenderer.ts` | internal | function | `tomtomStyle(key: string)` | encodeURIComponent | — |
| `src/services/mapRenderer.ts` | internal | function | `mapLibreStyle(options: { provider: MapDisplayProvider; tomtomKey: string; mapStyleUrl: string…)` | tomtomStyle | — |
| `src/services/mapRenderer.ts` | internal | typealias | `KakaoLatLng` | — | — |
| `src/services/mapRenderer.ts` | internal | typealias | `KakaoMap` | — | — |
| `src/services/mapRenderer.ts` | internal | typealias | `KakaoMarker` | — | — |
| `src/services/mapRenderer.ts` | internal | typealias | `KakaoOverlay` | — | — |
| `src/services/mapRenderer.ts` | internal | typealias | `KakaoEventTarget` | — | — |
| `src/services/mapRenderer.ts` | internal | typealias | `KakaoListener` | — | — |
| `src/services/mapRenderer.ts` | internal | interface | `KakaoMaps` | — | — |
| `src/services/mapRenderer.ts` | internal | const | `kakaoPromise: Promise<KakaoMaps> \\| null` | — | — |
| `src/services/mapRenderer.ts` | internal | function | `abortError(): DOMException` | — | — |
| `src/services/mapRenderer.ts` | internal | function | `waitForKakaoReady(resolve: (maps: KakaoMaps) => void, reject: (reason?: unknown) => void): void` | reject, maps.load, resolve | — |
| `src/services/mapRenderer.ts` | internal | function | `loadKakaoMaps(key: string): Promise<KakaoMaps>` | Promise.resolve, Promise.reject, document.querySelector, document.createElement, window.setTimeout, reject, window.clearTimeout, waitForKakaoReady, script.addEventListener, encodeURIComponent | — |
| `src/services/mapRenderer.ts` | internal | function | `waitForKakaoTiles(maps: KakaoMaps, map: KakaoMap, signal: AbortSignal): Promise<void>` | cleanup, resolve, reject, abortError, window.setTimeout, window.clearTimeout, signal.removeEventListener, maps.event.removeListener, signal.addEventListener, maps.event.addListener | — |
| `src/services/mapRenderer.ts` | internal | async function | `renderKakaoJourney(options: JourneyRenderOptions): Promise<MapRendererHandle>` | loadKakaoMaps, abortError, waitForKakaoTiles, bounds.extend, map.setCenter, map.setLevel, map.setBounds, options.popupNode, activeOverlay?.setMap, overlay.setMap | — |
| `src/services/mapRenderer.ts` | internal | async function | `mapLibreModule()` | import | — |
| `src/services/mapRenderer.ts` | internal | async function | `renderMapLibreJourney(options: JourneyRenderOptions): Promise<MapRendererHandle>` | mapLibreModule, abortError, mapLibreStyle, map.addControl, map.remove, reject, window.setTimeout, options.signal.removeEventListener, options.signal.addEventListener, map.once | — |
| `src/services/mapRenderer.ts` | internal | async function | `renderKakaoPicker(options: PickerRenderOptions): Promise<MapRendererHandle>` | loadKakaoMaps, abortError, waitForKakaoTiles, marker.getPosition, options.onPick, position.getLat, position.getLng, maps.event.addListener, event.latLng.getLat, event.latLng.getLng | — |
| `src/services/mapRenderer.ts` | internal | async function | `renderMapLibrePicker(options: PickerRenderOptions): Promise<MapRendererHandle>` | mapLibreModule, abortError, mapLibreStyle, map.addControl, map.remove, reject, window.setTimeout, options.signal.removeEventListener, options.signal.addEventListener, map.once | — |
| `src/services/mapRenderer.ts` | export | async function | `renderJourneyMap(options: JourneyRenderOptions): Promise<MapRendererHandle>` | renderKakaoJourney, renderJusoJourney, renderMapLibreJourney | — |
| `src/services/mapRenderer.ts` | export | async function | `renderPickerMap(options: PickerRenderOptions): Promise<MapRendererHandle>` | renderKakaoPicker, renderMapLibrePicker | — |
| `src/services/places.ts` | internal | function const | `uuid()` | crypto.randomUUID | — |
| `src/services/places.ts` | export | function | `placeOp(operationId: string, entityId: string, operationType: SyncQueueItem['operationType'], createdAt: string): SyncQueueItem` | — | 'place' 동기화 op 하나 — 형제(`audioOp`·`mediaOp`)와 같은 모양 이다. 여기서 만드는 `entityType`이 `sync.ts`의 `pushPendingPlaces`가 고르는 값이고, 다른 도메인 push 루프가 자연히 건너뛰는 값이기도 하다. |
| `src/services/places.ts` | export | interface | `LinkedPlaceNameSyncResult` | — | — |
| `src/services/places.ts` | export | async function | `mirrorLinkedPlaceNameInTransaction(d: JourneyDB, placeId: string, rawName: string, now: string): Promise<LinkedPlaceNameSyncResult>` | rawName.trim, d.localPlaces.get, uuid, d.localPlaces.put, d.syncQueue.add, placeOp, d.localMoments.filter((moment) => moment.placeId === placeI…, d.localMoments.filter, d.localMoments.put, momentIds.push | 연결된 장소 이름의 유일한 쓰기 문 . 호출자는 반드시 `localPlaces`·`localMoments`· `syncQueue`를 포함한 Dexie 쓰기 트랜잭션 안에 있어야 한다. 이름은 링크된 장소의 표시값이므로 양쪽에서 같게 유지한다. 좌표는 당시 기록이므로 손대지 않는다. 각 행과 각 op을 같은 트랜잭션에서 … |
| `src/services/places.ts` | export | async function | `verifyLinkedPlaceNameReadBack(placeId: string, name: string): Promise<void>` | db, Promise.all, d.localPlaces.get, d.localMoments.filter((moment) => moment.placeId === placeI…, d.localMoments.filter, linked.some | — |
| `src/services/places.ts` | export | interface | `NewPlace` | — | 새 장소를 만들 때 필요한 값. 좌표는 필수 — 좌표 없는 장소는 라이브러리에 담지 않는다. |
| `src/services/places.ts` | internal | const | `SAME_PLACE_METERS` | — | 같은 곳으로 볼 거리(m) — 제공자 id가 없을 때의 판정 기준. `provider.ts`와 같은 자를 쓴다. |
| `src/services/places.ts` | export | async function | `findExistingPlace(input: { name: string; latitude: number; longitude: number; provider?: string \…): Promise<LocalPlace \\| null>` | db, providerKeyOf, d.localPlaces.where('providerKey').equals(key).toArray, d.localPlaces.where('providerKey').equals, d.localPlaces.where, hit.find, p.name.trim, input.name.trim, d.localPlaces.toArray, all.find | 이미 담아 둔 같은 장소를 찾는다(활성만). 두 단계로 본다: ① 제공자 id가 같으면 같은 곳이다. 가장 강한 근거 — 이름이나 좌표가 조금 달라도 같다. ② id가 없으면(지도로 찍은 곳 등) 이름이 같고 가까울 때만 같은 곳으로 본다. 이름만으로 접으면 안 된다 — 「대학로」는 여러 도시에 있고, 그 구분이 원래 … |
| `src/services/places.ts` | export | async function | `registerMomentPlace(input: { name: string; latitude: number \\| null; longitude: number \\| null; }): Promise<string \\| null>` | input.name.trim, isRealCoord, savePlace | 순간 저장값을 장소 대장의 기준본으로 등록하고 그 항목 id를 돌려준다. |
| `src/services/places.ts` | internal | const | `reconcileInFlight: Promise<number> \\| null` | — | 옛 순간을 현재 계약으로 소급한다. 반복 실행해도 같은 이름+좌표는 같은 대장 행으로 모인다. 서버에서 나중에 내려온 옛 순간도 다음 홈 진입 때 다시 대상이 되므로 일회성 마커를 두지 않는다. |
| `src/services/places.ts` | export | async function | `reconcileMomentPlaces(): Promise<number>` | reconcileMomentPlacesOnce | — |
| `src/services/places.ts` | internal | async function | `reconcileMomentPlacesOnce(): Promise<number>` | db, (await d.localMoments.toArray()).filter( (m) => m.deletedAt…, (await d.localMoments.toArray()).filter, d.localMoments.toArray, m.placeName.trim, isRealCoord, compareInstants, a.id.localeCompare, m.placeLat!.toFixed, m.placeLng!.toFixed | — |
| `src/services/places.ts` | export | async function | `savePlace(input: NewPlace): Promise<LocalPlace>` | input.name.trim, Number.isFinite, db, d.transaction, findExistingPlace, new Date().toISOString, uuid, providerKeyOf, d.localPlaces.add, d.syncQueue.add | 장소를 라이브러리에 담는다 — 이미 있으면 그것을 돌려준다 (멱등). 왜 멱등인가: 사용자는 같은 카페를 열 번 고른다. 그때마다 새 행을 만들면 라이브러리가 쓰레기가 되고, 「이 근처에서 뭘 했더라」가 같은 점 열 개를 겹쳐 보여준다. |
| `src/services/places.ts` | export | async function | `listPlaces(): Promise<LocalPlace[]>` | db().localPlaces.toArray, db, rows .filter((p) => p.deletedAt === null) .sort, rows .filter, compareInstants | 담아 둔 장소 목록(활성만, 최근 갱신 순). |
| `src/services/places.ts` | export | async function | `placesNear(center: { lat: number; lng: number }, radiusMeters: number): Promise<{ place: LocalPlace; distanceMeters: number }[]>` | listPlaces, rows .map((place) => ({ place, distanceMeters: Math.round(r…, rows .map, Math.round, roughDistanceMeters | 좌표 근처의 장소(가까운 순). 서버에는 PostGIS + GiST 인덱스가 있지만(0022), 로컬은 전수 스캔이다. 개인 앱 규모 (장소 수백 건)에서 전수 스캔은 밀리초이고, 로컬에 공간 인덱스를 흉내 내는 복잡도는 그 이득에 비해 비싸다. 오프라인에서도 답이 나오는 것이 더 중요하다(원칙: 로컬 우선). |
| `src/services/places.ts` | export | interface | `PlacePatch` | — | 수정 가능한 칸. 이름·메모·분류와 좌표 (사용자가 지도에서 고쳐 확정할 수 있다). |
| `src/services/places.ts` | export | interface | `PlaceUpdateResult` | — | — |
| `src/services/places.ts` | internal | typealias | `PlaceNamePolicy` | — | — |
| `src/services/places.ts` | internal | async function | `updatePlaceWithNamePolicy(id: string, patch: PlacePatch, namePolicy: PlaceNamePolicy): Promise<PlaceUpdateResult>` | db, d.localPlaces.get, new Date().toISOString, uuid, patch.name.trim, Number.isFinite, d.transaction, d.localPlaces.put, d.syncQueue.add, placeOp | 이름 정책은 기본값을 두지 않는다. 사용자 편집과 옛 placeId 분리 소급은 사정권이 다르다. |
| `src/services/places.ts` | export | async function | `updatePlace(id: string, patch: PlacePatch): Promise<PlaceUpdateResult>` | updatePlaceWithNamePolicy | 장소를 고친다. 연결된 동안 이름은 한 값 이다. 대장에서 이름을 바꾸면 직접 연결된 활성·휴지통 순간의 `placeName`도 같은 트랜잭션에서 바뀐다. 반면 좌표는 당시 기록이므로 대장 좌표를 고쳐도 순간 좌표에는 전파하지 않는다. |
| `src/services/places.ts` | export | typealias | `DeleteUnusedPlaceResult` | — | — |
| `src/services/places.ts` | internal | async function | `softDeletePlaceInternal(id: string, onlyIfUnused: boolean): Promise<DeleteUnusedPlaceResult>` | db, d.transaction, d.localPlaces.get, d.localMoments.filter((moment) => moment.placeId === id).to…, d.localMoments.filter, linked.filter, new Date().toISOString, uuid, d.localPlaces.put, d.syncQueue.add | 장소 tombstone의 유일한 쓰기 문 . 대장 화면의 "미연결 장소 삭제"는 순간 참조 검사와 tombstone+op을 같은 트랜잭션에 둔다. 검사 뒤 삭제를 따로 쓰면 그 사이에 순간이 연결될 수 있고, 그러면 화면의 "미연결" 판정이 이미 낡은 값이 된다(동기화는 사용자 작업 사이에 끼어든다는 sync-offli… |
| `src/services/places.ts` | export | async function | `softDeletePlace(id: string): Promise<void>` | softDeletePlaceInternal | 삭제 — 하드 삭제 없음 (§0). `deletedAt` tombstone만 세운다. 순간들의 링크는 여기서 끊지 않는다. 서버는 `on delete set null`로 영구삭제 때만 끊고, tombstone 동안에는 되살리면 링크가 그대로 살아난다 (실행취소가 진짜 되돌리기가 되게). |
| `src/services/places.ts` | export | async function | `deleteUnusedPlace(id: string): Promise<DeleteUnusedPlaceResult>` | softDeletePlaceInternal | 직접 연결된 순간이 하나도 없는 장소만 휴지통으로 보낸다. 이름만 같은 순간은 링크가 아니므로 막지 않는다. 반대로 사진이 0장이어도 직접 연결된 순간이 있으면 막는다 — 사진 없는 글 기록도 사용자의 기억이기 때문이다. |
| `src/services/places.ts` | export | async function | `restorePlace(id: string): Promise<void>` | db, d.localPlaces.get, new Date().toISOString, uuid, d.transaction, d.localPlaces.put, d.syncQueue.add, placeOp | 되살리기 — 삭제와 쌍 이다(`check-domain-symmetry`가 이 대칭을 강제한다). |
| `src/services/places.ts` | export | async function | `fillAddressFromCoords(id: string): Promise<boolean>` | db, d.localPlaces.get, Number.isFinite, reverseGeocode, updatePlace | 좌표로 주소를 받아 대장 항목에 채운다. 🔴 이름은 건드리지 않는다. 지오코더가 준 것은 주소일 뿐이고, 그 장소를 뭐라 부를지는 사용자가 정한다 — 사용자가 「집」이라 적은 곳을 「Yunusobod tumani」로 덮으면 안 된다. 실패는 조용히 넘어간다 (throw하지 않는다). 이건 거들어 주는 기능이라, 주소를… |
| `src/services/places.ts` | export | async function | `allMomentsForPlaceLookup(): Promise< { id: string; tripId: string; title: string; occurredAt: string; placeName: string; placeI…` | db, d.localMoments.toArray, rows.map | 이 장소를 쓰는 순간들 — 대장이 「고치기 전에 어디에 영향이 가나」를 보여줄 때 쓴다. 🔴 전 여행을 훑는다. 장소는 여행에 매이지 않으므로(같은 카페를 여러 여행에서 쓴다) `listMoments(tripId)`로는 답이 안 나온다. 지운 순간은 뺀다. 판정(무엇을 「이 장소」로 볼 것인가)은 `domain/pla… |
| `src/services/places.ts` | export | async function | `allPlaceRecordMoments(): Promise< { id: string; tripId: string; tripTitle: string; title: string; occurredAt: string; placeN…` | db, Promise.all, d.localTrips.toArray, d.localMoments.toArray, trips.filter((t) => t.deletedAt === null).map, trips.filter, moments.filter((m) => tripTitles.has(m.tripId)).map, moments.filter, tripTitles.has, tripTitles.get | 장소 기록 모달의 읽기 전용 스냅샷. 저장·큐·동기화는 건드리지 않는다. |
| `src/services/places.ts` | export | async function | `mediaForPlaceRecordMoments(momentIds: readonly string[]): Promise<Map<string, { id: string; thumbBlob: Blob }[]>>` | orderPhotos, (await db().localMedia.where('momentId').anyOf([...momentId…, db().localMedia.where('momentId').anyOf([...momentIds]).toA…, db().localMedia.where('momentId').anyOf, db().localMedia.where, db, result.get, existing.push, result.set | 기록 모달을 실제로 열었을 때만 해당 순간들의 썸네일을 읽는다. 대장 진입에서 모든 Blob을 `toArray()`로 꺼내지 않아 긴 여행 기록의 메모리를 불필요하게 쓰지 않는다. |
| `src/ui/externalMapRow.ts` | export | interface | `ExternalMapRowOptions` | — | — |
| `src/ui/externalMapRow.ts` | export | function | `externalMapRow(place: PlaceLike, opts: ExternalMapRowOptions): HTMLElement \\| null` | externalMapTargets, el, wrap.appendChild, btn.setAttribute, btn.addEventListener, hasAgreedExternalMap, window.confirm, externalMapConsentText, rememberAgreedExternalMap, window.open | 바깥 지도 칩 줄. 열 곳이 없으면 `null`. @param place 좌표가 없어도 된다 — 이름만 있으면 이름으로 찾는 링크를 만든다 (`precision: 'name'`). 그게 이 부품이 장소 검색 실패 자리에서 쓰이는 이유다. |
| `src/ui/screens/mapView.ts` | internal | function | `markerColor(): string` | getComputedStyle(document.documentElement).getPropertyValue…, getComputedStyle(document.documentElement).getPropertyValue, getComputedStyle | 지도 마커 색 — 하드코딩하지 않고 `--a1`(여름 강조색) 토큰에서 읽는다 (색 SSOT는 tokens.css). MapLibre `Marker`는 CSS 변수가 아니라 문자열을 요구하므로 런타임에 계산값을 한 번 뽑는다. 토큰을 못 읽는 극단적 경우(테스트 등)만 그 토큰의 기본값으로 떨어진다. |
| `src/ui/screens/mapView.ts` | export | interface | `MapPoint` | — | — |
| `src/ui/screens/mapView.ts` | internal | function | `externalMapButton(place: PlaceLike): HTMLElement \\| null` | externalMapRow | 「다른 지도에서 열기」 줄. 없으면 만들지 않는다 (열 곳이 없으면 버튼도 없다). 왜 앱 지도 안 에 두는가(사용자 결정 2026-07-27): 앱 지도가 주(主)다 — 비공개이고 오프라인에서도 뜬다. 바깥 지도는 길찾기·스트리트뷰가 필요할 때 한 걸음 더 가서 여는 곳이다. 그리는 일은 `ui/externalMapR… |
| `src/ui/screens/mapView.ts` | internal | function | `downloadBlob(blob: Blob, filename: string): void` | URL.createObjectURL, el, document.body.appendChild, a.click, a.remove, setTimeout, URL.revokeObjectURL | — |
| `src/ui/screens/mapView.ts` | internal | function | `pointNode(p: MapPoint, objectUrls: string[]): HTMLElement` | el, URL.createObjectURL, objectUrls.push, box.appendChild | 팝업/목록에 쓰는 순간 카드(DOM 노드 — 문자열 보간 없음). objectUrls에 썸네일 URL 적재. |
| `src/ui/screens/mapView.ts` | internal | function | `emptyState(focusPlace?: PlaceLike): HTMLElement` | el, empty.append | 위치가 하나도 없을 때의 안내. 장소 칩에서 열었는데 좌표가 없는 경우 와 여행에 위치가 하나도 없는 경우 는 다른 사정이다. 같은 문장으로 뭉뚱그리면 사용자는 무엇을 해야 할지 알 수 없다 — 앞은 "이 장소에 좌표를 넣으려면 검색으로 고르라"이고, 뒤는 "사진에 GPS가 있으면 나타난다"이다. |
| `src/ui/screens/mapView.ts` | internal | interface | `JourneyMapMountOptions` | — | — |
| `src/ui/screens/mapView.ts` | internal | async function | `mountJourneyMap(options: JourneyMapMountOptions): Promise<void>` | ((import.meta.env.VITE_KAKAO_JAVASCRIPT_KEY as string \\| un…, ((import.meta.env.VITE_JUSO_MAP_KEY as string \\| undefined)…, ((import.meta.env.VITE_TOMTOM_API_KEY as string \\| undefine…, ((import.meta.env.VITE_MAP_STYLE_URL as string \\| undefined…, displayProviderForPoints, displayProviderFallbackOrder, options.mapEl.replaceChildren, renderJourneyMap, markerColor, pointNode | 제공자 선택·폴백은 화면 생명주기와 분리해 두 제공자가 같은 종료 계약을 지나게 한다. |
| `src/ui/screens/mapView.ts` | export | function | `openMapView(tripTitle: string, points: MapPoint[], focusPlace?: PlaceLike): void` | el, overlay.setAttribute, header.appendChild, geoBtn.setAttribute, geoBtn.addEventListener, toFeatureCollection, JSON.stringify, downloadBlob, closeBtn.setAttribute, actions.append | 지도 모달을 연다. 위치가 있는 순간(points)을 지도/목록으로 보여준다. @param focusPlace 장소 칩에서 연 경우의 그 장소 . 있으면 「구글지도로 열기」를 붙인다. 여행 전체 지도에는 붙이지 않는다 — 여러 장소를 링크 하나로 가리킬 수 없고, 억지로 하나를 고르면 앱이 말하지 않은 선택 을 하는 셈… |
| `src/ui/screens/mapView.ts` | internal | interface | `PickerStart` | — | — |
| `src/ui/screens/mapView.ts` | internal | async function | `resolvePickerStart(initial: { lat: number; lng: number } \\| null, signal: AbortSignal): Promise<PickerStart \\| null>` | readHere, hereFailMessage, reverseGeocode | 새 위치의 현재 좌표 읽기와 1회 국가 판정을 화면 조립에서 분리한다. |
| `src/ui/screens/mapView.ts` | export | function | `openMapPicker(initial: { lat: number; lng: number } \\| null): Promise<{ lat: number; lng: number } \\| null>` | el, overlay.setAttribute, header.appendChild, closeBtn.setAttribute, actions.append, body.append, modal.append, overlay.appendChild, renderAbort.abort, map?.destroy | 지도에서 위치를 골라 좌표를 반환한다(Nominatim에 없는 곳·정확한 지점용). 사용자가 지도를 탭하면 마커가 놓이고, "이 위치로 지정"으로 확정. 취소·닫기는 null. WebGL/타일 불가 기기에서는 안내만 표시(장소 검색으로 대체 가능). |
| `src/ui/screens/placeRegistry.ts` | internal | typealias | `GoToTrip` | — | — |
| `src/ui/screens/placeRegistry.ts` | internal | const | `PUBLIC_GEOCODER_GAP_MS` | — | — |
| `src/ui/screens/placeRegistry.ts` | internal | function | `wait(ms: number): Promise<void>` | window.setTimeout | — |
| `src/ui/screens/placeRegistry.ts` | internal | function | `toRegistry(p: { id: string; name: string; latitude: number; longitude: number; country: st…): RegistryPlace` | — | `LocalPlace` → 대장이 아는 최소 모양. 화면은 이 형태만 안다. |
| `src/ui/screens/placeRegistry.ts` | internal | function | `coordEditor(p: RegistryPlace, onSaved: () => void, note: HTMLElement): HTMLElement` | el, input.setAttribute, preview.replaceChildren, orderPair, parseTwo, preview.appendChild, coordPreview, row.appendChild, swapBtn.addEventListener, paint | 좌표 편집 한 줄 — 지금 좌표와 뒤집은 좌표를 나란히 보여준다(사용자 결정 2026-08-05). 저장은 한 벌이고 보기가 두 벌이다. 두 벌을 저장하면 어느 쪽이 정본인지 앱이 모르게 되고, 그러면 지도·통계가 조용히 한쪽을 고른다 — M-0057이 그 형태였다(기억이 기니만 앞바다에 찍힘). |
| `src/ui/screens/placeRegistry.ts` | internal | function | `parseTwo(v: string): [number, number]` | v.split(/[,\s]+/).filter, v.split, Number | `"41.3, 69.2"` → `[41.3, 69.2]`. 못 읽으면 NaN 쌍(orderPair가 null을 준다). |
| `src/ui/screens/placeRegistry.ts` | internal | function | `registryRow(p: RegistryPlace, moments: PlaceRecordMoment[], onChanged: () => void, goToTrip: GoToTrip): HTMLElement` | el, openRecords.setAttribute, openRecords.append, shortAddress, needsAddress, p.latitude.toFixed, p.longitude.toFixed, editOpen.setAttribute, summary.append, row.appendChild | 대장 한 줄. |
| `src/ui/screens/placeRegistry.ts` | internal | function | `appendUnusedPlaceDelete(body: HTMLElement, place: RegistryPlace, groups: ReturnType<typeof placeRecordGroups>, dismiss: () => void, onChanged: () => void): void` | el, deleteBtn.setAttribute, status.setAttribute, deleteBtn.addEventListener, window.confirm, (async () => { try { const result = await deleteUnusedPlace…, deleteUnusedPlace, [ result.activeMomentCount ? `연결된 순간 ${result.activeMomentC…, setNote, parts.join | 장소 행은 길게 유지하고, 기록은 별도 모달에서만 보여 준다. |
| `src/ui/screens/placeRegistry.ts` | internal | function | `openPlaceRecords(place: RegistryPlace, moments: PlaceRecordMoment[], goToTrip: GoToTrip, onChanged: () => void): void` | placeRecordGroups, el, overlay.setAttribute, close.setAttribute, header.append, titleWrap.append, URL.revokeObjectURL, release, overlay.remove, document.removeEventListener | — |
| `src/ui/screens/placeRegistry.ts` | internal | function | `unusedPlaceSelectionList(items: RegistryPlace[], selected: Set<string>, disabled: boolean, rerender: () => void): HTMLElement` | el, selectAll.addEventListener, selected.clear, selected.add, rerender, selectAllLabel.append, controls.append, section.appendChild, selected.has, check.setAttribute | — |
| `src/ui/screens/placeRegistry.ts` | internal | function | `openUnusedPlacePicker(onChanged: () => void): void` | el, overlay.setAttribute, close.setAttribute, title.append, header.append, modal.append, overlay.appendChild, document.body.appendChild, overlay.remove, document.removeEventListener | Separate, safe bulk cleanup dialog for places without a direct moment link. |
| `src/ui/screens/placeRegistry.ts` | internal | async function | `fillMissingAddresses(targets: RegistryPlace[], note: HTMLElement): Promise<void>` | targets.entries, setNote, fillAddressFromCoords, wait | — |
| `src/ui/screens/placeRegistry.ts` | export | function | `placeRegistryPanel(onChanged: () => void, goToTrip: GoToTrip): HTMLElement` | el, box.append, bulkNote.setAttribute, tools.append, search.setAttribute, box.appendChild, sortRegistry, searchRegistry, unlinkedPlaceMoments, all.filter | 위치관리대장 패널. 사진에 위치가 없거나 고치고 싶을 때 여기서 장소를 찾아 좌표를 확인·수정한다. 연결된 장소 이름은 순간과 함께 바뀌고, 당시 좌표는 그대로 남는다. 링크가 이미 끊긴 순간은 별도 「연결 필요」 목록에 보여 사용자가 정확한 장소를 다시 고를 수 있게 한다. |

## 5. 자동 검증 연결

- `tests/unit/coordInput.test.ts`
- `tests/unit/externalMap.test.ts`
- `tests/unit/geocode.test.ts`
- `tests/unit/geocodeProvider.test.ts`
- `tests/unit/geocodeProxy.test.ts`
- `tests/unit/geojson.test.ts`
- `tests/unit/here.test.ts`
- `tests/unit/jusoMap.test.ts`
- `tests/unit/mapProvider.test.ts`
- `tests/unit/momentPlaceReconcile.test.ts`
- `tests/unit/photoHint.test.ts`
- `tests/unit/placeRegistry.test.ts`
- `tests/unit/placeSiblingDiscipline.test.ts`
- `tests/unit/precision.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/ui/screens/placeRegistry.ts` — 739줄
- `src/services/places.ts` — 683줄
- `src/services/mapRenderer.ts` — 500줄
- `src/ui/screens/mapView.ts` — 439줄
- `src/services/geocode.ts` — 429줄
- `src/domain/place/registry.ts` — 273줄
- `src/domain/place/coordInput.ts` — 238줄
- `src/domain/place/externalMap.ts` — 200줄
- `src/domain/place/precision.ts` — 194줄
- `src/domain/place/photoHint.ts` — 160줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
