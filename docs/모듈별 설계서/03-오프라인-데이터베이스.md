---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 오프라인 데이터베이스와 Row Map 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

Dexie 스키마와 로컬 행·서버 행 변환 경계를 소유한다.

- 모듈 ID: `offline-db`
- 관측 파일: 8개 · 논리 줄: 1216 · 최상위 선언: 46개(외부 공개 45개)
- 코드 내용 SHA-256: `f15b2117a2b5e60a00b83e03cbe0c6b3988d521d95b81dedc807b5c5f79a00b0`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/audio/rowmap.ts`

### 다른 모듈로 나가는 정적 의존

- 여행·순간 핵심 도메인(`travel-core`): import/re-export 7건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 3건
- 사진편집기(`photo-editor`): import/re-export 1건

### 외부 패키지/런타임 의존

- `dexie`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/audio/rowmap.ts` | 139 | 5/5 | src/domain/media/naming.ts<br>src/domain/time.ts<br>src/offline/db.ts | deletion lifecycle |
| `src/domain/expense/rowmap.ts` | 69 | 3/3 | src/domain/time.ts<br>src/offline/db.ts | deletion lifecycle |
| `src/domain/media/rowmap.ts` | 145 | 5/5 | src/domain/media/naming.ts<br>src/domain/time.ts<br>src/offline/db.ts | deletion lifecycle |
| `src/domain/moment/rowmap.ts` | 84 | 3/3 | src/domain/time.ts<br>src/offline/db.ts | deletion lifecycle |
| `src/domain/place/rowmap.ts` | 132 | 4/4 | src/domain/time.ts<br>src/offline/db.ts | transactional persistence; deletion lifecycle |
| `src/domain/trip/rowmap.ts` | 75 | 3/3 | src/domain/time.ts<br>src/offline/db.ts | deletion lifecycle; browser key/value storage |
| `src/domain/video/rowmap.ts` | 106 | 5/5 | src/domain/media/naming.ts<br>src/domain/time.ts<br>src/offline/db.ts | deletion lifecycle |
| `src/offline/db.ts` | 466 | 17/18 | src/media/editor-core.ts | file/blob bytes; transactional persistence; deletion lifecycle |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/audio/rowmap.ts` | export | interface | `AudioRow` | — | Supabase journey.audio 행 (snake_case — 이 파일 밖에서 사용 금지). blob은 담지 않는다. |
| `src/domain/audio/rowmap.ts` | export | interface | `AudioMeta` | — | 행에서 복원되는 메타(바이트 제외). pull이 여기에 내려받은 blob을 붙여 LocalAudio를 만든다. |
| `src/domain/audio/rowmap.ts` | export | function | `toAudioRow(a: LocalAudio, userId: string, storagePath: string \\| null, device?: string): AudioRow` | Math.max | — |
| `src/domain/audio/rowmap.ts` | export | function | `fromAudioRow(r: AudioRow): WithInstants<AudioMeta>` | isoInstant, isoInstantOrNull | 서버 행 → 메타. 시각은 반드시 `isoInstant()`를 통과한다(M-0034). 왜 한 줄도 빼면 안 되나: 서버는 `…48.34+00:00`, 로컬은 `…48.340Z`로 같은 순간을 다르게 적는다. 문자열로 비교하는 자리(정렬·`mergeDecision`·진단의 시간 역전)가 그 차이를 시간 차이로 읽는다. … |
| `src/domain/audio/rowmap.ts` | export | function | `audioStoragePath(userId: string, audio: { id: string; tripId: string; recordedAt: string \\| null; mime: string }, tripTitle: string \\| null \\| undefined): string \\| null` | audioExt, tripFolderName, audioObjectName | Storage 경로 규약: `{userId}/{여행폴더}/{날짜_시간_제목__id32}.{ext}`. 사진과 같은 폴더에 나란히 산다. 다른 폴더로 가르고 싶은 모양이지만 그러면 안 된다 — 사용자가 콘솔에서 한 여행을 열면 그 여행의 자료가 한 자리에 있어야 하고, 그게 폴더명을 여행 제목으로 바꾼 이유였다(2026-… |
| `src/domain/expense/rowmap.ts` | export | interface | `ExpenseRow` | — | Supabase journey.expenses 행 (snake_case — 이 파일 밖에서 사용 금지). |
| `src/domain/expense/rowmap.ts` | export | function | `toExpenseRow(e: LocalExpense, userId: string, device?: string): ExpenseRow` | Math.max | — |
| `src/domain/expense/rowmap.ts` | export | function | `fromExpenseRow(r: ExpenseRow): WithInstants<LocalExpense>` | isoInstant, isoInstantOrNull | 서버 행 → 로컬 행. 시각은 반드시 `isoInstant()`를 통과한다(M-0034 — `domain/time.ts` 참조). |
| `src/domain/media/rowmap.ts` | export | interface | `MediaRow` | — | Supabase journey.media 행 (snake_case — 이 파일 밖에서 사용 금지). blob은 담지 않는다. |
| `src/domain/media/rowmap.ts` | export | interface | `MediaMeta` | — | 행에서 복원되는 메타(블롭 제외). pull이 여기에 다운로드한 blob을 붙여 LocalMedia를 만든다. |
| `src/domain/media/rowmap.ts` | export | function | `toMediaRow(m: LocalMedia, userId: string, storagePath: string \\| null, device?: string): MediaRow` | Math.max | — |
| `src/domain/media/rowmap.ts` | export | function | `fromMediaRow(r: MediaRow): WithInstants<MediaMeta>` | isoInstant, isoInstantOrNull | 서버 행 → 메타. 시각은 반드시 `isoInstant()`를 통과한다(M-0034 — `domain/time.ts` 참조). |
| `src/domain/media/rowmap.ts` | export | function | `mediaStoragePath(userId: string, media: { id: string; tripId: string; takenAt: string \\| null }, tripTitle: string \\| null \\| undefined): string` | tripFolderName, mediaObjectName | Storage 경로 규약: `{userId}/{여행폴더}/{날짜_시간_제목__id32}.webp`. 첫 칸은 반드시 userId다. 그게 이 앱의 인가 모델 전체다 — Edge Function은 검증된 sub로 `prefix`를 만들고 `key.startsWith(prefix)`가 아닌 키는 목록·삭제 어디에도 넣지 않… |
| `src/domain/moment/rowmap.ts` | export | interface | `MomentRow` | — | Supabase journey.moments 행 (snake_case — 이 파일 밖에서 사용 금지). |
| `src/domain/moment/rowmap.ts` | export | function | `toMomentRow(m: LocalMoment, userId: string, device?: string): MomentRow` | Math.max | — |
| `src/domain/moment/rowmap.ts` | export | function | `fromMomentRow(r: MomentRow): WithInstants<LocalMoment>` | isoInstant, isoInstantOrNull | 서버 행 → 로컬 행. 시각은 반드시 `isoInstant()`를 통과한다(M-0034 — `domain/time.ts` 참조). |
| `src/domain/place/rowmap.ts` | export | interface | `PlaceRow` | — | Supabase journey.places 행 (snake_case — 이 파일 밖에서 사용 금지). |
| `src/domain/place/rowmap.ts` | export | function | `providerKeyOf(provider: string \\| null, providerPlaceId: string \\| null): string \\| undefined` | — | `provider/providerPlaceId` → Dexie 인덱스 한 칸. 둘 중 하나라도 없으면 undefined 다. Dexie는 undefined인 속성을 인덱스에 넣지 않으므로, 지도에서 직접 찍은 장소들은 이 인덱스에서 통째로 빠진다 — 그게 맞다. 제공자 id가 없는 두 장소가 이름이 같다고 같은 곳이라 … |
| `src/domain/place/rowmap.ts` | export | function | `toPlaceRow(p: LocalPlace, userId: string, device?: string): PlaceRow` | Math.max | — |
| `src/domain/place/rowmap.ts` | export | function | `fromPlaceRow(r: PlaceRow): WithInstants<LocalPlace>` | providerKeyOf, isoInstant, isoInstantOrNull | 서버 행 → 로컬 장소. 시각은 반드시 `isoInstant()`를 통과한다(M-0034). 왜 한 줄도 빼면 안 되나: 서버는 `…48.34+00:00`, 로컬은 `…48.340Z`로 같은 순간을 다르게 적는다. 이 앱은 시각을 문자열로 비교하므로(LWW·정렬·진단) 표기가 곧 의미다. |
| `src/domain/trip/rowmap.ts` | export | interface | `TripRow` | — | Supabase trips 행 (snake_case — 이 파일 밖에서 사용 금지). |
| `src/domain/trip/rowmap.ts` | export | function | `toRow(t: LocalTrip, userId: string, device?: string): TripRow` | Math.max | — |
| `src/domain/trip/rowmap.ts` | export | function | `fromRow(r: TripRow): WithInstants<LocalTrip>` | isoInstant, isoInstantOrNull | 서버 행 → 로컬 행. 시각은 반드시 `isoInstant()`를 통과한다 (M-0034) — PostgREST는 `…48.34+00:00`, JS는 `…48.340Z`로 같은 순간을 다르게 적고, 이 앱은 시각을 문자열로 비교한다. 반환형이 `WithInstants`라 날것을 넣으면 컴파일이 막는다(§7 2층). |
| `src/domain/video/rowmap.ts` | export | interface | `VideoRow` | — | — |
| `src/domain/video/rowmap.ts` | export | interface | `VideoMeta` | — | — |
| `src/domain/video/rowmap.ts` | export | function | `toVideoRow(v: LocalVideo, userId: string, storagePath: string \\| null, device?: string): VideoRow` | Math.max | — |
| `src/domain/video/rowmap.ts` | export | function | `fromVideoRow(r: VideoRow): WithInstants<VideoMeta>` | isoInstant, isoInstantOrNull | — |
| `src/domain/video/rowmap.ts` | export | function | `videoStoragePath(userId: string, video: { id: string; tripId: string; takenAt: string \\| null; mime: string }, tripTitle: string \\| null \\| undefined): string \\| null` | videoExt, tripFolderName, videoObjectName | — |
| `src/offline/db.ts` | export | interface | `SyncMeta` | — | — |
| `src/offline/db.ts` | export | typealias | `CanonicalStage` | — | — |
| `src/offline/db.ts` | export | interface | `PendingCanonicalSnapshot` | — | 「이 기기 → 클라우드 최종본」의 재시작 가능한 작업 기록. 메타 스냅샷만 보관하고 Blob은 도메인 store의 내구성 사본을 사용한다. |
| `src/offline/db.ts` | export | interface | `LocalSyncState` | — | 동기화 제어 상태. 사용자 기억이 아니므로 백업·서버 병합 대상이 아니다. |
| `src/offline/db.ts` | export | interface | `LocalTrip` | — | — |
| `src/offline/db.ts` | export | interface | `LocalMoment` | — | — |
| `src/offline/db.ts` | export | interface | `LocalMedia` | — | — |
| `src/offline/db.ts` | export | interface | `LocalExpense` | — | — |
| `src/offline/db.ts` | export | interface | `LocalAudio` | — | 오디오 노트 — 순간에 붙는 짧은 녹음(≤60초). 소리와 말을 회수한다. ── 왜 별도 테이블인가 (§7 2층) ─────────────────────────────────────── `LocalMedia`에 `audioBlob?` 필드를 붙이는 방법도 있었지만 그러면 안 된다. `check-backup-coverag… |
| `src/offline/db.ts` | export | interface | `LocalVideo` | — | 영상 노트. 사진처럼 외부 원본은 건드리지 않고, 앱이 만든 경량본만 정본 바이트로 보관한다. `sourceBlob`은 경량본의 R2 GET 바이트 대조가 끝날 때까지만 남는 재시도용 스테이징 사본이다. 포스터도 재생 전 사용자에게 보이는 기억의 일부라 경량본과 같은 operation fence의 별도 R2 객체로 보관… |
| `src/offline/db.ts` | export | interface | `LocalPlace` | — | 장소 라이브러리 항목(마이그레이션 0022). 🔴 여행의 자식이 아니다. 형제(media·expenses·audio)는 전부 `momentId`/`tripId`를 갖고 부모가 지워지면 함께 지워진다. 장소는 사용자 소유 다 — 한 장소는 여러 여행에 걸치고, 작년 여행을 지웠다고 그 카페를 잊어야 할 이유가 없다. 이… |
| `src/offline/db.ts` | export | interface | `LocalFxRate` | — | — |
| `src/offline/db.ts` | export | typealias | `SyncQueueState` | — | 큐 항목 상태 — 셋뿐(SyncQueueItem.state 주석 참조). 성공은 행 삭제로 표현한다. |
| `src/offline/db.ts` | export | interface | `SyncQueueItem` | — | — |
| `src/offline/db.ts` | export | interface | `PurgedId` | — | — |
| `src/offline/db.ts` | export | class | `JourneyDB` | super, this.version(1).stores, this.version, this.version(2).stores, this.version(3).stores, this.version(4).stores, this.version(5).stores, this.version(6).stores, this.version(7).stores, this.version(8).stores | — |
| `src/offline/db.ts` | internal | const | `_db: JourneyDB \\| null` | — | — |
| `src/offline/db.ts` | export | function | `db(): JourneyDB` | — | — |

## 5. 자동 검증 연결

- `tests/unit/audioNote.test.ts`
- `tests/unit/audioRowmap.test.ts`
- `tests/unit/audioServerSync.test.ts`
- `tests/unit/audioSiblingDiscipline.test.ts`
- `tests/unit/backupFileRoundTrip.test.ts`
- `tests/unit/backupRoundtrip.test.ts`
- `tests/unit/bytesReconcileRun.test.ts`
- `tests/unit/canonicalSync.test.ts`
- `tests/unit/cascadeOps.test.ts`
- `tests/unit/diagnoseItems.test.ts`
- `tests/unit/expenseRowmap.test.ts`
- `tests/unit/instantFormat.test.ts`
- `tests/unit/mediaNaming.test.ts`
- `tests/unit/mediaRowmap.test.ts`
- `tests/unit/mediaSign.test.ts`
- `tests/unit/merge.test.ts`
- `tests/unit/momentPlaceReconcile.test.ts`
- `tests/unit/momentRowmap.test.ts`
- `tests/unit/momentTz.test.ts`
- `tests/unit/photoSearch.test.ts`
- `tests/unit/placeRowmap.test.ts`
- `tests/unit/placeSiblingDiscipline.test.ts`
- `tests/unit/purgeCascade.test.ts`
- `tests/unit/purgePropagation.test.ts`
- `tests/unit/restoreAtomicity.test.ts`
- `tests/unit/restoreDrill.test.ts`
- `tests/unit/restoreUnpurge.test.ts`
- `tests/unit/reuploadMissingBytes.test.ts`
- `tests/unit/rowmap.test.ts`
- `tests/unit/serverTrashAuthority.test.ts`
- `tests/unit/staleSyncWrite.test.ts`
- `tests/unit/strandedPurge.test.ts`
- `tests/unit/timeline.test.ts`
- `tests/unit/trash.test.ts`
- `tests/unit/videoRowmap.test.ts`
- `tests/unit/videoServerSync.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/offline/db.ts` — 466줄
- `src/domain/media/rowmap.ts` — 145줄
- `src/domain/audio/rowmap.ts` — 139줄
- `src/domain/place/rowmap.ts` — 132줄
- `src/domain/video/rowmap.ts` — 106줄
- `src/domain/moment/rowmap.ts` — 84줄
- `src/domain/trip/rowmap.ts` — 75줄
- `src/domain/expense/rowmap.ts` — 69줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
