---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 오디오 노트 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

녹음·재생·저장과 오디오 행/바이트 생명주기를 소유한다.

- 모듈 ID: `audio-notes`
- 관측 파일: 3개 · 논리 줄: 471 · 최상위 선언: 21개(외부 공개 17개)
- 코드 내용 SHA-256: `e5240641e09b73669b17e35deb1e62e474fc25289089de7103198b3af528031b`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/audio/note.ts`

### 다른 모듈로 나가는 정적 의존

- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 2건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 1건
- 화면·오버레이·상호작용(`ui-shell`): import/re-export 1건

### 외부 패키지/런타임 의존

- Node 내장 또는 저장소 내부 모듈만 정적으로 import한다.

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/audio/note.ts` | 77 | 8/8 | — | transactional persistence; browser media worker/codec |
| `src/services/audio.ts` | 164 | 5/7 | src/offline/db.ts<br>src/domain/media/quota.ts<br>src/domain/audio/note.ts | Dexie: localAudio; file/blob bytes; deletion lifecycle |
| `src/ui/audioNote.ts` | 230 | 4/6 | src/ui/dom.ts<br>src/domain/audio/note.ts<br>src/offline/db.ts | file/blob bytes; deletion lifecycle; browser media worker/codec; DOM interaction |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/audio/note.ts` | export | const | `MAX_SECONDS` | — | 녹음 길이 상한(초). 넘으면 자동으로 멈춘다 — 침묵 절삭이 아니라 멈춤 이다. |
| `src/domain/audio/note.ts` | export | const | `MIN_SECONDS` | — | 이보다 짧으면 실수로 눌렀다고 본다(빈 항목이 쌓이지 않게). |
| `src/domain/audio/note.ts` | export | const | `AUDIO_TYPES: { mime: string; ext: string }[]` | — | 브라우저가 실제로 만들어 줄 수 있는 오디오 형식 중 우리가 받는 것 . 앞에서부터 지원되는 첫 형식을 쓴다 — Opus가 같은 품질에 가장 작다. 확장자는 백업 ZIP 안 파일명과 재생 `type` 힌트에 쓰인다. |
| `src/domain/audio/note.ts` | export | function | `pickAudioType(supported: (mime: string) => boolean): { mime: string; ext: string } \\| null` | supported | 이 기기가 만들 수 있는 형식 하나를 고른다. 없으면 null — 「지원 안 함」을 정직하게 말한다. @param supported `MediaRecorder.isTypeSupported` 같은 판정 함수(주입 — 유닛이 갈래를 돈다). |
| `src/domain/audio/note.ts` | export | function | `extForAudioMime(mime: string): string` | AUDIO_TYPES.find, mime.startsWith, t.mime.split | mime → 확장자. 모르는 형식은 `bin`이 아니라 `webm` 으로 둔다(대부분이 그것이고, 재생이 된다). |
| `src/domain/audio/note.ts` | export | function | `formatDuration(sec: number): string` | Number.isFinite, Math.round, Math.floor, String(s % 60).padStart, String | 초 → `M:SS`. 화면의 칩(`🔊 0:14`)과 녹음 중 표시가 같은 함수 를 쓴다. 음수·NaN은 `0:00` — 지어낸 값을 보여주지 않는다. |
| `src/domain/audio/note.ts` | export | function | `acceptRecording(seconds: number, bytes: number): { ok: boolean; reason: string }` | Number.isFinite | 녹음을 저장할 수 있는가. 못 하면 왜인지 말한다(조용히 버리지 않는다). |
| `src/domain/audio/note.ts` | export | function | `recordingHint(elapsedSec: number): string` | Math.max, Math.floor, formatDuration | 녹음 중 남은 시간 안내. 상한이 다가오는 것을 미리 말한다 — 갑자기 멈추면 사용자는 고장이라고 생각한다(§12: 앱이 아는 것을 말하지 않으면 사람이 대신 알아내야 한다). |
| `src/services/audio.ts` | internal | function const | `uuid()` | crypto.randomUUID | — |
| `src/services/audio.ts` | export | function | `audioOp(operationId: string, entityId: string, operationType: SyncQueueItem['operationType'], createdAt: string): SyncQueueItem` | — | 'audio' 동기화 op 하나 — `media.ts`의 `mediaOp`와 같은 모양 이다. 여기서 만드는 `entityType`이 `sync.ts`의 `pushPendingAudio`가 고르는 값이고, 다른 도메인 push 루프(`if (op.entityType !== 'trip') continue`)가 자연히 건너… |
| `src/services/audio.ts` | internal | async function | `readStorageEstimate(): Promise<{ usage: number \\| null; quota: number \\| null }>` | navigator.storage.estimate | 브라우저 저장 현황(미지원이면 null — 추측하지 않는다). `media.ts`와 같은 규율. |
| `src/services/audio.ts` | export | async function | `addAudioToMoment(target: { momentId: string; tripId: string }, blob: Blob, durationSec: number, mime: string): Promise<LocalAudio>` | acceptRecording, quotaVerdict, estimateLocalBytes, readStorageEstimate, new Date().toISOString, uuid, Math.min, Math.round, db, d.transaction | 녹음을 순간에 붙인다. 로컬 커밋 + read-back까지 마친 뒤에야 "저장됨"이다 (불변식 #1). 실패는 전부 사람이 읽는 이유 로 던진다 — 조용히 버리면 사용자는 녹음이 사라진 줄 모른다. |
| `src/services/audio.ts` | export | async function | `listAudioByTrip(tripId: string): Promise<LocalAudio[]>` | db().localAudio.where('tripId').equals(tripId).toArray, db().localAudio.where('tripId').equals, db().localAudio.where, db, rows.filter | 이 여행의 오디오 노트(활성만) — 타임라인·저장 용량이 읽는다. |
| `src/services/audio.ts` | export | async function | `softDeleteAudio(id: string): Promise<void>` | db, d.localAudio.get, new Date().toISOString, uuid, d.transaction, d.localAudio.put, d.syncQueue.add, audioOp | 삭제 — 하드 삭제 없음 (§0). `deletedAt` tombstone만 세우고 blob은 그대로 둔다. 그래야 `restoreAudio`가 완전 복원한다(실행취소가 진짜 되돌리기가 된다). |
| `src/services/audio.ts` | export | async function | `restoreAudio(id: string): Promise<void>` | db, d.localAudio.get, new Date().toISOString, uuid, d.transaction, d.localAudio.put, d.syncQueue.add, audioOp | 되살리기 — 삭제와 쌍 이다(`check-domain-symmetry`가 이 대칭을 강제한다). |
| `src/ui/audioNote.ts` | internal | const | `playing: { audio: HTMLAudioElement; url: string; onStop: () => void; /** 이 재생에 붙인 이벤트 전부를 떼는 손잡이. **정리보다 먼저** 불러야 한다(아래 참조). */ …` | — | 지금 재생 중인 것 하나. 모듈 하나에 하나 — 동시 재생을 구조적으로 막는다 (각 칩이 자기 상태를 들고 있으면 "누가 재생 중인지"를 아무도 모른다). |
| `src/ui/audioNote.ts` | export | function | `stopAudioPlayback(): void` | p.detach, p.audio.pause, p.audio.removeAttribute, p.audio.load, URL.revokeObjectURL, p.onStop | 재생 중인 것을 멈추고 자원을 회수한다. 정리는 이 한 곳에서만 한다(§7 2층). ⚠️ 결함 이력(2026-07-28 사용자 실기기): "재생불가가 아닌데 재생불가라고 안내하네요." 정리 과정 자체가 `error` 이벤트를 발화시킨다 — 미디어 요소의 `src`를 비우고 `load()`를 부르면 브라우저는 "실을 것이… |
| `src/ui/audioNote.ts` | internal | function | `equalizer(): HTMLElement` | el, eq.setAttribute, eq.appendChild | 재생 중 이퀄라이저 세 칸. 멈추면 통째로 사라진다 — 장식이 아니라 상태 다. |
| `src/ui/audioNote.ts` | export | function | `audioChip(a: LocalAudio, onDelete?: () => void): HTMLElement` | el, formatDuration, btn.setAttribute, btn.append, icon.replaceChildren, chip.style.setProperty, setIdle, btn.addEventListener, e.stopPropagation, stopAudioPlayback | — |
| `src/ui/audioNote.ts` | export | interface | `RecordResult` | — | — |
| `src/ui/audioNote.ts` | export | function | `recordButton(onDone: (r: RecordResult) => void): HTMLButtonElement` | el, window.clearInterval, stream?.getTracks, t.stop, btn.classList.remove, btn.addEventListener, rec.stop, pickAudioType, MediaRecorder.isTypeSupported, navigator.mediaDevices.getUserMedia | 녹음 버튼. 누르면 시작, 다시 누르면 멈춘다. 상한(60초)에 닿으면 스스로 멈춘다 . 실패는 전부 버튼 라벨로 말한다 — 마이크 권한 거부·미지원은 사용자가 알아야 고친다(§12). |

## 5. 자동 검증 연결

- `tests/unit/audioNote.test.ts`
- `tests/unit/audioServerSync.test.ts`
- `tests/unit/audioSiblingDiscipline.test.ts`
- `tests/unit/bytesReconcileRun.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/ui/audioNote.ts` — 230줄
- `src/services/audio.ts` — 164줄
- `src/domain/audio/note.ts` — 77줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
