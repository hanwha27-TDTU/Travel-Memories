---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 인증·Supabase 클라이언트·동의 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

사용자 세션, 인증 게이트, Supabase 클라이언트와 개인정보·외부 전송 동의를 소유한다.

- 모듈 ID: `auth-security`
- 관측 파일: 4개 · 논리 줄: 342 · 최상위 선언: 28개(외부 공개 22개)
- 코드 내용 SHA-256: `de90632958a99498d0cb6b7dc84d9c8a94393fb77f301575684cf8c8df92ba82`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/authGate.ts`

### 다른 모듈로 나가는 정적 의존

- 플랫폼 런타임·파일 저장·업데이트(`platform-runtime`): import/re-export 1건

### 외부 패키지/런타임 의존

- `@supabase/supabase-js`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/authGate.ts` | 121 | 6/7 | — | transactional persistence; deletion lifecycle |
| `src/services/auth.ts` | 117 | 9/10 | src/services/supabase/client.ts<br>src/services/capacitorShell.ts | deletion lifecycle |
| `src/services/consent.ts` | 57 | 4/4 | — | browser key/value storage |
| `src/services/supabase/client.ts` | 47 | 3/7 | — | — |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/authGate.ts` | export | function | `canViewLocalRecords(cloudConfigured: boolean, signedIn: boolean): boolean` | — | 이 기기에 남은 기록을 화면에 보여도 되는가. - 클라우드를 쓰지 않는 배포: 언제나 예. 「다른 계정」이라는 개념 자체가 없다. - 클라우드 배포: 로그인해야 예. 로그아웃은 「이 계정의 기록을 볼 자격」의 경계다. 🔴 가리는 것과 지우는 것은 다른 일이다 (비타협 원칙 #1). 이 함수가 `false`를 줘도 De… |
| `src/domain/authGate.ts` | export | const | `AUTH_GATE_CASES: { cloudConfigured: boolean; signedIn: boolean; expected: boolean; why: string }[]` | — | 배선 자가확인용 알려진 입력·기대 출력 표. 진단 도구가 이걸 그대로 돌려 판정한다 — 표를 여기 두는 이유는 도구가 자기 기대값을 손으로 적으면 그 자체가 두 번째 사본이 되기 때문이다(§7 SSOT). 새 갈래가 생기면 여기만 늘린다. |
| `src/domain/authGate.ts` | export | typealias | `LocalDataAction` | — | 데이터 관리 화면이 로컬 자료에 하는 동작. 🔴 새 동작을 만들면 이 유니온에 넣어야 하고, 넣으면 아래 분류에서 빠뜨릴 수 없다 — `Record<LocalDataAction, …>`이라 한 줄만 빠져도 컴파일이 실패한다(§7 2층 구조적 강제). 산문으로 "새 동작도 분류하세요"라고 적는 것과, 안 하면 빌드가 깨… |
| `src/domain/authGate.ts` | internal | const | `NEEDS_SIGN_IN: Record<LocalDataAction, boolean>` | — | 동작마다 로그인을 요구하는가. 기본값을 두지 않는다 — 새 형제가 조용히 열린 쪽으로 태어나면 그게 이 결함의 재발이다(M-0060의 「기본값 금지」와 같은 규율). |
| `src/domain/authGate.ts` | export | function | `canRunLocalDataAction(action: LocalDataAction, cloudConfigured: boolean, signedIn: boolean): boolean` | — | 이 동작을 지금 실행해도 되는가. - 클라우드를 쓰지 않는 배포: 언제나 예. 「다른 계정」 개념이 없다(`canViewLocalRecords`와 같은 전제). - 클라우드 배포: 위 분류가 로그인을 요구하는 동작만 로그인 뒤에 열린다. |
| `src/domain/authGate.ts` | export | const | `LOCAL_DATA_ACTION_CASES: { action: LocalDataAction; cloudConfigured: boolean; signedIn: boolean; expected: boolean; why: string; }[]` | — | 동작 잠금의 자가확인 표. 위 표와 같은 이유로 여기 둔다(도구가 기대값을 손으로 적으면 두 번째 사본이다). |
| `src/domain/authGate.ts` | export | function | `authGateMismatches(): string[]` | AUTH_GATE_CASES.filter, canViewLocalRecords, LOCAL_DATA_ACTION_CASES.filter, canRunLocalDataAction, view.map, act.map | 표를 전부 돌려 어긋난 갈래를 돌려준다. 비어 있으면 배선이 살아 있는 것이다. 🔴 두 표를 여기서 함께 돈다. 그래야 이 함수를 이미 쓰고 있는 진단(`sessionState.ts`)이 새 규칙을 자동으로 따라온다 — 소비처를 손으로 고쳐야 한다면 그게 §7이 말하는 누락의 씨앗이다. |
| `src/services/auth.ts` | export | const | `SHELL_AUTH_REDIRECT` | — | 셸 딥링크 복귀 주소 — AndroidManifest의 intent-filter·Supabase 허용 목록과 1:1이다. |
| `src/services/auth.ts` | export | interface | `SessionUser` | — | — |
| `src/services/auth.ts` | export | async function | `currentUser(): Promise<SessionUser \\| null>` | supabase, c.auth.getUser | 현재 로그인 사용자(없으면 null). |
| `src/services/auth.ts` | export | async function | `signInWithGoogle(): Promise<void>` | supabase, shellState, c.auth.signInWithOAuth | Google 로그인 시작(전체 페이지 리다이렉트 · 셸에서는 딥링크 복귀 — ADR-0037). |
| `src/services/auth.ts` | export | function | `authCodeFromUrl(url: string): string \\| null` | u.searchParams.get | 딥링크 URL에서 PKCE code를 꺼낸다. 순수 함수(§10 ③) — 이 판별이 틀리면 로그인이 조용히 안 되므로 유닛이 직접 돌린다. 다른 스킴·code 없음·깨진 URL은 전부 null. |
| `src/services/auth.ts` | internal | interface | `AppPlugin` | — | 셸의 App 플러그인 최소 모양 — 딥링크 수신(appUrlOpen)만 쓴다. |
| `src/services/auth.ts` | export | function | `wireShellAuthReturn(): void` | shellPlugin, app.addListener, authCodeFromUrl, supabase, c.auth.exchangeCodeForSession(code).then, c.auth.exchangeCodeForSession, console.error | 셸에서 브라우저 로그인이 딥링크로 돌아오면 code→세션 교환을 배선한다. 크롬·옛 APK(App 플러그인 없음)에서는 무행동 — 기존 경로에 손대지 않는다(ADR-0036과 같은 규율). 성공은 onAuthStateChange가 화면에 알린다(홈이 이미 구독 중). |
| `src/services/auth.ts` | export | async function | `isAllowedUser(): Promise<boolean>` | supabase, c.rpc | 초대 허용 사용자 여부(초대제 잠금 — ADR-0021). DB의 journey.is_allowed()가 진짜 방어이며, 이 호출은 UI 게이트(친절 안내)용이다. 오류/미설정 시 false(보수적). |
| `src/services/auth.ts` | export | async function | `signOut(): Promise<void>` | supabase, c.auth.signOut | 로그아웃(세션 종료). H-14 로컬 데이터 keep/delete 선택은 후속 구현. |
| `src/services/auth.ts` | export | function | `onAuthChange(cb: (user: SessionUser \\| null) => void): () => void` | supabase, cb, c.auth.onAuthStateChange, data.subscription.unsubscribe | 인증 상태 변화 구독. 반환값 호출로 해지. 미설정 시 즉시 null 통지. |
| `src/services/consent.ts` | export | function | `hasAgreed(key: string): boolean` | localStorage.getItem | 이 기기에서 이 항목에 대해 이미 확인했는가. |
| `src/services/consent.ts` | export | function | `rememberAgreed(key: string): void` | localStorage.setItem | — |
| `src/services/consent.ts` | export | const | `PHOTO_GEO_CONSENT_KEY` | — | 🔴 사진 위치를 지오코더에 보내도 되는가 (사용자 제안 2026-07-30의 개인정보 경계). 좌표를 화면에 채우는 것 은 네트워크가 0이라 물을 것이 없다 — 그 값은 기기 밖으로 나가지 않는다. 물어야 하는 것은 "그 자리의 이름을 알아내려고 좌표를 밖으로 보내는 것" 이다. 왜 기존 장소 검색과 다르게 묻나: 이… |
| `src/services/consent.ts` | export | const | `PHOTO_GEO_CONSENT_TEXT` | — | — |
| `src/services/supabase/client.ts` | internal | const | `url` | — | — |
| `src/services/supabase/client.ts` | internal | const | `publishableKey` | — | — |
| `src/services/supabase/client.ts` | internal | function | `createJourneyClient(u: string, k: string)` | createClient | — |
| `src/services/supabase/client.ts` | export | typealias | `JourneyClient` | — | 여행 앱 클라이언트 타입 — journey 스키마 고정(추론 타입 그대로 사용). |
| `src/services/supabase/client.ts` | internal | const | `_client: JourneyClient \\| null` | — | — |
| `src/services/supabase/client.ts` | export | function | `supabase(): JourneyClient \\| null` | createJourneyClient | 환경변수가 있을 때만 클라이언트를 만든다. 없으면 null(로컬 전용 동작). |
| `src/services/supabase/client.ts` | export | function | `isConfigured(): boolean` | Boolean | — |

## 5. 자동 검증 연결

- `tests/unit/serverTrashAuthority.test.ts`
- `tests/unit/sessionVerdict.test.ts`
- `tests/unit/shellAuth.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/domain/authGate.ts` — 121줄
- `src/services/auth.ts` — 117줄
- `src/services/consent.ts` — 57줄
- `src/services/supabase/client.ts` — 47줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
