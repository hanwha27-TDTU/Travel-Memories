---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 오프라인 동기화와 정본 전환 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

operation 큐, pull/push, OCC, tombstone, canonical exact-set과 자동 동기화를 소유한다.

- 모듈 ID: `sync-canonical`
- 관측 파일: 12개 · 논리 줄: 5278 · 최상위 선언: 222개(외부 공개 139개)
- 코드 내용 SHA-256: `753763b2ad935521211b9e39758483b4e5ee4afc40ce8a9a0cb0c2057469f916`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/deviceFleetVerdict.ts`

### 다른 모듈로 나가는 정적 의존

- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 19건
- 인증·Supabase 클라이언트·동의(`auth-security`): import/re-export 6건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 6건
- 앱 셸과 라우팅(`app-shell`): import/re-export 3건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 2건
- 화면·오버레이·상호작용(`ui-shell`): import/re-export 2건
- 지도·장소·지오코딩(`map-place`): import/re-export 1건
- 휴지통·복원·영구삭제(`trash-purge`): import/re-export 1건

### 외부 패키지/런타임 의존

- `dexie`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/deviceFleetVerdict.ts` | 139 | 10/10 | — | — |
| `src/domain/syncBadgeVerdict.ts` | 183 | 7/7 | src/domain/syncProgress.ts | — |
| `src/domain/syncProgress.ts` | 44 | 4/4 | — | — |
| `src/domain/syncReleaseVerdict.ts` | 129 | 5/8 | src/app/syncReleaseContract.gen.ts<br>src/services/autoSync.ts | — |
| `src/domain/syncStatusVerdict.ts` | 106 | 3/5 | — | — |
| `src/domain/syncTombstoneVerdict.ts` | 154 | 8/8 | src/offline/db.ts<br>src/sync/merge.ts | deletion lifecycle |
| `src/services/autoSync.ts` | 289 | 10/21 | src/services/supabase/client.ts<br>src/services/auth.ts<br>src/services/sync.ts<br>src/domain/syncProgress.ts<br>src/offline/db.ts<br>src/sync/merge.ts | deletion lifecycle; DOM interaction |
| `src/services/canonicalSync.ts` | 930 | 11/39 | src/offline/db.ts<br>src/domain/trip/rowmap.ts<br>src/domain/moment/rowmap.ts<br>src/domain/expense/rowmap.ts<br>src/domain/media/rowmap.ts<br>src/domain/audio/rowmap.ts<br>src/domain/video/rowmap.ts<br>src/domain/place/rowmap.ts<br>src/domain/media/naming.ts<br>src/domain/syncProgress.ts<br>src/media/compress.ts<br>src/app/deviceId.ts<br>src/services/supabase/client.ts<br>src/services/r2.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localPlaces, localTrips, localVideos; Supabase table: sync_meta; file/blob bytes; transactional persistence; deletion lifecycle |
| `src/services/sync.ts` | 2717 | 52/84 | src/offline/db.ts<br>src/domain/time.ts<br>src/domain/trip/rowmap.ts<br>src/domain/moment/rowmap.ts<br>src/domain/expense/rowmap.ts<br>src/domain/media/rowmap.ts<br>src/domain/audio/rowmap.ts<br>src/domain/video/rowmap.ts<br>src/domain/media/naming.ts<br>src/domain/place/rowmap.ts<br>src/domain/place/coordInput.ts<br>src/media/compress.ts<br>src/sync/merge.ts<br>src/services/supabase/client.ts<br>src/services/r2.ts<br>src/services/storeState.ts<br>src/app/deviceId.ts<br>src/services/canonicalSync.ts<br>src/services/syncPlan.ts<br>src/domain/syncProgress.ts<br>src/services/purge.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localPlaces, localTrips, localVideos; Supabase table: audio, expenses, media, moments, places, trips, videos; file/blob bytes; transactional persistence; deletion lifecycle; browser key/value storage |
| `src/services/syncParity.ts` | 139 | 8/13 | src/services/supabase/client.ts<br>src/services/auth.ts<br>src/services/storeState.ts<br>src/services/autoSync.ts<br>src/domain/syncBadgeVerdict.ts | deletion lifecycle |
| `src/services/syncPlan.ts` | 106 | 7/9 | — | — |
| `src/sync/merge.ts` | 342 | 14/14 | src/domain/time.ts<br>src/offline/db.ts | transactional persistence; deletion lifecycle |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/deviceFleetVerdict.ts` | export | typealias | `FleetLevel` | — | — |
| `src/domain/deviceFleetVerdict.ts` | export | interface | `FleetMetricView` | — | — |
| `src/domain/deviceFleetVerdict.ts` | export | interface | `FleetDevice` | — | — |
| `src/domain/deviceFleetVerdict.ts` | export | const | `STALE_DEVICE_DAYS` | — | 얼마나 오래 안 올렸으면 「뒤처졌다」고 볼 것인가. 🔴 이건 결함 판정이 아니다. 안 쓰는 기기는 안 올리는 것이 정상이다 — 그래서 `todo`이지 `problem`이 아니다(§5 10항 — 차이 ≠ 결함, 겁주지 않는다). 이 값의 유일한 쓸모는 "저 기기에서 아직 안 올라온 게 있을 수 있다" 를 사용자가 떠올리… |
| `src/domain/deviceFleetVerdict.ts` | export | interface | `FleetObservation` | — | — |
| `src/domain/deviceFleetVerdict.ts` | export | function | `daysSince(iso: string, now: string): number \\| null` | Date.parse, Number.isNaN, Math.max, Math.floor | 며칠 전인지. 파싱 불가면 null. |
| `src/domain/deviceFleetVerdict.ts` | export | function | `fleetSizeMetric(o: FleetObservation): FleetMetricView` | — | ① 이 계정에 기기가 몇 대인가 — 파괴적 행동의 전제를 가르는 값이다(M-0048). |
| `src/domain/deviceFleetVerdict.ts` | export | function | `staleDeviceMetric(o: FleetObservation): FleetMetricView` | o.devices.filter, daysSince, stale.map((d) => d.label).join, stale.map | ② 오래 안 올린 기기가 있는가 — 그 기기에 아직 안 올라온 기록이 있을 수 있다. |
| `src/domain/deviceFleetVerdict.ts` | export | function | `fleetMetrics(o: FleetObservation): FleetMetricView[]` | fleetSizeMetric, staleDeviceMetric | — |
| `src/domain/deviceFleetVerdict.ts` | export | function | `fleetHeadline(o: FleetObservation): string` | staleDeviceMetric | 판정 한 문장. 🔴 여러 대일 때 그 사실을 먼저 말한다. 사용자가 이 도구를 여는 이유가 대개 "왜 기기마다 다르지?" 이고, 그 답의 출발점이 「몇 대인가」이기 때문이다. |
| `src/domain/syncBadgeVerdict.ts` | export | typealias | `SyncPhaseLike` | — | — |
| `src/domain/syncBadgeVerdict.ts` | export | interface | `ParitySnapshot` | — | 서버와의 대조 결과. 아직 안 했거나 못 했으면 `null`. |
| `src/domain/syncBadgeVerdict.ts` | export | interface | `BadgeInput` | — | — |
| `src/domain/syncBadgeVerdict.ts` | export | typealias | `BadgeLevel` | — | — |
| `src/domain/syncBadgeVerdict.ts` | export | interface | `BadgeView` | — | — |
| `src/domain/syncBadgeVerdict.ts` | export | function | `syncBadge(i: BadgeInput): BadgeView` | syncProgressPercent, syncProgressLabel | 배지 한 줄. 순서가 곧 우선순위다 — 위에 있는 것이 더 급하다. 아래로 갈수록 조용해진다. |
| `src/domain/syncBadgeVerdict.ts` | export | function | `badgeActionLabel(v: BadgeView): string` | parts.push, parts.join | 배지의 화면읽기 라벨 — 「›」만 보이는 이동 버튼이 무엇을 하는지 말한다. |
| `src/domain/syncProgress.ts` | export | typealias | `SyncProgressPhase` | — | — |
| `src/domain/syncProgress.ts` | export | interface | `SyncProgress` | — | — |
| `src/domain/syncProgress.ts` | export | function | `syncProgressPercent(progress: SyncProgress \\| null): number \\| null` | Math.round, Math.min | — |
| `src/domain/syncProgress.ts` | export | function | `syncProgressLabel(progress: SyncProgress \\| null): string` | — | — |
| `src/domain/syncReleaseVerdict.ts` | export | typealias | `SyncReleaseLevel` | — | — |
| `src/domain/syncReleaseVerdict.ts` | export | interface | `SyncReleaseMetricView` | — | — |
| `src/domain/syncReleaseVerdict.ts` | export | interface | `SyncReleaseObservation` | — | — |
| `src/domain/syncReleaseVerdict.ts` | internal | function | `shortHash(hash: string \\| null): string` | hash.slice | — |
| `src/domain/syncReleaseVerdict.ts` | internal | function | `ageMs(nowIso: string, thenIso: string \\| null): number \\| null` | Date.parse, Number.isFinite, Math.max | — |
| `src/domain/syncReleaseVerdict.ts` | internal | function | `duration(ms: number): string` | Math.max, Math.floor | — |
| `src/domain/syncReleaseVerdict.ts` | export | function | `syncReleaseMetrics(observation: SyncReleaseObservation): SyncReleaseMetricView[]` | expected.requiredOps.filter, observation.edge.ops?.includes, ageMs, missingOps.join, shortHash, duration | — |
| `src/domain/syncReleaseVerdict.ts` | export | function | `syncReleaseHeadline(metrics: readonly SyncReleaseMetricView[]): string` | metrics.some | — |
| `src/domain/syncStatusVerdict.ts` | export | interface | `SyncStatusLike` | — | 이 판정에 필요한 것만. `services/autoSync.ts`의 `SyncStatus`가 구조적으로 들어맞는다. |
| `src/domain/syncStatusVerdict.ts` | export | interface | `SyncStatusVerdict` | — | — |
| `src/domain/syncStatusVerdict.ts` | internal | function | `clock(iso: string): string` | Number.isNaN, d.getTime, String(n).padStart, String, p2, d.getHours, d.getMinutes, d.getSeconds | ISO → `HH:MM:SS`(로컬). 파싱 실패는 빈 문자열 — 지어내지 않는다. |
| `src/domain/syncStatusVerdict.ts` | internal | function | `lastOkPhrase(lastOkAt: string): string` | clock | 「마지막 성공 HH:MM:SS」 — 시각을 못 읽으면 시각 없이 사실만 말한다. |
| `src/domain/syncStatusVerdict.ts` | export | function | `autoSyncVerdict(st: SyncStatusLike): SyncStatusVerdict` | lastOkPhrase | 자동 동기화 상태 → 사용자에게 보일 문장과 판정. 대응하는 기대값은 「최근에 성공」이다(호출부가 `expected`로 붙인다). |
| `src/domain/syncTombstoneVerdict.ts` | export | typealias | `TombstoneSyncState` | — | — |
| `src/domain/syncTombstoneVerdict.ts` | export | interface | `TombstoneSyncRef` | — | — |
| `src/domain/syncTombstoneVerdict.ts` | export | interface | `TombstoneSyncFinding` | — | — |
| `src/domain/syncTombstoneVerdict.ts` | export | function | `classifyTombstoneSync(local: TombstoneSyncRef, server: TombstoneSyncRef \\| null, serverPurged: boolean): TombstoneSyncState` | mergeDecision | 서버 증거 하나를 판정한다. - 서버도 tombstone이면 삭제 의도는 이미 반영됐다. operation이 같으면 가장 강한 증거이고, 달라도 삭제 상태 자체는 확인됐으므로 다시 보내지 않는다. - 서버가 활성이면 기존 좀비 차단 규칙(`mergeDecision`)이 승자를 정한다. 더 최신 복원을 삭제로 덮지 않는다… |
| `src/domain/syncTombstoneVerdict.ts` | export | function | `isConfirmedTombstone(state: TombstoneSyncState): boolean` | — | — |
| `src/domain/syncTombstoneVerdict.ts` | export | interface | `TombstoneSyncPresentation` | — | — |
| `src/domain/syncTombstoneVerdict.ts` | export | function | `tombstoneSyncPresentation(findings: TombstoneSyncFinding[], unavailableReason: string \\| null = null): TombstoneSyncPresentation` | findings.filter, isConfirmedTombstone, [ confirmed ? `서버 반영 ${confirmed}건` : '', actionable ? `동기화…, parts.join | 사용자에게 보이는 수와 문장도 순수 함수로 잠근다(§10 전달 결함 방지). |
| `src/domain/syncTombstoneVerdict.ts` | export | function | `tombstoneFindingText(state: TombstoneSyncState): string` | — | 접힌 근거에 쓰는 상태 설명. 정상 둘은 화면에서 숨기므로 여기 들어오지 않는다. |
| `src/services/autoSync.ts` | export | typealias | `SyncPhase` | — | — |
| `src/services/autoSync.ts` | export | interface | `SyncStatus` | — | — |
| `src/services/autoSync.ts` | internal | const | `status: SyncStatus` | — | — |
| `src/services/autoSync.ts` | internal | const | `listeners` | — | — |
| `src/services/autoSync.ts` | export | function | `syncStatus(): SyncStatus` | — | — |
| `src/services/autoSync.ts` | export | function | `onSyncStatus(cb: (s: SyncStatus) => void): () => void` | listeners.add, listeners.delete | — |
| `src/services/autoSync.ts` | internal | function | `setStatus(patch: Partial<SyncStatus>): void` | cb | — |
| `src/services/autoSync.ts` | export | const | `DEBOUNCE_MS` | — | 저장 연타(사진 여러 장 등)를 한 번으로 합치는 창. |
| `src/services/autoSync.ts` | internal | const | `running: Promise<void> \\| null` | — | — |
| `src/services/autoSync.ts` | internal | const | `trailing` | — | — |
| `src/services/autoSync.ts` | internal | const | `timer: ReturnType<typeof setTimeout> \\| null` | — | — |
| `src/services/autoSync.ts` | internal | const | `deepPending` | — | 이 실행이 사용자가 직접 부른 것 인지. 디바운스·후행 실행이 요청을 합치므로 플래그도 합친다 — 창 안의 어느 요청 하나라도 deep이면 그 실행은 deep이다. 합치면서 잃으면 사용자가 누른 버튼이 조용히 얕은 동기화가 된다. |
| `src/services/autoSync.ts` | internal | async function | `runOnce(reason: string): Promise<void>` | supabase, setStatus, currentUser, new Date().toISOString, runSync, String | — |
| `src/services/autoSync.ts` | export | function | `requestSync(reason: string, opts: { deep?: boolean } = {}): Promise<void>` | running.then, Promise.resolve, (async () => { try { await runOnce(reason); while (trailing…, runOnce | 동기화를 요청한다 — 모든 상태 변경 뒤에 부른다. 겹쳐 불러도 안전하다: 도는 중이면 끝난 뒤 한 번만 더 돈다(후행 1회). 반환 프로미스는 "이 요청을 덮는 동기화가 끝났을 때" 풀린다. |
| `src/services/autoSync.ts` | export | function | `requestSyncSoon(reason: string, opts: { deep?: boolean } = {}): void` | clearTimeout, setTimeout, requestSync | 디바운스 요청 — 연타를 한 번으로 합친다(저장 루프 등). |
| `src/services/autoSync.ts` | internal | const | `installed` | — | — |
| `src/services/autoSync.ts` | internal | const | `initiallyCheckedUsers` | — | A browser session checks a signed-in account once, then only flushes real queued work. |
| `src/services/autoSync.ts` | export | function | `hasDueAutoSyncWork(queue: readonly SyncQueueItem[], state: Pick<LocalSyncState, 'pendingCanonical'> \\| undefined, nowIso: string): boolean` | queue.some, isRetryDue | True only for work that can safely be retried now, never for permanent failures. |
| `src/services/autoSync.ts` | internal | async function | `requestSyncIfNeeded(reason: string, knownUser?: SessionUser): Promise<void>` | supabase, currentUser, initiallyCheckedUsers.has, Promise.all, db().syncQueue.toArray, db, db().syncState.get, hasDueAutoSyncWork, new Date().toISOString, initiallyCheckedUsers.add | online/visibility are recovery signals, not permission for another full pull. A first signed-in check receives an existing cloud archive on a new device. Afterwards this starts on… |
| `src/services/autoSync.ts` | export | function | `installAutoSync(): () => void` | requestSyncIfNeeded, onAuthChange, window.addEventListener, document.addEventListener, window.removeEventListener, document.removeEventListener, stopAuth | 로그인 직후 1회, 또는 실제 미전송 변경이 있을 때만 자동 동기화한다. 트리거를 이렇게 고른 이유: · `online` — 대기열에 쌓인 변경을 연결되자마자 올린다. · `visibility` — 대기열이 남아 있을 때만 복구를 재개한다. 완료된 동기화를 5분마다 다시 읽으면 첫 동기화가 느리고 끝나지 않는 것처럼 보… |
| `src/services/autoSync.ts` | export | function | `__resetAutoSyncForTests(): void` | clearTimeout, listeners.clear | 테스트용 초기화(모듈 상태를 갖는 모듈의 의무). |
| `src/services/canonicalSync.ts` | export | const | `LEGACY_CANONICAL_VERSION` | — | — |
| `src/services/canonicalSync.ts` | internal | const | `PAGE_SIZE` | — | — |
| `src/services/canonicalSync.ts` | export | interface | `CanonicalMeta` | — | — |
| `src/services/canonicalSync.ts` | internal | interface | `RemoteResult` | — | — |
| `src/services/canonicalSync.ts` | export | interface | `CanonicalRemote` | — | — |
| `src/services/canonicalSync.ts` | internal | interface | `RemoteSnapshot` | — | — |
| `src/services/canonicalSync.ts` | internal | typealias | `CanonicalProgress` | — | — |
| `src/services/canonicalSync.ts` | export | interface | `CanonicalPreflight` | — | — |
| `src/services/canonicalSync.ts` | export | interface | `CanonicalPublishResult` | — | — |
| `src/services/canonicalSync.ts` | internal | function | `stateId(userId: string): string` | — | — |
| `src/services/canonicalSync.ts` | export | async function | `fetchAllRows(client: JourneyClient, table: string): Promise<RemoteResult<T[]>>` | client .from(table) .select('*') .order('updated_at', { asc…, client .from(table) .select('*') .order, client .from(table) .select, client .from, out.push | PostgREST 행 전체집합. updated_at 내림차순 + 페이지네이션은 모든 도메인이 공유한다. |
| `src/services/canonicalSync.ts` | export | async function | `fetchAllIds(client: JourneyClient, table: string): Promise<RemoteResult<string[]>>` | client .from(table) .select('id') .order('id', { ascending:…, client .from(table) .select('id') .order, client .from(table) .select, client .from, out.push, rows.map | updated_at이 없는 영구삭제 원장은 id 안정 정렬로 같은 페이지 계약을 지킨다. |
| `src/services/canonicalSync.ts` | internal | function | `parseMeta(value: unknown): CanonicalMeta \\| null` | Array.isArray | — |
| `src/services/canonicalSync.ts` | export | function | `canonicalRemote(client: JourneyClient): CanonicalRemote` | r2BlobStore, client.rpc, client .from('sync_meta') .select('canonical_version,canoni…, client .from('sync_meta') .select, client .from, parseMeta, fetchAllRows, fetchAllIds, blobs.upload, blobs.download | 실제 Supabase/R2 어댑터. 테스트는 CanonicalRemote fake를 주입한다. |
| `src/services/canonicalSync.ts` | internal | function | `requireRemote(result: RemoteResult<T>, label: string): T` | — | — |
| `src/services/canonicalSync.ts` | internal | function | `requireMeta(result: RemoteResult<CanonicalMeta \\| null>): CanonicalMeta` | requireRemote | — |
| `src/services/canonicalSync.ts` | internal | async function | `readMeta(remote: CanonicalRemote): Promise<CanonicalMeta>` | requireMeta, remote.ensureMeta | — |
| `src/services/canonicalSync.ts` | internal | async function | `readStableSnapshot(remote: CanonicalRemote, expected: string): Promise<RemoteSnapshot>` | Promise.all, remote.listTrips, remote.listPlaces, remote.listMoments, remote.listMedia, remote.listExpenses, remote.listAudio, remote.listVideos, remote.listPurgedIds, requireRemote | — |
| `src/services/canonicalSync.ts` | internal | async function | `fetchOrMissing(remote: CanonicalRemote, storagePath: string \\| null \\| undefined, deletedAt: string \\| null, entityLabel: string, id: string): Promise<{ data: Blob \\| null; missing: boolean }>` | remote.download | 🔴 M-0101(2026-08-05) — 죽은 tombstone 사진 하나가 새 기기 전체 최종본 소비를 막았다. `materializeMedia`/`materializeAudio`가 바이트 다운로드 실패를 활성/휴지통 구분 없이 throw했고, `replaceLocalSnapshot`은 모든 미디어를 먼저 다 받아야… |
| `src/services/canonicalSync.ts` | internal | async function | `materializeMedia(rows: MediaRow[], remote: CanonicalRemote, version: string, onItem?: ((completed: number) => void) \\| undefined): Promise<LocalMedia[]>` | db, (await d.localMedia.toArray()).map, d.localMedia.toArray, fromMediaRow, local.get, fetchOrMissing, compressForStorage, out.push, onItem | — |
| `src/services/canonicalSync.ts` | internal | async function | `materializeAudio(rows: AudioRow[], remote: CanonicalRemote, version: string, onItem?: ((completed: number) => void) \\| undefined): Promise<LocalAudio[]>` | db, (await d.localAudio.toArray()).map, d.localAudio.toArray, fromAudioRow, local.get, fetchOrMissing, out.push, onItem | — |
| `src/services/canonicalSync.ts` | internal | async function | `materializeVideos(rows: VideoRow[], remote: CanonicalRemote, version: string, onItem?: ((completed: number) => void) \\| undefined): Promise<LocalVideo[]>` | db, (await d.localVideos.toArray()).map, d.localVideos.toArray, fromVideoRow, local.get, fetchOrMissing, videoPosterStoragePath, out.push, onItem | — |
| `src/services/canonicalSync.ts` | internal | async function | `stampTable(table: Table<T, string>, version: string): Promise<void>` | table.toArray, rows.filter, table.bulkPut, changed.map | — |
| `src/services/canonicalSync.ts` | internal | async function | `stampAllTables(version: string): Promise<void>` | db, Promise.all, stampTable | — |
| `src/services/canonicalSync.ts` | internal | async function | `setLegacyBaseline(userId: string, version: string): Promise<void>` | db, d.transaction, stampAllTables, d.syncState.put, stateId, new Date().toISOString | — |
| `src/services/canonicalSync.ts` | internal | async function | `replaceLocalSnapshot(userId: string, meta: CanonicalMeta, snapshot: RemoteSnapshot, remote: CanonicalRemote, onProgress?: CanonicalProgress \\| undefined): Promise<number>` | db, onProgress, materializeMedia, materializeAudio, materializeVideos, readMeta, snapshot.trips.map, fromRow, snapshot.places.map, fromPlaceRow | — |
| `src/services/canonicalSync.ts` | internal | async function | `clearPending(userId: string, canonicalVersion: string): Promise<void>` | db().syncState.put, db, stateId, new Date().toISOString | — |
| `src/services/canonicalSync.ts` | internal | async function | `cleanupPaths(remote: CanonicalRemote, paths: string[]): Promise<void>` | remote.remove, console.warn | — |
| `src/services/canonicalSync.ts` | export | async function | `ensureCanonicalBeforeSync(remote: CanonicalRemote, userId: string, onProgress?: CanonicalProgress \\| undefined): Promise<CanonicalPreflight>` | db().syncState.get, db, stateId, remote.ensureMeta, console.warn, requireMeta, finalizePublishedSnapshot, ensureCanonicalBeforeSync, cleanupPaths, clearPending | 일반 동기화의 첫 문. applied이면 이번 실행은 여기서 끝내고 절대 push하지 않는다. |
| `src/services/canonicalSync.ts` | internal | function | `asRecord(rows: T[]): Record<string, unknown>[]` | — | — |
| `src/services/canonicalSync.ts` | internal | async function | `capturePending(remote: CanonicalRemote, userId: string, meta: CanonicalMeta): Promise<PendingCanonicalSnapshot>` | Promise.all, remote.listMedia, remote.listAudio, remote.listVideos, requireRemote, readMeta, db, d.transaction, d.localTrips.toArray, d.localPlaces.toArray | — |
| `src/services/canonicalSync.ts` | internal | async function | `updatePendingStage(userId: string, pending: PendingCanonicalSnapshot, stage: PendingCanonicalSnapshot['stage']): Promise<PendingCanonicalSnapshot>` | db().syncState.get, db, stateId, db().syncState.put, new Date().toISOString | — |
| `src/services/canonicalSync.ts` | internal | class | `CanonicalSnapshotChangedError` | — | — |
| `src/services/canonicalSync.ts` | internal | async function | `uploadPending(remote: CanonicalRemote, pending: PendingCanonicalSnapshot): Promise<void>` | db, d.localMedia.get, mediaTasks.push, d.localAudio.get, audioTasks.push, d.localVideos.get, videoPosterStoragePath, videoTasks.push, remote.upload, Promise.all | — |
| `src/services/canonicalSync.ts` | internal | async function | `sameBlobBytes(left: Blob, right: Blob): Promise<boolean>` | Promise.all, left.arrayBuffer, right.arrayBuffer | — |
| `src/services/canonicalSync.ts` | internal | async function | `finalizePublishedSnapshot(remote: CanonicalRemote, userId: string, pending: PendingCanonicalSnapshot): Promise<void>` | db, d.transaction, d.localMedia.get, d.localMedia.put, d.localAudio.get, d.localAudio.put, d.localVideos.get, d.localVideos.put, stampAllTables, d.syncQueue.bulkDelete | — |
| `src/services/canonicalSync.ts` | internal | async function | `resolvePublishReadBack(remote: CanonicalRemote, userId: string, pending: PendingCanonicalSnapshot, publishError?: string): Promise<PendingCanonicalSnapshot>` | readMeta, updatePendingStage, cleanupPaths, clearPending | — |
| `src/services/canonicalSync.ts` | export | async function | `publishCanonicalWithRemote(remote: CanonicalRemote, userId: string): Promise<CanonicalPublishResult>` | readMeta, db().syncState.get, db, stateId, capturePending, updatePendingStage, cleanupPaths, clearPending, uploadPending, remote.publish | 테스트 가능한 핵심 오케스트레이션. 실제 UI는 아래 client 어댑터를 호출한다. |
| `src/services/canonicalSync.ts` | export | async function | `publishDeviceAsCanonical(client: JourneyClient, userId: string): Promise<CanonicalPublishResult>` | publishCanonicalWithRemote, canonicalRemote | 명시적 사용자 액션: 현재 기기의 스냅샷을 클라우드 정확집합으로 게시한다. |
| `src/services/sync.ts` | export | interface | `SyncResult` | — | — |
| `src/services/sync.ts` | export | interface | `TripsRemote` | — | 원격 저장소 포트 — 네트워크 격리(테스트 시 fake 주입). |
| `src/services/sync.ts` | export | function | `tripsRemote(client: JourneyClient): TripsRemote` | client.from('trips').upsert, client.from, client.from('trips').select('*').eq('id', id).maybeSingle, client.from('trips').select('*').eq, client.from('trips').select, fetchAllRows | JourneyClient를 TripsRemote로 감싸는 얇은 어댑터(untested glue). |
| `src/services/sync.ts` | export | interface | `MomentsRemote` | — | 순간 원격 포트 — 네트워크 격리(trips와 대칭). |
| `src/services/sync.ts` | export | function | `momentsRemote(client: JourneyClient): MomentsRemote` | client.from('moments').upsert, client.from, client.from('moments').select('*').eq('id', id).maybeSingle, client.from('moments').select('*').eq, client.from('moments').select, fetchAllRows | — |
| `src/services/sync.ts` | export | interface | `ExpensesRemote` | — | 비용 원격 포트 — 네트워크 격리(moments와 대칭). |
| `src/services/sync.ts` | export | function | `expensesRemote(client: JourneyClient): ExpensesRemote` | client.from('expenses').upsert, client.from, client.from('expenses').select('*').eq('id', id).maybeSingle, client.from('expenses').select('*').eq, client.from('expenses').select, fetchAllRows | — |
| `src/services/sync.ts` | export | interface | `MediaRemote` | — | 사진 원격 포트 — 메타(테이블) + 태블릿 감상용 클라우드 정본(WebP). |
| `src/services/sync.ts` | export | function | `mediaRemote(client: JourneyClient): MediaRemote` | r2BlobStore, client.from('media').upsert, client.from, client.from('media').select('*').eq('id', id).maybeSingle, client.from('media').select('*').eq, client.from('media').select, fetchAllRows, blobs.uploadDisplay, blobs.download, blobs.remove | — |
| `src/services/sync.ts` | export | interface | `AudioRemote` | — | 소리 원격 포트 — 메타(journey.audio) + 원본 바이트 (R2). 사진과 하나만 다르다: 사진은 재인코딩한 WebP를 클라우드 정본으로 삼고, 소리는 녹음 바이트 자체가 정본 이라 그대로 올린다. 그래서 `uploadDisplay`가 아니라 `upload`다 — 이름이 다른 것 자체가 그 차이를 말한다. |
| `src/services/sync.ts` | export | function | `audioRemote(client: JourneyClient): AudioRemote` | r2BlobStore, client.from('audio').upsert, client.from, client.from('audio').select('*').eq('id', id).maybeSingle, client.from('audio').select('*').eq, client.from('audio').select, fetchAllRows, blobs.upload, blobs.download, blobs.remove | — |
| `src/services/sync.ts` | export | interface | `VideoRemote` | — | — |
| `src/services/sync.ts` | export | function | `videoRemote(client: JourneyClient): VideoRemote` | r2BlobStore, client.from('videos').upsert, client.from, client.from('videos').select('*').eq('id', id).maybeSingle, client.from('videos').select('*').eq, client.from('videos').select, fetchAllRows, blobs.upload, blobs.download, blobs.remove | — |
| `src/services/sync.ts` | internal | async function | `markFail(op: SyncQueueItem, status: number \\| undefined, reason?: string): Promise<void>` | classifyError, db().syncQueue.update, db, new Date(Date.now() + retryDelayMs(attempts)).toISOString, Date.now, retryDelayMs | 실패를 큐에 적는다. 왜 실패했는지도 함께 적는다 (2026-08-05 · M-0107). 🔴 왜 사유를 남기나: 사용자 기기에 `permanent_failed` 13건이 박혀 있었는데 화면도 진단도 이유를 말할 수 없었다 — 큐가 사유를 안 갖고 있었기 때문이다. 원인(순간 영구삭제가 자식을 남겨 FK 위반)을 알아… |
| `src/services/sync.ts` | internal | typealias | `SyncEntityType` | — | — |
| `src/services/sync.ts` | export | typealias | `PullMode` | — | `merge`는 평상시 양방향 동기화다. `server-read-only`는 서버 capability를 확인할 수 없는 배포 전환 구간 전용이며, 서버 쓰기뿐 아니라 기존 로컬 작업 큐를 pull이 지우는 것도 금지한다. |
| `src/services/sync.ts` | export | interface | `SyncAttempt` | — | — |
| `src/services/sync.ts` | export | function | `collapseSyncAttempts(items: SyncQueueItem[], entityType: SyncEntityType, nowIso = new Date().toISOString()): SyncAttempt[]` | new Date().toISOString, grouped.get, found.operationIds.push, grouped.set, [...grouped.values()] .filter(({ op }) => (op.state === 'lo…, [...grouped.values()] .filter, grouped.values, isRetryDue, operationIds.filter | 같은 엔티티의 연속 편집을 최신 작업 하나로 접는다. push는 큐의 각 op 시점 payload가 아니라 현재 로컬 행 을 읽는다. 따라서 v2 op와 v3 op이 함께 있으면 둘 다 v3 payload를 보내게 된다. 첫 요청 뒤 서버시각이 전진하므로 둘째 요청은 stale guard에 막히고 영원히 재시도될 수 있… |
| `src/services/sync.ts` | internal | function | `withSyncOperation(entity: T, operationId: string): T` | — | 최신 로컬 snapshot이 어느 큐 작업을 대표하는지 row에 명시한다. 옛 cascade/backfill도 보정된다. |
| `src/services/sync.ts` | internal | async function | `removeSuperseded(attempt: SyncAttempt): Promise<void>` | db().syncQueue.bulkDelete, db | 네트워크 전에 옛 op만 접는다. 최신 op은 성공 read-back 전까지 반드시 남긴다. |
| `src/services/sync.ts` | internal | function | `isSameSnapshot(cur: SyncMeta \\| undefined, expected: SyncMeta): boolean` | Boolean | push 중 새 편집이 생겼으면 그 최신 로컬 행에는 옛 서버 stamp를 씌우지 않는다. |
| `src/services/sync.ts` | internal | async function | `applyServerWinner(table: Table<T, string>, entityType: SyncEntityType, expected: T \\| undefined, server: T, mode: PullMode): Promise<boolean>` | db, d.transaction, table.get, Boolean, d.syncQueue.where('entityId').equals(server.id).toArray, d.syncQueue.where('entityId').equals, d.syncQueue.where, queued .filter( (q) => q.entityType === entityType && q.ope…, queued .filter, table.put | pull이 이긴 snapshot만 교체하고, 그 snapshot을 만들었던 도메인 op을 같은 트랜잭션에서 걷는다. |
| `src/services/sync.ts` | internal | async function | `rebaseRejectedLocal(table: Table<T, string>, expected: T, operationId: string, server: SyncMeta): Promise<boolean>` | mergeDecision, db, d.transaction, table.get, Boolean, table.put, Math.max | 조건부 쓰기가 거절됐지만 LWW상 로컬이 이겨야 하면, 서버의 현재 version을 새 기준선으로 삼는다. 같은 로컬 snapshot일 때만 고쳐 push 도중 생긴 더 최신 편집을 건드리지 않는다. |
| `src/services/sync.ts` | internal | async function | `removeUnreferencedBytes(remote: Pick<MediaRemote, 'remove'> \\| Pick<AudioRemote, 'remove'> \\| Pick<Vide…, candidate: string \\| undefined, referenced: string \\| null \\| undefined): Promise<void>` | unreferencedByteAction, remote.remove, console.warn | DB가 가리키지 않는 작업별 R2 키만 걷는다. 삭제 실패는 기억보다 고아 사본을 택해 재시도에 맡긴다. 🔴 판정은 여기 없다 (`unreferencedByteAction`이 한다 · T-047). 이 함수는 실행만 한다. |
| `src/services/sync.ts` | internal | async function | `removeUnreferencedVideoBytes(remote: Pick<VideoRemote, 'remove'>, candidate: string \\| undefined, referenced: string \\| null \\| undefined): Promise<void>` | removeUnreferencedBytes, videoPosterStoragePath | 영상 본체와 포스터는 하나의 논리 객체다. 둘 중 하나만 정리되는 상태를 만들지 않는다. |
| `src/services/sync.ts` | internal | async function | `sameBlobBytes(left: Blob, right: Blob): Promise<boolean>` | Promise.all, left.arrayBuffer, right.arrayBuffer | R2에서 되읽은 바이트가 로컬 표시본과 정확히 같은지 확인한다. 크기만 같다고 통과시키지 않는다. |
| `src/services/sync.ts` | internal | function | `uploadVideoObject(remote: VideoRemote, path: string, blob: Blob, contentType: string)` | upload | 바이트 업로드 여부는 호출부의 mustUploadBytes만 판정한다. 이 함수는 주어진 한 객체만 전송한다. |
| `src/services/sync.ts` | internal | function | `appendPurgeBytePath(paths: string[], path: string, domain: PurgeDomain): void` | paths.push, videoPosterStoragePath | — |
| `src/services/sync.ts` | internal | async function | `discardVerifiedLocalOriginal(expected: LocalMedia, verified: Blob): Promise<boolean>` | sameBlobBytes, db, d.transaction, d.localMedia.get, isSameSnapshot, d.localMedia.put | 서버에서 검증된 표시본이 유일한 정본이 된 뒤 로컬의 큰 입력 원본 복사본을 비운다. `editState`도 원본 좌표계에 묶인 값이라 함께 걷는다. 이후 재편집은 현재 표시본에서 새로 시작한다. |
| `src/services/sync.ts` | export | async function | `pruneVerifiedMediaOriginals(remote: Pick<MediaRemote, 'download'>): Promise<{ pruned: number; kept: number }>` | (await db().localMedia.toArray()).filter, db().localMedia.toArray, db, remote.download, discardVerifiedLocalOriginal | 옛 정책으로 이미 저장된 사진을 데려오는 1회성·멱등 정리다. 경로 기억만 믿지 않고 R2에서 바이트를 되읽어 같은 표시본임을 확인한 사진만 정리한다. |
| `src/services/sync.ts` | export | async function | `pushPending(remote: TripsRemote, userId: string): Promise<{ pushed: number; failed: number }>` | db, collapseSyncAttempts, d.syncQueue.orderBy('createdAt').toArray, d.syncQueue.orderBy, removeSuperseded, d.localTrips.get, d.syncQueue.delete, withSyncOperation, remote.upsert, toRow | 대기열의 로컬 작업을 서버에 반영. 각 작업마다: upsert(멱등) → 별도 read-back으로 일치 확인 → 서버시각/version 로컬 반영 → 작업 제거. HTTP 성공/upsert 표현만으로 완료 처리하지 않는다(불변식 5). |
| `src/services/sync.ts` | export | async function | `pullTrips(remote: TripsRemote, mode: PullMode): Promise<{ pulled: number; skippedEmptyCloud: boolean }>` | db, remote.listAll, purgedIdSet, (await d.localTrips.toArray()).filter, d.localTrips.toArray, isEmptyCloudAnomaly, purged.has, fromRow, d.localTrips.get, mergeDecision | 서버의 내 여행을 로컬에 병합(교체 아님). 빈-클라우드 가드로 로컬을 지키고, 각 행은 LWW/tombstone 결정으로만 반영한다. 서버에 없는 로컬 행은 지우지 않는다 (아직 push 안 된 로컬 전용일 수 있음). |
| `src/services/sync.ts` | export | async function | `pushPendingMoments(remote: MomentsRemote, userId: string): Promise<{ pushed: number; failed: number }>` | db, collapseSyncAttempts, d.syncQueue.orderBy('createdAt').toArray, d.syncQueue.orderBy, removeSuperseded, d.localMoments.get, d.syncQueue.delete, withSyncOperation, remote.upsert, toMomentRow | 순간 대기열 push(trips와 대칭: 멱등 upsert → read-back → 서버시각 반영 → 작업 제거). |
| `src/services/sync.ts` | export | async function | `pullMoments(remote: MomentsRemote, mode: PullMode): Promise<{ pulled: number; skippedEmptyCloud: boolean }>` | db, remote.listAll, purgedIdSet, (await d.localMoments.toArray()).filter, d.localMoments.toArray, isEmptyCloudAnomaly, purged.has, fromMomentRow, d.localMoments.get, mergeDecision | 서버의 내 순간을 로컬에 병합(교체 아님, 빈-클라우드 가드, LWW/tombstone). |
| `src/services/sync.ts` | export | async function | `pushPendingExpenses(remote: ExpensesRemote, userId: string): Promise<{ pushed: number; failed: number }>` | db, collapseSyncAttempts, d.syncQueue.orderBy('createdAt').toArray, d.syncQueue.orderBy, removeSuperseded, d.localExpenses.get, d.syncQueue.delete, withSyncOperation, remote.upsert, toExpenseRow | 비용 대기열 push(moments와 대칭: 멱등 upsert → read-back → 서버시각 반영 → 작업 제거). |
| `src/services/sync.ts` | export | async function | `pullExpenses(remote: ExpensesRemote, mode: PullMode): Promise<{ pulled: number; skippedEmptyCloud: boolean }>` | db, remote.listAll, purgedIdSet, (await d.localExpenses.toArray()).filter, d.localExpenses.toArray, isEmptyCloudAnomaly, purged.has, fromExpenseRow, d.localExpenses.get, mergeDecision | 서버의 내 비용을 로컬에 병합(교체 아님, 빈-클라우드 가드, LWW/tombstone). |
| `src/services/sync.ts` | export | async function | `pushPendingMedia(remote: MediaRemote, userId: string): Promise<{ pushed: number; failed: number }>` | db, collapseSyncAttempts, d.syncQueue.orderBy('createdAt').toArray, d.syncQueue.orderBy, removeSuperseded, d.localMedia.get, d.syncQueue.delete, d.localTrips.get, mediaStoragePath, mustUploadBytes | 사진 push: 표시본을 R2에 올리고 메타 행을 upsert → 행 read-back → R2 바이트 read-back → 로컬 원본 임시본 정리 → 작업 제거. 어느 확인이든 실패하면 원본과 op을 남겨 재시도한다. |
| `src/services/sync.ts` | internal | async function | `sweepServerOrphanMedia(remote: MediaRemote, row: MediaRow, storagePath: string \\| null): Promise<boolean>` | new Date().toISOString, remote.upsert, crypto.randomUUID, remote.remove | 부모 여행이 영구삭제된 서버 고아 사진 을 서버에서 지운다 — tombstone을 밀고 바이트를 지운다. 🔴 판정은 여기 없다 (`serverOrphanAction`이 한다). 이 함수는 실행만 한다 — 그래서 「지울까?」와 「어떻게 지우나」가 갈라지고, 앞의 것은 유닛으로 전수 검사된다(§10 ③). 🔴 왜 사진에… |
| `src/services/sync.ts` | export | async function | `pullMedia(remote: MediaRemote, mode: PullMode): Promise<{ pulled: number; skippedEmptyCloud: boolean }>` | db, remote.listAll, purgedIdSet, (await d.localMedia.toArray()).filter, d.localMedia.toArray, isEmptyCloudAnomaly, purged.has, fromMediaRow, d.localMedia.get, mergeDecision | — |
| `src/services/sync.ts` | export | interface | `PlacesRemote` | — | 장소 라이브러리 포트. 형제 중 가장 단순하다 — 바이트가 없어 업로드/다운로드가 없고(좌표와 이름뿐), 부모가 없어 push 순서 제약도 없다. 그 두 가지가 곧 장소가 형제와 다른 점이다. |
| `src/services/sync.ts` | export | function | `placesRemote(client: JourneyClient): PlacesRemote` | client.from('places').upsert, client.from, client.from('places').select('*').eq('id', id).maybeSingle, client.from('places').select('*').eq, client.from('places').select, fetchAllRows | — |
| `src/services/sync.ts` | export | async function | `pushPendingPlaces(remote: PlacesRemote, userId: string): Promise<{ pushed: number; failed: number }>` | db, collapseSyncAttempts, d.syncQueue.orderBy('createdAt').toArray, d.syncQueue.orderBy, removeSuperseded, d.localPlaces.get, d.syncQueue.delete, withSyncOperation, remote.upsert, toPlaceRow | 장소 push — upsert → 되읽기 → 서버시각 반영 → 큐 op 제거. 되읽기에서 좌표를 대조한다 (비용이 금액을 대조하는 것과 같은 자리). 200 응답은 "받았다" 이지 "그 값으로 있다"가 아니다 — 그리고 장소에서 틀리면 안 되는 값은 좌표다. |
| `src/services/sync.ts` | export | async function | `pullPlaces(remote: PlacesRemote, mode: PullMode): Promise<{ pulled: number; skippedEmptyCloud: boolean }>` | db, remote.listAll, purgedIdSet, (await d.localPlaces.toArray()).filter, d.localPlaces.toArray, isEmptyCloudAnomaly, purged.has, fromPlaceRow, d.localPlaces.get, mergeDecision | 서버의 내 장소를 로컬에 병합(교체 아님, 빈-클라우드 가드, LWW/tombstone). |
| `src/services/sync.ts` | export | async function | `pushPendingAudio(remote: AudioRemote, userId: string): Promise<{ pushed: number; failed: number }>` | db, collapseSyncAttempts, d.syncQueue.orderBy('createdAt').toArray, d.syncQueue.orderBy, removeSuperseded, d.localAudio.get, d.syncQueue.delete, d.localTrips.get, audioStoragePath, markFail | — |
| `src/services/sync.ts` | export | async function | `pullAudio(remote: AudioRemote, mode: PullMode): Promise<{ pulled: number; skippedEmptyCloud: boolean }>` | db, remote.listAll, purgedIdSet, (await d.localAudio.toArray()).filter, d.localAudio.toArray, isEmptyCloudAnomaly, purged.has, fromAudioRow, d.localAudio.get, mergeDecision | 소리 pull(비파괴) — 사진과 같은 규율. 서버가 더 최신일 때만 반영하고, tombstone은 로컬 blob을 지우지 않고 `deletedAt`만 세운다(로컬에 없으면 skip). 활성은 바이트를 내려받아 재구성하되 다운로드 실패 시 로컬을 그대로 둔다 (비파괴, 불변식 #8). |
| `src/services/sync.ts` | export | async function | `pushPendingVideos(remote: VideoRemote, userId: string): Promise<{ pushed: number; failed: number }>` | db, collapseSyncAttempts, d.syncQueue.orderBy('createdAt').toArray, d.syncQueue.orderBy, removeSuperseded, d.localVideos.get, d.syncQueue.delete, d.localTrips.get, videoStoragePath, markFail | — |
| `src/services/sync.ts` | export | async function | `pullVideos(remote: VideoRemote, mode: PullMode): Promise<{ pulled: number; skippedEmptyCloud: boolean }>` | db, remote.listAll, purgedIdSet, (await d.localVideos.toArray()).filter, d.localVideos.toArray, isEmptyCloudAnomaly, purged.has, fromVideoRow, d.localVideos.get, mergeDecision | — |
| `src/services/sync.ts` | export | async function | `backfillAudioOps(serverRows: AudioRow[], serverPurgedIds: ReadonlySet<string>): Promise<number>` | db, serverRows.map, fromAudioRow, (await d.syncQueue.toArray()).filter((q) => q.entityType ==…, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, new Date().toISOString, d.localAudio.toArray, queued.has, serverById.get | 소리를 처음으로 서버에 올려보내기 위한 백필 (2026-07-27, 일회성). 왜 필요한가(§9 4단계 — "옛 방식으로 만들어진 것을 누가 데려오는가?" ): v1.14~v1.19 동안 만들어진 오디오 노트에는 큐 op가 애초에 존재한 적이 없다. 코드에 push/pull을 붙이는 것만으로는 그 행들이 영원히 로컬에만… |
| `src/services/sync.ts` | export | async function | `backfillMediaGpsOps(serverRows: MediaRow[], serverPurgedIds: ReadonlySet<string>): Promise<number>` | db, serverRows.map, fromMediaRow, (await d.syncQueue.toArray()).filter((q) => q.entityType ==…, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, new Date().toISOString, d.localMedia.toArray, queued.has, serverPurgedIds.has | 좌표를 가진 사진의 행을 한 번 다시 올린다 (2026-08-01 · 마이그레이션 0024 백필). 왜 필요한가(§9 4단계 · M-0023의 근본형): 마이그레이션은 컬럼을 만들 뿐 값을 채우지 않는다. 이미 있는 사진 수십 장의 좌표는 이 기기 안에만 있고, 그 행들은 이미 서버에 올라가 있어 `syncQueue`에… |
| `src/services/sync.ts` | export | async function | `requeueMissingBytes(domain: PurgeDomain, ids: string[]): Promise<number>` | db, DOMAIN_PURGE[domain].table, (await d.syncQueue.toArray()).filter((q) => q.entityType ==…, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, new Date().toISOString, table.get, d.transaction, table.put, queued.has | 서버에 파일이 없는 기록의 바이트를 다시 올린다 (2026-07-28, M-0046 복구 경로). 언제 쓰나: 진단이 「서버에 없는 사진/소리/영상」을 짚었을 때. 그 상태는 서버 행은 경로를 적어 놓았는데 그 자리에 파일이 없다 는 뜻이고, 이 기기에 사본이 남아 있으면 고칠 수 있다. 어떻게: 이 기기가 기억하는 착… |
| `src/services/sync.ts` | internal | const | `BYTES_RECONCILE_KEY` | — | 마지막 바이트 대조 시각(ISO). 자동 동기화는 하루 한 번만 목록을 부른다. |
| `src/services/sync.ts` | export | const | `BYTES_RECONCILE_EVERY_MS` | — | 자동 동기화의 대조 주기. 사용자가 직접 부른 동기화(`deep`)는 이 값과 무관하게 항상 돈다. |
| `src/services/sync.ts` | export | function | `reconcileDue(lastAt: string \\| null, now: number, deep: boolean): boolean` | Date.parse, Number.isNaN | 지금 대조할 때인가 — 순수 함수라 유닛이 직접 잰다(경계값 포함). |
| `src/services/sync.ts` | export | async function | `reconcileMissingBytes(client: JourneyClient): Promise<{ requeued: Record<string, number>; note: string \\| null }>` | r2ListObjects, localBytesIds, listing.videoIds.filter, listing.videoPosterIds!.includes, unknown.push, [...local[domain]].filter, server.has, requeueMissingBytes, unknown.join | 로컬에 사본이 있는데 서버에 파일이 없는 것 을 찾아 다시 올리기로 큐에 넣는다. 이 함수는 고치기만 하고 지우지 않는다. 반대 방향(서버에 파일이 있는데 기록이 없음)은 손대지 않는다 — 그건 자료를 버리는 방향이고, 자동으로 할 일이 아니다(비타협 원칙 #1·#5). 진단이 사람에게 보여 주고 사람이 정한다. 모르면… |
| `src/services/sync.ts` | internal | async function | `reconcileBytesIfDue(client: JourneyClient, deep: boolean): Promise<void>` | localStorage.getItem, reconcileDue, Date.now, reconcileMissingBytes, Object.values(requeued).reduce, Object.values, Object.entries(requeued).map(([d, n]) => `${d} ${n}건`).join, Object.entries(requeued).map, Object.entries, console.info | 대조 단계 — 실패해도 동기화를 멈추지 않는다(못 고친 대가는 「어긋남이 조금 더 남는다」뿐). |
| `src/services/sync.ts` | internal | const | `AUDIO_BACKFILL_KEY` | — | 소리 백필 1회 실행 표식. 되돌려야 하면 `.v2`로 올려 전 기기가 한 번 더 돌게 한다. |
| `src/services/sync.ts` | internal | const | `MEDIA_GPS_BACKFILL_KEY` | — | 사진 GPS 백필 1회 실행 표식(마이그레이션 0024). 같은 규율 — 형제와 같은 자리에 둔다(§7). |
| `src/services/sync.ts` | export | async function | `backfillAudioOnce(remote: AudioRemote, ledgerRemote: Pick<PurgeRemote, 'ledgerAll'>): Promise<void>` | localStorage.getItem, remote.listAll, ledgerRemote.ledgerAll, backfillAudioOps, console.info, localStorage.setItem, new Date().toISOString | — |
| `src/services/sync.ts` | export | async function | `backfillMediaGpsOnce(remote: MediaRemote, ledgerRemote: Pick<PurgeRemote, 'ledgerAll'>): Promise<void>` | localStorage.getItem, remote.listAll, ledgerRemote.ledgerAll, backfillMediaGpsOps, console.info, localStorage.setItem, new Date().toISOString | — |
| `src/services/sync.ts` | internal | async function | `requeueIfServerStillActive(entityType: SyncEntityType, local: { id: string; deletedAt: string \\| null } \\| undefined, server: { deletedAt: string \\| null }, mode: PullMode): Promise<void>` | requeueDeleteAction, Boolean, db, (await d.syncQueue.where('entityId').equals(local.id).toArr…, d.syncQueue.where('entityId').equals(local.id).toArray, d.syncQueue.where('entityId').equals, d.syncQueue.where, d.syncQueue.add, crypto.randomUUID, new Date().toISOString | 서버가 아직 살아 있는데 로컬은 지운 상태 면 삭제를 다시 대기열에 올린다. 왜 pull에 두나: pull은 이미 서버 상태를 받아왔으므로 추가 비용 0으로 정확히 판정할 수 있다. "로컬 tombstone + 서버 활성" = 그 삭제가 서버에 도달하지 못했다는 직접 증거 다. ⚠️ 설계 이력(2026-07-25): 처… |
| `src/services/sync.ts` | internal | const | `STAMP_KEY` | — | 시각 표기 1회 정리 표식(M-0034). 되돌려야 하면 `.v2`로 올려 전 기기가 한 번 더 돌게 한다. |
| `src/services/sync.ts` | export | interface | `PurgeRemote` | — | 영구삭제 전파 포트 — 행을 실제로 지우고, 지운 id만 원장에 남긴다 (ADR-0030). 왜 바뀌었나(사용자 결정 2026-07-26): "의도를 가지고 삭제하는건데 서버에 왜 살려두나요? … 2번 이상 클릭으로 삭제한거라면 영원히 복구가 안되도록 기록줄까지도 삭제시켜야." ADR-0027이 행을 남긴 유일한 이유는… |
| `src/services/sync.ts` | internal | const | `LEDGER` | — | 원장 테이블 이름 — 문자열을 여러 곳에 손으로 적지 않는다. |
| `src/services/sync.ts` | internal | function | `ledgerOps(client: JourneyClient): Pick<PurgeRemote, 'ledgerAdd' \\| 'ledgerHas' \\| 'ledgerAll' \\| 'ledgerRemove'>` | client .from(LEDGER) .upsert, client .from, ids.map, client.from(LEDGER).select('id').eq('id', id).maybeSingle, client.from(LEDGER).select('id').eq, client.from(LEDGER).select, client.from, client.rpc, ((r.data ?? []) as { id: string }[]).map | 원장(`purged_ids`) 접근만 모은 조각. `purgeRemote`가 120줄 상한(`check-fn-size`)에 걸려 쪼갰는데, 결과적으로 경계가 맞아떨어졌다 — 원장은 "무엇을 지웠나"의 기록 이고 나머지(familyIds·mediaPath·hardDelete)는 "무엇을 지울까"의 조회·실행 이다. 다른 … |
| `src/services/sync.ts` | export | function | `purgeRemote(client: JourneyClient): PurgeRemote` | ledgerOps, childDomains, client.from(DOMAIN_PURGE[d].remoteTable).select('id').eq, client.from(DOMAIN_PURGE[d].remoteTable).select, client.from, ids.push, childDomains(parent).filter, client.from(t).select('storage_path').eq(col, id).not, client.from(t).select('storage_path').eq, client.from(t).select | — |
| `src/services/sync.ts` | export | interface | `PurgeVerification` | — | 영구삭제 뒤 사용자에게 보여 줄 서버 read-back 영수증. 응답 성공이 아니라 원장과 행 부재를 모두 센다. |
| `src/services/sync.ts` | export | async function | `verifyPurgeReceipt(remote: PurgeRemote, targets: readonly Pick<PurgeTarget, 'id' \\| 'domain' \\| 'underRoot'>[], onSettled?: (completed: number, total: number) => void): Promise<PurgeVerification>` | new Map(targets.map((target) => [`${target.domain}:${target…, targets.map, Promise.all, unique.map, asPurgeParent, remote.remainingInFamily, Promise.resolve, remote.ledgerHas, remote.stillThere, onSettled | 방금 영구삭제한 대상이 서버에서도 끝났는지 다시 읽는다. `pushPurges`도 같은 read-back을 수행하지만 그 결과는 동기화 전체 결과에 섞인다. 이 함수는 사용자가 고른 대상만 따로 영수증으로 집계한다. 각 대상은 서로 독립적이므로 병렬로 확인하고, 부모 대상은 가족 잔여 행도 함께 본다. 조회 오류는 성공… |
| `src/services/sync.ts` | export | interface | `BytesRemote` | — | 사진 바이트를 지우는 최소 포트 — 영구삭제가 쓰는 유일한 파괴 경로. |
| `src/services/sync.ts` | internal | async function | `removePurgeBytes(bytes: BytesRemote, paths: string[]): Promise<string \\| null>` | bytes.remove | — |
| `src/services/sync.ts` | export | async function | `pushPurges(remote: PurgeRemote, bytes?: BytesRemote): Promise<{ pushed: number; failed: number }>` | db, (await d.syncQueue.orderBy('createdAt').toArray()).filter, d.syncQueue.orderBy('createdAt').toArray, d.syncQueue.orderBy, purgeDomainOf, asPurgeParent, appendPurgeBytePath, remote.familyIds, markFail, ledgerIds.push | 영구삭제 작업을 서버로 밀어 자료를 실제로 지운다 (ADR-0030). 기존 도메인 push 루프가 이걸 처리하면 안 된다 — 영구삭제는 로컬 행을 이미 지웠으므로 그 루프들은 "로컬에 없는 고아 작업"으로 보고 조용히 폐기 한다(그러면 전파가 영영 안 된다). entityType을 `purge: `로 두어 기존 루프(… |
| `src/services/sync.ts` | export | async function | `retryFailedOps(): Promise<number>` | db, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, d.syncQueue.update | — |
| `src/services/sync.ts` | internal | async function | `normalizeTableStamps(table: Table<SyncMeta & { id: string }, string>): Promise<number>` | table.db.transaction, table.each, withCanonicalStamps, patches.push, table.update | 이미 저장된 행의 시각 표기를 정규형으로 맞춘다 (M-0034, 2026-07-27). 왜 코드 수정만으로 부족한가: `isoInstant()`는 앞으로 들어올 값을 막을 뿐이고, 서버 표기 (`…48.34+00:00`)로 이미 박힌 행은 그대로 남는다. 사용자 기기에 사진 9건이 그 상태였고 진단이 그걸 「시간 역전」… |
| `src/services/sync.ts` | export | async function | `normalizeStamps(): Promise<number>` | db, normalizeTableStamps | 로컬 테이블 전부 — 형제를 손으로 세지 않는다(§7). 고친 행 수를 돌려준다. |
| `src/services/sync.ts` | internal | async function | `normalizeStampsOnce(): Promise<void>` | localStorage.getItem, normalizeStamps, console.info, localStorage.setItem, new Date().toISOString | — |
| `src/services/sync.ts` | export | async function | `pushUnpurges(remote: PurgeRemote): Promise<{ pushed: number; failed: number }>` | db, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, ops.map, remote.ledgerRemove, console.error, remote.ledgerAll, ops.filter, still.has, d.syncQueue.delete | 복원이 되살린 id를 서버 원장에서 뺀다. 다른 push·pull보다 먼저 돌아야 한다 — 원장이 남아 있으면 ① BEFORE INSERT 트리거가 복원 push를 거부하고 ② 이어지는 원장 pull이 로컬 행까지 지운다. 2026-07-26에 정확히 그 순서로 사용자의 복원이 무효화됐다. 실패하면 op을 남긴다 (지우… |
| `src/services/sync.ts` | internal | async function | `revivePushOps(ids: string[]): Promise<number>` | db, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, target.has, d.syncQueue.update, console.info | 원장에 막혀 죽은 전파 op을 되살린다. 왜 필요한가: 트리거가 거부한 응답은 4xx라 `classifyError`가 'permanent' 로 본다 → op이 `permanent_failed`가 되고 push는 그 상태를 영원히 건너뛴다. 그래서 나중에 원장을 되돌리는 데 성공해도 그 행은 두 번 다시 올라가지 않는다 … |
| `src/services/sync.ts` | export | interface | `SyncOptions` | — | 동기화 실행 옵션. `deep` — 사용자가 직접 부른 동기화 인가. 자동(주기·화면 복귀·저장 직후)과 구별한다. 이름이 아니라 호출자의 의도 로 판정한다: 이유 문자열을 훑어 「수동」인지 맞히는 방식은 새 진입점이 생기면 조용히 빠진다(M-0040 — 이름으로 판정하면 새 형제를 정확히 놓친다). |
| `src/services/sync.ts` | internal | function | `reportProgress(opts: SyncOptions, progress: SyncProgress): void` | opts.onProgress | — |
| `src/services/sync.ts` | internal | interface | `EntityRemotes` | — | — |
| `src/services/sync.ts` | internal | async function | `pushEntityOps(remotes: EntityRemotes, userId: string, onDomainSettled?: (domain: SyncDomain) => void): Promise<{ pushed: number; failed: number }>` | runSyncPlan, pushPending, pushPendingPlaces, pushPendingMoments, pushPendingMedia, pushPendingExpenses, pushPendingAudio, pushPendingVideos, SYNC_DOMAINS.reduce | 부모→자식 순서를 한 곳에 둔다. pull이 만든 삭제 op의 안전한 후행 전송도 같은 문을 지난다. |
| `src/services/sync.ts` | internal | async function | `pullEntityRows(remotes: EntityRemotes, mode: PullMode, onDomainSettled?: (domain: SyncDomain) => void)` | runSyncPlan, pullTrips, pullPlaces, pullMoments, pullMedia, pullExpenses, pullAudio, pullVideos | — |
| `src/services/sync.ts` | internal | function | `summarizePulls(result: Awaited<ReturnType<typeof pullEntityRows>>)` | SYNC_DOMAINS.reduce, SYNC_DOMAINS.some | — |
| `src/services/sync.ts` | internal | async function | `runServerReadOnlySync(client: JourneyClient, opts: SyncOptions): Promise<SyncResult>` | reportProgress, summarizePulls, pullEntityRows, tripsRemote, placesRemote, momentsRemote, mediaRemote, expensesRemote, audioRemote, videoRemote | canonical capability를 증명할 수 없는 짧은 배포 전환 구간의 안전 경로. 서버에는 SELECT/R2 GET만 수행한다. 로컬 purge·unpurge·편집 큐는 그대로 두고, 서버 고아 tombstone 같은 정리 쓰기도 하지 않는다. capability가 돌아오면 다음 동기화가 큐를 처리한다. |
| `src/services/sync.ts` | export | async function | `runSync(client: JourneyClient, userId: string, opts: SyncOptions = {}): Promise<SyncResult>` | reportProgress, ensureCanonicalBeforeSync, canonicalRemote, runServerReadOnlySync, tripsRemote, momentsRemote, expensesRemote, mediaRemote, audioRemote, videoRemote | — |
| `src/services/syncParity.ts` | export | const | `PARITY_TTL_MS` | — | 이보다 오래된 대조는 「모름」으로 본다. 왜 30분인가: 성공한 동기화가 대조값을 갱신한다. 30분이 지났다는 것은 동기화가 그동안 한 번도 성공하지 못했다 는 뜻이고, 그러면 그 대조값도 믿을 수 없다. |
| `src/services/syncParity.ts` | internal | const | `cached: ParitySnapshot \\| null` | — | — |
| `src/services/syncParity.ts` | internal | const | `inFlight: Promise<ParitySnapshot \\| null> \\| null` | — | — |
| `src/services/syncParity.ts` | internal | const | `listeners` | — | — |
| `src/services/syncParity.ts` | internal | function | `notifyParityChange(): void` | listener | 대조의 시작·완료·실패를 배지처럼 캐시 소비자에게 알린다. |
| `src/services/syncParity.ts` | export | function | `onParityChange(listener: () => void): () => void` | listeners.add, listeners.delete | 대조 캐시가 바뀌었거나 대조 중인지 바뀔 때 화면이 다시 읽을 수 있게 한다. |
| `src/services/syncParity.ts` | export | function | `isParityRefreshing(): boolean` | — | 동기화 성공 직후에는 대조가 끝날 때까지 「확인 중」으로만 보인다. |
| `src/services/syncParity.ts` | export | function | `lastParity(now = Date.now()): ParitySnapshot \\| null` | Date.now, Date.parse, Number.isNaN | 마지막 대조 — 만료됐으면 null. 동기 함수 라 화면이 그리는 중에 바로 읽는다. |
| `src/services/syncParity.ts` | export | function | `refreshParity(): Promise<ParitySnapshot \\| null>` | (async () => { try { const c = supabase(); if (!c \\|\\| !(aw…, supabase, currentUser, compareStore, storeStateRemote, Object.values(cmp.counts).filter, Object.values, new Date().toISOString, notifyParityChange | 서버와 대조해 캐시를 갱신한다. 실패하면 캐시를 비운다 — 조회에 실패했는데 옛 「같음」을 들고 있으면 그게 곧 거짓 초록이다(§8). 동시에 여러 번 불려도 한 번만 돈다(single-flight) — 홈이 여러 번 다시 그려질 때 요청이 겹치는 것을 막는다(`autoSync`의 같은 규율). |
| `src/services/syncParity.ts` | export | async function | `refreshParityAfterSync(): Promise<ParitySnapshot \\| null>` | refreshParity | 성공한 동기화보다 먼저 시작한 대조는 완료 증거가 아니다. 그 대조가 이미 돌고 있으면 끝난 뒤 반드시 한 번 더 잰다 — single-flight를 "옛 대조 재사용"으로 바꾸지 않는다. |
| `src/services/syncParity.ts` | internal | const | `installed` | — | — |
| `src/services/syncParity.ts` | export | function | `installParityWatch(): () => void` | onSyncStatus, refreshParityAfterSync, off | 동기화가 성공으로 끝날 때마다 대조를 다시 잰다. 왜 여기서 거는가: 서버와의 관계가 바뀌는 유일한 계기가 동기화다. 화면이 스스로 주기를 만들면 그 주기가 또 하나의 관리 대상이 되고, 두 주기가 어긋나면 배지가 낡은 값을 보여준다(§7 — 같은 규율을 두 곳에 두지 않는다). |
| `src/services/syncParity.ts` | export | function | `__resetParityForTests(): void` | listeners.clear | 테스트용 초기화(모듈 상태를 갖는 모듈의 의무). |
| `src/services/syncPlan.ts` | export | const | `SYNC_DOMAINS` | — | — |
| `src/services/syncPlan.ts` | export | typealias | `SyncDomain` | — | — |
| `src/services/syncPlan.ts` | export | interface | `SyncPlanObserver` | — | — |
| `src/services/syncPlan.ts` | internal | interface | `RootSyncLevel` | — | — |
| `src/services/syncPlan.ts` | internal | interface | `DependentSyncLevel` | — | — |
| `src/services/syncPlan.ts` | export | typealias | `SyncLevel` | — | — |
| `src/services/syncPlan.ts` | export | const | `PUSH_SYNC_PLAN` | — | push는 서버 복합 FK가 요구하는 세 단계만 직렬이고, 같은 단계의 형제는 병렬이다. 여행과 장소는 독립 부모, 순간은 둘을 참조, 사진·비용·소리는 순간을 참조한다. |
| `src/services/syncPlan.ts` | export | const | `PULL_SYNC_PLAN` | — | pull 결과끼리는 입력·FK 의존성이 없으므로 일곱 도메인을 한 단계에서 모두 시작한다. |
| `src/services/syncPlan.ts` | export | async function | `runSyncPlan(plan: readonly SyncLevel[], tasks: Record<SyncDomain, () => Promise<T>>, observer: SyncPlanObserver = {}): Promise<Record<SyncDomain, T>>` | Promise.allSettled, level.domains.map, Promise.resolve().then, Promise.resolve, settled.forEach, observer.onDomainSettled, String, failures.push, failures.join, SYNC_DOMAINS.filter | 단계 안에서는 전부 병렬 시작하고 전부 정산한다. 하나가 실패해도 나머지 형제의 서버/로컬 부수효과가 끝날 때까지 기다려, 호출자가 다음 단계나 다음 동기화를 시작하지 못하게 한다. |
| `src/sync/merge.ts` | export | function | `mergeDecision(local: SyncMeta \\| undefined, server: SyncMeta): 'take-server' \\| 'keep-local'` | compareInstants | 서버 행을 로컬에 반영할지 결정. version 기반 tombstone 우위 + LWW (SYNC_PROTOCOL 불변식 2). 좀비데이터 절대 방지의 핵심 규칙: - 삭제(tombstone)는 "강한 의도"다. 활성 사본이 tombstone을 이기려면 오직 진짜 복원 (version이 tombstone보다 더 큼)이어… |
| `src/sync/merge.ts` | export | function | `isEmptyCloudAnomaly(serverRowCount: number, localActiveCount: number): boolean` | — | 빈-클라우드 가드(불변식 4): 서버가 0행인데 로컬에 활성 데이터가 있으면 이상 상황. RLS 오설정·잘못된 프로젝트일 수 있으므로 로컬을 절대 덮어쓰지 않는다. |
| `src/sync/merge.ts` | export | const | `RETRY_SCHEDULE_MS` | — | 재시도 대기 시간 (ms) — `docs/SYNC_PROTOCOL.md:31`의 계약을 코드로 옮긴 것. ⚠️ 2026-07-27까지 이 함수가 없었다. 계약은 문서에 5초/15초/60초/5분/15분으로 적혀 있었는데 `markFail`은 `attempts`를 증가만 시키고 아무도 읽지 않았다 (grep 0건). 그래서… |
| `src/sync/merge.ts` | export | function | `retryDelayMs(attempts: number, rand: () => number = Math.random): number` | Math.min, Math.max, Math.floor, Math.round, rand | — |
| `src/sync/merge.ts` | export | function | `isRetryDue(op: { nextRetryAt?: string }, nowIso: string): boolean` | Date.parse, Number.isNaN | 지금 이 op을 시도해도 되는가. `nextRetryAt`이 없으면(옛 항목·첫 시도) 항상 참이다 — 모르는 것을 "대기"로 반올림하면 옛 큐가 영영 안 나간다. |
| `src/sync/merge.ts` | export | function | `classifyError(status: number \\| undefined): 'retryable' \\| 'permanent'` | — | 오류 분류: 재시도 가능(네트워크·일시) vs 영구(검증·권한). status undefined = 네트워크 미도달 → 재시도. 401/403 = 인증/권한 → 영구(사용자 개입). |
| `src/sync/merge.ts` | export | function | `mustUploadBytes(e: { deletedAt: string \\| null; bytesMissing?: true; storagePath?: string; base…, unknownPathMeansNeverUploaded: boolean): boolean` | — | 바이트를 서버에 올려야 하는가 — 사진·소리·영상이 같은 문을 지난다 (§7 2층). ── 왜 이 함수가 생겼나 (2026-08-01 · M-0059, 사용자 실기기) ────────────── 두 형제의 업로드 조건이 손으로 따로 쓰여 있었고 갈라져 있었다 : ``` 소리: if (audio.deletedAt === … |
| `src/sync/merge.ts` | export | function | `writeLanded(server: { version: number; clientOperationId?: string; storagePath?: string \\| …, sentVersion: number, sentOperationId: string, sentPath?: string): boolean` | — | 🔴 내가 방금 보낸 쓰기가 서버에 실제로 착지했는가 (M-3 · 사진·소리·영상 read-back). 왜 「행이 있다」로는 부족한가(2026-08-01 감사): 사진·소리·영상 push는 upsert 뒤 `getById`로 되읽되 행이 존재하기만 하면 서버의 `updatedAt`/`version`을 자기 로컬에 덮어쓰… |
| `src/sync/merge.ts` | export | typealias | `ServerOrphanAction` | — | 서버 고아를 만났을 때 이 행을 어떻게 할 것인가 (2026-08-15 · T-047에서 꺼냄). ── 왜 꺼냈나 ──────────────────────────────────────────────────────────────── 이 판정은 `pullMedia`의 for 루프 안에 인라인으로 있었다. 그런데 이게 지키는… |
| `src/sync/merge.ts` | export | function | `serverOrphanAction(input: { /** 로컬에 이 id의 행이 없는가. */ readonly localMissing: boolean; /** 서버 행의 `de…): ServerOrphanAction` | — | — |
| `src/sync/merge.ts` | export | typealias | `RequeueDeleteAction` | — | 로컬은 지웠는데 서버는 아직 살아 있다 — 그 삭제를 다시 대기열에 올릴 것인가. ── 왜 꺼냈나 (T-047 · 2026-08-15) ────────────────────────────────────────── 이 판정은 일곱 pull 루프 전부 가 부른다(trip·moment·expense·media·place·au… |
| `src/sync/merge.ts` | export | function | `requeueDeleteAction(input: { /** 로컬 행이 있는가 — 없으면 대기열이 손댈 대상이 아니다. */ readonly hasLocal: boolean; /*…): RequeueDeleteAction` | — | — |
| `src/sync/merge.ts` | export | typealias | `UnreferencedByteAction` | — | DB가 더 이상 가리키지 않는 옛 객체 키를 지울 것인가 (T-047 · 2026-08-15). operation별 객체 키를 쓰므로 같은 사진이 다시 올라가면 옛 키가 R2에 남는다. 그걸 걷는 자리인데, 판정을 잘못하면 DB가 지금 가리키고 있는 바이트를 지운다 — 행은 살아 있는데 바이트만 사라지는, 이 앱에서 가… |
| `src/sync/merge.ts` | export | function | `unreferencedByteAction(candidate: string \\| undefined \\| null, referenced: string \\| null \\| undefined): UnreferencedByteAction` | — | — |

## 5. 자동 검증 연결

- `tests/unit/audioServerSync.test.ts`
- `tests/unit/bytesReconcile.test.ts`
- `tests/unit/bytesReconcileRun.test.ts`
- `tests/unit/canonicalSync.test.ts`
- `tests/unit/cascadeOps.test.ts`
- `tests/unit/contractGaps.test.ts`
- `tests/unit/deviceFleetVerdict.test.ts`
- `tests/unit/fetchAllRows.test.ts`
- `tests/unit/instantFormat.test.ts`
- `tests/unit/merge.test.ts`
- `tests/unit/purgeCascade.test.ts`
- `tests/unit/purgePropagation.test.ts`
- `tests/unit/restoreUnpurge.test.ts`
- `tests/unit/reuploadMissingBytes.test.ts`
- `tests/unit/staleSyncWrite.test.ts`
- `tests/unit/syncBadgeVerdict.test.ts`
- `tests/unit/syncParallelism.test.ts`
- `tests/unit/syncProgress.test.ts`
- `tests/unit/syncReleaseVerdict.test.ts`
- `tests/unit/syncStatusVerdict.test.ts`
- `tests/unit/syncTombstoneVerdict.test.ts`
- `tests/unit/videoServerSync.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/services/sync.ts` — 2717줄
- `src/services/canonicalSync.ts` — 930줄
- `src/sync/merge.ts` — 342줄
- `src/services/autoSync.ts` — 289줄
- `src/domain/syncBadgeVerdict.ts` — 183줄
- `src/domain/syncTombstoneVerdict.ts` — 154줄
- `src/domain/deviceFleetVerdict.ts` — 139줄
- `src/services/syncParity.ts` — 139줄
- `src/domain/syncReleaseVerdict.ts` — 129줄
- `src/domain/syncStatusVerdict.ts` — 106줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
