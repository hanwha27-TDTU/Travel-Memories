---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 사진 수집·파생본·저장 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

사진 입력, EXIF 선읽기, 파생본 생성, 앱 보관본 표시·저장과 R2 바이트 경계를 소유한다.

- 모듈 ID: `photo-storage`
- 관측 파일: 13개 · 논리 줄: 2426 · 최상위 선언: 120개(외부 공개 82개)
- 코드 내용 SHA-256: `c56df05b933a93e6f913e1df9b5a189483d472d14d291b92b726d1c831c082b0`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/media/download.ts`

### 다른 모듈로 나가는 정적 의존

- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 3건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 3건
- 사진편집기(`photo-editor`): import/re-export 2건
- 화면·오버레이·상호작용(`ui-shell`): import/re-export 2건
- 인증·Supabase 클라이언트·동의(`auth-security`): import/re-export 1건
- 지도·장소·지오코딩(`map-place`): import/re-export 1건
- 플랫폼 런타임·파일 저장·업데이트(`platform-runtime`): import/re-export 1건
- 오프라인 동기화와 정본 전환(`sync-canonical`): import/re-export 1건

### 외부 패키지/런타임 의존

- Node 내장 또는 저장소 내부 모듈만 정적으로 import한다.

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/media/download.ts` | 33 | 2/3 | src/domain/media/naming.ts | — |
| `src/domain/media/naming.ts` | 241 | 15/16 | — | — |
| `src/domain/media/order.ts` | 162 | 8/8 | src/domain/time.ts | Dexie: localMedia |
| `src/domain/media/quota.ts` | 96 | 8/8 | — | transactional persistence |
| `src/domain/media/windowsDrop.ts` | 43 | 6/7 | — | file/blob bytes |
| `src/media/compress.ts` | 82 | 4/9 | — | file/blob bytes; browser media worker/codec; DOM interaction |
| `src/media/exif.ts` | 345 | 6/15 | src/domain/place/coordInput.ts | — |
| `src/services/media.ts` | 454 | 11/15 | src/offline/db.ts<br>src/domain/media/quota.ts<br>src/media/exif.ts<br>src/domain/time.ts<br>src/media/compress.ts<br>src/domain/media/order.ts<br>src/media/editor-core.ts | Dexie: localMedia, localMoments, localTrips; file/blob bytes; transactional persistence; deletion lifecycle; DOM interaction |
| `src/services/r2.ts` | 414 | 13/25 | src/services/supabase/client.ts | network fetch; Edge Function invoke; file/blob bytes; deletion lifecycle |
| `src/services/storage.ts` | 106 | 3/3 | src/offline/db.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localTrips, localVideos; file/blob bytes; deletion lifecycle |
| `src/ui/mediaSave.ts` | 49 | 2/2 | src/services/fileSave.ts<br>src/ui/dom.ts | file/blob bytes; DOM interaction |
| `src/ui/photoViewer.ts` | 325 | 1/6 | src/ui/dom.ts<br>src/domain/time.ts<br>src/ui/photoEditor.ts<br>src/services/media.ts<br>src/services/autoSync.ts<br>src/domain/media/download.ts<br>src/offline/db.ts<br>src/ui/mediaSave.ts | deletion lifecycle; DOM interaction |
| `src/ui/pickOriginal.ts` | 76 | 3/3 | — | DOM interaction |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/media/download.ts` | internal | function | `photoExt(mime: string): string \\| null` | mime.toLowerCase, normalized.includes | — |
| `src/domain/media/download.ts` | export | function | `photoDownloadFilename(takenAt: string, tripTitle: string, mediaId: string, mime: string): string` | photoExt, photoFileBase | — |
| `src/domain/media/download.ts` | export | function | `videoDownloadFilename(takenAt: string, tripTitle: string, videoId: string, mime: string): string` | videoExt, videoFileBase | — |
| `src/domain/media/naming.ts` | export | function | `fsSafe(s: string, max = 40): string` | (s \\|\\| '') .replace(/[/\\:*?"<>\\|]/g, '') .replace(/\s+/g,…, (s \\|\\| '') .replace(/[/\\:*?"<>\\|]/g, '') .replace, (s \\|\\| '') .replace | 객체 키·ZIP 경로에서 위험한 문자를 걷어낸다. 잘라내되 빈 문자열은 만들지 않는다 (호출부가 대체어를 준다). |
| `src/domain/media/naming.ts` | internal | function | `hex(id: string, len = 32): string` | id.replace(/-/g, '').toLowerCase().slice, id.replace(/-/g, '').toLowerCase, id.replace | id에서 하이픈을 뺀 소문자 hex. 32자(uuid) 또는 그보다 짧게 자른 접두. |
| `src/domain/media/naming.ts` | export | function | `operationStoragePath(currentPath: string, entityId: string, operationId: string): string` | hex, currentPath.lastIndexOf, currentPath.slice, file.lastIndexOf, file.slice, currentSuffix.test, stem.replace | 바이트 객체를 동기화 작업마다 새 키 로 격리한다(M-0087). DB의 OCC가 stale 행을 거절해도 R2 PUT은 되돌릴 수 없다. 같은 `storagePath`를 먼저 덮으면 메타데이터는 최신인데 바이트만 옛 판인 분리 상태가 된다. 그래서 operation id를 파일명에 넣되, Edge Function이 영… |
| `src/domain/media/naming.ts` | export | function | `tripFolderName(title: string \\| null \\| undefined, tripId: string): string` | fsSafe, hex | 여행 폴더명: `제주여행__c9ff5188`. 제목이 바뀌어도 이 값은 안 바뀐다 — 뒷자리가 id에서 오기 때문이다. 그래서 여행 이름을 고쳐도 이미 올라간 사진이 흩어지지 않는다(R2에는 '이름 바꾸기'가 없어서, 흩어지면 복사· 되읽기·삭제라는 최고 위험 연산으로만 되돌릴 수 있다 — `sync-offline-de… |
| `src/domain/media/naming.ts` | export | function | `stampFromISO(iso: string \\| null \\| undefined): { date: string; time: string }` | Date.parse, Number.isNaN, String(n).padStart, String, d.getFullYear, p, d.getMonth, d.getDate, d.getHours, d.getMinutes | ISO → { date:'YYYYMMDD', time:'HHMM' }. 사용자 로컬 시각 기준(check-timezone 대상). 파싱 불가면 0으로. |
| `src/domain/media/naming.ts` | export | function | `mediaObjectName(takenAt: string \\| null \\| undefined, tripTitle: string \\| null \\| undefined, mediaId: string): string` | stampFromISO, fsSafe, hex | R2 객체 이름 (확장자 제외): `20260716_1432_제주여행__<하이픈 뺀 32자>`. 뒤 32자는 장식이 아니라 계약 이다 — `mediaIdOfKey`(Edge Function)가 이걸 되읽어 어느 사진인지 판정한다. 그 둘이 어긋나면 멀쩡한 사진이 「설명할 수 없는 파일」로 보이고, 앱은 그걸 문제로 띄운… |
| `src/domain/media/naming.ts` | export | function | `photoFileBase(takenAt: string \\| null \\| undefined, title: string, mediaId: string): string` | stampFromISO, fsSafe, hex | 백업 ZIP 안의 사진 파일명(확장자·용도 제외): `날짜_시간_제목__id8`. R2 이름과 일부러 다르다 : ZIP은 사람이 탐색기로 열어 보는 것이라 짧아야 하고, 복원은 `trip.json`의 경로로 되읽으므로 id 전체가 필요 없다. R2는 그 반대다(위 머리말 참조). 서로 다른 이유를 여기 적어 두는 것이,… |
| `src/domain/media/naming.ts` | export | function | `audioObjectName(recordedAt: string \\| null \\| undefined, tripTitle: string \\| null \\| undefined, audioId: string): string` | stampFromISO, fsSafe, hex | R2 오디오 객체 이름 (확장자 제외) — 사진(`mediaObjectName`)과 같은 모양 이다. 왜 같아야 하나(§7 사용자 대면 대칭): 사용자는 Cloudflare 콘솔에서 두 종류를 한 폴더 안에 나란히 본다. 여기서 규칙이 갈리면 "왜 소리만 이름이 다르지?"가 되고, 그건 이 파일이 애초에 생긴 이유(UU… |
| `src/domain/media/naming.ts` | export | const | `AUDIO_EXTS` | — | 녹음 MIME → 파일 확장자. Edge Function의 `AUDIO_EXTS`와 같은 목록이어야 한다 — 앱이 `.webm`으로 올리는데 함수가 그 확장자를 거부하면 소리는 영영 서버에 못 간다. `tests/unit/audioNaming.test.ts`가 그 목록을 대조한다. 모르는 형식은 `bin`이 아니라 `w… |
| `src/domain/media/naming.ts` | export | function | `audioExt(mime: string): string \\| null` | (mime \\|\\| '').toLowerCase, m.includes | — |
| `src/domain/media/naming.ts` | export | function | `audioFileBase(recordedAt: string \\| null \\| undefined, title: string, audioId: string): string` | stampFromISO, fsSafe, hex | 백업 ZIP 안의 오디오 노트 파일명(확장자 제외): `날짜_시간_제목__id8`. 사진(`photoFileBase`)과 같은 모양 이다 — 사용자는 탐색기에서 두 종류를 나란히 본다. 여기서 규칙이 갈리면 "왜 소리만 이름이 다르지?"가 되고, 그건 §7 「사용자 대면 대칭」 위반이다. ⚠️ 이 함수가 뒤늦게 생긴 이… |
| `src/domain/media/naming.ts` | export | function | `videoObjectName(takenAt: string \\| null \\| undefined, tripTitle: string \\| null \\| undefined, videoId: string): string` | stampFromISO, fsSafe, hex | 영상도 사진·소리와 같은 여행 폴더/식별자 규약을 쓴다. |
| `src/domain/media/naming.ts` | export | function | `videoPosterStoragePath(videoPath: string): string \\| null` | videoPath.lastIndexOf, videoPath.slice, file.lastIndexOf, file.slice, stem.lastIndexOf, stem.slice | 영상 본체와 한 작업에 묶이는 R2 포스터 경로. 본체의 승인된 operation 경로에서만 파생한다. 여행 제목이나 현재 시각으로 다시 이름을 만들면 DB가 승인한 본체와 다른 세대의 포스터가 짝지어질 수 있다(M-0087). 마지막 id 32자는 그대로 유지해 행이 영구삭제된 뒤에도 Edge 목록/정리가 같은 영상의… |
| `src/domain/media/naming.ts` | export | const | `VIDEO_EXTS` | — | — |
| `src/domain/media/naming.ts` | export | function | `videoExt(mime: string): string \\| null` | (mime \\|\\| '').toLowerCase, m.includes | — |
| `src/domain/media/naming.ts` | export | function | `videoFileBase(takenAt: string \\| null \\| undefined, title: string, videoId: string): string` | stampFromISO, fsSafe, hex | — |
| `src/domain/media/order.ts` | export | interface | `OrderableMedia` | — | 정렬에 필요한 최소 모양 — 블롭까지 끌고 오지 않는다(유닛이 가벼워야 갈래를 다 돈다). |
| `src/domain/media/order.ts` | export | function | `orderPhotos(rows: readonly T[]): T[]` | [...rows].sort, compareInstants | 한 순간의 사진을 화면에 놓을 순서로 정렬한다. 원본 배열을 바꾸지 않는다. 규칙(위에서부터 이긴다): ① 사용자가 정한 자리(`sortOrder`)가 있으면 그 순서. 없는 것은 뒤로 — 새로 넣은 사진이 맨 뒤에 붙는다. 중간에 끼어들면 사용자가 정해 둔 배열이 소리 없이 흐트러진다. ② 둘 다 없으면 촬영시각 순(… |
| `src/domain/media/order.ts` | export | function | `moveItem(list: readonly T[], from: number, to: number): T[]` | next.splice | 배열에서 한 항목을 다른 자리로 옮긴다 — 드래그가 끝났을 때 쓰는 순수 연산. 범위 밖 인덱스는 조용히 자르지 않고 그대로 돌려준다 : 잘못된 입력을 「그럴듯한 결과」로 바꾸면 그 결함이 화면까지 살아서 간다(§8 — 모르는 것을 반올림하지 않는다). |
| `src/domain/media/order.ts` | export | function | `renumber(orderedIds: readonly string[]): Map<string, number>` | orderedIds.forEach, m.set | 옮긴 뒤의 순서를 번호로 굳힌다 — 그 순간의 사진 전부에 0부터 다시 매긴다. 🔴 왜 전부인가: 옮긴 하나만 번호를 주면 나머지는 `null`로 남아 「일부는 내 순서, 일부는 촬영시각」이 된다. 그 상태의 규칙은 사용자가 예측할 수 없고, 사진을 하나 더 넣으면 배열이 또 달라진다. 한 번 손대면 그 순간은 전부 … |
| `src/domain/media/order.ts` | export | interface | `Rect` | — | 드래그 중 지금 놓으면 몇 번째가 되는가 — 포인터 좌표를 항목 사각형들과 대조한다. 🔴 뜻을 하나로 못박는다 : 돌려주는 값은 `moveItem(list, from, to)`의 `to`와 같은 뜻 , 즉 "옮긴 뒤 이 항목이 앉을 자리" 다. 처음엔 「이 항목 앞에 끼운다」로 짰다가 두 뜻이 섞여 유닛이 RED로 잡… |
| `src/domain/media/order.ts` | export | interface | `Offset` | — | 한 칸을 원래 자리에서 얼마나 밀어야 하는가(px). `{0,0}`이면 그대로 있는다. |
| `src/domain/media/order.ts` | export | function | `shiftOffsets(rects: readonly Rect[], from: number, to: number): Offset[]` | rects.map | 🔴 끄는 동안 다른 칸들이 실제로 비켜서는 자리 (사용자 요청 2026-08-06: "이동하는 느낌이 나게 해줄 순 있나요?" ). 예전 판은 놓일 칸에 테두리만 그렸다. 자리는 알려 주지만 들고 가는 느낌 이 없어서, 사용자는 손을 떼기 전까지 「내가 지금 무엇을 하고 있는지」를 그림으로 확인할 수 없었다. ── 왜… |
| `src/domain/media/order.ts` | export | function | `dropIndex(rects: readonly Rect[], x: number, y: number, fallback: number): number` | rects.map((r, i) => ({ r, i })).filter, rects.map, sameRow.find, rects.filter | — |
| `src/domain/media/quota.ts` | export | interface | `StorageEstimateLike` | — | 브라우저가 알려준 저장 현황. 지원하지 않으면 둘 다 null. |
| `src/domain/media/quota.ts` | export | typealias | `QuotaLevel` | — | — |
| `src/domain/media/quota.ts` | export | interface | `QuotaVerdict` | — | — |
| `src/domain/media/quota.ts` | export | function | `humanBytes(n: number): string` | Math.round, (n / 1024).toFixed, (n / 1024 ** 2).toFixed, (n / 1024 ** 3).toFixed | 사람이 읽는 크기. 소수 한 자리까지 — 「0.0MB」로 반올림해 0을 보여주지 않는다. |
| `src/domain/media/quota.ts` | export | const | `BLOCK_RATIO` | — | 이 비율을 넘기면 저장을 막는다. 여유를 조금 남겨야 텍스트 기억이 계속 저장된다. |
| `src/domain/media/quota.ts` | export | const | `WARN_RATIO` | — | 이 비율을 넘기면 경고하되 진행한다. 진단의 「저장 공간 사용률」 기대값과 같은 값. |
| `src/domain/media/quota.ts` | export | function | `quotaVerdict(needBytes: number, est: StorageEstimateLike): QuotaVerdict` | Math.max, humanBytes, Math.round | 이만큼 더 넣어도 되는가. @param needBytes 이번에 넣으려는 바이트(원본+파생본 추정 합). @param est 브라우저가 알려준 현황. 판정 규칙: · `quota`를 모르면 `unknown` + 진행 — 모르는 것을 문제로도 정상으로도 반올림하지 않는다. · 넣은 뒤가 95%를 넘으면 `blocked` … |
| `src/domain/media/quota.ts` | export | function | `estimateLocalBytes(fileSize: number): number` | Math.round | 사진 한 장이 로컬에서 차지할 바이트의 보수적 추정 . 새 사진은 클라우드 검증 전까지 blob 셋(스테이징 원본·표시본·썸네일)을 들고 있다. 검증 뒤 원본은 회수되지만 인테이크의 순간 최대치를 견뎌야 하므로 사전점검은 넉넉히 잡는 쪽이 안전 하다 — 적게 잡아 통과시켰다가 커밋에서 터지면 그게 바로 막으려던 실패다. |
| `src/domain/media/windowsDrop.ts` | export | interface | `DroppedFileLike` | — | — |
| `src/domain/media/windowsDrop.ts` | internal | const | `PHOTO_EXT` | — | — |
| `src/domain/media/windowsDrop.ts` | export | function | `isWindowsPlatform(platform: string, userAgent: string): boolean` | /^win/i.test, platform.trim, /windows/i.test | Windows를 나타내는 platform 또는 UA 중 하나만 있어도 활성화한다. |
| `src/domain/media/windowsDrop.ts` | export | function | `isDroppedPhoto(file: DroppedFileLike): boolean` | file.type.toLowerCase().startsWith, file.type.toLowerCase, PHOTO_EXT.test | File Explorer가 MIME을 비워 넘기는 사진도 받는다. 파일 타입은 바이트의 증명이 아니므로, 최종 디코드·EXIF 판정은 기존 인테이크가 한다. |
| `src/domain/media/windowsDrop.ts` | export | function | `partitionDroppedPhotos(files: readonly T[]): { photos: T[]; rejected: T[]; }` | (isDroppedPhoto(file) ? photos : rejected).push, isDroppedPhoto | 다중 드롭 순서를 유지한 채 사진과 나머지를 가른다. |
| `src/domain/media/windowsDrop.ts` | export | typealias | `PhotoDropIntent` | — | — |
| `src/domain/media/windowsDrop.ts` | export | function | `photoDropIntent(photoCount: number, directlyOnMoment: boolean): PhotoDropIntent` | — | 한 장을 빈 영역에 놓는 경우만 새 순간 작성으로 분기한다. |
| `src/media/compress.ts` | export | interface | `CompressedImage` | — | — |
| `src/media/compress.ts` | export | const | `DISPLAY_MAX` | — | — |
| `src/media/compress.ts` | export | const | `THUMB_MAX` | — | — |
| `src/media/compress.ts` | internal | const | `DISPLAY_QUALITY` | — | — |
| `src/media/compress.ts` | internal | const | `THUMB_QUALITY` | — | — |
| `src/media/compress.ts` | internal | function | `scaledSize(w: number, h: number, maxEdge: number): { w: number; h: number }` | Math.max, Math.round | — |
| `src/media/compress.ts` | internal | async function | `toBitmap(source: Blob): Promise<ImageBitmap \\| HTMLImageElement>` | createImageBitmap, URL.createObjectURL, img.decode, URL.revokeObjectURL | — |
| `src/media/compress.ts` | internal | async function | `encode(bmp: ImageBitmap \\| HTMLImageElement, srcW: number, srcH: number, maxEdge: number, quality: number): Promise<CompressedImage>` | scaledSize, document.createElement, canvas.getContext, ctx.drawImage, canvas.toBlob, resolve | — |
| `src/media/compress.ts` | export | async function | `compressForStorage(original: Blob): Promise<{ display: CompressedImage; thumb: CompressedImage }>` | toBitmap, encode, bmp.close | 원본 Blob → { display(≤1600 WebP), thumb(≤320 WebP) }. 원본은 건드리지 않는다. |
| `src/media/exif.ts` | export | const | `EXIF_HEAD_BYTES` | — | EXIF를 읽을 때 파일 앞에서 얼마를 읽는가 — 저장 경로와 🔬 관측 창이 반드시 같은 값 을 쓴다. 🔴 왜 여기 한 곳인가(H-2 · 2026-08-01 감사): 예전엔 저장 경로가 256KB(`media.ts`), 🔬 창이 512KB(`tripDetail.ts`)를 손으로 따로 썼다. 그 사이에 EXIF가 앉… |
| `src/media/exif.ts` | export | interface | `ExifData` | — | — |
| `src/media/exif.ts` | internal | interface | `Entry` | — | — |
| `src/media/exif.ts` | internal | function | `readEntries(view: DataView, tiffStart: number, ifdOffset: number, little: boolean): Map<number, Entry>` | view.getUint16, view.getUint32, map.set | — |
| `src/media/exif.ts` | internal | function | `typeSize(type: number): number` | — | — |
| `src/media/exif.ts` | internal | function | `valuePtr(view: DataView, tiffStart: number, e: Entry, little: boolean): number` | typeSize, view.getUint32 | — |
| `src/media/exif.ts` | internal | function | `readAscii(view: DataView, tiffStart: number, e: Entry, little: boolean): string` | valuePtr, view.getUint8, String.fromCharCode | — |
| `src/media/exif.ts` | internal | function | `readDMS(view: DataView, tiffStart: number, e: Entry, little: boolean): number \\| undefined` | valuePtr, view.getUint32, rat | RATIONAL 3쌍(도/분/초)을 십진도로. |
| `src/media/exif.ts` | internal | function | `exifDateToWall(s: string): string \\| undefined` | /^(\d{4}):(\d{2}):(\d{2}) (\d{2}):(\d{2}):(\d{2})$/.exec, s.trim, Number.isNaN, Date.parse | EXIF 표기(`2026:07:29 19:08:00`) → 벽시계 (`2026-07-29T19:08:00`). 🔴 여기서 `new Date(...)`를 부르지 않는다. 그 한 줄이 M-0049였다 — 시간대 없는 문자열을 `Date`에 넣으면 기기 시간대 로 해석되고, 그 순간 절대시각이 조용히 틀어진다. |
| `src/media/exif.ts` | export | function | `parseExifOffset(s: string): number \\| undefined` | /^([+-])(\d{2}):(\d{2})$/.exec, s.trim, Number, Number.isFinite | `+09:00`·`-05:30`·`+00:00` → 분. 형식이 아니면 `undefined`(0으로 반올림하지 않는다). |
| `src/media/exif.ts` | internal | function | `parseTiff(view: DataView, tiffStart: number): ExifData` | view.getUint16, view.getUint32, readEntries, e0.get, exifIfd.get, exifDateToWall, readAscii, parseExifOffset, gps.get, readDMS | — |
| `src/media/exif.ts` | export | function | `readJpegExif(buf: ArrayBuffer): ExifData` | view.getUint16, view.getUint32, parseTiff | JPEG ArrayBuffer에서 EXIF(촬영시각·GPS) 추출. JPEG 아님/EXIF 없음/파싱 실패 → {}. |
| `src/media/exif.ts` | export | interface | `ExifProbe` | — | 이 바이트에서 관측한 사실. 판정이 아니다. |
| `src/media/exif.ts` | internal | function | `rawRationals(view: DataView, tiffStart: number, e: Entry, little: boolean): string` | valuePtr, Math.min, parts.push, view.getUint32, parts.join | RATIONAL 3쌍을 `n/d,n/d,n/d`로 — 가공하지 않고 적힌 그대로 . |
| `src/media/exif.ts` | export | function | `probeJpeg(buf: ArrayBuffer): ExifProbe` | view.getUint16, view.getUint32, out.app1.push, readEntries, e0.get, exifIfd.get, exifDateToWall, readAscii, gps.get, rawRationals | — |
| `src/services/media.ts` | internal | function | `uuid(): string` | crypto.randomUUID | — |
| `src/services/media.ts` | internal | function | `mediaOp(operationId: string, entityId: string, operationType: SyncQueueItem['operationType'], createdAt: string): SyncQueueItem` | — | 'media' 동기화 op 하나(moments/expenses와 동일 형태). |
| `src/services/media.ts` | export | interface | `AddPhotoTarget` | — | — |
| `src/services/media.ts` | export | interface | `PhotoMeta` | — | 사진 1장을 순간에 추가. EXIF 우선 추출 → 압축본·썸네일 생성 → 로컬 내구성 커밋 + read-back. 원본(file)은 클라우드 표시본 검증 전까지 originalBlob으로 스테이징한다. editedBlob이 있으면(비파괴 편집 결과) 압축본·썸네일은 그것에서 파생하되, EXIF(촬영시각·GPS)는 항상 … |
| `src/services/media.ts` | internal | async function | `readStorageEstimate(): Promise<StorageEstimateLike>` | navigator.storage.estimate | 브라우저 저장 현황(미지원이면 null — 추측하지 않는다). |
| `src/services/media.ts` | export | function | `resolveTakenAt(exifTakenAt: string \\| null, momentOccurredAt: string \\| null \\| undefined, fileLastModified: number): string` | new Date(fileLastModified \\|\\| Date.now()).toISOString, Date.now | 촬영시각을 무엇으로 정하는가 — 순수 함수라 유닛이 모든 갈래를 직접 돌린다(§10 ③). 이 값은 그냥 메타데이터가 아니라 R2 파일 이름의 앞부분 이 된다 (`20260717_0536_제주여행__…`). 틀리면 사용자가 저장소를 열었을 때 사진이 엉뚱한 날짜로 정렬되고, 이름을 사람이 읽게 만든 목적 자체가 무너진다… |
| `src/services/media.ts` | export | async function | `readPhotoMeta(file: File, fallbackZone: string): Promise<PhotoMeta>` | readJpegExif, file.slice(0, EXIF_HEAD_BYTES).arrayBuffer, file.slice, zoneOffsetAtWall, deviceZone, wallClockToInstant | 사진 한 장의 촬영 메타를 읽는다. 🔴 `fallbackOffsetMin`을 기본값 없이 받는다 (M-0049). EXIF `DateTimeOriginal`은 시간대가 없는 벽시계라 누군가는 오프셋을 정해야 하고, 그걸 아는 것은 여행을 아는 호출부다. 기본값을 두면 "안 넘겨도 컴파일되는" 길이 생기고, 그 길은 예… |
| `src/services/media.ts` | export | async function | `addPhotoToMoment(file: File, target: AddPhotoTarget, editedBlob?: Blob, editState?: EditState): Promise<LocalMedia>` | readStorageEstimate, quotaVerdict, estimateLocalBytes, db().localTrips.get, db, readPhotoMeta, db().localMoments.get, resolveTakenAt, compressForStorage, new Date().toISOString | — |
| `src/services/media.ts` | export | async function | `softDeleteMediaLocalFirst(id: string): Promise<void>` | db, d.localMedia.get, new Date().toISOString, uuid, d.transaction, d.localMedia.put, d.syncQueue.add, mediaOp | 사진 1장 삭제 — 하드 삭제 금지(§0): deletedAt tombstone만 세팅한다. 표시본·썸네일과 아직 남은 스테이징 원본은 그대로 두므로 되살리기가 완전 복원한다. version+1로 LWW 기준. |
| `src/services/media.ts` | export | async function | `reorderMomentPhotos(momentId: string, orderedIds: readonly string[]): Promise<void>` | db, new Date().toISOString, renumber, d.localMedia.where('momentId').equals(momentId).toArray, d.localMedia.where('momentId').equals, d.localMedia.where, target.get, changed.push, uuid, d.transaction | 🔴 한 순간의 사진 순서를 굳힌다 (사용자 지시 2026-08-06 · 마이그레이션 0029). 받은 순서대로 0부터 번호를 다시 매긴다 — 그 순간의 사진 전부에 . 옮긴 하나만 매기면 「일부는 내 순서, 일부는 촬영시각」이 되어 사용자가 규칙을 예측할 수 없다(`order.ts` 참조). ── 이 함수가 지키는 계… |
| `src/services/media.ts` | export | async function | `restoreMediaLocalFirst(id: string): Promise<void>` | db, d.localMedia.get, new Date().toISOString, uuid, d.transaction, d.localMedia.put, d.syncQueue.add, mediaOp | 사진 되살리기(실행취소) — deletedAt=null 복원. version+1로 삭제를 이긴다(LWW). |
| `src/services/media.ts` | export | async function | `reeditMediaLocalFirst(id: string, editedBlob: Blob, editState?: EditState): Promise<LocalMedia>` | db, d.localMedia.get, compressForStorage, new Date().toISOString, uuid, d.transaction, d.localMedia.put, d.syncQueue.add, mediaOp | 저장된 사진 재편집 — 사용자 파일과 저장된 EXIF(촬영시각·GPS)는 건드리지 않고, 현재 클라우드 정본(표시본) 또는 아직 남은 스테이징 원본에서 새 표시본·썸네일을 만든다. 스테이징 원본이 정리된 뒤에는 현재 표시본 기준 재편집이므로 이전 editState는 이어 쓰지 않는다. |
| `src/services/media.ts` | internal | async function | `rotate90Blob(source: Blob): Promise<Blob>` | createImageBitmap, URL.createObjectURL, img.decode, URL.revokeObjectURL, document.createElement, canvas.getContext, ctx.translate, ctx.rotate, ctx.drawImage, (bmp as ImageBitmap).close | 현재 표시본을 90°(시계방향) 회전한 새 Blob(PNG·무손실 중간본). 원본은 건드리지 않는다. |
| `src/services/media.ts` | export | async function | `rotateMediaLocalFirst(id: string): Promise<LocalMedia>` | db, d.localMedia.get, rotate90Blob, compressForStorage, new Date().toISOString, uuid, d.transaction, d.localMedia.put, d.syncQueue.add, mediaOp | 저장된 사진을 90°(시계방향) 회전 — 비파괴. 보이는 표시본을 그대로 돌려 눕힌 사진을 세운다. 사용자 파일·EXIF는 그대로(§0). 현재 표시본·썸네일만 다시 만들고 version+1(LWW). |
| `src/services/media.ts` | export | async function | `listMediaByTrip(tripId: string): Promise<LocalMedia[]>` | db().localMedia.where('tripId').equals(tripId).toArray, db().localMedia.where('tripId').equals, db().localMedia.where, db, orderPhotos, rows.filter | 여행의 활성 사진(순간별 그룹용). tombstone 제외. 🔴 정렬을 여기서 건다 (2026-08-06). 예전엔 정렬이 아예 없어 Dexie가 준 순서 (무작위 uuid 순)가 그대로 화면에 나갔고, 기기마다 다를 수도 있었다. 규칙은 `domain/media/order.ts` 한 곳에만 있다 — 화면이 각자 정렬… |
| `src/services/r2.ts` | internal | const | `FN` | — | — |
| `src/services/r2.ts` | internal | const | `UUID_RE` | — | — |
| `src/services/r2.ts` | internal | const | `TRANSIENT_ATTEMPTS` | — | — |
| `src/services/r2.ts` | internal | const | `SIGN_TIMEOUT_MS` | — | — |
| `src/services/r2.ts` | internal | const | `DOWNLOAD_TIMEOUT_MS` | — | — |
| `src/services/r2.ts` | internal | function | `wait(ms: number): Promise<void>` | setTimeout | — |
| `src/services/r2.ts` | export | function | `isTransientFunctionError(error: unknown): boolean` | — | 모바일 브라우저의 순간 전송 단절만 짧게 재시도한다. 인증·입력 오류는 그대로 돌려준다. |
| `src/services/r2.ts` | internal | function | `functionErrorDetail(error: unknown): string` | String | — |
| `src/services/r2.ts` | internal | function | `retryDelayMs(failedAttempt: number): number` | Math.max | — |
| `src/services/r2.ts` | export | interface | `BlobStore` | — | 바이트 저장소 포트 — MediaRemote의 바이트 3종과 같은 모양. |
| `src/services/r2.ts` | export | function | `mediaIdFromPath(path: string): string \\| null` | (path.split('/').pop() ?? '').replace, path.split('/').pop, path.split, base.split('__').pop, base.split, /^[0-9a-f]{32}$/i.test, tail.toLowerCase, h.slice, UUID_RE.test | 저장 경로에서 mediaId를 뽑는다. 두 형식을 다 읽는다: · 새 형식 `…/제주여행__c9ff5188/20260716_1432_제주여행__<32자>.webp` (2026-07-27~) · 옛 형식 `…/<uuid>.webp` ⚠️ Edge Function의 `mediaIdOfKey`와 같은 규칙이어야 한다. 둘은 … |
| `src/services/r2.ts` | export | function | `restOfPath(path: string): string \\| null` | path.split('/').filter, path.split, parts.slice(1).join, parts.slice | 전체 경로에서 사용자 폴더를 뗀 접미 를 돌려준다 — 함수에 보낼 값이다. 왜 떼나: 첫 칸은 함수가 검증된 sub로 직접 붙인다. 클라이언트가 보낸 값을 쓰지 않으므로 남의 폴더를 가리키는 것이 원리적으로 불가능하다. 그 경계를 지키려면 여기서 떼어 보내야 한다. |
| `src/services/r2.ts` | internal | interface | `SignResult` | — | — |
| `src/services/r2.ts` | internal | async function | `callSign(client: JourneyClient, op: 'put' \\| 'get' \\| 'delete' \\| 'probe' \\| 'list' \\| 'deleteMany' \\| 'abortMu…, mediaId: string \\| null, extra?: Record<string, unknown>): Promise<{ data: SignResult \\| null; error?: string }>` | client.functions.invoke, functionErrorDetail, isTransientFunctionError, wait, retryDelayMs | — |
| `src/services/r2.ts` | export | interface | `R2Capabilities` | — | — |
| `src/services/r2.ts` | export | async function | `r2Capabilities(client: JourneyClient): Promise<R2Capabilities>` | callSign, Array.isArray, d.ops.every, /^[0-9a-f]{64}$/.test | 배포된 함수가 밝힌 실제 계약. 사용자 자료를 읽거나 쓰지 않는 read-only probe다. |
| `src/services/r2.ts` | internal | function | `isRetryableDownloadStatus(status: number): boolean` | — | — |
| `src/services/r2.ts` | internal | async function | `downloadSignedUrl(url: string): Promise<{ data: Blob \\| null; error?: string; status?: number }>` | setTimeout, controller.abort, fetch, isRetryableDownloadStatus, res.body?.cancel, res.blob, String, clearTimeout, wait, retryDelayMs | — |
| `src/services/r2.ts` | export | function | `explainR2Error(code: string): string` | — | 함수 응답의 원인 코드를 사람이 읽을 수 있게. 진단표(앱 내 가이드)와 같은 어휘를 쓴다. |
| `src/services/r2.ts` | export | async function | `r2Probe(client: JourneyClient): Promise<{ ok: boolean; detail: string }>` | callSign, explainR2Error, (d.missing ?? []).join | 함수·시크릿까지 닿는지 확인(검증 사다리 2번). R2로의 실제 업로드는 증명하지 못한다. |
| `src/services/r2.ts` | export | interface | `R2Listing` | — | 서버에 실제로 있는 사진 파일 목록(내 폴더만). 왜 필요한가(사용자 지적 2026-07-26): 지금까지 "R2에 고아 파일이 남았나"를 확인하는 유일한 방법이 사용자가 Cloudflare 콘솔을 열어 화면을 캡처해 주는 것 이었다. 그건 §8("진단 도구는 관측이 아니라 판정을 한다")에 어긋난다 — 앱이 스스로 말… |
| `src/services/r2.ts` | export | async function | `r2ListObjects(client: JourneyClient): Promise<R2Listing>` | callSign, explainR2Error, Array.isArray, (d.ids as unknown[]).filter, (d.audioIds as unknown[]).filter, (d.videoIds as unknown[]).filter, (d.videoPosterIds as unknown[]).filter | — |
| `src/services/r2.ts` | export | async function | `r2DeleteMany(client: JourneyClient, mediaIds: string[]): Promise<{ requested: number; sent: number; stillThere: string[]; verified: boolean; error?: string …` | callSign, explainR2Error, Array.isArray, (d.stillThere as unknown[]).filter | 여러 장을 한 번에 지우고, 함수가 되읽어 확인한 결과를 받는다. 왜(M-0029): 건당 왕복이면 100장에 100번이고 매번 JWT를 다시 검증한다. 더 중요한 건 "성공 응답"을 완료로 치지 않는 것이다 — 함수가 지운 뒤 목록을 다시 읽어 `stillThere`를 준다. |
| `src/services/r2.ts` | export | async function | `r2AbortMultipart(client: JourneyClient): Promise<{ aborted: number; error?: string }>` | callSign, explainR2Error | 내 폴더 아래 미완료 멀티파트 조각 을 중단한다(보이지 않으면서 용량을 먹던 것). |
| `src/services/r2.ts` | export | function | `r2BlobStore(client: JourneyClient): BlobStore` | restOfPath, callSign, explainR2Error, signed, fetch, upload, downloadSignedUrl | — |
| `src/services/storage.ts` | export | interface | `StorageUsage` | — | — |
| `src/services/storage.ts` | export | function | `formatBytes(n: number): string` | (n / 1024).toFixed, (n / (1024 * 1024)).toFixed, (n / (1024 * 1024 * 1024)).toFixed | 바이트를 사람이 읽는 단위로. |
| `src/services/storage.ts` | export | async function | `computeStorageUsage(): Promise<StorageUsage>` | db, Promise.all, d.localTrips.toArray, d.localMoments.toArray, d.localExpenses.toArray, d.localMedia.toArray, d.localAudio.toArray, d.localVideos.toArray, d.syncQueue.toArray, media.map | — |
| `src/ui/mediaSave.ts` | export | interface | `ViewerDownload` | — | — |
| `src/ui/mediaSave.ts` | export | function | `createMediaSaveControl(kind: MediaSaveKind, currentFile: () => ViewerDownload): { button: HTMLButtonElement; status: HTMLElement }` | el, button.setAttribute, status.setAttribute, button.addEventListener, event.stopPropagation, (async () => { try { const file = currentFile(); const rece…, currentFile, saveFileBlob, mediaSaveMessage, mediaSaveErrorMessage | 사진·영상 뷰어가 같은 저장 상태 전이와 사용자 문장을 지나게 하는 공용 컨트롤. |
| `src/ui/photoViewer.ts` | internal | typealias | `Pts` | — | 화면에 있는 두 포인터 사이 거리 — 핀치 확대의 기준. |
| `src/ui/photoViewer.ts` | internal | function | `pinchDist(pts: Pts): number` | pts.values, Math.hypot | 🔴 래칫이 밀어준 추출 (2026-07-30). `clock`을 받으며 이 함수가 한 줄 길어졌고 `check-fn-size`가 막았다. 주석을 줄이는 대신 순수한 것을 밖으로 냈다 — 두 손가락 계산은 DOM이 필요 없으므로 최상위에 있어야 하고, 그래야 유닛이 닿는다(게이트의 원래 목적). |
| `src/ui/photoViewer.ts` | internal | function | `pinchMid(pts: Pts): { x: number; y: number }` | pts.values | 두 포인터의 중점 — 확대의 중심(손가락 사이가 화면에 고정돼 보이게). |
| `src/ui/photoViewer.ts` | internal | function | `buildPhotoSave(current: () => LocalMedia, tripTitle: string, editButton: HTMLButtonElement): { actions: HTMLElement; status: HTMLElement }` | createMediaSaveControl, current, photoDownloadFilename, el, actions.addEventListener, event.stopPropagation, actions.append | — |
| `src/ui/photoViewer.ts` | internal | function | `buildCloseButton(close: () => void): HTMLButtonElement` | el, button.setAttribute, button.addEventListener, event.stopPropagation, close | — |
| `src/ui/photoViewer.ts` | export | function | `openPhotoViewer(list: LocalMedia[], startIndex: number, onChanged: () => Promise<void>, clock: TripClock, tripTitle: string): void` | Math.max, Math.min, URL.createObjectURL, el, overlay.setAttribute, img.addEventListener, e.stopPropagation, overlay.remove, URL.revokeObjectURL, document.removeEventListener | 사진을 전체화면으로 연다. @param onChanged 회전·재편집으로 사진이 바뀌면 호출(뒤 목록 썸네일 갱신). @param clock 이 사진들이 속한 여행의 시계. 편집기 제목의 촬영시각을 그 자리 기준 으로 적는다 — 타임라인이 19:08이라 말한 사진을 열었더니 제목이 21:08이면 사용자는 다른 사진을 열… |
| `src/ui/pickOriginal.ts` | export | const | `ORIGINAL_ACCEPT` | — | 사진 선택기가 처리할 수 없는 형식을 하나 섞어 일반 파일 선택기 로 내려보낸다. 이것이 사진 입력칸의 기본값이다 (2026-08-01) — 원본 바이트가 그대로 와야 위치가 산다. |
| `src/ui/pickOriginal.ts` | export | const | `GALLERY_ACCEPT` | — | 갤러리(사진 선택기)를 여는 `accept`. 위치가 지워질 수 있다 — 사용자가 편의를 위해 명시적으로 고를 때만 쓴다. 기본값으로 되돌리지 마라(그 순간 위치가 조용히 사라진다). |
| `src/ui/pickOriginal.ts` | export | function | `wireAltPick(input: HTMLInputElement, btn: HTMLButtonElement, accept: string): void` | btn.addEventListener, input.addEventListener, window.addEventListener, input.click | 입력칸의 `accept`를 한 번만 바꿔서 열고, 끝나면 되돌린다. 입력칸을 새로 만들지 않는다 — 하나를 두 방식으로 여는 것뿐이라, 고른 뒤의 처리 (미리보기·EXIF·압축·저장)는 기존 경로를 그대로 지난다. 두 번째 입력칸을 만들면 그 처리를 한 벌 더 손으로 배선하게 되고, 그 순간 한쪽이 낡는다(§7 2층).… |

## 5. 자동 검증 연결

- `tests/unit/audioNote.test.ts`
- `tests/unit/audioRowmap.test.ts`
- `tests/unit/audioSiblingDiscipline.test.ts`
- `tests/unit/contractGaps.test.ts`
- `tests/unit/exif.test.ts`
- `tests/unit/mediaDownload.test.ts`
- `tests/unit/mediaNaming.test.ts`
- `tests/unit/mediaOrder.test.ts`
- `tests/unit/mediaSign.test.ts`
- `tests/unit/photoMetaSniff.test.ts`
- `tests/unit/photoTakenAt.test.ts`
- `tests/unit/r2Retry.test.ts`
- `tests/unit/videoPolicy.test.ts`
- `tests/unit/videoServerSync.test.ts`
- `tests/unit/windowsDrop.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/services/media.ts` — 454줄
- `src/services/r2.ts` — 414줄
- `src/media/exif.ts` — 345줄
- `src/ui/photoViewer.ts` — 325줄
- `src/domain/media/naming.ts` — 241줄
- `src/domain/media/order.ts` — 162줄
- `src/services/storage.ts` — 106줄
- `src/domain/media/quota.ts` — 96줄
- `src/media/compress.ts` — 82줄
- `src/ui/pickOriginal.ts` — 76줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
