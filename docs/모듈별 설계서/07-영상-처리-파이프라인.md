---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 영상 처리 파이프라인 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

영상 파생본·포스터 생성, 저장·재생·다운로드와 서버 왕복을 소유한다. 독립 타임라인 편집기는 현재 코드에 없다.

- 모듈 ID: `video-pipeline`
- 관측 파일: 5개 · 논리 줄: 974 · 최상위 선언: 69개(외부 공개 35개)
- 코드 내용 SHA-256: `03d759cca699979ee790cad1cdfd0ad6252c5233e0a0fc9599e0625277b697d6`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/videoRoundTripVerdict.ts`

### 다른 모듈로 나가는 정적 의존

- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 4건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 4건
- 인증·Supabase 클라이언트·동의(`auth-security`): import/re-export 2건
- 오프라인 동기화와 정본 전환(`sync-canonical`): import/re-export 2건
- 휴지통·복원·영구삭제(`trash-purge`): import/re-export 1건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 1건
- 화면·오버레이·상호작용(`ui-shell`): import/re-export 1건

### 외부 패키지/런타임 의존

- Node 내장 또는 저장소 내부 모듈만 정적으로 import한다.

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/videoRoundTripVerdict.ts` | 64 | 9/9 | — | deletion lifecycle |
| `src/media/video.ts` | 200 | 13/17 | — | file/blob bytes; DOM interaction |
| `src/services/videoRoundTrip.ts` | 543 | 7/35 | src/offline/db.ts<br>src/services/auth.ts<br>src/services/supabase/client.ts<br>src/services/autoSync.ts<br>src/services/sync.ts<br>src/services/videos.ts<br>src/services/trips.ts<br>src/services/trash.ts<br>src/media/video.ts<br>src/domain/video/rowmap.ts<br>src/domain/media/naming.ts<br>src/domain/videoRoundTripVerdict.ts | Dexie: localMoments, localTrips, localVideos; Supabase table: purged_ids, sync_meta, trips, videos; file/blob bytes; integrity/crypto; deletion lifecycle; browser media worker/codec; DOM interaction; browser key/value storage |
| `src/services/videos.ts` | 119 | 5/7 | src/offline/db.ts<br>src/domain/media/quota.ts<br>src/media/video.ts | Dexie: localVideos; file/blob bytes; deletion lifecycle |
| `src/ui/videoViewer.ts` | 48 | 1/1 | src/offline/db.ts<br>src/domain/media/download.ts<br>src/ui/dom.ts<br>src/ui/mediaSave.ts | DOM interaction |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/videoRoundTripVerdict.ts` | export | const | `VIDEO_ROUND_TRIP_STEPS` | — | — |
| `src/domain/videoRoundTripVerdict.ts` | export | typealias | `VideoRoundTripStep` | — | — |
| `src/domain/videoRoundTripVerdict.ts` | export | typealias | `VideoRoundTripState` | — | — |
| `src/domain/videoRoundTripVerdict.ts` | export | interface | `VideoRoundTripStepResult` | — | — |
| `src/domain/videoRoundTripVerdict.ts` | export | interface | `VideoRoundTripRun` | — | — |
| `src/domain/videoRoundTripVerdict.ts` | export | function | `sameVideoRoundTripGeneration(saved: string \\| null \\| undefined, current: string \\| null \\| undefined): boolean` | — | 비어 있거나 달라진 세대에서는 진단 fixture 정리 쓰기를 재개하지 않는다. |
| `src/domain/videoRoundTripVerdict.ts` | export | function | `videoRoundTripR2PairAbsent(bodyStatus: number \\| undefined, posterStatus: number \\| undefined): boolean` | — | 영상 본체와 포스터가 모두 404일 때만 exact R2 cleanup으로 판정한다. |
| `src/domain/videoRoundTripVerdict.ts` | export | const | `VIDEO_ROUND_TRIP_LABEL: Record<VideoRoundTripStep, string>` | — | — |
| `src/domain/videoRoundTripVerdict.ts` | export | function | `videoRoundTripView(run: VideoRoundTripRun \\| null): { level: 'ok' \\| 'todo' \\| 'problem' \\| 'unknown'; headline: string; actual: string; expected: stri…` | run.steps.filter | — |
| `src/media/video.ts` | export | const | `VIDEO_ACCEPT` | — | — |
| `src/media/video.ts` | export | const | `MAX_VIDEO_SECONDS` | — | — |
| `src/media/video.ts` | export | const | `MAX_VIDEO_SOURCE_BYTES` | — | — |
| `src/media/video.ts` | export | const | `MAX_VIDEO_BYTES` | — | — |
| `src/media/video.ts` | export | const | `VIDEO_MAX_LONG_EDGE` | — | — |
| `src/media/video.ts` | export | const | `VIDEO_MAX_FRAME_RATE` | — | — |
| `src/media/video.ts` | export | interface | `VideoInfo` | — | — |
| `src/media/video.ts` | export | interface | `PreparedVideo` | — | — |
| `src/media/video.ts` | export | interface | `VideoProgress` | — | — |
| `src/media/video.ts` | export | function | `fitVideoSize(width: number, height: number, maxLongEdge = VIDEO_MAX_LONG_EDGE): { width: number; height: number }` | Number.isFinite, Math.min, Math.max, Math.round, even | — |
| `src/media/video.ts` | export | function | `validateVideoSource(file: Pick<File, 'size' \\| 'type'>): void` | file.type.startsWith | — |
| `src/media/video.ts` | internal | function | `validateVideoInfo(info: VideoInfo): void` | Number.isFinite, fitVideoSize | — |
| `src/media/video.ts` | internal | async function | `inputInfo(file: Blob): Promise<VideoInfo>` | import, input.getPrimaryVideoTrack, Promise.all, input.computeDuration, video.getDisplayWidth, video.getDisplayHeight, input.dispose | — |
| `src/media/video.ts` | internal | async function | `transcodeAttempt(file: Blob, info: VideoInfo, format: 'mp4' \\| 'webm', onProgress?: (progress: VideoProgress) => void): Promise<Blob \\| null>` | import, fitVideoSize, Conversion.init, onProgress, conversion.execute, /memory\\|allocation/i.test, input.dispose | — |
| `src/media/video.ts` | internal | function | `waitFor(target: EventTarget, event: string, timeoutMs = 15_000): Promise<void>` | window.setTimeout, target.removeEventListener, reject, window.clearTimeout, resolve, target.addEventListener | — |
| `src/media/video.ts` | export | async function | `createVideoPoster(blob: Blob): Promise<Blob>` | URL.createObjectURL, document.createElement, waitFor, Math.min, Math.max, fitVideoSize, canvas.getContext, ctx.drawImage, canvas.toBlob, video.pause | — |
| `src/media/video.ts` | export | async function | `prepareVideo(file: File, onProgress?: (progress: VideoProgress) => void): Promise<PreparedVideo>` | validateVideoSource, onProgress, inputInfo, validateVideoInfo, transcodeAttempt, createVideoPoster | — |
| `src/services/videoRoundTrip.ts` | internal | const | `LAST_KEY` | — | — |
| `src/services/videoRoundTrip.ts` | internal | const | `JOURNAL_KEY` | — | — |
| `src/services/videoRoundTrip.ts` | internal | const | `LOCK_NAME` | — | — |
| `src/services/videoRoundTrip.ts` | export | const | `VIDEO_ROUND_TRIP_PREFIX` | — | — |
| `src/services/videoRoundTrip.ts` | internal | interface | `Journal` | — | — |
| `src/services/videoRoundTrip.ts` | internal | function | `readJson(key: string): T \\| null` | localStorage.getItem, JSON.parse | — |
| `src/services/videoRoundTrip.ts` | internal | function | `writeJson(key: string, value: unknown): void` | localStorage.setItem, JSON.stringify | — |
| `src/services/videoRoundTrip.ts` | internal | function | `journalIds(journal: Journal): string[]` | — | — |
| `src/services/videoRoundTrip.ts` | export | function | `writeCrashJournal(storage: Pick<Storage, 'getItem' \\| 'setItem'>, key: string, value: unknown): void` | JSON.stringify, storage.setItem, String, storage.getItem | — |
| `src/services/videoRoundTrip.ts` | internal | function | `writeJournal(journal: Journal): void` | readJson, writeCrashJournal | — |
| `src/services/videoRoundTrip.ts` | internal | function | `removeJournal(runToken: string): void` | readJson, localStorage.removeItem, localStorage.getItem | — |
| `src/services/videoRoundTrip.ts` | export | interface | `CrossTabLockManager` | — | — |
| `src/services/videoRoundTrip.ts` | export | async function | `withVideoRoundTripLock(task: () => Promise<T>, locksOverride?: CrossTabLockManager \\| null): Promise<{ acquired: true; value: T } \\| { acquired: false }>` | locks.request, task | Web Locks가 없는 환경에서는 낙관적 localStorage 락으로 내려가지 않고 실패로 닫는다. journal은 crash 뒤 exact ID를 보존하고, Web Lock은 살아 있는 탭끼리 journal 소유권을 직렬화한다. |
| `src/services/videoRoundTrip.ts` | export | function | `lastVideoRoundTrip(): VideoRoundTripRun \\| null` | readJson, Array.isArray | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `sha256(blob: Blob): Promise<string>` | crypto.subtle.digest, blob.arrayBuffer, [...new Uint8Array(digest)].map((x) => x.toString(16).padSt…, [...new Uint8Array(digest)].map, x.toString(16).padStart, x.toString | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `canonicalVersion(): Promise<string>` | supabase, client.from('sync_meta').select('canonical_version').maybeS…, client.from('sync_meta').select, client.from | — |
| `src/services/videoRoundTrip.ts` | internal | class | `CanonicalChangedError` | — | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `assertGeneration(expected: string): Promise<void>` | canonicalVersion, sameVideoRoundTripGeneration | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `syncInGeneration(expected: string, reason: string): Promise<void>` | assertGeneration, syncOrThrow | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `syncOrThrow(reason: string): Promise<void>` | requestSync, syncStatus | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `syntheticVideo(): Promise<{ blob: Blob; posterBlob: Blob; width: number; height: number; durationSec: number }>` | document.createElement, canvas.getContext, canvas.captureStream, ['video/webm;codecs=vp8', 'video/webm'].find, MediaRecorder.isTypeSupported, stream.getTracks().forEach, stream.getTracks, track.stop, chunks.push, resolve | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `assertPlayable(blob: Blob): Promise<void>` | URL.createObjectURL, document.createElement, document.body.append, setTimeout, reject, clearTimeout, resolve, video.play, video.pause, video.removeAttribute | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `serverVideo(id: string): Promise<VideoRow \\| null>` | supabase, client.from('videos').select('*').eq('id', id).maybeSingle, client.from('videos').select('*').eq, client.from('videos').select, client.from | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `serverExists(table: 'trips' \\| 'moments' \\| 'videos', id: string): Promise<boolean>` | supabase, client.from(table).select('id').eq('id', id).maybeSingle, client.from(table).select('id').eq, client.from(table).select, client.from | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `serverPurged(id: string): Promise<boolean>` | supabase, client.from('purged_ids').select('id').eq('id', id).maybeSi…, client.from('purged_ids').select('id').eq, client.from('purged_ids').select, client.from | — |
| `src/services/videoRoundTrip.ts` | internal | function | `sameVideoMeta(actual: LocalVideo, expected: LocalVideo): boolean` | — | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `forcePurgeFixture(journal: Journal, domain: 'video' \\| 'trip', bytePath?: string \\| null): Promise<void>` | assertGeneration, db, crypto.randomUUID, new Date().toISOString, d.transaction, d.syncQueue.put, d.purgedIds.put, syncInGeneration | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `cleanupVideoRoundTripLeftoversLocked(): Promise<string[]>` | readJson, journalIds, supabase, db, assertGeneration, d.localVideos.get, videoPosterStoragePath, writeJournal, softDeleteVideo, syncInGeneration | Web Lock을 이미 가진 호출자만 쓴다. 저장된 exact journal 밖의 id는 절대 만지지 않는다. |
| `src/services/videoRoundTrip.ts` | export | async function | `cleanupVideoRoundTripLeftovers(): Promise<string[]>` | readJson, withVideoRoundTripLock, journalIds | 저장된 고유 journal만 대상으로 하는 멱등 정리. 일반 사용자 id나 제목 prefix 검색은 하지 않는다. |
| `src/services/videoRoundTrip.ts` | internal | function | `result(step: VideoRoundTripStep, state: VideoRoundTripStepResult['state'], ms: number \\| null, error: string \\| null, note: string \\| null = null): VideoRoundTripStepResult` | — | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `commitFixture(input: { ids: { tripId: string; momentId: string; videoId: string }; operationI…): Promise<LocalVideo>` | db, d.transaction, d.localTrips.add, d.localMoments.add, d.localVideos.add, d.syncQueue.bulkAdd, op, videoOp, Promise.all, d.localTrips.get | — |
| `src/services/videoRoundTrip.ts` | internal | typealias | `StepRunner` | — | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `runServerLifecycle(runStep: StepRunner, ctx: { generation: string; ids: { tripId: string; momentId: string; videoId: st…): Promise<void>` | runStep, syncInGeneration, assertGeneration, serverVideo, fromVideoRow, videoPosterStoragePath, writeJournal, videoRemote, supabase, Promise.all | — |
| `src/services/videoRoundTrip.ts` | internal | async function | `runVideoRoundTripLocked(): Promise<VideoRoundTripRun>` | cleanupVideoRoundTripLeftoversLocked, new Date().toISOString, VIDEO_ROUND_TRIP_STEPS.map, result, writeJson, currentUser, supabase, crypto.randomUUID, at.slice(0, 19).replace, at.slice | — |
| `src/services/videoRoundTrip.ts` | export | async function | `runVideoRoundTrip(): Promise<VideoRoundTripRun>` | withVideoRoundTripLock, readJson, new Date().toISOString, VIDEO_ROUND_TRIP_STEPS.map, result, journalIds, writeJson | — |
| `src/services/videos.ts` | internal | function const | `uuid()` | crypto.randomUUID | — |
| `src/services/videos.ts` | export | function | `videoOp(operationId: string, entityId: string, operationType: SyncQueueItem['operationType'], createdAt: string): SyncQueueItem` | — | — |
| `src/services/videos.ts` | internal | async function | `readStorageEstimate(): Promise<{ usage: number \\| null; quota: number \\| null }>` | navigator.storage.estimate | — |
| `src/services/videos.ts` | export | async function | `addVideoToMoment(target: { momentId: string; tripId: string }, file: File, onProgress?: (progress: VideoProgress) => void): Promise<LocalVideo>` | prepareVideo, quotaVerdict, readStorageEstimate, new Date().toISOString, new Date(file.lastModified).toISOString, uuid, db, d.transaction, d.localVideos.add, d.syncQueue.add | — |
| `src/services/videos.ts` | export | async function | `listVideosByTrip(tripId: string): Promise<LocalVideo[]>` | db().localVideos.where('tripId').equals(tripId).toArray, db().localVideos.where('tripId').equals, db().localVideos.where, db, rows.filter | — |
| `src/services/videos.ts` | export | async function | `softDeleteVideo(id: string): Promise<void>` | db, d.localVideos.get, new Date().toISOString, uuid, d.transaction, d.localVideos.put, d.syncQueue.add, videoOp | — |
| `src/services/videos.ts` | export | async function | `restoreVideo(id: string): Promise<void>` | db, d.localVideos.get, new Date().toISOString, uuid, d.transaction, d.localVideos.put, d.syncQueue.add, videoOp | — |
| `src/ui/videoViewer.ts` | export | function | `openVideoViewer(item: LocalVideo, tripTitle: string): void` | URL.createObjectURL, el, overlay.setAttribute, close.setAttribute, createMediaSaveControl, videoDownloadFilename, actions.append, video.pause, video.removeAttribute, video.load | — |

## 5. 자동 검증 연결

- `tests/unit/videoPolicy.test.ts`
- `tests/unit/videoRoundTripLock.test.ts`
- `tests/unit/videoRoundTripVerdict.test.ts`
- `tests/unit/videoServerSync.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/services/videoRoundTrip.ts` — 543줄
- `src/media/video.ts` — 200줄
- `src/services/videos.ts` — 119줄
- `src/domain/videoRoundTripVerdict.ts` — 64줄
- `src/ui/videoViewer.ts` — 48줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
