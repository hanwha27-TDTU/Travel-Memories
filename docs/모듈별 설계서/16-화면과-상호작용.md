---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 화면·오버레이·상호작용 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

공통 DOM, 화면·패널, 디자인 토큰, 토스트와 앱 전역 상호작용을 소유한다.

- 모듈 ID: `ui-shell`
- 관측 파일: 19개 · 논리 줄: 12015 · 최상위 선언: 295개(외부 공개 68개)
- 코드 내용 SHA-256: `cf0f1ee5a2d8c5e2c8ba6e578e942a1a8ec0afa06c6bec4311d5b153f52e4706`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/ui/screens/aboutApp.ts`
- `src/ui/screens/companionRecords.ts`
- `src/ui/screens/dataManager.ts`

### 다른 모듈로 나가는 정적 의존

- 앱 셸과 라우팅(`app-shell`): import/re-export 19건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 17건
- 지도·장소·지오코딩(`map-place`): import/re-export 16건
- 인증·Supabase 클라이언트·동의(`auth-security`): import/re-export 10건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 9건
- 오프라인 동기화와 정본 전환(`sync-canonical`): import/re-export 7건
- 비용·통화·환율(`expense-fx`): import/re-export 6건
- 진단·무결성·사용자 판정(`diagnostics`): import/re-export 5건
- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 5건
- 플랫폼 런타임·파일 저장·업데이트(`platform-runtime`): import/re-export 4건
- 영상 처리 파이프라인(`video-pipeline`): import/re-export 3건
- 오디오 노트(`audio-notes`): import/re-export 2건
- 백업·복원·ZIP 무결성(`backup-restore`): import/re-export 2건
- 휴지통·복원·영구삭제(`trash-purge`): import/re-export 2건
- 사진편집기(`photo-editor`): import/re-export 1건

### 외부 패키지/런타임 의존

- Node 내장 또는 저장소 내부 모듈만 정적으로 import한다.

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/persistAdvice.ts` | 171 | 9/11 | — | platform bridge |
| `src/services/storeState.ts` | 596 | 16/18 | src/offline/db.ts<br>src/services/purge.ts<br>src/app/deviceId.ts<br>src/services/supabase/client.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localPlaces, localTrips, localVideos; Supabase table: audio, media, purged_ids, videos; deletion lifecycle |
| `src/ui/dom.ts` | 175 | 8/8 | — | file/blob bytes; DOM interaction |
| `src/ui/dragReorder.ts` | 276 | 2/11 | src/domain/media/order.ts | DOM interaction |
| `src/ui/lazyScreens.ts` | 72 | 6/6 | src/ui/screens/mapView.ts<br>src/domain/place/externalMap.ts | — |
| `src/ui/screens/aboutApp.ts` | 240 | 1/5 | src/ui/dom.ts<br>src/app/changelog.ts<br>src/ui/screens/researchNote.ts<br>src/ui/screens/designOverview.ts<br>src/ui/screens/mechChecks.ts<br>src/app/registry.gen.ts<br>src/domain/gateControlView.ts | integrity/crypto; DOM interaction |
| `src/ui/screens/companionRecords.ts` | 220 | 4/6 | src/offline/db.ts<br>src/domain/moment/companions.ts<br>src/domain/time.ts<br>src/ui/dom.ts | Dexie: localMedia, localMoments, localTrips; deletion lifecycle; DOM interaction |
| `src/ui/screens/dataManager.ts` | 1016 | 1/22 | src/ui/dom.ts<br>src/offline/db.ts<br>src/ui/screens/diagnosticsHub.ts<br>src/ui/screens/guide.ts<br>src/ui/lazyScreens.ts<br>src/services/trash.ts<br>src/services/backup.ts<br>src/services/fileSave.ts<br>src/services/capacitorShell.ts<br>src/services/backupMeta.ts<br>src/services/trips.ts<br>src/services/autoSync.ts<br>src/services/sync.ts<br>src/services/storage.ts<br>src/services/fx.ts<br>src/ui/screens/r2Setup.ts<br>src/ui/screens/placeRegistry.ts<br>src/domain/expense/format.ts<br>src/services/canonicalSync.ts<br>src/services/auth.ts<br>src/services/supabase/client.ts<br>src/domain/authGate.ts<br>src/app/router.ts | file/blob bytes; deletion lifecycle; DOM interaction; platform bridge |
| `src/ui/screens/designOverview.ts` | 219 | 1/1 | src/ui/dom.ts<br>src/app/blueprint.ts | transactional persistence; deletion lifecycle; DOM interaction |
| `src/ui/screens/guide.ts` | 780 | 1/21 | src/ui/dom.ts<br>src/app/apk.ts<br>src/app/installers.ts<br>src/app/playbook.ts<br>src/services/capacitorShell.ts<br>src/app/selfEval.ts<br>src/ui/screens/diagnosticsHub.ts<br>src/app/registry.gen.ts<br>src/app/agents.ts<br>src/app/gates.ts<br>src/media/compress.ts<br>src/app/constitution.gen.ts<br>src/ui/screens/mechChecks.ts<br>src/app/platformMap.gen.ts | file/blob bytes; transactional persistence; deletion lifecycle; DOM interaction |
| `src/ui/screens/home.ts` | 1018 | 3/35 | src/services/supabase/client.ts<br>src/domain/authGate.ts<br>src/services/trips.ts<br>src/ui/toast.ts<br>src/services/auth.ts<br>src/services/autoSync.ts<br>src/domain/syncBadgeVerdict.ts<br>src/services/syncParity.ts<br>src/ui/dom.ts<br>src/ui/lazyScreens.ts<br>src/app/registry.gen.ts<br>src/ui/theme.ts<br>src/app/router.ts<br>src/offline/db.ts<br>src/domain/time.ts<br>src/services/moments.ts<br>src/services/places.ts<br>src/domain/trip/timeTree.ts<br>src/domain/trip/homeSections.ts | transactional persistence; deletion lifecycle; DOM interaction |
| `src/ui/screens/r2Setup.ts` | 398 | 1/12 | src/ui/dom.ts<br>src/services/supabase/client.ts<br>src/services/r2.ts | DOM interaction |
| `src/ui/screens/researchNote.ts` | 140 | 1/2 | src/ui/dom.ts<br>src/app/researchLog.ts<br>src/app/hashchain.ts | integrity/crypto; DOM interaction |
| `src/ui/screens/tripDetail.ts` | 3847 | 1/117 | src/ui/dom.ts<br>src/ui/externalMapRow.ts<br>src/ui/audioNote.ts<br>src/services/audio.ts<br>src/offline/db.ts<br>src/services/videos.ts<br>src/media/video.ts<br>src/ui/videoViewer.ts<br>src/ui/toast.ts<br>src/ui/dragReorder.ts<br>src/domain/media/order.ts<br>src/services/trips.ts<br>src/domain/moment/whenDefault.ts<br>src/services/media.ts<br>src/domain/place/photoHint.ts<br>src/domain/place/here.ts<br>src/services/here.ts<br>src/ui/pickOriginal.ts<br>src/services/nativePhotos.ts<br>src/services/consent.ts<br>src/services/moments.ts<br>src/services/expenses.ts<br>src/domain/expense/format.ts<br>src/domain/expense/fx.ts<br>src/services/fx.ts<br>src/domain/place/geojson.ts<br>src/domain/trip/homeSections.ts<br>src/domain/media/windowsDrop.ts<br>src/ui/lazyScreens.ts<br>src/ui/screens/mapView.ts<br>src/services/geocode.ts<br>src/domain/place/provider.ts<br>src/domain/place/coordInput.ts<br>src/services/places.ts<br>src/domain/place/registry.ts<br>src/services/supabase/client.ts<br>src/domain/place/precision.ts<br>src/ui/photoEditor.ts<br>src/ui/photoViewer.ts<br>src/domain/time.ts<br>src/services/homeZone.ts<br>src/domain/moment/timeline.ts<br>src/domain/moment/timelineOrder.ts<br>src/domain/moment/companions.ts<br>src/services/autoSync.ts<br>src/app/router.ts | file/blob bytes; integrity/crypto; transactional persistence; deletion lifecycle; DOM interaction; browser key/value storage |
| `src/ui/styles/app.css` | 2517 | 0/0 | — | deletion lifecycle |
| `src/ui/styles/fonts.css` | 46 | 0/0 | — | — |
| `src/ui/styles/tokens.css` | 114 | 0/0 | — | — |
| `src/ui/theme.ts` | 90 | 11/16 | — | DOM interaction; browser key/value storage |
| `src/ui/toast.ts` | 80 | 2/4 | src/ui/dom.ts | DOM interaction |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/persistAdvice.ts` | export | typealias | `PersistSurface` | — | 앱이 지금 어느 문으로 실행 중인가 — 「설치됨」의 세 갈래는 사용자가 할 일이 서로 다르다. |
| `src/domain/persistAdvice.ts` | export | interface | `PersistContext` | — | — |
| `src/domain/persistAdvice.ts` | export | function | `persistResultNote(ctx: PersistContext, granted: boolean): string` | refusalReason | 「저장소 보호」 요청 결과를 사용자 문장 하나로. `granted`면 표면과 무관하게 같은 말이다. |
| `src/domain/persistAdvice.ts` | internal | const | `BACKUP_FALLBACK` | — | 어떤 표면에서도 참인 마지막 행동 — 거절 문장이 막다른 곳에서 끝나지 않게 한다(§7-D). |
| `src/domain/persistAdvice.ts` | internal | function | `refusalReason(ctx: PersistContext): string` | — | 표면별 다음 행동. 원인이 아니라 해 볼 것 을 말한다. 크롬이 공개한 판단 신호가 여럿이므로 여럿을 말하고 더 흔한 것을 먼저 둔다(§8 · M-0056). |
| `src/domain/persistAdvice.ts` | export | function | `surfaceLabel(surface: PersistSurface): string` | — | 진단 지표로 보여줄 「지금 어디서 실행 중인가」. 왜 지표인가: 사용자가 "설치는 했는데 왜 안 되지" 를 물을 때, 앱이 자기가 무엇으로 보이는지 를 말해 주지 않으면 그 질문에 답할 방법이 없다(§12 — 앱이 아는 것을 말하지 않으면 사람이 나른다). |
| `src/domain/persistAdvice.ts` | export | function | `persistIsMeaningful(surface: PersistSurface): boolean` | — | 이 표면에서 「저장소 보호」가 의미 있는 질문인가 . 🔴 안드로이드 셸에서는 앱 자료가 브라우저의 공간 회수 대상이 아니라고 알려져 있지만, 이 앱은 그것을 재본 적이 없다. 그래서 「안전하다」고 말하지 않고 판정을 `unknown`으로 두되(§8 — 모르는 것을 정상으로 반올림하지 않는다), 사용자에게는 표면 이름과… |
| `src/domain/persistAdvice.ts` | export | function | `shouldAutoRequestPersist(ctx: PersistContext & { persisted: boolean \\| null; alreadyTried: boolean }): boolean` | — | 🔴 앱이 스스로 한 번 요청할 것인가 (§12 — 사람에게 대신 시키던 일을 앱이 한다). 지금까지는 사용자가 진단 화면을 찾아 들어가 버튼을 눌러야만 요청이 갔다. 그런데 크롬은 이 요청에 프롬프트를 띄우지 않는다 — 설치·사용 빈도 같은 신호로 조용히 판단한다. 즉 설치된 표면에서는 물어볼 것이 없고 , 사용자를 … |
| `src/domain/persistAdvice.ts` | export | interface | `StorageHeadlineInput` | — | 🔴 「저장소 안전」의 판정 한 문장 . `unknown`일 때 왜 모르는지 가 문장에 담겨야 한다(§7-E). 왜 이 함수가 생겼나(M-0113 · 사용자 태블릿 캡처): 예전 판은 `unknown`이면 무조건 「이 브라우저는 저장 용량을 알려주지 않아요」 라고 말했다. 그런데 v1.84에서 셸의 persist를 `u… |
| `src/domain/persistAdvice.ts` | export | function | `storageHeadline(i: StorageHeadlineInput): string` | persistIsMeaningful | — |
| `src/domain/persistAdvice.ts` | export | function | `persistAskNote(ask: { granted: boolean; auto: boolean; when: string } \\| null, surface: PersistSurface): string` | — | 🔴 「왜 아직 미적용인가」에 답하는 한 줄 (T-005 · 사용자 태블릿 2026-08-06). 화면이 「미적용 · 확인 불가」라고만 말하면 사용자는 "앱이 물어보긴 한 건가?" 에서 멈춘다. 그 상태로는 이 과제가 영원히 열려 있다. 앱은 물어본 사실을 알고 있으므로 말해야 한다(§12). 🔴 거절을 「영영 안 된… |
| `src/services/storeState.ts` | export | interface | `DeviceSeen` | — | — |
| `src/services/storeState.ts` | export | interface | `StoreComparison` | — | — |
| `src/services/storeState.ts` | export | interface | `MediaFileAudit` | — | 사진 파일 대조 — 서버 기록과 서버 파일이 짝이 맞는가(2026-07-26 신설). 두 방향은 성격이 완전히 다르다. 한 숫자로 합치면 사용자가 할 일이 정반대인 둘이 섞인다 (진단 §4의 "대기 중인 작업 3건"이 정확히 그 실수였다): · `orphans` — 파일은 있는데 기록이 없다. 잉여 파일이다. 기억은 안… |
| `src/services/storeState.ts` | export | interface | `Listing` | — | 한 저장소의 목록 응답. |
| `src/services/storeState.ts` | export | function | `unionListings(listings: Listing[]): Listing` | listings.find, listings.every, listings.flatMap, listings.reduce, listings.some, Math.min, listings.map | 여러 저장소의 목록을 합친다 (2026-07-26 — 저장소 혼재 상태 때문에 생겼다). R2 전환(07-25) 이전 사진의 바이트는 여전히 Supabase Storage에 있다. 한 곳만 보고 "파일이 없다"고 판정하면 멀쩡한 사진 여러 장을 문제로 단정하는 거짓 경보 가 된다. 한쪽이라도 못 읽으면 합집합 전체를 버… |
| `src/services/storeState.ts` | export | function | `auditMediaFiles(fileIds: string[], rowIds: string[], opts: { foreign?: number; truncated?: boolean } = {}): MediaFileAudit` | [...files].filter, rows.has, [...rows].filter, files.has | 대조 자체는 순수 함수 다 — 유닛이 네트워크 없이 모든 경계를 직접 돌린다. (동기화 결정 로직을 `sync/merge.ts`로 뽑아낸 것과 같은 이유 — LESSONS §6.) |
| `src/services/storeState.ts` | export | function | `auditPairedVideoFiles(videoIds: string[], posterIds: string[], rowIds: string[], opts: { truncated?: boolean } = {}): MediaFileAudit` | [...bodies].filter, posters.has, [...objects].filter, rows.has, [...rows].filter, complete.has | 영상은 본체와 포스터가 모두 있어야 한 파일 세트다. 어느 한쪽 누락도 복원 가능으로 세지 않는다. |
| `src/services/storeState.ts` | export | interface | `StoreStatePort` | — | 원격 포트 — 네트워크 격리(테스트에서 fake 주입). |
| `src/services/storeState.ts` | export | function | `storeStateRemote(client: JourneyClient): StoreStatePort` | client .from(DOMAIN_PURGE[d].remoteTable) .select('id', { c…, client .from(DOMAIN_PURGE[d].remoteTable) .select, client .from, client .from(DOMAIN_PURGE[d].remoteTable) .select('updated_…, rows.push, client.from('purged_ids').select, client.from, client.from(DOMAIN_PURGE[d].remoteTable).select('id').not, client.from(DOMAIN_PURGE[d].remoteTable).select, ids.push | — |
| `src/services/storeState.ts` | export | function | `foldDevices(rows: { stamp: string; at: string }[], thisId: string, thisLabel?: string): DeviceSeen[]` | parseDeviceStamp, byId.get, byId.set, [...byId.values()].sort, byId.values, b.lastPushAt.localeCompare | 서버 스탬프 목록 → 기기별 최신 1건. 순수 함수 (유닛이 모든 경계를 직접 돌린다). `thisLabel`을 주면 이 기기 줄만 그 이름으로 덮어쓴다. 왜 필요한가(§9 4단계 — 옛 데이터를 누가 데려오나): 이름은 push할 때 서버 행에 찍힌 채로 남는다. 사용자가 방금 이름을 바꿔도 다음 저장 전까지 서버엔 … |
| `src/services/storeState.ts` | internal | typealias | `LocalRow` | — | 로컬 행 한 줄에서 이 파일이 보는 것 — id와 생사뿐이다. |
| `src/services/storeState.ts` | internal | async function | `allLocalRows(): Promise<Record<PurgeDomain, LocalRow[]>>` | db, Promise.all, d.localTrips.toArray, d.localMoments.toArray, d.localMedia.toArray, d.localExpenses.toArray, d.localAudio.toArray, d.localVideos.toArray, d.localPlaces.toArray | 로컬 5종 행을 한 번에 읽는다. 왜 모으나(§7 — 다음 형제가 자동으로 따라오게): 아래 세 지표가 각자 테이블을 손으로 열거하고 있었다. 도메인이 하나 늘면 세 곳을 다 고쳐야 하고, 한 곳을 빠뜨리면 그 도메인만 조용히 안 세지는 형태가 된다 — 이 저장소가 이미 세 번 겪은 그 모양이다. |
| `src/services/storeState.ts` | export | async function | `localActiveCounts(): Promise<Record<PurgeDomain, number>>` | allLocalRows, rows[d].filter | 로컬 활성 개수 — 서버와 같은 기준 (tombstone 제외)으로 센다. 기준이 다르면 대조가 거짓이 된다. |
| `src/services/storeState.ts` | export | async function | `localBytesIds(): Promise<Record<PurgeDomain, Set<string>>>` | db, d.localMedia.toArray, out.media.add, d.localAudio.toArray, out.audio.add, d.localVideos.toArray, out.video.add | 이 기기가 바이트를 들고 있는 id들 (사진·소리·영상). 진단이 「서버에 파일이 없다」를 판정할 때 그것이 기억 손실인지, 그냥 다시 올리면 되는 일인지 를 가르는 유일한 근거다. 🔴 왜 필요한가(2026-07-28, M-0046): 이 물음을 안 하고 「자료는 이미 없으니 기록 줄을 치우세요」라고 말했다. 로컬 휴… |
| `src/services/storeState.ts` | export | async function | `localTombstoneCount(): Promise<number>` | allLocalRows, PURGE_DOMAINS.reduce, rows[d].filter | 로컬 휴지통(tombstone) 개수. 왜 필요한가(2026-07-26 사용자 지적): 지금까지 대조는 활성만 했다. 그래서 자료가 전부 휴지통으로 옮겨가면 활성이 양쪽 0이 되어 화면이 「같습니다」라고 말했다 — 정작 13건이 어디 있는지는 어느 지표도 말하지 않았다. 지운 것도 자료다. 대조 대상이어야 한다. |
| `src/services/storeState.ts` | export | interface | `FilesPort` | — | 파일 목록 포트 — R2 어댑터를 그대로 받지 않고 필요한 모양만 받는다. storeState가 R2를 import하면 저장소 종류를 아는 게 되고, 그러면 Supabase Storage로 되돌릴 때 이 파일도 고쳐야 한다(되돌리기가 환경변수 하나여야 한다는 계약이 깨진다). |
| `src/services/storeState.ts` | export | async function | `compareStore(port: StoreStatePort, files?: FilesPort): Promise<StoreComparison>` | Promise.all, port.activeCounts, localActiveCounts, port.deviceStamps, port.remnantCounts, port.tombstonedIds, purgedIdSet, localTombstoneCount, port.purgedLedgerIds, pendingUnpurgeIds | — |
| `src/services/storeState.ts` | export | const | `DOMAIN_LABEL: Record<PurgeDomain, string>` | — | 사람이 읽는 도메인 이름 — 화면이 손으로 다시 적지 않게 여기 한 곳. |
| `src/ui/dom.ts` | export | interface | `TextRun` | — | ` 강조 ` 를 `<strong>` 으로 바꿔 넣는다. innerHTML을 쓰지 않는다 — 조각을 잘라 textContent로만 채우므로 사용자 입력이 섞여도 마크업이 될 수 없다. 왜 el() 안에 있는가(실제 결함 M-0012, 2026-07-26 사용자 화면에서 발견): 화면 문자열 곳곳에 ` … ` 가 쓰여 있었… |
| `src/ui/dom.ts` | export | function | `parseEmphasis(text: string): TextRun[]` | text.includes, text.split, runs.push, push | 문자열을 강조 조각으로 자른다 — 순수 함수 (DOM 없음). 왜 분리했나: 유닛 검사 환경에 DOM이 없어서(jsdom 미설치) 렌더 함수를 직접 못 돌린다. 규칙 자체를 순수 함수로 빼면 모든 경계를 유닛이 실제로 실행 할 수 있고, 진짜 `<strong>`이 그려지는지는 라이브 렌더가 본다. 두 층이 각자 볼 수 … |
| `src/ui/dom.ts` | export | function | `applyText(node: HTMLElement, text: string): void` | parseEmphasis, runs.some, node.replaceChildren, document.createElement, node.appendChild, document.createTextNode | — |
| `src/ui/dom.ts` | export | function | `el(tag: K, className?: string, text?: string): HTMLElementTagNameMap[K]` | document.createElement, applyText | — |
| `src/ui/dom.ts` | export | typealias | `NoteState` | — | 상태 줄(.sync-note)의 시각 위계 — 평소엔 조용히, 문제일 땐 눈에 띄게. ok = 정상(동기화됨·저장됨). 배경 없이 작고 옅게 — 화면을 거의 안 쓴다. info = 알아둘 것(대기 N건·로컬 모드·로그인 안내). 옅은 색 알약. error = 실패. 눈에 띄는 색 — 놓치면 안 되는 것만 여기 온다. 두… |
| `src/ui/dom.ts` | export | interface | `NoteAction` | — | 상태 줄을 누를 수 있게 만드는 목적지. 왜(사용자 제안 2026-07-26): 첫 화면의 「동기화 대기 13건」이 말만 하고 갈 곳을 주지 않았다. 사용자는 [데이터 관리] → [진단 도구] → [동기화 상태]를 스스로 찾아 들어가야 했다. 진단 §7-B와 같은 자리다 — 판정만 하고 행동을 못 주면 관측으로 되돌아간… |
| `src/ui/dom.ts` | export | function | `setNote(node: HTMLElement, text: string, state: NoteState, go: NoteAction \\| null, progressPercent: number \\| null = null, progressLabel = '동기화 진행률'): void` | applyText, node.classList.toggle, Number.isFinite, Math.max, Math.min, Math.round, el, meter.setAttribute, String, meter.append | 상태 줄을 갱신한다. `go`는 선택이 아니라 필수 다(`null`을 명시해야 한다). 왜: 갈 곳이 있는데 안 준 상태가 이 저장소의 최빈 결함군이고(§7), 선택적 매개변수는 누락을 조용히 삼킨다 — M-0007이 정확히 그 형태였다. `null`을 적게 만들면 "여긴 갈 곳이 없다"가 결정 이 되고, 옆에 이유를 … |
| `src/ui/dom.ts` | export | function | `downloadBlob(blob: Blob, filename: string): void` | URL.createObjectURL, el, document.body.appendChild, a.click, a.remove, setTimeout, URL.revokeObjectURL | 만든 Blob을 파일로 내려받는다(임시 `<a download>` 클릭). 백업 내보내기(데이터 관리·진단 도구 양쪽)가 같은 절차를 쓴다 — 손으로 두 벌 두면 한쪽만 URL을 못 revoke하는 결함이 조용히 갈라진다(§7 SSOT). |
| `src/ui/dragReorder.ts` | internal | const | `HOLD_MS` | — | 꾹 누르기로 인정할 시간(ms). 짧으면 스크롤이 드래그로 오인되고, 길면 「반응이 없다」가 된다. |
| `src/ui/dragReorder.ts` | internal | const | `SLOP_PX` | — | 누르는 동안 이만큼 넘게 움직이면 스크롤 의도 로 보고 길게 누르기를 취소한다(px). |
| `src/ui/dragReorder.ts` | internal | const | `LIFT_SCALE` | — | 들린 칸을 살짝 키운다 — 「이건 지금 내 손에 들려 있다」를 한눈에. 🔴 CSS가 아니라 여기서 정하는 이유: 들린 칸의 `transform`은 손가락 위치 를 담아야 하고, 인라인 `transform`은 클래스의 것을 통째로 덮는다. 두 곳에서 같은 속성을 쓰면 하나가 조용히 사라진다 — 그래서 한 곳(여기)에서 … |
| `src/ui/dragReorder.ts` | internal | function | `prefersReducedMotion(): boolean` | window.matchMedia | 움직임을 줄여 달라고 한 사용자인가 — 「비켜서기」는 남기되 장식(확대)은 끈다 . |
| `src/ui/dragReorder.ts` | export | interface | `DragReorderOptions` | — | — |
| `src/ui/dragReorder.ts` | internal | function | `measure(container: HTMLElement, sel: string): DragBase` | Array.from, container.querySelectorAll, els.map, e.getBoundingClientRect | 지금 컨테이너 안의 칸들과 그 사각형(뷰포트 기준). 🔴 드래그가 시작될 때 한 번만 부른다. 예전 주석은 "매 이동마다 다시 잰다" 였는데 그 전제는 칸을 밀지 않던 시절의 것이다 — 지금은 `transform`으로 밀므로 다시 재면 밀린 자리 가 나와 판정이 자기 출력을 먹는다(화석 주석을 그대로 두면 다음 사람이… |
| `src/ui/dragReorder.ts` | internal | function | `blockPlatformClaims(container: HTMLElement, isDragging: () => boolean): () => void` | e.preventDefault, isDragging, container.addEventListener, window.addEventListener, container.removeEventListener, window.removeEventListener | 🔴 플랫폼이 이 제스처를 가져가는 길을 전부 막는다 — 한 곳에 모아 둔다. 이 저장소는 여기서 두 번 넘어졌다(M-0115 · M-0116), 그리고 막는 이벤트가 서로 달랐다 : · `dragstart` — `<img>` 위에서 누르고 움직이면 크롬이 네이티브 이미지 드래그 를 시작하며 포인터를 가로채 `point… |
| `src/ui/dragReorder.ts` | internal | function | `postDragClickBlock(container: HTMLElement): { arm: (x: number, y: number) => void; off: () => void }` | performance.now, Math.hypot, e.preventDefault, e.stopImmediatePropagation, container.addEventListener, container.removeEventListener | 재배열 직후 같은 놓기 좌표에 브라우저가 합성하는 click 한 번만 캡처 단계에서 소비한다. |
| `src/ui/dragReorder.ts` | internal | interface | `DragBase` | — | 🔴 드래그가 시작될 때 한 번 잰 자리. 이 묶음이 드래그 내내 유일한 좌표계 다. 다시 재면 안 되는 이유: 아래에서 칸들을 `transform`으로 미는데, `getBoundingClientRect`는 밀린 뒤의 자리를 준다. 그걸 다시 판정에 넣으면 자기 출력이 자기 입력이 되어(피드백) 칸이 떨리고, 「몇 번째… |
| `src/ui/dragReorder.ts` | internal | function | `applyShift(base: DragBase, from: number, to: number): void` | shiftOffsets, base.els.forEach | 드래그 중 다른 칸들이 비켜선다 — 사용자 요청 2026-08-06( "이동하는 느낌이 나게" ). 🔴 예전엔 `style.order`로 밀었다가 좌표계가 갈라졌다 : `order`는 화면 순서 를 바꾸는데 그때 다시 잰 배열은 DOM 순서 라, 한 번 밀고 나면 「몇 번째 칸 위인가」와 「몇 번째로 옮기는가」가 서로… |
| `src/ui/dragReorder.ts` | export | function | `attachDragReorder(opts: DragReorderOptions): () => void` | postDragClickBlock, clearTimeout, applyShift, measure, clearHold, activeEl.classList.remove, container.classList.remove, (e.target as HTMLElement).closest, container.contains, measure(container, itemSelector).els.indexOf | 컨테이너에 「꾹 눌러 끌기」를 붙인다. 붙인 것을 떼는 함수를 돌려준다(다시 그릴 때 호출). 흐름: `pointerdown` → 420ms 버팀 → 드래그 시작 → `pointermove`로 미리보기 → `pointerup` 확정. 그 사이에 손가락이 10px 넘게 움직이면 스크롤로 보고 취소 한다 — 목록을 넘기려던… |
| `src/ui/lazyScreens.ts` | export | async function | `openDiagnosticsHub(toolId?: string): Promise<void>` | import, m.openDiagnosticsHub | 진단 도구 허브. `toolId`를 주면 해당 도구를 펼친 채로 연다. |
| `src/ui/lazyScreens.ts` | export | async function | `openDataManager(opts: { onChanged: () => void; goToTrip: (tripId: string) => void }): Promise<void>` | import, m.openDataManager | '데이터 관리' 허브(백업·복원·휴지통·저장소 설정). |
| `src/ui/lazyScreens.ts` | export | async function | `openAboutApp(): Promise<void>` | import, m.openAboutApp | 개발자 정보(버전·변경 이력·연구노트·설계 개요도 진입점). |
| `src/ui/lazyScreens.ts` | export | async function | `openMapView(tripTitle: string, points: MapPoint[], focusPlace?: PlaceLike): Promise<void>` | import, m.openMapView | 여행 지도 보기. MapLibre 자체는 mapView 안에서 한 번 더 지연 로드된다. |
| `src/ui/lazyScreens.ts` | export | async function | `openMapPicker(initial: { lat: number; lng: number } \\| null): Promise<{ lat: number; lng: number } \\| null>` | import, m.openMapPicker | 지도로 좌표 고르기. 사용자가 고른 좌표(또는 취소 시 null)를 돌려준다. 지연 로드 실패(오프라인 등)는 취소와 같게 다룬다 — 좌표를 못 고른 것이지 오류가 아니다. |
| `src/ui/lazyScreens.ts` | export | async function | `openCompanionRecords(person: string, opts: { goToTrip: (tripId: string, target?: { momentId?: string; mediaId?: stri…): Promise<void>` | import, m.openCompanionRecords | 「이 사람과 함께한 기록」 — 동행인 칩을 누르면 열린다(사용자 지시 2026-08-15). 여행 상세에서 여는 보조 화면이므로 정적 import를 만들지 않는다 (§7 2층 · check-lazy-screens). |
| `src/ui/screens/aboutApp.ts` | internal | interface | `NormTier` | — | 문서 규범 체계(법령 체계) — 기존 문서 지도를 규범 위계로 재구성. 새 사실을 만들지 않는다. |
| `src/ui/screens/aboutApp.ts` | internal | const | `NORM_TIERS: NormTier[]` | — | — |
| `src/ui/screens/aboutApp.ts` | internal | function | `buildGateControlSection(bodyEl: HTMLElement): void` | bodyEl.appendChild, el, summarizeGateControl, gateControlHeadline, head.append, row.append, chips.appendChild, row.appendChild, list.appendChild | 게이트 대조군 현황 절 — top-level로 뽑았다(함수 크기 래칫 · §7 구조적 강제). 🔴 판정 렌더러를 쓰지 않는다. ✓/! 글리프가 붙으면 「지금 정상」으로 읽히는데, 앱은 게이트가 실제로 돌았는지 볼 수 없다(§8 · diagGroups의 ERRORS-GATE-HEALTH가 "앱이 이걸 판정하는 척하면 그… |
| `src/ui/screens/aboutApp.ts` | internal | function | `buildNormTiersSection(bodyEl: HTMLElement): void` | bodyEl.appendChild, el, head.append, docs.appendChild, row.append, tiers.appendChild | 문서 규범 체계 절 — top-level(함수 크기 래칫 · §7 구조적 강제). |
| `src/ui/screens/aboutApp.ts` | export | function | `openAboutApp(): void` | el, overlay.setAttribute, titleRow.append, titleWrap.append, closeBtn.setAttribute, header.append, modal.append, overlay.appendChild, avatar.setAttribute, pinfo.append | '개발자 정보' 모달을 연다. 현재 화면은 유지하고 위에 오버레이로 뜬다. |
| `src/ui/screens/companionRecords.ts` | export | interface | `CompanionRecordsOptions` | — | — |
| `src/ui/screens/companionRecords.ts` | internal | interface | `TripBucket` | — | — |
| `src/ui/screens/companionRecords.ts` | export | function | `companionBuckets(moments: readonly LocalMoment[], trips: readonly LocalTrip[], person: string): TripBucket[]` | trips.map, momentHasCompanion, byTrip.get, arr.push, byTrip.set, [...byTrip.entries()] .map(([tripId, list]) => ({ tripId, t…, [...byTrip.entries()] .map, byTrip.entries, titleById.get, list.slice().sort | 여행별로 묶는다. 순수 함수 — 화면을 모른다(§10 ③, 유닛으로 잰다). |
| `src/ui/screens/companionRecords.ts` | export | function | `similarNameNote(person: string, allNames: readonly string[]): string \\| null` | person.trim, allNames.filter, n.trim, o.startsWith, target.startsWith, others.join | 표기가 흔들린 이름이 같은 사람일 수 있다 는 사실을 사용자에게 알릴 문장. 🔴 단정하지 않는다 (§8 · M-0056): 「같은 사람입니다」가 아니라 「같은 사람이라면」 이다. 앱은 그걸 모른다 — 아는 것은 비슷한 이름이 따로 적혀 있다 는 사실뿐이다. |
| `src/ui/screens/companionRecords.ts` | internal | function | `momentCard(moment: LocalMoment, tripId: string, mediaByMoment: Map<string, LocalMedia[]> \\| null, urls: string[], dismiss: () => void, opts: CompanionRecordsOptions): HTMLElement` | el, mediaByMoment?.get, photo.setAttribute, URL.createObjectURL, urls.push, photo.appendChild, photo.addEventListener, dismiss, opts.goToTrip, photos.appendChild | 순간 한 장 — 사진 줄 + 제목. 🔴 `openCompanionRecords`에서 뽑았다 : 길이 래칫 (`check-fn-size`)이 걸렸고, 우회하는 대신 덜어냈더니 창의 뼈대와 카드 그리기가 갈렸다. 게이트가 설계를 밀어준 자리다(§11 마지막 문단). 🔴 `mediaByMoment`가 `null`이면 「0… |
| `src/ui/screens/companionRecords.ts` | export | async function | `openCompanionRecords(person: string, opts: CompanionRecordsOptions): Promise<void>` | el, overlay.setAttribute, close.setAttribute, titleWrap.append, header.append, modal.append, overlay.appendChild, document.body.appendChild, URL.revokeObjectURL, release | — |
| `src/ui/screens/dataManager.ts` | internal | interface | `DataManagerOpts` | — | — |
| `src/ui/screens/dataManager.ts` | internal | function | `fmtBytes(n: number): string` | (n / 1024).toFixed, (n / 1024 / 1024).toFixed | — |
| `src/ui/screens/dataManager.ts` | internal | typealias | `DataManagerIcon` | — | — |
| `src/ui/screens/dataManager.ts` | internal | const | `DATA_MANAGER_ICON_PATHS: Record<DataManagerIcon, readonly string[]>` | — | 데이터 관리 화면의 장식 아이콘 SSOT — 플랫폼마다 달라지는 컬러 이모지를 쓰지 않는다. |
| `src/ui/screens/dataManager.ts` | internal | function | `dataManagerIcon(name: DataManagerIcon, className: string): SVGSVGElement` | document.createElementNS, svg.classList.add, svg.setAttribute, path.setAttribute, svg.appendChild | — |
| `src/ui/screens/dataManager.ts` | internal | function | `buildUsageCard(): HTMLElement` | el, title.append, dataManagerIcon, document.createTextNode, card.append, rows.appendChild, card.appendChild, name.append, left.appendChild, r.append | 저장 용량 요약 카드 — 사진(blob) vs 텍스트(기록)로 나눠 보여준다. 비동기로 채운다. |
| `src/ui/screens/dataManager.ts` | internal | function | `fmtDate(iso: string): string` | Number.isNaN, d.getTime, d.getFullYear, String(d.getMonth() + 1).padStart, String, d.getMonth, String(d.getDate()).padStart, d.getDate | — |
| `src/ui/screens/dataManager.ts` | internal | function | `backupPanel(): HTMLElement` | el, box.append, status.setAttribute, backupFreshness, getLastBackupAt, fresh.classList.toggle, renderFresh, (async () => { try { const { blob, stats } = await make(); …, make, save | — |
| `src/ui/screens/dataManager.ts` | internal | function | `restorePanel(onChanged: () => void): HTMLElement` | el, box.append, status.setAttribute, label.append, document.createTextNode, fileInput.addEventListener, (async () => { try { const androidMaxMemory = await android…, androidRuntimeMaxMemory, backupImportLimitBytes, shellState | — |
| `src/ui/screens/dataManager.ts` | internal | async function | `fetchTrashLists(): Promise<{ trips: LocalTrip[]; kids: TrashedChild[]; fromServer: boolean }>` | supabase, Promise.all, listDeletedTripsFromServer, listTrashedChildrenFromServer, listDeletedTrips, listTrashedChildren | 🔴 휴지통 목록의 원천(2026-08-05 · 사용자 결정): 온라인이면 서버가 정본 이다 — 로그인된 모든 기기가 항상 같은 것을 봐야 한다("절대절대절대 기기끼리 내용이 다르면 안 됩니다"). 서버 조회가 실패(오프라인 등)할 때만 이 기기의 로컬 기록으로 내려가고, 그 사실을 화면에 밝힌다 — "로컬 기준"과 "… |
| `src/ui/screens/dataManager.ts` | internal | interface | `TrashRowCtx` | — | 여행/자식 줄이 공유하는 동작 컨텍스트 — trashPanel의 클로저를 top-level 행 함수로 넘긴다. |
| `src/ui/screens/dataManager.ts` | internal | function | `tripRow(t: LocalTrip, ctx: TrashRowCtx): HTMLElement` | el, info.append, fmtDate, restore.addEventListener, (async () => { try { // 서버 조회로만 알던 여행이면(다른 기기가 지운 것) 먼저 이 기…, supabase, prepareTripForAction, restoreTripFromTrash, requestSync, ctx.onChanged | 여행 한 줄 — 자식 줄과 같은 자리·같은 어휘 (§7 사용자 대면 대칭). |
| `src/ui/screens/dataManager.ts` | internal | function | `childRow(c: TrashedChild, ctx: TrashRowCtx): HTMLElement` | el, info.append, fmtDate, restore.addEventListener, (async () => { try { // 서버 조회로만 알던 항목이면(다른 기기가 지운 것) 먼저 이 기…, supabase, prepareChildForAction, restoreTrashedChild, requestSync, ctx.onChanged | 자식 한 줄 — 여행 줄과 같은 자리·같은 어휘 (§7 사용자 대면 대칭). |
| `src/ui/screens/dataManager.ts` | internal | function | `buildBulkTrashControls(args: { purgeNote: HTMLElement; toSync: TrashRowCtx['toSync']; onChanged: () =>…): { el: HTMLElement; setEntries: (entries: TrashPurgeEntry[]) => void }` | el, actions.append, elBulk.append, begin.addEventListener, cancel.addEventListener, confirm.addEventListener, (async () => { let committed = false; try { const client = …, supabase, setNote, Promise.all | 휴지통 일괄 영구삭제의 2단계 확인·준비·단일 commit을 화면 본체에서 분리한다. |
| `src/ui/screens/dataManager.ts` | internal | function | `trashPanel(onChanged: () => void, closeHub: () => void): HTMLElement` | el, box.append, box.appendChild, purgeNote.setAttribute, sourceNote.setAttribute, closeHub, openDiagnosticsHub, buildBulkTrashControls, render, box.insertBefore | — |
| `src/ui/screens/dataManager.ts` | internal | interface | `HubCard` | — | — |
| `src/ui/screens/dataManager.ts` | internal | function | `currencyPanel(): HTMLElement` | el, wrap.append, sel.setAttribute, fxBase, currencyLabel, sel.appendChild, sel.addEventListener, setFxBase, row.append, wrap.appendChild | 환율 기준통화 설정 — 비용 옆에 "≈ 얼마"로 보여줄 통화를 고른다. |
| `src/ui/screens/dataManager.ts` | internal | function | `canonicalPanel(onChanged: () => void): HTMLElement` | el, wrap.append, status.setAttribute, begin.addEventListener, confirm.focus, cancel.addEventListener, begin.focus, confirm.addEventListener, (async () => { try { const client = supabase(); const user …, supabase | 일반 병합과 다른 명시적 위험 작업. 첫 클릭은 설명만 펼치고, 두 번째 버튼만 실제 게시를 한다. 브라우저 confirm을 쓰지 않아 경고 문장과 영향 범위가 화면에 계속 남게 한다. |
| `src/ui/screens/dataManager.ts` | internal | function | `cards(onChanged: () => void, goToTrip: (tripId: string, target?: TripNavigationTarget) => void, reopen: () => HTMLElement \\| void): HubCard[]` | h.detail, currencyPanel, h.close, openR2Setup, backupPanel, restorePanel, canonicalPanel, openDiagnosticsHub, placeRegistryPanel, goToTrip | — |
| `src/ui/screens/dataManager.ts` | internal | function | `buildHubShell(): { overlay: HTMLElement; bodyEl: HTMLElement; closeBtn: HTMLButtonElement; }` | el, overlay.setAttribute, titleWrap.append, closeBtn.setAttribute, header.append, modal.append, overlay.appendChild | 허브 오버레이의 껍데기(모달·제목·닫기·본문). top-level — 함수 크기 래칫(§7 구조적 강제). |
| `src/ui/screens/dataManager.ts` | internal | function | `buildHubButton(c: HubCard, signedIn: boolean, onOpen: (card: HubCard) => void): HTMLButtonElement` | el, btn.setAttribute, ic.appendChild, dataManagerIcon, mid.append, chev.setAttribute, btn.append, canRunLocalDataAction, isConfigured, btn.addEventListener | 허브 카드 하나를 버튼으로 만든다(top-level — 함수 크기 래칫, §7 구조적 강제). 🔴 잠금은 여는 순간 건다. 눌러서 화면이 열린 뒤 막으면 그 사이에 이미 보인다. 판정은 손으로 쓰지 않고 `domain/authGate.ts` 한 곳이 한다(§7 2층 — M-0102가 두 곳에 손으로 있어 한쪽이 우회로… |
| `src/ui/screens/dataManager.ts` | export | function | `openDataManager(opts: DataManagerOpts): void` | buildHubShell, stopAuth, overlay.remove, document.removeEventListener, document.contains, prevFocus.focus, close, bodyEl.appendChild, buildUsageCard, el | '데이터 관리' 허브를 연다. 현재 화면 위에 오버레이로 뜬다. |
| `src/ui/screens/designOverview.ts` | export | function | `openDesignOverview(): void` | el, overlay.setAttribute, titleWrap.append, closeBtn.setAttribute, header.append, modal.append, overlay.appendChild, body.appendChild, selfCheck, scoreTop.append | '설계 개요도' 모달을 연다. 현재 화면 유지하고 위에 오버레이. |
| `src/ui/screens/guide.ts` | internal | const | `AGENT_CATEGORY_ORDER: readonly AgentCategory[]` | — | 가이드에서 에이전트를 묶어 보여줄 순서(분류 자체는 agents.ts가 정본). |
| `src/ui/screens/guide.ts` | internal | function | `h(text: string): HTMLElement` | el | — |
| `src/ui/screens/guide.ts` | internal | function | `p(text: string): HTMLElement` | el | — |
| `src/ui/screens/guide.ts` | internal | function | `bullets(items: string[]): HTMLElement` | el, ul.appendChild | — |
| `src/ui/screens/guide.ts` | internal | function | `defs(rows: [string, string][]): HTMLElement` | el, row.append, box.appendChild | 좌:라벨 / 우:값 정의행 목록. |
| `src/ui/screens/guide.ts` | internal | function | `steps(items: [string, string][]): HTMLElement` | el, items.forEach, step.appendChild, String, body.append, box.appendChild | 번호 붙은 흐름 단계(설계개요도·검증 흐름용). |
| `src/ui/screens/guide.ts` | internal | function | `note(text: string): HTMLElement` | el | — |
| `src/ui/screens/guide.ts` | internal | function | `playbookDownloads(): HTMLElement` | el, URL.createObjectURL, playbookFilename, document.body.appendChild, a.click, a.remove, setTimeout, URL.revokeObjectURL, html.addEventListener, save | 📄 설치 가이드(플레이북) 내려받기 — 화면 가이드와 같은 SSOT (apk.ts·changelog)에서 실행 시점에 조립해 파일로 저장한다(app/playbook.ts). 버전은 앱 버전과 통일되고, 앱이 업데이트되면 이 문서도 자동으로 따라온다. HTML(예쁘게 읽힘)·Markdown(이식) 둘 다 준다. |
| `src/ui/screens/guide.ts` | internal | function | `panel(children: HTMLElement[]): HTMLElement` | el, wrap.appendChild | — |
| `src/ui/screens/guide.ts` | internal | function | `installerSection(guide: InstallerGuide, preferred: boolean): HTMLElement` | el, title.append, section.append, steps, guide.steps.map, h, bullets | — |
| `src/ui/screens/guide.ts` | internal | function | `renderIconPicker(): HTMLElement` | iconSwitcher, panel, h, p, el, t.setAttribute, String, sw.setIcon, mark, t.append | 🎨 앱 아이콘 선택 (ADR-0038). 셸(APK)에서만 실제로 바꿀 수 있다 — 웹/PWA는 설치 시 아이콘이 고정된다(§5: 안 되는 버튼을 보여주지 않는다 → 크롬에서는 「앱에서만」 안내만). 라벨·순서·키는 `APP_ICONS`(SSOT)에서, 전환은 `iconSwitcher()`(네이티브) 한 곳으로만. |
| `src/ui/screens/guide.ts` | internal | function | `platformTable(): HTMLElement` | el, byWhere.has, byWhere.set, order.push, byWhere.get(r.where)?.push, byWhere.get, head.append, group.appendChild, left.append, row.append | 무엇이 어디서 도는가 표 — `platformMap.gen.ts`에서 읽는다. 🔒 이 표의 내용을 여기에 손으로 적지 않는다 (사용자 요청 2026-07-26: "이 내용을 적을 때 손으로 적지 말고 기계화시켜서 작성하게 설계해주세요" ). 각 행은 `scripts/gen-platform-map.mjs`가 코드를 읽어… |
| `src/ui/screens/guide.ts` | internal | interface | `GuideCard` | — | — |
| `src/ui/screens/guide.ts` | internal | interface | `GuideGroup` | — | — |
| `src/ui/screens/guide.ts` | internal | const | `CONNECT_GROUP: GuideGroup` | — | — |
| `src/ui/screens/guide.ts` | internal | const | `DEV_GROUP: GuideGroup` | — | — |
| `src/ui/screens/guide.ts` | internal | const | `GROUPS: GuideGroup[]` | — | — |
| `src/ui/screens/guide.ts` | internal | function | `selfEvalPanel(): HTMLElement` | summarize, panel, el, score.append, String, meta.append, head.append, wrap.appendChild, gradeLegend, top.append | 자기점검평가 — 가중치 × 점수 × 증거수준 으로 100점 척도를 만든다. 점수 자체가 목적이 아니다. 가중치가 큰데 점수·증거가 낮은 항목이 곧 다음에 할 일 이다. 조작 방지는 `check-self-eval` 게이트가 맡는다(가중치 합 100 · 증거별 점수 상한 · gap 필수 · 6하원칙 6필드 필수). 화면은 … |
| `src/ui/screens/guide.ts` | internal | function | `buildCardButton(card: GuideCard, onOpen: (card: GuideCard) => void): HTMLElement` | el, ic.setAttribute, mid.append, chev.setAttribute, btn.append, btn.addEventListener, onOpen | — |
| `src/ui/screens/guide.ts` | internal | function | `buildGroup(group: GuideGroup, onOpen: (card: GuideCard) => void): HTMLElement` | el, box.append, grid.appendChild, buildCardButton, box.appendChild | — |
| `src/ui/screens/guide.ts` | export | function | `openGuide(): void` | el, overlay.setAttribute, titleWrap.append, closeBtn.setAttribute, header.append, modal.append, overlay.appendChild, grid.appendChild, buildGroup, bodyEl.appendChild | '가이드' 모달을 연다. 현재 화면은 유지하고 위에 오버레이로 뜬다. |
| `src/ui/screens/home.ts` | internal | typealias | `Navigate` | — | — |
| `src/ui/screens/home.ts` | internal | const | `unsubscribeAuth: (() => void) \\| null` | — | — |
| `src/ui/screens/home.ts` | internal | const | `unsubscribeSync: (() => void) \\| null` | — | 동기화 상태·대조 구독 — 홈을 다시 그릴 때 겹치지 않게 모듈에 들고 있다가 해지한다. |
| `src/ui/screens/home.ts` | internal | const | `unsubscribeParity: (() => void) \\| null` | — | — |
| `src/ui/screens/home.ts` | internal | const | `unsubscribeParityChange: (() => void) \\| null` | — | — |
| `src/ui/screens/home.ts` | internal | function | `tripCardShell(t: LocalTrip, navigate: Navigate): HTMLElement` | el, card.setAttribute, navigate, card.addEventListener, e.preventDefault, go | 카드 껍데기 — 활성·보관함 두 카드가 이 한 곳 을 쓴다(§7 구조적 강제). 🔴 예전엔 두 함수가 각자 `cover--${index % 3}`·베일·노이즈를 손으로 붙였다. 그래서 「목록 카드에서 커버를 걷어낸다」는 결정이 두 곳을 다 고쳐야만 지켜졌다 — 한쪽만 고치면 보관함만 조용히 옛 모양으로 남는다. 껍데기… |
| `src/ui/screens/home.ts` | internal | function | `tripCardInfo(t: LocalTrip): HTMLElement` | el, info.appendChild, formatTripDuration, formatTripPeriod | 제목·날짜 — 두 카드가 같은 자리에 같은 어휘 로 말한다(§7 사용자 대면 대칭). 🔴 상태 배지가 여기 있었는데 뺐다 (사용자 결정 2026-08-06). 실측하니 여행 9장 중 7장에 똑같은 「완료」가 붙어 있었다 — 거의 모두에 붙는 표시는 정보가 아니라 소음 이다 (§8). 뜻 없는 색을 걷어낸 2026-08… |
| `src/ui/screens/home.ts` | internal | function | `tripCard(t: LocalTrip, navigate: Navigate, onDelete: (t: LocalTrip) => void, onEdit: (t: LocalTrip) => void): HTMLElement` | tripCardShell, el, del.setAttribute, del.addEventListener, e.stopPropagation, onDelete, edit.setAttribute, edit.addEventListener, onEdit, actions.append | — |
| `src/ui/screens/home.ts` | internal | function | `archivedCard(t: LocalTrip, navigate: Navigate, onRestore: (id: string) => void): HTMLElement` | tripCardShell, el, restore.setAttribute, restore.addEventListener, e.stopPropagation, onRestore, card.append, tripCardInfo | 보관함 카드 — 카드는 div(role=button)로 만들어 내부에 '복원' 버튼을 중첩 허용. |
| `src/ui/screens/home.ts` | internal | function | `buildControls(): HTMLElement` | el, seg.setAttribute, currentSeason, b.setAttribute, String, b.addEventListener, setSeason, btn.setAttribute, buttons.set, seg.appendChild | 계절 세그먼트 + 라이트/다크 토글 컨트롤. |
| `src/ui/screens/home.ts` | internal | function | `buildHeader(authArea: HTMLElement, syncStatus: HTMLElement, onData: () => void, onHome: () => void): HTMLElement` | el, homeBtn.setAttribute, homeBtn.addEventListener, titleRow.appendChild, verBadge.setAttribute, verBadge.addEventListener, openAboutApp, titleRow.append, headTools.append, headTop.append | 헤더 조립 — 제목 줄(+계정) / 컨트롤 줄. 왜 함수로 뺐나: `renderHome`이 길이 래칫에 걸렸다(`check-fn-size`). 래칫은 한 방향이라 "늘린 만큼 다른 데로 덜어내라"고 말한다 — 그 요구가 이 추출을 만들었고, 결과적으로 헤더 구조가 한눈에 읽힌다. 게이트가 설계를 밀어준 사례다. `aut… |
| `src/ui/screens/home.ts` | internal | async function | `trySync(): Promise<void>` | requestSync | 동기화 요청 — 규칙은 `services/autoSync.ts` 한 곳 에 있다(§7). 로그인 여부·온라인 여부를 여기서 다시 묻지 않으므로 인자를 받지 않는다. 예전엔 쓰지도 않는 `_user`를 받아 호출부마다 넘기고 있었다 — 안 쓰는 인자는 "이게 판단에 쓰인다"는 거짓 신호 라 다음 사람이 그 전제 위에 코드… |
| `src/ui/screens/home.ts` | internal | function | `tripActions(deps: { refresh: () => Promise<void>; fail: (msg: string) => void }): { deleteTrip: (t: LocalTrip) => void; restoreTrip: (id: string) => void; }` | deps.fail, String, window.confirm, (async () => { try { const children = await softDeleteTripL…, softDeleteTripLocalFirst, trySync, deps.refresh, showUndoToast, restoreTripLocalFirst, say | 카드의 삭제·복원 — `renderHome`에서 분리(길이 래칫 `check-fn-size`, §11). 두 행동이 한 곳에 있는 이유: 둘 다 되돌릴 수 있어야 하고 (비타협 원칙 #5) 끝나면 다시 읽어야 한다 (§8 — 고쳤다고 말하지 말고 재판정). 그 규율을 형제가 각자 구현하면 한쪽만 조용히 빠진다 — 실제로… |
| `src/ui/screens/home.ts` | internal | function | `treeBtn(cls: string, label: string, count: number, pressed: boolean, onClick: () => void): HTMLButtonElement` | el, b.setAttribute, String, b.appendChild, b.addEventListener | 기간 트리 버튼 한 줄 — 라벨 + 개수. 눌림 상태는 aria-pressed(색만으로 인코딩하지 않는다). |
| `src/ui/screens/home.ts` | internal | function | `renderPeriodTree(nav: HTMLElement, tree: TimeTree, sel: PeriodSel, onSelect: (next: PeriodSel) => void): void` | nav.appendChild, treeBtn, onSelect, monthLabel, Boolean | 기간 트리 렌더 — 여행 데이터에서 파생(SSOT), 여행 있는 연·월만 그린다(빈 달은 침묵). |
| `src/ui/screens/home.ts` | internal | function | `emptyListState(sel: TripStatus \\| null, onClear: () => void): HTMLElement` | el, empty.appendChild, back.addEventListener | 목록이 아예 빌 때의 빈 상태. `sel`이 null이면 요약 화면(= 여행이 하나도 없음), 아니면 그 상태만 비어 있다는 뜻이다. 🔴 막다른 문장으로 끝내지 않는다 (§13): 상태 칩을 눌러 들어왔다면 되돌아갈 길을 준다. |
| `src/ui/screens/home.ts` | export | function | `signedOutLockState(): HTMLElement` | el, empty.appendChild | 로그아웃(또는 아직 로그인 전) 상태에서 다른 계정의 이 기기 로컬 기록을 가리는 화면. 🔴 클라우드 모드(isConfigured())에서는 로그인 여부가 "이 계정의 여행을 볼 자격"의 경계다 (사용자 요청 2026-08-05: "로그아웃하면 사진 데이터가 보이지 않아야 하는데"). 가리는 것과 지우는 것은 다르다 … |
| `src/ui/screens/home.ts` | export | function | `applySignedOutLock(locked: boolean, ui: { viewBar: HTMLElement; fold: HTMLDetailsElement; form: HTMLFormElement; fi…): boolean` | ui.clearPeriod, ui.list.appendChild, signedOutLockState | 로그아웃 잠금을 화면에 적용한다 — refresh()에서 분리(함수 크기 래칫, §7 구조적 강제). 잠갔으면 true를 돌려 refresh()가 나머지(목록 렌더)를 건너뛰게 한다. 무엇을 가리나: 목록·새 여행 폼·기간 트리· 상태 칩 줄 . Dexie는 손대지 않는다 — 다음 refresh(로그인 뒤)가 같은 데이… |
| `src/ui/screens/home.ts` | internal | async function | `gateAccess(u: SessionUser \\| null, status: HTMLElement): Promise<SessionUser \\| null>` | accessVerdict, probeAllowed, signOut | 초대제 잠금 게이트(ADR-0021): 허용 목록에 없다고 확인된 사용자만 자동 로그아웃 안내. top-level로 뽑음(함수 크기 래칫, §7 구조적 강제) — `status` 줄만 클로저 대신 인자로 받는다. 🔴 판정은 여기가 아니라 `accessVerdict()`가 한다 (§10 ③). 이 함수에 남은 일은 물어… |
| `src/ui/screens/home.ts` | internal | function | `renderStatusChips(bar: HTMLElement, chips: ReadonlyArray<{ status: TripStatus; label: string; count: number }>, sel: TripStatus \\| null, onSelect: (next: TripStatus \\| null) => void): void` | el, b.setAttribute, String, b.appendChild, b.addEventListener, onSelect, bar.appendChild | 상태 칩 줄 — 「계획 중·진행 중·완료·보관 중」(사용자 요청 2026-08-06). 예전 [📦 보관함] 토글이 하던 일을 이 줄이 그대로 받았다. 보관만 전용 버튼을 갖고 나머지 셋은 못 갖던 비대칭 이 이걸로 사라진다(§7 — 형제에게는 대칭을 기본값으로 준다). 눌림은 `aria-pressed`로 말한다(색만으… |
| `src/ui/screens/home.ts` | internal | function | `renderSection(sec: HomeSection<LocalTrip>, card: (t: LocalTrip) => HTMLElement, onMore: (status: TripStatus) => void): HTMLElement` | el, head.appendChild, String, box.appendChild, grid.appendChild, card, more.addEventListener, onMore | 요약 구획 하나 — 제목(+총 개수) · 카드 격자 · [모두 보기]. 카드 격자를 `.trip-list`로 감싸는 이유: 넓은 화면의 2·3열 규칙이 그 클래스 한 곳 에 있다(§7 구조적 강제). 구획마다 자기 격자를 쓰면 다음 구획이 조용히 갈라진다. |
| `src/ui/screens/home.ts` | internal | function | `renderListArea(list: HTMLElement, ctx: { /** 상태 필터까지만 적용한 대상(기간 필터 이전) — 「여행 자체가 없음」을 가른다. */ items: LocalTrip[];…): void` | list.appendChild, emptyListState, filteredEmptyState, homeSections, renderSection, el, grid.appendChild, ctx.card | 목록 자리를 그린다 — 빈 상태 / 요약 구획 / 평평한 목록 셋 중 하나 . `refresh()`에서 뽑았다: 길이 래칫(`check-fn-size`)이 막았고, 래칫은 한 방향이라 "늘린 만큼 덜어내라"고 말한다. 그 요구가 이 추출을 만들었고 결과적으로 세 갈래가 한눈에 읽힌다 — 게이트가 설계를 밀어준 사례다(§… |
| `src/ui/screens/home.ts` | internal | function | `filteredEmptyState(onReset: () => void): HTMLElement` | el, empty.appendChild, back.addEventListener | 기간 필터가 다 걸러낸 상태 — 막다른 문장으로 끝내지 않는다(§13): 되돌아갈 버튼을 준다. |
| `src/ui/screens/home.ts` | internal | function | `openTripEditor(initial: Pick<LocalTrip, 'title' \\| 'startDate' \\| 'endDate'> \\| null, onSave: (value: { title: string; startDate: string; endDate: string }) => Promi…): void` | el, overlay.setAttribute, modal.appendChild, title.setAttribute, start.setAttribute, end.setAttribute, actions.append, modal.append, overlay.appendChild, error.setAttribute | 새 여행·여행 편집이 공유하는 별도 창. |
| `src/ui/screens/home.ts` | internal | function | `buildTripForm(onCreate: (value: { title: string; startDate: string; endDate: string }) => Pro…, onSearch: () => void): HTMLFormElement` | el, input.setAttribute, input.addEventListener, onSearch, submit.addEventListener, openTripEditor, form.append | 새 여행 버튼 + 사진이 붙은 순간 제목 검색. |
| `src/ui/screens/home.ts` | internal | async function | `loadHomeSnapshot(query: string, reconcilePlaces: boolean): Promise<{ pending: number; all: LocalTrip[]; }>` | reconcileMomentPlaces, Promise.all, listTrips, listArchivedTrips, pendingSyncCount, searchPhotoTitles, photoHits.map, query.trim, allUnfiltered.filter, hitTrips.has | — |
| `src/ui/screens/home.ts` | internal | function | `editTripFromHome(t: LocalTrip, refresh: () => Promise<void>): void` | openTripEditor, updateTripLocalFirst, trySync, refresh | — |
| `src/ui/screens/home.ts` | internal | interface | `PeriodUi` | — | 기간 필터 UI 묶음 — 선택 상태를 갖고, refresh가 apply()로 목록을 거른다. |
| `src/ui/screens/home.ts` | internal | function | `buildPeriodUi(): PeriodUi` | el, nav.setAttribute, fold.append, window.matchMedia, wideMq.addEventListener, buildTimeTree, renderPeriodTree, onChange, periodLabel, sortTripsNewestFirst | — |
| `src/ui/screens/home.ts` | internal | function | `renderSyncNote(status: HTMLElement, pending: number, user: SessionUser \\| null): void` | syncEngineStatus, syncBadge, isConfigured, Boolean, lastParity, isParityRefreshing, badgeActionLabel, requestSync, openDiagnosticsHub, setNote | — |
| `src/ui/screens/home.ts` | internal | function | `wireBadgeWatch(repaint: () => void, refreshAfterSync: () => void): void` | onSyncStatus, repaint, refreshAfterSync, installParityWatch, onParityChange | 배지가 스스로 최신을 유지하게 배선한다 — renderHome에서 분리(함수 크기 래칫, §7). 🔴 동기화가 끝나면 배지를 다시 그린다. 안 그러면 「동기화 중…」이나 「확인 전」이 화면에 박힌 채 남는다 — 판정을 고쳐도 그리지 않으면 사용자에게는 안 고친 것이다(M-0022). 대조 갱신 자체는 `installP… |
| `src/ui/screens/home.ts` | internal | function | `clearHomeSubscriptions(): void` | unsubscribeAuth, unsubscribeSync, unsubscribeParity, unsubscribeParityChange | 화면을 다시 만들기 전 모든 모듈 전역 구독을 정확히 해지한다. |
| `src/ui/screens/home.ts` | internal | function | `renderHomeAuth(authArea: HTMLElement, user: SessionUser \\| null, status: HTMLElement): void` | isConfigured, authArea.appendChild, el, out.addEventListener, signOut, authArea.append, inBtn.addEventListener, signInWithGoogle().catch, signInWithGoogle, String | 로그인 상태만 그리는 작은 셸. 홈 목록 refresh의 비동기 소유권과 섞지 않는다. |
| `src/ui/screens/home.ts` | internal | async function | `applyHomeAuth(raw: SessionUser \\| null, authArea: HTMLElement, status: HTMLElement, refresh: () => Promise<void>): Promise<SessionUser \\| null>` | gateAccess, renderHomeAuth, refresh | 로그인 상태를 홈에 반영하는 한 가지 경로 . 🔴 왜 뽑았나: 구독(`onAuthChange`)과 첫 확인(`currentUser`)이 같은 세 단계 — 판정 → 로그인 영역 다시 그리기 → 목록 갱신 — 을 각자 적고 있었다. 한쪽만 고치면 갈라지고, 갈라진 쪽은 조용해진다(§7 · M-0060). 이제 순서가 한 … |
| `src/ui/screens/home.ts` | export | function | `renderHome(mount: HTMLElement, navigate: Navigate): () => void` | clearHomeSubscriptions, el, status.setAttribute, wrap.appendChild, buildHeader, openDataManager, refresh, navigate, buildPeriodUi, chipBar.setAttribute | 홈을 그리고 정리 함수를 돌려준다 (2026-08-13 · 외부 리뷰 지적). 🔴 왜 반환값이 생겼나: 예전엔 구독 해지가 `renderHome()`이 다시 불릴 때만 돌았다. 그래서 홈 → 여행 상세로 넘어가면 홈의 인증·동기화·대조 구독이 살아남아 떼어낸 DOM을 계속 참조하고 이벤트에 반응했다. 화면이 자기 생명… |
| `src/ui/screens/r2Setup.ts` | internal | typealias | `Tone` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | interface | `Note` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | interface | `Step` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | const | `TONE_LABEL: Record<Tone, string>` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | const | `PLAIN: [string, string][]` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | const | `STEPS: Step[]` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | const | `VALUES: [string, string][]` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | const | `TROUBLE: [string, string][]` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | const | `LADDER: [string, string, string][]` | — | — |
| `src/ui/screens/r2Setup.ts` | internal | const | `PROVEN: [string, string][]` | — | 실측 기록 — 이 절차를 따라 실제로 성공한 날의 증거(추정이 아니라 관측). 다음 사람(또는 6개월 뒤의 나)이 "이 문서가 실제로 통하는가"를 의심할 때 이 줄이 답이다. |
| `src/ui/screens/r2Setup.ts` | internal | function | `probePanel(): HTMLElement` | el, btn.setAttribute, btn.addEventListener, supabase, setNote, r2Probe(client) .then((r) => setNote(note, r.detail, r.ok ?…, r2Probe(client) .then, r2Probe, wrap.append | 검증 사다리 2번 — 앱의 로그인 경로로 함수·시크릿까지 닿는지 확인. 증명하지 못하는 것 : R2로의 실제 업로드(CORS). 그건 3번(사진 저장)에서만 드러난다. |
| `src/ui/screens/r2Setup.ts` | export | function | `openR2Setup(): void` | el, overlay.setAttribute, tw.append, closeBtn.setAttribute, header.append, modal.append, overlay.appendChild, body.appendChild, plainBox.appendChild, qa.append | — |
| `src/ui/screens/researchNote.ts` | internal | function const | `short(hex: string)` | hex.slice | — |
| `src/ui/screens/researchNote.ts` | export | function | `openResearchNote(): void` | el, overlay.setAttribute, titleWrap.append, closeBtn.setAttribute, header.append, modal.append, overlay.appendChild, bodyEl.appendChild, anchorRow.append, integrity.append | '연구노트' 모달을 연다. 현재 화면은 유지하고 위에 오버레이로 뜬다. |
| `src/ui/screens/tripDetail.ts` | internal | interface | `PlaceField` | — | 장소 입력 + 🔍 검색(Nominatim) + 결과 선택. 결과 텍스트는 textContent로만(외부 데이터·XSS 방지). |
| `src/ui/screens/tripDetail.ts` | internal | interface | `PickedBadge` | — | 선택 확인 배지 + 정밀도 안내 줄. `buildPlaceField`에서 떼어낸 이유는 래칫(`check-fn-size`)이지만, 떼고 보니 이게 맞다 — 「무엇을 골랐는지 사용자에게 말하는 일」은 「장소를 고르는 일」과 다른 관심사다(§11의 게이트가 설계를 밀어준다 ). `set(detail, precision)`이… |
| `src/ui/screens/tripDetail.ts` | internal | function | `pickedLocationText(detail: string, coords: { lat: number; lng: number } \\| null, address?: Pick<AddressParts, 'country' \\| 'region' \\| 'city'> \\| null): string` | coords.lat.toFixed, coords.lng.toFixed, [address?.country ?? '', city] .filter((part, index, parts)…, [address?.country ?? '', city] .filter, parts.indexOf, detail.includes | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildPickedBadge(onClear: () => void): PickedBadge` | el, badge.setAttribute, clearBtn.setAttribute, copyBtn.addEventListener, showNoticeToast, navigator.clipboard.writeText(copyValue).then, navigator.clipboard.writeText, badge.append, clearBtn.addEventListener, pickedLocationText | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `placeInputOf(field: PlaceField): { placeName: string; placeId: string \\| null; placeLat: number \\| null; placeLng: number \\| null; }` | field.getCoords, field.getName, field.getPlaceId | 장소 필드 → 순간 입력의 장소 네 칸 . 왜 한 곳에서 만드나(§7 2층): 이름·위도·경도·링크는 함께 움직여야 한다. 손으로 네 줄을 적는 자리가 둘(생성 폼·편집 폼)이면 언젠가 한쪽만 필드를 늘린다 — 그리고 링크만 빠지면 「저장은 됐는데 장소별 모아보기에 안 나오는」 조용한 결함이 된다. |
| `src/ui/screens/tripDetail.ts` | internal | interface | `CompanionField` | — | 동행인 입력 — 한 명씩 넣는다 (사용자 지시 2026-08-15). 🔴 왜 자유 텍스트를 버렸나 : 예전엔 `<input>` 하나에 「아버지, 어머니」를 통째로 적었다. 그러면 앱이 사람을 세지 못한다 — 「이 사람과 함께한 기록」도, 사람별 칩도 불가능하다. 문제는 저장 형태가 아니라 입력이 사람을 구분하지 않았다… |
| `src/ui/screens/tripDetail.ts` | internal | function | `companionField(initial = ''): CompanionField` | el, parseCompanions, input.setAttribute, chips.replaceChildren, x.setAttribute, x.addEventListener, removeCompanion, paint, chip.appendChild, chips.appendChild | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `wireFirstPress(button: HTMLButtonElement, activate: () => void): void` | button.addEventListener, performance.now, activate, event.preventDefault | 터치·펜은 첫 pointerup에서 바로 실행하고, 뒤따르는 합성 click은 한 번만 삼킨다. 마우스 click과 키보드 Enter/Space는 기존 click 경로를 그대로 쓴다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `wireMomentEditToggle(o: { button: HTMLButtonElement; form: HTMLElement; photoRow: HTMLElement; video…): void` | wireFirstPress, o.button.setAttribute, String, o.onClose, requestAnimationFrame, o.form.scrollIntoView, (o.form.querySelector('input') as HTMLInputElement \\| null)…, o.form.querySelector | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `initializeMomentEditToggle(button: HTMLButtonElement, form: HTMLElement, photoRow: HTMLElement, videoRow: HTMLElement, initiallyOpen: boolean, onClose: () => void): void` | button.setAttribute, String, wireMomentEditToggle | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `labeledField(label: string, field: HTMLElement): HTMLElement[]` | el | — |
| `src/ui/screens/tripDetail.ts` | internal | const | `LIVE_LOOKUP_MS` | — | 치는 동안 대장을 다시 읽기까지 기다리는 시간. 한글은 자모마다 `input` 이벤트가 뜨므로(ㅅ → 시) 그대로 두면 한 글자에 두세 번 읽는다. 사람이 「바로」로 느끼는 한계(~150ms)보다 짧게 두어 기다린다는 느낌은 주지 않는다. |
| `src/ui/screens/tripDetail.ts` | internal | interface | `ResultsOwner` | — | 🔴 결과 상자의 주인을 정하는 표식 — 이 상자에 그리는 손이 둘 이기 때문이다: 치는 동안의 대장 조회(비동기 · 120ms 뒤)와 [🔍 검색](비동기 · 네트워크). 둘 다 `await` 뒤에 그리므로 늦게 시작한 쪽이 먼저 끝날 수 있다. 그러면 사용자가 방금 [🔍 검색]으로 띄운 결과를 직전 타이핑의 답이 … |
| `src/ui/screens/tripDetail.ts` | internal | function | `placeResultsOwner(): ResultsOwner` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | interface | `PickedPlace` | — | `runPlaceSearch`가 고른 결과를 폼에 되돌려 주는 모양. 한 곳으로 모아 누락을 막는다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `savedPick(p: LocalPlace): PickedPlace` | verdictFromStored | 대장에 담긴 장소 → 폼에 앉힐 값. 🔴 한 곳에만 있어야 한다. 이 매핑이 두 벌이면(치는 동안 / [🔍 검색] 결과) 언젠가 한쪽만 `placeId`를 안 붙이고, 그러면 「저장은 됐는데 장소별 모아보기에 안 나오는」 조용한 결함이 된다 — 이 파일이 `placeInputOf`를 한 곳에 둔 이유와 같은 규율이다… |
| `src/ui/screens/tripDetail.ts` | internal | interface | `PlaceSearchContext` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | async function | `runPlaceSearch(q: string, ctx: PlaceSearchContext): Promise<void>` | supabase, providersOf, ensureProviders, sortRegistry, searchRegistry, listPlaces, searchPlaces, renderSavedPlaces, ctx.apply, savedPick | 장소 검색 한 번 — 「내 장소」를 먼저 그리고, 지오코더 결과를 그 아래 그린다. `buildPlaceField`에서 떼어낸 이유는 래칫(`check-fn-size`)이지만 떼고 보니 이게 맞다: 검색해서 고르는 일 과 폼의 상태를 들고 있는 일 은 다른 관심사다(§11 「게이트가 설계를 밀어준다」). 상태는 `app… |
| `src/ui/screens/tripDetail.ts` | internal | function | `wireMapPickButton(mapBtn: HTMLButtonElement, results: HTMLElement, ctx: CoordApplyContext, current: () => { lat: number; lng: number } \\| null): void` | mapBtn.addEventListener, openMapPicker(current()) .then((coords) => { if (!coords) r…, openMapPicker(current()) .then, openMapPicker, current, ctx.commit, ctx.input.value.trim, ctx.setPicked, fillNameFromReverse | 🗺 지도 버튼 — 지도에서 지점을 찍고, 이름이 비어 있으면 그 자리의 주소를 물어 채운다 . 예전엔 좌표만 남고 이름은 사용자가 전부 타이핑해야 했다. 앱이 알 수 있는 것을 사람에게 시키고 있던 자리다(§12 — "지금 이 앱은 사람에게 무엇을 대신 시키고 있는가?" ). |
| `src/ui/screens/tripDetail.ts` | internal | function | `photoFileInput(): HTMLInputElement` | el, wireNativeIntake, input.setAttribute | 사진 입력칸 공장 — 두 폼(생성·추가)이 같은 문을 지난다 (§7 2층). 여기 담긴 두 규율은 각각 나흘짜리였다: `ORIGINAL_ACCEPT`(M-0064 — 갤러리 선택기가 위치를 지운다) · `wireNativeIntake`(ADR-0036 — 셸 안에서는 ACCESS_MEDIA_LOCATION 문으로). 손… |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildAddPhotoRow(): { wrap: HTMLElement; input: HTMLInputElement; progress: HTMLElement }` | el, photoFileInput, label.append, document.createTextNode, progress.setAttribute, wrap.append, galleryPickButton | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `videoFileInput(): HTMLInputElement` | el, input.setAttribute | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `updateVideoPickerLabel(input: HTMLInputElement): void` | input.parentElement?.querySelector | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildVideoPicker(extraClass = ''): { input: HTMLInputElement; label: HTMLLabelElement }` | videoFileInput, el, `moment-photo-label moment-video-label form-utility moment-…, pickerFace, text.classList.add, text.setAttribute, label.append, input.addEventListener, updateVideoPickerLabel | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildAddVideoRow(): { wrap: HTMLElement; input: HTMLInputElement }` | el, buildVideoPicker, wrap.append | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `wireAddVideo(input: HTMLInputElement, progress: HTMLElement, target: { momentId: string; tripId: string; refresh: () => Promise<void> }): void` | input.addEventListener, Array.from, (async () => { try { for (const [index, file] of files.entr…, files.entries, addVideoToMoment, Math.round, updateVideoPickerLabel, target.refresh, trySync, String | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `pickerFace(host: HTMLElement, icon: string, text: string): HTMLSpanElement` | el, ic.setAttribute, host.append | 첨부 버튼 세 형제(📷 사진 · 🖼️ 갤러리 · 🎬 영상)의 한 벌 껍데기 . 🔴 왜 생겼나(2026-08-15 · 사용자 실기기 · M-0163): 셋이 각자 마크업을 갖고 있었다 — 사진은 텍스트 노드 하나(`'📷 사진 추가 '`), 갤러리는 `button`의 문자열, 영상만 아이콘·글자 span이었다. 그… |
| `src/ui/screens/tripDetail.ts` | internal | function | `galleryPickButton(input: HTMLInputElement): HTMLButtonElement` | el, pickerFace, btn.setAttribute, wireAltPick | 🖼️ 갤러리에서 버튼 — 안드로이드 사진 선택기(갤러리)로 고른다. 생성 폼과 「사진 추가」 두 곳이 같은 부품을 쓴다 (§7 2층). 라벨·설명·배선을 손으로 두 벌 만들면 한쪽만 고쳐지는 날이 오고, 이 저장소는 그 사고를 이미 세 번 겪었다. 🔴 셸(APK) 안에서는 네이티브 가로채기를 비켜서야 한다 (2026… |
| `src/ui/screens/tripDetail.ts` | internal | function | `wireHereButton(o: { btn: HTMLButtonElement; note: HTMLElement; ctx: CoordApplyContext; hasCoor…): void` | o.btn.addEventListener, o.hasCoord, window.confirm, readHere() .then((r) => { if (!r.coord) { // 실패해도 **말한다**. …, readHere() .then, readHere, hereFailMessage, o.ctx.commit, o.ctx.setPicked, hereLabel | 📍 내 위치 버튼 — 한 번 눌러 지금 자리를 장소로 넣는다(v1.33). 사용자 요구(2026-07-31): "장소가 바로 입력되게 하고 싶은거야. 어떤 방식이든 결과가 중요함." 사진 EXIF는 안드로이드가 지워서 넘기므로(M-0054) 그 길로는 결과가 안 나온다. 기기의 현재 위치는 막히지 않는다. 🔴 지키는… |
| `src/ui/screens/tripDetail.ts` | internal | function | `wireNameEdit(o: { input: HTMLInputElement; coordIsIndependent: () => boolean; setPicked: (de…): void` | o.input.addEventListener, o.coordIsIndependent, o.setPicked, o.input.value.trim, o.clearCoord | 이름을 손으로 고쳤을 때 좌표를 어떻게 할 것인가 — 이 필드에서 가장 미묘한 규칙이라 이름을 붙여 밖으로 뺐다(래칫이 밀어줬지만, 떼고 보니 이게 맞다). · 검색으로 얻은 좌표 는 이름과 한 몸이다. 이름을 바꾸면 그 좌표는 다른 장소 일 수 있으므로 무효화한다 — 안 그러면 「경복궁」을 지우고 「덕수궁」이라 적었는… |
| `src/ui/screens/tripDetail.ts` | internal | interface | `PlaceFieldShell` | — | 장소 필드의 뼈대(입력칸 + 검색·지도·내 위치 버튼 + 결과 상자). 상태가 없는 순수 DOM 조립이다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildPlaceFieldShell(initialName: string): PlaceFieldShell` | el, input.setAttribute, searchBtn.setAttribute, mapBtn.setAttribute, hereBtn.setAttribute, actions.append, row.append | — |
| `src/ui/screens/tripDetail.ts` | internal | interface | `CoordApplyContext` | — | 좌표를 적용할 때 필요한 최소한 — 필드의 나머지 상태를 밖으로 흘리지 않는다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `applyPastedCoord(ctx: CoordApplyContext, c: ParsedCoord): void` | ctx.commit, ctx.input.value.trim, parseCoordinateInput, c.lat.toFixed, c.lng.toFixed, ctx.setPicked, fillNameFromReverse | 붙여넣은 좌표를 적용한다 — 지도로 찍은 것과 같은 성격 이라 이름과 독립이다. 입력칸에 좌표 문자열이 그대로 남아 있으면 그건 장소 이름이 아니다. 비우고 그 자리의 이름을 대신 물어봐 준다 (역지오코딩) — 앱이 알 수 있는 것을 사람에게 타이핑시키지 않는다(§12). |
| `src/ui/screens/tripDetail.ts` | internal | async function | `fillNameFromReverse(ctx: CoordApplyContext, lat: number, lng: number): Promise<void>` | reverseGeocode, ctx.input.value.trim, ctx.setPicked | 좌표 → 그 자리의 이름. 비어 있을 때만 채운다(사용자가 쓴 것을 덮지 않는다). 실패하면 조용히 비워 둔다 — 좌표는 이미 확정됐고 이름은 사용자가 적으면 된다. 편의 기능이 기억 저장을 막으면 안 된다(§0의 정신 — 앱이 사용자를 이기지 않는다). |
| `src/ui/screens/tripDetail.ts` | internal | function | `renderCoordInput(host: HTMLElement, parsed: ParsedCoord, onUse: (c: ParsedCoord) => void): void` | el, coordInputLabel, box.appendChild, box.classList.add, swapBtn.addEventListener, swapCoord, renderCoordInput, onUse, host.appendChild | 붙여넣은 좌표를 확인시키는 줄 — 읽은 값과 출처를 말하고, 틀렸으면 뒤바꿀 수 있게 한다. 🔴 왜 즉시 적용하지 않고 보여주는가: 좌표를 잘못 읽으면 기억이 엉뚱한 곳에 찍힌다. 특히 위·경도 순서는 두 값이 모두 ±90 안이면 원리적으로 모호하다(로마 41.9, 12.5). 그래서 적용은 하되 무엇으로 읽었는지 화… |
| `src/ui/screens/tripDetail.ts` | internal | function | `renderSavedPlaces(host: HTMLElement, saved: readonly LocalPlace[], onPick: (p: LocalPlace) => void): void` | host.appendChild, el, [p.region, p.city].filter(Boolean).join, [p.region, p.city].filter, b.append, b.addEventListener, onPick | 「내 장소」 — 이미 담아 둔 장소 중 질의에 맞는 것. 검색 결과보다 위에 그린다. 두 번째 방문부터는 네트워크가 답을 주기 전에 이미 옳은 답이 화면에 있고, 비행기 모드에서도 그렇다(로컬 우선). 원래 신고가 "같은 곳을 다시 찾을 때마다 부정확한 검색을 반복한다" 는 문제이기도 했다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `renderPlaceResults(host: HTMLElement, places: readonly PlaceResult[], onPick: (p: PlaceResult) => void): void` | host.appendChild, el, providerLabel, b.append, precisionGlyph, precisionLabel, needsRefine, grade.classList.add, b.appendChild, b.addEventListener | 검색 결과 목록을 그린다. 두 가지를 결과마다 말한다: 무엇인지(이름·주소)와 얼마나 정밀한지 . 예전엔 뒤가 없어서 도(道) 중심점과 건물 출입구가 화면에서 구별되지 않았다(2026-07-30 신고). 어느 제공자가 답했는지는 목록 위에 한 번만 적는다 — 행마다 붙이면 소음이 된다. |
| `src/ui/screens/tripDetail.ts` | internal | async function | `renderProbe(box: HTMLElement, files: File[]): Promise<void>` | box.replaceChildren, el, box.appendChild, import, files.slice, f.slice(0, EXIF_HEAD_BYTES).arrayBuffer, f.slice, crypto.subtle.digest, f.arrayBuffer, [...new Uint8Array(digest)].map((b) => b.toString(16).padSt… | 앱이 받은 바이트의 사실 을 접힌 상자로. 좌표를 못 얻었을 때만 나온다(정상은 침묵 — §8). 🔴 왜 이게 필요했나(2026-08-01 · M-0066): 나흘 동안 "왜 위치가 안 들어오나" 를 추측으로 좁혔고 네 번 틀렸다. 그 사이 사용자는 스크린샷을 찍어 날랐다 — §12가 금지하는 상태다. 앱은 그 바이트… |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildPickPreview(photoInput: HTMLInputElement, onMetas: (metas: PhotoMeta[]) => void, fallbackZone: () => string): { el: HTMLElement; count: HTMLElement; setFiles: (files: File[]) => void }` | el, dt.items.add, render, URL.revokeObjectURL, wrap.replaceChildren, Array.from, files.entries, URL.createObjectURL, previewUrls.push, x.setAttribute | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `wireAddPhoto(input: HTMLInputElement, progress: HTMLElement, o: { momentId: string; tripId: string; fallbackZone: string; /** 이 순간이 **이미 장소를…): void` | input.addEventListener, Array.from, (async () => { try { const saved = await addPhotosToExistin…, addPhotosToExistingMoment, savedPhotoStatus, String | 「기존 순간에 사진 추가」 배선. 🔴 굽기 전에 메타를 먼저 읽는다 (§0 — 사진 압축 전에 촬영시각·GPS를 읽어 별도 저장). 그 값으로 편집 폼의 장소 칸을 채운다 — 인테이크도 다시 읽지만(그쪽은 파일 이름·GPS 저장용) 여기서 재사용하면 순서 가정이 생겨 더 얽힌다. 앞 256KB 읽기라 값이 싸다. |
| `src/ui/screens/tripDetail.ts` | internal | interface | `ExistingMomentPhotoOptions` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | async function | `addPhotosToExistingMoment(files: File[], onProgress: (msg: string) => void, o: ExistingMomentPhotoOptions): Promise<number>` | Promise.all, files.map, readPhotoMeta, processPhotosIntoMoment, placeFromPhotos, o.refresh, trySync | 버튼 선택과 Windows 드롭이 함께 쓰는 기존 순간 사진 추가의 단일 경로. |
| `src/ui/screens/tripDetail.ts` | internal | typealias | `DropNavigator` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `isFileDrag(event: DragEvent): boolean` | Array.from(event.dataTransfer?.types ?? []).includes, Array.from | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `closestMomentCard(zone: HTMLElement, clientY: number): HTMLElement \\| null` | Array.from, zone.querySelectorAll, card.getBoundingClientRect, Math.abs | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `directMomentCard(zone: HTMLElement, eventTarget: EventTarget \\| null): HTMLElement \\| null` | eventTarget.closest, zone.contains | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `draggedFileCount(event: DragEvent): number` | Array.from(event.dataTransfer?.items ?? []).filter, Array.from | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `setupWindowsTimelinePhotoDrop(zone: HTMLElement, onDrop: (files: File[], target: { momentId: string; hasPlace: boolean }, report…, onNewMoment: (file: File, report: (msg: string) => void) => Promise<void>): { mount: () => void }` | isWindowsPlatform, zone.classList.add, el, overlay.setAttribute, overlay.appendChild, target?.classList.remove, directMomentCard, photoDropIntent, draggedFileCount, closestMomentCard | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `setupTripTimelinePhotoDrop(zone: HTMLElement, context: SinglePhotoMomentContext): { mount: () => void }` | setupWindowsTimelinePhotoDrop, addPhotosToExistingMoment, showNoticeToast, String, openSinglePhotoMomentComposer | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `asMomentPhotoDropTarget(card: HTMLElement, moment: { id: string; placeName: string; placeLat?: number \\| null; placeLng?: …): [HTMLElement, boolean]` | momentHasPlace, String | — |
| `src/ui/screens/tripDetail.ts` | internal | async function | `placeFromPhotos(momentId: string, hasPlace: boolean, metas: readonly PhotoMetaLike[], refresh: () => Promise<void>): Promise<void>` | photoHintOf, photoPlaceNotice, showNoticeToast, hasAgreed, reverseGeocode, updateMomentLocalFirst, showUndoToast, refresh | 🔴 사진 위치를 순간에 실제로 써 넣는다 (사용자 지적 2026-07-31 · 스크린샷). 왜 폼 칸을 채우는 것으로는 안 되나: 「기존 순간에 사진 추가」는 곧바로 저장하고 화면을 다시 그린다 (사진이 보여야 하므로). 그러면 폼은 저장된 데이터로 새로 만들어지고 내가 채운 칸은 그 즉시 사라진다 — 실제로 그렇게 … |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildZoneSuggest(clock: TripClock, apply: (zone: string) => Promise<void>): { el: HTMLElement; suggest: (metas: readonly PhotoMetaLike[]) => void }` | el, box.setAttribute, btn.setAttribute, box.append, photoHintOf, hasAgreed, (async () => { const hit = await reverseGeocode(lat, lng); …, reverseGeocode, zonesForCountry, cc.toUpperCase | 🕒 사진이 찍힌 나라로 여행 시간대를 제안한다 (사용자 제안 2026-07-30: "초기값은 사진찍은 장소로 셋팅..어때?" ). 🔴 자동으로 정해 버리지 않는다. 좌표→시간대는 경계 데이터셋이 필요해 하지 않고(수 MB), 우리가 아는 것은 나라까지 다. 나라의 시간대가 하나면 사실상 답이지만, 여럿이면 (미국 2… |
| `src/ui/screens/tripDetail.ts` | internal | function | `makeDoSearch(o: { input: HTMLInputElement; results: HTMLElement; searchBtn: HTMLButtonElemen…): () => void` | o.input.value.trim, o.owner.claim, parseCoordinateInput, renderCoordInput, runPlaceSearch(q, { results: o.results, // 이미 좌표가 있으면 그 근처를…, runPlaceSearch, o.near | 장소 검색 실행 — 좌표를 먼저 보고 , 아니면 지오코더에 묻는다. 최상위로 뽑은 이유는 래칫이지만 결과가 낫다: 「무엇을 검색으로 볼 것인가」는 이 화면의 규칙이 아니라 장소 도메인의 규칙 이고, 클로저 밖에 있어야 다음 사람이 찾는다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `wireLiveRegistry(input: HTMLInputElement, results: HTMLElement, pick: (p: LocalPlace) => void, owner: ResultsOwner): () => void` | owner.holds, input.value.trim, listPlaces, searchRegistry, sortRegistry, results.replaceChildren, renderSavedPlaces, liveLookupNote, results.appendChild, el | ⌨️ 치는 동안 「내 장소」를 바로 보여준다 (사용자 지적 2026-08-05: "글자를 치면 바로 장소조회가 되야 하는데 그렇지 않네요" ). 맞는 지적이고, 형제 비대칭 이었다(§7 사용자 대면 대칭): 위치관리대장 화면은 치는 즉시 찾아 주는데(`search.addEventListener('input', paint… |
| `src/ui/screens/tripDetail.ts` | internal | function | `photoPlaceSuggester(o: { note: HTMLElement; ctx: CoordApplyContext; setPicked: (detail: string \\| n…): (metas: readonly PhotoMetaLike[]) => void` | photoHintOf, o.taken, photoPlaceNotice, o.ctx.commit, o.setPicked, photoPlaceLabel, hasAgreed, window.confirm, rememberAgreed, fillNameFromReverse | 📷 사진 위치로 장소 칸을 채우는 규칙 (사용자 제안 2026-07-30). 최상위로 뽑은 이유는 래칫(`check-fn-size`)이지만, 떼고 보니 이게 맞다 — 여기 담긴 것은 "장소를 고르는 일" 이 아니라 개인정보 경계 다(좌표가 언제 기기 밖으로 나가는가). 그 판단이 300줄짜리 클로저 안에 묻혀 있으면 … |
| `src/ui/screens/tripDetail.ts` | internal | function | `placeCoordState(initial: { lat: number \\| null; lng: number \\| null; placeId?: string \\| null })` | — | 장소 필드가 들고 있는 좌표 상태 넷 — 값 둘(위·경도)과 그 값의 성격 둘 (지도로 직접 찍었나 · 대장의 어느 항목인가). 🔴 왜 밖으로 뺐나: 이 넷을 손으로 비우는 자리가 세 곳 이었고(배지 해제·이름 손편집·폼 초기화) 채우는 자리가 둘이었다. 손으로 적힌 자리가 다섯이면 여섯 번째가 생길 때 하나가 빠진다… |
| `src/ui/screens/tripDetail.ts` | internal | function | `enrichExistingPlaceBadge(o: { initial: { name: string; placeId?: string \\| null }; coords: () => { lat: …): void` | o.coords, (async () => { const linked = o.initial.placeId ? (await li…, (await listPlaces()).find, listPlaces, o.setPicked, verdictFromStored, hasAgreed, reverseGeocode | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `bindPlaceBadge(badge: PickedBadge, coords: () => { lat: number; lng: number } \\| null): ( detail: string \\| null, precision?: PrecisionVerdict \\| null, address?: Pick<AddressParts, 'count…` | badge.set, coords | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `wirePlaceSearchEnter(input: HTMLInputElement, search: () => void): void` | input.addEventListener, event.preventDefault, search | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildPlaceField(initial: { name: string; lat: number \\| null; lng: number \\| null; placeId?: st…): PlaceField` | buildPlaceFieldShell, placeCoordState, placeResultsOwner, buildPickedBadge, st.clear, picked.set, bindPlaceBadge, wrap.append, setPicked, st.coords | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `orderBar(tripId: string, current: TimelineOrder, rerender: () => void): HTMLElement` | orderToggleLabel, el, btn.setAttribute, btn.addEventListener, writeTimelineOrderChoice, rerender, bar.appendChild | 타임라인 순서 토글 줄. 🔴 `renderTripDetail`에서 뽑았다 — 길이 래칫(`check-fn-size`)이 걸렸고, 우회하는 대신 덜어냈다. 판정은 `timelineOrder.ts`가 하고 여기는 그리고 저장만 한다. 🔴 라벨·화면읽기 문구·다음 값이 한 함수(`orderToggleLabel`)에서 나온… |
| `src/ui/screens/tripDetail.ts` | internal | function | `orderKey(tripId: string): string` | — | 타임라인 순서의 사용자 선택 을 여행별로 기억한다. 🔴 localStorage에 둔다 — IndexedDB도 서버도 아니다. 이건 사용자의 기억 이 아니라 표시 선호 다(테마·계절과 같은 부류 · `ui/theme.ts`가 같은 규율을 쓴다). 서버로 보내면 새 형제가 되어 동기화·tombstone·백업을 전부 상속해… |
| `src/ui/screens/tripDetail.ts` | internal | function | `readTimelineOrderChoice(tripId: string): TimelineOrderChoice` | localStorage.getItem, orderKey | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `writeTimelineOrderChoice(tripId: string, value: TimelineOrderChoice): void` | localStorage.removeItem, orderKey, localStorage.setItem | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `parseAmount(raw: string): number \\| null` | raw.replace, Number, Number.isFinite | 금액 입력(콤마·공백 허용) → 양수 숫자 또는 null. |
| `src/ui/screens/tripDetail.ts` | internal | function | `currencySelect(current: string): HTMLSelectElement` | el, sel.setAttribute, currencyLabel, sel.appendChild | 통화 선택 select 요소 생성(현재값 반영). |
| `src/ui/screens/tripDetail.ts` | internal | typealias | `Navigate` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | const | `STATUS_LABELS` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | async function | `trySync(): Promise<void>` | requestSync | 동기화 요청 — 규칙은 `services/autoSync.ts` 한 곳 에 있다. 예전엔 이 함수가 여기와 home.ts에 손으로 두 벌 있었고, 둘 다 오류를 조용히 삼켰으며 겹쳐 호출되면 runSync가 중복 실행됐다(§7 — 같은 규율을 두 곳에 구현하면 갈라진다). |
| `src/ui/screens/tripDetail.ts` | internal | function | `savedPhotoStatus(localText: string): string` | syncStatus | 로컬 커밋과 클라우드 정본 확인을 같은 말로 뭉개지 않는다. |
| `src/ui/screens/tripDetail.ts` | internal | const | `EMOTIONS` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | const | `WEEKDAYS` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `coverIndex(id: string): number` | id.charCodeAt | id → 안정적 커버 인덱스(0..2). |
| `src/ui/screens/tripDetail.ts` | internal | interface | `WhenField` | — | 발생 시각 필드 — 입력칸 + 근거 한 줄 + 여행 기간 밖 경고. 생성 폼과 편집 폼이 이 한 곳 을 쓴다(§7). 예전엔 편집에만 시각 칸이 있고 생성엔 없어서, 소급 입력이 주 흐름인 사용자가 "저장하고 → 다시 열어서 → 고친다" 를 매번 해야 했다. 두 폼에 손으로 각각 만들면 그 비대칭이 다시 자란다. 규율 … |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildWhenField(trip: { startDate: string \\| null; endDate: string \\| null; timeZone?: string }…, clock: TripClock, latestMomentAt: () => string \\| null = () => null): WhenField` | el, input.setAttribute, warn.setAttribute, inputClockHint, new Date().toISOString, wrap.append, fromLocalInputValue, clockOffsetAtWall, toLocalInputValue, momentWhen | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildEmotionRow(initial: string): { el: HTMLElement; value(): string; reset(): void }` | el, row.setAttribute, btn.setAttribute, String, b.setAttribute, b.addEventListener, sync, buttons.set, row.appendChild | 감정 선택 줄 — 생성 폼과 편집 폼이 같은 구현을 쓴다(§7). 같은 위젯을 두 곳에 손으로 만들면 한쪽만 고쳐지는 날이 온다. |
| `src/ui/screens/tripDetail.ts` | internal | interface | `SinglePhotoMomentContext` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `mountSinglePhotoMomentShell(onFinish: () => void): { body: HTMLElement; loading: HTMLElement; finish: () => void; isClosed: () => boolean; }` | el, overlay.setAttribute, head.appendChild, close.setAttribute, loading.setAttribute, body.appendChild, modal.append, overlay.appendChild, document.body.appendChild, onFinish | — |
| `src/ui/screens/tripDetail.ts` | internal | async function | `openSinglePhotoMomentComposer(file: File, context: SinglePhotoMomentContext, report: (msg: string) => void): Promise<void>` | URL.revokeObjectURL, mountSinglePhotoMomentShell, report, readPhotoMeta, shell.isClosed, el, disposePreview, URL.createObjectURL, showPreview, previewWrap.append | 빈 타임라인에 놓은 한 장을 EXIF 기반 새 순간으로 검토·편집한 뒤 저장한다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `toLocalInputValue(iso: string, offsetMin: number): string` | atOffset | ISO(UTC) → datetime-local 입력값('YYYY-MM-DDTHH:mm', 로컬시각). |
| `src/ui/screens/tripDetail.ts` | internal | function | `momentHasPlace(m: { placeName: string; placeLat?: number \\| null; placeLng?: number \\| null }): boolean` | Boolean, isRealCoord | 이 순간에 쓸 만한 장소가 있는가 (순수). 🔴 이미 저장된 0,0은 장소로 치지 않는다 (M-0057). 사용자 기록에 이미 들어가 있고, 옛 파서가 넣은 것이라 사용자가 지운 적이 없다. 「장소가 있다」로 세면 ①칩이 기니만 앞바다 좌표를 보여주고 ②사진에서 위치를 채우는 길이 「이미 있음」으로 막힌다. 지우지는 … |
| `src/ui/screens/tripDetail.ts` | internal | function | `placeChip(m: { id: string; placeName: string; placeLat?: number \\| null; placeLng?: numbe…, countryCode: string \\| null): HTMLElement` | isRealCoord, lat!.toFixed, lng!.toFixed, el, mapButton.setAttribute, mapButton.addEventListener, e.stopPropagation, openMapView, chip.appendChild, copy.setAttribute | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `placeCountryCodeById(places: readonly LocalPlace[]): ReadonlyMap<string, string \\| null>` | places.map | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `appendAudioChips(chips: HTMLElement, list: LocalAudio[], refresh: () => void): void` | chips.appendChild, audioChip, (async () => { await softDeleteAudio(a.id); refresh(); // *…, softDeleteAudio, refresh, showUndoToast, restoreAudio | 소리 칩을 칩 줄에 붙인다. 사진 격자가 아니라 칩 줄 이다 — 격자는 훑는 곳인데 소리는 재생해야 내용을 알아서 훑기를 나쁘게 한다. 장소·비용과 같은 한 줄 정보다(§7 화면 대칭). |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildRecordButton(momentId: string, tripId: string, status: HTMLElement, refresh: () => void): HTMLButtonElement` | recordButton, (async () => { try { await addAudioToMoment({ momentId, tri…, addAudioToMoment, refresh, button.classList.add | 🎙 녹음 버튼 — 저장까지 책임진다. 결과 문장을 `status` 요소에 그대로 쓴다. 실패를 조용히 넘기지 않는다 : 저장 공간 부족·형식 미지원·너무 짧은 녹음은 전부 사람이 읽는 문장으로 나온다(§12 — 앱이 아는 것을 말하지 않으면 사용자가 대신 알아내야 한다). 그리고 성공 문장이 어디에 저장됐는지 를 말한… |
| `src/ui/screens/tripDetail.ts` | internal | function | `groupByMoment(rows: T[]): Map<string, T[]>` | map.get, arr.push, map.set | 순간 id로 묶는다. 사진·비용·소리가 같은 모양의 코드 세 벌 을 갖고 있어서 하나로 뽑았다 (`check-fn-size` 래칫이 밀어 준 추출 — 게이트가 설계를 밀어 준 또 한 번). |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildPhotoGrid(o: { mediaList: LocalMedia[]; momentId: string; tripTitle: string; objectUrls: …): HTMLElement` | el, mediaList.entries, URL.createObjectURL, objectUrls.push, img.addEventListener, openPhotoViewer, pdel.setAttribute, pdel.addEventListener, e.stopPropagation, (async () => { try { await softDeleteMediaLocalFirst(md.id)… | 순간 카드의 사진 썸네일 줄 — 탭하면 크게 보고, 꾹 누르면 순서를 바꾼다 (2026-08-06). 왜 뽑았나: `renderTripDetail`은 이미 래칫이 걸린 큰 함수다. 여기 안에 드래그 배선까지 넣으면 그 함수가 또 커지고, 이 조각은 유닛이 못 보는 자리에 묻힌다. 래칫이 밀어 준 추출이다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildVideoGrid(o: { videos: LocalVideo[]; tripTitle: string; objectUrls: string[]; refresh: ()…): HTMLElement` | el, URL.createObjectURL, o.objectUrls.push, button.setAttribute, Math.round, button.append, button.addEventListener, openVideoViewer, del.setAttribute, del.addEventListener | — |
| `src/ui/screens/tripDetail.ts` | internal | async function | `processVideosIntoMoment(files: File[], momentId: string, tripId: string, onProgress: (message: string) => void): Promise<void>` | files.entries, addVideoToMoment, onProgress, Math.round | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `fxTableForCached(occurredAt: string, clock: TripClock, cache: Map<string, FxRateTable \\| null>): FxRateTable \\| null` | fxKey, fxDateFor, todayDate, momentWhen, fxBase, cache.get | — |
| `src/ui/screens/tripDetail.ts` | internal | async function | `hydrateFxTables(moments: LocalMoment[], expenses: LocalExpense[], clock: TripClock, cache: Map<string, FxRateTable \\| null>): Promise<boolean>` | fxBase, todayDate, moments.map, ex.originalCurrency.toUpperCase, byId.get, dates.add, fxDateFor, momentWhen, fxKey, cache.has | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `fromLocalInputValue(v: string, offsetMin: number): string \\| undefined` | wallClockToInstant | datetime-local 입력값(로컬시각) → ISO(UTC). 빈/무효는 undefined(변경 안 함). |
| `src/ui/screens/tripDetail.ts` | internal | function | `dayHeaderLabel(g: DayGroup): string` | Number.isNaN, d.getTime, d.getMonth, d.getDate, d.getDay | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `renderTripNotFound(wrap: HTMLElement, navigate: Navigate): void` | el, nf.appendChild, back.addEventListener, navigate, wrap.appendChild | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `tripTargetController(initial?: TripNavigationTarget)` | [...timeline.querySelectorAll<HTMLElement>('.moment-card[da…, timeline.querySelectorAll, card.classList.add, card.scrollIntoView, card.querySelector<HTMLInputElement>('.moment-edit .place-i…, card.querySelector, focusPlaceInput, requestAnimationFrame, byMoment.get, mediaList.findIndex | URL target을 한 번만 소비하고, 다음 상세 refresh까지 강조 상태를 보존한다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `heroButton(cls: string, text: string, aria: string, onClick?: () => void): HTMLButtonElement` | el, btn.setAttribute, btn.addEventListener | 히어로의 버튼 셋(←·지도·편집)은 모양이 같다 — `type`·`aria-label`·클릭을 세 번 적으면 한 곳만 고쳐진다(§7). 접근성 라벨을 빠뜨릴 수 없게 인자로 강제 한다. |
| `src/ui/screens/tripDetail.ts` | export | function | `renderTripDetail(mount: HTMLElement, tripId: string, navigate: Navigate, target?: TripNavigationTarget): () => void` | el, mount.appendChild, (async () => { const trip = await getTrip(tripId); if (!tri…, getTrip, renderTripNotFound, homeZone, coverIndex, hero.append, heroButton, navigate | 여행 상세를 그리고 정리 함수를 돌려준다 (형제 `renderHome`과 같은 계약 · 2026-08-13). 🔴 지금 이 화면은 정리할 것이 없다 — 모듈 전역 구독·타이머가 하나도 없고(실측), 걸어 둔 리스너는 전부 `mount.innerHTML = ''`로 떼어지는 DOM에 붙어 있다. 그래도 빈 함수를 돌려주… |
| `src/ui/screens/tripDetail.ts` | internal | interface | `ZoneField` | — | 여행 시간대 칸 — 「그 자리의 시계」를 정하는 단 하나의 자리. 🔴 `<datalist>`에서 `<select>`로 바꿨다 (사용자 제안 2026-07-30: "드롭다운해서 선택하게끔" ). 처음엔 `<input list=…>`로 만들었다 — 타이핑으로 418개를 좁힐 수 있어서다. 그런데 그때 내가 직접 「안드로이… |
| `src/ui/screens/tripDetail.ts` | internal | interface | `ZoneSelectOptions` | — | — |
| `src/ui/screens/tripDetail.ts` | internal | const | `PINNED_ZONES: readonly string[]` | — | 여행·집 시간대 칸이 공유하는 「자주 씀」 목록(§7 SSOT) — 서울·타슈켄트. |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildZoneSelect(o: ZoneSelectOptions): ZoneField` | el, search.setAttribute, sel.setAttribute, sel.appendChild, new Date().toISOString, z.slice(z.indexOf('/') + 1).replace, z.slice, z.indexOf, zoneLabel, (o.pinned ?? []).filter | 🔴 시간대 선택기 — 여행과 집이 같은 한 곳을 지난다 (§7 2층 · 화면 대칭). 두 칸을 손으로 두 벌 만들면 한쪽만 낡는다. 실제로 그럴 뻔했다: 여행 칸을 `<select>`로 바꾸는 순간 집 칸이 사라진 `<datalist>`를 계속 가리키고 있었다 — 컴파일은 통과하고 화면에서만 조용히 죽는 부류다(§10… |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildZoneField(initial: string): ZoneField` | buildZoneSelect, new Date().toISOString, zoneLabel, zonePreview | 여행 시간대 칸 — 「그 자리의 시계」. 비우는 것을 막지 않는다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildHomeZoneField(): ZoneField` | buildZoneSelect, homeZone, deviceZone, new Date().toISOString, zoneLabel, zonePreview | 집 시간대 칸 — 환산 꼬리표의 기준. 여행 시간대 바로 아래에 둔다(같은 성격이라 같은 자리). 이 값은 여행이 아니라 이 기기의 표시 설정 이라 저장 위치가 다르다(localStorage). 그래서 고르는 즉시 저장한다 — 여행의 [저장] 버튼과 묶으면 "여행을 안 고쳤는데 왜 저장?"이 된다. |
| `src/ui/screens/tripDetail.ts` | internal | async function | `loadTripDetailRows(tripId: string)` | Promise.all, listMoments, listMediaByTrip, listExpensesByTrip, listVideosByTrip, listPlaces | 상세 새로고침의 독립 읽기를 한 묶음으로 유지한다. 새 읽기 형제가 생겨도 호출부는 늘어나지 않는다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `toMapPoints(moments: LocalMoment[], byMoment: Map<string, LocalMedia[]>, clock: TripClock, places: LocalPlace[]): MapPoint[]` | places.map, byMoment.get, momentCoord, placeById.get, momentWhen, out.push | — |
| `src/ui/screens/tripDetail.ts` | internal | async function | `saveMomentExpense(momentId: string, tripId: string, money: { amount(): string; currency(): string; note(): string }): Promise<void>` | parseAmount, money.amount, createExpenseLocalFirst, money.currency, money.note | 순간에 딸린 비용을 저장한다(금액이 유효할 때만). 실패가 순간 저장을 무르지 않는다 — 비용은 부가정보이므로, 저장 실패로 사용자가 방금 적은 기록을 잃게 하지 않는다. (renderTripDetail에서 뽑아낸 헬퍼 — 생성 흐름을 짧게 유지한다.) |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildMoneyRow(existing: LocalExpense \\| undefined): { el: HTMLElement; amount(): string; currency(): string; note(): string; /** 저장 후 초기화 — 감정 줄(`emoti…` | el, String, amountIn.setAttribute, currencySelect, row.append, noteIn.setAttribute, wrap.append | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `timeGutter(when: { time: string; home: string }): HTMLElement[]` | el, out.push | 타임라인 시간 열 — 그 자리의 시각(크게) + 집 시간 환산(작게, 다를 때만 ). 최상위로 뽑은 이유가 둘이다. ①`when`만 주면 되므로 DOM 클로저에 있을 필요가 없다. ②라이브 검사가 `.tl-time` / `.tl-time-home` 두 줄의 관계를 재는데, 그 규칙(환산은 다를 때만)이 화면 코드 깊숙이… |
| `src/ui/screens/tripDetail.ts` | internal | function | `zoneNotice(caveat: string, onFix: () => void): HTMLElement \\| null` | el, box.setAttribute, box.appendChild, fix.setAttribute, fix.addEventListener | 시간대 미지정 고지 — 타임라인 맨 위에 한 번만 . 없으면 `null`. 왜 순간마다가 아니라 한 번인가: 같은 문장이 100번 나오면 그건 고지가 아니라 배경이 된다. 그리고 §12 — 말하기만 하고 끝내지 않는다. 그 자리에서 고칠 버튼 을 함께 준다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildEditPanel(trip: LocalTrip, onSave: (patch: { title: string; startDate: string; endDate: string; status: Lo…, onDelete: () => Promise<void>): HTMLElement` | el, titleIn.setAttribute, startIn.setAttribute, endIn.setAttribute, dates.append, status.setAttribute, status.appendChild, cancel.addEventListener, row.append, confirmRow.appendChild | 여행 날짜·상태·제목·시간대 편집 패널 + 삭제. onSave(patch)/onDelete() 호출 후 상위에서 처리. |
| `src/ui/screens/tripDetail.ts` | internal | async function | `processPhotosIntoMoment(files: File[], momentId: string, tripId: string, onProgress: (msg: string) => void): Promise<number>` | new Array(files.length).fill, onProgress, openPhotoEditor, discarded.add, discarded.has, addPhotoToMoment | 파일들을 배치 편집(← 이전/다음·나머지 모두 원본) 후 한 순간에 일괄 저장한다. 생성 흐름과 "기존 순간에 사진 추가" 흐름이 공유한다(단일 경로 — SSOT). 사진은 한 장씩 열고 굽고 저장하므로 배치가 커도 메모리는 장당 한 장만 쓴다(명시적 장수 제한 없음). |
| `src/ui/screens/tripDetail.ts` | internal | function | `openPlaceCandidates(field: PlaceField, enabled: boolean): void` | requestAnimationFrame, field.showSavedPlaces | 대장에서 재연결할 때만 현재 이름으로 「내 장소」 후보를 즉시 펼친다. |
| `src/ui/screens/tripDetail.ts` | internal | function | `buildMomentEditForm(m: LocalMoment, trip: { startDate: string \\| null; endDate: string \\| null; timeZone?: string }…, clock: TripClock, existingExpense: LocalExpense \\| undefined, openPlacePicker: boolean, onSave: ( // 서비스의 계약 타입을 그대로 쓴다 — 필드가 늘 때 화면·서비스 두 곳을 고치지 않는다(SSOT). patch: Upd…, onCancel: () => void): HTMLElement` | el, titleIn.setAttribute, buildEmotionRow, buildPlaceField, companionField, noteIn.setAttribute, buildMoneyRow, buildWhenField, timeField.set, cancel.addEventListener | — |
| `src/ui/screens/tripDetail.ts` | internal | function | `fxDetailRows(ex: LocalExpense, occurredAt: string, offsetMin: number, base: string, t: FxRateTable, conv: number): HTMLElement[]` | ex.originalCurrency.toUpperCase, fxDateFor, todayDate, el, r.append, rows.push, line, unitRate, formatRate, formatMoney | 환율 상세 행 — 사용자가 직접 검산할 수 있게 적용일·단위환율·계산식·출처를 모두 보인다. 숫자를 그냥 믿으라고 하지 않는다(정직한 완료의 UI 판). |
| `src/ui/screens/tripDetail.ts` | internal | function | `convertedTotalStat(expenses: LocalExpense[], moments: LocalMoment[], totals: string[], clock: TripClock, fxCache: Map<string, FxRateTable \\| null>): HTMLElement \\| null` | fxBase, Object.keys(sumByCurrency(expenses)).includes, Object.keys, sumByCurrency, moments.map, ex.originalCurrency.toUpperCase, byId.get, fxTableForCached, convertAmount, missing.add | 환산 합계 칸 — 각 비용을 자기 사용일 환율로 환산해 더한다(한 날 환율로 뭉뚱그리지 않는다). 🔴 `renderTripDetail`에서 뽑았다 — 길이 래칫(`check-fn-size`)이 걸렸고, 주석을 줄이는 대신 덜어냈다. 게이트가 설계를 밀어준 자리다(§11 마지막 문단). 🔴 환산 못 한 통화를 숨기지 … |
| `src/ui/screens/tripDetail.ts` | internal | function | `stat(value: string, label: string): HTMLElement` | el, s.appendChild | — |
| `src/ui/theme.ts` | export | typealias | `ThemeChoice` | — | — |
| `src/ui/theme.ts` | export | typealias | `Season` | — | — |
| `src/ui/theme.ts` | internal | const | `THEME_KEY` | — | — |
| `src/ui/theme.ts` | internal | const | `SEASON_KEY` | — | — |
| `src/ui/theme.ts` | export | const | `SEASONS: readonly Season[]` | — | — |
| `src/ui/theme.ts` | export | const | `SEASON_LABEL: Record<Season, string>` | — | — |
| `src/ui/theme.ts` | internal | function | `safeGet(key: string): string \\| null` | localStorage.getItem | — |
| `src/ui/theme.ts` | internal | function | `safeSet(key: string, value: string): void` | localStorage.setItem | — |
| `src/ui/theme.ts` | internal | function | `seasonForMonth(month1to12: number): Season` | — | 현재 월(북반구) 기준 계절 — 사용자가 고르지 않았을 때의 기본값. |
| `src/ui/theme.ts` | export | function | `storedTheme(): ThemeChoice \\| null` | safeGet | 저장된 테마 선택(없으면 null → OS 선호 따름). |
| `src/ui/theme.ts` | export | function | `effectiveTheme(): ThemeChoice` | storedTheme, window.matchMedia | 실제 적용 중인 테마(저장값 우선, 없으면 OS 선호). |
| `src/ui/theme.ts` | export | function | `currentSeason(): Season` | safeGet, (SEASONS as readonly string[]).includes, seasonForMonth, new Date().getMonth | 현재 계절(저장값 우선, 없으면 이번 달 기준). |
| `src/ui/theme.ts` | export | function | `initTheme(): void` | storedTheme, document.documentElement.setAttribute, currentSeason | 앱 부팅 시 1회: 저장된 선호를 문서에 반영(FOUC 최소화). |
| `src/ui/theme.ts` | export | function | `setTheme(choice: ThemeChoice): void` | document.documentElement.setAttribute, safeSet | 테마를 명시적으로 설정하고 저장. |
| `src/ui/theme.ts` | export | function | `toggleTheme(): ThemeChoice` | effectiveTheme, setTheme | 라이트/다크 토글. 반환값 = 적용된 테마. |
| `src/ui/theme.ts` | export | function | `setSeason(season: Season): void` | document.documentElement.setAttribute, safeSet | 계절 설정하고 저장. |
| `src/ui/toast.ts` | internal | const | `activeTimer: ReturnType<typeof setTimeout> \\| null` | — | — |
| `src/ui/toast.ts` | internal | function | `readMs(message: string): number` | Math.min, Math.max | 🔴 읽을 시간을 준다 — 머무는 시간이 문장 길이를 따라간다(2026-07-31 · M-0055). 5초는 「여행을 지웠어요」(8자) 시절의 값이었다. v1.31이 70자가 넘는 안내 문장 을 같은 그릇에 담으면서, 사용자는 다 읽기도 전에 사라지는 문장을 보게 됐다 — 그 문장의 뒷부분이 하필 "그래서 어떻게 하면 … |
| `src/ui/toast.ts` | export | function | `showUndoToast(message: string, onUndo: () => void \\| Promise<void>): void` | document.querySelector('.undo-toast')?.remove, document.querySelector, clearTimeout, el, toast.setAttribute, toast.appendChild, toast.remove, btn.addEventListener, close, onUndo | 삭제 등 되돌릴 수 있는 작업 뒤에 호출. onUndo는 한 번만 실행된다. |
| `src/ui/toast.ts` | export | function | `showNoticeToast(message: string): void` | document.querySelector('.undo-toast')?.remove, document.querySelector, clearTimeout, el, toast.setAttribute, toast.appendChild, document.body.appendChild, setTimeout, toast.remove, readMs | 되돌릴 것이 없는 알림 토스트 . 실행취소 버튼 없이 문장만 띄운다(머무는 시간은 `readMs`). 왜 필요했나(2026-07-31): 「사진에 위치 정보가 없어요」처럼 앱이 아무것도 안 했다는 사실 을 말해야 하는 자리가 생겼다. 아무것도 안 했으니 되돌릴 것도 없는데, 그렇다고 침묵하면 사용자는 고장인지 없는 건지… |

## 5. 자동 검증 연결

- `tests/unit/mediaFileAudit.test.ts`
- `tests/unit/persistAdvice.test.ts`
- `tests/unit/richText.test.ts`
- `tests/unit/storeState.test.ts`
- `tests/unit/strandedPurge.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/ui/screens/tripDetail.ts` — 3847줄
- `src/ui/styles/app.css` — 2517줄
- `src/ui/screens/home.ts` — 1018줄
- `src/ui/screens/dataManager.ts` — 1016줄
- `src/ui/screens/guide.ts` — 780줄
- `src/services/storeState.ts` — 596줄
- `src/ui/screens/r2Setup.ts` — 398줄
- `src/ui/dragReorder.ts` — 276줄
- `src/ui/screens/aboutApp.ts` — 240줄
- `src/ui/screens/companionRecords.ts` — 220줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
