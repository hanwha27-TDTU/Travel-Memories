---
shape: prose
shape_reason: 책임→흐름→파일→API→검증→한계가 복구 순서로 이어지는 생성 기술서라 순서 자체가 내용이다.
---
<!-- GENERATED by scripts/gen-module-design-docs.mjs. DO NOT EDIT. -->
# 휴지통·복원·영구삭제 설계서

> **생성물 · 손편집 금지.** 정본은 아래에 열거된 실행 코드다. 코드 변경 뒤 `npm run gen:module-designs`를 실행한다. `check-module-design-docs`가 커밋본과 재생성본을 글자 단위로 대조한다.

## 1. 책임과 현재 경계

soft delete, restore, purge, 연쇄 삭제와 잔재 판정을 소유한다.

- 모듈 ID: `trash-purge`
- 관측 파일: 4개 · 논리 줄: 1498 · 최상위 선언: 69개(외부 공개 55개)
- 코드 내용 SHA-256: `dd47af04c54fd1ab56b2fbc1ad5489d91d9cf36f0b5056e8e6d717839ca3050b`
- 생성기 스키마: 1

## 2. 진입점과 모듈 간 흐름

### 관측된 진입점

- `src/domain/trashVerdict.ts`

### 다른 모듈로 나가는 정적 의존

- 오프라인 데이터베이스와 Row Map(`offline-db`): import/re-export 10건
- 인증·Supabase 클라이언트·동의(`auth-security`): import/re-export 4건
- 여행·순간 핵심 도메인(`travel-core`): import/re-export 4건
- 오디오 노트(`audio-notes`): import/re-export 2건
- 비용·통화·환율(`expense-fx`): import/re-export 1건
- 지도·장소·지오코딩(`map-place`): import/re-export 1건
- 사진 수집·파생본·저장(`photo-storage`): import/re-export 1건
- 오프라인 동기화와 정본 전환(`sync-canonical`): import/re-export 1건
- 영상 처리 파이프라인(`video-pipeline`): import/re-export 1건

### 외부 패키지/런타임 의존

- `dexie`

## 3. 파일·저장소·외부 경계

| 파일 | 줄 | export/선언 | 저장소 내부 의존 | 코드에서 관측한 I/O·생명주기 신호 |
|---|---:|---:|---|---|
| `src/domain/trashVerdict.ts` | 179 | 11/11 | — | deletion lifecycle |
| `src/services/purge.ts` | 719 | 29/32 | src/offline/db.ts<br>src/domain/trip/rowmap.ts<br>src/domain/moment/rowmap.ts<br>src/domain/expense/rowmap.ts<br>src/domain/media/rowmap.ts<br>src/domain/audio/rowmap.ts<br>src/domain/video/rowmap.ts<br>src/domain/place/rowmap.ts<br>src/services/canonicalSync.ts<br>src/services/supabase/client.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localPlaces, localTrips, localVideos; file/blob bytes; transactional persistence; deletion lifecycle |
| `src/services/trash.ts` | 466 | 14/22 | src/offline/db.ts<br>src/domain/time.ts<br>src/services/homeZone.ts<br>src/services/purge.ts<br>src/domain/audio/note.ts<br>src/services/moments.ts<br>src/services/media.ts<br>src/services/expenses.ts<br>src/services/audio.ts<br>src/services/videos.ts<br>src/services/places.ts<br>src/services/supabase/client.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localPlaces, localTrips, localVideos; transactional persistence; deletion lifecycle |
| `src/services/trashState.ts` | 134 | 1/4 | src/offline/db.ts<br>src/services/supabase/client.ts<br>src/services/auth.ts<br>src/services/trips.ts<br>src/services/trash.ts<br>src/domain/trashVerdict.ts | Dexie: localAudio, localExpenses, localMedia, localMoments, localVideos; deletion lifecycle |

## 4. 최상위 API와 내부 조립점

| 파일 | 공개성 | 종류 | 시그니처 | 본문에서 관측한 호출(최대 10) | 코드 주석/JSDoc |
|---|---|---|---|---|---|
| `src/domain/trashVerdict.ts` | export | typealias | `TrashLevel` | — | — |
| `src/domain/trashVerdict.ts` | export | interface | `TrashMetricView` | — | 지표 하나의 표시값 — `Metric`과 같은 모양이되 ui를 import하지 않는다(도메인→화면 의존 금지). |
| `src/domain/trashVerdict.ts` | export | interface | `EmptyOnRestore` | — | 되살려도 자료가 비는 항목 하나. |
| `src/domain/trashVerdict.ts` | export | interface | `OrphanTrashed` | — | 되살릴 부모가 사라진 항목 하나. |
| `src/domain/trashVerdict.ts` | export | interface | `TrashObservation` | — | 관측값 — 서비스가 읽기 전용으로 모아 온다. 판정은 여기서만 한다. 🔴 `serverTrashCount`가 `null`인 것은 0이 아니라 「모름」 이다(§8). 조회 실패와 「서버 휴지통이 비었다」를 같은 값으로 접으면, 서버에 지운 것이 쌓여 있는데 화면이 조용해진다. |
| `src/domain/trashVerdict.ts` | export | const | `TRASH_ITEM_CAP` | — | 접힌 출처·요약이 함께 쓰는 상한 — 조용히 자르지 않고 「외 N건」을 적는다(§5 3항). |
| `src/domain/trashVerdict.ts` | export | function | `trashSourceMetric(o: TrashObservation): TrashMetricView` | — | ① 휴지통 목록의 기준 이 서버인가. ADR-0049 이후 온라인이면 서버가 정본이라 모든 기기가 같은 휴지통을 본다. 이 지표는 그 계약이 실제로 서 있는지를 잰다 — 서버를 못 읽어 이 기기 기록으로 폴백 중이면 그때가 바로 기기끼리 달라지는 상태이고, 사용자가 알아야 할 유일한 순간이다. 🔴 개수를 대조하지 않는… |
| `src/domain/trashVerdict.ts` | export | function | `emptyOnRestoreMetric(o: TrashObservation): TrashMetricView` | o.emptyOnRestore.filter, [media ? `사진 ${media}` : '', audio ? `소리 ${audio}` : '', vi…, parts.join | ② 되살려도 자료가 비는 항목. 왜 이 지표가 생겼나: M-0101을 고치며 「휴지통 사진의 바이트를 못 받아도 최종본 전체를 막지 않는다」로 바꾸고 빈 자리에 `bytesMissing` 표시를 남겼다. 자료구조는 정직해졌는데 그 표시를 사용자가 볼 곳을 안 만들었다 — 되살리기 버튼은 멀쩡히 보이고, 눌러야 사진이 비… |
| `src/domain/trashVerdict.ts` | export | function | `orphanTrashedMetric(o: TrashObservation): TrashMetricView` | — | ③ 되살릴 곳이 없는 항목. 순간을 영구삭제하면 그 순간 행만 사라진다(`purgeChildPermanently`는 자식을 데려가지 않는다 — 자식은 `trip_id`로만 묶여 있다). 그때 휴지통에 있던 그 순간의 사진·소리·영상·비용은 되살려도 갈 부모가 없다. 어느 기존 도구도 이걸 못 본다 — `checkInte… |
| `src/domain/trashVerdict.ts` | export | function | `trashMetrics(o: TrashObservation): TrashMetricView[]` | trashSourceMetric, emptyOnRestoreMetric, orphanTrashedMetric | 세 지표를 한 번에 — 도구는 이 순서대로 화면에 올린다(순서도 계약이다 · §7 화면 대칭). |
| `src/domain/trashVerdict.ts` | export | function | `trashHeadline(o: TrashObservation): string` | orphanTrashedMetric | 판정 한 문장. 🔴 문장을 손으로 쓰지 않고 무리별 개수에서 만든다 (§7-C — 「엉뚱한 곳을 가리킴」 방지). 지표를 하나 더 늘렸는데 문장만 옛 지표를 가리키던 사고가 이 저장소에서 두 번 났다. 그리고 빈 상태를 초록으로 칠하지 않는다 (§7-C) — 휴지통이 비어 있으면 그렇다고 말한다. |
| `src/services/purge.ts` | export | const | `PURGE_DOMAINS` | — | — |
| `src/services/purge.ts` | export | typealias | `PurgeDomain` | — | — |
| `src/services/purge.ts` | export | const | `PURGE_PARENTS` | — | 부모 노릇을 하는 도메인 — 이 행을 지우면 서버 FK가 자식을 함께 지운다. 🔴 왜 이 개념이 생겼나 (2026-08-05 · M-0107, 사용자 실기기 Windows PC): 서버 스키마는 `media/expenses/audio → moments → trips`로 두 단계 `ON DELETE CASCADE`인데,… |
| `src/services/purge.ts` | export | typealias | `PurgeParent` | — | — |
| `src/services/purge.ts` | export | const | `PARENT_KEY: Record<PurgeParent, { column: string; index: string }>` | — | 부모를 가리키는 키 — 서버 컬럼과 로컬 인덱스를 한 곳에서 짝지운다. 두 곳에 손으로 적으면 갈라진다(M-0060 — "같은 판정을 두 번 쓰지 마라"). |
| `src/services/purge.ts` | internal | interface | `DomainPurge` | — | — |
| `src/services/purge.ts` | export | const | `DOMAIN_PURGE: Record<PurgeDomain, DomainPurge>` | — | 도메인 등록부. Record이므로 빠뜨리면 컴파일이 안 된다. 새 동기화 도메인이 생기면 컴파일러가 여기로 데려온다 — 그게 이 타입의 목적이다. |
| `src/services/purge.ts` | export | function | `cascadeChildDomains(parent: PurgeParent): PurgeDomain[]` | PURGE_DOMAINS.filter, DOMAIN_PURGE[d].cascadeParents.includes | 이 부모를 영구삭제할 때 함께 지워야 하는 자식 도메인들 — 등록부에서 역으로 뽑는다. 🔴 장소는 어느 부모의 자식도 아니다(C-1). 예전엔 `PURGE_DOMAINS.filter(d => d !== 'trip')`로 뽑아 장소까지 넣었고, `trip_id` 없는 장소 테이블에 `trip_id`로 질의해 여행 영구삭… |
| `src/services/purge.ts` | export | function | `asPurgeParent(domain: PurgeDomain): PurgeParent \\| null` | (PURGE_PARENTS as readonly string[]).includes, cascadeChildDomains | 이 도메인이 부모인가 — 지우면 서버 FK가 자식을 데려가는가. 아니면 `null`(잎). 🔴 이 한 줄이 M-0107의 자리다. 예전 코드는 `if (domain === 'trip')`이라고 손으로 적어 순간을 잎처럼 다뤘다. 이제 부모 여부는 등록부가 답한다 — 새 부모가 생겨도 따라온다. |
| `src/services/purge.ts` | export | interface | `PurgeTarget` | — | 영구삭제 대상 한 칸 — id와 도메인이 함께 다닌다(표식·전파 op·하드 삭제가 같은 목록을 쓴다). |
| `src/services/purge.ts` | export | const | `TRASH_DOMAINS` | — | 휴지통이 다루는 도메인 전부. 사용자에게 보이는 층의 진실원이다. 🔴 2026-07-27까지 이 자리에는 `LOCAL_ONLY_DOMAINS = ['audio']`라는 별도 목록 이 있었다. 소리에는 서버 표가 없어 `remoteTable` 이름을 지어낼 수 없었기 때문이다. 사용자가 그 전제를 없앴다( "서버에 올라… |
| `src/services/purge.ts` | export | typealias | `TrashDomain` | — | — |
| `src/services/purge.ts` | export | function | `localTableOf(d: TrashDomain): Table<{ id: string }, string>` | DOMAIN_PURGE[d].table | 도메인의 로컬 테이블 — 화면·휴지통이 테이블 이름을 손으로 적지 않게 한 곳에서 준다. |
| `src/services/purge.ts` | export | function | `purgeOpType(domain: PurgeDomain): string` | — | 큐에 담기는 영구삭제 작업의 entityType. 기존 push 루프(`!== 'trip'` …)가 자연히 건너뛴다. |
| `src/services/purge.ts` | export | const | `UNPURGE_OP` | — | 원장 되돌리기(복원) 작업의 entityType. 도메인이 없다 — 원장은 종류를 모른다. |
| `src/services/purge.ts` | export | async function | `requestUnpurge(ids: string[]): Promise<number>` | db, (await d.syncQueue.toArray()).filter((q) => q.operationType…, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, [...new Set(ids)].filter, queued.has, new Date().toISOString, d.syncQueue.bulkAdd, fresh.map, crypto.randomUUID | 백업 복원이 되살린 id들을 서버 원장에서 빼달라고 큐에 올린다. 왜 큐인가(2026-07-26 사용자 실기기): 복원은 오프라인에서도 되고, 서버 호출은 실패할 수 있다. 그때 그냥 넘어가면 다음 동기화가 원장을 pull해 복원한 것을 다시 지운다 — 실제로 그렇게 됐고 사용자에겐 아무 오류도 안 보였다. 의사를 남겨… |
| `src/services/purge.ts` | export | async function | `pendingUnpurgeIds(): Promise<Set<string>>` | (await db().syncQueue.toArray()).filter, db().syncQueue.toArray, db, rows.map | 아직 서버 원장에서 못 뺀 복원 대상 id들 — `applyPurgedLedger`가 이걸 건너뛴다. |
| `src/services/purge.ts` | export | function | `purgeDomainOf(entityType: string): PurgeDomain \\| null` | entityType.startsWith, entityType.slice, (PURGE_DOMAINS as readonly string[]).includes | entityType에서 도메인을 되읽는다. 영구삭제 작업이 아니면 null. |
| `src/services/purge.ts` | export | async function | `purgedIdSet(): Promise<Set<string>>` | (await db().purgedIds.toArray()).map, db().purgedIds.toArray, db | 영구삭제 표식 조회 — pull이 이 id를 건너뛴다. 없으면 표식이 장식이 되고 서버 tombstone을 다시 받아와 휴지통에 되살아난다 . 네 pull 함수가 모두 써야 한다. |
| `src/services/purge.ts` | export | function | `purgeMarks(entries: { id: string; domain: PurgeDomain }[], at: string): PurgedId[]` | entries.map | — |
| `src/services/purge.ts` | internal | function | `targetOf(domain: PurgeDomain, row: { id: string; storagePath?: string }): PurgeTarget` | — | 바이트를 가진 도메인이면 로컬 행이 기억하는 경로를 target에 실어 준다(행이 사라지기 전에). |
| `src/services/purge.ts` | export | async function | `collectPurgeTargets(parent: PurgeParent, id: string): Promise<PurgeTarget[]>` | cascadeChildDomains, DOMAIN_PURGE[child] .table() .where(key) .equals(id) .toArr…, DOMAIN_PURGE[child] .table() .where(key) .equals, DOMAIN_PURGE[child] .table() .where, DOMAIN_PURGE[child] .table, targets.push, targetOf, targets.filter, seen.has, seen.add | 영구삭제 가족을 이 기기에서 모은다 — 부모 자신 + 부모를 가리키는 모든 로컬 자식. 🔴 여행과 순간이 같은 함수를 지난다 (§7 2층). 예전엔 여행만 가족을 모았고 순간은 `purgeChildPermanently`에서 자기 한 행만 지웠다 — 그런데 서버 FK는 순간의 자식도 지운다. 그 비대칭이 M-0107이다… |
| `src/services/purge.ts` | export | function | `purgeOpOf(t: PurgeTarget, at: string): SyncQueueItem` | crypto.randomUUID, purgeOpType | 전파 작업 하나. `bytePath`는 서버 행이 이미 없을 때의 유일한 근거 다(M-0107). |
| `src/services/purge.ts` | export | async function | `commitPurge(targets: PurgeTarget[], at: string, opts: { dropEntityOps?: boolean } = {}): Promise<void>` | db, [...new Set(targets.map((t) => t.domain))].map, targets.map, DOMAIN_PURGE[dm].table, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, ids.has, purgeDomainOf, d.transaction, d.purgedIds.bulkPut | 가족 전체를 한 트랜잭션에 영구삭제한다 — 표식 → 전파 op → 로컬 하드 삭제. 순서가 곧 안전이다(§2-Z): 표식을 먼저 넣어야 중간에 실패해도 "지웠는데 표식이 없어 되살아나는" 창이 생기지 않는다. 셋이 한 트랜잭션인 이유는 불변식 #1과 같다 — "곧 이어서 쓸 것"은 없는 것과 같다(M-0033). `dr… |
| `src/services/purge.ts` | export | async function | `applyRemotePurge(domain: PurgeDomain, id: string): Promise<boolean>` | db, d.purgedIds.get, DOMAIN_PURGE[domain].table, d.transaction, d.purgedIds.put, new Date().toISOString, table.delete, table.get | 서버가 알려준 영구삭제를 이 기기에 적용한다. 멱등 — 이미 적용됐으면 아무 일도 하지 않는다. 사용자 결정(2026-07-26): 이 기기에 아직 서버로 못 보낸 변경이 있어도 그냥 지운다. 사용자가 "지워라"라고 한 의도를 그대로 따르고, 동작을 단순·예측 가능하게 유지한다. (영구삭제를 누른 기기는 이미 "보낼 것… |
| `src/services/purge.ts` | export | async function | `applyPurgedLedger(ids: string[]): Promise<number>` | db, new Date().toISOString, PURGE_DOMAINS.map, DOMAIN_PURGE[dm].table, d.transaction, (await d.syncQueue.toArray()).filter((q) => q.operationType…, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, restoring.has, d.purgedIds.get | 서버 원장 에서 받은 id들을 이 기기에 적용한다(ADR-0030). 원장은 id만 담는다 — 자료를 서버에 남기지 않는 것이 목적이므로 종류도 안 담는다. 그래서 어느 도메인인지 모른 채 받아, 네 테이블을 모두 훑어 지운다. 등록부를 도는 구조라 새 도메인이 생기면 자동으로 따라온다(§7 — 다음 형제가 자동으로 따… |
| `src/services/purge.ts` | export | async function | `sweepPurgedOrphans(): Promise<number>` | db, d.purgedIds.toArray, pendingUnpurgeIds, marks.map((m) => m.id).filter, marks.map, restoring.has, cascadeChildDomains, DOMAIN_PURGE[child].table, table.orderBy(PARENT_KEY[parent].index).uniqueKeys, table.orderBy | 부모가 영구삭제됐는데 이 기기에 남아 있는 자식들을 데려간다 (2026-08-05 · M-0107). ── 왜 이게 필요한가 (사용자 실기기 Windows PC) ───────────────────────────── 서버 FK는 `media/expenses/audio → moments → trips`로 `ON DELET… |
| `src/services/purge.ts` | export | async function | `requeueUnpropagatedPurges(ids: string[]): Promise<number>` | db, (await d.syncQueue.toArray()).filter((q) => purgeDomainOf(q…, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, purgeDomainOf, d.purgedIds.bulkGet, new Date().toISOString, ids.entries, queued.has, d.syncQueue.add | 전파되지 않은 영구삭제를 다시 큐에 넣는다 (2026-07-26 실기기에서 드러난 상태). 왜 이런 상태가 생기나: ADR-0027 이전 의 `purgeTripPermanently`는 로컬 표식만 남기고 서버엔 아무것도 알리지 않았다(ADR-0025 — "서버 행은 tombstone으로 남겨 둔다"). 그래서 그 시절에… |
| `src/services/purge.ts` | export | async function | `purgeServerOnly(domain: PurgeDomain, ids: string[]): Promise<number>` | db, (await d.syncQueue.toArray()).filter((q) => purgeDomainOf(q…, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, purgeDomainOf, new Date().toISOString, DOMAIN_PURGE[domain].table, queued.has, d.transaction, d.purgedIds.put | 서버에만 있는 tombstone을 영구삭제한다 (로컬엔 행이 없는 경우). 왜 필요한가(2026-07-26, 휴지통 확장 직후 사용자 실기기): 서버에 tombstone 사진이 남아 있는데 휴지통엔 안 보였다. `listTrashedChildren`은 로컬 Dexie를 보는데, `pullMedia`가 "로컬에 없는 to… |
| `src/services/purge.ts` | internal | function | `purgeRowsCol(domain: PurgeDomain, r: Record<string, unknown>): { id: string; entity: unknown }` | fromRow, fromMomentRow, fromExpenseRow, fromPlaceRow, fromMediaRow, fromAudioRow, fromVideoRow | — |
| `src/services/purge.ts` | export | async function | `fetchServerDomainEntities(domain: PurgeDomain, client: JourneyClient): Promise<T[] \\| null>` | Promise.all, fetchAllRows, fetchAllIds, rowsRes.data .filter((r) => !purged.has(r.id as string)) .m…, rowsRes.data .filter, purged.has, purgeRowsCol | 서버가 지금 아는 도메인 한 종류의 행 전부 를 로컬과 같은 모양(`LocalX`)으로 가져온다. 화면 표시·라벨링에만 쓴다 — 어떤 값도 로컬 Dexie에 쓰지 않는다(그건 정상 pull의 일이다). 영구삭제 원장에 있는 id는 제외한다(다른 기기가 이미 영구삭제한 것을 휴지통에 다시 보여주지 않는다). @return… |
| `src/services/purge.ts` | export | async function | `ensureLocalTombstone(domain: PurgeDomain, id: string, client: JourneyClient): Promise<boolean>` | DOMAIN_PURGE[domain].table, table.get, client.from(DOMAIN_PURGE[domain].remoteTable).select('*').e…, client.from(DOMAIN_PURGE[domain].remoteTable).select('*').eq, client.from(DOMAIN_PURGE[domain].remoteTable).select, client.from, purgeRowsCol, table.put, Boolean | 이 기기에 없는 tombstone 행을 서버에서 메타데이터만 채운다(바이트 없이 — 복원·영구삭제는 바이트가 필요 없고, 필요해지면 정상 pull이 채운다). 이미 로컬에 있으면 그대로 둔다(멱등). 활성(살아 있는) 행은 이 경로로 만들지 않는다 — 그건 비파괴 pull의 일이고, 여기서 만들면 이 기기가 본 적 없는… |
| `src/services/trash.ts` | export | typealias | `ChildDomain` | — | 여행이 아닌 도메인. 여행은 `listDeletedTrips`가 따로 다룬다. |
| `src/services/trash.ts` | export | const | `CHILD_DOMAINS: ChildDomain[]` | — | — |
| `src/services/trash.ts` | export | interface | `TrashedChild` | — | — |
| `src/services/trash.ts` | export | interface | `TrashPurgeEntry` | — | 휴지통 화면이 서비스에 넘기는 영구삭제 선택. 도메인과 id를 분리하지 않는다. |
| `src/services/trash.ts` | export | const | `CHILD_LABEL: Record<ChildDomain, string>` | — | 사람이 읽는 도메인 이름 — 화면이 손으로 다시 적지 않게 여기 한 곳(§7). |
| `src/services/trash.ts` | internal | typealias | `ChildRow` | — | 도메인 공통 결과 줄. |
| `src/services/trash.ts` | internal | interface | `ChildSource` | — | 한 도메인의 tombstone 행을 휴지통 줄로 바꾸는 규칙. `fromEntity`가 라벨을 만드는 유일한 곳 이다(§7 SSOT) — 로컬 Dexie 읽기(`rows`)도, 서버 직접 조회(`listTrashedChildrenFromServer`, 2026-08-05)도 같은 함수 를 지난다. 두 경로가 라벨을 각자… |
| `src/services/trash.ts` | export | typealias | `ClockOf` | — | 여행 id → 그 여행의 시계. 라벨의 날짜를 그 자리 기준 으로 적기 위해 필요하다. 🔴 인자로 받는 이유(§7 2층): 도메인마다 각자 `localTrips`를 다시 읽으면 ①같은 표를 다섯 번 읽고 ②한 도메인이 옛 방식(기기 시계)을 쓰는 것이 조용해진다. 등록부가 주는 도구로 두면 새 도메인이 태어날 때 같은… |
| `src/services/trash.ts` | internal | interface | `ChildSourceMap` | — | 도메인별 휴지통 규칙 등록부 . 왜 Record인가(§7 2층): 예전엔 `listTrashedChildren` 안에 도메인마다 `for` 루프를 손으로 썼다. 그 구조에서는 새 도메인을 만들면서 여기를 안 고쳐도 컴파일이 통과한다 — 실제로 오디오가 그렇게 태어나 순간·여행을 지워도 소리만 활성으로 남았고, 개별 삭제… |
| `src/services/trash.ts` | internal | const | `CHILD_SOURCE: ChildSourceMap` | — | — |
| `src/services/trash.ts` | internal | async function | `childRowsLocal(domain: ChildDomain, clockOf: ClockOf): Promise<ChildRow[]>` | (await CHILD_SOURCE.moment.rows()).map, CHILD_SOURCE.moment.rows, CHILD_SOURCE.moment.fromEntity, (await CHILD_SOURCE.media.rows()).map, CHILD_SOURCE.media.rows, CHILD_SOURCE.media.fromEntity, (await CHILD_SOURCE.expense.rows()).map, CHILD_SOURCE.expense.rows, CHILD_SOURCE.expense.fromEntity, (await CHILD_SOURCE.audio.rows()).map | 도메인 하나의 로컬 행을 화면 줄로 바꾼다. `switch`로 명시하는 이유(§7 2층): `CHILD_SOURCE[domain]`를 `ChildDomain` 변수로 동적 색인하면 `rows()`의 반환 타입과 `fromEntity()`의 인자 타입이 유니온으로 섞여, 실제로는 안전한 짝(같은 도메인끼리)인데도 타입이 … |
| `src/services/trash.ts` | internal | async function | `childRowsServer(domain: ChildDomain, client: JourneyClient, clockOf: ClockOf): Promise<ChildRow[] \\| null>` | fetchServerDomainEntities, rows.map, CHILD_SOURCE.moment.fromEntity, CHILD_SOURCE.media.fromEntity, CHILD_SOURCE.expense.fromEntity, CHILD_SOURCE.audio.fromEntity, CHILD_SOURCE.video.fromEntity, CHILD_SOURCE.place.fromEntity | 서버판 — 같은 `fromEntity`를 지나므로 라벨 문구가 로컬판과 절대 갈라지지 않는다. `null` = 조회 실패. |
| `src/services/trash.ts` | internal | function | `sortNewestFirst(rows: TrashedChild[]): TrashedChild[]` | rows.sort, compareInstants | — |
| `src/services/trash.ts` | export | async function | `listTrashedChildren(): Promise<TrashedChild[]>` | db, d.localTrips.toArray, trips.filter((t) => t.deletedAt !== null).map, trips.filter, homeZone, trips.map, zoneById.get, childRowsLocal, deadTrips.has, out.push | 부모가 살아 있는데 혼자 지워진 순간·사진·비용·소리 — 이 기기의 로컬 기록 기준. 최근 삭제 먼저. 부모 여행이 함께 tombstone이면 제외한다 — 여행 휴지통이 이미 보여주므로 중복이고, 자식만 복원하면 부모 없는 자식이 생긴다. 🔴 온라인이면 `listTrashedChildrenFromServer`를 우선한… |
| `src/services/trash.ts` | export | async function | `listTrashedChildrenFromServer(client: JourneyClient): Promise<TrashedChild[] \\| null>` | fetchServerDomainEntities, trips.filter((t) => t.deletedAt !== null).map, trips.filter, homeZone, trips.map, zoneById.get, childRowsServer, deadTrips.has, out.push, sortNewestFirst | 🔴 서버가 지금 아는 것을 그대로 보여준다 (2026-08-05 · 사용자 결정). "휴지통 개념을 아예 서버로 확정짓자 — 절대절대절대 기기끼리 내용이 다르면 안 됩니다." `listTrashedChildren`(로컬)은 이 기기가 그 항목을 본 적 있어야만 보여준다(불변식 #8이 일반 동기화에는 옳지만, 화면 표시… |
| `src/services/trash.ts` | export | async function | `prepareChildForAction(domain: ChildDomain, id: string, client: JourneyClient): Promise<boolean>` | ensureLocalTombstone | 서버판 목록에 있는 항목을 행동(복원·영구삭제) 하려면 먼저 이 기기에 채워야 한다. 이미 로컬에 있으면 아무 일도 하지 않는다(멱등) — `restoreTrashedChild`/`purgeChildPermanently`가 그 다음을 그대로 잇는다. |
| `src/services/trash.ts` | export | class | `PendingChildSyncError` | super | 영구삭제를 막는 사전 조건 — 여행 쪽(`PendingSyncError`)과 같은 규율 이다. |
| `src/services/trash.ts` | export | class | `PendingTrashSyncError` | super | 일괄 영구삭제에도 개별 삭제와 같은 "먼저 동기화" 경계를 적용한다. |
| `src/services/trash.ts` | export | async function | `purgeTrashedBatch(entries: readonly TrashPurgeEntry[]): Promise<{ selected: number; targets: number; targetRefs: Pick<PurgeTarget, 'id' \\| 'domain' \\| 'und…` | new Map(entries.map((entry) => [`${entry.domain}:${entry.id…, entries.map, Promise.all, unique.map, localTableOf, table.get, asPurgeParent, collectPurgeTargets, collectPurgeTargets2, families.flat | 휴지통의 선택 항목을 하나의 트랜잭션 으로 영구삭제한다. 목록을 `purge Permanently()`로 순서대로 돌리면 앞 항목은 지워지고 뒤 항목에서 막힐 수 있다. 그래서 가족 전체를 먼저 병렬로 수집하고(서로 독립적인 읽기), 모든 대기 작업을 검사한 뒤 `commitPurge` 한 번으로 표식·전파 큐·로컬 행… |
| `src/services/trash.ts` | export | async function | `purgeChildPermanently(domain: ChildDomain, id: string): Promise<void>` | db, localTableOf, table.get, asPurgeParent, collectPurgeTargets, collectPurgeTargets2, targets.map, (await d.syncQueue.toArray()).filter, d.syncQueue.toArray, ids.has | 순간·사진·비용·소리 하나를 영구삭제 한다. `purgeTripPermanently`와 같은 순서·같은 규율: ① 사전 조건 — 이 가족 에 대기 중인 큐 작업이 있으면 거부한다. 작업이 없다 = tombstone이 서버에 반영됐다는 뜻이므로 "서버에 활성 행이 남는" 갈래가 사라진다. ② 표식을 먼저 넣는다 — 중간에… |
| `src/services/trash.ts` | internal | async function | `collectPurgeTargets2(domain: ChildDomain, id: string): Promise<PurgeTarget[]>` | localTableOf(domain).get, localTableOf | 잎 도메인 한 칸 — `collectPurgeTargets`와 같은 모양으로 만든다(바이트 경로 포함). |
| `src/services/trash.ts` | export | async function | `restoreTrashedChild(domain: ChildDomain, id: string): Promise<void>` | db, restoreMediaLocalFirst, restoreExpenseLocalFirst, restoreAudio, restoreVideo, restorePlace, (await d.localMedia.where('momentId').equals(id).toArray())…, d.localMedia.where('momentId').equals(id).toArray, d.localMedia.where('momentId').equals, d.localMedia.where | 자식 하나를 복원 한다. 도메인 분기와 딸린 것 모으기를 여기서 한다 — 화면이 하면 안 된다. 왜(M-0007): `restoreMomentLocalFirst(id, mediaIds, expenseIds)`는 딸린 사진·비용 목록을 인자로 받는데, 선택적 매개변수의 기본값이 누락을 삼킨다. 실제로 여행 복원에서 비용을… |
| `src/services/trashState.ts` | internal | async function | `readServerTrash(): Promise<{ count: number \\| null; reason: string \\| null }>` | supabase, currentUser, Promise.all, listDeletedTripsFromServer, listTrashedChildrenFromServer | 서버 휴지통 건수를 읽는다. 못 읽으면 개수가 아니라 이유를 돌려준다 — 조회 실패와 「비었다」를 같은 0으로 접으면 서버에 쌓인 것을 화면이 조용히 숨긴다(§8). 여행·자식 둘 다 성공해야 개수를 낸다. 하나만 성공한 값을 쓰면 「서버 기준」이라는 말 자체가 거짓이 된다 — ADR-0049가 화면에서 전부-아니면-폴… |
| `src/services/trashState.ts` | internal | async function | `readEmptyOnRestore(): Promise<EmptyOnRestore[]>` | db, Promise.all, d.localMedia.toArray, d.localAudio.toArray, d.localVideos.toArray, out.push, r.id.slice, pick | 되살려도 사진·소리·영상이 빈 채로 오는 항목. `bytesMissing`은 추측이 아니라 확인한 사실 을 적는 표시다(M-0060의 규율) — 「바이트가 이 기기에 없음을 확인했다」는 뜻이다. 그 표시가 붙은 tombstone 행만 센다. |
| `src/services/trashState.ts` | internal | async function | `readOrphans(): Promise<OrphanTrashed[]>` | db, Promise.all, d.localMoments.toArray, d.localMedia.toArray, d.localAudio.toArray, d.localVideos.toArray, d.localExpenses.toArray, d.purgedIds.toArray, moments.map, purged.map | 되살릴 부모가 사라진 휴지통 자식. 순간을 영구삭제하면 그 순간 행만 없어지고(자식은 `trip_id`로만 묶여 있다) 휴지통에 있던 그 순간의 사진·소리·영상·비용은 돌아갈 곳을 잃는다. `checkIntegrity`는 살아 있는 행만 보므로 이 상태를 원리적으로 못 본다 — 그래서 여기서 잰다. 🔴 부모를 「없음」… |
| `src/services/trashState.ts` | export | async function | `collectTrashState(): Promise<TrashObservation>` | Promise.all, readServerTrash, readEmptyOnRestore, readOrphans, listDeletedTrips, listTrashedChildren, isConfigured | 세 관측을 한 번에 모은다. 서버 조회 실패는 예외가 아니라 값 으로 흐른다. |

## 5. 자동 검증 연결

- `tests/unit/audioSiblingDiscipline.test.ts`
- `tests/unit/diagnoseItems.test.ts`
- `tests/unit/placeSiblingDiscipline.test.ts`
- `tests/unit/purgeCascade.test.ts`
- `tests/unit/purgePropagation.test.ts`
- `tests/unit/restoreAtomicity.test.ts`
- `tests/unit/restoreUnpurge.test.ts`
- `tests/unit/serverTrashAuthority.test.ts`
- `tests/unit/strandedPurge.test.ts`
- `tests/unit/syncTombstoneVerdict.test.ts`
- `tests/unit/trash.test.ts`
- `tests/unit/trashVerdict.test.ts`

## 6. 복구 시 먼저 볼 큰 파일

- `src/services/purge.ts` — 719줄
- `src/services/trash.ts` — 466줄
- `src/domain/trashVerdict.ts` — 179줄
- `src/services/trashState.ts` — 134줄

## 7. 이 설계서가 증명하는 것과 못 하는 것

- 증명: 생성 당시 파일 모집단, 내용 해시, 최상위 선언·시그니처, 정적 import, 코드에 나타난 저장소/네트워크/파일/삭제 경계, 직접 import 테스트 연결.
- 확인 불가: 런타임 호출 순서, 동적 문자열 import, 운영 Supabase/R2의 현재 상태, 브라우저·실기기 렌더 품질, 사용자 데이터의 실제 분포.
- 함수 본문이 바뀌어 추출 표가 같아도 **내용 해시가 달라져** 재생성을 강제한다. 그러나 해시 변화가 설계 의미를 설명해 주지는 않으므로, 중요한 의도·불변식은 코드 타입·이름·JSDoc·테스트로 먼저 표현해야 한다.
